# Claude Code Configuration

This project keeps one agent-context file: [AGENTS.md](AGENTS.md). This stub exists only because
Claude Code discovers `CLAUDE.md` and never `AGENTS.md`; the `@` import below is what pulls the real
content in. Add nothing here — put it in `AGENTS.md`, which every agent tool reads.

@AGENTS.md
