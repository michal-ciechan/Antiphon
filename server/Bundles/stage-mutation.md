Run pending PCs after publication. Read the plan/design, full Code/Review reports, docs/orchestration-loop.md and docs/testing-and-build.md.

INVARIANTS: Require SourceLanding Worktree on the distinct same-board verification card, same commissioning project, confirmed operation O and L=O.VerifiedSourceSha. Before any mutant verify HEAD=L, clean tracked source/index, exact creation identity and no previous commands running. Record untracked/output inventory. Missing identity, dirty source or moved HEAD stops dependent work; never reset or substitute current master.

Use local inherited execution only. Never give snapshot access to an external executor, broker, remote service or pre-existing process. Custody must contain all descendants; terminal/root exit or worker claims are not exit proof.

Run every PC-n and separately named variant using exact methods: green baseline, specified compiling defect, intended assertion red, restore fixed bytes, refresh timestamps/rebuild, restored green. Zero tests, build/fixture errors and stale DLLs are not evidence. Record nonzero counts, expected assertion, per-variant paths and L.

Discover and name each missing PC, including zero-PC plans; justify no applicable guard. Use adequate existing assertions. Missing detection, invalid design or required production/test repairs are findings for separate caller-commissioned work, never repairs in this snapshot.

Never commit or push from the snapshot, including plan/evidence amendments. Never merge, land or deploy. Await every owned command and restore every mutation before settlement. Preserve logs, the full report and restoration.json outside the tree in the assigned evidence root. Report exact final SHA, clean tracked source/index and remaining outputs; interrupted runs name contaminated paths and pending IDs.

Carry original Code task ID, verification card, C/O/L/R and evidence through triage. Distinguish coverage survivors, reproducible unmutated-L product defects, invalid/equivalent mutants and infrastructure failures. Cleanup is a separate caller-owned CleanupVerification action requiring the irreversible task seal and native receipts for every accepted attempt; it never kills.

next: none only when all PCs/variants and discovery complete cleanly after restoration. Completed actionable findings use next: decide for caller triage and linked remediation; this is not automatically a human question. Incomplete evidence is failed/blocked with remaining IDs, never clean.
