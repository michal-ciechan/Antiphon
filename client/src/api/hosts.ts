export type HostStats = { hostId: string; displayName: string; platform: string | null; state: string; reason: string | null; observedAt: string | null; intervalSeconds: number | null; cores: number | null; current: null | { cpuPercent: number | null; load1: number | null; load5: number | null; load15: number | null; memoryUsedBytes: number | null; memoryAvailableBytes: number | null; memoryTotalBytes: number | null; swapUsedBytes: number | null; swapTotalBytes: number | null; disks: Array<{ path: string; freeBytes: number; totalBytes: number }>; processCount: number | null; processes: Array<{ name: string; sessionId: string | null; cpuPercent: number | null; workingSetBytes: number }> }; rollups: null | Record<string, { cpuPercent: { avg: number; max: number } | null; load1: { avg: number; max: number } | null; memoryUsedBytes: { avg: number; max: number } | null }>; antiphon: { tasksInFlight: number; byStage: Record<string, number>; byKind: Record<string, number>; queued: number; held: number; landsPending: number; sessionsLive: number | null; seatsDeclared: number | null; buildSlots: { occupied: number; budget: number; waiters: number; availableMb: number | null } | null } }
export type HostSeries = { hostId: string; metric: string; window: string; intervalSeconds: number; points: Array<{ t: string; v: number }> }
export const hostKeys = { stats: ['hosts', 'stats'] as const, series: (id: string, metric: string, window: string) => ['hosts', id, 'series', metric, window] as const }
export function useHostStats(): { data?: HostStats[] } { return {} }
export function useHostSeries(_id: string, _metric: string, _window: string): { data?: HostSeries } { return {} }
