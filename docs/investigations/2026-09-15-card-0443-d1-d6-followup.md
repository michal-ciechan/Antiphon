# CARD-0443 D1-D6 follow-up

Status: D1-D6 fixes pushed and scoped verification complete; ready for re-review. No land performed.

- Original landing owner: `6cb9c0d9-baae-471f-b1d1-b00ee7266af4`.
- Rejected source: `6b2189863aa140d09c17f43f4286a0bb5e3a3b5b`.
- Repair branch: `fix/card-0443-691f2941`.
- Implementation under test: `232a3bc3` (parent fix `02f4174d`). Both pushed.
- Checkout: `C:\Antiphon\worktrees\card-task-691f2941`.
- Evidence: `C:\Antiphon\repair-evidence\task-691f2941`.

## Changes

1. D1: capture-bearing Outcome payloads retain the existing identity/status envelope,
   compact equal approval coordinates, and bound optional diagnostic display text in UTF-8
   to fit 1,024 bytes. Full publication details remain on the terminal event; structured
   evidence remains in the capture. Receipt checks the original notification Body. A
   historical spilled queue pointer cannot confirm it. Busy/eligible producer tests now
   execute both modern and inbox profiles, with/without owners. Multibyte envelope tests
   retain capture identity, owner name/PID, Git/probe facts and the explicit truncation mark.
2. D2: the 32 KiB capture bound preserves the first qualifying native observation, including
   a sole tail observation. Retry nomination deserializes the freshly read committed capture
   and validates its correlation. Summary and nomination share the operation/error predicate.
3. D3: identity walks receive the same cancellation/deadline check as the DELETE probe.
   Checks precede each native open and each subsequent native metadata call; enumeration
   and attributes have the same boundary checks. Already-owned handles are disposed.
4. D4: post-retry branch completion uses the remaining additional-work token inside the
   timeout handler. Expiry preserves completed directory/registration facts, keeps the
   branch, and records the budget reason with the outer settlement token.
5. D5: add Slow to WorktreeCleanupJournalTests, WorktreeGuardedCleanupTests and
   AgentTaskWorktreeLockOutcomeTests, matching their existing allowlist entries.
6. D6: distinguish the remaining-budget timer from the single 250 ms backoff. Native-code
   selection tests control monotonic time while retaining real Git and live UTC; dedicated
   tests expire the allowance before retry and during post-retry branch work. Retry count
   assertions include the actual result and durable attempt instead of discarding them.

## D6 evidence and limits

At untouched `6b218986`, the five native-code variants executed: four passed, error 32
failed with one removal instead of two. Its original assertion did not preserve the
cleanup reason. Evidence: `rejected-source-native-codes.trx`.

At `02f4174d`, the isolated real-clock diagnostic returned a clean result: two removals,
committed error-32 capture and `retry_directory_complete`. Initial failure to second
completion was about 6.7 seconds. See `retry-reason-02f4174d\run.trx` and its full log.
This does not establish the discarded reason from the earlier run. The policy test now
separates nomination from elapsed host time and independently verifies actual deadline
refusal. No production timeout, retry count or authority assertion was widened.

The diagnostic test itself passed 1/1. Its outer wrapper initially failed while trying
to print a nonexistent empty failures.json; that reporting bug was repaired. Fresh TRX,
not that wrapper exit, establishes the one pass.

## Verification

Build `232a3bc3`: 227 warnings, zero errors (`build-232a3bc3.log`).

| Scope | Total | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|
| Unit at 232a3bc3 | 2,432 | 2,427 | 4 | 1 |
| Affected integrations at 232a3bc3 | 94 | 94 | 0 | 0 |
| Named R-1/R-2/R-3/R-4 at 232a3bc3 | 19 | 18 | 1 | 0 |
| **Final-source total** | **2,545** | **2,539** | **5** | **1** |

All 13 new Unit cases pass. C487_G060 passes. Remaining Unit failures are the four
previously recorded baseline methods: Registry_matches_compiled_metadata,
C487_G142, every_test_class_is_tagged_unit_xor_integration and C487_G068.
Three identify the unrelated HerdrPaneDisposalEndpointTests lane metadata; G142
expects an obsolete stage handoff. The one skipped case is the pre-existing
AgentTuiSecretProtectorTests.Restored_key_file_symlink_is_rejected_without_mutating_target.
All four were rerun individually at `1282f178d8763c4a92eacce553c5eda7d24169fa`:
each executed one test and failed with the exact same message (`baseline-comparison.json`).
The retained baseline source and assembly hash matched the earlier review's record before use.

The 94-case scope executed WorktreeGuardedCleanupTests (38), WorktreeCleanupJournalTests (16),
AgentTaskWorktreeLockOutcomeTests (8), WorktreeLockDiagnosticsWindowsTests (2), and
AgentTaskLandReceiptTests (30), all passing. Elapsed test-run time was 43m26.244s. This includes
the new committed-tail and pre-retry/post-retry budget regressions, real owned Windows file/root
sharing probes, all native-code choices, single backoff, and complete busy/idle delivery through
both configured transport profiles. Recipient transport is a controlled adapter; this is not a
new real-provider transport qualification.

The affected duration tripwire still flags 30 existing AgentTaskLandReceiptTests executions,
also identified by the previous review. The new CARD-0443 classes pass their Slow registry
contract. No duration threshold or assertion was waived (`affected-duration.log`).

R-1 executed the held-file case (1), interface-default cases (4), legacy authority cases
(5), and legacy-landed sweep case (1): 11/11 passed. R-2's final removal-boundary matrix
passed 6/6. R-4's settlement/release ordering case passed 1/1. R-3's cleanup-only reland
case failed with `stored.LandAttempt` expected 2, actual 1. Its fresh exact-method run at
the retained baseline executed one test and failed with the identical message
(`baseline-r3-comparison.json`). All five final-source failures are thus baseline-confirmed.
R-5 graph/census coverage is in
Unit; R-6 receipt controls are in the 94-case scope. See `final-runs.json` for exact
per-scope counts and TRX paths.

Exact invocations, fresh TRXs, per-case failures and logs are in each named results
directory. `run-scope.ps1`, `run-regressions.ps1` and `check-baseline.ps1` record repeatable
commands. Source stays frozen during runs. No intentional mutation or PC execution ran.

## Remaining scope

CARD-0536 owns D7. This pass does not claim the missing ordinary targets or post-land
PCs are complete. Re-review these six fixes before integrating them through the original
landing owner. No land, deployment or restart was performed. The implementation checkout
was clean after testing; the final tracked addition is this evidence report only.

## Output cleanup limitation

Automatic approval review rejected the proposed deletion of all 15 producer-owned
`bin-c443-fix` directories with **blocked by policy**. The entire command was rejected,
so no deletion ran. Subsequent read-only checks verified that all 15 paths are inside
the repair checkout, use the exact producer output name, and contain no reparse points
in their ancestry or contents. They remain; no alternate deletion mechanism was tried.
`cleanup.json` and `validated-output-cleanup.txt` contain the retained paths. The retained
baseline outputs belonged to the earlier pass and were not modified or claimed as ours.

All owned build/test invocations completed. No Antiphon.Tests process remained under
the repair checkout at the final census. The build-manifest hashes bind the tested
assemblies to `232a3bc3c1cbb9d58cb142a086a41c0891751c6f`.
