# CARD-0527 F8/F9 repair: Code 0d48a707

Original Code / landing owner: `2853f966-b31e-4893-9bcc-b25fb9c0a4be`.
Branch: `feat/card-task-0d48a707`; worktree: `C:\Antiphon\worktrees\card-task-0d48a707`.
Review base: `4d2a95508024253dddd647b6f9c8f71f63341da5`.
Evidence: `C:\Antiphon\evidence\code-0d48a707`.

## Contract

F8: every gated mutation has a unique `antiphon-operation` trailer. Successful commit
exit is followed by strict exact-task/operation SHA and path inspection. Failed or
ambiguous inspection throws a distinct pending-receipt exception; settlement stays
unpersisted, creates no child or completion obligation, and retries by its existing
exact settlement digest. Recovered paths are strict too. Root commits use `diff-tree
--root`. The endpoint returns 503 `commit_inspection_pending` with `committed: true`,
operation ID and any known SHA. Its authenticated read-only GET recovers that exact
operation after later HEAD movement, without another commit. The script preserves
this completed-mutation fact and prints the recovery route instead of a refusal.

F9: exact settlement recovery precedes project/global/task policy resolution.
Policy controls permission for a new mutation; it cannot erase a completed mutation.
Current residual dirt stays untouched and remains visible in the recovered receipt.

No database migration. Restart after eventual landing: **server**, runner unnecessary.
This Code task does not land or deploy.

## Ordinary verification design

Execute every V-1..V-55 and R-1..R-17 in the
[original plan](../superpowers/plans/2026-09-15-card-0527-commit-on-settle-plan.md), using
the [prior repair's coverage-to-class list](2026-09-15-card-0527-review-repair.md#ordinary-verification-design).
Use full Unit plus the named gate/policy/routing/endpoint/script classes, selected
reply methods, selected worktree and Card PR methods, outbox/receipt/attention/retention
classes, ordinary distillation, ProjectConfig, drawer/detail, and live R-17 contract.
Build Tests once into `bin-c527f8/` and E2E once into `bin-c527f8e2e/`, reusing
`--no-build`; fresh TRX per invocation. No namespace/full-assembly integration run.
Expected cost remains about 24 minutes plus builds and exact inherited-failure checks.

New rows are within those same affected classes (estimated under one additional minute):

| ID | Exact method | Variants / decisive ordinary result |
|---|---|---|
| V-56 | AgentTaskReplyIntegrationTests.C527_post_commit_inspection_failure_defers_settlement_until_complete_receipt | SHA / paths (2); one real commit, two unavailable attempts with no saved settlement/child/outbox, then exact complete parent UserPrompt and nonzero path event |
| V-57 | AgentTaskCommitEndpointTests.Post_commit_inspection_failure_returns_recoverable_success_without_recommitting | SHA / paths x existing / unborn repository (4); 503 preserves success and operation, GET twice returns original full SHA and one real file after HEAD moves, no further commit/push |
| V-58 | AgentTaskReplyIntegrationTests.C527_policy_flip_after_commit_save_failure_recovers_exact_parent_receipt | Project / global Off x clean / residual (4); completed commit survives save failure and policy flip through exact full parent UserPrompt, no child/new commit |
| V-59 | AgentTaskCommitEndpointTests.Commit_recovery_requires_the_same_authenticated_task_and_operation | 1; missing/wrong token 403, other task's operation and unknown operation 404, no mutation |
| V-60 | CommitOnSettleScriptTests.Task_commit_script_preserves_a_completed_commit_with_pending_inspection | 1; nonzero script result names completed commit and exact recovery URL, no refusal |
| V-61 | GatedCommitServiceTests.Post_commit_receipt_uses_the_operation_even_when_HEAD_advances | 1; exact operation SHA/path survives an intervening commit before first inspection |

## Pending Mutation inventory

All PC-1..PC-45 and every variant remain pending as recorded in the original plan and
prior repair. No deliberate mutants are run by Code. Additional controls are pending:

| ID | Compiling defect | Exact method / decisive assertion |
|---|---|---|
| PC-46 | Return Committed with null SHA instead of throwing after unavailable operation SHA inspection | V-56 method, SHA variant; no child/outbox until recovery, then committed receipt |
| PC-47 | Treat failed post-commit path inspection as an empty successful list | V-56 method, paths variant; first attempt must remain unpersisted |
| PC-48 | Move exact settlement recovery back after the effective Off return | V-58 method, all four project/global and residual variants; committed header and complete UserPrompt |
| PC-49 | Ignore failure of TryDiffTreePathsAsync in settlement recovery | V-56 method, paths variant; second unavailable attempt must remain unpersisted |
| PC-50 | Use current HEAD for the gated operation receipt | V-61 method; original SHA and new.txt, not later commit/path |
| PC-51 | Drop exact antiphon-task trailer validation for operation recovery | V-59 method; other task's operation must remain 404 |
| PC-52 | Remove the pending-inspection script branch | V-60 method; completed-commit text and recovery URL |
| PC-53 | Remove the recovered task/token identity comparison | V-59 method; other authenticated task must remain 403 |

Use `/*/*/<Class>/<ExactMethod>` for each red/restore/green cycle. V-57's four endpoint
variants are ordinary companion coverage for PC-46/47/50; Mutation must assess these
guards and missing-control discovery independently, including recovery unavailable,
ambiguous operation identity, and root-commit path handling.

## Evidence status

Implementation checkpoint; ordinary execution pending. This section will record actual
counts, every V/R outcome and filters after execution. Existing coverage limits remain:
controlled terminal adapters supply complete transcript receipts; native provider delivery,
long inbox spill receipts and absent CommitBaselineSha audit are not newly qualified.
