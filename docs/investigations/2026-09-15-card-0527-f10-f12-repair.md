# CARD-0527 F10/F11/F12 repair: Code b23741b2

Original Code / landing owner: `2853f966-b31e-4893-9bcc-b25fb9c0a4be`.
Branch: `feat/card-task-b23741b2`.
Worktree: `C:\Antiphon\worktrees\card-task-b23741b2`.
Reviewed subject: `29708a88ff35a5f12b943459a7b5f02f3069501c`.
Evidence root: `C:\Antiphon\evidence\code-b23741b2`.
Findings: `C:\Antiphon\evidence\review-32c1f938\review-findings.md`.

## Repairs

- F10: exact settlement trailer recovery runs before current repository/status checks.
  Unavailable repository/history inspection stays pending. A known completed commit
  with unavailable residual status stays pending, with no saved settlement, child or
  completion outbox. Healthy recovery records the original commit and paths and
  delivers the complete correlated parent UserPrompt, preserving later foreign work.
- F11: HTTP commit requires a nonempty explicit list of repository-relative file
  paths. Missing/null/empty lists and invalid entries return 422 before lease admission
  or Git calls. The internal Worktree caller retains its explicit whole-tree mode.
- F12: verbose check-ignore negation records re-include a path. They are excluded
  from ignored-path results; escaped literal leading exclamation marks remain
  exclusions. Both commit gating and child audit consume those effective results.

No migration or runtime transport change. Restart after eventual landing: **server**.
This task does not land or deploy.

## Ordinary verification design

Run all V-1..V-61 and R-1..R-17 from the
[original plan](../superpowers/plans/2026-09-15-card-0527-commit-on-settle-plan.md),
[prior repair coverage-to-class list](2026-09-15-card-0527-review-repair.md#ordinary-verification-design)
and [F8/F9 additions](2026-09-15-card-0527-f8-f9-repair.md#ordinary-verification-design).
Use full Unit, the named combined gate/policy/routing/endpoint/script classes,
selected reply/worktree/Card PR methods, outbox/receipt/attention/retention classes,
ordinary distillation, ProjectConfig, drawer/detail and live R-17 contract. No
namespace or full-assembly integration run. Estimated cost: 24 minutes plus builds,
about two minutes for the new cases, and targeted inherited-failure checks.

Build Tests once into producer-owned `bin-c527b237/`, E2E once into
`bin-c527b237e2e/`; reuse --no-build. Require fresh TRX, intended method identities
and nonzero expanded counts for every selection. Reproduce only the five inherited
.NET failure methods and one client failure at pre-Code
`b97abfd83819e2d1b705b4ed0527cfa8953b797f` in an isolated comparison checkout.

| ID | Exact method (class) | Variants and decisive evidence |
|---|---|---|
| V-62 | C527_retry_prerequisite_failure_preserves_commit_until_complete_parent_receipt (AgentTaskReplyIntegrationTests) | repository/status (2); failed save after one real commit; two actual inspection failures after exact lookup; no settlement/outbox/receipt; healthy complete receipt carries original SHA/two paths and foreign residual; later sweep does not duplicate |
| V-63 | Commit_requires_explicit_paths_before_lease_or_mutation (AgentTaskCommitEndpointTests) | omitted/null/empty list, null/blank/empty entry, parent/rooted/pathspec/directory/NUL entry (11); each runs with held and free lease, 422, zero Git verbs, byte-identical index, unchanged HEAD/files; V-33 additionally proves selected commit leaves foreign staged and working versions intact |
| V-64 | Ignore_exceptions_and_escaped_literals_follow_Git_semantics (GatedCommitServiceTests) | allowed/actual exclusion/escaped literal x scoped/whole (6); allowed commits exact file, exclusions refuse without staging and preserve index/HEAD |
| V-65 | C527_child_audit_distinguishes_ignore_exceptions_in_complete_parent_receipt (AgentTaskReplyIntegrationTests) | allowed/actual exclusion/escaped literal (3); complete immutable parent receipt, warning and incident agree on exclusion; allowed exception has no false REVERT |

## Pending SourceLanding Mutation controls

All PC-1..PC-53 and every original/prior-repair variant remain pending, including the
updated PC-23/27/30 recipes. Code runs no deliberate mutants. Mutation owns every
red/restore/green cycle and missing-control discovery. Additional controls:

| ID | Compiling defect | Exact method / decisive assertion |
|---|---|---|
| PC-54 | Return null after a failed repository inspection in TryCommitOnSettleAsync | V-62 exact method, repository arm: task must stay Dispatched with no receipt; eventual original committed receipt |
| PC-55 | Return the no-commit-attempted refusal after failed status even with a recovered commit | V-62 exact method, status arm: same pending and complete receipt assertions |
| PC-56 | Skip the endpoint's entire path validation when Paths is null or empty | V-63 exact method, omitted/null/empty arms: 422 under both lease states; no Git calls or foreign/index mutation |
| PC-57 | Remove the endpoint's per-entry Any validation, retaining only nonempty list validation | V-63 exact method, all eight invalid-entry arms: 422 before lease/mutation |
| PC-58 | Remove the leading-! continue in CheckIgnoredAsync | V-64 exact method, allowed scoped/whole arms: Committed with exact allowed.txt |
| PC-59 | Strip leading backslashes before the ! test in CheckIgnoredAsync | V-64 exact method, escaped literal scoped/whole arms: IgnoredPathStaged and no staging |
| PC-60 | Remove the leading-! continue in CheckIgnoredAsync (independent audit cycle) | V-65 exact method, allowed arm: no false REVERT/incident in complete parent receipt; actual/escaped exclusions remain companions |

Each cycle uses `/*/*/<Class>/<ExactMethod>`. Parameter variants run within that
method; report each decisive arm. PC-58/59/60 share a source method and run separately.
These additions do not qualify native provider transport: controlled terminal
adapters persist the complete submitted UserPrompt; Git, HTTP, PostgreSQL, queue and
outbox are real. Inherited limits remain: long inbox spill receipts, absent
CommitBaselineSha child audit, and ambiguous operation recovery are not newly covered.

## Evidence status

Implementation committed; ordinary execution pending. Final outcome, per-ID results,
filters, expanded counts, inherited comparison and output cleanup will be recorded here.
