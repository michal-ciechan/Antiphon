Found during CARD-0698, read-only sample 2026-09-25 07:20:29-07:23:29 UTC, deployed source 323872edbf9a91927e68b0adfcd93d6b6c7545aa.

CompletionNoteWorkHostedService.ScanAsync waits one second after each pass (server/Infrastructure/Orchestration/CompletionNoteWorkHostedService.cs:51), then RecoverMissingSourcedCompletionNotesAsync selects all terminal sourced tasks with Result and ParentSessionId, without a CompletionNoteQueuedAt exclusion or bounded page (:66-76). RepairFromAsync projects existing queue notes and issues ApplyAsync once per distinct task (server/Application/Services/CompletionNoteStamp.cs:34-48), even when the task already has its stamp and the conditional UPDATE affects zero rows. The scanner then calls HasCompletionNoteAsync for every historical candidate (:89-91), making up to three existence queries per task (server/Application/Services/AgentTaskCheckService.cs:291-315). The durable stamp prevents duplicate notes but does not retire a candidate from this scanner's work.

Stored pg_stat_activity evidence observed at least 28 matching land-notification existence calls in three minutes (9.34/min), 25 completed observations averaging 7.44ms, total 186.07ms; 22 conditional stamp UPDATE calls (7.34/min), 21 completed observations averaging 0.161ms. These are sparse sampling lower bounds, not exact execution counts. AgentTaskLandNotifications accumulated 1,052 sequential scans and 1.40M tuple reads across the 180.881s window; those table counters include other callers and cannot all be assigned to this scanner. Transcript queries remain CARD-0698's dominant measured cost; this is a separate historical-work amplification defect.

Evidence: docs/investigations/2026-09-25-card-0698-postgres-load/{activity,before,after,summary}.json, query fingerprints c8af6330c2b1 and 7a16da20c108. Investigation only; no fix or test design performed. Preserve completion-note recovery and durable idempotency semantics in subsequent planning.
