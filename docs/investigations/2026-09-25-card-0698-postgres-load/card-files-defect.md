Found during CARD-0698, read-only sample 2026-09-25 07:20:29-07:23:29 UTC, deployed source 323872edbf9a91927e68b0adfcd93d6b6c7545aa.

CardTaskFileService.SyncAllAsync selects every board and invokes SyncBoardAsync for each (server/Application/Services/CardTaskFileService.cs:43-53). Each InspectBoardAsync queries all other boards across projects, then invokes UniqueBoardSlugAsync for every unpinned board (server/Application/Services/CardTaskFilePolicy.cs:203-209). UniqueBoardSlugAsync repeats the sibling-board SELECT (: server/Application/Services/CardTaskFileService.cs:197-206). The publication eligibility check occurs only after this loop (CardTaskFilePolicy.cs:213). GetCardStatusAsync also runs the same inspection (:145-151), so a read of an opted-out card still takes this path. The global loop composes with the per-board loop; with B unpinned boards it can perform order-B-squared sibling reads per complete sweep. This is work amplification, not evidence that the 60s timer is firing early.

pg_stat_activity captured 224 distinct sibling SELECT executions in three minutes (at least 74.69/min); all were observed completed, averaging 0.0585ms, total 13.103ms. The Boards table registered 9,099 sequential scans in 180.881s (3,018.3/min), reading 877,915 tuples. Table scans are not SQL call counts and other Boards queries contribute. This query had zero active-time hits at 2s sampling and is not the principal Postgres CPU consumer. The repeated database/network/Git inspection work is an independent structural defect.

Evidence: docs/investigations/2026-09-25-card-0698-postgres-load/{activity,before,after,summary}.json; query fingerprint b2f9a5cacf69. CARD-0698's card read also returned syncCardFiles=false and reason=board_not_opted_in. No fix or test design performed; publication privacy and cleanup visibility remain required semantics for subsequent planning.
