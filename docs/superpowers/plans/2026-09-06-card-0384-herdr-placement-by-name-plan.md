# CARD-0384: Herdr placement by workspace and tab name

Plan date: 2026-09-06. Ground truth: `73614e3b193ef02bcf51c08bb236772473e0ff60`.
Board card: CARD-0384 / GitHub #39. Related incident: CARD-0388 / GitHub #44.

Implement an optional, agent-owned name pin ahead of last-pane resolution. A configured tab is a dedicated single-pane launch target: find it, create it when absent, launch into an idle PowerShell shell, or adopt an exactly identified occupant. Refuse foreign occupancy without moving elsewhere. Keep the existing placement path for agents without a tab pin. This is ready for TestDesign; no implementation or live-agent changes were made by this Plan stage.

## Ground truth

Paths and line numbers below describe the inspected commit, not proposed code.

| Card assumption / relevant fact | Actual implementation and implication |
|---|---|
| Last-pane is keyed by session ID and disappears when its pane disappears. | `src/Antiphon.SessionRunner/HerdrLastPane.cs:14` stores workspace/tab/pane IDs, origin and env **names**; `HerdrPaneChild.cs:343-428` tries the current session then `ReusePaneOfSessionId`, deletes a mismatched-workspace or missing-pane record, and returns `Allocate`. A live foreign occupant instead throws `pane_occupied` and retains the record. |
| The allocator finds a slot, not a tab label. | `HerdrPaneChild.cs:1124-1166` verifies live sidecar panes against `pane.list`, then calls `HerdrPaneAllocator.Allocate`; `HerdrPaneAllocator.cs:29-66` groups by tab and fills a group with fewer than four panes. **Tabs are already created with `opts.PaneTitle`**, not necessarily untitled (`HerdrPaneChild.cs:1158-1159`). The missing operation is label lookup. Also, runtime currently supplies `TabNumber: 0` for every census row (`SessionRunnerRuntime.cs:321-335`), so production ties break by tab ID, despite the documented lowest-numbered policy. Do not fix that separate discrepancy here. |
| Workspace reuse already exists. | `HerdrPaneChild.cs:1047-1100` selects an exact `antiphon-ws` token first, otherwise exactly one **ordinal, case-sensitive**, untagged label match, otherwise creates a managed workspace. Creation returns a root tab/pane. Reused operator workspaces are never stamped. `docs/herdr-sessions.md:132-150` describes this contract. |
| Agent/project placement settings exist. | They do not. `HerdrLaunchContextResolver.cs:37-94` derives project context from card board, agent board, then pool project, or returns `none` / `Antiphon`. `SessionRunnerContracts.cs:36-59` has `WorkspaceKey`, `WorkspaceLabel`, `WorkspaceCwd`, `PaneTitle`, kind, slug and previous-session hint, but no tab label. |
| Start, AlwaysOn and fresh fallback need identical placement. | `AgentSupervisorService.cs:190-207` calls `AgentControlService.StartAsync`, selecting fresh after repeated failures. `AgentControlService.cs:316-349` queues same-row resume; `:353-441` creates a fresh row and captures the previous ID before replacing `PersistentSessionId`. `AgentSessionService.cs:1451-1497` reconstructs Herdr context centrally for every launch. `:349-358` also handles missing-resume-target fallback with a **fresh conversation on the same row ID**. Both fresh shapes must carry the pin. |
| Attach can launch into an idle shell. | It cannot, and must not be changed to do so. `AgentControlService.cs:476-638` is inspect, persist row, then bind; runner `HerdrPaneChild.cs:106-175` requires detected kind, one expected-family foreground process, expected PID/native identity, and Grok transcript availability. Empty/no-agent is currently `pane_unoccupied` at the runner; the server also has its own checks. Attached origin detaches on stop (`:787-793`), and is excluded from allocator census (`SessionRunnerRuntime.cs:328-329`). |
| The current target-resolution description can simply gain a label fallback. | `docs/herdr-sessions.md:213-225` says last-pane comes before allocator and restricts idle-shell typing to `Origin = launched`. Named placement needs a **separate, higher-priority branch**, because an operator's explicitly named idle shell has no launched-origin history and an old last-pane may point at MavRef-DL. |
| Every failed detection closes the pane. | Already corrected in this checkout by CARD-0383: `HerdrPaneChild.cs:603-608,997-1017` retains an idle shell at detection timeout, writes last-pane, and sets `_keepPaneOnKill`; `KillAsync:795-801` honors it. This improves retry stability but cannot discover/create `Orch`. |
| Relaunch already sets cwd in a reused shell. | It does not generally do so: `CompleteTypedLaunchAsync:579` passes cwd only for the newly created workspace root. An existing operator shell needs an explicit quoted `Set-Location -LiteralPath request.Cwd`, as well as the existing env application/removal. |
| Returning `pane_occupied` automatically makes public Start return 409. | Runner `POST /sessions` maps launch exceptions through `HerdrProblemMapper` (`Program.cs:175-191`, `HerdrProblemMapper.cs:12-16`); `SessionRunnerHttpClient.cs:81-87` preserves the typed problem. But public Start **queues** work (`AgentControlService.cs:238-241,346-349,440-442`) and later failure is stored by `AgentSessionService.cs:361-382`. Add a read-only preflight for the public 409; retain the authoritative launch-time check for races. |
| A dedicated named tab stays dedicated automatically. | Not unless reserved tabs leave the allocator census: today every live launched-origin sidecar is eligible (`SessionRunnerRuntime.cs:321-335`). Another ordinary agent could split the new Orch pane and make its next launch fail the single-pane rule. |

### Actual launch and attach call chains

Standing start / supervision -> `AgentControlService.StartAsync` -> `StartInteractiveSessionAsync` -> `AgentSessionLaunchQueue.EnqueueInteractiveSession` -> `AgentSessionService.LaunchInteractiveAsync` / `LaunchInteractiveProcessAsync` -> central launch-spec enrichment (`AgentSessionService.cs:1451`) -> runner adapter / `RunnerTerminalSession.StartAsync` -> `SessionRunnerHttpClient.StartAsync` -> runner `POST /sessions` -> `SessionRunnerRuntime.StartAsync:94` -> `RunnerSession.StartHerdrAsync:1118` -> `HerdrPaneChild.LaunchAsync:261`.

Today the child does guards -> connect -> ensure workspace -> resolve last-pane -> adopt, relaunch, consume newly created root, or allocate -> complete typed launch. `StartHerdrAsync:1169-1177` calls `KillAsync` on any failure. Therefore merely assigning `_paneId` during inspection can make refusal cleanup close a pane the launch never acquired.

Attach is a separate path: `AgentControlService.AttachHerdrAsync` -> runner inspect HTTP -> runner `/sessions/attach` -> `SessionRunnerRuntime.AttachHerdrAsync:205` -> `HerdrPaneChild.AttachAsync`. Runner `FindBoundPane:280-315` checks live/pending sessions, active sidecars and historical last-pane records. Reuse its evidence carefully; a retired previous session is allowed for fresh placement, but a still-live previous session is not.

### Herdr wire evidence

Read-only local command `herdr --version` reported **0.8.2**; `herdr api schema --json` reported protocol **20**, schema **1**. Its request union contains `method: tab.list` with `TabListParams.workspace_id`; success `ResponseResult` contains `{ type: "tab_list", tabs: TabInfo[] }`. `TabInfo` includes `label`, `workspace_id`, `tab_id`, `pane_count`, and `number`. `TabCreateParams` already accepts `workspace_id`, `label`, `cwd`, `env`, and `focus` (default false). No live pane was inspected or mutated to obtain this schema.

`HerdrClient.cs:162-175` already wraps `tab.create`; `HerdrApiModels.cs:24-31` already models `TabInfo`. Add only the missing typed list wrapper/params/envelope and fake route. Do not infer tab labels from pane labels, agent names, terminal titles or sidecars.

## Decisions

### D-1: Persist optional names on Agent; leave IDs runner-owned

Add nullable `Agent.HerdrWorkspaceLabel` and `Agent.HerdrTabLabel`, mapped to nullable text columns with a 256-character application/EF limit. Add the corresponding camelCase create/update/detail/list DTO fields and client types. Generate the migration with EF CLI; no pane, tab or workspace ID columns and no data backfill. Per-project overrides are unnecessary for v1; agent ownership is explicitly permitted by the card and allows two standing agents to choose different tabs.

Create: missing/null/blank means default. Update: missing/null leaves the field unchanged; an empty/whitespace string explicitly clears it, following existing optional-update conventions. Trim surrounding whitespace, preserve case for display, reject control characters or labels over the limit with 422. Settings take effect on the next launch; saving or calling Start on an already live agent does not move it. Retain settings when switching backend; ignore them while using PtyHost.

Apply the name pin to a standing, cardless, non-pool agent (`session.CardId == null`, owning agent exists, `!IsPoolDelegate`). Card-spawn and pool placement remain their current project/allocator path. Labels may be configured before starting the standing seat; they are not derived automatically from its name, slug, current tab or attached pane.

Settings UI: two optional text inputs in Herdr mode on `AgentCreateModal` and `AgentSettingsModal`: **Workspace label** (placeholder: project name) and **Tab label** (placeholder: automatic placement). Explain that the tab is dedicated to one pane and changes apply on next launch. Clearing sends the explicit empty-string update. Pane display name and Herdr agent slug remain independent of the configured tab name.

### D-2: Keep the workspace contract; make tab label an independent launch field

Keep `WorkspaceKey = project:<guid>` / `none` and project `WorkspaceCwd` unchanged. Resolve existing defaults first, then apply the eligible owning agent's nonblank workspace override to `WorkspaceLabel`; carry its tab override in a new optional trailing `HerdrLaunchOptions.TabLabel = null`.

Workspace selection remains token first, then one untagged exact ordinal label, then creation. **A matching token still wins even if its workspace has been renamed or the override names another workspace.** The workspace override changes the fallback/create label, not established token identity. This is the brief's permitted existing workspace half; do not promise force relocation or consolidate duplicate workspaces. Null tab label takes the old last-pane path, including its existing WorkspaceKey check, even when only a workspace-label override is supplied.

Tab matching is exact `OrdinalIgnoreCase` on Windows and `Ordinal` elsewhere; no substring, fuzzy, cwd, title or agent-slug matching. More than one match is 409 `herdr_tab_ambiguous`, listing candidate IDs/labels, with no creation or allocation. An existing matching tab must have exactly one enumerated pane in that tab/workspace, agreeing with its reported count; otherwise 409 `herdr_tab_invalid`. Do not choose a root/focused pane from a multi-pane tab: the card explicitly scopes that out.

Refactor resolver early returns so every eligible standing path receives the overrides. The existing central spec builder remains the authority for actual queued launches, including missing-resume-target fallback. Preserve `ReusePaneOfSessionId` through `with` updates rather than rebuilding a partial options record that can drop new fields.

### D-3: Named resolution outranks every last-pane hint

After existing kind/backend/provider/Grok launch guards and Herdr protocol validation:

1. Ensure the workspace using D-2.
2. If `TabLabel` is null, execute the existing last-pane -> new-root -> allocator flow. Do not send `tab.list` on this path just to reproduce its current behavior.
3. Otherwise list tabs **in the resolved workspace**, validate their workspace identity, and select by D-2. Last-pane cannot choose another tab or workspace and cannot rescue an occupied/invalid named target.
4. No matching tab in an existing workspace: call `tab.create(workspace_id, label: TabLabel, cwd: request.Cwd, env: request.Env)` once. Use its returned root pane, with no split and no additional rename to `PaneTitle`.
5. If this launch just created the workspace, consume its returned root tab/pane and rename that **new** tab to `TabLabel`. This retains CARD-0323's no-empty-root invariant instead of creating a redundant second tab merely to satisfy the literal operation name in the card. The resulting workspace still has exactly one correctly named tab and one pane. Never consume a reused operator workspace's unrelated root.
6. Existing single-pane target: re-read `pane.get` and `pane.process_info`; require returned IDs still belong to the selected workspace/tab. Check pending/live Antiphon claims. Classify using the foreground process list, not the screen or `AgentStatus`.
7. Zero non-shell foreground processes plus a positively identified live PowerShell shell: acquire it for a typed launch. This permission comes from the explicit tab pin, so a missing or attached-origin historical record does not force allocation. Unknown shell/PID or a non-PowerShell shell is 409 `pane_occupied`, with no typing.
8. Exactly one live foreground process, matching `pane.Agent` kind and expected process family, whose parsed argv names **request.SessionId**: adopt in place, bind PID/start evidence and existing transcript machinery, type no launch script. Reuse `TryReadNativeSessionId`; metadata alone is insufficient. A live previous ID is foreign on a fresh-row launch. Codex currently supplies no native session ID in argv, so an occupied Codex pane refuses; an idle named shell can launch Codex normally.
9. Any other occupant/claim is 409 `pane_occupied`. Retain both name settings and all relevant last-pane records, touch no occupant, and never fall through to allocator, attach, another backend or another tab.

Errors reading Herdr are errors, not absence. Never emulate `AllocatePaneAsync`'s `pane.list` failure-as-empty behavior in the named branch. If a selected tab/pane disappears or moves before acquisition, return 409 `pane_changed`; do not switch targets mid-attempt. A later explicit start or supervision retry resolves names again and can recreate an absent tab. This bounds races without retry loops that create extra furniture.

### D-4: Preserve launch safety, cwd, environment and origin

Factor shared foreground classification out of current last-pane logic where helpful, but preserve the unpinned branch's decisions. The named branch can call the existing typed-launch and adopt completions only after acquiring its target. Do not set `_paneId`, rename a pane, report pane metadata or write a script during read-only resolution/preflight.

Track acquisition and the launch's positively identified shell/child before entering any cleanup-capable code. Existing `KillAsync:807-808` treats a null sidecar as having no unexpected-process evidence; that is unsafe for an existing operator shell if it changes occupant between inspection and launch. For named launches, cleanup must never close/kill an unexpected foreground process when no sidecar exists. Keep the shell and pin, remove only this attempt's transient claim, and preserve the original refusal/error. Use the same ownership guard if a target disappears, moves or gains an occupant before typing. On adopted-process setup failure, no unproven PID is killed.

Recheck foreground and membership immediately before typing/adopting. Herdr has no atomic compare-and-launch RPC; this reduces, but cannot eliminate, a human action in the final RPC gap. Do not claim atomicity against external clients. The runner coordination in D-5 eliminates competing Antiphon launches, and cleanup still refuses unexpected occupants.

For a named idle shell, pass `request.Cwd` to the existing script builder every time. Keep UTF-8 BOM, properly quoted `Set-Location -LiteralPath`, env assignment/redaction, one typed script invocation, and separate Enter. Do not change the prompt-delivery contract or call `agent.start`/`agent.prompt`.

Last-pane may contribute stale `LaunchEnvNames` **only when its actual pane/tab/workspace IDs match this selected target** (current or retired previous session). Ignore mismatched historical records until success, rather than deleting them during named resolution. With no matching record, do not clear arbitrary environment variables in the operator shell. Success writes the current launch's env names and deletes consumed current/previous hints as today; a refusal leaves the records untouched. Detection-timeout idle-shell retention from CARD-0383 still applies.

Typed named launches and named adoption use managed `origin=launched`: explicitly assigning the named single pane makes this the agent's managed launch seat, including ordinary Stop behavior. `attach-herdr` remains `origin=attached`, retains detach-on-stop behavior, does not infer/write a pin, and never starts an idle shell. Existing running attached sessions are not converted by a settings edit or Layer-A runner adoption.

### D-5: Reserve named tabs and serialize target acquisition

Add optional `WorkspaceLabel` and `TabLabel` snapshots to runner sidecar/last-pane JSON. These are diagnostic/placement context and a reservation marker; the Agent fields are the durable pin. Old JSON without them remains valid. Copy them through `WriteSidecar`, `HerdrLastPane.FromSidecar` and `FromLaunchRequest`. Pruning sidecars/hints never clears the Agent pin. Layer-A adoption restores the marker from sidecar without DB access and continues its current process/transcript identity checks.

Exclude the **whole tab** containing any live named-launch sidecar from `CollectLiveAntiphonPanes`, not just that one pane. Preserve attached exclusions. Do not make the allocator read `last-pane/`, infer reservations from labels alone, or modify its geometry/order. An idle named shell with no live Antiphon sidecar is already not allocator capacity. This is the only change to unpinned agents' candidate set: configured dedicated tabs are reserved, while all formerly eligible unpinned tabs retain current behavior. Clearing a pin affects the next launched session's reservation, not a running session's snapshot.

Add runner-owned, instance-scoped placement coordination (a concrete service, no static state): serialize workspace find/create by WorkspaceKey, then serialize the short target select/claim phase by resolved workspace ID. Claims cover tab and pane IDs and survive until a sidecar is published or the attempt fails. Named preflight, named launches and attach acquisition consult these claims; a second session cannot type into a pending first launch. Distinct target launches may wait for detection concurrently after acquisition. The ordinary allocator excludes pending dedicated claims as well as committed reservations. Do not hold a global semaphore across the up-to-60-second detection wait.

Use live/pending bindings and active sidecars as refusal evidence. Historical last-pane bindings permit the current ID and the explicitly carried retired `ReusePaneOfSessionId`; another session's historical claim refuses rather than being silently stolen. Do not exempt a still-live previous session simply because its ID is in that hint. Release transient claims in `finally` on cancellation and every failure, retaining durable names/hints. Recheck under the shared acquisition coordination before any bind/type operation. Do not add process kills to supervision to resolve contention.

### D-6: Honor HTTP refusal without making every Start synchronous

Add runner `POST /herdr/placement/check` accepting a small `HerdrPlacementCheckRequest(SessionId, HerdrLaunchOptions)` with no executable/env values. It uses the same read-only workspace selection, named-tab match and occupant classifier. It never creates a workspace/tab, refreshes metadata, deletes last-pane, reserves a pane, writes a sidecar, or types. Missing workspace/tab returns an allowed result with action `create`; idle/adoptable targets return `relaunch`/`adopt`; occupancy/ambiguity/shape changes use the same typed 409s as launch. No IDs from this response become a persisted pin or authoritative queued target.

Before mutating/enqueueing the proposed interactive session row, choose its ID (existing resume ID or a new GUID), resolve its effective name context and call this check for named Herdr placement only. Preserve the existing idempotent already-live Start and existing supervision-latch semantics. On preflight refusal, public `POST /api/agents/{id}/start` surfaces `ConflictException(Code="pane_occupied")` via existing middleware, preserving `PersistentSessionId`, queued-message associations, settings and last-pane. Supervisor uses this same Start path and records failure/backoff through its existing catch. No new alert sink or retry threshold is part of this card.

Launch always re-resolves and rechecks independently. An occupant arriving **after** an allowed preflight is a runner 409 and an asynchronous Failed session with the runner detail in `FailureReason`; the already returned public HTTP response cannot retroactively become 409. State this distinction in docs/tests. Never translate `pane_occupied` into `ResumeTargetMissingException`, so it cannot trigger conversation fallback or allocator fallback.

Add optional `HerdrNamedTabPlacement` to runner capabilities, advertised only when Herdr is enabled and this implementation exists. The server requires true for named preflight **and** actual named launch. Otherwise report the existing runner-capability mismatch with upgrade guidance. This prevents a new server sending an unknown `TabLabel` to an older runner that silently uses the allocator. Old server -> new runner and null tab -> either supported Herdr runner remain compatible. The check route alone is not a capability guarantee for queued launch.

### D-7: #44 is only partially resolved

Once shipped and explicitly configured on **PM-Orchestrator-Grok** (`2ee02f40-7b6d-48b1-96fc-c4344c651910`) with `herdrWorkspaceLabel="PredictionMarkets"`, `herdrTabLabel="Orch"`, this design fixes the reported missing-Orch -> MavRef-DL placement, subject to the retained workspace-token precedence. It recreates Orch after loss and refuses foreign occupancy instead of filling a specialist tab. No migration automatically configures that agent.

It does **not** establish or fix why the Grok child died (`HerdrChildGone`, detect-none, `HerdrPaneClosed`), Windows multiline `--rules` handling linked to GitHub #41, or which of the two orchestrators owns Slack. CARD-0383 already supplies a missing-native-session guard and idle-timeout retention in the inspected base; do not reimplement it. #44 should reference this plan for placement, configure/verify the pin after landing, then investigate remaining process/argv evidence and the twin PtyHost agent separately. Do not close #44 solely because a pane now lands on Orch; sustained running and transcript-backed delivery still need evidence there.

### Rejected alternatives

- Adding a label fallback after last-pane: a valid old specialist-tab ID would still win.
- Implementing placement in attach-herdr: it changes detach/ownership semantics and still cannot create a missing tab safely through the inspect/bind contract.
- Persisting IDs or relying on metadata TTL for the pin: layout loss/TTL expiry would erase intent; runner startup also cannot consult the server DB.
- Reusing the first same-kind process, a previous fresh-row ID, or `agent_session` metadata alone: insufficient session identity.
- Selecting a root/focused pane in a multi-pane tab or splitting another tab: outside v1 and contradicts dedicated placement.
- Correcting allocator tab ordering, consolidating workspaces, global supervision retry changes, or repairing Grok argv here: separate work, unnecessary for label resolution.

## Replacement target-resolution documentation

During Code, replace the table under `docs/herdr-sessions.md` section 4 (current lines 213-225) and update sections 1/3/3a to distinguish explicit named authority from attached-origin history. Include the settings/PATCH-clear contract, token precedence, allocator reservations, capability requirement and public preflight/queued-race distinction.

| Priority / target state | Resolution |
|---|---|
| Existing launch guards fail or Herdr cannot be verified | Explicit refusal/error; no alternative backend or placement. |
| Resolve workspace | Matching `antiphon-ws` token; else unique untagged exact workspace label; else create managed workspace. Workspace override supplies the fallback/create label. |
| Tab label configured, no matching tab | Create one labelled tab at agent cwd, with one pane; if workspace was just created, name/use its returned root instead. Never allocator/split. |
| Tab label configured, exactly one single-pane match, idle PowerShell | Launch script in that pane, at agent cwd, applying current env and clearing only proven stale launch-env names. No new tab or split. |
| Tab label configured, exactly one single-pane match, one live expected-kind process with this session ID in argv | Adopt that process; type no launch script. |
| Tab label configured, foreign/unidentifiable/wrong-kind occupant or another session's claim | 409 `pane_occupied`; retain names and last-pane; never steal/fallback. |
| Tab label configured, duplicate label or tab not exactly one pane | 409 `herdr_tab_ambiguous` / `herdr_tab_invalid`; no mutation/fallback. |
| Selected named target disappears/moves before acquisition | 409 `pane_changed`; later attempt resolves names again. |
| No tab label, no valid last-pane | Existing new-workspace-root / allocator path; dedicated named tabs excluded from capacity. |
| No tab label, launched-origin idle PowerShell last-pane | Existing in-place relaunch. Attached-origin empty history does not authorize typing. |
| No tab label, matching live last-pane occupant | Existing exact-argv-ID/kind adoption. |
| No tab label, occupied last-pane | Existing 409 `pane_occupied`, record retained, no allocator fallback. |
| Typed launch detect timeout, target still only its verified shell | Preserve pane and write last-pane as CARD-0383 does; name pin survives regardless of pane loss. |

## Implementation slices

All slices belong to Code after TestDesign. Keep changes additive and avoid moving unrelated launch behavior.

| Slice | Files / concrete work | Tests to add or extend |
|---|---|---|
| S1: Agent settings and central context | `server/Domain/Entities/Agent.cs`; `server/Infrastructure/Data/AppDbContext.cs`; CLI-generated `server/Migrations/*`; `server/Application/Dtos/AgentDtos.cs`; `server/Application/Services/AgentService.cs`; `HerdrLaunchContextResolver.cs`; `AgentSessionService.cs`; `AgentControlService.cs`; `src/Antiphon.SessionRunner.Contracts/SessionRunnerContracts.cs`. Add fields, validation, API mappings and optional wire field; preserve overrides/previous-ID hint on all standing launch variants. | New `HerdrPlacementSettingsTests` for round-trip/clear/backward compatibility/scoping; extend `HerdrLaunchContextResolverTests` beyond its current four title-only tests; extend `SessionRunnerHttpClientHerdrWireTests` for new/absent TabLabel. |
| S2: Typed listing and pure selection | `src/Antiphon.SessionRunner/HerdrApiModels.cs`, `HerdrClient.cs`; new concrete `HerdrNamedTabResolver.cs` with shared read-only workspace/tab selection and occupant decisions; `tests/Antiphon.SessionRunner.Tests/FakeHerdrServer.cs`. Add tab.list envelope and meaningful fake tab labels/counts/move/delete/race controls. Keep mutation in acquisition/child code. | `HerdrClientTests`, new `HerdrNamedTabPlacementTests`: wire schema, exact/case match, workspace scoping, duplicate/invalid tab, RPC failure never treated as absent. |
| S3: Resolution, ownership and reservations | `HerdrPaneChild.cs`, `SessionRunnerRuntime.cs`, `HerdrPaneSidecar.cs`, `HerdrLastPane.cs`; new instance-owned `HerdrPlacementCoordinator.cs`, registered/injected by runner composition. Implement D-3 through D-5; root label differs from pane title; cwd on named reuse; guard failed-launch cleanup before sidecar; preserve hint on refusal; exclude dedicated tabs from census. Existing script builder can be reused, with a small signature/name refactor if needed. | New placement tests through runtime, plus `HerdrLaunchShapeTests`, `HerdrPaneChildKillTests`, `HerdrAttachTests`, `HerdrPaneSidecarTests`, `HerdrAdoptionSweepTests`, `HerdrPaneAllocatorTests`. Include interleaved named launches and attach, stale prior IDs, borrowed shell cleanup and dedicated-tab exclusion. |
| S4: Public preflight and capability refusal | `SessionRunnerContracts.cs`; runner `Program.cs`, runtime and `HerdrProblemMapper.cs`; `server/Application/Interfaces/ISessionRunnerClient.cs`; `server/Infrastructure/Agents/SessionRunner/SessionRunnerHttpClient.cs`; `AgentControlService.cs`. Read-only check before interactive-row/enqueue mutations, plus launch recheck/capability gate; update affected fake clients deliberately. | New `HerdrPlacementPreflightTests`; extend `AgentControlServiceIntegrationTests`, `AgentSupervisionTests`, `HerdrAlwaysOnChannelParityTests`, HTTP wire tests. Pin public 409 before queue versus race-time runner 409/Failed row; old runner refuses without posting named launch. |
| S5: Configuration UI | `client/src/api/agents.ts`, `client/src/features/agents/AgentCreateModal.tsx`, `AgentSettingsModal.tsx`; reuse text inputs and existing mutation hooks. | New `AgentHerdrPlacement.test.tsx`: create/settings round-trip, clear, backend visibility, labels kept on backend switch, save does not restart/move a live session. |
| S6: Owner docs and completion evidence | `docs/herdr-sessions.md` replacement table above; `docs/ops-http.md` and `docs/antiphon-api.md` document preflight/public error timing and runner route. Report acceptance results and #44 boundary. | Execute TestDesign's concrete tests and scoped regressions; verify optional isolated real-Herdr probe evidence if TestDesign calls for it. No production agent reconfiguration in Code tests. |

S1/S2 precede S3; S4 uses the shared resolver and S1 contracts; S5 uses S1 API fields. Do not release a partial combination that accepts `herdrTabLabel` but silently launches through an incapable runner. Land/migrate/restart through the repository's normal orchestration and main-checkout procedures, not from this plan worktree.

## Acceptance and TestDesign handoff

The brief does not fold TestDesign into Plan. The next stage must turn these observable requirements into named V/R/PC cases, including red-then-green discriminators. The following are required outcomes, not a claim that tests were run.

| Card acceptance | Required observation |
|---|---|
| Named standing start / AlwaysOn restart / fresh fallback lands on Orch in ExampleProject, creating it if missing. | Exercise first launch, same-ID restart, manual fresh/new ID, supervisor fresh threshold, and missing-native-target same-ID fallback. Put an eligible unpinned specialist tab and an old last-pane pointing there in the fixture. Assert selected workspace/tab labels and sidecar IDs, no split, and the correct tab/root creation count. Delete target pane/tab and repeat. |
| Existing idle labelled tab: no new tab or split elsewhere. | Seed an **operator-created** single-pane Orch with no sidecar and a different cwd. Assert same tab/pane IDs, zero tab.create/pane.split/tab.rename, quoted cwd/env launch script, and correct pane title independent of Orch. Repeat with attached-origin historical evidence. |
| Foreign process -> 409 pane_occupied, pin/hint retained, no allocator fallback. | Wrong kind, different ID, no argv ID, multiple processes, occupied Codex, live previous ID, bound/pending claim and non-PowerShell shell. Assert HTTP status **and code**, no pane close/kill/send/rename/metadata side effects, unchanged current/previous hint bytes and DB labels. Free the pane and retry successfully on the same named tab. Cover public preflight and a post-preflight race separately. |
| No tab label -> unchanged behavior. | Existing CARD-0224 last-pane/reuse/occupied tests and CARD-0323 workspace/root tests remain green. Null/absent wire field produces prior decisions/RPCs. Verify allocator still fills unpinned tabs and cannot split a live dedicated named tab, including after runner adoption. |
| Documentation and persistence. | Settings persist across session IDs/pane loss; null/empty update semantics are explicit; no DB ID columns; old sidecars deserialize; target-resolution documentation agrees with runtime tests. |

Additional discriminators: tab case and workspace-token precedence; duplicate and multi-pane refusal; pane-count/ID disagreement; tab-list error; concurrent duplicate launches; live claim versus historical previous hint; pre-sidecar cleanup when a foreign process appears; cancellation releases only transient claims; detect-timeout keeps names/hints; successful adoption types nothing; old runner feature capability refuses before wrong placement. Avoid tests that assert only that a helper returned an enum: drive the runtime/fake RPC boundary for mutation and cleanup assertions.

Use existing `FakeHerdrServer`, fake process liveness, unique temp session-log directories and class-scoped TUnit runs. Server integration fixtures must use the established production-runner guard or an isolated runner, never 17204. Process-spawning tests need the assembly-local `ParallelLimiter<ProcessSpawnLimit>`; global supervisor sweeps follow existing ungrouped `NotInParallel` isolation and assertions scoped to seeded rows. Use an advancing test clock for queue-dependent paths. Run `Antiphon.Tests` and `Antiphon.Agents.Pty.Tests` sequentially if both are needed.

Command pattern for Code (replace ClassName with the concrete touched class, one scoped run at a time):

```powershell
dotnet run --project tests/Antiphon.SessionRunner.Tests --property:OutputPath=bin-c384/ -- --treenode-filter "/*/*/ClassName/*"
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c384/ -- --treenode-filter "/*/*/ClassName/*"
pwsh -File scripts/test-client.ps1 AgentHerdrPlacement.test
```

No `dotnet test`, no production session starts/stops, no full-suite expansion merely for confidence. If a real-Herdr smoke is required to validate shell typing, use an isolated named Herdr session and disposable cwd; confirm launch identity from argv and transcript as applicable, not a screen redraw. TestDesign should specify exact setup, owned-process cleanup and pass criteria before Code runs it.

## Plan-stage validation and remaining work

Read the full CARD-0384 and CARD-0388 via `scripts/card.ps1 get`, traced the production launch/attach paths, and read the installed Herdr protocol schema. No builds or tests ran; there is no runtime implementation in this artifact. All decisions above are implementation defaults with reasons; no caller decision blocks TestDesign. The next stage should especially challenge the public 409 timing, named-pane ownership/cleanup, concurrency claims and the dedicated-tab census boundary before Code.

## Verification design

TestDesign date: 2026-09-06, against plan commit `d0e46d09` on ground truth `73614e3b`. This section adds verification only; the fix design above is unchanged. Layers: **unit** = pure class, no I/O; **integration** = `SessionRunnerRuntime` + `FakeHerdrServer` in `Antiphon.SessionRunner.Tests`, or Postgres-backed service harness in `Antiphon.Tests`; **E2E** = server harness driving a real in-process runtime through `DirectSessionRunnerClient` + `FakeHerdrServer`; **live probe** = operator's real herdr, headed opt-in only. Every named test is a concrete case Build writes; no test asserts only that a helper returned an enum, and every refusal case asserts at the RPC/file boundary.

### Fixture extensions Build adds before the first test (prerequisites, not tests)

- `FakeHerdrServer`: route `tab.list` (`{type:"tab_list", tabs:[TabInfo]}` with `pane_count` from `Panes.Count`, scoped by `workspace_id`; unknown workspace → `not_found`); `SeedTab(workspaceId, label, paneCount = 1, cwd = null)` returning the `TabState`; `SetTabReportedPaneCount(tabId, n)` to make the reported count disagree with enumeration; `RemoveTab(tabId)`; `MoveTab(tabId, toWorkspaceId)`; `FailMethod(method, code)` so one RPC returns an error; `GateMethod(method)` returning a handle whose `Release()` lets the next matching request answer (the request is recorded when it arrives, answered when released). All existing behaviour and `Requests` recording unchanged; `HerdrClientTests.Typed_wrappers_round_trip_workspace_tab_pane_and_agent_against_fake_state` must stay green after the routing table grows.
- `Antiphon.Tests` harness runner-client fake used by `AgentControlServiceIntegrationTests` / `AgentSupervisionTests` / `HerdrPlacementPreflightTests`: a `PlacementCheck` script hook (`Func<HerdrPlacementCheckRequest, Task<HerdrPlacementCheckResult>>`) plus a recorded list of check calls, and a `StartRefusal` hook that throws `ConflictException(Code)` from `StartAsync` to simulate a launch-time runner 409. `FakeAgentProtocolAdapter.StartedHerdr` already captures `HerdrLaunchOptions` and is the assertion point for `TabLabel`.
- Assertion vocabulary used below. **Zero-mutation set** = after the attempt, the fake request log delta contains none of `pane.close`, `pane.send_text`, `pane.send_keys`, `pane.split`, `tab.create`, `tab.rename`, `tab.close`, `pane.rename`, `pane.report_metadata`, `agent.rename`, `agent.start`; every `last-pane/*.json` under the test log root is byte-identical to a snapshot taken before the attempt; no sidecar exists for the attempting session; every fake pane's foreground list is unchanged; any dummy process started by the test is still alive. `workspace.report_metadata` (token refresh in `EnsureWorkspaceAsync`) is permitted on the **launch** path because it is today's behaviour and idempotent; it is forbidden on the **check** path.

### Proves it works now

S1 — settings and central context (`Antiphon.Tests`)

- V-1: Create with `herdrWorkspaceLabel`/`herdrTabLabel` persists trimmed values and the detail DTO returns them | integration | `HerdrPlacementSettingsTests.Create_persists_trimmed_labels_and_detail_returns_them` | input `" Orch "` → detail `herdrTabLabel == "Orch"`, `herdrWorkspaceLabel == "PredictionMarkets"`; list DTO carries the same.
- V-2: Update null leaves a label unchanged; empty/whitespace clears it; each field independent | integration | `HerdrPlacementSettingsTests.Update_null_keeps_and_empty_clears_each_label_independently` | PATCH `{herdrTabLabel: ""}` → tab null, workspace unchanged; PATCH `{}` → both unchanged; PATCH `{herdrWorkspaceLabel: "   "}` → workspace null.
- V-3: Labels over 256 chars or containing control characters are rejected with 422 and nothing is written | integration | `HerdrPlacementSettingsTests.Labels_over_256_or_with_control_characters_are_422` | `ValidationException` (existing 422 middleware path, same as `Start_with_an_ANTIPHON_override_name_is_refused_422`); re-read agent unchanged.
- V-4: The Agent entity gains exactly two nullable 256-char text columns and no pane/tab/workspace ID columns | unit (EF model) | `HerdrPlacementSettingsTests.Agent_model_has_two_label_columns_and_no_herdr_id_columns` | `AppDbContext.Model` Agent entity: `HerdrWorkspaceLabel` and `HerdrTabLabel` nullable with `GetMaxLength() == 256`; no Agent property name ends in `PaneId`, `TabId` or `WorkspaceId`; migration `Up` adds exactly these two columns.
- V-5: Labels survive fresh session IDs and a backend round-trip, and a label edit on a live agent launches nothing | integration | `HerdrPlacementSettingsTests.Labels_persist_across_fresh_session_ids_and_backend_switch`; `HerdrPlacementSettingsTests.Updating_labels_on_a_live_agent_launches_nothing` | two `Fresh: true` starts and PATCH backend PtyHost→Herdr leave both labels intact; PATCH labels while a session is Running records no runner call and the session row is untouched.
- V-6: Resolver applies overrides only to an eligible standing agent and never changes the workspace key | integration (db) | `HerdrLaunchContextResolverTests.Standing_cardless_agent_overrides_workspace_label_and_carries_tab_label`; `.Card_session_ignores_owner_labels`; `.Pool_delegate_ignores_labels`; `.Blank_labels_leave_defaults`; `.Workspace_override_changes_label_not_key` | `WorkspaceKey` stays `project:<guid>` / `none`; `WorkspaceLabel` is the override only for the standing case; `TabLabel` non-null only for the standing case; PtyHost sessions produce `Herdr == null` regardless of labels.
- V-7: The central spec builder carries `TabLabel` on every standing launch shape and preserves the previous-pane hint | integration | `AgentControlServiceIntegrationTests.Named_resume_start_carries_TabLabel`; `.Named_fresh_start_carries_TabLabel_and_ReusePaneOfSessionId`; `.Named_missing_resume_target_fallback_relaunch_carries_TabLabel` (same fixture shape as `Start_interactive_falls_back_to_fresh_session_when_claude_conversation_is_missing`, adapter throws `ResumeTargetMissingException` once); `AgentSupervisionTests.Supervised_fresh_threshold_restart_carries_TabLabel_and_previous_pane_hint` (`FreshAfterResumeFailures = 2`, advance the clock through two failed resumes) | `adapter.StartedHerdr.TabLabel == "Orch"` on each launch; fresh-row launches have `ReusePaneOfSessionId == previousId`; the same-ID fallback launch has `ReusePaneOfSessionId == null` and the same `SessionId`; card spawn (`Card_spawn_leaves_ReusePaneOfSessionId_null` extended) has `TabLabel == null` even when the owning agent is labelled.
- V-8: `TabLabel` rides the wire and its absence deserialises null | unit | `SessionRunnerHttpClientHerdrWireTests.TabLabel_appears_on_the_POST_body`; `.Old_launch_body_without_tabLabel_deserialises_null` | POST body JSON contains `"tabLabel":"Orch"`; a pre-field body yields `Herdr.TabLabel == null`.

S2 — typed listing and pure selection (`Antiphon.SessionRunner.Tests`)

- V-9: `tab.list` wrapper sends `workspace_id` and deserialises label, number and pane_count | integration (fake) | `HerdrClientTests.Tab_list_round_trips_labels_counts_and_scopes_by_workspace` | request `method == "tab.list"`, `params.workspace_id == w1`; result has only w1's tabs with their labels and counts; an unknown workspace surfaces `HerdrApiException`.
- V-10: Pure selection decisions | unit | `HerdrNamedTabResolverTests.Exact_label_selects_the_single_pane_tab`; `.Comparer_is_explicit_ignore_case_matches_orch_and_ordinal_does_not`; `.Two_matches_are_herdr_tab_ambiguous_listing_both_ids`; `.Multi_pane_tab_or_count_disagreement_is_herdr_tab_invalid`; `.Tab_in_another_workspace_is_never_selected`; `.Tab_list_failure_propagates_and_is_not_absence`; `.Substring_and_trimmed_labels_do_not_match` | decision/exception as named; ambiguity message contains both tab IDs and labels; the resolver type exposes no herdr mutation method (compile-time: it takes only list/get/process_info delegates).

S3 — resolution, ownership, reservations (`Antiphon.SessionRunner.Tests`, new `HerdrNamedTabPlacementTests` `[NotInParallel("HerdrNamedTabPlacement")]`, through `SessionRunnerRuntime.StartAsync` + `FakeHerdrServer`, helper `StartNamedAsync(..., tabLabel: "Orch")`)

- V-11: Absent tab in an existing workspace → one labelled `tab.create` at the agent's cwd with one pane, no split, no rename | integration | `Named_launch_creates_the_labelled_tab_once_with_agent_cwd_and_no_split` | exactly one `tab.create` with `workspace_id` = existing, `label == "Orch"`, `cwd == request.Cwd`, `env == request.Env`; `tab.rename == 0`, `pane.split == 0`; fake tab has one pane; sidecar `TabId` is the created tab, `TabLabel == "Orch"`, `WorkspaceLabel == "PredictionMarkets"`; pane title (`pane.report_metadata title`) equals `PaneTitle`, not `Orch`.
- V-12: Absent workspace → `workspace.create` and rename its root tab; no second tab | integration | `Named_launch_into_a_new_workspace_renames_the_root_tab_and_creates_no_second_tab` | `workspace.create == 1`, `tab.rename == 1` with the returned root `tab_id` and `label == "Orch"`, `tab.create == 0`; workspace has exactly one tab with one pane; `LastLaunchScriptContent` contains the quoted `Set-Location -LiteralPath` prelude.
- V-13: Existing operator-created single-pane idle `Orch` (no sidecar, no last-pane, different cwd) → typed launch in place with cwd and env; attached-origin history does not block it | integration | `Named_launch_reuses_an_operator_idle_labelled_tab_in_place_with_cwd`; `Named_launch_types_into_an_idle_shell_despite_attached_origin_history` | same tab/pane IDs in the sidecar; `tab.create`/`pane.split`/`tab.rename` all 0; script contains `Set-Location -LiteralPath '<request.Cwd>'` and one `Set-Item` per env entry; pane title == `PaneTitle`; tab label still `Orch`; sidecar `Origin == launched` even when the seeded history was `attached` (contrast: unpinned attached-origin history still allocates — existing `R20`/`R21` stay green).
- V-14: Named pin outranks a valid last-pane and an allocator slot | integration | `Named_pin_outranks_a_valid_last_pane_and_an_allocator_slot` | fixture: live unpinned session in tab `MavRef-DL` (one pane, eligible slot) **and** a last-pane for this session ID pointing at that pane; named start lands on `Orch`; `MavRef-DL` pane count unchanged; `pane.split == 0`; the mismatched last-pane bytes are unchanged until success; after success this session's record is gone (A-8) and a sidecar names the `Orch` pane.
- V-15: Same-ID restart after child death relaunches into the same labelled pane and clears only proven stale env names | integration | `Named_same_id_restart_relaunches_into_the_same_labelled_pane` | `SweepVanishedSessions` + `ClearDetectedAgent` + shell-only → second start: same pane ID, `tab.create == 0`, script has `Remove-Item -LiteralPath 'Env:<stale>'` for the names the first launch set and this one drops; a last-pane whose IDs do not match the selected pane contributes no `Remove-Item` lines.
- V-16: Fresh-row launch (new ID + `ReusePaneOfSessionId`) targets the labelled tab whether or not the previous pane was it | integration | `Named_fresh_id_lands_on_the_labelled_tab_when_the_previous_pane_was_it`; `Named_fresh_id_ignores_a_previous_pane_elsewhere` | (a) previous pane is the idle `Orch` pane → relaunch there with its env names, `tab.create == 0`, previous record consumed on success; (b) previous pane is in `MavRef-DL` → `Orch` used, `MavRef-DL` untouched.
- V-17: A deleted labelled tab is recreated on the next start and the stale record does not redirect the target | integration | `Named_launch_recreates_a_deleted_labelled_tab` | `RemoveTab(orch)` while the agent is down → next start `tab.create == 1` with `label == "Orch"`; new sidecar IDs; no `pane.split`.
- V-18: Own live process (one expected-kind process whose argv names `request.SessionId`) is adopted and nothing is typed | integration | `Named_launch_adopts_its_own_live_process_and_types_nothing` | `pane.send_text` delta 0; DTO `Running`; sidecar `ChildPid` == occupant pid, `TabLabel == "Orch"`.
- V-19: Every foreign-occupant shape is 409 `pane_occupied` with the zero-mutation set, then a freed pane relaunches | integration | `Named_launch_refuses_every_foreign_occupant_shape_with_zero_side_effects` (loop over: wrong kind; right kind different ID; right kind no argv ID; two foreground processes; occupied Codex with right kind; live previous ID on a fresh-row launch; non-PowerShell shell pid; missing shell pid) then `ClearDetectedAgent` + shell-only → `Named_launch_succeeds_on_the_same_tab_once_the_occupant_is_gone` | each attempt: `HerdrLaunchException.Code == HerdrProblemTypes.PaneOccupied`, message contains the pane ID; zero-mutation set holds (last-pane bytes identical, no sidecar, no `pane.close`, no `tab.create`); retry after freeing lands on the same pane ID.
- V-19b: Duplicate labels, multi-pane tab, and pane-count disagreement refuse with their own codes and the zero-mutation set | integration | `Named_launch_refuses_duplicate_labels_listing_both_tabs`; `Named_launch_refuses_a_multi_pane_or_miscounted_labelled_tab_without_mutation`; `Tab_list_failure_fails_the_named_launch_and_never_allocates` | codes `HerdrProblemTypes.TabAmbiguous` / `TabInvalid`; `FailMethod("tab.list")` → the RPC error propagates and `tab.create == 0`; never a root/focused pane chosen from a multi-pane tab.
- V-20: A pane held by another live or pending session refuses | integration | `Named_launch_refuses_a_pane_claimed_by_another_live_or_pending_session` | live session B in the `Orch` pane (sidecar) → A's named start 409 `pane_occupied` naming B; pending B (gated at `pane.send_text`) → same; zero-mutation set.
- V-21: Target that vanishes or moves between selection and acquisition is `herdr_pane_changed`, never re-targeted | integration | `Named_target_that_vanishes_before_acquisition_is_pane_changed_not_reallocated` | `GateMethod("pane.process_info")`; while gated `RemoveTab`/`MoveTab`; release → `Code == HerdrProblemTypes.PaneChanged`; `tab.create == 0`, `pane.split == 0`, zero-mutation set.
- V-22: Two near-simultaneous named launches on the same label yield one winner, one refusal, one tab | integration | `Two_concurrent_named_launches_yield_one_winner_one_refusal_and_one_tab` (tab present) and `Two_concurrent_named_launches_into_an_absent_tab_create_it_once` | gate `pane.send_text`; start A (parks post-claim); start B → B completes with `pane_occupied` **before** A is released (assert B's task is complete while the gate is still held); release → A `Running`; `tab.create == 1`; launch-script `pane.send_text == 1`; tabs labelled `Orch` in the workspace == 1. The race is real: without the coordinator both launches list tabs before either creates or types (D-5); the gate makes the window deterministic instead of relying on timing.
- V-23: Cancellation releases only the transient claim | integration | `Cancelled_named_launch_releases_its_claim_and_keeps_names_and_hints` | cancel A while gated → `OperationCanceledException`; B then succeeds on the same pane; last-pane files unchanged; no sidecar for A.
- V-24: Failure before a sidecar exists never closes an operator shell or kills an unproven pid | integration (spawns a dummy; `HerdrPaneChildKillTests`, `[ParallelLimiter<ProcessSpawnLimit>]`) | `HerdrPaneChildKillTests.Named_launch_failure_before_sidecar_never_closes_the_operator_shell_or_kills_an_unproven_pid` | seeded idle `Orch`; after typing, the fake detects the wrong kind (`LaunchScriptAgentKind = codex`, expected `claude`) and a `StartDummy()` pid appears in the foreground; the wrong-kind `HerdrLaunchException` surfaces unchanged; `pane.close == 0`; `dummy.HasExited == false`; pane still present; last-pane for this session absent; script redacted. The unpinned `Wrong_kind_fails_keeps_script_and_tears_down` still asserts `pane.close` for an allocated pane.
- V-25: Detect timeout in a named idle shell keeps the pane with a labelled last-pane and the next start relaunches there | integration | `Named_detect_timeout_keeps_the_pane_and_the_next_start_relaunches_there` | `LaunchDetectTimeoutMs` small, no detection → `pane.close == 0`; last-pane JSON has `"tabLabel":"Orch"`; second start: same pane, `tab.create == 0`.
- V-26: Sidecar and last-pane carry optional labels and old files still load | unit | `HerdrPaneSidecarTests.Placement_labels_round_trip_and_old_files_load_with_null_labels`; `.FromSidecar_and_FromLaunchRequest_copy_labels` | JSON property names `workspaceLabel`/`tabLabel`; a pre-field file → both null; `Retire` preserves them.
- V-27: A live dedicated named tab is not allocator capacity, including after runner-restart adoption | integration | `Unpinned_launches_never_split_a_live_dedicated_named_tab`; `HerdrAdoptionSweepTests.R22_dedicated_tab_exclusion_survives_runner_restart_adoption` | named A live in `Orch`; unpinned B (same `WorkspaceKey`) → `tab.create`, not `pane.split` on A; C and D split B's tab; `Orch` pane count stays 1; a new runtime adopting A from its sidecar still excludes the tab for unpinned E. `HerdrPaneAllocatorTests` unchanged and green (allocator untouched).
- V-28: Attach refuses a pane under a pending named claim | integration | `HerdrAttachTests.Pending_named_launch_claim_refuses_attach_as_bound` | gate A at `pane.send_text`; attach A's pane → `HerdrProblemTypes.PaneBound` naming A; release; A `Running`.
- V-29: Null `TabLabel` never sends `tab.list` and takes today's decisions | integration | `HerdrLaunchShapeTests.Null_TabLabel_never_sends_tab_list` plus the existing unpinned suite (listed under Out of scope → "unchanged behaviour must stay green") | `tab.list == 0` across resume, relaunch, allocate and occupied flows.

S4 — preflight and capability

- V-30: Runner check is read-only and classifies | integration | `HerdrPlacementCheckRouteTests.Check_reports_create_when_tab_or_workspace_is_absent_and_mutates_nothing`; `.Check_reports_relaunch_for_an_idle_named_shell`; `.Check_reports_adopt_for_own_live_process`; `.Check_refuses_occupied_ambiguous_and_invalid_with_the_launch_codes`; `.Check_route_maps_refusals_to_409_problem_details` (same shape as `Pane_occupied_on_sessions_post_is_a_409_with_its_code`) | request log delta ⊆ {`workspace.list`, `tab.list`, `pane.get`, `pane.process_info`}; no `workspace.create`, no `workspace.report_metadata`; no files written; no claim left (a different session's named launch immediately after succeeds).
- V-31: Public Start preflights before any row or queue mutation, only for named Herdr placement | integration | `HerdrPlacementPreflightTests.Public_start_refused_by_preflight_is_409_before_any_row_or_queue_mutation`; `.Preflight_passes_the_chosen_session_id_and_named_options`; `.Unpinned_start_never_calls_preflight`; `.Already_live_start_stays_idempotent_and_skips_preflight`; `AgentSupervisionTests.Supervisor_preflight_refusal_records_failure_and_backoff_without_enqueue` | scripted refusal → `ConflictException.Code == "pane_occupied"`; agent `Status` not `Running`, `PersistentSessionId` unchanged, `AgentSessions` count for the agent unchanged, the resumable row's `Status` still its prior value (never `Starting`), `adapter.StartedHerdr == null` after `WaitForIdleAsync`, DB labels unchanged; the recorded check request has `SessionId` == the existing resume ID (resume arm) or a fresh GUID that then becomes the row ID (fresh arm), `Herdr.TabLabel == "Orch"`, no exe/args/env; unpinned agent → zero check calls; supervisor → `ConsecutiveFailures` incremented, `NextRestartAt` set, nothing enqueued.
- V-32: A runner 409 after an allowed preflight is an asynchronous Failed row with the runner detail and never a fallback | integration | `HerdrPlacementPreflightTests.Runner_409_after_allowed_preflight_fails_the_row_asynchronously_and_never_falls_back` | public Start returns normally with a `PersistentSessionId`; after `WaitForIdleAsync` the row is `Failed`, `FailureReason` contains the runner detail, agent `Status == Failed`, `PersistentSessionId` still that row; launch attempts == 1 (no `ResumeTargetMissingException` relaunch, no allocator).
- V-33: Capability gate | integration | `HerdrPlacementPreflightTests.Named_launch_refused_when_runner_lacks_named_tab_placement_capability`; `.Unpinned_launch_on_an_old_runner_is_unaffected` | `ConflictException.Code == HerdrProblemTypes.Refused`, message contains `herdr-named-tab-placement`; no check call, `adapter.StartedHerdr == null`; the unpinned agent launches with `TabLabel == null`.
- V-34: Check wire contract | unit | `SessionRunnerHttpClientHerdrWireTests.Placement_check_posts_options_without_exe_or_env_and_maps_409_to_conflict` | body has `sessionId` and `herdr.*` only; 200 → result `Action`; 409 problem → `ConflictException` carrying the runner code and detail; 503 → runner-unavailable path.
- V-35: End-to-end named AlwaysOn across all three entry points | E2E | `HerdrAlwaysOnChannelParityTests.Named_AlwaysOn_agent_lands_on_its_labelled_tab_across_crash_restart_and_fresh_threshold` (`DirectSessionRunnerClient` + `FakeHerdrServer`, `FreshAfterResumeFailures = 2`, advancing clock) | initial start, supervised same-ID restart, and supervised fresh-threshold restart each end with exactly one tab labelled `Orch` holding one pane in the workspace labelled `PredictionMarkets`; a seeded unpinned specialist tab keeps its pane count; three sidecars over time all name that tab.

S5 — client (`client/src/features/agents/AgentHerdrPlacement.test.tsx`)

- V-36: Settings UI | unit (vitest + msw) | `AgentHerdrPlacement.test.tsx`: `create modal shows both inputs only in Herdr mode with the documented placeholders`; `create submits camelCase herdrWorkspaceLabel and herdrTabLabel`; `settings modal pre-fills from detail and sends only changed fields`; `clearing a field sends an empty string not undefined`; `switching backend away and back keeps typed labels`; `saving labels on a live agent PATCHes and never POSTs start` | assertions on the msw-captured `UpdateAgentRequest`/`CreateAgentRequest` bodies and absence of a `/start` request.

S6 — optional live probe

- V-37: Real herdr creates, reuses and refuses on a labelled tab | live probe (headed, skips when not eligible) | `HerdrNamedTabPlacementLiveTests` `[Category("Headed")] [NotInParallel("Headed")] [ParallelLimiter<ProcessSpawnLimit>]`, gated by `HerdrLiveSession.SkipIfNotEligibleAsync`; workspace label `antiphon-c384-<guid>`, tab label `Orch`, cwd = a disposable temp dir. Probe **placement only** through the read-only check route and `tab.create`/`tab.list`, so no agent kind detection is needed: create → `tab.list` shows exactly one `Orch` with `pane_count == 1` and `pane.get` cwd equals the temp dir; a second check against the idle pane reports `relaunch` for the same `tab_id`; start a `pwsh -NoExit` child in that pane (pid recorded) → check reports `pane_occupied` and the pane is still listed afterwards. Cleanup in `finally`: `tab.close` every tab this test created, kill only pids this test started. Pass criterion is the `tab.list`/`pane.process_info` evidence, never a screen redraw. Build reports skipped if ineligible; this is not part of the floor.

### Guards the regression

- R-1: Re-ordering named resolution behind last-pane (label as a fallback) | caught by V-14 because the selected tab label is asserted `Orch` while a valid last-pane names the specialist pane.
- R-2: Falling back to the allocator, attach, or another tab on refusal | caught by V-19/V-19b because `tab.create` and `pane.split` deltas are asserted 0 after the 409.
- R-3: Deleting or rewriting last-pane records during named resolution or on refusal | caught by V-19 and V-14 because last-pane bytes are compared to a pre-attempt snapshot.
- R-4: Assigning `_paneId` (or writing a sidecar) before acquisition so `StartHerdrAsync`'s catch closes an unacquired pane | caught by V-24 and V-19 because `pane.close == 0` and the dummy pid is still alive after failure.
- R-5: Weakening the placement coordinator or the pending-claim check | caught by V-22 (`pane.send_text` launch count 1, `tab.create` 1) and V-28 (attach refused while a claim is pending).
- R-6: Sending `tab.list` on the unpinned path | caught by V-29 because `tab.list == 0`.
- R-7: Counting a dedicated named tab as allocator capacity | caught by V-27 because the `Orch` pane count stays 1 after three unpinned launches.
- R-8: Moving the preflight after the row/queue mutation, or preflighting unpinned agents | caught by V-31 because the resumable row's status and the adapter's start capture are asserted unchanged, and the unpinned test asserts zero check calls.
- R-9: Translating `pane_occupied` into `ResumeTargetMissingException` (conversation or allocator fallback) | caught by V-32 because launch attempts == 1 and the row is `Failed` with the runner detail.
- R-10: Dropping the runner capability gate (a new server posting `TabLabel` to an old runner) | caught by V-33 because the refusal code and the absence of a start are asserted.
- R-11: Fuzzy, substring, or hard-coded case matching | caught by V-10 because the comparer is explicit and both behaviours are pinned.
- R-12: Rebuilding `HerdrLaunchOptions` without `with` and dropping `TabLabel` or `ReusePaneOfSessionId` on one launch shape | caught by V-7 because all four server shapes assert both fields.
- R-13: Sidecar/last-pane schema change that breaks pre-field files | caught by V-26.
- R-14: Choosing a root/focused pane from a multi-pane labelled tab | caught by V-19b because `herdr_tab_invalid` and the zero-mutation set are asserted.
- R-15: Skipping the pre-acquisition membership recheck | caught by V-21 because `herdr_pane_changed` and `tab.create == 0` are asserted after the gated mutation.
- R-16: Making the check route create or refresh anything | caught by V-30 because the request-log delta is asserted to be the read-only set.

### Positive controls

Build runs each: break, see red, revert, see green, and reports all three (test name, red output line, green rerun).

- PC-1: break R-1 by loading `HerdrLastPane` and returning its `Relaunch` target before the `opts.TabLabel is null` branch in `HerdrPaneChild.LaunchAsync`; expect `Named_pin_outranks_a_valid_last_pane_and_an_allocator_slot` red.
- PC-2: break R-2 by replacing the named-branch `throw` for a foreign occupant with `return new TargetPane(TargetPaneKind.Allocate, ...)`; expect `Named_launch_refuses_every_foreign_occupant_shape_with_zero_side_effects` red on the `tab.create`/`pane.split` delta.
- PC-3: break R-3 by adding `HerdrLastPane.TryDelete(_settings.SessionLogPath, request.SessionId)` immediately before the named-branch refusal throw; expect the same test red on the byte-identical assertion.
- PC-4: break R-4 by assigning `_paneId = selected.PaneId` at selection time in the named branch; expect `Named_launch_failure_before_sidecar_never_closes_the_operator_shell_or_kills_an_unproven_pid` red (`pane.close` observed).
- PC-5: break R-5 by making `HerdrPlacementCoordinator.ClaimAsync` return a released no-op claim (or skip the pending-claim consult); expect `Two_concurrent_named_launches_yield_one_winner_one_refusal_and_one_tab` red (`pane.send_text` launch count 2) and `Pending_named_launch_claim_refuses_attach_as_bound` red.
- PC-6: break R-6 by calling `_client.TabListAsync(workspaceId, ct)` unconditionally before the null-`TabLabel` early return; expect `Null_TabLabel_never_sends_tab_list` red.
- PC-7: break R-7 by deleting the `sidecar.TabLabel is not null` tab-exclusion clause in `CollectLiveAntiphonPanes`; expect `Unpinned_launches_never_split_a_live_dedicated_named_tab` red (`Orch` pane count 2).
- PC-8: break R-8 by moving the `CheckHerdrPlacementAsync` call in `StartInteractiveSessionAsync` to after `_launchQueue.EnqueueInteractiveSession`; expect `Public_start_refused_by_preflight_is_409_before_any_row_or_queue_mutation` red (row `Starting` / adapter started).
- PC-9: break R-9 by catching `ConflictException { Code: "pane_occupied" }` in `LaunchInteractiveAsync` and rethrowing `ResumeTargetMissingException`; expect `Runner_409_after_allowed_preflight_fails_the_row_asynchronously_and_never_falls_back` red (launch attempts 2).
- PC-10: break R-10 by short-circuiting the feature check to `true` in the server's named-launch gate; expect `Named_launch_refused_when_runner_lacks_named_tab_placement_capability` red.
- PC-11: break R-11 by replacing the comparer equality with `label.Contains(tabLabel, StringComparison.OrdinalIgnoreCase)`; expect `Substring_and_trimmed_labels_do_not_match` and `Comparer_is_explicit_ignore_case_matches_orch_and_ordinal_does_not` red.
- PC-12: break R-14 by returning `panes[0]` instead of throwing `herdr_tab_invalid` when `panes.Count != 1`; expect `Named_launch_refuses_a_multi_pane_or_miscounted_labelled_tab_without_mutation` red.
- PC-13: break R-15 by deleting the post-gate `pane.get` membership recheck in named acquisition; expect `Named_target_that_vanishes_before_acquisition_is_pane_changed_not_reallocated` red.
- PC-14: break R-16 by calling `EnsureWorkspaceAsync` from the check path; expect `Check_reports_create_when_tab_or_workspace_is_absent_and_mutates_nothing` red (`workspace.create` or `workspace.report_metadata` in the delta).
- PC-15: break R-12 by constructing a new `HerdrLaunchOptions(...)` (positional, omitting `TabLabel`) in the fresh-row path of `AgentControlService`; expect `Named_fresh_start_carries_TabLabel_and_ReusePaneOfSessionId` red.

### Defaults the plan left open (Build takes these as written; deviations go back to Plan)

- A-1: The plan writes `pane_changed`; the existing constant is `HerdrProblemTypes.PaneChanged == "herdr_pane_changed"`. Reuse the constant. Tests assert against the constant, never the literal.
- A-2: Add `HerdrProblemTypes.TabAmbiguous = "herdr_tab_ambiguous"` and `TabInvalid = "herdr_tab_invalid"` to the contracts, titles in `HerdrProblemMapper`, HTTP 409. Do not duplicate them as `HerdrLaunchException.Code*` literals.
- A-3: Non-PowerShell or unknown shell in a **named** tab is `pane_occupied` (D-3 §7), while the unpinned path keeps `pane_shell`; `Shell_check_refuses_a_non_PowerShell_shell_pid` stays green unchanged.
- A-4: A live/pending claim refuses a **named launch** with `pane_occupied` (D-3 §9) and refuses **attach** with `herdr_pane_bound` (existing). V-20 and V-28 assert these respectively.
- A-5: Capability token is `RunnerCapabilityFeatures.HerdrNamedTabPlacement = "herdr-named-tab-placement"` (kebab-case like `herdr-attach`); the server refusal is `ConflictException(HerdrProblemTypes.Refused)` whose message names the token.
- A-6: Check route `POST /herdr/placement/check`; request `HerdrPlacementCheckRequest(Guid SessionId, HerdrLaunchOptions Herdr)`; response 200 `HerdrPlacementCheckResult(string Action /* "create" | "relaunch" | "adopt" */, string? WorkspaceId, string? TabId, string? PaneId)` where IDs are diagnostic only; refusals are 409 problem-details with the launch codes; unreachable is 503. Server interface `ISessionRunnerClient.CheckHerdrPlacementAsync` is a default interface member returning `Action = "create"` so untouched fakes compile; the harness fake overrides it and records calls (V-31 needs the record).
- A-7: The public `ConflictException.Code` on preflight refusal is the runner code passed through (`pane_occupied`, `herdr_tab_ambiguous`, `herdr_tab_invalid`), not a constant `pane_occupied`.
- A-8: On **success**, the current session's last-pane record and the carried `ReusePaneOfSessionId` record are deleted whether or not they matched the selected pane (the session now has a live sidecar; a stale record would make `FindBoundPane` refuse a pane this session no longer owns). On **refusal**, every record is untouched. V-14/V-16 assert the success half, V-19 the refusal half.
- A-9: Sidecar/last-pane JSON fields are `workspaceLabel` and `tabLabel` (camelCase via `JsonSerializerDefaults.Web`), written as `null` when absent; old files load with nulls.
- A-10: `HerdrNamedTabResolver` takes its `StringComparer` as a constructor argument; runner composition passes `OrdinalIgnoreCase` on Windows and `Ordinal` elsewhere; unit tests pin both without depending on the host OS.
- A-11: Two new test classes share the plan's name `HerdrPlacementPreflightTests`; the runner-side one is `HerdrPlacementCheckRouteTests` (`Antiphon.SessionRunner.Tests`) so `--treenode-filter` names are unambiguous.
- A-12: A single preflight per public Start, with the ID the row will use; the queued same-ID missing-resume-target fallback is not re-preflighted (the runner rechecks at launch anyway, D-6).
- A-13: Preflight is skipped when Start is already idempotent-live (existing early return) and for PtyHost agents regardless of labels.

### Out of scope

- Atomicity against a human typing in the final RPC gap: herdr has no compare-and-launch; D-4 forbids claiming it. V-21/V-22 pin the mitigations only.
- The allocator's `TabNumber: 0` census ordering and `HerdrPaneAllocator` itself: untouched by this card, `HerdrPaneAllocatorTests` stays as-is.
- Grok child death, `--rules` multiline handling (#41), and Slack ownership (#44 remainder): D-7 boundaries.
- Applying the migration or configuring PM-Orchestrator-Grok in production: Land/operator steps, never in tests.
- Documentation agreement (`docs/herdr-sessions.md` table, `docs/ops-http.md`, `docs/antiphon-api.md`): reviewed by eye in S6; no doc-pinning test.
- Non-Windows runtime case rule: only the comparer is unit-tested (A-10); no Linux run.
- The live probe (V-37) is opt-in evidence, not part of the floor; a skip is a legitimate report.
- **Unchanged behaviour that must stay green (named so Build re-runs them, not to rewrite them):** `HerdrLaunchShapeTests` — `Resume_with_a_last_pane_must_not_call_tab_create`, `ReusePaneOfSessionId_targets_the_previous_sessions_pane_for_a_fresh_id`, `Codex_occupied_pane_is_refused_even_with_the_right_kind`, `Relaunch_in_place_never_calls_tab_rename_or_pane_split`, `Relaunch_in_place_reapplies_env_and_clears_the_names_it_no_longer_carries`, `Fresh_workspace_uses_the_created_root_pane`, `Fresh_root_keeps_launch_cwd_and_environment`, `Unique_untagged_label_reuses_the_operator_workspace_without_stamping`, `Ambiguous_or_foreign_label_match_creates_a_managed_workspace`, `Own_antiphon_ws_token_wins_over_untagged_same_label`, `Timeout_on_an_idle_shell_keeps_the_pane_and_writes_a_last_pane_record`, `Relaunch_after_a_kept_pane_targets_it_and_clears_its_env_names`, `Shell_check_refuses_a_non_PowerShell_shell_pid`, `Wrong_kind_fails_keeps_script_and_tears_down`; `HerdrAdoptionSweepTests` R14–R21; `HerdrAttachTests` — `Bound_pane_is_refused_naming_the_holder`, `Last_pane_of_another_session_is_refused`, `Attached_pane_is_not_an_allocator_slot`, `Pane_occupied_on_sessions_post_is_a_409_with_its_code`; all of `HerdrPaneChildKillTests`, `HerdrPaneAllocatorTests`, `HerdrPaneSidecarTests`; `AgentControlServiceIntegrationTests` — `Fresh_herdr_arm_sets_ReusePaneOfSessionId_to_the_previous_session`, `Resume_herdr_arm_leaves_ReusePaneOfSessionId_null`, `Card_spawn_leaves_ReusePaneOfSessionId_null`, `Start_is_idempotent_when_a_live_session_already_exists`; all of `SessionRunnerHttpClientHerdrWireTests`; both `HerdrAlwaysOnChannelParityTests`; `AgentAttachHerdrTests` — `Refuses_when_runner_lacks_attach_capability`, `Stop_on_an_attached_agent_leaves_the_pane_process_alive`; client `AgentSessionBackend.test.tsx`, `AgentCreateModal.test.tsx`.

### Cost

- suites forced: `Antiphon.SessionRunner.Tests` classes `HerdrClientTests`, `HerdrNamedTabResolverTests`, `HerdrNamedTabPlacementTests`, `HerdrPlacementCheckRouteTests`, `HerdrLaunchShapeTests`, `HerdrAttachTests`, `HerdrAdoptionSweepTests`, `HerdrPaneChildKillTests`, `HerdrPaneSidecarTests`, `HerdrPaneAllocatorTests` (one `--treenode-filter "/*/*/<Class>/*"` run each, `--property:OutputPath=bin-c384/`); `Antiphon.Tests` classes `HerdrPlacementSettingsTests`, `HerdrLaunchContextResolverTests`, `SessionRunnerHttpClientHerdrWireTests`, `HerdrPlacementPreflightTests`, `AgentControlServiceIntegrationTests`, `AgentSupervisionTests`, `HerdrAlwaysOnChannelParityTests`, `AgentAttachHerdrTests`; client `pwsh -File scripts/test-client.ps1 AgentHerdrPlacement.test` plus `AgentSessionBackend.test` and `AgentCreateModal.test`. Never the full `Antiphon.Tests` assembly; never `dotnet test`; runner and pty test projects sequentially if both are needed (only the runner project is here).
- verification floor ~ 25 min: runner project build ~2 min + ten class runs ~5 min (process-spawn lane serialises Kill/Attach/Sweep classes); server project build ~3 min + Postgres container ~1 min + eight class runs ~10 min (`AgentSupervisionTests` and `HerdrAlwaysOnChannelParityTests` are global `[NotInParallel]` with real queues); client ~2 min. Positive controls add ~15 × (edit + one class run ≈ 1–3 min) ≈ 30–40 min on top of the floor. Live probe, when eligible, ~5 min extra. Delete every `bin-c384` directory (about a dozen) before reporting.
