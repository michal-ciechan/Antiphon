# CARD-0498: Preserve land source evidence and explain failed landings

Status: Plan and TestDesign complete; Code is the next stage. Runtime implementation and verification remain pending. The verification design is the final `## Verification design` section (TestDesign task `0c8a18f7`, baseline `9ebbba77`).

Authoring baseline: `0b0f62e0`. Plan task: `5a7b92a5`. Investigation: `34665810`. Card: `CARD-0498` (`65a54e96-6a8f-4992-89fc-5c7631c446bd`, Antiphon board).

## Outcome and scope

A monitor pass during source inspection must not make a valid land fail or erase its observed source snapshot. If land execution does throw, the request must retain a specific, bounded failure code and diagnostic identity, including when no landing operation exists. HTTP task detail, `delegate.ps1 -Status`, and the durable outcome notification must expose that evidence.

The card and its revision history, and the full Investigate report, were read before choosing this design. The investigation establishes three `DbUpdateConcurrencyException` failures at the resolver's Observed snapshot save: task `2cca15a0` twice and task `3fcd2c22` once. Use this finding as the starting point; do not reopen the Review-gate hypothesis. `3fcd2c22` was TestDesign, despite the card describing it as Plan.

This card covers resolver/request persistence, exception diagnostics, conservative Git inspection diagnostics, and CLI presentation. It preserves explicit full-SHA caller approval without Review evidence, the null merge target default of `refs/heads/master`, the asynchronous land acknowledgment, publication evidence, repository leases, source-child journals, and conservative cleanup. It does not retry historical incidents, change approval policy, broaden Git publication behavior, or introduce a new notification transport.

Owners: [project conventions](../../project-context.md), [orchestration](../../orchestration-loop.md), [HTTP operations](../../ops-http.md), [testing/build](../../testing-and-build.md), [credential custody](../../agent-credentials.md). Read [session-runtime invariants](../../session-runtime-invariants.md) before modifying notification production. Queue delivery and transcript confirmation need no behavioral change.

## Ground truth

| Card premise or possible shortcut | Confirmed behavior at the authoring baseline | Design consequence |
|---|---|---|
| Plan/non-reviewed lands are rejected by a new Review gate. | `AgentTaskLandService.RequestAsync` accepts `ExplicitCaller` approval; Review validation runs only for supplied evidence. The three failures were EF concurrency exceptions. | Add Plan and TestDesign regression cases; add no role exemption or mandatory Review requirement. |
| Missing merge target explains the incidents. | Admission snapshots and resolver coordinates default to `refs/heads/master`. | Exercise null targets through admission and execution. |
| Every named source refusal loses its reason. | `SourceRefusalReason` reaches `LandRequestStatusDto.From`; `FormatSourceRefusal` and `LandNotificationPayload` carry named refusal evidence. | Preserve these paths. Fix the exception path and missing CLI fields. |
| The monitor only writes when a warning fires. | `AgentTaskLandMonitorService.SweepAsync` updates `LastEvaluatedAt` and rotates the request token every pass, under an `AgentTasks` row lock. | A genuine competing write must remain in the regression test, including below warning thresholds. |
| The resolver's transaction prevents the race. | `SaveRequestAsync` saves its tracked request after Git work without acquiring the monitor's lock or refreshing its original token. | Coordinate the short save transaction; preserve the already computed patch before reloading. |
| Saving Observed is the only exposed write. | Resolver child checkpoints, refusals and Resolved use the same helper. Operation creation attaches request/task outside that helper; service fallback source reasons also save directly. | Cover all these handoffs, not only the incident line. |
| Failure handling retains the thrown error. | `FailRequestAsync` clears tracking and reloads, but discards its exception argument and persists generic `landing_failed` prose before publication. | Put typed failure evidence in the terminal transaction, after reload. |
| `-Land` can print an eventual refusal immediately. | `-Land` returns a 202 queue acknowledgment; later outcomes arrive through notifications/status. | Keep acknowledgment semantics. Improve the durable outcome and `-Status`; add no poll/wait mode. |
| The Git result contains safe stderr ready to print. | `LandingGit.ExecuteAsync` drains and deliberately discards stderr; its `Diagnostic` is a generated `git_exit_N`, not stderr. Inspection further reduces command results to reason codes. | Carry safe command/exit/type metadata. Do not resurrect raw stderr or label `Diagnostic` as stderr. |
| A healthy current server proves this source is active. | Investigate recorded different loaded and checkout SHAs. | Code activation must be verified separately after publication; this plan makes no live-runtime claim. |

## Decisions

### D-1. Use the existing task-row lock for short source checkpoints

Choose coordinated writes over automatic replay. Introduce a small concrete `AgentTaskLandRequestWriter` in `server/Application/Services/`, using the existing `AppDbContext` directly. It centralizes task-first locking and guarded source-checkpoint application. It is not a repository abstraction or a process-wide mutex. Keep external I/O in existing Git seams and EF types out of Domain.

The resolver must compute a typed, explicit source patch outside the database transaction. Capture its persisted baseline before modifying tracked state: request ID/task ID, attempt, approval identity, source coordinates, source-resolution fields and active operation binding. Prefer explicit patch records at call sites to deriving arbitrary entity modifications. A patch can explicitly clear a field; null must not ambiguously mean both clear and leave alone.

For every checkpoint:

1. Begin a short transaction and acquire `SELECT 1 FROM "AgentTasks" WHERE "Id" = ... FOR UPDATE`, matching the monitor and settlement lock order. If a transaction is already owned by the caller, use it without nesting or prematurely committing it. No Git command, child wait, verifier, event-bus publication or repository-lease acquisition runs inside this transaction.
2. Reload both the task and request under the lock. Retain the typed patch and baseline outside the EF objects so reload cannot discard the pending observation. Do not call `ChangeTracker.Clear()` here or mark a whole entity Modified.
3. Revalidate current request ID, `IsPending`, absent terminal event, eligible task state, request/task attempt and requested-at mirrors, and unchanged approval and coordinates. Approval identity comprises schema, expected SHA, evidence ID, approval kind/time, verify filter and the four coordinate snapshots. A different request, attempt, canceled/terminal request or changed approval is stale work: return an explicit `StaleRequest` disposition and stop this invocation without Git continuation or settlement of the replacement.
4. Compare the freshly persisted resolver-owned source state and operation binding with the captured baseline. Monitor clocks, reconciliation fields and warning/error changes are compatible; a newer source observation, child checkpoint, resolution state or operation is not. If the same current attempt has incompatible source progress, stop with a bounded `source_resolution_state_changed` failure while retaining the newer evidence. Never apply the stale patch. This is an unexpected internal conflict, not permission to rerun resolution.
5. Apply only the resolver-owned patch, rotate its token, and advance `LastProgressAt` using the checkpoint time without moving it backward. Preserve monitor-owned `LastEvaluatedAt`, reconciliation, warning/error fields and hold information. Save and commit before performing the next dependent action. Refresh the local baseline from the committed values.

Resolver-owned fields are `SourceResolutionState`, the local/remote/candidate/resolved SHAs, source relationship, remote ref/fingerprint/observation ref/time, source filesystem identity, source refusal/diagnostic fields and source-advance child fields. Operation attachment and progress clocks are explicit coordinated transitions, not unrestricted members of a generic patch. Approval, attempts, request lifecycle and terminal identity are never copied from a stale resolver object.

Apply the helper to Observed, AdvanceStarted, child started/drained, Resolved, and both refusal helpers. A failed checkpoint must stop dependent source fast-forward or operation creation. A stale/conflicted checkpoint inside the owned-child start callback must fail that callback so the existing owned-process exception/await/drain path runs; it cannot return normally and allow the child to proceed with unacknowledged custody. Existing owned-child handling remains responsible for an already launched child and its journal; this change cannot bypass that contract.

Preserve `LastEvaluatedAt` semantics in the monitor. Removing token rotation while still changing fields defeats optimistic concurrency; omitting evaluation timestamps changes existing status/monitoring behavior. Reducing genuinely redundant writes may be a later optimization, but is not the correctness fix here. The deterministic test must pass even when the monitor really commits a new token.

Rejected: a lock held across Git inspection; an in-process semaphore that excludes neither a second server nor recovery; removing the concurrency token; unbounded retries; catching every EF error and replaying `ResolveAsync`; or reloading and copying all stale current values back onto the entity. A narrowly guarded retry could work, but the existing shared row-lock convention gives a simpler single-attempt checkpoint contract and covers threshold-writing monitors too. An unexpected remaining `DbUpdateConcurrencyException` becomes D-3 evidence, not success or a hidden retry.

### D-2. Close the adjacent request-write and stale-settlement gaps

`CreateOperationAsync` must prepare its Git-derived candidate outside the transaction, then acquire the same task lock, reload request/task/previous operation, and revalidate the captured approval, source state and active-operation identity before attaching it. Replace the previous operation, insert the new operation and update task/request bindings atomically. Preserve the existing ordering needed by the unique active-operation constraint, rotating changed tokens. If another operation is now bound, abandon the stale candidate; reuse it only through the established identity-validated resume path. Never attach an operation to a replaced or terminal request.

Remove the two direct source-reason saves in `RunRequestAsync`. A source refusal returned by the resolver or its operation-creation helper must carry a typed reason and optional safe diagnostic into guarded persistence. A late fallback reason must not overwrite existing source evidence or close a new request. `StaleRequest` is a distinct resolver result from a current request's actual refusal; the caller must not route it through `PersistRefusalAsync`.

Failure and refusal settlement must carry the originally executing request identity through to the transaction. Checking `requestId` only before taking the lock is insufficient. Capture an immutable execution identity when the attempt is admitted, before Git work, and retain it in the scoped service/explicit failure argument for the hosted catch; do not infer the failed attempt from the latest row during recovery. Direct failure callers without that execution context must capture the current identity before their failure transaction and revalidate it under the lock. Clear failed EF tracking before loading durable failure state, acquire the task lock, then reload and check the expected request and execution attempt again. Factor the terminal transaction as needed so publication classification, diagnostic persistence, terminal event, outbox row and `ClearPending` use that same locked state. Do not nest existing settlement transactions or reload away a just-assigned diagnostic.

Check affected recovery/admission call sites when adopting the helper. Existing admission, cancellation, hold, protocol `TransitionAsync`, and settlement locks remain; preserve their behavior. The recovery sweep's task restart-warning/reset write must also reload and recheck the expected pending request under the task lock before saving, because operation creation now deliberately relies on that common writer order. This is a focused writer audit, not a general protocol rewrite.

Lock order remains repository mutation lease (where already held), task row, then request/operation/notification writes. Monitor and terminal persistence never acquire the repository lease while holding a task lock. Audit and test this order to avoid introducing a resolver/monitor deadlock.

### D-3. Persist bounded failure evidence on the land request

Add the following nullable columns to `AgentTaskLandRequest`, map lengths in `AppDbContext`, and generate the migration and model snapshot with the EF CLI. Additive diagnostics do not change approval schema version 2 or its check constraints.

| Field | Bound / meaning |
|---|---|
| `TerminalFailureCode` | 100 ASCII characters; stable classifier for an unexpected terminal execution failure. |
| `FailureDiagnosticId` | Nullable GUID; identifies this handled failure across persisted request, event, notification and server log. |
| `FailureExceptionType` | 200 characters; simple exception type name, restricted to identifier characters; no message, stack or assembly-qualified generic arguments. |
| `SourceDiagnosticCommand` | 160 characters; fixed command template/operation label, not arbitrary argv or a repository path. |
| `SourceDiagnosticExitCode` | Nullable integer; only an actually returned process exit code. |
| `SourceDiagnosticCode` | 100 ASCII characters; generated safe code such as `git_exit_128`, `git_start_failed`, or `stderr_suppressed`. |
| `SourceDiagnosticExceptionType` | 200 characters; same type-name contract for a handled inspection I/O failure. |

Introduce a bounded classifier/formatter (`LandFailureDiagnostic` in Application, and an EF-free value shape if Domain needs one). Map known exception categories by type, never by arbitrary message matching:

| Condition | `TerminalFailureCode` |
|---|---|
| `DbUpdateConcurrencyException` | `landing_concurrency_conflict` |
| Guarded source baseline conflict, D-1 | `source_resolution_state_changed` |
| Other `DbUpdateException` | `landing_persistence_failed` |
| `TimeoutException` | `landing_timeout` |
| `IOException` | `landing_io_error` |
| `UnauthorizedAccessException` | `landing_access_denied` |
| Other unexpected exception, including cancellation without host shutdown | `landing_unexpected_exception` |
| Exception after durable publication | `landing_interrupted_after_publication` |

The exception type supplies detail without claiming a false specific cause. The concurrency code does not assert which tracked entity conflicted: the failing operation can include request, task or operation writes. Host shutdown cancellation retains the existing hosted-service exit/recovery behavior and creates no new terminal failure. Do not inspect arbitrary `Data`, SQL, inner messages, exception `ToString()` or stack text for public diagnostic content.

Generate the diagnostic ID once when the failure is handled, before the persistence attempt. Log it with task ID, expected request ID, attempt, exception type, code and available durable operation ID/phase, so it is useful even if terminal persistence fails. The standard log entry should use these bounded fields; do not expand raw exception messages into the new public path. Direct `FailAsync`/`FailRequestAsync` callers must get equivalent evidence, not depend on a hosted-service-only formatter. If a persistence error is logged too, use the same diagnostic ID and a distinct persistence-error type.

Apply diagnostics inside the locked terminal transaction after all reloads. For an unpublished failure, the request, one `LandRefused` terminal event, its warning, pending-marker clearing and one Outcome outbox record commit together. Preserve any already durable observed snapshot and ordinary `SourceRefusalReason`; do not set a fake source refusal for an execution exception. Do not manufacture SHAs or an operation from in-memory state whose save failed. If the database cannot commit, the request stays pending with no asserted terminal outcome; existing recovery retries apply and the bounded correlated log records the failed persistence attempt.

For a published operation, retain the existing publication/cleanup result and `landing_interrupted_after_publication` semantics. Attach diagnostic evidence to the request and its existing outcome shape without turning it into `LandRefused`, clearing verified/remote evidence, downgrading completed cleanup, or authorizing deletion. An already committed terminal event wins: a repeated failure callback must not replace its diagnostic, emit another outcome, or mutate an old outbox body/digest. A fresh explicit request gets fresh nullable fields; historical opaque records remain unknown rather than being backfilled from guessed log matches.

Rejected: only improving the log line; putting failure solely on a landing operation that may not exist; replacing ordinary source codes with generic exception codes; changing the delegate task's `Succeeded` status; or persisting unbounded exception messages.

### D-4. Carry safe Git inspection metadata without restoring stderr

Extend `LandSourceInspection` with an optional typed diagnostic, retaining its existing two-argument construction and `Accepted`/`Reason` semantics. Command failures directly available in `InspectAsync` (`status`, ignored-file listing, identity probes) carry a fixed command template, actual exit code and generated `git_exit_N`. For `RequiredAsync` failures used by identity inspection, a small `IOException` subtype carrying only that safe metadata can preserve the command identity through the current `identity_io_error` catch. Existing callers catching `IOException` continue to work.

Use fixed labels such as `git status --porcelain=v1`, `git ls-files --others --ignored`, `git rev-parse <identity>`, `git worktree list`, `git check-ref-format <ref>` and `filesystem canonicalization`. Dynamic refs, paths, endpoint strings, configuration values, stdout and arbitrary exception messages are omitted. Filesystem errors have no invented command exit code. Plain semantic refusals such as `source_dirty`, `wrong_repository` and `active_sequencer` need no synthetic Git diagnostic.

Carry the optional diagnostic from every resolver-consumed inspection, including the fresh inspection in operation creation, into the request's source-diagnostic columns with its actual refusal reason. Where resolver commit lookup already has a `LandingGitResult`, retain its safe command/exit metadata on `commit_lookup_failed`. A locally available typed destination exception may also pass through safely. Do not rewrite remote observation, push, verifier, cleanup or all Git result types to obtain more detail.

`LandingGit.ExecuteAsync` currently discards stderr intentionally. Keep that policy. D-4 does not expose raw or truncation-only stderr: truncation does not remove secrets. It also does not install a broad regular-expression scrubber and assume arbitrary hook output is safe. `SourceDiagnosticCode` is explicitly a generated diagnostic, never described as captured stderr; status can state that stderr was suppressed. A future richer stderr feature needs its own safe-content contract. This conservative limit still makes `status_error` distinguishable from a path-access exception and identifies the failing command and exit where those facts exist.

Bounds and identifier/template allowlists are enforced at production, not only by database max lengths. Test arbitrary exception messages, credential-shaped URLs, query values, line breaks, terminal escape characters and oversized diagnostics using synthetic markers. None may reach public DTO/event/notification/CLI output or the new correlated log fields.

### D-5. Expose existing and new evidence consistently

Append optional/defaulted fields to `LandRequestStatusDto` and its mapper; mirror them as optional fields in `client/src/api/agentTasks.ts`. Existing task GET remains the HTTP surface. Do not add a separate failure endpoint, new status column, or client UI feature.

`delegate.ps1 -Status` prints these conditional lines before the delegate report:

```text
Candidate source: <candidateSourceSha>
Source refusal: <sourceRefusalReason>
Land execution failure: <terminalFailureCode>; diagnostic <failureDiagnosticId>; exception <failureExceptionType>
Source inspection: <sourceDiagnosticCommand>; exit <sourceDiagnosticExitCode>; diagnostic <sourceDiagnosticCode>
Source inspection exception: <sourceDiagnosticExceptionType>
Landing reason: <landing.reason>
```

Print the operation's existing `reason` independently of source/exception fields, including on publication with residue. Missing optional fields from an older server are tolerated and do not print false failure lines. Keep approval, observed/resolved SHA, hold, reconciliation and receipt lines. The CLI must not infer publication from a reason, Succeeded delegate status, or all-null SHA fields.

For new terminal events, format the same bounded code, ID and type from the request plus already durable source SHA evidence. `LandNotificationPayload` snapshots those facts through the terminal event detail in the existing atomic outbox path. Normal source refusal detail gains safe command metadata when present. The notification header, source event linkage, publication/cleanup fields, content digest, delivery retry and transcript receipt semantics remain unchanged. No direct send or new polling loop is introduced. `ReplyTo=None` still creates its normal NotRequired receipt state and exposes diagnostics through task GET/status.

Example for a failure before any operation exists:

```text
land unconfirmed: landing_concurrency_conflict; diagnostic=<guid>; exception=DbUpdateConcurrencyException;
expected=<approved SHA>; local=null; remote=null; candidate=null
```

This is evidence that land execution failed before a durable source snapshot, not a claim that the local target never changed. Existing source refusals retain their existing code; a handled `status_error` is not relabeled as an unexpected exception.

Rejected: parsing historical event prose back into typed state; rewriting immutable existing outbox payloads; waiting for execution in `-Land`; or hiding the operation reason whenever a request reason is present.

## Implementation slices

Each slice includes its implementation tests. TestDesign adds the exact verification design and mutation controls to this same document before Code starts. Suggested new filenames below are deliberate seams, not a requirement to create duplicate frameworks.

| Slice | Production files / change | Tests and completion evidence |
|---|---|---|
| S1: Request checkpoints | New `server/Application/Services/AgentTaskLandRequestWriter.cs`; `AgentTaskLandSourceResolver.cs`; request result/call sites in `AgentTaskLandService.cs`. Implement typed patches, task lock, fresh guards and operation attachment. Align the recovery task write with this lock order. Keep monitor evaluation semantics. | New `Application/AgentTaskLandSourcePersistenceTests.cs`; extend `AgentTaskLandMonitoringTests.cs` and controlled harness only as required. Deterministic real-EF interleaving and stale request/progress/operation guards. |
| S2: Durable terminal diagnostics | `server/Domain/Entities/AgentTaskLandRequest.cs`, `server/Infrastructure/Data/AppDbContext.cs`, CLI-generated `server/Migrations/*` plus snapshot; new `server/Application/Services/LandFailureDiagnostic.cs`; `AgentTaskLandService.cs`, `server/Infrastructure/Orchestration/AgentTaskLandHostedService.cs`. Put diagnostic and terminal settlement in one guarded transaction. | New `Application/AgentTaskLandFailureDiagnosticTests.cs`; extend `AgentTaskLandPersistenceFailureTests.cs` and `AgentTaskLandApprovalPersistenceTests.cs`. Fresh-context persistence, atomic fault cuts, stale callback and publication-preservation cases. |
| S3: Safe inspection detail | `server/Application/Dtos/LandingDtos.cs`, `server/Infrastructure/Git/LandingGit.cs`, resolver diagnostic propagation; a small safe exception/value type if required. No broad Git interface rewrite. | Extend `Infrastructure/LandingGitTests.cs` and `Application/AgentTaskLandFailureDiagnosticTests.cs`. Test real inspection producer behavior via the existing overridable command seam, plus typed refusal propagation and suppression. |
| S4: HTTP/status/outbox | `server/Application/Dtos/LandRequestStatusDto.cs`, mapper use in `AgentTaskService.cs` if needed, `LandNotificationPayload.cs` only if needed beyond event-detail formatting, `scripts/delegate.ps1`, `client/src/api/agentTasks.ts`. | Extend `AgentTaskLandContractEndpointTests.cs`, `DelegateScriptLandStatusTests.cs`, `AgentTaskLandNotificationPersistenceTests.cs` / `AgentTaskLandNotificationRecoveryTests.cs`; genuine producer-to-GET-to-pwsh status test. |
| S5: Approval regressions and owner docs | Exercise Plan/TestDesign with explicit SHA, null Review evidence and null merge target; update `docs/ops-http.md` and the request/receipt section of `docs/orchestration-loop.md` with diagnostic fields, correlation and stderr limits. | Extend `AgentTaskLandApprovalRequestTests.cs`, `AgentTaskLandSourceFreshnessTests.cs` and endpoint/status cases; run the bounded affected regression set and ordinary Unit lane. |

S2 depends on S1's locked settlement contract. S3/S4 use the fields defined by S2. Land the implementation only after ordinary Code verification and separate Review; this Plan stage changes only this document.

## Verification requirements for TestDesign

TestDesign must add the canonical `## Verification design` section with exact V/R/PC identifiers, methods, commands, expanded argument cases and intended assertions. The following are mandatory design inputs, not claims that tests have been implemented or run.

### Deterministic race and write ownership

Use `LandingProtocolHarness`, controlled `ILandingGit`, `FakeTimeProvider`, and separate `AppDbContext` instances against one `TestDbFixture.CreateIsolatedSchemaAsync()` PostgreSQL schema. Do not use EF InMemory or just throw a synthetic concurrency exception as proof of the race.

Pause resolution after it loaded the request and before the Observed save, using the controlled inspection/observation hook and explicit completion barriers. In a second context execute the real `AgentTaskLandMonitorService.SweepAsync`, await its commit, and in a third context verify that the token and `LastEvaluatedAt` changed. Then release resolution. Bound barrier waits for fixture failures, release them in `finally`, and await every task; use no sleeps as ordering evidence.

Before the fix this ordering must produce the real `DbUpdateConcurrencyException` at source persistence. With the fix, inspect committed Observed evidence at a checkpoint before later resolution can mask it, then allow completion. Assert exact expected/local/remote/candidate SHAs, relationship, fingerprint and observation identity; unchanged approval; preserved monitor evaluation/reconciliation fields; no generic terminal refusal; and no extra Git inspection/replay attributable to the fix. Final resolution/publication and outbox counts must be checked from fresh contexts.

Run variants below threshold, at warning, and at error; retain exactly-once aged notifications and meaningful-progress clock behavior. Also stage the reverse ordering (resolver holds its short checkpoint lock, monitor waits) with a test-only EF command/save interceptor if the current Git hooks cannot reach it. The purpose is to establish both ordering and lack of a long transaction across Git.

Cover pending request replacement, terminal/canceled state, changed attempt, changed approval/coordinates, newer source observation and changed active operation between computation and save. A stale worker must stop without closing the replacement or overwriting new state. Exercise AdvanceStarted/child-start/child-clear/Resolved and operation-binding checkpoints so the fix cannot simply move the exception one save later. Preserve child journals and fail-before-dependent-mutation assertions.

### Failure producer to caller

Inject a real exception from the execution seam before an operation exists, after source evidence is committed, and after confirmed publication. Exercise the hosted drain's exception handoff at least once; a direct call to `FailRequestAsync` alone cannot prove that the actual catch passes the diagnostic correctly. Use `DbUpdateConcurrencyException` to test its classifier separately from the real monitor-race reproduction, plus a distinct I/O/timeout/generic type. Persist no synthetic secret markers carried in their messages.

Read the resulting request through a fresh context and the real task GET endpoint, invoke the actual `delegate.ps1 -Status` against that producer-generated HTTP state, and inspect the committed Outcome body. Assert the same code/diagnostic ID/type at all surfaces and in captured bounded log fields. A hand-authored JSON stub is useful for older DTO compatibility, but does not replace this chain. Use the existing guarded `LandContractWebAppFactory`/`AntiphonWebAppFactory`; drive the land service deliberately when hosted drain is disabled. Retain production-runner isolation.

Inject terminal-transaction failure before save, after save/before commit, at commit and after commit. Precommit cuts must leave zero terminal outcomes and no persisted terminal diagnostic/cleared pending marker; after-commit retry must preserve exactly one event/outbox/diagnostic. If storage remains unavailable, require correlated log evidence and a pending request, not an invented durable verdict. Include a replacement arriving before failure takes its task lock. Published cases retain the exact remote/verified receipt and appropriate cleanup state, never a refusal or new deletion authority.

For inspection refusal, use the real `LandingGit.InspectAsync` producer with controlled command failures for `status`, ignored-file listing, required identity commands and a filesystem exception. Prove code/template/exit/type propagation and distinguish actual exits from unknown values. Assert the raw synthetic stderr and hostile exception text never appear in request columns, JSON, events, notifications, CLI output or new log fields. Include zero/negative/large exit values and bounds where valid; never invent an exit for a launch failure. Ensure ordinary semantic reasons still work without diagnostics.

### Approval and presentation compatibility

Parameterize `AgentTaskRole.Plan` and `AgentTaskRole.TestDesign`, with `ReviewEvidenceId=null`, full explicit expected SHA and `MergeTargetRef=null`. Exercise admission and full land execution, including remote publication evidence. Verify `ApprovalKind=ExplicitCaller`, schema 2, retained expected SHA, default target `refs/heads/master`, and no `legacy_review_binding_required` refusal. Use controlled Git for the matrix plus a bounded real local bare-remote docs-only case for each role if required to establish the actual default-target integration; never land against a live repository in a test.

Retain rejection of missing/abbreviated SHA, mismatching supplied Review evidence, pending-identity changes and Mutation snapshot lands. Preserve source-freshness refusal and exact-SHA recovery cases already pinned by CARD-0488. Extend status tests with existing source-only, operation-only, combined and all-null/legacy fields, ensuring the actual report remains after the status facts. Keep `DelegateScriptLandCompatibilityTests` and `DelegateScriptLandApprovalTests` green: version/SHA fences and 202 acknowledgment remain unchanged.

### Execution shape and positive controls

Ordinary verification uses one producer-owned isolated build, the Unit lane, and the named affected integration classes in the slice table. Most of the concurrency matrix uses controlled Git and real isolated PostgreSQL; reserve process-heavy Git/HTTP/pwsh cases for their boundary assertions. Give every new class its correct Unit/Integration category and every process-spawning class the assembly-local `ParallelLimiter<ProcessSpawnLimit>`. Do not co-run the Pty assembly. Scope DB assertions to the owned request/task/schema.

Base recipe for Code, with fresh result directories per invocation and TestDesign's final class filters:

```powershell
dotnet build tests/Antiphon.Tests --property:OutputPath=bin-c498/ --nologo
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/*/*[Category=Unit]' --report-trx --report-trx-filename unit.trx --results-directory .antiphon/c498-unit
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/(AgentTaskLandSourcePersistenceTests*)|(AgentTaskLandFailureDiagnosticTests*)|(AgentTaskLandMonitoringTests*)/*' --report-trx --report-trx-filename request.trx --results-directory .antiphon/c498-request
```

TestDesign must name additional filters for S3-S5 and existing approval/recovery/notification boundaries; the example request filter is not the full regression set. Include ordinary persistence and protocol controls actually affected by the writer audit. Inspect fresh TRX method names and nonzero counts for every class; an exit-zero or discovery listing is not evidence. Run the duration tripwire and explain new slow boundary tests. Type-check the changed client contract; no browser feature or E2E sweep is required solely for optional type additions.

Positive controls should include removing the task lock/reload protection (the real monitor race returns), applying stale source fields after reload (newer evidence is overwritten), dropping the failure diagnostic during terminal reload/DTO mapping, restoring generic exception prose, omitting each requested CLI field, and discarding inspection command/exit metadata. Design independent guard/visibility controls rather than one giant test whose first failure masks later omissions. Define intended assertion failures and exact method filters. PCs execute in post-land Mutation under the current companion/SourceLanding protocol, after ordinary Code and separate Review; the new plan does not require pre-land mutation runs.

## Acceptance and handoff

Done for the implementation means the deterministic monitor interleaving succeeds with committed source evidence; stale work cannot overwrite or settle newer work; unexpected exceptions produce bounded durable diagnostics through HTTP/status/outbox; useful safe inspection metadata survives; and explicit-SHA non-reviewed Plan/TestDesign lands with null targets still publish correctly. Review must also check additive migration compatibility, lock order, atomic settlement/idempotence and the raw-diagnostic boundary.

No product decision is pending. The selected defaults are short coordinated checkpoints, additive request diagnostics, command/exit/type metadata with stderr suppression, and the existing asynchronous land contract. If TestDesign finds a checkpoint cannot be verified under these guards, return `next: plan` with the exact gap.

This document is authored in the authorized Shared canonical checkout on `master`; commit and push this exact plan path so the next stage can branch from it. It is not a succeeded Worktree task and does not require a self-directed `/land/v2` request. For the later implementation, the orchestrator uses the original Code task's approved SHA and normal land receipt, then the canonical deployment runbook if activation is required. Confirm `/api/version` contains the intended implementation before relying on live behavior. Plan publication itself requires no server restart.

## Verification design

Status: TestDesign complete at baseline `9ebbba77`; execution is pending Code (ordinary V/R) and post-land Mutation (PCs). Severity: **CRITICAL** (a monitor pass can fail a valid land and erase its source snapshot). This section verifies D-1 through D-5 without changing them. Every `C498_` method below is **to implement**; existing `C448_`/`C467_`/`C488_`/`C495_` names are retained coverage that must stay green.

### Inspection

Paths are repo-relative under `tests/Antiphon.Tests/` unless stated. "Read" means the whole body was read at the baseline, not a regression selection.

| Bodies / fixture sections read | Boundaries -> coverage |
|---|---|
| `server/Application/Services/AgentTaskLandSourceResolver.cs`: `ResolveAsync` (entry reload, child-recovery save, Observed/AdvanceStarted/child-start/child-clear/Resolved saves, both refusal helpers), `CreateOperationAsync` (Git-derived candidate, previous deactivation, attach), `SaveRequestAsync` (token rotation, no lock, no reload) | Seven exposed saves plus attach are the interleaving points -> V-1/V-2/V-3, R-1/R-2. The entry reload means a monitor pass *between* reload and any save is the incident; pause hooks are placed after the reload. |
| `AgentTaskLandMonitorService.SweepAsync`: task `FOR UPDATE`, reload, unconditional `LastEvaluatedAt` + token rotation, threshold-gated aged rows | Below/at-warning/at-error variants -> V-1; token rotation is the race precondition -> G-9/PC-9. |
| `AgentTaskLandService.cs`: `RunRequestAsync` (admission transaction, two direct source-reason saves at lines 275-296, protocol call), `FailRequestAsync` (drops `exception`, pre-lock `requestId` check, published branch), `PersistRefusalAsync`/`SettleLandedAsync` (locked terminal transaction, `CompleteRequest`, `AddNotification`, `ClearPending`), `SweepAsync` (unlocked restart-warning write at lines 548-556), `FormatSourceRefusal` | Exception path, stale settlement, direct saves, recovery write -> V-4/V-5/V-6, R-3/R-4. |
| `Infrastructure/Orchestration/AgentTaskLandHostedService.cs`: outer/inner catches, `OperationCanceledException when stoppingToken` break, `FailRequestAsync` handoff, "Could not persist" log | Hosted catch is the only real producer of the exception argument -> V-4, G-23. |
| `Infrastructure/Git/LandingGit.cs`: `ExecuteAsync` (stderr drained and discarded, `git_exit_N`, `git_start_failed`, `git_timeout`), `InspectAsync` catches (`identity_io_error`/`identity_inaccessible`/`invalid_identity`), `IdentityAsync` `RequiredAsync` calls, `CanonicalDirectoryAsync` IOException constants | Command/exit/type carriage and stderr policy -> V-7, G-24..G-27. |
| `AgentTaskLandingProtocol.TransitionAsync`/`SaveAsync` (task lock inside protocol transitions; request progress write) | Lock order audit: protocol already holds the lease when it takes the task lock -> G-8 oracle applies to resolver only; protocol transitions retained by existing checkpoint matrices. |
| `LandNotificationPayload.Create`, `LandRequestStatusDto.From`, `AgentTaskService.GetAsync` land projection (lines 1471-1509), `client/src/api/agentTasks.ts` `landRequest` shape, `scripts/delegate.ps1` `Status` block (lines 491-531) | Exposure surfaces -> V-8/V-9; each printed line is independently omittable -> G-30..G-36. |
| `AgentTaskLandRequest.cs`, `AppDbContext` land-request mapping (max lengths, `CK_AgentTaskLandRequests_V2Approval`/`_OidShape`, unique pending index), latest migration `20260912122418_AddCompletionNoteDeliveryStamp` | Additive columns/migration -> V-10, G-38. |
| `TestHelpers/LandingProtocolHarness.cs`: DI (`Clock` read at `BuildServices`, scoped `AppDbContext` with `SaveFault` + `TransactionFault`, no scoped `AgentTaskLandService`), `RunQueuedAsync`, `FailAsync`, `SaveFault` arming (`RequestResolution`, `TerminalCut`, `EventKind`, `AfterSaveAcknowledged`, `AfterAcknowledged`), `TransactionFault` (committing/committed only) | Controlled real-EF harness for V-1..V-6. Missing setup: `OnTransactionStarted` hook, hosted-service registration helper, recording logger. |
| `TestHelpers/ControlledLandingGit.cs`: hooks `BeforeInspection`, `OnSourceObservation(attempt)`, `OnAncestorCheck` (synchronous `GetAwaiter().GetResult()`, not used for barriers), `BeforeCommand` (returns injected result, fires before the owned `started` callback), `AfterCommand`, `OwnedTrace`; `InspectAsync` is in-memory and never calls `RunAsync` | Pause points per checkpoint -> V-2 table. Missing setup: injected inspection diagnostic by call index. |
| `TestHelpers/LandingSafetyHarness.cs`, `LandingGitFixture.cs` (`FixtureGit.BeforeCommand` may throw; `ConfigureProcess` env; independent observer clone; `AssertRemoteSourceAsync`) | Real-Git rows for V-7/V-11 and one real exception body for V-8. |
| `Application/AgentTaskLandMonitoringTests.cs` (`C467_V15_ThresholdsUseMeaningfulProgress` competing sweeps, `LastEvaluatedAt` pin), `AgentTaskLandPersistenceFailureTests.cs` (`C467_V06_AtomicSettlementFaultMatrix` four cuts, `C448_V23_*` published-failure retention), `AgentTaskLandNotificationPersistenceTests.cs` (`C467_V07_ConcurrentSettlementAndExplicitCleanup` double `FailAsync`, `C467_V05_OutcomeObligationMatrix` outcome body), `AgentTaskLandNotificationRecoveryTests.cs` (`C488_PreOperationReceiptUsesRequest` body reaches queue), `AgentTaskLandRequestTests.fail_async_writes_land_failed_and_clears_the_pending_row` (asserts generic prose; must be updated to the typed detail, not deleted), `AgentTaskLandSweepTests` (bare service, no hooks), `AgentTaskLandRecoveryTests` (worker death, restart reconciliation), `AgentTaskLandSourceFreshnessTests` (hook usage patterns, `RefuseAtPhaseAsync`), `AgentTaskLandConcurrencyControlledTests`, `AgentTaskLandApprovalRequestTests` (`SeedSucceededWorktreeAsync` seeds Role=Code, no MergeTargetRef), `AgentTaskLandApprovalPersistenceTests` (`C488_MigrationDoesNotInventApproval` downgrade/upgrade recipe), `AgentTaskLandContractEndpointTests` + `LandContractSeeds` + `LandContractWebAppFactory` (in-memory TestServer, drain/sweep removed), `DelegateScriptLandStatusTests`/`DelegateScriptLandApprovalTests` + `LandApiStub`/`DelegateScriptRunner` (real pwsh, loopback replay), `Infrastructure/LandingGitTests.cs` (`C448_V05_*` inject `synthetic-secret-marker` diagnostics) | Retained regressions R-1..R-8 and nearest fixtures for every new file. |
| `tests/Antiphon.E2E/AgentTaskLandDeliveryE2ETests.cs` (`C467_V29_RealOutcomeProducerMatrix`, `C467_V23_BusyCallerDoesNotBlockAnotherLand` busy caller) and `Fixtures/LandDeliveryFixture.cs` (`InitializeAsync(busy)`, `ArrangeOutcomeAsync`, `RequestAsync` via real `delegate.ps1`, `ReceiptAsync` complete-UserPrompt oracle, `AssertOnePromptAsync`, `StatusAsync`) | Recipient evidence for the changed outcome body -> V-12, G-40. Missing setup: `execution-exception` arrangement. |
| `docs/testing-and-build.md` PC section (method-scoped red/green, batching by file/method, `bin-<name>/`), `scripts/test-duration-tripwire.ps1` | Cost and execution shape below. |

Missing setup to implement (test-harness extensions, not production seams): `ControlledLandingGit.InspectionCalls` counter and `InjectInspection: Func<int, LandSourceInspection?>` (returns a refusal with an attached diagnostic for a given call index); `SaveFault.OnTransactionStarted: Func<DbContext, Task>?` raised from a new `TransactionFault.TransactionStartedAsync` override; `LandingProtocolHarness.AddHostedLandService()` registering scoped `AgentTaskLandService` through `CreateLand` so a test-constructed `AgentTaskLandHostedService` uses the harness DI; `RecordingLogger<T>` capturing structured state pairs; `LandingSafetyHarness.AddDocsOnlySourceAsync()` and `SetRoleAsync(role, mergeTarget)`; `LandDeliveryFixture.ArrangeOutcomeAsync("execution-exception")` (creates `<Root>/not-a-repo` and repoints `RepoPath`); a test-only `LandingGit` subclass whose `ConfigureProcess` points `GIT_CONFIG_GLOBAL` at a malformed file whose directory name carries the marker. Barrier helper: two `TaskCompletionSource`s (`paused`, `release`), every wait bounded by `WaitAsync(TimeSpan.FromSeconds(30))`, `release.TrySetResult()` in `finally`, every started task awaited; no `Task.Delay` is ordering evidence.

Boundary combinations covered: monitor age below/at-warning/at-error x pause point (V-1/V-2); every stale dimension x the Observed checkpoint (V-3); newer-progress kind x Observed (V-3); exception seam x exception type (V-4); terminal cut x retry (V-5); command x exit value including 0/negative/`int.MinValue`/`int.MaxValue` (V-7); role x relationship x controlled/real Git (V-11); busy/idle recipient (V-12). Excluded combinations are listed under Out of scope.

### Delivery inventory

| Producer -> destination | Durable join / persistence boundary | Recovery and observable receipt |
|---|---|---|
| Resolver checkpoint (Observed/AdvanceStarted/child/Resolved/refusal/attach) -> request row | Request ID + attempt + approval identity; short task-locked transaction; token rotated by the writer | Stale worker stops (`StaleRequest`), newer evidence retained (`source_resolution_state_changed`); V-1/V-2/V-3. No delivery leaves this hop; a committed Observed row is the receipt. |
| Hosted drain exception -> `FailRequestAsync` -> terminal `LandRefused` event + Warning + Outcome outbox + `ClearPending` | Execution identity (request ID, attempt) captured at admission; diagnostic ID generated before persistence; one locked transaction; unique terminal index per request; unique `SourceEventId` per notification | Cuts before-save/after-save/commit leave zero terminal rows and a pending request with correlated log; after-commit retry keeps exactly one of each; replacement/attempt change before the lock stops settlement (V-5). Terminal insert alone is not delivery acceptance. |
| Published-then-failed -> existing `Landed`/`LandedWithResidue` settlement | Operation ID + publication receipt already durable; diagnostic attached to the request without a new terminal type | Retains remote/verified evidence and cleanup state; no `LandRefused`; no deletion authority (V-4b). |
| Outcome outbox -> notification worker -> `SessionQueuedMessage` -> caller session prompt | Notification ID -> `SourceLandNotificationId` (unique) -> transcript `UserPrompt` with `ConfirmingPromptSequence` above the delivery baseline | Unchanged transport (CARD-0467/0488), but the body content changes. V-12 runs the real Program/queue/runner/native FakeGrok: busy recipient and already-eligible recipient, each ending in `LandDeliveryFixture.ReceiptAsync` (complete body via `PromptSubmissionMatch.IsCompleteIn`, correct session, sequence strictly above `LastDeliveryBaselineSequence`) and `AssertOnePromptAsync`. Existing `C467_V25_HardCrashAfterOutcomeCommitRecoversReceipt`/`C467_V26_HardCrashAfterQueueInsertReusesRow`/`C467_V31_EnqueueFailureRecoversAutomatically` retain the crash/enqueue-failure recovery at each handoff for the unchanged transport. |
| Request/operation rows -> task GET -> `delegate.ps1 -Status` | Same request ID and diagnostic ID | V-8/V-9. Substitute declared: inside `Antiphon.Tests` the real GET JSON is captured from the in-memory TestServer and replayed byte-for-byte through `LandApiStub` to the real script; that cannot prove Kestrel transport or auth, which this card does not change. V-12's `StatusAsync` runs the real script against the real HTTP host. Polling status never discharges a receipt. |

Substitutes and what they cannot prove: controlled `ILandingGit` proves interleaving, guards and disposition, not real process/journal behaviour (real-Git rows in V-7/V-11 and the retained CARD-0448 crash matrices cover that); synthetic `DbUpdateConcurrencyException` thrown from a hook proves the classifier, not the race (V-1 proves the race with the real monitor); hand-authored status JSON in V-9 proves CLI formatting and older-server tolerance, not production; a recorded logger proves bounded fields reached `ILogger`, not sink formatting.

### Proves it works now

Class aliases (namespace `Antiphon.Tests.Application` unless stated): **SP** `Application/AgentTaskLandSourcePersistenceTests.cs` (new, `[Category("Integration")]`, `LandingProtocolHarness`, `FakeTimeProvider` assigned to `h.Clock` before `InitializeAsync`); **FD** `Application/AgentTaskLandFailureDiagnosticTests.cs` (new, Integration, `LandingProtocolHarness` + test-constructed `AgentTaskLandHostedService`); **LF** `Application/LandFailureDiagnosticTests.cs` (new, `[Category("Unit")]`, pure classifier/formatter/DTO); **LG** `Infrastructure/LandingGitTests.cs` (extend; `Antiphon.Tests.Infrastructure`; `[ParallelLimiter<ProcessSpawnLimit>]`); **CE** `AgentTaskLandContractEndpointTests.cs` (extend; `LandContractWebAppFactory`, `[NotInParallel]`); **SS** `DelegateScriptLandStatusTests.cs` (extend; real pwsh); **AR** `AgentTaskLandApprovalRequestTests.cs`; **AP** `AgentTaskLandApprovalPersistenceTests.cs`; **SF** `AgentTaskLandSourceFreshnessTests.cs`; **NP** `AgentTaskLandNotificationPersistenceTests.cs`; **MT** `AgentTaskLandMonitoringTests.cs`; **DE** `tests/Antiphon.E2E/AgentTaskLandDeliveryE2ETests.cs` (`Antiphon.E2E`). Marker string for every hostile input: `synthetic-secret-marker://user:pw@host/?q=1` followed by a newline, ESC (0x1B) `[31m`, and 5,000 filler characters.

| ID | Layer and exact methods | Setup and decisive oracle |
|---|---|---|
| V-1 | Real EF interleaving: `SP.C498_MonitorPassDuringObservedInspection(int ageSeconds)` rows 0, 300, 900 | Equal path (`expectedSourceSha = h.Git.SourceHead`). Pause at `OnSourceObservation(1)` (after entry reload, before the Observed save). Record the request token from a fresh context. Run the real `AgentTaskLandMonitorService.SweepAsync` in a second `h.CreateContext()` with `FakeTimeProvider` advanced `ageSeconds` past `LastProgressAt` and default `DelegationSettings`; await it. Third context: token changed, `LastEvaluatedAt == monitorNow`, `WarningAt`/`ErrorAt` and `Aged` notification count 0/1/2 per row. Release. Inside `OnSourceObservation(2)` a fresh context must already see the committed Observed row: `SourceResolutionState == Observed`, `LocalBeforeSha == SourceHead`, `RemoteSourceSha == h.Git.RemoteSource`, `CandidateSourceSha == expected`, `SourceRelationship == Equal`, `RemoteSourceFingerprint == h.Git.Fingerprint`, `SourceObservationRef` starts with `refs/antiphon/land/{task:N}/{request:N}/source-observed/`, `SourceObservedAt == clock`, `LastEvaluatedAt == monitorNow` (monitor-owned field preserved), `LastProgressAt == checkpoint clock`. After completion: `run == Complete`; operation `Publication == Landed`, `Cleanup == Complete`; request `ResolvedSourceSha == expected`, `ExpectedSourceSha`/`ApprovalKind`/`ReviewEvidenceId` unchanged, `WarningAt`/`ErrorAt` preserved, aged count unchanged (exactly once), `TerminalFailureCode == null`, no `LandRefused`; `h.Git.SourceObservationAttempts == 2` and `h.Git.InspectionCalls == 2` (no replay). Pre-fix this ordering throws `DbUpdateConcurrencyException` from `RunQueuedAsync`. |
| V-2 | Every checkpoint: `SP.C498_MonitorPassAtEveryCheckpoint(string checkpoint)` rows `observed`, `advance-started`, `child-started`, `child-cleared`, `resolved`, `operation`, `refuse-observed`, `refuse-plain` | Behind path (`expected = h.Git.AdvanceRemoteSource()`) except `refuse-*`. Pause points: `observed` = `OnSourceObservation(1)`; `advance-started` = `BeforeInspection` call 2; `child-started` = `BeforeCommand` for `merge --ff-only` (fires before the owned `started` callback); `child-cleared` = `AfterCommand` for the merge; `resolved` = `OnSourceObservation(4)`; `operation` = `BeforeInspection` call 4; `refuse-observed` = `OnSourceObservation(1)` with `h.Git.DivergeRemoteSource()` (`RefuseObservedAsync`, `source_remote_diverged`); `refuse-plain` = `BeforeInspection` call 1 after writing `keep.txt` dirty (`RefuseAsync`, `source_dirty`). Monitor sweep during each pause (age 0), await, release. Progress rows: `Landed`, `Resolved`, `h.Git.SourceHead == expected`, one terminal event; refusal rows: exact `SourceRefusalReason`, `TerminalFailureCode == null`, one `LandRefused`; all rows: `LastEvaluatedAt == monitorNow`, `SourceAdvanceChild*` null at the end, no `DbUpdateConcurrencyException`. |
| V-2b | Lock scope and reverse ordering: `SP.C498_ResolverCheckpointDoesNotHoldLockAcrossGit(string hook)` rows `inspection`, `observation`, `ancestor-command`; `SP.C498_MonitorWaitsForResolverCheckpoint` | First: inside the named Git hook run a monitor sweep in a fresh context with `WaitAsync(TimeSpan.FromSeconds(10))`; it must complete and rotate the token (no task lock is held across Git). Second: `h.Fault.AfterSaveAcknowledged` filtered to a save whose `AgentTaskLandRequest` entry has `SourceResolutionState` modified to `Observed` asserts `ctx.Database.CurrentTransaction != null`, then starts the monitor sweep task and returns without awaiting it; the resolver commits; the test awaits the sweep. Oracle: the sweep completes without `DbUpdateConcurrencyException` (it blocked at the task `FOR UPDATE` and reloaded after the commit) and the final row has `SourceResolutionState == Observed` with `LastEvaluatedAt == monitorNow`. Without the task lock the monitor blocks on the row UPDATE instead and fails its own token check. |
| V-3 | Stale and newer-progress guards: `SP.C498_StaleRequestGuards(string change)` rows `replaced`, `canceled`, `terminal`, `attempt`, `approval-sha`, `approval-evidence`, `approval-kind`, `approval-filter`, `coordinates-target`, `coordinates-source`; `SP.C498_NewerSourceProgressConflict(string change)` rows `newer-observation`, `child-checkpoint`, `resolved`, `operation`; `SP.C498_StaleOperationAttachAbandonsCandidate(string change)` rows `replaced-request`, `other-operation-bound`, `approval-changed`; `SP.C498_ChildStartCallbackConflictFailsCallback` | Pause at `OnSourceObservation(1)` (attach rows: `BeforeInspection` call 2 in the Equal path, after the Git-derived candidate; callback row: `BeforeCommand` for the merge in the Behind path). Mutate from a fresh context with token rotation: `replaced` cancels the current request and inserts a new pending one with task mirrors; `terminal` inserts a terminal event and sets `TerminalEventId`; `attempt` bumps `request.Attempt`/`task.LandAttempt`; approval rows change one identity field each; coordinates rows change one snapshot each; newer-progress rows write the newer Observed/child/Resolved fields or insert an active `AgentTaskLanding` bound to the request. Release, record `traceAtRelease = h.Git.Trace.Count`. Stale rows: `RunQueuedAsync` returns `Complete` without throwing; `h.Git.Trace.Count == traceAtRelease` and no `merge` in `OwnedTrace` after release; mutated values retained; current/replacement request still `IsPending` with `SourceResolutionState == None`, zero terminal events on any request of the task, zero Outcome rows, `TerminalFailureCode == null`. Newer-progress rows: `TerminalFailureCode == "source_resolution_state_changed"`, `FailureDiagnosticId != null`, one `LandRefused`, one Outcome row, injected newer values unchanged (never the stale patch), no `merge`/`fetch` after release. Attach rows: `AgentTaskLandings.Count(task)` equals the injected count, `task.ActiveLandingId` unchanged, replacement `LandingOperationId == null`, no `push`. Callback row: `Should.ThrowAsync` from `RunQueuedAsync`; `h.Git.SourceHead == h.Git.SeedSha` (merge never dispatched), no operation, replacement pending and untouched. |
| V-3b | Recovery sweep write: `SP.C498_RecoverySweepWriteReloadsUnderLock(string change)` rows `replaced`, `canceled` | Seed the harness request pending with `LandStartedAt` set and `Attempt == task.LandAttempt == 1`. Arm `h.Fault.OnTransactionStarted` (first transaction after arming): from a fresh context replace the request (cancel + new pending, `LandStartedAt == null`, `LandAttempt == 0`) or set the task `Canceled`. `h.SweepAsync()` completes without exception; no Warning event containing `Land attempt 1`; `replaced`: task mirrors equal the replacement, exactly one queue entry whose `RequestId` is the replacement; `canceled`: nothing enqueued, request canceled with `task_no_longer_eligible`. |
| V-4 | Hosted drain exception producer: `FD.C498_HostedDrainClassifiesExecutionException(string seam, string type)` seams `before-operation` (`BeforeInspection` call 1 throws), `after-observed` (`OnSourceObservation(2)` throws); types `io` (`IOException`), `timeout` (`TimeoutException`), `unauthorized` (`UnauthorizedAccessException`), `concurrency` (`DbUpdateConcurrencyException`), `persistence` (`DbUpdateException`), `generic` (`InvalidOperationException`), `canceled-not-shutdown` (`OperationCanceledException(new CancellationToken(true))`) | `h.AddHostedLandService()`; `h.RequestAsync(expected)` enqueues; `new AgentTaskLandHostedService(h.Queue, scopeFactory, recordingLogger).StartAsync`; bounded wait (30 s) for a terminal event; `StopAsync`. Fresh context: `TerminalFailureCode` equals the D-3 table (`landing_io_error`, `landing_timeout`, `landing_access_denied`, `landing_concurrency_conflict`, `landing_persistence_failed`, `landing_unexpected_exception`, `landing_unexpected_exception`); `FailureExceptionType` equals the simple type name; `FailureDiagnosticId != null`; `SourceRefusalReason == null`; `State == Completed`, `IsPending == false`, `task.LandRequestedAt == null`; terminal `LandRefused` whose `Detail` starts `land unconfirmed: <code>; diagnostic=<id:N>; exception=<type>;` and contains `expected=<E>; local=<L>; remote=<R>; candidate=<C>` (nulls literal `null` for `before-operation`; the committed Observed SHAs for `after-observed`, which also keeps `SourceResolutionState == Observed`); one Warning; one Outcome row (`NotRequired`) whose `Body` contains the same triple; zero `AgentTaskLanding` rows for `before-operation`. Recording logger: one record with state pairs `TaskId`, `RequestId`, `Attempt == 1`, `ExceptionType`, `Code`, `DiagnosticId` equal to the persisted values. No persisted column, event, outbox body or bounded log value contains the marker, a newline or ESC; `FailureExceptionType.Length <= 200`, `TerminalFailureCode.Length <= 100`. |
| V-4b | Published then failed: `FD.C498_FailureAfterPublicationRetainsPublication(string kind)` rows `landed`, `already-present` | `h.Fault.AfterAcknowledged` throws `IOException(marker)` at `LandPhase.PublicationConfirmed` (after that commit). Hosted drain as V-4. Terminal type `Landed`/`LandedWithResidue`/`AlreadyPresent` (never `LandRefused`); `op.LastReason == "landing_interrupted_after_publication"`, `op.Publication` retained, `RemoteConfirmedAt`/`ObservedRemoteTargetSha` retained, `h.Git.RemoteTarget == op.VerifiedSourceSha` (or seed for already-present); request `TerminalFailureCode == "landing_interrupted_after_publication"`, `FailureExceptionType == "IOException"`, `FailureDiagnosticId != null`; no `worktree remove` after the throw; `Directory.Exists(h.Git.Source)`; one Outcome row with `publication=Landed|AlreadyPresent`. |
| V-5 | Terminal transaction cuts and identity: `FD.C498_TerminalTransactionFaultMatrix(string cut)` rows `before-save`, `after-save`, `commit`, `after-commit`; `FD.C498_ReplacementBeforeFailureLock(string change)` rows `replaced`, `attempt`; `FD.C498_RepeatedFailureCallbackIsIdempotent`; `FD.C498_DirectFailAsyncGetsEquivalentEvidence(string type)` rows `timeout`, `io`, `generic` | Cuts: `h.Fault.TerminalCut = cut; h.Fault.EventKind = LandRefused`; `BeforeInspection` call 1 throws `IOException`; hosted drain logs "Could not persist land failure" with the same `DiagnosticId` and `PersistenceErrorType == "InjectedSaveFailure"`. Precommit rows: `IsPending`, `TerminalEventId == null`, `TerminalFailureCode == null`, `FailureDiagnosticId == null`, `LandRequestedAt != null`, zero terminal events, zero Outcome rows; then `h.Fault.TerminalCut = null; h.Git.BeforeInspection = null; RestartServicesAsync; SweepAsync; RunQueuedAsync` lands normally with `TerminalFailureCode == null`. `after-commit`: exactly one event/outbox/diagnostic; `RestartServicesAsync; h.FailAsync(new IOException("retry"))` keeps count 1 and the first `FailureDiagnosticId`. Identity: `OnSourceObservation(1)` throws and arms `OnTransactionStarted`, whose first firing replaces the request or bumps the attempt from a fresh context; zero terminal events on any request, replacement pending, `TerminalFailureCode == null` everywhere, log record has `Attempt == 1`. Idempotent: after V-4 `io`, `h.FailAsync(new InvalidOperationException("second"))` keeps one event, one outbox row, unchanged `FailureDiagnosticId`/`FailureExceptionType`/`Body`/`ContentDigest`. Direct: `h.RequestAsync(); h.FailAsync(new TimeoutException(marker))` without a run gives `landing_timeout`/`TimeoutException`/non-null ID and the same event/outbox shape, no marker. |
| V-6 | Shutdown: `SP.C498_HostShutdownDuringResolutionLeavesRequestPending` | Hosted drain paused at `OnSourceObservation(1)`; call `hosted.StopAsync` (not awaited), then release; the next `ct.ThrowIfCancellationRequested()` in the controlled Git raises with the stopping token; await stop. Request `IsPending`, zero terminal events, `TerminalFailureCode == null`, `FailureDiagnosticId == null`, `LandRequestedAt != null`, zero Outcome rows. |
| V-7 | Real inspection producer: `LG.C498_InspectionCarriesCommandAndExit(string command, int exit)` rows (`status`, 0) accepted control, (`status`, 1), (`status`, 128), (`status`, -1), (`status`, int.MinValue), (`status`, int.MaxValue), (`ls-files`, 128); `LG.C498_RequiredCommandFailureKeepsCommandIdentity(string command)` rows `worktree list`, `rev-parse --absolute-git-dir`, `rev-parse --git-common-dir`; `LG.C498_FilesystemFailureHasNoInventedExit(string kind)` rows `missing-worktree`, `unauthorized`, `hostile-io`, `start-failed`; `LG.C498_RealStderrStaysSuppressed`; `LG.C498_SemanticRefusalsCarryNoDiagnostic(string reason)` rows `source_dirty`, `active_sequencer`, `source_equals_target`, `wrong_repository` | `LandingGitFixture`; `FixtureGit.BeforeCommand` injects `new LandingGitResult(exit, "", marker)` for the named verb. Command rows: `Reason` `status_error`/`ignored_status_error`, `Diagnostic.Command` `git status --porcelain=v1`/`git ls-files --others --ignored`, `ExitCode == exit`, `Code == $"git_exit_{exit}"`, `ExceptionType == null`. Required rows: `Reason == identity_io_error`, `ExceptionType == "LandingGitCommandException"`, `Command` `git worktree list`/`git rev-parse <identity>`, `ExitCode == 128`, `Code == git_exit_128`. Filesystem rows: `missing-worktree` (coordinates with a nonexistent `WorktreePath`) `ExceptionType == "IOException"`, `Command == "filesystem canonicalization"`, `ExitCode == null`, `Code == "path_missing_or_inaccessible"`; `unauthorized` (`BeforeCommand` throws `UnauthorizedAccessException(marker)`) `identity_inaccessible`, `ExceptionType == "UnauthorizedAccessException"`, exit/command/code null; `hostile-io` (`BeforeCommand` throws `IOException(marker)`) `identity_io_error`, `ExceptionType == "IOException"`, exit/code null; `start-failed` (`BeforeCommand` throws `IOException("git_start_failed")`) `Code == "git_start_failed"`, `ExitCode == null`. Stderr: the config-error subclass runs real git; the first `RequiredAsync` fails exit 128; `Reason == identity_io_error`, `Code == git_exit_128`, and neither `LandingGitResult.Diagnostic`/`Output` nor the inspection diagnostic contains the marker. Semantic rows: `Diagnostic == null`. Every row: `AssertRemoteSourceAsync`. |
| V-7b | Diagnostic carriage into the request: `FD.C498_InspectionDiagnosticPropagatesToRequest(string arm)` rows `status_error`, `identity_io_error`, `identity_inaccessible`, `source_dirty`, `operation-inspection`, `commit_lookup_failed` | `h.Git.InjectInspection` returns the refusal with diagnostic on call 1 (`operation-inspection`: call 2 only); `commit_lookup_failed`: `BeforeCommand` returns exit 128 for `rev-parse --verify refs/heads/master^{commit}`. After `RunQueuedAsync`: `SourceRefusalReason` equals the arm's reason; `SourceDiagnosticCommand`/`ExitCode`/`Code`/`ExceptionType` exact (`source_dirty`: all null; `commit_lookup_failed`: `git rev-parse <identity>`, 128, `git_exit_128`); `TerminalFailureCode == null` and `FailureDiagnosticId == null` (a handled refusal is not an exception); terminal `Detail` and Outcome `Body` contain `command=<template>; exit=<n>; diagnostic=<code>` when present and never the marker. |
| V-8 | HTTP + outbox from a real producer: `CE.C498_TaskGetExposesFailureDiagnostics(string producer)` rows `hosted-catch`, `direct`; `CE.C498_TaskGetExposesInspectionDiagnostics`; `NP.C498_FailureOutcomeBodyCarriesDiagnostic` | CE: seed via `LandContractSeeds.SeedSucceededWorktreeAsync` with `RepoPath` repointed to a test-created empty temp directory; `POST /land/v2` -> 202; `Release`; `hosted-catch` constructs `AgentTaskLandHostedService` with the factory's `IServiceScopeFactory` (the factory removed the registration) and starts it: the real `RepositoryMutationLease.TryAcquireAsync` -> `LandingGit.CommonDirectoryAsync` fails exit 128 -> `IOException("git_exit_128")` escapes `RunRequestAsync` -> real catch -> `landing_io_error`; `direct` resolves `AgentTaskLandService` from a scope and calls `FailRequestAsync(task, requestId, new IOException(marker))`. `GET /api/agent-tasks/{id}`: `landRequest.terminalFailureCode == "landing_io_error"`, `failureDiagnosticId` equals the row, `failureExceptionType == "IOException"`, `sourceRefusalReason == null`, `landing == null`; capture the JSON and replay it through `LandApiStub.Compatible(taskStatusBody: json)` to the real `delegate.ps1 -Status`: output contains `Land execution failure: landing_io_error; diagnostic <id>; exception IOException` and `Publication: Unconfirmed; cleanup: NotStarted`; the Outcome row body carries the same triple; no marker anywhere. Inspection projection: seed a completed request row with all four `SourceDiagnostic*` columns and read them back through GET (`sourceDiagnosticCommand`, `sourceDiagnosticExitCode`, `sourceDiagnosticCode`, `sourceDiagnosticExceptionType`); declared projection-only. NP: `LandingSafetyHarness`, `Fixture.Git.BeforeCommand` throws `InvalidOperationException(marker)` on the first `ls-remote`; `RunAsync` throws; `h.FailAsync(ex)`; Outcome body contains `landing_unexpected_exception; diagnostic=<id:N>; exception=InvalidOperationException` and an approval line equal to the row's `ExpectedSourceSha`/`LocalBeforeSha`/`RemoteSourceSha`/`CandidateSourceSha` (`null` literal where null); `AssertRemoteSourceAsync`. |
| V-9 | CLI presentation: `SS.C498_StatusPrintsDiagnosticLines(string scenario)` rows `exception-only`, `source-only`, `combined`, `operation-reason`, `residue-with-reason`, `legacy-fields-absent`; retained `SS.C467_V18_StatusAndAcceptance` (11 rows), `DelegateScriptLandApprovalTests.C488_StatusShowsDistinctSourceFacts` | Hand-authored JSON (declared compatibility stubs). Exact lines from D-5 with the given values; every fact line precedes `EXACT REPORT AFTER FACTS`; `exception-only` prints `Land execution failure:` and neither `Source inspection` line; `source-only` prints `Source refusal:`, `Source inspection:` and `Source inspection exception:` and no `Land execution failure`; `combined` prints `Candidate source:`, `Source refusal:`, `Land execution failure:` and `Source inspection:`; `operation-reason` (`landing.publication == Unconfirmed`, `reason == target_changed`, request exception present) prints both `Land execution failure:` and `Landing reason: target_changed`; `residue-with-reason` (`Landed`, `cleanup Refused`, `reason cleanup_refused`) prints `Landing reason:` and no `Land execution failure`; `legacy-fields-absent` (older server: none of the new fields, `landing == null`) prints none of the six new lines and still prints `Land:`/`Publication: Unconfirmed`. Retained `-Land` rows: 202 acknowledgment, no version probe on `-Status`. |
| V-10 | Persistence and migration: `AP.C498_DiagnosticColumnsRoundTrip`; `AP.C498_MigrationIsAdditive`; `AP.C498_ColumnBoundsEnforcedByDatabase`; `LF.C498_DtoMapsDiagnosticFields`; `LF.C498_PayloadSnapshotsDiagnosticFromEvent` | Round trip at max lengths through a fresh context. Migration: downgrade an owned database to the migration before `*_AddLandFailureDiagnostics`, insert a legacy request via raw SQL (recipe from `C488_MigrationDoesNotInventApproval`), upgrade twice; all seven new columns null, approval fields and `SchemaVersion` unchanged, both `CK_AgentTaskLandRequests_*` constraints still present in `pg_constraint`, `db.Database.HasPendingModelChanges() == false`. Bounds: raw insert of a 101-character `TerminalFailureCode` fails with SQL state `22001`. DTO: `LandRequestStatusDto.From` maps all seven; `JsonSerializerDefaults.Web` names `terminalFailureCode`, `failureDiagnosticId`, `failureExceptionType`, `sourceDiagnosticCommand`, `sourceDiagnosticExitCode`, `sourceDiagnosticCode`, `sourceDiagnosticExceptionType`; absent values serialize null. Payload: body contains the triple from the event detail; digests differ when the code differs. |
| V-10b | Classifier and bounds (Unit): `LF.C498_ClassifierMapsByType(Type exceptionType, string code)` rows for every D-3 line plus `FileNotFoundException -> landing_io_error`, `DbUpdateConcurrencyException -> landing_concurrency_conflict` (wins over `DbUpdateException`), `TaskCanceledException -> landing_unexpected_exception`; `LF.C498_ExceptionTypeNameIsBoundedIdentifier` (generic type name with backtick -> identifier-only; 250-character nested class name -> length 200; message never included); `LF.C498_CodeAndCommandBounds` (`git_exit_{int.MinValue}` fits 100; unknown template -> null; newline/ESC/URL-shaped content rejected); `LF.C498_FormatTerminalDetail` (exact `land unconfirmed: landing_concurrency_conflict; diagnostic=<id:N>; exception=DbUpdateConcurrencyException;` then `expected=<E>; local=null; remote=null; candidate=null`; source refusal with metadata `status_error expected=... local=... remote=... candidate=...; command=git status --porcelain=v1; exit=128; diagnostic=git_exit_128`) | Pure; no database. |
| V-11 | Approval compatibility: `AR.C498_NonReviewedRolesAdmitExplicitSha(AgentTaskRole role)` rows `Plan`, `TestDesign`, `Mutation`; `CE.C498_PlanAndTestDesignExplicitShaAdmitted(AgentTaskRole role)` rows `Plan`, `TestDesign`; `SF.C498_NonReviewedRoleLandsWithDefaultTarget(AgentTaskRole role, string relationship)` rows Plan/TestDesign x `equal`/`behind`; `SF.C498_NonReviewedRoleLandsDocsOnlyAgainstRealRemote(AgentTaskRole role)` rows `Plan`, `TestDesign` | Seed role with `MergeTargetRef == null`, `ReviewEvidenceId == null`, full 40-hex SHA. AR/CE: `queued`/202; row `ApprovalKind == ExplicitCaller`, `SchemaVersion == 2`, `ExpectedSourceSha` retained, `ReviewEvidenceId == null`, `TargetFullRefSnapshot == "refs/heads/master"`; `Mutation` row throws `ConflictException("verification_publication_forbidden")` (CE: 409); retained abbreviated/missing SHA and mismatched-evidence rows stay red-on-input. SF controlled: `RunQueuedAsync == Complete`; operation `TargetFullRef == DestinationFullRef == "refs/heads/master"`, `ApprovalKind == ExplicitCaller`, `ReviewEvidenceId == null`, `Publication == Landed`, `Cleanup == Complete`; `h.Git.RemoteTarget == op.VerifiedSourceSha`; request `SourceRefusalReason == null`, `TerminalFailureCode == null`; no event contains `legacy_review_binding_required`. SF real: `LandingSafetyHarness`, docs-only commit under `docs/`, remote `refs/heads/master` equals `op.VerifiedSourceSha`; `AssertRemoteSourceAsync`. |
| V-12 | Native recipient: `DE.C498_FailureOutcomeReachesCaller(bool busy)` rows `false` (already eligible), `true` (busy until `ReleaseBusyAsync`) | `InitializeAsync(busy)`; `ArrangeOutcomeAsync("execution-exception")`; `RequestAsync()` (real `delegate.ps1 -Land`, 202); `ReleaseExecutionAsync()`; busy row: wait until the Outcome row has `QueueMessageId != null` and `ConfirmedAt == null`, then `ReleaseBusyAsync()`. `var note = await f.ReceiptAsync()` (complete `UserPrompt`, correct session, sequence above baseline); terminal type `LandRefused`; request `TerminalFailureCode == "landing_io_error"`, `FailureExceptionType == "IOException"`, `FailureDiagnosticId != null`, zero `AgentTaskLanding` rows; `note.Body` contains `landing_io_error; diagnostic=<id:N>; exception=IOException`; `AssertOnePromptAsync(note)`; `f.StatusAsync()` (real HTTP -> real pwsh) contains `Land execution failure: landing_io_error; diagnostic <id>; exception IOException`. |
| V-13 | Monitor precondition: `MT.C498_MonitorRotatesTokenEveryPass` | Pending request 1 s old; one sweep; `ConcurrencyToken` changed and `LastEvaluatedAt == now` with zero aged rows. Retained `C467_V15_ThresholdsUseMeaningfulProgress` (competing sweeps, exactly-once aged rows, `LastProgressAt` untouched). |

Parameterized methods execute every named row with fresh nonzero TRX evidence; a skipped row is uncovered. `AgentTaskLandRequestTests.fail_async_writes_land_failed_and_clears_the_pending_row` is updated to assert the typed detail (`land unconfirmed: landing_unexpected_exception; diagnostic=`) and still asserts the message `git launch failed` is absent.

### Guards the regression

| ID | Exact tests and decisive assertion |
|---|---|
| R-1 | `SP.C498_MonitorPassDuringObservedInspection` (all rows): `RunQueuedAsync` returns `Complete` with a committed Observed row seen inside `OnSourceObservation(2)`. Pre-fix red is the real `DbUpdateConcurrencyException` from the Observed save, the incident on tasks `2cca15a0`/`3fcd2c22`. |
| R-2 | `SP.C498_MonitorPassAtEveryCheckpoint` (all rows) and `SP.C498_MonitorWaitsForResolverCheckpoint`: no later save can be the new incident line; the monitor genuinely commits a new token during the pause and still both sides succeed. |
| R-3 | `FD.C498_HostedDrainClassifiesExecutionException` and `FD.C498_DirectFailAsyncGetsEquivalentEvidence`: `TerminalFailureCode` is never `landing_failed` prose; `FailureDiagnosticId` links row, event, outbox body and log. |
| R-4 | `FD.C498_TerminalTransactionFaultMatrix` and `FD.C498_ReplacementBeforeFailureLock`: precommit cuts leave zero terminal rows; a replacement or new attempt is never settled by a stale failure. Retained `C467_V06_AtomicSettlementFaultMatrix`, `C488_ApprovalOutcomeTransactionAtomic`, `C467_V07_ConcurrentSettlementAndExplicitCleanup`. |
| R-5 | `FD.C498_FailureAfterPublicationRetainsPublication`; retained `C448_V23_NotificationFailureCannotReplaceConfirmedPublication` and `C448_V23_DeliveryFailureKeepsTheCommittedPublicationAndEvent`: publication evidence survives a later exception; `LandRefused` count 0. |
| R-6 | `LG.C498_RealStderrStaysSuppressed`, `LG.C498_FilesystemFailureHasNoInventedExit`, `FD.C498_InspectionDiagnosticPropagatesToRequest`; retained `C448_V05_RemoteErrorsAreUnknown`, `C448_V05_AncestryExitIsTriState`, `C448_V30_RefsParsingAndDestinationsFailClosed`: the marker never reaches any column, event, body, JSON, CLI output or bounded log field; handled refusal codes keep their names. |
| R-7 | `SF.C498_NonReviewedRoleLandsWithDefaultTarget`, `SF.C498_NonReviewedRoleLandsDocsOnlyAgainstRealRemote`, `AR.C498_NonReviewedRolesAdmitExplicitSha`, `CE.C498_PlanAndTestDesignExplicitShaAdmitted`; retained `C488_CallerShaWithoutReviewIsValid`, `C488_DetachedFollowUpPublishesReviewedFix`, `C488_StaleApprovalRefusesDetachedFix`, `C495_V2RouteRejectsAbbreviatedSha`, `C495_V2RouteRejectsFreshMissingApproval`, `C495_V2RouteRejectsMismatchedEvidence`: explicit-SHA Plan/TestDesign lands with a null merge target publish to `refs/heads/master`; no role gate, no `legacy_review_binding_required`. |
| R-8 | `SS.C498_StatusPrintsDiagnosticLines["legacy-fields-absent"]`, retained `C467_V18_StatusAndAcceptance` (11 rows), `C488_StatusShowsDistinctSourceFacts`, all `DelegateScriptLandCompatibilityTests` (24 methods): older servers print no false failure line; `-Land` remains a 202 acknowledgment with unchanged version/SHA fences. |
| R-9 | `SP.C498_RecoverySweepWriteReloadsUnderLock`; retained `AgentTaskLandSweepTests.an_interrupted_attempt_warns_once_nulls_started_at_and_enqueues_once_across_two_passes` and `three_interrupted_attempts_refuse_and_do_not_enqueue`: the restart-warning write cannot clobber a replacement. |
| R-10 | `DE.C498_FailureOutcomeReachesCaller` (both rows); retained `C467_V29_RealOutcomeProducerMatrix`, `C467_V25_HardCrashAfterOutcomeCommitRecoversReceipt`, `C467_V26_HardCrashAfterQueueInsertReusesRow`, `C467_V31_EnqueueFailureRecoversAutomatically`: the failure outcome is a complete native prompt, once, with the diagnostic triple. |

### Guard inventory

Each G-n maps 1:1 to PC-n; none is excluded. Independently bypassable checks have separate rows even when they share a helper. A PC's test method may be shared with another PC only with a different argument row and a different decisive assertion.

| Guard | Plan reference / safety invariant | Control |
|---|---|---|
| G-1 | D-1 §1-2: every resolver checkpoint takes the task row lock and reloads request/task before applying | PC-1 |
| G-2 | D-1 §3: request identity revalidation (ID, `IsPending`, terminal event, attempt/mirrors) under the lock | PC-2 |
| G-3 | D-1 §3: approval and coordinate identity revalidation under the lock | PC-3 |
| G-4 | D-1 §4: resolver-owned baseline comparison; stale patch never applied over newer source progress | PC-4 |
| G-5 | D-1 §5: only the resolver-owned patch is applied; monitor-owned `LastEvaluatedAt`/warning/reconciliation fields preserved | PC-5 |
| G-6 | D-1: a failed or stale checkpoint stops dependent Git work and operation creation | PC-6 |
| G-7 | D-1: a conflicted checkpoint inside the owned-child `started` callback fails that callback | PC-7 |
| G-8 | D-1/D-2: no Git command or lease acquisition inside the checkpoint transaction; task lock is short | PC-8 |
| G-9 | D-1 ground truth: the monitor still rotates the token and writes `LastEvaluatedAt` every pass (race precondition) | PC-9 |
| G-10 | D-2: operation attach reloads and revalidates approval/source/active-operation identity under the lock | PC-10 |
| G-11 | D-2: no direct source-reason saves; a late fallback reason never overwrites existing source evidence/diagnostics | PC-11 |
| G-12 | D-2: `StaleRequest` is never routed through `PersistRefusalAsync` | PC-12 |
| G-13 | D-2: failure settlement captures execution identity before Git and revalidates request/attempt under the lock | PC-13 |
| G-14 | D-2: the recovery sweep restart-warning write reloads and rechecks the pending request under the task lock | PC-14 |
| G-15 | D-3: classifier maps by exception type, concurrency before generic persistence | PC-15 |
| G-16 | D-3: diagnostics persist inside the locked terminal transaction together with event/warning/outbox/`ClearPending` | PC-16 |
| G-17 | D-3: an already committed terminal event wins; a repeated failure callback never replaces diagnostic/outbox | PC-17 |
| G-18 | D-3: a published operation's failure keeps its publication outcome; no `LandRefused`, no deletion authority | PC-18 |
| G-19 | D-3/D-4: no raw exception message, stderr or arbitrary text in any public diagnostic field | PC-19 |
| G-20 | D-3/D-4: length and identifier/template bounds enforced at production, not only by column length | PC-20 |
| G-21 | D-3: diagnostic ID generated once before persistence and logged with bounded fields even when persistence fails | PC-21 |
| G-22 | D-3: direct `FailAsync`/`FailRequestAsync` callers receive the same typed evidence as the hosted catch | PC-22 |
| G-23 | D-3: host-shutdown cancellation creates no terminal failure | PC-23 |
| G-24 | D-4: directly observed command failures carry the fixed template and the actual exit code | PC-24 |
| G-25 | D-4: filesystem and launch failures never invent an exit code | PC-25 |
| G-26 | D-4: `LandingGit.ExecuteAsync` keeps stderr out of every result | PC-26 |
| G-27 | D-4: `RequiredAsync` failures preserve command identity through the existing `IOException` catch | PC-27 |
| G-28 | D-4/D-5: a handled inspection refusal keeps its own code and sets no terminal failure code; an exception sets no fake source refusal | PC-28 |
| G-29 | D-5: `LandRequestStatusDto.From` and the task GET carry every new field | PC-29 |
| G-30 | D-5: `-Status` prints `Land execution failure:` when a terminal failure code exists | PC-30 |
| G-31 | D-5: `-Status` prints `Source inspection:` when inspection metadata exists | PC-31 |
| G-32 | D-5: `-Status` prints `Source inspection exception:` when an inspection exception type exists | PC-32 |
| G-33 | D-5: `-Status` prints `Landing reason:` independently of request fields | PC-33 |
| G-34 | D-5: `-Status` prints `Candidate source:` when present | PC-34 |
| G-35 | D-5: `-Status` prints `Source refusal:` when present | PC-35 |
| G-36 | D-5: missing optional fields from an older server print no false failure line | PC-36 |
| G-37 | D-5: the terminal event detail and Outcome body carry code, diagnostic ID and type | PC-37 |
| G-38 | D-3: migration is additive; nullable columns; approval constraints unchanged | PC-38 |
| G-39 | Ground truth / S5: explicit-SHA Plan/TestDesign lands admit and publish with the default target; no role gate | PC-39 |
| G-40 | Delivery: an exception-path Outcome row is created with the request's real `ReplyTo`/destination so the caller receives it | PC-40 |

Guards = 40, mapped = 40, missing = 0, duplicate PC mappings = 0.

### Positive controls

Each PC: apply the compiling defect, run the exact method filter red at the named assertion, restore, rebuild, run the same filter green. Filters are `--treenode-filter "/*/*/<Class>/<Method>"` against `bin-c498pc/`; parameterized rows are selected with the runner's argument syntax, otherwise the whole method runs and the named row must be the red one. Namespaces: `Antiphon.Tests.Application` for SP/FD/LF/CE/SS/AR/AP/SF/NP/MT, `Antiphon.Tests.Infrastructure` for LG, `Antiphon.E2E` for DE.

| PC | Break | Expect red |
|---|---|---|
| PC-1 | `AgentTaskLandRequestWriter`: delete the `SELECT 1 ... FOR UPDATE` and the request/task reload before applying the patch | `SP.C498_MonitorPassDuringObservedInspection` row 0: `RunQueuedAsync` throws `DbUpdateConcurrencyException` where `run.ShouldBe(LandRunResult.Complete)` is expected |
| PC-2 | Writer revalidation: skip the `request.Id == expected && IsPending && TerminalEventId is null` check | `SP.C498_StaleRequestGuards["replaced"]`: `old.SourceResolutionState.ShouldBe(LandSourceResolutionState.None)` fails (Observed applied to the canceled row) |
| PC-3 | Writer revalidation: drop the approval identity comparison (`ExpectedSourceSha`, evidence, kind, filter, four snapshots) | `SP.C498_StaleRequestGuards["approval-sha"]`: `stored.SourceResolutionState.ShouldBe(None)` fails |
| PC-4 | Writer: remove the persisted-baseline comparison so the stale patch is applied | `SP.C498_NewerSourceProgressConflict["newer-observation"]`: `stored.LocalBeforeSha.ShouldBe(injectedNewer)` fails |
| PC-5 | Writer apply step: copy the captured `LastEvaluatedAt`/`WarningAt`/`ErrorAt` from the stale object onto the reloaded row | `SP.C498_MonitorPassDuringObservedInspection` row 300: `stored.LastEvaluatedAt.ShouldBe(monitorNow)` fails |
| PC-6 | Resolver: treat a `StaleRequest` disposition as success and continue to recheck/operation creation | `SP.C498_StaleRequestGuards["canceled"]`: `h.Git.Trace.Count.ShouldBe(traceAtRelease)` fails (extra `fetch`) |
| PC-7 | Resolver `started` callback: catch the writer conflict and return normally | `SP.C498_ChildStartCallbackConflictFailsCallback`: `h.Git.SourceHead.ShouldBe(h.Git.SeedSha)` fails (merge dispatched) |
| PC-8 | Resolver: begin the checkpoint transaction and take the task lock before `git.ObserveSourceAsync`, committing after the Observed save | `SP.C498_ResolverCheckpointDoesNotHoldLockAcrossGit["observation"]`: monitor `WaitAsync(10 s)` throws `TimeoutException` |
| PC-9 | `AgentTaskLandMonitorService.SweepAsync`: delete `request.ConcurrencyToken = Guid.NewGuid()` | `MT.C498_MonitorRotatesTokenEveryPass`: `after.ConcurrencyToken.ShouldNotBe(before.ConcurrencyToken)` fails |
| PC-10 | `CreateOperationAsync`: attach without reloading/revalidating the request under the lock | `SP.C498_StaleOperationAttachAbandonsCandidate["replaced-request"]`: `landings.Count.ShouldBe(0)` fails |
| PC-11 | `RunRequestAsync`: restore the direct `SourceRefusalReason = resolved.Reason` save that also nulls `SourceDiagnosticCode` before `RefuseAsync` | `FD.C498_InspectionDiagnosticPropagatesToRequest["status_error"]`: `SourceDiagnosticCode.ShouldBe("git_exit_128")` fails |
| PC-12 | `RunRequestAsync`: route the `StaleRequest` disposition through `RefuseAsync` | `SP.C498_StaleRequestGuards["terminal"]`: run throws `DbUpdateException` (unique terminal index) where `Complete` is expected |
| PC-13 | `FailRequestAsync`: remove the under-lock attempt/request recheck, keeping only the pre-lock `requestId` check | `FD.C498_ReplacementBeforeFailureLock["attempt"]`: `terminalEvents.Count.ShouldBe(0)` fails |
| PC-14 | `SweepAsync` restart-warning write: remove the transaction/lock/reload/recheck | `SP.C498_RecoverySweepWriteReloadsUnderLock["replaced"]`: `SweepAsync` throws `DbUpdateConcurrencyException` or `warnings.Count.ShouldBe(0)` fails |
| PC-15 | `LandFailureDiagnostic.Classify`: test `DbUpdateException` before `DbUpdateConcurrencyException` | `FD.C498_HostedDrainClassifiesExecutionException["before-operation","concurrency"]`: `TerminalFailureCode.ShouldBe("landing_concurrency_conflict")` fails with `landing_persistence_failed` |
| PC-16 | `FailRequestAsync`: persist the diagnostic columns with a separate `SaveChangesAsync` before the terminal transaction | `FD.C498_TerminalTransactionFaultMatrix["before-save"]`: `TerminalFailureCode.ShouldBeNull()` fails |
| PC-17 | Terminal path: overwrite `FailureDiagnosticId`/`FailureExceptionType` before the `TerminalEventId is not null` early return | `FD.C498_RepeatedFailureCallbackIsIdempotent`: `FailureDiagnosticId.ShouldBe(first)` fails |
| PC-18 | `FailRequestAsync`: send the published branch through `PersistRefusalAsync` | `FD.C498_FailureAfterPublicationRetainsPublication["landed"]`: `LandRefused` count `ShouldBe(0)` fails |
| PC-19 | Diagnostic formatter: assign `exception.Message` to `FailureExceptionType` | `FD.C498_HostedDrainClassifiesExecutionException["before-operation","io"]`: `FailureExceptionType.ShouldNotContain("synthetic-secret-marker")` fails |
| PC-20 | Formatter: remove the 200-character clamp/identifier filter for the type name | `LF.C498_ExceptionTypeNameIsBoundedIdentifier`: `Length.ShouldBeLessThanOrEqualTo(200)` fails |
| PC-21 | Hosted/failure logging: omit `DiagnosticId` from the persistence-error log record | `FD.C498_TerminalTransactionFaultMatrix["commit"]`: `persistenceLog.DiagnosticId.ShouldBe(handledLog.DiagnosticId)` fails |
| PC-22 | `FailAsync`/`FailRequestAsync` direct path: persist `landing_failed` without the classifier | `FD.C498_DirectFailAsyncGetsEquivalentEvidence["timeout"]`: `TerminalFailureCode.ShouldBe("landing_timeout")` fails |
| PC-23 | `AgentTaskLandHostedService`: remove the `when (stoppingToken.IsCancellationRequested)` filter so cancellation reaches `FailRequestAsync` | `SP.C498_HostShutdownDuringResolutionLeavesRequestPending`: `TerminalFailureCode.ShouldBeNull()` fails |
| PC-24 | `LandingGit.InspectAsync`: return `new(null, "status_error")` without the diagnostic | `LG.C498_InspectionCarriesCommandAndExit["status",128]`: `Diagnostic.ShouldNotBeNull()` fails |
| PC-25 | Inspection diagnostic for filesystem/launch failures: default `ExitCode = 1` | `LG.C498_FilesystemFailureHasNoInventedExit["missing-worktree"]`: `ExitCode.ShouldBeNull()` fails |
| PC-26 | `LandingGit.ExecuteAsync`: append the drained stderr text to `Diagnostic` on failure | `LG.C498_RealStderrStaysSuppressed`: `Diagnostic.ShouldNotContain(marker)` fails |
| PC-27 | `LandingGit.RequiredAsync`: throw plain `IOException(result.Diagnostic)` instead of the typed subtype | `LG.C498_RequiredCommandFailureKeepsCommandIdentity["worktree list"]`: `Command.ShouldBe("git worktree list")` fails |
| PC-28 | Refusal persistence: set `TerminalFailureCode = "landing_io_error"` whenever `SourceDiagnosticExceptionType` is present | `FD.C498_InspectionDiagnosticPropagatesToRequest["identity_io_error"]`: `TerminalFailureCode.ShouldBeNull()` fails |
| PC-29 | `LandRequestStatusDto.From`: pass `null` for `FailureDiagnosticId` | `CE.C498_TaskGetExposesFailureDiagnostics["hosted-catch"]`: JSON `failureDiagnosticId.ShouldBe(row.FailureDiagnosticId)` fails |
| PC-30 | `delegate.ps1`: delete the `Land execution failure:` `Write-Output` | `SS.C498_StatusPrintsDiagnosticLines["exception-only"]`: `ShouldContain("Land execution failure: ")` fails |
| PC-31 | `delegate.ps1`: delete the `Source inspection:` line | `SS.C498_StatusPrintsDiagnosticLines["source-only"]`: `ShouldContain("Source inspection: git status")` fails |
| PC-32 | `delegate.ps1`: delete the `Source inspection exception:` line | `SS.C498_StatusPrintsDiagnosticLines["source-only"]`: `ShouldContain("Source inspection exception: ")` fails |
| PC-33 | `delegate.ps1`: print `Landing reason:` only when no request failure fields exist | `SS.C498_StatusPrintsDiagnosticLines["operation-reason"]`: `ShouldContain("Landing reason: target_changed")` fails |
| PC-34 | `delegate.ps1`: delete the `Candidate source:` line | `SS.C498_StatusPrintsDiagnosticLines["combined"]`: `ShouldContain("Candidate source: ")` fails |
| PC-35 | `delegate.ps1`: delete the `Source refusal:` line | `SS.C498_StatusPrintsDiagnosticLines["combined"]`: `ShouldContain("Source refusal: ")` fails |
| PC-36 | `delegate.ps1`: print `Land execution failure:` unconditionally (no null guard) | `SS.C498_StatusPrintsDiagnosticLines["legacy-fields-absent"]`: `ShouldNotContain("Land execution failure")` fails |
| PC-37 | Terminal detail formatter: omit the `diagnostic=<id>` token from the event detail | `NP.C498_FailureOutcomeBodyCarriesDiagnostic`: `note.Body.ShouldContain("diagnostic=" + id.ToString("N"))` fails |
| PC-38 | Migration: make `TerminalFailureCode` `NOT NULL DEFAULT ''` | `AP.C498_MigrationIsAdditive`: `stored.TerminalFailureCode.ShouldBeNull()` fails |
| PC-39 | `AgentTaskLandService.RequestAsync`: throw `ConflictException` for any role other than `Code` | `SF.C498_NonReviewedRoleLandsWithDefaultTarget[Plan, "equal"]`: `RequestAsync` throws where `queued` is expected |
| PC-40 | Exception-path settlement: create the Outcome notification with `State = NotRequired` regardless of `ReplyTo` | `DE.C498_FailureOutcomeReachesCaller[true]`: `ReceiptAsync` fails its 60 s bound (`complete native Land receipt` never observed) |

Mutation reports each PC as break, red (exact failing assertion and executed method name from a fresh TRX), restore, green. PCs execute post-land under the SourceLanding protocol; Code implements the tests and runs V/R; ordinary Review judges the pending PC design and the V/R evidence before land. Batching: {PC-1..PC-8, PC-10, PC-12} share `AgentTaskLandRequestWriter`/resolver and run singly; {PC-9}, {PC-11, PC-13, PC-14, PC-16, PC-17, PC-18, PC-22} touch `AgentTaskLandService` distinct methods and may batch in two groups that do not share a method; {PC-15, PC-19, PC-20, PC-21, PC-37} touch `LandFailureDiagnostic`/logging distinct members; {PC-24..PC-27} touch `LandingGit` distinct members; {PC-30..PC-36} each delete a different script line and may batch; {PC-23, PC-28, PC-29, PC-38, PC-39, PC-40} are independent files.

### Out of scope

- Retrying or backfilling the historical incidents (tasks `2cca15a0`, `3fcd2c22`); D-3 leaves historical rows unknown.
- Raw or truncated stderr exposure, a regular-expression scrubber, or richer Git result types for remote observation, push, verifier and cleanup (D-4 explicitly limits carriage to inspection and commit lookup).
- Approval policy changes, a Review gate for any role, or `-Land` waiting/polling modes (ground truth rows 1 and 8).
- A Kestrel-backed `Antiphon.Tests` host: the transport is unchanged; V-12 covers real HTTP to the real script once per recipient state, and V-8 replays the real GET bytes.
- Real `git_start_failed` from a missing `git` binary: `Process.Start` on Windows raises `Win32Exception` before `ExecuteAsync` can produce that code; the code is exercised through the thrown-`IOException` row and the classifier row for a generic type.
- Combinations excluded: monitor age x every non-Observed checkpoint (V-2 uses age 0; thresholds are independent of the checkpoint and covered by V-1 and the retained `C467_V15` matrix); every stale dimension x every checkpoint (V-3 covers all dimensions at Observed plus the attach and child-start seams, the shared writer makes the remaining products the same code path, which PC-1..PC-7 mutate directly); every exception type x published failure (type does not alter the published branch; `IOException` only).
- Client UI features, browser or E2E sweeps for the optional TypeScript fields (type-check only).
- Full `Antiphon.Tests` assembly runs during Code; the nightly keeps the full run.

### Cost

Ordinary V/R floor (Code), one producer-owned isolated build, fresh result directories per invocation; all figures estimated from the 2026-09-10 profile (Unit lane 70 s) and per-class timings of the sibling land classes:

```powershell
dotnet build tests/Antiphon.Tests --property:OutputPath=bin-c498/ --nologo                                              # ~4 min
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/*/*[Category=Unit]' --report-trx --report-trx-filename unit.trx --results-directory .antiphon/c498-unit   # ~1.5 min
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/(AgentTaskLandSourcePersistenceTests*)|(AgentTaskLandFailureDiagnosticTests*)|(LandFailureDiagnosticTests*)|(AgentTaskLandMonitoringTests*)/*' --report-trx --report-trx-filename request.trx --results-directory .antiphon/c498-request   # ~5 min
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/(LandingGitTests*)|(AgentTaskLandSourceFreshnessTests*)|(AgentTaskLandPersistenceFailureTests*)|(AgentTaskLandRecoveryTests*)|(AgentTaskLandNotificationPersistenceTests*)|(AgentTaskLandNotificationRecoveryTests*)/*' --report-trx --report-trx-filename git.trx --results-directory .antiphon/c498-git   # ~11 min (ProcessSpawnLimit serial)
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/(AgentTaskLandContractEndpointTests*)|(DelegateScriptLandStatusTests*)|(DelegateScriptLandCompatibilityTests*)|(DelegateScriptLandApprovalTests*)/*' --report-trx --report-trx-filename surface.trx --results-directory .antiphon/c498-surface   # ~3 min
dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/(AgentTaskLandApprovalRequestTests*)|(AgentTaskLandApprovalPersistenceTests*)|(AgentTaskLandRequestTests*)|(AgentTaskLandSweepTests*)|(AgentTaskLandConcurrencyControlledTests*)|(AgentTaskLandAdmissionControlledTests*)|(AgentTaskLandBoundaryControlledTests*)|(LandingProtocolHarnessTests*)/*' --report-trx --report-trx-filename protocol.trx --results-directory .antiphon/c498-protocol   # ~4 min
dotnet build tests/Antiphon.E2E --property:OutputPath=bin-c498/ --nologo                                                # ~3 min
dotnet run --project tests/Antiphon.E2E --no-build --property:OutputPath=bin-c498/ -- --treenode-filter '/*/*/AgentTaskLandDeliveryE2ETests/C498_FailureOutcomeReachesCaller*' --report-trx --report-trx-filename delivery.trx --results-directory .antiphon/c498-e2e   # ~6 min (class filter if the method wildcard is rejected: ~25 min)
pwsh -File scripts/test-duration-tripwire.ps1 -Trx .antiphon/c498-git/git.trx                                          # ~0.5 min per TRX
npx tsc -b   # in client/, type-check of the optional landRequest fields, ~1 min
```

| Lane | Minutes (estimated) |
|---|---|
| Setup/build (Tests + E2E) | 7 |
| Unit lane | 1.5 |
| New/affected integration classes (request, git, surface, protocol) | 23 |
| E2E recipient rows | 6 |
| Tripwire + client type-check | 2 |
| **Ordinary V/R floor** | **~40** |

PC floor (Mutation, `bin-c498pc/`, method-scoped): 40 PCs; 26 of them batch into 7 groups by the rule above (different files/methods), 14 run singly; each group or single needs one break build, one red run, restore, one green build, one green run. Build ~2.5 min each, red/green runs 0.2-1.5 min per method (E2E PC-40 ~6 min each way).

| Item | Minutes (estimated) |
|---|---|
| 21 break/restore build pairs x 5 min | 105 |
| 80 method-scoped runs (40 red + 40 green) averaging 0.7 min | 56 |
| PC-40 E2E red + green | 12 |
| **PC floor** | **~175 serial; ~95 wall across two worktree shards** |

Total verification floor (estimated): ~40 min ordinary + ~175 min PC serial = ~215 min; ~135 min wall with sharding. Expected slow-boundary additions for the tripwire: `SP.C498_MonitorPassAtEveryCheckpoint` (8 harness rows, ~3 s each), `FD.C498_TerminalTransactionFaultMatrix` (restart + re-run per row, ~6 s), `LG.C498_RealStderrStaysSuppressed` (real git, ~3 s), `CE.C498_TaskGetExposesFailureDiagnostics` (pwsh replay, ~4 s), `DE.C498_FailureOutcomeReachesCaller` (native, ~3 min per row); no other test is expected to exceed 5 s.

Savings: class-scoped regression (~23 min) instead of the `Antiphon.Tests.Application` namespace slice (~26 min on the CARD-0239 evidence, 94 min for the 2026-09-10 broad slice) saves at least 3-70 min per Code iteration; batching independent PCs into 7 groups saves ~95 min of build time against 40 single-PC cycles; capping E2E at the two recipient rows instead of the class saves ~19 min. Zero-cost items: none; every lane has a nonzero floor.

--- next stage ---
next: code
handoff: Implement CARD-0498 slices S1-S5 from the plan's Decisions plus the Verification design (SP/FD/LF/LG/CE/SS/AR/AP/SF/NP/MT/DE C498_ methods, harness extensions listed under Inspection), run the Unit lane and the five class filters plus the two E2E rows from the Cost section, and report fresh TRX counts per class.
artifact: docs/superpowers/plans/2026-09-13-card-0498-land-refusal-diagnostics-concurrency-plan.md
