# CARD-0420: Give runner restarts an honest wait budget and outcome

Plan date: 2026-09-07. Inspected checkout: `2843bcc1` on `master`.

Raise the restart script's default health wait to 180 seconds, distinguish an expired observation window from a failed restart action, and allow waiting again without another kill. Add lightweight startup timing and phase diagnostics while preserving the complete startup-adoption sweep before HTTP starts. This is a Plan artifact for separate TestDesign; no runtime change or live restart was performed.

## Evidence and ground truth

The full [measured investigation](../../investigations/2026-09-06-card-0420-runner-restart-timeout.md), committed as `2843bcc1`, is the evidence baseline. Its timing table covers two restarts on 2026-09-06, not a sampled latency distribution.

| Card assumption or question | Observed implementation / evidence | Consequence |
|---|---|---|
| A normal restart takes a few seconds. | `scripts/run-daemon.ps1` sleeps three seconds between service runs, then invokes an incremental build before launching the built executable. The incident took about 99.3 seconds from old-process exit to listening. | Replace the `~3s` restart promise with the actual sequence: supervisor delay, build, adoption, HTTP readiness. |
| Sixty seconds is enough for build and adoption. | Build-begin to listening was about 36.5 seconds in the comparison and 90.3 seconds in the incident. Build wall intervals were about 17 and 52 seconds; launch-request to listening about 19.5 and 38.3 seconds. | The default is demonstrably insufficient for the incident. Choose explicit headroom, without claiming a p95 or maximum. |
| Adoption might run after HTTP becomes available. | `src/Antiphon.SessionRunner/Program.cs` awaits `AdoptOrphanedHostsAsync` before starting HTTP. `SessionRunnerRuntime` restores claims, processes Herdr sidecars, then processes pty-host manifests serially. | Keep the barrier and ordering. No background or parallel adoption in this card. |
| Serving early health is a harmless optimization. | `server/Application/Services/SessionReconciliationService.cs` skips the session pass when the runner is unreachable; a responding runner's missing session is marked Failed. | A healthy response cannot precede the initial adoption sweep. Keep `/sessions` unavailable during that sweep as well. |
| Session count predicts a suitable deadline. | There were 42 versus 41 adopted pty-host sessions, but the interval from restored claims to adoption summary was 3.446 versus 23.225 seconds. That interval includes more than pipe connection work. | No session-count multiplier or claimed per-session throughput. |
| Unchanged runner source means its build can be skipped. | Both the Scheduled Task and AppHost launch paths use `run-daemon.ps1 -BuildProjectDir`. Referenced projects, props/targets, package/content inputs, and the repository SHA stamp also affect outputs. | Preserve incremental build on each launch. No new freshness cache or no-build option. |
| Timeout means restart failed. | `restart-session-runner.ps1` starts its deadline after kill/relaunch selection, exits 1 on expiry, and leaves supervision running. The current polling operations can overrun that deadline. | Separate action failure from wait expiry; report elapsed time and the last supported observation. |
| Existing logs can identify the slow phase precisely. | They do not record first successful health observations, exact claim-restore duration, or build-target costs. Runtime resolution is included in a proxy interval. | Add coarse phase measurements now. Detailed build-target profiling is deferred because it is unnecessary to correct the script contract. |

Sources inspected in addition to the investigation: the three runner scripts; `Program.cs`, `SessionRunnerRuntime.cs`, `RunnerBuildIdentity.cs`; the reconciliation service; `DaemonLogRotationTests`, `PtyHostDeploymentGuardTests`, and the existing adoption/reconciliation test surfaces. The relevant owners are [bootstrap](../../bootstrap.md), [session runtime invariants](../../session-runtime-invariants.md), [testing/build operations](../../testing-and-build.md), and [project conventions](../../project-context.md).

## Decisions

### D-1: Correct the operational contract; make no startup speed claim

Choose the card's timeout/reporting acceptance arm. Preserve current incremental builds, serial adoption, transcript-claim restoration before adoption, and the existing Herdr Pending/exited/live decisions. Keep launching the built executable directly: replacing it with `dotnet run` would reintroduce the kill-on-close job that destroys detached pty-hosts.

The readiness assertion means the **initial sweep has completed and every processed surviving session has its existing representation**. It does not require every session to be Running: current terminal registrations and Herdr Pending are valid sweep results. It also does not mean all transcript tailers have finished asynchronous catch-up. Do not strengthen or weaken those existing contracts through diagnostics.

Do not add an adoption cancellation deadline, truncate a sweep when the script stops waiting, change per-host pipe timeouts, or change reconciliation. The script's timer never reaches the runner's cancellation token or session lifecycle.

### D-2: Use a bounded 180-second observation window

Change `-TimeoutSec` from 60 to **180**, retaining a positive integer override. This gives roughly 80 seconds of margin beyond the measured old-exit-to-listen incident, before allowing for the unmeasured health-observation delay. It is a conservative operational default based on two observations, **not** a percentile, guaranteed startup bound, or proof that every future restart fits.

Keep the parameter's existing meaning: the health-wait budget begins after kill/relaunch selection finishes. Record command entry, relaunch-request completion, and wait start separately so total command time and wait time cannot be confused. `-WaitOnly` starts its own observation budget immediately after argument validation and read-only setup.

Use a monotonic stopwatch for the budget and elapsed durations; use UTC round-trip timestamps with milliseconds for correlation. Probe `/health` immediately, then at up to two-second intervals. Bound each HTTP request by the lesser of five seconds and the remaining budget, and clamp sleeps to that remainder. Avoid the current repeated TCP-enumeration gate: connection refusal is already a valid HTTP probe result. Do not start another probe once the budget is exhausted, extend/reset the budget on phase progress, or accept a response completed after the budget as in-budget success. Cancellation must stop only the probe, with a small documented scheduling tolerance in its test.

Success still requires a real HTTP 200 from the expected runner listener; a build-success event, phase marker, live PID, or open port is insufficient. Retain listener ownership verification when reporting the service PID, and do not describe a foreign listener as the runner. Use existing process identity (PID plus start time/path) and, where needed, the existing `/capabilities` build identity; no health body or public DTO change is needed.

### D-3: Make the final result machine-readable and truthful

Emit a stable final line beginning `RUNNER RESTART RESULT:`, with a compact JSON object carrying `outcome`, `mode` (`restart` or `wait-only`), `waitElapsedMs`, `commandElapsedMs`, `timeoutSec`, `observedAtUtc`, `lastObservedPhase`, and known process/attempt identities. Optional fields contain `firstHealth200ObservedAtUtc`, `lastProbeResult`, and the latest current-attempt error code. Missing evidence is null/unknown, never invented. Keep diagnostic text before this line and use the same outcome vocabulary in human-readable text.

| Exit | Outcome | Meaning and message |
|---|---|---|
| 0 | `healthy` | A successful health response was observed within the budget. Give the first 200 observation time, elapsed wait, and verified runner identity. |
| 2 | `wait-expired` | No qualifying 200 was observed within the budget. Say **"Health wait expired; runner readiness is unconfirmed. Startup may still be in progress."** Give the last observed phase and its age, latest probe result, and whether the identified supervisor/runner is still alive. This is not a declaration that startup succeeded or failed. |
| 1 | `action-failed` | A concrete restart operation failed, for example `Start-ScheduledTask` threw or an owned service could not be stopped. Report the operation and actual error, without claiming that no other launch can be running. Invalid argument combinations also fail before mutation. |

Elapsed time alone, absent health, a missing PID file, or an unreadable log can never yield `action-failed`. A build failure followed by the supervisor's existing retry is **retrying after a failed attempt**: continue waiting, retain that error in diagnostics, and return `wait-expired` if the budget ends. Likewise show a positively observed exited supervisor/runner or launch error without calling a transient retry a permanently broken service. A subsequent successful attempt can still yield `healthy`.

At expiry do not kill/cancel any supervisor, build, runner, pty-host or session; do not rewrite desired state, trigger another Scheduled Task, or request a hard restart. State explicitly that the script has stopped waiting and has not stopped the background startup. The exit-code change is intentional: callers that only accept 0 remain conservative, while callers can now distinguish incomplete observation from an action error. Inspect repository callers during Code and update any timeout-specific handling; the inspected references currently recommend the command rather than parsing its exit 1.

### D-4: Add a non-destructive way to continue waiting

Add `-WaitOnly` to `scripts/restart-session-runner.ps1`. It uses the identical health observer and result contract, but skips desired-state writes, process stopping, PID-file deletion, and Scheduled Task operations. `-WaitOnly` combined with `-Hard` or `-KillSessions` is an argument error before any side effect. Diagnostic logging is allowed; daemon control is not.

Expiry instructions must give this concrete command:

```powershell
pwsh -NoProfile -File scripts/restart-session-runner.ps1 -WaitOnly -TimeoutSec 180
```

Also retain the command to inspect `logs\session-runner.log`, and point to the dated runner log documented in bootstrap. Do not recommend re-running the ordinary restart or `-Hard` to resolve a mere health-wait expiry.

### D-5: Add lightweight, correlated instrumentation in this card

**Instrumentation is required**, but detailed build-target profiling is not. The script needs evidence to report a phase honestly, and future tuning needs the currently missing health and coarse phase intervals. Use versioned structured milestone records in the existing bounded logs; do not add a database table, readiness endpoint, control-state file, or general monitoring framework.

Use a fixed `ANTIPHON_STARTUP` marker followed by one JSON object, with schema version, event name, UTC timestamp, producer PID/start time, and attempt ID. Add only relevant numeric durations/counts, phase/outcome names and exit codes. No prompts, rules text, environment dumps, credentials, or build command argument dumps. Existing logging retention remains in force. Keep new PowerShell source ASCII-only and compatible with the Windows PowerShell 5.1 fallback.

1. **Supervisor (`run-daemon.ps1`).** Give each build/launch iteration a fresh attempt ID. Log build start/completion with monotonic elapsed milliseconds and exit code; launch requested/wrapper started; service exit; retry scheduled; and supervisor stopping. Retain the current build invocation, output capture, retry delays and launch semantics. Record supervisor and wrapper identities distinctly: the current service PID file names `cmd.exe`, not necessarily the runner executable. Pass the attempt ID and supervisor identity to the child through that `ProcessStartInfo`'s environment only, without modifying machine/user environment. Both autostart and AppHost already use this supervisor, so both paths get the same records. Non-runner daemons must retain their current behavior.
2. **Runner (`Program.cs`, `SessionRunnerRuntime.cs`, a small instance-owned `RunnerStartupDiagnostics` helper if useful).** Consume optional correlation data in the composition root; a standalone runner generates its own local attempt identity. Record managed startup entry, runtime resolution start/end, adoption-sweep start/end, and `ApplicationStarted`. Within the existing sweep measure claim restoration, the Herdr pass (including its existing cleanup), and the pty-host manifest pass separately. Record zero-work phases and failed phases too. Enrich existing per-host adoption outcomes with elapsed time around the adoption attempt; do not instrument or alter each pipe/replay/rules substep in this card. Preserve the meaning of the current returned adopted count; distinguish pty-host success counts from Herdr and exited/Pending counts instead of relabeling it a fleet total.
3. **Observer (`restart-session-runner.ps1` and a small script helper).** Timestamp wait start, each change in last observed phase, first successful 200, and the final result. Report phase changes promptly and one continuing-wait line at most every 15 seconds; no success-style heartbeat during an unconfirmed phase. The recorded health time is **this observer's first completed 200**, not the unknowable first instant the endpoint could answer or the first request made by another client. Keep polling resolution visible in the measurement record.

Treat milestone parsing as optional diagnostics. Read bounded appended log data, retaining an incomplete last line until it completes; support file rotation/truncation and begin `-WaitOnly` with a bounded tail. Suggested limits are 256 KiB per read and 8 KiB per milestone. If a gap, malformed record, old schema, stale attempt, or I/O failure prevents attribution, report phase unknown. Do not grep unversioned prose such as "Adopted" or reuse an old "Build succeeded" as current evidence. Accept current phase attribution only for matching attempt and process start identities, excluding pre-restart service events. `lastObservedPhase` plus age means exactly that; a live process stuck in a phase is not proof of continued progress.

Log-write and parse failures must not fail, delay materially, or change adoption; they must never change the HTTP success predicate. The diagnostics helper cannot swallow/reclassify an adoption exception, skip disposal, or replace its existing fate decision. Use existing injected logging and instance state, with monotonic timers local to the measured operation.

The first rollout may use an old, long-lived supervisor that has not loaded the new script. Missing supervisor markers must degrade to `unknown`; health observation still works. Document a single planned supervisor refresh during deployment to load the new instrumentation, rather than forcing a hard restart from the health observer.

### D-6: Defer optimizations with explicit evidence thresholds

- **Early HTTP / background adoption:** rejected here because it changes the safety contract. A future design would have to keep every session-list consumer from treating partial readiness as absence, not merely introduce a separate health check.
- **Bounded parallel adoption:** deferred. First measure phase and per-host totals, then examine shared claims, interpretation/replay, Herdr effects and resource contention. Similar session counts with different totals do not establish safe concurrency or a speedup.
- **Skip unchanged builds:** deferred. A future design needs a complete graph/input and output-validity policy, including SHA stamping, and evidence that this saves meaningful time. Do not substitute runner source timestamps for that policy.
- **Adaptive timeout by session count or indefinite waiting:** rejected. Neither is supported by the measurements; a fixed configurable budget plus an honest expiry remains useful under long tails or stalls.
- **Detailed MSBuild target tracing:** deferred from the shipped path. The coarse build timer answers whether build remains dominant. If it does, collect a separate controlled target-performance summary under the testing/build guide before proposing a cache or build change. Do not turn on default binlogs or dump environment/property values into diagnostics.

No operator decision blocks this plan. D-1 through D-6 are implementation defaults; a separate TestDesign is required because the change touches process supervision and the startup safety barrier.

## Implementation slices

| Slice | Files / work | Tests and completion evidence |
|---|---|---|
| S1: Supervisor timing | `scripts/run-daemon.ps1`; small shared milestone-writing helper under `scripts/` if needed. Add identities, durations and events around existing operations. Keep direct-exe launch and log rotation behavior. | New `DaemonStartupDiagnosticsTests` in `tests/Antiphon.SessionRunner.Tests`, using the existing throwaway-script approach in `DaemonLogRotationTests`; exercise successful launch, failed build followed by retry, service exit and rotation. Re-run `DaemonLogRotationTests` and `PtyHostDeploymentGuardTests`. |
| S2: Runner phase timing | `src/Antiphon.SessionRunner/Program.cs`, `SessionRunnerRuntime.cs`, optional `RunnerStartupDiagnostics.cs`. Add observations around the current barrier and phase sequence. | New `RunnerStartupDiagnosticsTests` for phase ordering/durations, zero manifests, errors and log-write failure. Behavioral HTTP barrier proof in new `RunnerStartupReadinessTests`; existing adoption tests below remain the fate/claims regression evidence. |
| S3: Wait/result contract | `scripts/restart-session-runner.ps1`, proposed `scripts/session-runner-restart-health.ps1` helper containing the observer/classifier. Default 180; monotonic bounded probes; exit/result vocabulary; phase reading; `-WaitOnly`. Separate importing the observer from executing destructive entrypoint code. | New `RunnerRestartHealthTests` in `tests/Antiphon.SessionRunner.Tests`. Run real helper/script control flow against temp files, an isolated endpoint and injected process/task/clock seams; never invoke the production restart path. Test actual outcome and side effects, not only source strings. |
| S4: Operator contract | Update `docs/bootstrap.md` with timeout semantics, exit codes, continuing-wait command, instrumentation records, first-observed-health limitation and supervisor-refresh rollout. Update script help/examples and add a short barrier/diagnostics clarification to `docs/session-runtime-invariants.md`. | Verify documented commands/parameters and final result examples against S3. All touched PowerShell scripts must parse under pwsh and Windows PowerShell 5.1 and remain ASCII. |

No server/client/database change is required. Do not edit generated `docs/cards/` files. Keep the plan and its future TestDesign in this tracked document.

## Acceptance and TestDesign handoff

TestDesign should append the repository's required `## Verification design` with executable V-n/R-n/PC-n items. At minimum it must settle these proof obligations:

1. Reproduce the timing defect with a controlled observer clock: a build/adoption/health sequence taking approximately 100 seconds returns `healthy` with the new default. An explicit 60-second wait returns `wait-expired`, not `action-failed`; a later `-WaitOnly` returns `healthy` with no additional control operation. Also cover startup beyond 180 seconds, a stalled phase, and no diagnostic evidence. Use fake time for the 60/100/180-second cases, plus a short real-clock endpoint check for the HTTP cancellation boundary; do not add minutes of sleeps to every test run.
2. Cover immediate health, refusals/non-200 responses, a hung HTTP response at the end of the budget, wall-clock jumps, and a 200 completing after expiry. Assert elapsed bounds, first-200 fields and exact result/exit meanings. A process, listener or build-success event alone cannot pass readiness.
3. Prove `-WaitOnly`, invalid combinations, and wait expiry never invoke stop/tree-kill, Scheduled Task start, desired-state writes or PID deletion. Distinguish pre-wait stop/launch errors from health timeout. Retrying build/start failures remain observable and can recover within the same wait.
4. Exercise stale attempts/PID reuse, partial/malformed records, log rotation/truncation, missing logs, and an old supervisor with no new markers. Diagnostics must degrade to unknown without creating false success or action failure. Attributable build, adoption, awaiting-HTTP and retry states must be distinguishable in output.
5. **Behaviorally prove the barrier through the actual production startup path** using an isolated runner with a deliberately held adoption operation. Before release, neither `/health` nor `/sessions` may produce a successful response; after release, the first successful session list contains the expected adopted/terminal/Pending representations. The harness must positively establish adoption was entered and held; an arbitrary delay or failed process start is not a passing proof. TestDesign must select a controllable fake host/Herdr server or narrow startup-composition seam without adding a production "skip adoption" flag.
6. Keep a positive control that starts HTTP before the held sweep finishes and makes that test red, then restore the barrier and get green. Also require controls for admitting a late 200 and accidentally executing a restart from `-WaitOnly`. Code reports the red/revert/green results explicitly.
7. Re-run the existing relevant regressions: `PtyHostAdoptionTests`, `HerdrAdoptionSweepTests`, `GrokRulesAdoptionTests`, `TranscriptAdoptionSafetyTests`, and `SessionReconciliationServiceTests` (especially `Unreachable_runner_skips_the_session_pass` and `Session_unknown_to_runner_is_failed_and_its_agent_reset`). TestDesign may select exact relevant methods where a full class is costly, with reasons. Do not replace adoption/claim behavior tests with log-text tests.

Use `dotnet run --project tests/Antiphon.SessionRunner.Tests --property:OutputPath=bin-c420/ -- --treenode-filter "/*/*/<ClassName>/*"` for named runner classes, and the corresponding `tests/Antiphon.Tests` invocation for reconciliation. Run assemblies sequentially. New process-spawning tests carry that assembly's `ParallelLimiter<ProcessSpawnLimit>`. All child runners, manifests, transcript roots, logs, endpoints and process cleanup belong to the isolated fixture; no production port 17204, live provider login or live Scheduled Task is a test fixture. A host booting real server `Program` must keep `ProductionRunnerGuard`/the refusing runner client. No browser/client/E2E suite is forced by this card.

## Rollout, measurement and rollback

Code-stage acceptance is isolated. A live runner restart is a later, deliberately scheduled deployment operation from the canonical checkout, after verifying checkout/build ownership; do not restart the caller's running fleet to collect a Plan or TestDesign sample.

The deployer should check the committed implementation is in the main checkout, record current runner/session identities through the documented operations lane, and perform one planned `-Hard` refresh if needed to replace the old supervisor code. Do not use `-KillSessions`, re-register the Scheduled Task, or launch a second supervisor. If the wait expires, continue with `-WaitOnly` and the logs. Verify both new supervisor milestone schema and the running runner's existing `/capabilities` build identity: HTTP 200 alone cannot prove that new code loaded. Read back the expected sessions and account for any genuine exits under the existing lifecycle rules.

Keep one evidence row per actual restart: commit/attempt/process identities; observed build duration; managed-entry/runtime-resolution/claims/Herdr/pty/adoption durations; `ApplicationStarted`; first observer 200; command and wait elapsed; configured budget; outcome; and relevant session counts. Do not equate `ApplicationStarted` with first successful HTTP, or compare measurements with different start points as the same metric. Collect subsequent normal restarts without creating a scheduled benchmark or extra production restarts. This card can close on the corrected behavior and preserved barrier; a statistically meaningful latency distribution or a startup speedup is not a hidden acceptance requirement.

Rollback is revert of the implementation plus a planned supervisor/runner refresh to load it. There is no data migration. The previous script's 60-second reporting limitation returns on rollback; operators can still supply a longer `-TimeoutSec`. Diagnostic loss alone is not a reason to kill sessions or loosen readiness.

## Verification design

TestDesign date: 2026-09-07; inspected and based on landed plan `4ec7d98f`. D-1 through D-6 remain unchanged. This section specifies tests for Code to implement and execute; it does not claim those tests already exist or pass. Verification is feasible with existing fixtures and the internal script seams already authorized by S3. No production startup bypass or live restart is needed.

### Fixtures and execution boundary

All new C# tests belong to `tests/Antiphon.SessionRunner.Tests`. Proposed classes are `RunnerRestartHealthTests`, `RunnerRestartScriptCompatibilityTests`, `DaemonStartupDiagnosticsTests`, `RunnerStartupDiagnosticsTests`, and `RunnerStartupReadinessTests`. Shared drivers/data go under that project's `Fixtures/RunnerRestart/`; helpers can stay internal to the assembly. Tag tests appropriately as Unit/Integration. Every class that starts pwsh, powershell.exe, the runner or a dummy child must carry `[ParallelLimiter<ProcessSpawnLimit>]`; the HTTP runner fixture also uses `[NotInParallel("SessionLiveness")]` to match existing adoption tests.

**F-1: Execute the actual restart decision flow with intercepted external operations.** In the S3 helper, place the command body in a callable function, proposed `Invoke-RunnerRestart`, and obtain its OS/time dependencies from an internal factory, proposed `New-RunnerRestartPlatform`. Keep default argument binding and final process exit in the real entry script. The platform supplies monotonic time, UTC time, sleep, health transport, listener/process identity reads, and every daemon-control operation (state/PID writes, stop/tree-kill/kill-all, task start). Logging/parser code and the outcome/loop logic remain real. These are internal script functions, not new public `-TestMode`, `-SkipAdoption`, endpoint or kill-bypass switches.

Run wrapper cases in a new child PowerShell process against a temp repository-shaped directory: copy `restart-session-runner.ps1` byte-for-byte into `scripts/`, and supply a same-named helper shim that dot-sources the **real** helper then overrides only the dependency factory with the test platform. Keep the entry script and real observer/core unmodified. Any additional support scripts are copied unchanged. This exercises the real parameter defaults, argument validation, branch selection, formatting and `exit`, including each documented argument combination. Verify the entry copy's hash before executing it. Before the harness can execute, require the entry script to be thin (argument/setup/import/core invocation/output/exit only) and the real helper import to be inert; no direct OS control may sit outside the platform. Review that boundary and test the import for zero platform calls. Never invoke the current destructive entry script against the real checkout as a test.

The fake platform records every operation in an append-only JSONL call trace **before** returning or throwing. It has no native task/process mutation fallback; an unconfigured operation throws a fixture error. For simulated restart cases use synthetic PID/start/path tuples exclusively through this platform. Seed sentinel state/PID files under the temp root, compare their bytes before/after, and independently assert the mutation trace. Safe fake writes target only those files; safe fake stop/task operations record calls without executing an OS command. `WaitOnly` and invalid-argument fixtures install throw-on-call mutations, even if their synthetic process census is empty, so a hidden restart branch cannot pass by having nothing to kill. Test harness setup/teardown is outside the command trace.

**F-2: A deterministic clock and scripted probes, not a second observer implementation.** Hold `{MonotonicMs, UtcEpoch, UtcOffsetMs}` in the platform. `Sleep(ms)` advances monotonic time and records the requested duration; the fake probe records its supplied budget and advances by its scripted duration before returning a status/transport error and completion time. Time advancement also appends scripted supervisor/runner milestones to a real fixture log. UTC is derived separately and can jump without changing monotonic time. All delays in a virtual test use this clock; no frozen clock is injected into a real server graph. Give the fake platform a 1,000-operation fuse that fails the fixture if a mutated loop fails to terminate. A fuse failure alone is not the expected positive-control assertion.

Use a fixed UTC origin such as `2026-09-07T10:00:00.000Z` and millisecond precision. With immediate refusal probes and two-second sleeps, the standard scenario is: wait starts at virtual 0; supervisor building at 0; build complete and runner launch at 52,000; runtime/claims/Herdr/pty phases finish before 99,000; health first returns 200 at 100,000. The fake records only these independent inputs. It must not compute the expected outcome, deadline, result JSON or phase selection; assertions specify those independently. Short physical endpoint tests below cover what a scripted probe cannot prove.

**F-3: Real HTTP cancellation.** Run a fixture-owned `TcpListener` on `IPAddress.Loopback` port 0, with asynchronous accept/read and a request-received signal. Feed its assigned URL to the same production HTTP adapter through F-1's internal configuration. One handler returns a minimal HTTP 200; another accepts `/health` but sends no response until teardown. Use a real stopwatch/production sleep/cancellation implementation and a two-second observation budget. Start timing at the observer's recorded wait start, excluding PowerShell boot. The hung-request case must finish `wait-expired` between 2,000 and 3,000 ms and must have accepted a request; use 1,000 ms as the explicit Windows scheduling tolerance. The adapter's supplied remaining deadline is also asserted with the exact virtual tests, so this tolerance cannot hide a five-second request timeout. The fixture has a 15-second outer cleanup budget, cancels its server task, and keeps accepting probes after observer expiry to prove the observer did not stop the server. A refused connection is not a valid substitute for the hung-response fixture. Run this adapter check under both pwsh and Windows PowerShell 5.1.

**F-4: Real supervisor, fake build and harmless service.** Extend the approach in `DaemonLogRotationTests`: temp work/log/state/PID paths, actual `run-daemon.ps1` in a captured child PowerShell, and a fixture `.cmd` service. A driver-scoped `dotnet` function intercepts the script's unqualified build invocation, records arguments/attempts, emits synthetic build output, and sets `$global:LASTEXITCODE`; it never invokes the installed SDK. Assert the interception was used. The harmless service records its inherited attempt/supervisor values, increments a launch counter, and sets its own temp desired-state to stopped at the selected iteration. Keep real five-second build retry and three-second service retry delays in this small fixture; long build costs belong to F-2. Capture stdout/stderr asynchronously, set a 45-second outer limit, and on failure clean up only captured child PID/start-time pairs. No Scheduled Task, main `bin/` output, real fake-gateway broker, or real build runs inside this fixture.

**F-5: Held adoption in the production executable.** Use the existing executable-launch pattern in `GrokRulesHttpAcceptanceTests` and the existing `TestSessionLogRoot.Create` helper (defined in `PtyHostLeakSweep.cs`). The runner executable must come from this test run's `AppContext.BaseDirectory`, with its matching runtime files; fail if it is missing instead of falling back to a production binary. Launch it directly with `UseShellExecute=false`, `CreateNoWindow=true`, and async output capture. Reserve a random loopback port with `TcpListener(..., 0)`, record it, release the reservation just before launching, and pass `--urls http://127.0.0.1:<port>`. Exclude all documented shared stack ports; a bind collision is fixture failure with captured evidence, never a barrier pass.

Pass explicit child-only settings/arguments for `SessionRunner:SessionLogPath`, `SessionRunner:PtyHostDir` and `Serilog:LogPath` under the fixture root, `SessionRunner:Herdr:Enabled=true`, and `SessionRunner:Herdr:Session=<FakeHerdrServer.Session>`. Disable CPU watchdog and Herdr status push for this fixture; give the liveness sweep a 60,000 ms interval to avoid background fate changes during the first-list assertion. Keep actual adoption, its real liveness probe and the Herdr event pump. Do not use a provider login, transcript discovery, production Codex/Grok home, or real Herdr session.

Create one owned inert `cmd.exe` child with redirected input kept open, and record its PID/start time. Seed a `HerdrPaneSidecar` using `SaveAtomic` and the existing `HerdrAdoptionSweepTests.SeedPane` structure: unique session/workspace/tab/pane IDs, this child's real PID and start time, temp cwd, no transcript sidecar or rules receipt. Set the fake pane's foreground process list to that child; never use the example hard-coded PID 4243 or the test host's PID. Also seed one `PtyHostManifest` with a distinct session ID, `HostPid=0`, a unique unused pipe name, and a recorded exit code/reason. This supplies an existing terminal representation without starting a pty-host or risking a kill of an unrelated process.

Before starting the runner, call `fake.GateMethod("pane.read")`. Await the fake's recorded `pane.read` request for the seeded pane, and verify the runner and owned dummy are alive. This is the positive evidence that the **actual startup adoption** reached the hold. While that gate remains held, independently probe `/health` and `/sessions` for five seconds using 250 ms per-request cancellation and 100 ms cadence. Fail on any successful HTTP response, any `ApplicationStarted` milestone or any early runner exit. A setup exception, no received RPC, a bind error or a dead runner is a fixture failure, never success.

Use two data rows in `RunnerStartupReadinessTests.Adoption_blocks_http_until_the_complete_session_set_is_represented`: (a) release the gate normally, then expect the first successful `/sessions` response to contain exactly the seeded Herdr Running session plus the terminal pty session; (b) drop the held request and dispose the fake server while keeping the owned dummy alive, then expect the first successful list to contain Herdr `Starting` with the existing Pending/unreachable reason plus the same terminal pty session. Disposing the fake in row (b) prevents the event pump from immediately recovering Pending before that assertion. Both rows must subsequently return health 200. Assert the first successful list, not a list polled until the missing row eventually appears. Capture the full pair of HTTP observations and phase records.

Allow 20 seconds to reach the gate and 15 seconds after release for health/list, with a 60-second fixture watchdog. At teardown always release/drop gates, stop only the owned runner/dummy handles, and await exit/output tasks before deleting the resolved fixture root. Keep artifacts on failure. Do not use the production restart script or process-name sweeps. The fixture adds no startup-composition seam and no production skip/delay flag: the existing `FakeHerdrServer.GateMethod`, `Requests`, `Release` and `Drop` APIs are sufficient.

### Proves it works now

Every named new test below is implemented by Code. Parameterized rows are independently asserted; Code reports actual executed counts, not the number of V-items.

| ID | Behavior and layer | Test / procedure | Required assertion |
|---|---|---|---|
| V-1 | Observed slow startup fits the new default; wrapper integration with virtual time | `RunnerRestartHealthTests.Default_budget_accepts_100_second_startup` and `Explicit_60_second_wait_can_be_continued_without_restart`, F-1/F-2 | Invoke the actual entry script with no TimeoutSec: result/exit `healthy`/0, timeout 180, wait 100,000 ms, first-200 time origin +100 s. With explicit 60, result/exit `wait-expired`/2 at 60,000 ms and no first-200 value. Continue the same simulated startup at absolute +60 s using a fresh `-WaitOnly` observation: healthy after 40,000 ms with no new control calls. |
| V-2 | Budget has a fixed monotonic origin; wrapper integration with virtual time | `Budget_starts_after_restart_actions_and_never_resets` and `Wall_clock_jumps_do_not_change_wait_budget` | Make restart actions consume 7,000 ms: wait still starts afterward, command elapsed includes them. A permanently unhealthy runner ends after exactly the configured wait despite phase changes, retries and an unchanged live PID. UTC jumps of +1 hour and -1 hour change reported UTC only, not probe/sleep counts or monotonic expiry. Cover missing diagnostics and a stuck adopting phase as `wait-expired`, never `action-failed`. |
| V-3 | Immediate and deadline-bound HTTP; unit and short transport integration | `Probe_and_sleep_are_clamped_to_remaining_budget`, `Late_200_is_not_in_budget_success`, `Immediate_200_needs_no_initial_sleep`, and `Hung_http_response_is_canceled_at_wait_deadline` | Immediate verified 200 needs one probe/no sleep. No probe starts at/after expiry; every probe budget is min(5,000 ms, remaining), every sleep is min(2,000 ms, remaining). Test 1 ms and 750 ms remaining. A scripted transport returning 200 after deliberately overrunning its supplied budget by 1 ms must yield `wait-expired` with no successful first-200 field. Apply F-3's accepted-but-hung real request and 2,000..3,000 ms bound on both shells. |
| V-4 | Only verified HTTP 200 establishes success; wrapper/adapter integration | `Health_requires_200_from_the_expected_runner` | Data: connection refusal, timeout, 204, 302 with redirect following disabled, 404 and 503; all expire even with a live PID/open listener/build-success/ApplicationStarted marker. A foreign process returning 200 and a reused expected PID with a different start time cannot pass. An expected process with 200 does pass even with unreadable/missing startup logs. Record the actual runner PID separately from the supervisor and cmd wrapper. |
| V-5 | Read-only continuation and invalid combinations; wrapper integration | `WaitOnly_never_changes_daemon_control_state` and `Invalid_arguments_fail_before_any_control_operation` | WaitOnly rows: immediate healthy, eventual healthy, expiry, missing PID files and desired-state initially stopped. Assert zero write/stop/kill/task calls and byte-identical sentinel files. WaitOnly+Hard, WaitOnly+KillSessions, TimeoutSec 0 and -1 exit 1 before control mutation. Preserve intentional binding errors rather than coercing invalid budgets to defaults. |
| V-6 | Expiry leaves the attempt running; wrapper and transport integration | `Expiry_has_no_restart_or_stop_side_effects` | Snapshot the control trace at wait start for soft restart, Hard restart and WaitOnly; its mutation suffix stays empty through expiry. Simulated build/service/supervisor remain alive. In F-3 the server continues accepting requests after observer expiry. Output names WaitOnly as the continuation command and explains that only observation stopped; it must not suggest Hard/KillSessions as timeout recovery. |
| V-7 | Action errors, retry observations and success are distinct; wrapper integration | `Action_errors_report_the_failing_operation`, `Retrying_startup_can_recover_or_expire` | A task-start exception or an owned-stop error gives action-failed/1 with the injected operation/error and zero later launch/probe calls. Virtual build exit 17 + retry, launch error + retry, and service exit + retry each stay in the same health wait: one row later reaches healthy/0, another expires/2 retaining the current attempt's code. Absent/unreadable PID state or a detected dead supervisor with no successful health is still incomplete observation, not a newly invented action error. |
| V-8 | Result and human output agree; wrapper integration | `Final_result_matches_exit_and_first_observation` | Parse exactly one final `RUNNER RESTART RESULT:` JSON line, with no nonempty output after it. Assert all D-3 required fields, matching mode/exit/outcome, UTC precision, nonnegative durations, commandElapsedMs >= waitElapsedMs, and verified/unknown identities as appropriate. Healthy has first-200 observed time within its budget; other outcomes have absent/null first-200. For action failure before wait, elapsed wait is zero. Invalid typed numeric/combination arguments handled by the script use its action-failed result; unbindable PowerShell syntax/type errors may remain host binding errors, but must be nonzero and side-effect-free. |
| V-9 | Current correlated phases are useful, bounded and optional; unit plus wrapper integration | `Only_current_attempt_and_process_records_define_phase`, `Diagnostic_reader_survives_rotation_gaps_and_partial_records`, `Progress_output_is_throttled_without_changing_deadline` | Drive real parser inputs for build, adoption, awaiting HTTP, retry, then a new attempt. Verify phase age and next-poll visibility. Only-stale ID, same PID/different start, prior service, foreign producer, unknown schema and legacy prose yield unknown. An unrelated stale record cannot overwrite a trusted current phase. Partial JSON/UTF-8 is buffered until its complete newline; malformed/oversize records, >256 KiB gaps, truncation/replacement and read errors do not affect outcome. Recover when a fresh attributable record arrives. Repeated identical phases yield at most one continuing-wait line per 15 s; transitions appear by the next probe cycle and do not reset the timer. |
| V-10 | Supervisor emits real correlated attempts without changing launch/retry behavior; integration | `DaemonStartupDiagnosticsTests.Successful_launch_records_build_and_wrapper_identity`, `Build_failure_retries_without_launching_failed_output`, `Every_relaunch_builds_and_gets_a_fresh_attempt`, `Nonrunner_without_build_keeps_its_launch_behavior`, F-4 | Success events carry version, UTC milliseconds, correct supervisor/wrapper identities and nonnegative build duration. Failed first build (17) produces no service launch until a later build succeeds; retry still requests five seconds. Two service runs with unchanged inputs invoke the build twice, with distinct attempt IDs and the three-second service restart delay. Child correlation agrees with its producer's actual record and does not replace the driver's environment values. The no-build generic daemon still starts/stops normally. Assertions use recorded build/launch counts as well as milestones. |
| V-11 | Actual runner phase records cover no work, multiple backends and errors; unit/integration | `RunnerStartupDiagnosticsTests.Empty_sweep_records_zero_work_phases`, `Canceled_manifest_pass_preserves_cancellation`, `Diagnostic_sink_failure_does_not_change_adoption`, plus F-5 output | Empty root: explicit claim/Herdr (disabled or zero-work)/pty summaries, adopted count 0. With a seeded manifest and a canceled token at manifest entry: original OperationCanceledException propagates and the phase is failed/canceled, not completed. Use a recording logger that throws only for new `ANTIPHON_STARTUP` instrumentation while allowing old runtime logs: adoption fate/count/claims and exception propagation remain identical to a nonthrowing sink. F-5 supplies real managed-entry, resolution, claims, Herdr, pty, sweep-complete, ApplicationStarted and process identity order; completed zero-work pty successes stay 0 although the session list has terminal/Herdr rows. |
| V-12 | Production startup preserves complete-session readiness; process/HTTP integration | `RunnerStartupReadinessTests.Adoption_blocks_http_until_the_complete_session_set_is_represented`, F-5 both data rows | Gate entry is proved by an actual pane.read request and live owned processes; health/sessions cannot succeed and ApplicationStarted cannot occur while held. Release yields first complete list + 200; normal release gives Herdr Running, dropped/unreachable release gives Pending, and the terminal pty row is present in both. Telemetry never makes the gate optional. |
| V-13 | PowerShell fallback, help and deployment guards remain valid; script integration/static contracts | `RunnerRestartScriptCompatibilityTests.Touched_scripts_parse_as_ascii_on_both_shells`, `Documented_arguments_execute_the_expected_mode` | Parse all touched daemon/autostart/restart/helper scripts with `System.Management.Automation.Language.Parser.ParseFile` under pwsh and powershell.exe; zero parser errors and all bytes <=127. Execute F-1's immediate healthy, short expiry, WaitOnly and action-error cases on both shells, plus F-3/F-4 under both shells to exercise APIs rather than syntax only. Help shows default 180 and WaitOnly examples. Retain existing `PtyHostDeploymentGuardTests` and `DaemonLogRotationTests`. |

Inject throwing behavior at the instrumentation sink, not at the whole ILogger pipeline, so V-11 concerns the new diagnostics rather than unrelated pre-existing logging behavior. Also assert that synthetic prompt/rules/environment sentinels do not appear inside new milestone JSON or final-result fields. Inspect actual emitted supervisor and runner records with the production PowerShell decoder at least once (V-9/V-10/F-5), so two independently invented schemas cannot both pass their own fixture tests.

### Guards the regression

| ID | Regression | Caught by / reason |
|---|---|---|
| R-1 | Restore the 60-second default, reset deadlines on progress, use UTC as the budget clock, or leave a five-second in-flight request beyond the remainder. | V-1/V-2/V-3 assert the actual default, exact virtual elapsed/probe budgets, wall-clock independence and the short real transport bound. |
| R-2 | Turn observation expiry into failed action, or retry/kill the runner when the wait ends. | V-5/V-6/V-7/V-8 independently check exit/result and the empty mutation suffix; fake mutations record before throwing. |
| R-3 | Admit health based on process/log status, foreign listener, stale build result or late 200. | V-3/V-4/V-9 have live-looking negative evidence and stale/reused identities while keeping health unavailable. |
| R-4 | Move HTTP before adoption or hide rows until a later post-health poll. | V-12 probes both endpoints while a real adoption RPC is held and asserts the first successful list in Running and Pending cases. |
| R-5 | Instrumentation changes adoption fate or transcript ownership. | V-11 plus `PtyHostAdoptionTests` (whole class), `GrokRulesAdoptionTests` (whole class), and these `TranscriptAdoptionSafetyTests` methods: `Claims_are_restored_from_sidecars_before_new_adoption_runs`, `Sidecar_path_is_retailed_directly_after_restart_with_no_discovery`, `Restart_adopt_without_a_bound_transcript_stays_unbound_until_new_input`, `THE_CARD_0181_shape_exact_id_bind_displaces_a_stale_sidecar_claim_after_restart`. These exercise survival, real recorded exits, rules receipt verification, claim restoration and bind ownership, not just text logs. |
| R-6 | Redefine Herdr Pending or initial fate decisions to make health come up sooner. | V-12 plus `HerdrAdoptionSweepTests.R1_runner_restart_adopts_when_pane_lists_the_child`, `R2_restored_empty_pane_with_os_dead_is_RestartPresumedDead`, `R6_unreachable_at_restart_with_os_alive_is_pending_then_adopts_when_herdr_returns`, and `R7_herdr_unreachable_and_os_dead_is_ChildGone_without_a_socket`. Named methods cover the relevant sweep decisions; unrelated placement/relaunch cases do not force a full Herdr class run. |
| R-7 | Treat an unreachable runner as an empty authoritative list, or weaken the missing-session consequence instead of preserving readiness. | In `tests/Antiphon.Tests`, re-run `SessionReconciliationServiceTests.Unreachable_runner_skips_the_session_pass`, `Session_unknown_to_runner_is_failed_and_its_agent_reset`, and `Pending_herdr_session_is_not_failed_by_pass_1`. They pin why the barrier exists, while V-12 tests the runner side of it. Existing per-test data/production-runner guard remain active. |
| R-8 | Skip unchanged builds, use dotnet run, alter generic daemon retry/rotation, or break the fallback shell. | V-10/V-13 plus `DaemonLogRotationTests` and `PtyHostDeploymentGuardTests`; assert build count on two unchanged launches and child inheritance as well as source deployment contracts. |

### Positive controls

Run controls only after the named unmodified tests are green. Each row is a **temporary one-line production change**, not a changed expectation or a mock instructed to fail. Record the exact diff/location, failing assertion and test count, then revert only that line and rerun the same test green. Never commit a mutant. Use F-1 intercepted operations and F-5 isolated processes throughout. A fixture timeout, no selected tests, compilation error or setup failure does not count as the intended red. Names of newly introduced internal functions are proposals; choose the corresponding actual predicate at Code time and report its location.

| ID | Temporary change | Required red, then restored green |
|---|---|---|
| PC-1 | Set the entry script's default `TimeoutSec` back to 60. | V-1 `Default_budget_accepts_100_second_startup` returns expiry instead of healthy and exposes timeoutSec 60. |
| PC-2 | Remove the elapsed-at-completion predicate from the HTTP-success condition, leaving status/identity checks. | V-3 `Late_200_is_not_in_budget_success` wrongly accepts the 1 ms late response. |
| PC-3 | Replace the per-probe `min(5000, remainingMs)` budget with 5000. | V-3 `Probe_and_sleep_are_clamped_to_remaining_budget` records an oversized 1/750 ms remainder request. No long real wait is necessary for this control. |
| PC-4 | Change the guard surrounding the real restart-control block from `if (-not $WaitOnly)` to `if ($true)`. | V-5 `WaitOnly_never_changes_daemon_control_state` reports a forbidden mutation in the trace. The test platform absorbs it; no live state changes. |
| PC-5 | Add one call to the existing platform's stop-service operation on the wait-expired return path. | V-6 `Expiry_has_no_restart_or_stop_side_effects` sees a nonempty post-wait-start mutation suffix. |
| PC-6 | Map wait expiry to action-failed/exit 1 instead of wait-expired/exit 2. | V-7's no-health/no-action-error row or V-1's explicit-60 row fails the independent outcome/exit assertions. |
| PC-7 | Replace the successful HTTP status predicate with true, retaining a response/identity requirement. | V-4's expected-runner 503 row incorrectly passes. |
| PC-8 | Replace the runner identity-match predicate with true, retaining the 200/time checks. | V-4's foreign 200 / reused PID row incorrectly passes; the fake identity source makes this safe. |
| PC-9 | Bypass the attempt/process-start match check when consuming a startup milestone. | V-9's only-stale record incorrectly supplies a phase instead of unknown. |
| PC-10 | In `Program.cs`, replace the awaited adoption assignment with `var adopted = 0; _ = runtime.AdoptOrphanedHostsAsync(probe, CancellationToken.None);` (use the corresponding expression if S2 renames locals). | V-12 sees early HTTP success or ApplicationStarted while pane.read is held. Require that specific failure; if the gate setup failed, fix the fixture before accepting this control. |
| PC-11 | Remove/comment the `RestoreTranscriptClaims();` call at the start of `AdoptOrphanedHostsAsync`. | R-5's existing `Claims_are_restored_from_sidecars_before_new_adoption_runs` fails its cross-session claim assertion. |
| PC-12 | Replace the new instrumentation helper's isolated sink-error suppression with `throw;`. | V-11 `Diagnostic_sink_failure_does_not_change_adoption` fails its unchanged-fate/completion assertion using the throwing instrumentation-only sink. |
| PC-13 | Change the supervisor's build guard to false (`if ($false)` in place of `if ($BuildProjectDir)`). | V-10 `Every_relaunch_builds_and_gets_a_fresh_attempt` sees zero rather than two build invocations. Its harmless service still exits via temp state; no build or launch of production output occurs. |

For modified C# controls (PC-10/PC-11/PC-12), rebuild using the same isolated output path before both red and restored-green runs. For PowerShell-only controls the already-built C# harness can run with `--no-build`, because it reads the actual scripts each time; verify the fixture copy was refreshed after each edit. Also retain V-11's original-cancellation assertion while adding diagnostics: instrumentation must not convert a canceled sweep into a successful one.

### Commands and reporting

Run from the Code checkout, sequentially. The following commands are the required target filters after the named tests exist; this TestDesign did not execute them. No namespace-wide or full-solution suite is required.

```powershell
$runnerClasses = @(
    'RunnerRestartHealthTests',
    'RunnerRestartScriptCompatibilityTests',
    'DaemonStartupDiagnosticsTests',
    'RunnerStartupDiagnosticsTests',
    'RunnerStartupReadinessTests',
    'DaemonLogRotationTests',
    'PtyHostDeploymentGuardTests',
    'PtyHostAdoptionTests',
    'GrokRulesAdoptionTests'
)
foreach ($testClass in $runnerClasses) {
    dotnet run --project tests/Antiphon.SessionRunner.Tests --property:OutputPath=bin-c420/ -- --treenode-filter "/*/*/$testClass/*"
    if ($LASTEXITCODE -ne 0) { throw "Runner verification failed: $testClass (exit $LASTEXITCODE)" }
}

$runnerMethods = @(
    'TranscriptAdoptionSafetyTests/Claims_are_restored_from_sidecars_before_new_adoption_runs',
    'TranscriptAdoptionSafetyTests/Sidecar_path_is_retailed_directly_after_restart_with_no_discovery',
    'TranscriptAdoptionSafetyTests/Restart_adopt_without_a_bound_transcript_stays_unbound_until_new_input',
    'TranscriptAdoptionSafetyTests/THE_CARD_0181_shape_exact_id_bind_displaces_a_stale_sidecar_claim_after_restart',
    'HerdrAdoptionSweepTests/R1_runner_restart_adopts_when_pane_lists_the_child',
    'HerdrAdoptionSweepTests/R2_restored_empty_pane_with_os_dead_is_RestartPresumedDead',
    'HerdrAdoptionSweepTests/R6_unreachable_at_restart_with_os_alive_is_pending_then_adopts_when_herdr_returns',
    'HerdrAdoptionSweepTests/R7_herdr_unreachable_and_os_dead_is_ChildGone_without_a_socket'
)
foreach ($testMethod in $runnerMethods) {
    dotnet run --project tests/Antiphon.SessionRunner.Tests --property:OutputPath=bin-c420/ -- --treenode-filter "/*/*/$testMethod"
    if ($LASTEXITCODE -ne 0) { throw "Runner regression failed: $testMethod (exit $LASTEXITCODE)" }
}

$reconciliationMethods = @(
    'Unreachable_runner_skips_the_session_pass',
    'Session_unknown_to_runner_is_failed_and_its_agent_reset',
    'Pending_herdr_session_is_not_failed_by_pass_1'
)
foreach ($testMethod in $reconciliationMethods) {
    dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c420/ -- --treenode-filter "/*/*/SessionReconciliationServiceTests/$testMethod"
    if ($LASTEXITCODE -ne 0) { throw "Reconciliation regression failed: $testMethod (exit $LASTEXITCODE)" }
}
```

For each PC, use the same project/output/filter shape restricted to the single named method, e.g. `--treenode-filter "/*/*/RunnerStartupReadinessTests/Adoption_blocks_http_until_the_complete_session_set_is_represented"`. Require at least one executed test and every intended data row; a zero-test selection is failure even if the host exits 0. Keep the full runner-class invocation for the first build, then `--no-build` is permissible only while its compiled inputs are unchanged. Never use `dotnet test`.

Code reports one table covering V-1..V-13, R-1..R-8 and PC-1..PC-13: actual commands, executed/pass/fail/skip counts, relevant observed durations, and artifact paths. PC rows carry red assertion, reverted line and green result. Preserve structured result lines, synthetic call traces, producer milestones and F-5 gate/request/first-list evidence in test output. Missing Windows fallback execution or a missing fixture dependency is reported as a verification gap, not silently relabeled green. Do not create provider sessions to repair a fixture. Before committing, verify all mutants are reverted and `git diff --check` passes. Generated state and test output are not deliverables to stage.

### Out of scope

- Production restarts, Scheduled Task changes, live provider authentication/messages, live Herdr panes and empirical fleet latency sampling. They belong to the separately scheduled deployment/measurement procedure above; acceptance here is isolated.
- Parallel/background adoption, build-input caches, reduced pipe deadlines, detailed MSBuild target profiling, session-count timeout scaling and a latency percentile. D-1/D-6 explicitly defer them. F-4 proves that builds still happen, not how fast the real SDK can compile.
- Browser/client/E2E and `Antiphon.Agents.Pty.Tests` suites. No changes to those layers are planned. The existing runner adoption tests provide relevant host survival evidence without forcing unrelated UI or provider canaries.
- Full transcript discovery/Herdr placement/reconciliation assemblies. The named R-5/R-6/R-7 methods target the sweep, claims and readiness consequences; expand only if Code changes those other behaviors or a specific failure warrants it.
- A new worktree guard, lock/restart-election policy or process-reaping policy. F-1 intercepts control and validates no new expiry/WaitOnly mutations; it does not authorize changes to unrelated operational policy.

### Cost

Suites forced: the nine runner classes and eight runner methods listed above, then three reconciliation methods in `Antiphon.Tests`; both shells for the compatibility/real adapter/supervisor fixtures. No live environment is required beyond Windows, the installed PowerShell hosts, the pinned .NET SDK and the repository's isolated Postgres test fixture for reconciliation.

Estimated verification floor is **about 20 minutes with warm build/package/container caches**, including all 13 red/revert/green controls; allow roughly 30-40 minutes for cold builds/container startup and diagnostics. These are planning estimates, not measured timings. The 60/100/180-second scenarios take virtual milliseconds; the purposeful real waits are two-second HTTP deadlines, five-second barrier holds, and the supervisor's existing three/five-second retry delays. If costs exceed this because of build/output issues, follow `docs/testing-and-build.md` rather than widening assertions or borrowing production binaries. Report actual elapsed time and counts in Code.

--- next stage ---
next: code
handoff: Implement CARD-0420 S1-S4 under unchanged D-1..D-6, then execute V-1..V-13, R-1..R-8 and PC-1..PC-13 with the specified isolated script/clock/HTTP/Herdr fixtures; preserve adoption-before-HTTP, report counts and red/revert/green evidence, and do not restart the live fleet.
artifact: docs/superpowers/plans/2026-09-07-card-0420-runner-restart-timeout-plan.md
