# CARD-0388: Bound repeated Herdr failures and document orchestrator ownership

Plan date: 2026-09-06. Card: CARD-0388 / GitHub #44 (`fdd607e6-19c5-4b87-8198-fc84e70aaf0a`, Antiphon board). Inspected checkout and freshly fetched `origin/master`: `567f9e952d81a3be392b8699c6061c2dbbf605b5`.

After three consecutive qualifying terminal Herdr attempts for one standing AlwaysOn agent, persist a restart hold, cancel its pending restart, and surface an actionable Attention row. Keep the existing exponential ladder below that threshold and for unrelated failures. Treat the twin orchestrators as an operator ownership decision with a documented handover, without introducing a same-cwd uniqueness rule. This is a Plan artifact, ready for separate TestDesign; it changes no running agents.

## Dependency and incident verdict

| Item | Verified status and scope | Consequence for this card |
|---|---|---|
| CARD-0384 / GitHub #39 | Live card is InProgress. Code task `6a5d6f1f-1586-4380-83b9-b80d83392894` is Dispatched, not settled; branch `feat/card-task-6a5d6f1f` has committed tip `a842fe14`. The plan plus TestDesign lives at `docs/superpowers/plans/2026-09-06-card-0384-herdr-placement-by-name-plan.md` on that branch. Its plan commit is `d0e46d09`. Neither plan nor named-placement implementation is present on fetched master. | Idea 1 belongs entirely to CARD-0384. Its named-tab branch outranks last-pane and allocator, creates/reuses a dedicated single-pane target, refuses occupancy, and reserves the tab against other allocator launches. **Code for CARD-0388 must verify CARD-0384 has landed and take that base, or wait.** Do not copy its in-flight source or implement another placement mechanism. TestDesign can proceed from the documented contract. |
| CARD-0382 / GitHub #41 | Live card is Done; close reason names landed `567f9e95`, also the inspected/fetched master. Server composition/runtime and runner backstop refuse unsafe Windows Grok rules as `grok_rules_argv_unsafe`. | Idea 2 is already addressed by a refusal, not a transport. Do not implement file/env rules transport here, strip the orchestrator bundle, weaken the guard, or promise a successful bundled Grok launch. Full rules delivery belongs to CARD-0395. |
| CARD-0383 / GitHub #40 | Its implementation is already in this base: native-session existence guard plus idle-shell detect-timeout retention. See `docs/herdr-sessions.md` sections 4 and 8. | Preserve `herdr_grok_native_session_missing` and the kept last-pane behavior. A retained idle shell must not receive a new instruction telling the operator to close it. |

The full CARD-0388 incident text records 2026-09-05 UTC: `HerdrChildGone` at 00:18:46; `herdr did not detect agent kind 'grok' within 60000ms (last observed: none)` at 00:20:06; `HerdrPaneClosed` at 00:21:36, 00:23:17 and 00:25:56; and another timeout at 00:28:46 with restart attempt 7/backoff 5.3 minutes. A manual start put pane `w2:p1Y` on `w2:tX` (`MavRef-DL`), then it disappeared approximately 60 seconds later. These are the historical observations, not a new reproduction.

**CARD-0382 is a plausible explanation for the timeout/close pair, not a proven diagnosis of this incident.** Its original incident explicitly contains clap's `unexpected argument 'own'`, and describes the same 60-second detect timeout followed by cleanup. CARD-0388 contains no clap output or failing argv, and its earlier ChildGone is not explained by that text. The shared timing is compatible with Grok exiting before detection; it does not distinguish argv corruption from missing native resume, a wrapper/provider failure, or another early exit. Do not declare either a confirmed duplicate or a confirmed different root cause. Once the CARD-0382 guards run, that particular unsafe-rules request refuses before pane allocation; a newly observed pane-producing timeout requires its own evidence and a check of loaded server/runner versions.

Evidence limit: the current `http://localhost:17202` API returns 98 agents, but neither historical ID/name nor the PM specialist names are in that list; GET for `2ee02f40-7b6d-48b1-96fc-c4344c651910` returns 404. No matching PM channel title was found. Therefore current Slack ownership and the original process output cannot be verified here. Do not infer that the twins were fixed, deleted everywhere, or never existed. The card is sufficient evidence for designing bounded retries; attribution of the historical launch failure remains unconfirmed.

## Ground truth

Paths and line numbers describe `567f9e95` and will move when CARD-0384 lands.

| Card assumption / question | Code fact and design implication |
|---|---|
| There might be a retry ceiling already. | `server/Application/Services/AgentSupervisorService.cs:106-290` intentionally never gives up. It checks `Suspended`, treats Starting/Running/Stopping as live, schedules a dead persistent session, increments `ConsecutiveFailures` after a prior attempt, and calls `AgentControlService.StartAsync`. Synchronous start exceptions grow the same ladder. There is no Herdr-specific threshold. |
| Attempt 7 / 5.3m seems abnormal. | `SupervisionSettings.cs:11-23`: tick 10s, base 5s, cap 2,592,000s (30d), health reset 10min, fresh after two failures. `Backoff(6)` is 320s = 5.3m. The observed delay is expected policy, not a backoff arithmetic defect. Existing hourly/daily `BackoffEscalated` incidents are too late for this pane loop. |
| A Start response proves the agent recovered. | `AgentControlService.cs:316-349` marks/resets a same-ID row Starting and enqueues launch; `AgentSessionService.cs:333-401` can fail it later. Fresh fallback can instead create a new session ID. Successful enqueue must not reset a failure streak. |
| The named incident strings can be counted in AgentIncidents. | `AgentIncidentKind.cs` contains Crash=0, StartFailure=1, RestartScheduled=2, BackoffEscalated=4, HerdrStatusDisagreement=34, HerdrUnreachable=37 and HerdrPaneLeftOpen=38. **HerdrPaneClosed, HerdrChildGone and detect-timeout are not incident kinds.** Their typed homes are `Application/Interfaces/AgentExitReason.cs` (6, 7, 10) and `SessionRunnerContracts.cs:644-679` (`HerdrExitReasons`); runner launch refusal `HerdrLaunchException.CodeDetectTimeout` is `detect_timeout`. |
| All detect failures leave an incident. | `HerdrPaneChild.cs:607,982-983` uses the typed timeout; `AgentSessionRuntime.cs:238-246` deliberately logs HerdrLaunchDetectTimeout without an incident. Runtime exit stores human-readable FailureReason (`:167-181`); queued launch catch can overwrite it with `ex.Message` (`AgentSessionService.cs:361-382`). Counting messages or generic Crash rows is lossy and can double count cleanup. |
| Runtime events are the only closer. | `SessionReconciliationService.cs:189-205` also closes a row when the runner reports an unobserved exit, using typed `ExitReason`. Include this durable recovery path. Herdr events themselves are only verification triggers; `docs/herdr-sessions.md` section 7 warns of replayed pane_closed events. |
| Stop and Start already express human intent. | `AgentControlService.cs:665-742`: Stop sets user `Suspended`; **any** Start (human, bridge, supervisor) clears it, `NextRestartAt`, and `LivenessLatchedAt`, but preserves general ConsecutiveFailures. Do not overload Suspended or the boot-reply latch with an automatic Herdr hold. |
| Suppressing only Tick is enough. | `ChannelBridgeService.cs:439-442` also starts a dead bound agent. A hold must survive this call and other ordinary internal Starts. The bridge currently handles model-held refusal specially; a new hold refusal needs an explicit branch rather than an unhandled consumer failure. |
| Existing Attention automatically retains an unresolved hold. | `AttentionService.cs:1419-1490` only groups recent Error-or-worse incidents within the lookback. A hold must be projected from durable state so it survives 24h and incident pruning. The boot-reply/queued-input projections demonstrate condition-based rows, but their time-limited incident queries are not the lifetime of this hold. |
| Same cwd means both agents own the same channel. | `Domain/Entities/ChatChannel.cs` has one nullable AgentId per channel; `ChatChannelService.cs:43-79` updates that binding explicitly. Cwd, agent name, backend, and AlwaysOn do not choose a channel owner. Same-cwd specialists are legitimate; a uniqueness guard would reject them without establishing Slack ownership. |

## Decisions

### D-1: Three qualifying failures, with a separate durable streak

Add `Supervision:HerdrFailureLimit` with default **3**, validated as an integer from 1 through 10. Three counts the initial failed process attempt plus two more failed attempts, not three retries after the initial failure. It allows transient recovery while stopping well before attempt 7. No time-based half-open probe and no automatic unlimited reset after a cooldown. Changing the limit cannot clear an existing hold.

Eligibility when recording/consuming an outcome: owning persistent standing session (`CardId == null`, non-pool agent), agent AlwaysOn, and **session snapshot** backend Herdr. Apply across ClaudeCode, Grok and Codex on that backend, not only one agent name or cwd. Maintain the existing general `ConsecutiveFailures`, delay formula, fresh threshold and escalation tiers; they have different meanings.

| Actual terminal evidence | Herdr streak action |
|---|---|
| Failed attempt with typed HerdrPaneClosed | Increment once. |
| Failed attempt with typed HerdrChildGone (confirmed dead child) | Increment once, in the same family. Excluding it would let the recorded mixed loop evade the limit. |
| Failed launch with runner code `detect_timeout`, including kept-pane HerdrLaunchDetectTimeout exit | Increment once. A timeout and its cleanup close are one outcome. |
| New terminal attempt with a confirmed different cause | Reset the Herdr streak; ordinary backoff remains. |
| Synchronous refusal before a new process/session attempt, e.g. `grok_rules_argv_unsafe`, `pane_occupied`, missing native session, quota/model hold | Neither count nor reset the pane-failure streak; retain its existing refusal/backoff path. No pane was launched. If the refusal occurs asynchronously on a new row, its nonqualifying terminal outcome resets the Herdr streak. |
| Runner/Herdr unreachable with no confirmed terminal outcome; Starting; unknown evidence | Do not infer a failure. No probe/kill/retype is added. |
| HerdrRestartPresumedDead; HerdrPaneLeftOpen; HerdrStatusDisagreement; provider stall; delivery failure; intentional Stop/detach/policy refresh | Not a qualifying cause. Preserve their existing recovery rules. Never count a pane-close cleanup of an intentional termination. |

The unit is a process-attempt outcome, not incident rows, timer ticks, pane count, or session ID alone. Both the observer and reconciler can report one outcome, while resume reuses the ID and refreshes `StartedAt` (`AgentControlService.cs:324-330`). Dedupe by **(SessionId, StartedAt)** using persisted database precision. Separate agents never share a counter, even in the same cwd. Mixed PaneClosed -> detect_timeout -> ChildGone reaches three.

### D-2: Record typed outcome evidence; consume it before another launch can erase it

Add a small domain enum `HerdrSupervisionFailureKind` with NonQualifying, PaneClosed, ChildGone and DetectTimeout; add nullable `AgentSession.HerdrSupervisionFailureKind`. Null means no classified evidence (including legacy/unknown), while NonQualifying positively identifies an excluded terminal cause and can reset the streak. This is narrowly scoped attempt evidence, not a second general termination taxonomy. Map the existing Application exit enum and runner exception code into it outside Domain. No Domain dependency on Application/runner contracts and no new runner wire protocol is needed.

Write the field in the same save as the terminal outcome at `AgentSessionRuntime.ObserveExitAsync`, `AgentSessionService.LaunchInteractiveAsync`'s failure catch, and the reconciler's runner-reported-exit arm. Capture the typed `ConflictException.Code` before the launch catch discards it into prose. Preserve first-closer termination semantics. DetectTimeout takes diagnostic precedence over its secondary cleanup PaneClosed; compatible observations may enrich the same attempt, never create another count. An unrelated later exception must not erase already proven qualifying cause for that attempt. Do not classify using arbitrary `FailureReason.Contains(...)` or backfill guessed causes from old incident text. Old rows have null evidence; the limit becomes effective as new attempts produce evidence.

Clear this session evidence on an actual new launch generation, including same-row resume and same-ID missing-resume fallback, alongside existing exit/termination reset. Existing ownership/liveness checks remain the authority for accepting an exit event; this change does not make a raw historical Herdr pane event authoritative. A fresh queued launch must not inherit the prior generation's evidence.

Extend `AgentSupervisionState` with:

- `HerdrConsecutiveFailures` (default 0).
- `HerdrFailureHeldAt` (nullable UTC timestamp).
- `LastHerdrFailureKind` (nullable enum, for the hold/agent display).
- `LastHerdrObservedSessionId` and `LastHerdrObservedStartedAt` (nullable pair identifying the consumed terminal attempt, including a nonqualifying terminal attempt).
- `HerdrHealthySince` (nullable timestamp for observed continuous Running).

Use one small concrete `HerdrSupervisionStateService` for consuming terminal evidence and changing this state, called from both SuperviseAsync and StartAsync. It uses AppDbContext, typed settings and TimeProvider; no runner/pane client and no dependency on AgentSupervisorService (avoid a control -> recorder -> supervisor -> control cycle). Add the hold audit incident directly in the same unit of work, as existing user-latch code does.

Consume the most recent classified terminal persistent attempt **before** scheduling/due-start logic and **before** Start clears/reset/replaces a resumable row. Do not consume null/unknown evidence. A queued launch whose failure cleanup is still owned by the launch queue is not yet a finalized attempt: let its catch finish classifying before consuming/relaunching it, using the existing launch-ownership seam. This prevents cleanup's generic killed outcome being consumed as NonQualifying before the actual detect-timeout catch arrives. Persist the dedupe pair, streak, hold and single audit incident together. Use the existing agent row lock with an actual transaction spanning the read/change/save; `FOR UPDATE` in an autocommit query alone is not mutual exclusion. Join an ambient transaction when present; keep this lock/transaction short and out of runner RPCs or detection waits. Re-read state when handing off to Start rather than saving a stale supervisor entity over an explicit retry. A concurrent manual/bridge Start must consume the same outcome once and observe the hold before enqueue. Tests must interleave these entry points.

On the third consumed qualifying outcome, set HeldAt, clear NextRestartAt and HealthySince, and save the hold. Do this even if a delay was already scheduled. Do not increment the ordinary counter a second time for merely processing the hold, and do not leave a misleading next-retry timestamp. The supervisor checks the hold again before invoking Start. If Start observes a concurrently committed hold and throws `herdr_supervision_held`, handle that separately from the generic StartFailure catch: reload state and leave NextRestartAt null, with no extra failure/escalation incident. Later ticks and server restarts perform no start, scheduling, or fresh-conversation fallback for that agent. The hold owns **future starts only**: it never calls kill, pane.close, tab.close, or a global runner stop. Existing cleanup and retained last-pane behavior remain unchanged.

### D-3: Healthy reset and explicit operator retry

Observe Running for the existing `HealthyUptimeResetMinutes` (10 by default) before clearing the Herdr streak. Start the new `HerdrHealthySince` at the first Running observation; clear it while Starting/terminal and when creating/resuming a new generation. Do not count the launch's 60-second detect wait as healthy time. Persist the timestamp across server restart. A brief Running interval, successful Start return, ready banner, or isolated transcript row does not reset the streak. Do not alter the general ladder's existing health calculation in this card.

A tripped hold stays until **explicit retry**, even if the failure limit, backend, cwd, labels or AlwaysOn setting changes. Add optional trailing `StartAgentRequest.ResetHerdrFailureHold = false`. While held, an ordinary dead-agent Start refuses `ConflictException` / 409 code `herdr_supervision_held` with the agent, count, last cause and remedy. Crucially, check this before `ClearSupervisionLatchAsync`, composition, named-placement preflight, row mutation and enqueue. An already-live Start remains idempotent and never kills/moves that process; merely observing a live session does not acknowledge the hold.

The operator's explicit retry sends `{"resetHerdrFailureHold":true}` (optionally alongside existing `fresh:true`). Under the same short state transaction, consume any unobserved old outcome first, then clear HeldAt and the Herdr streak/healthy timestamp, retain the consumed-attempt pair so the old failure cannot be counted again, and record a timeline-only retry acknowledgement. Preserve general ConsecutiveFailures and existing fresh/resume semantics. Normal launch validation still applies; this flag bypasses no Grok, provider, quota, placement or foreign-occupant guard. After another three qualifying failures it holds again. Do not require a new endpoint or permission modal.

Supervisor, channel bridge, policy refresh and other automatic callers leave the flag false. In ChannelBridgeService, handle this specific conflict as an unavailable held agent using the existing non-delivery/drop-reporting path: no retry loop, no reroute to the twin, no false delivery confirmation and no unhandled consumer exception. Do not create a new Slack send route. In the agent UI the existing Start affordance becomes **Retry and resume** when held and sends the explicit flag only on that click. Ordinary starts do not send it.

Stop keeps its user-Suspended behavior and does not relabel the automatic hold as SuspendedByUser. Existing LivenessLatchedAt behavior remains separate. An attach operation may bind an operator-started live process under its existing safety rules but must not silently clear the Herdr hold through the shared latch helper; explicit retry/acknowledgement remains necessary to permit later automatic relaunch.

### D-4: State-backed Attention, with one audit incident per hold

Append `AgentIncidentKind.HerdrSupervisionHeld` using the next free numeric value **on the rebased Code base** (53 is already used here; do not reserve a stale number across CARD-0384/other lands). Record Error severity once when HeldAt transitions null -> nonnull. Detail: agent ID/name, count and limit, terminal attempt ID/StartedAt, last typed cause, and the remedy to inspect/fix then explicitly retry. Keep rules bodies, environment values and provider secrets out of the record.

Use timeline-only recording (`raiseAlert:false` semantics). The human action lives in **Attention**, per the repository's decision contract; no new alert sink, direct Slack publisher, pager kind or recurring notification is part of this change. Existing operational failure alerts continue unchanged. If the operator wants a Slack summary, that is an explicitly configured existing digest/notification choice, not an acceptance condition or another delivery mechanism here.

Add `AttentionKind.HerdrSupervisionHeld`, one row per held agent, derived from nonnull HeldAt rather than a recent-incident query. Show even beyond the usual lookback, after incident pruning, or after disabling AlwaysOn while the hold still exists. Include OpenAgent (and the failed session drawer when available). Register its client type/visual/group and use existing SignalR invalidation through AgentChanged. Exclude the dedicated hold incident kind from the generic recent-incident projection so one unresolved episode is not two rows, and an acknowledged episode does not linger as a stale actionable incident.

Extend AgentSupervisionDto and mappings with held timestamp, Herdr count and last cause; include this state when present even if AlwaysOn was later disabled. Update SupervisionBadge to display **Herdr retries paused** and the reason, independently of the user-suspended badge and ordinary backoff countdown. Clearing the hold removes the Attention row; the incident stays as history. Attention itself is read-only and never schedules/spawns/moves a card.

### D-5: Twin ownership is an operator runbook, not a cwd guard

No create/PATCH refusal, duplicate-cwd warning, new uniqueness index, auto-stop or automatic backend election. The actual routing record is ChatChannel.AgentId; same cwd cannot establish whether the agents are competing owners, separate channel roles or specialists. A generic warning would add noise to supported multi-agent work and still not resolve Slack ownership.

Add a standing-orchestrator ownership subsection to `docs/herdr-sessions.md`, linked from `docs/ops-http.md`. State: one declared **primary per project/channel role**, explicit channel IDs and agent IDs, and a stopped/unbound standby unless a distinct role is recorded. Multiple AlwaysOn agents sharing cwd remain allowed. Never choose by backend, display name, a slug suffix, channel silence or pane position.

For the historical PM pair, recommend retaining **the currently verified working channel owner** while repairing the Herdr seat. The card's historical fallback candidate is `PredictionMarkets-Orchestrator` (`0c83e820`, Grok/PtyHost, formerly Running), with `PM-Orchestrator-Grok` (`2ee02f40-7b6d-48b1-96fc-c4344c651910`, Grok/Herdr) as standby. This is a recommendation under historical evidence, not a claim about current binding or authority to change live configuration. Do not recreate those IDs or transfer Slack based on this document.

Operator procedure, once the correct deployment is available:

1. Read agents, exact channel catalog bindings (provider/external channel identity and AgentId), session/transcript evidence and any active card/task responsibility. Record a small ownership table in the owning project's tracked operations document: role, full agent ID/name, backend, channel IDs, primary/standby and decision date. Keep generic procedure here; put the actual choice on CARD-0388's decision revision/Attention if it needs the caller's choice. Never edit generated `docs/cards/` files.
2. Preserve the verified working owner. If both seats are AlwaysOn for the same role, decide which is primary before stopping/rebinding either. A stopped standby should also be unbound from that role's channels: inbound Start otherwise defeats the intent of a simple stop under existing semantics. No delete is required. Do not interrupt current work without the ownership decision.
3. After CARD-0384 lands and is loaded, configure the intended Herdr seat with `herdrWorkspaceLabel=PredictionMarkets`, `herdrTabLabel=Orch`. Its workspace-token precedence still applies; inspect the resolved workspace rather than assuming the label forces relocation. Never close the retained timeout shell merely to clear an Attention item.
4. Before testing a launch, resolve any `grok_rules_argv_unsafe` refusal through CARD-0395 or an already verified supported configuration; named placement and this hold do not make multiline rules work. Do not remove mandatory bundles or switch backend silently.
5. Explicitly retry the repaired, unbound standby. Verify actual Orch placement, no specialist split, sustained Running through the health window, and transcript identity. If a channel handover is chosen, wait for outstanding replies/work to settle, update the single channel binding explicitly, read it back, and verify a real authorized inbound turn with transcript-confirmed UserPrompt and its routed reply. This Plan/Code stage sends no test message to a live Slack channel.
6. Record the outcome and stop/unbind the old role owner if a handover was made. If not, retain the working primary and document the Herdr seat as standby. Working-directory duplication by itself remains harmless and permitted.

No operator answer blocks Plan or TestDesign: D-1 through D-4 are implementation defaults; D-5 leaves the deployment-specific ownership selection to the operator before live handover.

### Rejected alternatives and boundaries

- Reuse ConsecutiveFailures as the threshold: mixes unrelated failures and doesn't identify one terminal attempt; would change the general never-give-up policy.
- Count Crash/RestartScheduled/StartFailure text: duplicates one failure, misses the kept-timeout path, loses typed codes and breaks after retention.
- Reuse Suspended or LivenessLatchedAt: confuses human Stop/provider-stall semantics and is cleared by automatic Start.
- Auto half-open every few minutes or silently fall back to PtyHost: restores the pane loop or silently changes the chosen backend.
- Kill remaining panes when the threshold is reached: exceeds the hold's purpose and violates ownership/kept-pane guarantees.
- A new generic circuit-breaker framework, attempt-history table, fleet-wide rate limiter, cwd lease or channel election system: unnecessary for this bounded per-agent hold.
- Treat CARD-0384 as already deployed, or make a multiline rules transport here: duplicates other cards and would misstate the remaining dependencies.

## Implementation slices

Land this Plan via the normal delegate land operation before TestDesign. Land the resulting TestDesign artifact before Code. **Code waits for CARD-0384's completed land and rebases onto that result**; do not build against an unlanded sibling. Re-read CARD-0384's actual public-preflight implementation because Start and the hold gate will share that method. Named placement must be configured, not just shipped, for the operational Orch acceptance.

| Slice | Files and concrete result | Tests to add/extend |
|---|---|---|
| S1: Typed evidence and durable state | `Domain/Entities/AgentSession.cs`, `AgentSupervisionState.cs`; new `Domain/Enums/HerdrSupervisionFailureKind.cs`; `Domain/Enums/AgentIncidentKind.cs`; `Infrastructure/Data/AppDbContext.cs`; CLI-generated migration and snapshot; `Application/Settings/SupervisionSettings.cs`, validator. Add evidence/state/defaults. Update runtime, launch catch and reconciler writers and new-generation clearing. | New `HerdrSupervisionFailureEvidenceTests`; extend `AgentSessionRuntimeTests`, `SessionReconciliationServiceTests`, settings validator tests and focused interactive-launch tests. Pin typed timeout across exception/cleanup order and no wrong-layer dependencies. |
| S2: Consume once and hold starts | New `Application/Services/HerdrSupervisionStateService.cs`; `AgentSupervisorService.cs`, `AgentControlService.cs`, `Application/Dtos/AgentDtos.cs`; normal DI composition registration. Implement D-1 through D-3 with transaction/dedupe and explicit flag. Do not touch runner allocation. | New `HerdrSupervisionBackoffTests` plus `AgentSupervisionTests` and `AgentControlServiceIntegrationTests`: three attempts, same-ID/fresh-ID, mixed causes, duplicate observations, restart persistence, held-before-due, health, acknowledgement and interleaved calls. |
| S3: Bridge and operator surfaces | `ChannelBridgeService.cs`; `AgentService.cs`; `AttentionService.cs`, `AttentionDtos.cs`; `client/src/api/agents.ts`, `attention.ts`; `features/agents/AgentsPage.tsx`; `features/attention/attentionVisuals.ts`. Explicit bridge refusal handling; persistent Attention projection, badge and Retry and resume request. | Focused `HerdrSupervisionAttentionTests`, bridge held-start test in `ChannelBridgeTests`; `AgentsPage.test.tsx` and `attentionVisuals.test.ts`. Verify no automatic reset flag, no phantom delivery, one Attention row and no direct alert send. |
| S4: Integration with landed named placement | Extend `HerdrAlwaysOnChannelParityTests` using isolated runtime/FakeHerdrServer and the landed CARD-0384 fixture hooks; only adapt production code if a demonstrated seam is missing. | Three failed starts with Orch pin and an existing MavRef-DL specialist: placement stays named, no fourth launch/split/create after hold, kept pane/hint survives, explicit repaired retry works. Preserve CARD-0384 public preflight refusal/no-fallback behavior and CARD-0382 no-pane refusal. |
| S5: Owner docs and completion report | `docs/herdr-sessions.md` supervision/ownership subsections, `docs/ops-http.md` Start flag/hold/recovery, `docs/antiphon-api.md` request/code documentation; update touched never-give-up XML comments to name this exception. Keep CARD-0384's placement table and CARD-0382/0383 guards. | Manual documentation review; report TestDesign V/R/PC evidence, dependency SHA, migration and loaded-version requirements. No production agent/channel mutations in Code. |

S1 precedes S2; S3 uses S2 contracts; S4 verifies the assembled behavior; S5 describes the resulting implementation. Generate EF migrations through CLI only. This plan adds server/schema/client behavior; no runner protocol/allocator changes are planned. Deploy from the main checkout using existing bootstrap procedures, not from this linked worktree. Verify the actual loaded server and named-placement runner version; a healthy endpoint does not prove either change loaded.

## Acceptance and TestDesign handoff

This medium-complexity change has a separate TestDesign stage. The following are required outcomes, not tests claimed to have run. TestDesign must supply named V/R cases and economical red-then-green positive controls before Code.

1. A standing Herdr agent fails three process attempts (initial plus two retries): after the third terminal outcome is consumed, HeldAt exists, NextRestartAt is null, and every later tick produces zero launch/placement calls. Verify ordinary 5s/10s/... scheduling before the threshold and retained general fresh-after-two policy. With a configured limit of 1, the first failed attempt holds; invalid limits fail validation.
2. Same session ID with different persisted StartedAt counts separately; a fresh session ID counts on the same agent; observer+reconciler+timeout cleanup for one generation count once, in either failure-writer order. Saving only a typed code or only a helper return is insufficient: drive the real terminal-writer and supervisor/start seams.
3. Mix PaneClosed, DetectTimeout and ChildGone; all three reach the same threshold. Include current kept-pane timeout with no AgentIncidentKind timeout row, and exact runner exception code propagated through the real interactive catch. Confirm generic crash/restart incidents do not multiply counts.
4. Negative controls: PtyHost and pool/card sessions; intentional Stop/detach/policy refresh; raw replayed Herdr event while the process is live; runner unreachable/unknown; HerdrRestartPresumedDead; provider/model/rules/placement refusal. Verify nonqualifying new terminal attempt reset versus pre-session refusal no-count/no-reset. A pending held retry must not bypass using fresh fallback.
5. Recreate service/context after two failures, then after the hold; preserve streak/dedupe/latch. Prune incidents and advance over 24h: one Attention row remains. Reset it explicitly: no actionable stale duplicate appears from the old hold incident. Concurrent Tick/Start consume once and cannot schedule/start through a committed hold; late stale tracked state cannot undo an acknowledgement or hold.
6. A queued Start or <10min Running interval does not reset. A 60s Starting interval contributes zero healthy time. Continuous observed Running for the configured interval resets the Herdr streak. New generation resets the health timestamp; server restart preserves it. Test general backoff recovery unchanged separately.
7. Held Start without flag: typed 409 before clearing other latches, preflight, row/enqueue and runner side effects. Explicit retry clears only the intended hold/streak, preserves ordinary failure count and the consumed old key, records acknowledgement, and runs all normal guards. Another three failures hold again. Changing labels, backend, limit or AlwaysOn does not silently acknowledge.
8. Channel inbound/automatic Start cannot set the flag or recreate panes. The bridge records its established non-delivery outcome, and never confirms/queues a delivered UserPrompt or reroutes to a twin. UI displays the actual hold and emits the flag only on explicit Retry and resume; Stop and LivenessLatchedAt regressions remain green.
9. Assembled named-placement scenario in S4 proves zero fourth pane-producing attempt, unchanged specialist tab and retained timeout last-pane. The hold introduces zero kill/close RPCs. CARD-0382 unsafe-rules refusal still reaches zero runner/pane side effects; named-placement refusal remains distinct from a detect failure.
10. Ownership is validated by the runbook, not a new code guard: two legitimate AlwaysOn agents in the same cwd remain creatable, and channel binding is unchanged until an explicit operator action. No test uses the real historical PM agent IDs or live Slack routing.

Positive controls should remove the hold gate, allow automatic Start to clear it, remove attempt dedupe, erase typed timeout evidence in cleanup, reset on enqueue, and make Attention rely only on recent incidents; the respective behavior tests must fail. TestDesign may combine controls when one integration scenario proves the distinction. Do not force a wide mutation matrix for documentation or copy tests that merely assert new field assignments.

Use class-scoped TUnit runs, `dotnet run`, not `dotnet test`. Candidate focused classes are in the slices; TestDesign finalizes the required subset and exact test names. Example command form:

```powershell
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c388/ -- --treenode-filter "/*/*/HerdrSupervisionBackoffTests/*"
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c388/ -- --treenode-filter "/*/*/AgentSupervisionTests/*"
pwsh -File scripts/test-client.ps1 AgentsPage.test
pwsh -File scripts/test-client.ps1 attentionVisuals.test
```

Global supervisor sweeps use ungrouped `[NotInParallel]` and row-scoped assertions against the shared test Postgres. Queue paths need a real-clock-offset/advancing TimeProvider, not a frozen clock with real timers. Process-spawning classes require their assembly's `ParallelLimiter<ProcessSpawnLimit>`; never co-run Antiphon.Tests with Antiphon.Agents.Pty.Tests. Use the established production-runner guard or an isolated runner; never start tests on port 17204. No real Herdr or provider canary is required to prove this server-side hold. If later requested, specify an isolated named Herdr session and owned-process cleanup first. Clean only verified worktree-local `bin-c388/` outputs after tests. No tests/builds ran in Plan.

## Completion and remaining operational evidence

Plan delivers the threshold, evidence source, persistence/dedupe, reset/bypass policy, Attention lifetime, Code slices and ownership procedure. Separate TestDesign is next. The original live symptom is not declared fixed by this artifact: closing CARD-0388 still requires named placement actually landed/configured, the selected seat's launch configuration to pass the rules guard, and an operator-recorded channel owner. Retain the historical cause as unconfirmed unless process/argv evidence establishes it.

## Verification design

TestDesign date: 2026-09-06, against plan commit `7c0794ee` on ground truth `567f9e95` (fetched `origin/master` at design time is still `567f9e95`; CARD-0384 has **not** landed — its branch `feat/card-task-6a5d6f1f` tip `a842fe14` is the TestDesign commit, no Code yet). This section adds verification only; D-1 through D-5 above are unchanged. Every case below is a concrete test Build writes, named so a filter can run it; no case asserts only that a new field was assigned or that a helper returned an enum.

Layers: **unit** = pure class, no I/O (`[Category("Unit")]`); **integration** = `Antiphon.Tests` on the shared `TestDbFixture` Postgres with `FakeAgentProtocolAdapter` (the runner is a stub); **E2E** = `HerdrAlwaysOnChannelParityTests` harness driving a real in-process `SessionRunnerRuntime` through `DirectSessionRunnerClient` + `FakeHerdrServer`; **client** = vitest + msw.

Conventions:

- `HELD` = `herdr_supervision_held` (D-3 code). `KIND` = `HerdrSupervisionFailureKind` with members `NonQualifying`, `PaneClosed`, `ChildGone`, `DetectTimeout` (D-2). `STATE` = the agent's `AgentSupervisionStates` row re-read from a fresh `AppDbContext`, never the tracked entity.
- "qualifying" (answers the open question precisely, per D-1): a **consumed terminal process attempt** whose `KIND` is `PaneClosed`, `ChildGone` or `DetectTimeout`. The three are one family: a different member does **not** reset the streak, so PaneClosed → DetectTimeout → ChildGone holds on the third. Only a consumed attempt whose `KIND` is `NonQualifying` resets to zero; a null `KIND` (unknown evidence, legacy row, synchronous refusal that never produced a row) neither counts nor resets. Ten minutes of observed continuous Running also resets (D-3).
- "no launch" = the sentinel adapter (see fixtures) has `Started == false`, no `AgentSessions` row for the agent was added, `LaunchQueue.WaitForIdleAsync` returns at once, and no `RestartScheduled`/`StartFailure` incident for the agent has `CreatedAt >= STATE.HerdrFailureHeldAt`.
- Runner outcome simulation in the server harness: `adapter.ThrowOnStart = new ConflictException("herdr did not detect agent kind 'grok' within 60000ms (last observed: none)", "detect_timeout")` is the exact shape `SessionRunnerHttpClient` produces from the runner's 409 (`SessionRunnerHttpClient.cs:580`) and reaches the real `AgentSessionService.LaunchInteractiveAsync` catch; exit events go through the real `AgentSessionRuntime.ObserveExitAsync(sessionId, exitCode, AgentExitReason, ct)`; runner-reported closes go through the real `SessionReconciliationService` with a `FakeRunnerClient` whose session carries `Status = "Exited"` and an `ExitReason`.

Fixture extensions Build adds before the first test (prerequisites, not tests):

- `AgentSupervisionTests.BuildHarness` becomes `internal static` (as `AgentControlServiceIntegrationTests.BuildHarness` already is) and gains optional `SupervisionSettings? supervision = null`, `ISessionRunnerClient? runner = null`, `bool includeModelAvailability = false`. `HerdrSupervisionBackoffTests` and `HerdrSupervisionAttentionTests` reuse it; do not copy a ninth harness (testing guide, CARD-0297 lesson). Its clock is already an offset over the real clock; keep it so (CARD-0222).
- `FakeAgentProtocolAdapter.StartGate` (`TaskCompletionSource?`): when set, `StartAsync` awaits it before honouring `ThrowOnStart`. Needed by V-15 only.
- Sentinel adapter convention: the last adapter handed to a harness has `ThrowOnStart = new InvalidOperationException("CARD-0388 sentinel: a launch reached the sentinel")`; a wrongly permitted launch therefore leaves a loud `FailureReason`, and every "no launch" assertion also checks no session row carries that text.
- `AttentionServiceTests.Scenario` gains `HoldHerdrAsync(Guid agentId, DateTime heldAt, int count, HerdrSupervisionFailureKind kind, Guid? consumedSessionId)` writing the `STATE` columns directly; and `Scenario` (or a copy of its minimal agent/session seeding) becomes reachable from `HerdrSupervisionAttentionTests`.
- `HerdrAlwaysOnChannelParityTests.BuildHarness` gains `int launchDetectTimeoutMs = 60_000` (set on the `HerdrSettings` it builds) and `SupervisionSettings? supervision = null`.
- The E2E lane needs `DirectSessionRunnerClient` to surface a `HerdrLaunchException` with `Code == "detect_timeout"` as `ConflictException(message, "detect_timeout")`, exactly as `SessionRunnerHttpClient` maps the HTTP 409. Build verifies this mapping before writing V-29; if the in-process client currently rethrows the raw exception, that is the "demonstrated missing seam" S4 permits fixing, and PC-4 depends on it.

Defaults decided by this design (Build implements as written; a human may override before Code starts — that is the `decide` in the next-stage block):

- **T-1 Exit-reason classification** (`Application/Services/HerdrSupervisionFailureEvidence.FromExitReason`): `HerdrPaneClosed` → PaneClosed; `HerdrChildGone` → ChildGone; `HerdrLaunchDetectTimeout` → DetectTimeout; `ProcessExited`, `KilledByRequest`, `MemoryKilled`, `CpuSpinKilled`, `HerdrRestartPresumedDead`, `HerdrPaneLeftOpen`, `HerdrDetached` → NonQualifying; `Unknown` → null. An exit event that arrives on a row already closed as `OperatorRequest` or `PolicyRefresh` writes no evidence (today's first-closer semantics stand; an intentional stop neither counts nor resets). A qualifying event arriving on a row whose evidence is already qualifying keeps the earlier value (DetectTimeout precedence over its cleanup PaneClosed); a non-qualifying event never downgrades a qualifying value on the same generation.
- **T-2 Launch-catch classification** (`FromLaunchFailure(Exception)`): `ConflictException{Code:"detect_timeout"}` → DetectTimeout; every other exception that lands the row Failed (`pane_occupied`, `grok_rules_argv_unsafe`, `herdr_grok_native_session_missing`, plain `InvalidOperationException`, `AgentLaunchBlockedException`) → NonQualifying. `ResumeTargetMissingException` is not terminal (fallback relaunch) and writes nothing.
- **T-3 Reconciler classification**: pass-2 "runner reported Exited" uses T-1 on `runnerSession.ExitReason`; pass-1 "unknown to runner" writes null (no inference from absence).
- **T-4 Incident kinds on this base**: `AgentIncidentKind.HerdrSupervisionHeld = 54` and `HerdrSupervisionRetried = 55` (Info, timeline-only, written by the explicit retry). Both renumber to the next free values on the rebased Code base; tests reference the names only.
- **T-5 Hold tick incidents**: the tick that consumes the third outcome still records today's `Crash` (the observation) and one `HerdrSupervisionHeld` (Error, `raiseAlert:false` semantics, `FailureReason` = the consumed session id); it records no `RestartScheduled` and no `BackoffEscalated`, and leaves `NextRestartAt` null.
- **T-6 Acknowledgement order**: `ResetHerdrFailureHold:true` is honoured at the hold gate, before `ClearSupervisionLatchAsync`, quota, model-availability, composition, named-placement preflight and enqueue. A later guard refusing that same Start (`model_disabled`, `grok_rules_argv_unsafe`, `pane_occupied`) leaves the hold acknowledged and the agent under ordinary supervision; the `HerdrSupervisionRetried` incident is still written. If the retry's own consumption of an unobserved outcome would reach the limit, no `HerdrSupervisionHeld` incident is written because `HeldAt` never persists non-null in that unit of work.
- **T-7 Bridge held branch**: `ChannelBridgeService.HandleInboundAsync` catches `ConflictException{Code:HELD}` from `EnsureAgentSessionAsync`, records one `ChannelReplyLost` incident per hold episode (`FailureReason == "HerdrSupervisionHeld"`, dedupe on `CreatedAt >= STATE.HerdrFailureHeldAt`, Critical when channel-bound, mirroring the ProviderCapacity shape), calls the existing `RaiseBridgeDropAlertAsync`, sends **no** chat notice, enqueues nothing, and returns.
- **T-8 Attention row**: `AttentionKind.HerdrSupervisionHeld = 31`, severity Error, `AgentId` set, `SessionId = STATE.LastHerdrObservedSessionId`, actions `[OpenAgent]` plus `OpenDrawer` when that session id is non-null, headline `Herdr retries paused for <agent>: <n> of <limit> attempts failed (<last kind>); inspect, fix, then Retry and resume`, evidence = the `HerdrSupervisionHeld` incident message when one exists, else the headline. `RecentCriticalIncident` grouping excludes kind `HerdrSupervisionHeld`.
- **T-9 Queue ownership**: while `AgentSessionLaunchQueue.Owns(deadRow.Id)` the supervisor neither consumes that row nor schedules from it on that tick.
- **T-10 Dedupe precision**: the pair is compared after a database round trip (Npgsql `timestamp` is microsecond precision); Build compares the re-read row's `StartedAt`, never the in-memory `DateTime`.
- **T-11 Held-Start message**: contains the agent name, `<n> of <limit>`, the last `KIND` name, and the literal `resetHerdrFailureHold`; contains no rules text, env value or exe path.

### Proves it works now

S1 — typed evidence and durable state

- V-1: classification is a pure, total mapping | unit | new `tests/Antiphon.Tests/Application/HerdrSupervisionFailureEvidenceTests.cs` — `Every_exit_reason_maps_per_T1` (`[Arguments]` over all eleven `AgentExitReason` values), `Launch_failures_map_per_T2` (`[Arguments]` over the exception shapes named in T-2), `Domain_enum_has_exactly_four_members_in_declared_order` | expected `KIND`/null per T-1/T-2; enum values `NonQualifying=0, PaneClosed=1, ChildGone=2, DetectTimeout=3`.
- V-2: settings | unit | same file — `HerdrFailureLimit_defaults_to_three`, `HerdrFailureLimit_outside_1_to_10_fails_validation` (`[Arguments(0)]`, `[Arguments(11)]`, `[Arguments(-1)]`), `HerdrFailureLimit_1_and_10_validate` | `SupervisionSettingsValidator.Validate` fails with a message containing `Supervision:HerdrFailureLimit`; 1 and 10 succeed; existing `InstructionFileStampTests` validator cases stay green.
- V-3: schema | unit (EF model) | same file — `Model_has_the_evidence_and_state_columns_and_the_migration_adds_only_them` | `AppDbContext.Model`: `AgentSession.HerdrSupervisionFailureKind` nullable int; `AgentSupervisionState` has `HerdrConsecutiveFailures` (non-null int, default 0), `HerdrFailureHeldAt`, `LastHerdrFailureKind`, `LastHerdrObservedSessionId`, `LastHerdrObservedStartedAt`, `HerdrHealthySince` (all nullable); the CLI-generated migration's `Up` adds exactly those six columns and nothing else.
- V-4: runtime exit writer | integration | `AgentSessionRuntimeTests` — `HerdrPaneClosed_exit_is_Failed_not_a_clean_stop` (extended: `session.HerdrSupervisionFailureKind == PaneClosed`), new `HerdrChildGone_exit_is_Failed_and_stamps_ChildGone`, `HerdrLaunchDetectTimeout_exit_is_Failed_and_does_not_record_HerdrPaneLeftOpen` (extended: `== DetectTimeout`), new `A_pane_closed_exit_after_DetectTimeout_evidence_keeps_DetectTimeout` (row seeded Failed/SystemRequest/DetectTimeout, then `ObserveExitAsync(PaneClosed)`), new `A_process_exit_stamps_NonQualifying_and_an_operator_stopped_row_gets_no_evidence` (seed `TerminationSource = OperatorRequest`, Stopped; `ObserveExitAsync(HerdrPaneClosed)` → status, source and evidence unchanged) | per T-1; existing status/`FailureReason` assertions untouched.
- V-5: launch-catch writer | integration | `AgentSessionLaunchFailureTests` (its private `LaunchFixture`) — new `A_runner_detect_timeout_409_stamps_DetectTimeout_and_keeps_the_runner_prose` (`ThrowOnStart` = the detect_timeout `ConflictException`; row `Status == Failed`, `TerminationSource == SystemRequest`, `HerdrSupervisionFailureKind == DetectTimeout`, `FailureReason` contains `did not detect agent kind`), new `A_runner_pane_occupied_409_on_a_row_stamps_NonQualifying`, `Grok_raw_multiline_rules_in_a_launch_argument_refuses_the_launch_before_the_process_starts` (extended: `== NonQualifying`), `Resume_not_found_kills_the_first_process_before_the_fallback_relaunch` (extended: evidence null after the successful fallback), new `A_missing_resume_fallback_that_then_times_out_stamps_DetectTimeout_on_the_same_row` (resume adapter throws `ResumeTargetMissingException`, fresh adapter throws detect_timeout).
- V-6: reconciler writer | integration | `SessionReconciliationServiceTests` — new `Runner_reported_HerdrChildGone_exit_stamps_ChildGone`, `Runner_reported_HerdrPaneClosed_exit_fails_the_session_not_a_clean_stop` (extended: `== PaneClosed`), new `Runner_reported_ProcessExited_exit_stamps_NonQualifying`, `Session_unknown_to_runner_is_failed_and_its_agent_reset` (extended: evidence null), new `A_reconciler_close_after_the_runtime_stamped_DetectTimeout_keeps_it` | per T-1/T-3.
- V-7: a new generation clears evidence | integration | `AgentControlServiceIntegrationTests` — new `Resume_start_clears_prior_herdr_evidence_on_the_reused_row` (seed Failed row with `DetectTimeout`; `StartAsync(Fresh:false)`; after `WaitForIdleAsync` the same row id has `StartedAt` advanced and evidence null) and `Fresh_start_creates_a_row_with_null_evidence`.

S2 — consume once and hold (new `tests/Antiphon.Tests/Application/HerdrSupervisionBackoffTests.cs`, `[Category("Integration")]`, ungrouped `[NotInParallel]` because it drives the global supervisor sweep; every assertion scoped to the test's agent id; harness `AgentSupervisionTests.BuildHarness` with `HerdrFailureLimit = 3`, `BackoffBaseSeconds = 5`, `FreshAfterResumeFailures = 2`, `HealthyUptimeResetMinutes = 10`; agent created with `SessionBackend.Herdr`, `AlwaysOn: true`)

- V-8: three mixed qualifying attempts hold the agent and stop every later launch | integration | `Three_mixed_qualifying_attempts_hold_the_agent_and_no_fourth_launch_happens` — adapters `[a1, a2, a3, sentinel]`; tick boots attempt 1 (row A Running) → `ObserveExitAsync(A, null, HerdrPaneClosed)`; tick (consume) → `STATE.HerdrConsecutiveFailures == 1`, `NextRestartAt == now+5s`, `RestartScheduled` says `attempt 2`; advance 10s, tick → attempt 2 resumes row A with `a2.ThrowOnStart` = detect_timeout; `WaitForIdleAsync`; tick → streak 2, `NextRestartAt == now+10s`; advance 20s, tick → attempt 3 is fresh (general `ConsecutiveFailures >= 2`) into row B; `ObserveExitAsync(B, null, HerdrChildGone)`; tick | after the last tick: `STATE.HerdrFailureHeldAt != null`, `HerdrConsecutiveFailures == 3`, `LastHerdrFailureKind == ChildGone`, `LastHerdrObservedSessionId == B.Id`, `LastHerdrObservedStartedAt == B.StartedAt` (re-read), `NextRestartAt == null`, `HerdrHealthySince == null`; incidents for the agent contain exactly one `HerdrSupervisionHeld` (Error) and no `RestartScheduled`/`BackoffEscalated` at or after `HeldAt`; then advance 1h and tick three times → "no launch"; general `ConsecutiveFailures` unchanged across those ticks.
- V-9: ladder below the threshold and fresh policy unchanged | integration | `Ordinary_scheduling_and_fresh_fallback_are_unchanged_below_the_threshold` — the V-8 timeline stopped after attempt 2 | `Backoff` values 5s then 10s; attempt 3's `StartedSessionId != A.Id` (fresh at two general failures); `HeldAt == null`; `AgentSupervisionTests.Crash_is_recorded_and_restart_scheduled_with_backoff_details` and `Backoff_ladder_reaches_30_day_cap_and_escalates_once_per_tier` stay green.
- V-10: limit of one | integration | `A_limit_of_one_holds_on_the_first_qualifying_attempt` (`HerdrFailureLimit = 1`) | hold after the first consumed PaneClosed; a `HerdrFailureLimit = 10` harness with three failures does not hold.
- V-11: one attempt observed three ways counts once, in either writer order | integration | `One_attempt_seen_by_the_launch_catch_runtime_and_reconciler_counts_once` `[Arguments("catch-first")]`, `[Arguments("exit-first")]` — attempt fails via detect_timeout catch; also `ObserveExitAsync(A, null, HerdrLaunchDetectTimeout)` and a reconciler pass whose `FakeRunnerClient` reports A `Exited`/`HerdrPaneClosed`, in the parameterised order; two ticks | `HerdrConsecutiveFailures == 1`; evidence stays `DetectTimeout`; one `Crash` incident for A.
- V-12: same id / new StartedAt counts again; a fresh id counts on the same agent; separate agents never share | integration | `Same_row_resumed_twice_and_a_fresh_row_count_three_and_a_sibling_agent_stays_at_zero` — second AlwaysOn Herdr agent in the same cwd with one PaneClosed | first agent holds at 3; sibling `HerdrConsecutiveFailures == 1`, no hold.
- V-13: dedupe survives database precision | integration | `Dedupe_pair_survives_the_timestamp_round_trip` — set A's `StartedAt = now.AddTicks(7)` before the exit; tick, tick | streak 1 after both ticks (T-10).
- V-14: non-qualifying terminal attempt resets; pre-session refusal neither counts nor resets | integration | `A_non_qualifying_terminal_attempt_resets_the_streak` (two qualifying, then `ThrowOnStart = new InvalidOperationException("boom")` → tick → streak 0, `HeldAt == null`, ordinary `StartFailure`/backoff as today) and `A_synchronous_refusal_before_any_row_leaves_the_streak_alone` (two qualifying, then `includeModelAvailability` with a `ModelAvailabilityHold` for the alias → due tick → `ModelDisabledException` path as today → streak still 2, no evidence row added, `StartFailure` incident with the hold key as today).
- V-15: a launch the queue still owns is not consumed early | integration | `A_launch_still_owned_by_the_queue_is_consumed_only_after_its_catch_classifies_it` — attempt 2 gated with `StartGate`; tick while gated | streak unchanged and no `RestartScheduled` for that row while `LaunchQueue.Owns(A.Id)`; release → catch stamps `DetectTimeout` → next tick streak 2.
- V-16: negative controls | integration | `PtyHost_pool_and_card_sessions_never_move_the_herdr_streak` — PtyHost AlwaysOn agent with three `HerdrPaneClosed` exits; a card-owned session (`CardId != null`) on a Herdr agent; a pool delegate agent | `HerdrConsecutiveFailures == 0`, `HeldAt == null` for each; `Intentional_stop_detach_presumed_dead_and_pane_left_open_do_not_count` — Stop via `StopAsync` then `ObserveExitAsync(HerdrPaneClosed)` (no consumption, `Suspended` true as today); `HerdrDetached`, `HerdrRestartPresumedDead`, `HerdrPaneLeftOpen` each reset a seeded streak of 2 to 0 without holding.
- V-17: persistence across restart | integration | `Streak_hold_and_dedupe_survive_a_server_restart` — two qualifying; dispose harness; new harness on the same DB; third qualifying → hold; dispose; new harness; advance 1h; tick ×3 | "no launch"; `HerdrHealthySince` seeded non-null survives a harness rebuild unchanged.
- V-18: due-start recheck and stale-state protection | integration | `Supervisor_rechecks_the_hold_before_a_due_start_and_writes_no_failure_incident` (seed `HeldAt != null` and `NextRestartAt` in the past → tick → "no launch", `NextRestartAt` repaired to null, no `StartFailure`/`BackoffEscalated`) and `A_stale_supervisor_entity_cannot_overwrite_an_explicit_retry` (scope 1 `Supervisor()` loads state via `GetOrCreateStateAsync`; scope 2 explicit retry clears the hold; scope 1 `TickAsync` → `STATE.HeldAt` still null, streak still 0).
- V-19: concurrent Tick and Start | integration | `Concurrent_tick_and_start_consume_one_outcome_and_neither_starts_through_the_committed_hold` — two qualifying consumed, third row stamped but unconsumed; open a side transaction holding the agent row lock (`SELECT ... FOR UPDATE`); start `Supervisor().TickAsync` and `Control.StartAsync(new StartAgentRequest())` on separate scopes; release the lock; await both | `HerdrConsecutiveFailures == 3` exactly; exactly one `HerdrSupervisionHeld` incident; the Start task faulted with `ConflictException.Code == HELD`; "no launch"; the supervisor's own `HELD` handling wrote no `StartFailure`.
- V-20: health reset | integration | `Ten_minutes_of_observed_Running_resets_the_herdr_streak` (two qualifying; attempt Running; tick sets `HerdrHealthySince`; advance 9m tick → streak 2; advance 2m tick → streak 0), `Sixty_seconds_of_Starting_contributes_no_healthy_time` (row Starting for 60s then Running; `HerdrHealthySince >= first Running observation`), `A_short_Running_interval_or_a_successful_enqueue_does_not_reset` (streak 2 after a 3m Running tick; after a bare `StartAsync` return), `A_new_generation_clears_HealthySince` (exit + resume → null) | `AgentSupervisionTests.Healthy_uptime_resets_the_ladder` stays green (general ladder untouched).
- V-21: held Start ordering | integration | `Held_start_without_the_flag_is_409_before_latch_clear_composition_and_the_model_gate` — seed hold, `LivenessLatchedAt = t`, `Suspended = true`, and a `ModelAvailabilityHold` for the alias (`includeModelAvailability`) | `ConflictException.Code == HELD` (not `ModelDisabledException`); message per T-11; `STATE.LivenessLatchedAt == t`, `Suspended` still true; `PersistentSessionId` unchanged; no session row added; sentinel not started; no incident added.
- V-22: explicit retry | integration | `Explicit_retry_clears_only_the_herdr_hold_and_launches_through_every_normal_guard` — seed hold with general `ConsecutiveFailures = 5`; `StartAsync(new StartAgentRequest(ResetHerdrFailureHold: true))` | adapter started; `STATE.HerdrFailureHeldAt == null`, `HerdrConsecutiveFailures == 0`, `HerdrHealthySince == null`, `LastHerdrObservedSessionId/StartedAt` retained, `ConsecutiveFailures == 5`, `Suspended == false`; exactly one `HerdrSupervisionRetried` (Info); then three more qualifying attempts hold again with a second `HerdrSupervisionHeld`. Companion cases: `Explicit_retry_with_fresh_true_starts_a_new_row`; `Explicit_retry_still_refuses_a_held_model_and_leaves_the_hold_acknowledged` (T-6 pin: `ModelDisabledException`, `HeldAt == null`, `HerdrSupervisionRetried` present); `The_flag_on_an_unheld_agent_is_an_ordinary_start` (no `HerdrSupervisionRetried`); `Explicit_retry_consumes_an_unobserved_outcome_first_so_it_cannot_count_again` (streak 2 consumed, third row stamped unconsumed → retry → pair == third row, streak 0, no `HerdrSupervisionHeld` written, next tick after the launch keeps streak 0).
- V-23: nothing but the flag clears a hold | integration | `Changing_limit_backend_labels_or_always_on_does_not_clear_the_hold` — PATCH `AlwaysOn=false`, PATCH backend PtyHost, rebuild harness with `HerdrFailureLimit = 10`, PATCH back to Herdr/AlwaysOn | `HeldAt` unchanged throughout; tick after re-enabling → "no launch"; `Stop_while_held_keeps_the_hold_and_records_SuspendedByUser` (Stop → `Suspended`, `SuspendedByUser`; `HeldAt` unchanged; a following Start without the flag is `HELD` and `Suspended` stays true); `Held_start_on_an_already_live_agent_is_idempotent_and_does_not_acknowledge` (seed hold + Running row → Start returns, no throw, `HeldAt` unchanged, session untouched).
- V-24: attach does not acknowledge | integration | `AgentAttachHerdrTests` — new `Attach_binds_a_live_pane_without_clearing_the_herdr_hold` (existing attach fixture; seed hold first) | attach succeeds as today; `LivenessLatchedAt`/`Suspended` cleared as today; `HeldAt` unchanged; a later exit + tick → "no launch".

S3 — bridge, Attention, DTO, client

- V-25: channel inbound cannot bypass a hold | integration | `ChannelBridgeTests` — new `Herdr_held_agent_inbound_is_dropped_with_one_incident_and_no_start_notice_or_reroute` (`BridgeQueueHarness` `AlwaysOn = true`, session row set Stopped, `STATE` seeded held) | `HandleInboundAsync` returns without throwing; no new `AgentSessions` row; `h.Adapter.SentInput` empty; `h.Messaging.SentReplies` empty (T-7); `PendingCountAsync == 0`; exactly one `ChannelReplyLost` with `FailureReason == "HerdrSupervisionHeld"` after two inbound messages; `HeldAt` unchanged; `Held_agent_without_fallback_sends_notice_and_does_not_throw` stays green.
- V-26: Attention is state-backed | integration | new `tests/Antiphon.Tests/Application/HerdrSupervisionAttentionTests.cs` — `A_held_agent_is_one_error_row_derived_from_state_without_any_incident`, `The_row_survives_the_lookback_incident_pruning_and_always_on_off` (advance 48h; delete the agent's incidents; `AlwaysOn=false`), `The_hold_incident_is_not_also_a_recent_critical_incident_row` (seed the `HerdrSupervisionHeld` incident → exactly one row for the agent about the hold; no `RecentCriticalIncident` row of that kind), `Clearing_the_hold_removes_the_row_and_leaves_no_stale_incident_row`, `Two_held_agents_are_two_rows`, `Detail_dto_carries_the_hold_even_after_always_on_is_disabled` (`AgentService.GetByIdAsync(...).Supervision` has `HerdrFailureHeldAt`, `HerdrConsecutiveFailures`, `LastHerdrFailureKind`) | per T-8; `AttentionServiceTests.summary_counts_match_a_full_sweep_for_the_same_rows` stays green with the new kind counted.
- V-27: client | client | `client/src/features/agents/AgentsPage.test.tsx` — `shows Herdr retries paused with the reason, independent of suspended and the countdown` (supervision `{ herdrFailureHeldAt, herdrConsecutiveFailures: 3, lastHerdrFailureKind: 'DetectTimeout', suspended: false, nextRestartAt: null }`), `relabels Start as Retry and resume and posts resetHerdrFailureHold true` (msw captures `POST /agents/:id/start` body `{"resetHerdrFailureHold":true}`), `an ordinary start posts no resetHerdrFailureHold key`; `client/src/features/attention/attentionVisuals.test.ts` — add `'HerdrSupervisionHeld'` to the kinds array so the existing exhaustive cases (`maps every kind…`, `lands every kind in a declared group`, `maps every visual kind onto exactly one home bucket`) cover it, plus `expect(homeBucketOf(item({ kind: 'HerdrSupervisionHeld', severity: 'Error' }))).toBe('broken')`; `client/src/api/agents.ts` `AgentIncidentKind` union gains `'HerdrSupervisionHeld' | 'HerdrSupervisionRetried'`.

S4 — assembled named placement (E2E; **hard precondition: CARD-0384 landed on `origin/master`**, see Cost)

- V-28: named AlwaysOn seat holds after three failures, keeps its timeout shell, launches nothing more, and resumes only on explicit retry | E2E | `HerdrAlwaysOnChannelParityTests.Named_AlwaysOn_herdr_agent_holds_after_three_failures_keeps_its_timeout_shell_and_resumes_only_on_explicit_retry` — harness `launchDetectTimeoutMs: 500`, `HerdrFailureLimit = 3`; agent `herdrWorkspaceLabel = "PredictionMarkets"`, `herdrTabLabel = "Orch"`, Grok kind; a second unpinned AlwaysOn specialist agent B started first and left Running in a tab seeded `MavRef-DL` (0384 `SeedTab`); attempt 1: named start → Running on `Orch` → `fake.RemovePane(orchPane)` → runtime observes `HerdrPaneClosed`; attempt 2 (supervised, tab recreated): same → PaneClosed; attempt 3 (supervised): `fake.LaunchScriptAgentKind = null` → detect timeout → kept idle shell, `last-pane/<id>.json` with `"tabLabel":"Orch"`, server row `Failed` with `DetectTimeout` (through the in-process 409 mapping) | after the consuming tick: `STATE` held; snapshot `fake.Requests.Count` and the last-pane bytes; advance 1h and tick three times; also `fake.AddReplayPaneClosed(B's pane, ws)` during those ticks | request-log delta contains none of `tab.create`, `pane.split`, `pane.send_text`, `pane.send_keys`, `pane.close`, `tab.close`, `workspace.create`, `agent.start`; the kept `Orch` pane is still listed with exactly one pane in exactly one tab labelled `Orch`; last-pane bytes identical; B still Running, `MavRef-DL` pane count unchanged, B's row has null evidence and B's `STATE` streak 0 (replayed event ignored while live); then `LaunchScriptAgentKind` restored and `StartAsync(ResetHerdrFailureHold: true)` → Running in the **same** kept pane id, `tab.create` delta 0, sidecar names `Orch`, `HeldAt == null`.
- V-29: the CARD-0382 and CARD-0388 failure signatures are distinguishable going forward (open gap 1) | E2E | `HerdrAlwaysOnChannelParityTests.Unsafe_grok_rules_on_the_named_seat_refuse_before_any_pane_and_leave_the_streak_untouched` — same named Grok seat with `orchestrator` attached, non-Normal reply style and a multiline `SystemPromptAppend` (the 0382 unsafe composition) | `ConflictException.Code == "grok_rules_argv_unsafe"`; `fake.Requests` delta empty; no session row; `STATE.HerdrConsecutiveFailures == 0`, no evidence, no hold. Paired in the same class with V-28's attempt 3, this pins the discriminator: an argv-corruption launch on current code is refused **before** any Herdr RPC and leaves a null/absent evidence trail, whereas the CARD-0388 shape produces a pane, a typed `DetectTimeout`/`PaneClosed`/`ChildGone` row and a streak increment. The two observables (any pane RPC; a non-null `KIND`) separate them for every new occurrence. What no test can do is re-attribute the 2026-09-05 incident: its rows carry null evidence and no argv or process output was captured (see Out of scope).
- V-30: placement refusal is not a detect failure | E2E | `HerdrAlwaysOnChannelParityTests.Named_placement_refusal_is_non_qualifying_not_a_detect_failure` — seed a foreign occupant in `Orch`; synchronous preflight `pane_occupied` (0384 V-31) → no row, streak unchanged; then allow the preflight and make the runner 409 at launch (0384 V-32 shape) → asynchronous Failed row with `NonQualifying` → a seeded streak of 2 resets to 0.

### Guards the regression

- R-1: the hold gate is dropped from `SuperviseAsync` or from `StartAsync` | caught by V-8 and V-21 because the sentinel adapter starts (`FailureReason` carries the sentinel text) and no `HELD` is thrown.
- R-2: an automatic Start (supervisor, bridge, policy refresh) clears the hold through `ClearSupervisionLatchAsync` or passes the flag | caught by V-21 (`LivenessLatchedAt` untouched, `Suspended` still true), V-25 (no session row after inbound) and V-23 (tick after re-enabling AlwaysOn launches nothing).
- R-3: dedupe by row id only, or by in-memory `StartedAt` | caught by V-11, V-12 and V-13 because the count would be 3 (or 2) instead of 1.
- R-4: a later cleanup writer erases `DetectTimeout` (runtime exit overwrites with PaneClosed, or the launch catch discards the code into prose before classifying) | caught by V-4 `…keeps_DetectTimeout`, V-5 `…stamps_DetectTimeout…`, V-6 `…keeps_it`, and V-28 attempt 3 (would classify NonQualifying and reset instead of holding).
- R-5: evidence not cleared on resume, or a successful enqueue resets the streak | caught by V-7 (stale evidence would be re-consumed on the next tick) and V-20 `…successful_enqueue_does_not_reset`.
- R-6: Attention derived from recent incidents instead of `STATE` | caught by V-26 `…survives_the_lookback…` (row disappears after 48h or pruning) and `…not_also_a_recent_critical_incident_row` (two rows for one episode).
- R-7: the supervisor's `HELD` refusal handled by the generic catch (writes `StartFailure`, schedules `NextRestartAt`) | caught by V-18 and V-19.
- R-8: the streak counted from `Crash`/`RestartScheduled` rows or `FailureReason.Contains` | caught by V-11 (three writers, one count), V-16 (`HerdrPaneLeftOpen` and PtyHost rows must not count) and V-1 (no string mapping exists to satisfy).
- R-9: a queued launch consumed while the queue still owns it | caught by V-15.
- R-10: the streak reset by a synchronous refusal, or not reset by a non-qualifying terminal row | caught by V-14 and V-30.
- R-11: the hold kills or closes panes | caught by V-28's request-log delta and kept-pane assertions.
- R-12: CARD-0382 fail-closed refusal weakened or moved after pane allocation | caught by V-29 plus the existing `GrokRulesLaunchRefusalTests` (all cases), `AgentSessionLaunchFailureTests` Grok rules cases, and runner `GrokRulesRunnerRefusalTests`/`GrokRulesArgvPolicyTests` in the forced run list.
- R-13: CARD-0383 kept-pane behaviour changed (a `HerdrPaneLeftOpen` incident or a pane close on detect timeout) | caught by `AgentSessionRuntimeTests.HerdrLaunchDetectTimeout_exit_is_Failed_and_does_not_record_HerdrPaneLeftOpen`, runner `HerdrLaunchShapeTests.Timeout_on_an_idle_shell_keeps_the_pane_and_writes_a_last_pane_record` and `Relaunch_after_a_kept_pane_targets_it_and_clears_its_env_names`, and V-28's kept-pane relaunch.
- R-14: Stop/LivenessLatchedAt semantics relabelled by the hold | caught by `AgentSupervisionTests.Stop_suspends_supervision_until_manual_start`, `StartAsync_clears_the_liveness_latch_on_an_already_running_agent`, and V-23.
- R-15: a same-cwd uniqueness guard or automatic channel election creeps in (D-5 says none) | caught by V-12's sibling agent (second AlwaysOn agent in the same cwd is created and supervised independently) and `ChannelBridgeTests.Bound_channel_routes_the_message_into_the_agent_session` (binding untouched). No mechanism is tested because none is built.
- R-16: CARD-0384 preflight ordered before the hold gate | caught by V-21 extended after the rebase with the 0384 `PlacementCheck` hook: recorded check calls == 0 when held.

### Positive controls

Build runs each: break, see the named test red, revert, see green, and reports all three per control.

- PC-1: break R-1 by commenting out the `HerdrFailureHeldAt` check in `AgentControlService.StartAsync`; expect V-21 `Held_start_without_the_flag_is_409_before_latch_clear_composition_and_the_model_gate` and V-8 red (sentinel starts).
- PC-2: break R-1/R-7 by commenting out the supervisor's pre-start hold recheck; expect V-18 `Supervisor_rechecks_the_hold_before_a_due_start_and_writes_no_failure_incident` red.
- PC-3: break R-2 by adding `HerdrFailureHeldAt = null` inside `ClearSupervisionLatchAsync`; expect V-21 and V-25 red.
- PC-4: break R-4 by removing the `ConflictException.Code` capture in the `LaunchInteractiveAsync` catch (classify from `ex.Message` only); expect V-5 `A_runner_detect_timeout_409_stamps_DetectTimeout_and_keeps_the_runner_prose` and V-28 red.
- PC-5: break R-4 by letting `ObserveExitAsync` overwrite existing evidence unconditionally; expect V-4 `A_pane_closed_exit_after_DetectTimeout_evidence_keeps_DetectTimeout` and V-11 `exit-first` red.
- PC-6: break R-3 by comparing `LastHerdrObservedSessionId` only (drop `StartedAt`); expect V-12 red; then break it the other way by comparing the in-memory `StartedAt` before save; expect V-13 red.
- PC-7: break R-5 by not nulling `HerdrSupervisionFailureKind` in the resume branch of `StartAsync`; expect V-7 and V-12 red.
- PC-8: break R-6 by projecting the Attention row from the `HerdrSupervisionHeld` incident within the lookback instead of `STATE`; expect V-26 `The_row_survives_the_lookback_incident_pruning_and_always_on_off` red.
- PC-9: break R-9 by removing the `LaunchQueue.Owns` check before consumption; expect V-15 red.
- PC-10: break R-10 by treating `NonQualifying` as "no evidence" (skip instead of reset); expect V-14 `A_non_qualifying_terminal_attempt_resets_the_streak` red; then treat a synchronous `ModelDisabledException` as a terminal reset; expect `A_synchronous_refusal_before_any_row_leaves_the_streak_alone` red.
- PC-11: break T-7 by removing the bridge's `HELD` catch; expect V-25 red (unhandled `ConflictException` escapes `HandleInboundAsync`).
- PC-12: break V-20 by starting `HerdrHealthySince` at `StartedAt` instead of the first Running observation; expect `Sixty_seconds_of_Starting_contributes_no_healthy_time` red.
- PC-13: break R-11 by adding a `pane.close` for the last-pane in the hold branch; expect V-28 red (request-log delta and kept pane).
- PC-14: break V-2 by widening the validator to 0..10; expect `HerdrFailureLimit_outside_1_to_10_fails_validation` red.

### Out of scope

- **Historical attribution of the 2026-09-05 incident** (open gap 1): not testable. The rows from that night carry null `KIND` (D-2 backfills nothing), no clap output or argv was captured, and the first `HerdrChildGone` at 00:18:46 is unexplained by argv corruption. V-29 proves the two signatures are separable for any **new** occurrence; the operator's forward check after deploy is: if the repaired seat's Start refuses with `grok_rules_argv_unsafe`, the seat is 0382-class and CARD-0395 owns it; if it allocates a pane and then times out or closes, that is a different cause that needs its own process evidence and a loaded-version check. CARD-0388's close reason must keep the historical cause marked unconfirmed either way.
- **Twin-orchestrator ownership** (D-5): an operator runbook, verified by reading, not by code; no uniqueness, election or auto-stop mechanism exists to test. R-15 only pins that none was added.
- No live Herdr, Grok or Slack canary; no real PM agent IDs or channel bindings; no production migration or configuration.
- The Domain enum's layering (no `using` of Application or runner contracts) is reviewed by eye; the repo has no architecture test to extend.
- No PowerShell wrapper for the explicit retry: `scripts/` has no agent-start script today, so the operator surface is the documented `POST /api/agents/{id}/start {"resetHerdrFailureHold":true}` (S5 docs), unlike `model-availability.ps1 clear` / `routing-pin.ps1 clear` which wrap DELETE routes. A wrapper is a separate small card if wanted.
- The ordinary backoff ladder, escalation tiers, `FreshAfterResumeFailures` arithmetic and `Healthy_uptime_resets_the_ladder` are unchanged and only re-run, not re-designed.
- `HerdrChildGone` production on the runner is not re-proven here; `HerdrAdoptionSweepTests.R7_herdr_unreachable_and_os_dead_is_ChildGone_without_a_socket` already does, and the server side consumes the typed reason (V-4, V-6).
- Documentation agreement (`docs/herdr-sessions.md`, `docs/ops-http.md`, `docs/antiphon-api.md`) is reviewed by eye in S5; no doc-pinning test.

### Cost

- **Hard precondition and sequencing (open gap 2):** S1–S3 build on current `origin/master` (`567f9e95`). Before S4, Code must verify CARD-0384 has landed by (a) `git fetch origin master` and `git merge-base --is-ancestor <0384 land SHA> origin/master`, (b) `HerdrPlacementPreflightTests` and `FakeHerdrServer.SeedTab` present on that master, (c) `DirectSessionRunnerClient` advertising `HerdrNamedTabPlacement` in the parity harness; then rebase this branch onto it, re-run the S2 class (the hold gate must still precede the new preflight: R-16), and only then write V-28..V-30. If CARD-0384 is not landed when S3 is done, Code stops after S3 with S4 explicitly reported blocked and the 0384 land SHA named as the unblock; it must not build against `feat/card-task-6a5d6f1f` or implement placement itself. Report the 0384 SHA in the completion header.
- suites forced (class filters only, `--property:OutputPath=bin-c388/`, never `dotnet test`, never the full assembly): `Antiphon.Tests` — `HerdrSupervisionFailureEvidenceTests`, `AgentSessionRuntimeTests`, `SessionReconciliationServiceTests`, `AgentSessionLaunchFailureTests`, `AgentControlServiceIntegrationTests`, `HerdrSupervisionBackoffTests`, `AgentSupervisionTests`, `AgentAttachHerdrTests`, `ChannelBridgeTests`, `HerdrSupervisionAttentionTests`, `AttentionServiceTests`, `GrokRulesLaunchRefusalTests`, `InstructionFileStampTests`, and after the 0384 rebase `HerdrAlwaysOnChannelParityTests`; `Antiphon.SessionRunner.Tests` — `HerdrLaunchShapeTests`, `HerdrAdoptionSweepTests`, `GrokRulesRunnerRefusalTests`, `GrokRulesArgvPolicyTests` (regression only; no runner code changes are planned); client — `pwsh -File scripts/test-client.ps1 AgentsPage.test` and `attentionVisuals.test`. Server and runner projects sequentially; never alongside `Antiphon.Agents.Pty.Tests`.
- verification floor ~ 30 min: server build ~3 min + Postgres container ~1 min + fourteen class runs ~18 min (`HerdrSupervisionBackoffTests`, `AgentSupervisionTests`, `SessionReconciliationServiceTests` and `HerdrAlwaysOnChannelParityTests` are global `[NotInParallel]` with real queues and advancing clocks, ~2–4 min each) + runner build ~2 min + four class runs ~3 min + client ~2 min. Positive controls add 14 × (edit + one class run ≈ 1–3 min) ≈ 25–35 min. Delete every `bin-c388` directory (about a dozen) before reporting.
