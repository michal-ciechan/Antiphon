# CARD-0382: refuse unsafe Grok rules on Windows argv

Status: Plan and TestDesign complete; next stage Code. Scope: the explicitly permitted fail-closed alternative. This prevents the bad launch; it does **not** make a multiline orchestrator bundle launch successfully. Do not report the card's successful-delivery/detection acceptance criteria as met by this mitigation.

Repository evidence: `dde6e057f7a987cfaf5054ba851e15283b8e7835`. Read the live descriptions of CARD-0382 (GitHub #41) and CARD-0395 on 2026-09-06. No production process, profile, wrapper, or board state was changed during planning.

## Decisions

### D-1. Use the authorized fail-closed alternative

On Windows, refuse a Grok standing-instructions payload containing CR, LF, or NUL, or more than **4,096 UTF-16 code units**, before it is appended to launch arguments. Check the complete resolved argv again before launching. Keep `InstructionBundleComposer.EnsureWithinCommandLineBudget` as an additional total-size guard; its existing 30,000-character default is not newline validation.

The 4,096 limit is a conservative application policy, not a claimed Windows per-token limit or a proof that arbitrary operator wrappers preserve every character. Spaces alone are allowed, and allowed text is passed unchanged. Never normalize newlines into spaces, truncate, encode text into an opaque argv blob, omit the rules and continue, or select another provider/backend automatically. The guard applies to Windows Grok on both Herdr and PtyHost, independent of whether the executable is Grok or a configured wrapper. Other providers and non-Windows launches keep their present behavior.

Reason: no verified native file/env channel equivalent to the existing per-session append has been established for the installed binary. A short instruction telling the model to read a file would change this from system-prompt delivery into model-dependent tool use. That is not proof of full delivery.

### D-2. What the CLI evidence actually establishes

The installed executable resolves to `C:\Users\lndco\.grok\bin\grok.exe`. `grok.exe --version` returned `grok 1.0.13 (5e9a58528b76) [stable]`; `grok.exe --help` exposes `--rules <RULES>` as extra system-prompt rules, with no rules-file flag or rules environment variable advertised. `--prompt-file` is a single-turn user-prompt input, not this channel.

The actual upstream TUI implementation was traced, rather than assuming response-file support:

| Upstream source, pinned at public revision `72a61251fcffb464bcc687aeb5a998e5a98ec0c9` | Evidence |
|---|---|
| [TUI parser, `app/cli.rs:526`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-pager/src/app/cli.rs#L526) | `rules` is an optional string; the flag also has an append-system-prompt alias. No file parser or env binding is attached. |
| [TUI startup, `app/mod.rs:833`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-pager/src/app/mod.rs#L833) and [ACP metadata, `acp/mod.rs:483`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-pager/src/acp/mod.rs#L483) | The string is cloned into connection options and then into metadata. |
| [Runtime prompt construction, `mvp_agent/mod.rs:1104`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-shell/src/agent/mvp_agent/mod.rs#L1104) | The metadata string is appended inside the human-rules block. No `@path` read occurs. |
| [Config overlay, `env_overlay.rs:9`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-config/src/env_overlay.rs#L9) and [rule-directory limitation, `inspect/mod.rs:551`](https://github.com/xai-org/grok-build/blob/72a61251fcffb464bcc687aeb5a998e5a98ec0c9/crates/codegen/xai-grok-shell/src/inspect/mod.rs#L551) | `GROK_CONFIG_PATH` loads JSON/TOML configuration, not markdown. `extra_rule_dirs` only reclassifies already discovered files; arbitrary-directory discovery is explicitly not wired. It cannot justify the proposed rules-file transport. |

Version boundary: public `SOURCE_REV` is `a549186d9d39311f2d3ee4208db62af8c65aa476`, not the installed binary's monorepo revision. None of the 41 public SOURCE_REV revisions inspected matched `5e9a58528b76`. Thus this is a source trace plus installed CLI help evidence, **not** a binary-matched or wire-level canary proving every possible transport absent in 1.0.13. This uncertainty is a reason to refuse unsupported transport, not to ship a guessed `--rules @path` syntax. No model request or headed Grok probe was run for this Plan.

### D-3. Refusal must be specific and occur before side effects

Use stable problem code `grok_rules_argv_unsafe`. The diagnostic identifies the agent/task/session, the offending flag, the reason (`line_break`, `nul`, `token_too_long`, or `missing_value`), and the character count/limit when relevant. It must not include the rules text, environment values, a command line, or a suggested unsafe override.

Named/card composition should throw a `ConflictException` with that code before launch queue insertion. Dispatcher handling must settle a composition refusal through the existing failed-dispatch path, with no brief queued and no timeout-based retry. Audit the post-commit window explicitly: `AgentTaskDispatcher` currently persists the claimed session/task before it builds the launch spec at line 2959. Its outer catch at 590 already calls `FailAndNotifyAsync` (2038), but labels the error as occurring before a session existed. Add a narrow rules-refusal branch that terminalizes the newly claimed, unlaunched session with the correct reason before normal task failure/notification; preserve the generic handling for other failures. Do not assume task settlement automatically repairs the session row. A rejected launch must leave no `Starting` session or `Dispatched` task waiting for the watchdog; record system-request termination through the existing lifecycle helpers where a session row exists. Notification of the parent remains valid; the forbidden queue action is the child's task brief.

Runner refusals use the same problem code and HTTP 409 before creating/replacing a `RunnerSession`, starting a PtyHost, connecting to Herdr, allocating/reusing a pane, writing a launch script, or typing. Implement a dedicated runner exception and explicit mapping in `POST /sessions`; an uncaught `ArgumentException` is not a suitable response contract.

### D-4. One validation policy, early checks plus a launch backstop

Add a small pure `GrokRulesArgvPolicy` in `src/Antiphon.SessionRunner.Contracts/`, which both server and runner already reference. It owns the 4,096 constant, reason codes, payload validation, and recognition of `--rules value`, `--rules=value`, `--append-system-prompt value`, and its equals form. Check every occurrence, including duplicates; a later good value must not hide an earlier bad one. Respect the child CLI's option terminator; never assume the resolved executable's basename determines its provider. The caller supplies the known Grok kind and Windows decision; tests can exercise both platform decisions without global environment mutation.

Call payload validation at the two composition sites before their `AddRange`, after channel rendering on the named-agent path. Call argv validation at `AgentSessionService.BuildRuntimeLaunchSpecAsync` after session identity/other overlays and before adapter start. This catches unsafe profile/definition args that did not originate in bundle composition. Keep model selection, identity selection, composition order, stamps, and the full command-line budget intact.

The runner checks again using the runner OS and the request's Grok transcript/kind identity. For Herdr it must inspect the **effective** arguments, including the whole-argument `$env:NAME` / `${env:NAME}` substitution currently performed by `HerdrLaunchScript.TryResolveEnvToken`. Reuse that resolution logic for validation; checking only the short token would let a multiline env value bypass the guard. Resolve each complete argv element before flag scanning, so equals forms and an env-supplied flag are also covered. The guard's early check and the script must use the same substitution semantics. This does not authorize adding a new general env expansion facility.

The process-boundary contract is the invariant, not an assertion that `CreateProcessW` itself categorically cannot represent LF. The card establishes a failing launch chain. The present PowerShell source-string test establishes escaping in a script, not the native child argv produced by all subsequent parsers.

### D-5. CARD-0395 owns the durable-file implementation

This mitigation writes **no rules file**, so it introduces no second convention. Reserve one shared future convention: `<SessionLogPath>\instructions\grok\<Antiphon-session-id:N>\rules.md`, UTF-8 without BOM, with the exact final rendered composition (no extra front matter or truncation). A future file transport must materialize it on the runner host, atomically replace it on an actual launch/resume, retain it for the session/resume lifetime, and expose the same absolute path to CARD-0395's reminder mechanism. It must not use the short-lived Herdr `.launch.ps1`, a shared cwd `AGENTS.md`, or one global `.grok/rules/` entry that other agents would load.

That is a design reservation for CARD-0395, not a format already implemented or supported by Grok. Confirm a native append mechanism with the installed binary and a captured request before enabling it. If a future solution uses model-driven re-reading, measure and document the changed delivery semantics. The upstream source linked above also says rules are creation-only and not refreshed on resume; CARD-0395 must measure the installed version instead of inheriting the current blanket compaction/resume assurances. Compaction events, hooks, typed reminders, and session-resume identity changes are outside this mitigation.

### D-6. Operational impact and acceptance boundary

Every composed attachment normally contains newlines, so this change refuses most **new Windows Grok delegates**, as well as the standing orchestrator in the reproduction. Already-running sessions and warm reuse with no process launch are not altered. This is an availability tradeoff explicitly allowed by the brief's fail-closed alternative; it is not a transparent repair. Do not special-case `fakegrok` or silently strip bundles to make tests or deployment appear healthy.

| Card acceptance | Result of the selected alternative |
|---|---|
| Full composed orchestrator rules reach Grok without clap failure | Still outstanding: this launch is refused before Grok starts. |
| Herdr detects Grok well before its timeout; pane stays open | Still outstanding for the refused bundle. No pane is touched and no detect timer is started. |
| Multiline composed rules never become a Windows native argv argument | Required red-then-green automated proof below. |

The next stage can design tests for this authorized mitigation without a new user answer. The caller must keep the successful-launch work visible on CARD-0382/CARD-0395 and must not close those acceptance items on refusal-only evidence. If restoring bundled Windows Grok availability is required in this same change, return to investigation of a measured file/env transport; do not widen Code into an unplanned provider integration.

## Ground truth in Antiphon

Line anchors below refer to the repository evidence commit; linked files are the implementation owners.

| Assumption or question | Actual composition/launch code |
|---|---|
| `AgentControlService` directly appends standing rules | It now calls `ComposeForAgentAsync` at [AgentControlService.cs:262](../../../server/Application/Services/AgentControlService.cs), then passes `composition.ExtraArgs` into `AgentLaunchOptions` at line 272. |
| Where the actual standing text is formed | [AgentSessionLaunchComposer.cs:95](../../../server/Application/Services/AgentSessionLaunchComposer.cs): attachments, reply style, prompt append; channel rendering at 104; budget at 109; Grok `--rules` plus **rendered text** at 114-116. Card and orchestration launch callers also use this composer (`CardService:1239`, `OrchestratorService:939`). |
| Where delegates differ | [AgentTaskDispatcher.cs:3496](../../../server/Application/Services/AgentTaskDispatcher.cs), `ComposeDelegateArgs`: role/attached bundles at 3564, budget at 3569, raw Grok payload at 3579. Both registry and profile branches call it (`BuildLaunchSpec:3385`, `BuildLaunchSpecAsync:3428`). |
| Does the existing guard detect line breaks? | [InstructionBundleComposer.cs:151](../../../server/Application/Services/InstructionBundleComposer.cs) counts UTF-16 characters plus argument overhead. It performs no CR/LF validation. Composition preserves custom text verbatim; separators are `\n\n`. Default budget is 30,000 in `DelegationSettings:286`. |
| Could a resolver fix the payload automatically? | [AgentRegistry.cs:113](../../../server/Application/Services/AgentRegistry.cs) and [AgentTuiLaunchResolver.cs:394](../../../server/Application/Services/AgentTuiLaunchResolver.cs) append `ExtraArgs` literally. The managed profile supplies its own executable/base arguments. [AgentLaunchSpec.cs](../../../server/Application/Dtos/AgentLaunchSpec.cs) carries argv as a list; it has no rules-file payload. |
| One server process-start funnel | [AgentSessionService.cs:1450](../../../server/Application/Services/AgentSessionService.cs): `BuildRuntimeLaunchSpecAsync` adds identity, Claude overlay, cwd/session/backend/Herdr data, and the API-key tripwire. Its three callers lead to adapter starts for card, fresh, and resume. |
| Last transport boundary | [SessionRunnerRuntime.cs:94](../../../src/Antiphon.SessionRunner/SessionRunnerRuntime.cs) validates requests, then constructs/registers the session and starts the selected child lane. Herdr writes the fully quoted args at [HerdrLaunchScript.cs:65](../../../src/Antiphon.SessionRunner/HerdrLaunchScript.cs); [HerdrPaneChild.cs:578](../../../src/Antiphon.SessionRunner/HerdrPaneChild.cs) writes the script, types it, polls detection, then deletes the script on success. |
| Existing test proves safe native transport | [HerdrLaunchShapeTests.cs:21](../../../tests/Antiphon.SessionRunner.Tests/HerdrLaunchShapeTests.cs) asserts script text/BOM containing the multiline rules. It never inspects Grok argv. Keep its generic PowerShell escaping coverage, but rename/reword it and use a non-rules generic fixture so it does not advertise the forbidden Grok launch as supported. |
| Timeout closes the pane in the current checkout | The card records the older observed failure. CARD-0383 has since added idle-shell retention in `HerdrPaneChild.TryKeepIdleShellPaneOnDetectTimeoutAsync:989`. Preserve that and native-session resume guards. This fix acts before either behavior is needed. |

## Code slices

| Slice | Files and concrete change | Tests |
|---|---|---|
| S1: shared policy | New `src/Antiphon.SessionRunner.Contracts/GrokRulesArgvPolicy.cs`; add a server wrapper in `server/Application/Services/GrokLaunchArgs.cs` that maps violations to `ConflictException`. No configuration escape hatch. | New `GrokRulesArgvPolicyTests`: payload boundaries, flag forms, duplicates, platform/kind gating, contents absent from errors. |
| S2: composition and settlement | `AgentSessionLaunchComposer.cs`, `AgentTaskDispatcher.cs`, `AgentSessionService.cs`: early payload checks and final argv guard. Handle the dispatcher post-commit refusal immediately through existing failure/settlement services. Verify named, card-backed, profile, registry, cold-dispatch and relaunch paths. | New `GrokRulesLaunchRefusalTests`; extend `AgentSessionRuntimeTests` and `GrokDelegateDispatchTests` for no adapter call, no queued brief, terminal rows/reason. |
| S3: runner backstop | `SessionRunnerRuntime.cs` before session registration; new runner `GrokRulesLaunchException.cs`; explicit 409 mapping in `Program.cs`. Reuse/extract the existing `HerdrLaunchScript` whole-env-token argument resolution so effective arguments are validated before Herdr work. | New `GrokRulesRunnerRefusalTests` with `FakeHerdrServer`, isolated runner HTTP mapping, zero child/pane mutations and no script creation; adjust `HerdrLaunchShapeTests` semantics. |
| S4: align documentation and existing expectations | `docs/agent-kinds.md` section 2 and Grok section, `docs/herdr-sessions.md`, `docs/ai-agent-tui-configuration.md`; qualify claims about successful Grok rules launches/compaction. No generated `docs/cards/` edits. Audit existing Grok-positive launch tests named below. | Preserve meaningful non-Grok/provider/transport coverage; give bundled Windows Grok cases explicit refusal expectations without disabling the guard. |

Existing tests needing deliberate review: `DelegateBundleLaunchTests`, `DelegateLaunchArgvIntegrityTests`, `GrokDelegateDispatchTests`, `GrokDelegateEndToEndTests`, `CodexDelegateDispatchTests` (its Grok sibling assertions), `CardSpawnModelArgumentTests`, and any real-Grok canary that currently relies on delegate bundle launch. Do not mechanically replace their entire behavior with a refusal assertion: isolate pure composition/model assertions where appropriate and keep runnable supported-lane coverage. TestDesign should list the exact affected cases before Code starts.

## Verification design

TestDesign remains a separate stage. These are the required behavioral oracles it must turn into executable cases and a class-filtered run list.

| ID | Required evidence |
|---|---|
| V-1 | Compose the actual embedded `orchestrator` attachment plus reply style and a custom append containing spaces, LF/CRLF, quotes, backticks and a unique sentinel. Exercise named Grok Herdr and a cold Grok delegate through production composition. Assert the specific refusal and **zero adapter/runner launch calls**, not merely a string missing from a helper's output. Removing S2 makes this test red. |
| V-2 | Supply raw unsafe `--rules` through a profile and registry definition, bypassing composition. Exercise fresh and resume through `AgentSessionService`; assert no adapter start. Removing the runtime guard makes it red. |
| V-3 | Send unsafe Grok requests directly to an isolated Windows runner on both backend names. Include an ordinary payload, equals form, alias, and `$env:RULES` resolving to the sentinel payload. Assert 409 `grok_rules_argv_unsafe`, no registered session/host/script, and no Herdr allocation, send-text, close, or detection polling. The test must run without a reachable real Herdr instance. Removing S3 makes it red. |
| V-4 | Test CR-only, LF-only, CRLF, NUL, 4,096 and 4,097 UTF-16 units (include supplementary characters), missing value and duplicate flags. A short single-line rules string with spaces remains byte-identical; the existing total budget still rejects an over-budget launch independently. |
| V-5 | A cold-dispatch refusal after the claim transaction leaves a failed task/session with the specific reason; no queued brief, watchdog wait, or automatic provider/backend retry. Existing running/warm sessions receive no kill or rewrite. |
| R-1 | Claude/Codex and non-Grok Raw/profile arguments retain existing behavior, including multiline payloads. Non-Windows Grok policy branch remains unchanged. The model and native-session identity tests retain meaningful independent assertions. |
| R-2 | Existing Herdr env application/redaction, native-session resume guard, idle-pane timeout retention, and generic script escaping tests still pass. |

The central acceptance test is **no process launch at all for the unsafe composition**. It therefore proves that the composed payload cannot reach a native `CreateProcess` argument under the chosen refusal behavior. A fake Herdr server reporting `agent=grok`, a script string preserving LF, or a healthy HTTP runner is not a successful full-rules-delivery oracle.

No live canary is necessary to prove refusal. A later successful file transport must instead capture the complete composed sentinel at the real Grok request boundary and observe real Herdr detection before the timeout. Use `RealCliStubEnv.ForGrok` and the established dual-hit stub oracle, never hand-built provider redirects. Do not reinterpret a refused/skipped launch as that canary passing.

Run TUnit with `dotnet run --project tests/<Project> --property:OutputPath=bin-card0382/ -- --treenode-filter "/*/*/<Class>/*"`, using the exact new/touched classes listed by TestDesign. Run server and runner projects sequentially; also never overlap `Antiphon.Tests` with `Antiphon.Agents.Pty.Tests`. Process-spawning classes require their assembly's `ParallelLimiter<ProcessSpawnLimit>`; real-Program fixtures must use the established production-runner guard or an isolated random runner. No namespace-wide/full-suite default, production restart, or deployment is part of Plan/TestDesign.

### TestDesign stage (2026-09-06, on plan commit `854f0c84`)

The oracle table above is the contract; the sections below are the executable cases. Scope restated so no later reader mistakes green for delivery: this design verifies a **fail-closed guard**. Card acceptance items 1 and 2 ("grok receives the full composed rules", "herdr detects grok well under the timeout") remain **UNMET** and are not evidenced by anything below; full-rules delivery is a separate, still-open follow-up (CARD-0382 stays open on those items; CARD-0395 owns the file transport). Nothing in this section may be cited as "grok received the rules".

Conventions used below:

- `SENTINEL` = `card0382-sentinel-<Guid:N>` embedded inside every unsafe text; every refusal diagnostic is asserted **not** to contain it.
- `CODE` = `grok_rules_argv_unsafe` (plan D-3).
- "unsafe orchestrator composition" = `InstructionBundleComposer.Compose(["orchestrator"], AgentReplyStyles.ComposedKey(<any non-Normal AgentReplyStyle>), append)` where `append` = `"Custom line one with spaces\r\nline \"two\" with `backticks`\nline three " + SENTINEL`. The embedded `orchestrator` bundle is 6,738 chars, so this composition violates both `line_break` and `token_too_long`.
- Layers: *unit* = pure, no DB; *integration* = `Antiphon.Tests` with the shared Postgres fixture and `FakeAgentProtocolAdapter`; *runner* = `Antiphon.SessionRunner.Tests` with `FakeHerdrServer`; *E2E* = in-process `DirectSessionRunnerClient` + real `fakegrok.exe` on ConPTY.

Defaults decided by this design (Build implements as written; a human may override before Code starts):

- **D-T1 Check order at the two composition sites**: existing `EnsureWithinCommandLineBudget` first, then the rules policy. Consequence: `GrokDelegateDispatchTests::the_command_line_budget_guards_a_grok_launch_the_same_way` stays byte-identical (over-budget still surfaces as `InvalidOperationException`).
- **D-T2 Reason precedence** when several apply: `nul` → `line_break` → `token_too_long` → `missing_value`. The unsafe orchestrator composition therefore reports `line_break`.
- **D-T3 Value semantics**: the argv element after a recognized flag is its value whatever it looks like; a trailing flag with no element → `missing_value`; `--rules=` (empty) and a spaces-only value pass; elements after a bare `--` are not scanned (plan D-4 option terminator).
- **D-T4 Env tokens**: the PtyHost lane performs no `$env:NAME` substitution (CARD-0341/CARD-0345 semantics), so a whole-token value on PtyHost is a single-line literal and passes; only the Herdr lane resolves tokens, on the server with `DollarEnvArg` and on the runner with `HerdrLaunchScript.TryResolveEnvToken`, before flag scanning.
- **D-T5 Grok delegate E2E**: one of the three Windows Grok delegate E2E cases becomes the V-5 end-to-end refusal; the other two get an explicit skip naming this card (see Out of scope). Every `delegate.ps1` dispatch composes `delegate-basics` (multiline), so no dispatched Grok worker can launch on Windows under D-6.

### Proves it works now

- V-1: unsafe composition is refused at both production composition sites, before any adapter or runner call.
  - V-1a: named Grok agent on the Herdr backend | integration | new `tests/Antiphon.Tests/Application/GrokRulesLaunchRefusalTests.cs::Named_grok_herdr_agent_with_orchestrator_rules_is_refused_before_any_session_exists` — harness `AgentControlServiceIntegrationTests.BuildHarness(tempRoot, [adapter], includeLaunchResolver: true)`; seed a Grok TUI profile (copy the `SeedProfileAsync` shape from `CardSpawnModelArgumentTests`); `AgentService.CreateAsync(new CreateAgentRequest(name, workspace, TuiProfileId: grok.Id, SessionBackend: SessionBackend.Herdr))`; `AgentBundleAttachments.SetAsync(db, agent, ["orchestrator"], now, ct)`; set a non-Normal `ReplyStyle` and `SystemPromptAppend` = unsafe `append`; `harness.Control.StartAsync(agent.Id, new StartAgentRequest(Fresh: true))` | `ConflictException.Code == CODE`; message names the agent, `--rules`, `line_break`; message contains neither `SENTINEL`, any line of the orchestrator bundle, the exe path, nor the word "override"; `adapter.Started == false`; `adapter.Prompts` empty; `AgentSessions.Count(s => s.Cwd == workspace) == 0`; agent `Status == Idle` (mirrors `Start_with_remote_control_on_a_grok_agent_is_refused_and_launches_nothing`).
  - V-1b: same agent on the default PtyHost backend | integration | `…::Named_grok_pty_host_agent_with_orchestrator_rules_is_refused_before_any_session_exists` | same expectations.
  - V-1c: cold Grok delegate through `AgentTaskDispatcher.BuildLaunchSpec` | integration (no DB) | `…::Cold_grok_delegate_with_composed_bundles_is_refused_at_compose_time` — `DelegateBundleLaunchTests`-style `CreateHarness`/`SpecOf`, task Worker/Investigate, kind Grok, attached `["orchestrator"]` | `ConflictException.Code == CODE`; message contains `DelegationReportFormatter.Short(task.Id)` and `Worker/Investigate`; no `SENTINEL`; in the same test the identical task on ClaudeCode and Codex composes without throwing (control).
  - V-1d: card-backed spawn | integration | `CardSpawnModelArgumentTests::Grok_assigned_card_spawn_with_multiline_standing_instructions_is_refused_and_creates_no_session` (reuses that file's private `SeedProfileAsync`/`SeedAssignedCardAsync`/`SpawnOnlyCardAsync`); set `agent.SystemPromptAppend` = unsafe `append` | `ConflictException.Code == CODE`; `adapter.Started == false`; `AgentSessions.Count(s => s.CardId == card.Id) == 0`.

- V-2: raw unsafe `--rules` that never went through composition is refused by the runtime guard in `AgentSessionService.BuildRuntimeLaunchSpecAsync`, fresh and resume, on both backends. All in `AgentSessionLaunchFailureTests` (its `LaunchFixture` is private to that file; the plan's mention of `AgentSessionRuntimeTests` was the wrong file — that class covers event routing, not launches).
  - V-2a: fresh, PtyHost | integration | `Grok_raw_multiline_rules_in_a_launch_argument_refuses_the_launch_before_the_process_starts` — `LaunchFixture.CreateAsync(adapter)`, `SetSessionKindAsync(fixture.SessionId, AgentKind.Grok)`, spec `new AgentLaunchSpec("fake", AgentKind.Grok, "fake", ["--always-approve", "--rules", "line one\nline two " + SENTINEL], { GROK_HOME }, fixture.Workspace, 120, 30)`, `fixture.LaunchInteractiveAsync(spec: spec)` | `ConflictException.Code == CODE` (type is `ConflictException`, not `ResumeTargetMissingException`); `adapter.Started == false`; session `Status == Failed`, `FailureReason` contains `CODE` and `--rules` and not `SENTINEL`, `TerminationSource == SystemRequest`, `EndedAt != null`.
  - V-2b: resume | `…_on_resume_too` — `resume: true` with `GROK_HOME/sessions/<escaped cwd>/<sessionId>/` seeded, fixture built with two adapters (`CreateAsync(resumeAdapter, freshAdapter)`) | same refusal; `freshAdapter.Started == false` (no create fallback — that fallback is keyed on `GrokNativeSessionMissing` only).
  - V-2c: Herdr backend, registry-style env token | `Grok_herdr_launch_whose_rules_come_from_an_env_token_is_refused_server_side` — session row `SessionBackend = Herdr`; spec `Backend: SessionBackend.Herdr`, args `["--rules", "$env:GROK_RULES"]`, env `GROK_RULES = "a\nb " + SENTINEL` | refused with `CODE`/`line_break`; message names `GROK_RULES` as the source token but not its value; `adapter.Started == false`. (Server-side registry args are not expanded — `AgentRegistry.Resolve` appends `ArgsTemplate` literally — so this is the server half of the env-token bypass closure.)
  - V-2d: PtyHost with the same token | `Grok_pty_host_launch_with_an_env_token_value_is_not_expanded_and_passes_the_rules_guard` | `adapter.Started == true`; `adapter.StartedArgs` contains the literal `$env:GROK_RULES` (pins D-T4 so Build does not over-block).
  - V-2e: equals form and alias | `Grok_rules_equals_form_and_append_system_prompt_alias_are_refused` — args `["--rules=a\nb"]`, then `["--append-system-prompt", "a\r\nb"]` | refused with `CODE` each; `adapter.Started == false`.

- V-3: the runner refuses before session registration, on both lanes, with no real Herdr. New `tests/Antiphon.SessionRunner.Tests/GrokRulesRunnerRefusalTests.cs`, `[NotInParallel("HerdrLaunchShape")]`, helpers copied from `HerdrLaunchShapeTests` (`BuildSettings`, `BuildRuntime(settings, fake)`, the `GrokIdentityRequest` shape with `TranscriptFormat: TranscriptFormats.Grok` and `Herdr.AgentKind = HerdrAgentKinds.Grok`), `fake.LaunchScriptAgentKind = HerdrAgentKinds.Grok`. Common expectations for every refused case: `Should.ThrowAsync<GrokRulesLaunchException>` (the S3 runner exception) with `Code == CODE`; message contains the session id, the flag, the reason, and not `SENTINEL`; `fake.Requests.ShouldBeEmpty()`; `fake.LastLaunchScriptContent == null`; `runtime.List().ShouldBeEmpty()`; `runtime.GetAsync(id)` throws; `File.Exists(HerdrLaunchScript.PathFor(...)) == false`; `HerdrPaneSidecar.PathFor(...)` and `HerdrLastPane.PathFor(...)` absent.
  - V-3a: Herdr, ordinary payload `["--always-approve", "--no-alt-screen", "--session-id", id, "--rules", "line one\nline two " + SENTINEL]` | runner | `Herdr_grok_multiline_rules_are_refused_before_herdr_is_contacted` | common expectations.
  - V-3b: Herdr, `--rules=<multiline>` | `Herdr_grok_rules_equals_form_is_refused` | common.
  - V-3c: Herdr, `--append-system-prompt <multiline>` | `Herdr_grok_append_system_prompt_alias_is_refused` | common.
  - V-3d: Herdr, args `["--rules", "$env:RULES"]`, env `RULES = multiline + SENTINEL` | `Herdr_env_token_resolving_to_multiline_rules_is_refused` | common; message names `RULES`, not its value.
  - V-3e: Herdr, args `["$env:FLAG", "line one\nline two"]`, env `FLAG = "--rules"` | `Herdr_env_supplied_rules_flag_is_refused` | common.
  - V-3f: Herdr, 4,097-unit single line | `Herdr_grok_oversized_rules_are_refused` | common; reason `token_too_long`; message carries the count and the 4,096 limit.
  - V-3g: Herdr through a wrapper exe: `pwsh.exe` with args `["-NoProfile", "-File", "C:\Users\x\.local\bin\gkp.ps1", "--project", "$env:X_LLM_PROJECT", "--rules", <multiline>]` and env carrying `X_LLM_PROJECT`, `GROK_BASE_URL`, `XAI_API_KEY` so `HerdrGkpLaunchGuard` passes | `Herdr_grok_wrapper_launch_with_multiline_rules_is_refused_by_kind_not_by_exe_name` | common. Proves the guard keys on the request's Grok identity, not the executable basename.
  - V-3h: PtyHost lane: `Backend: null`, `TranscriptFormat: grok`, exe `C:\tools\grok.exe` (nonexistent), same unsafe args | `Pty_host_grok_multiline_rules_are_refused_before_a_session_is_registered` | `GrokRulesLaunchException` with `CODE` — **not** a launcher failure; `runtime.List()` empty; `runtime.GetAsync(id)` throws. (The exception type is what PC-7 flips on this lane.)
  - V-3i: positive control, Herdr, single-line `["--rules", "keep it terse " + SENTINEL]` | `Herdr_grok_single_line_rules_proceed_to_herdr_unchanged` | `fake.Requests` non-empty; `fake.LastLaunchScriptContent` contains `'--rules', 'keep it terse <SENTINEL>'`; `dto.Status == "Running"` (mirrors `Grok_create_is_never_gated`).
  - V-3j: non-Grok control, Claude Herdr request with a multiline `--append-system-prompt` | `Herdr_claude_multiline_append_is_not_refused` | not refused; `fake.Requests` non-empty.
  - V-3k: HTTP contract | runner (pure) | `Grok_rules_refusal_maps_to_409_with_its_code` — call the **same static mapping method** that the new `catch (GrokRulesLaunchException)` clause in `src/Antiphon.SessionRunner/Program.cs` `POST /sessions` calls (Build names it; the test references that symbol, not a copy), in the shape of `HerdrAttachTests::Pane_occupied_on_sessions_post_is_a_409_with_its_code` | `ProblemHttpResult.StatusCode == 409`; `ProblemDetails.Type == CODE`; `Detail` contains the reason and session id, not `SENTINEL`.

- V-4: policy boundaries and pass-through | unit | new `tests/Antiphon.SessionRunner.Tests/GrokRulesArgvPolicyTests.cs` `[Category("Unit")]` against `Antiphon.SessionRunner.Contracts.GrokRulesArgvPolicy` (a payload API and an argv API; both take explicit `isWindows` and `isGrok` decisions and are validate-only — they return a violation or nothing, never a transformed value).
  - refused: CR-only; LF-only; CRLF; NUL; LF plus NUL (reports `nul` per D-T2); LF plus 4,097 units (reports `line_break`); 4,097 BMP chars (`token_too_long`, count 4,097, limit 4,096); 2,048 supplementary characters plus one BMP char (4,097 UTF-16 units — pins UTF-16 counting, not rune counting); trailing `--rules` with no value (`missing_value`); duplicates `--rules good --rules bad` and `--rules bad --rules good` (the diagnostic names the offending occurrence index); `--rules=a\nb`; `--append-system-prompt a\nb`; `--append-system-prompt=a\nb`.
  - allowed: a single line with spaces, double quotes, backticks, `$`, and non-ASCII; spaces only; empty string; `--rules=` (empty); exactly 4,096 BMP chars; exactly 2,048 supplementary characters (4,096 units); any payload when `isWindows == false`; any payload when `isGrok == false`; `--rules` appearing after a bare `--`; path-shaped single-line values `C:\Antiphon\logs\instructions\grok\<id>\rules.md`, `@C:\Antiphon\logs\instructions\grok\<id>\rules.md`, and `--rules=C:\…\rules.md` — the **CARD-0395 anti-conflict pin**: a future path-on-argv transport must pass this policy without changing it.
  - diagnostics: never contain the payload (`SENTINEL`), an env value, or the word "override"; always contain the flag name and reason, and the count/limit for `token_too_long`.
  - budget independence: `InstructionBundleComposer.EnsureWithinCommandLineBudget` with a 31,000-char single-line composition still throws `InvalidOperationException`; the existing `DelegateBundleLaunchTests::an_over_budget_composition_fails_the_launch_and_names_the_task` and `GrokDelegateDispatchTests::the_command_line_budget_guards_a_grok_launch_the_same_way` stay byte-identical under D-T1.
  - byte-identical safe lane at the composition boundary | integration | `GrokRulesLaunchRefusalTests::Named_grok_agent_with_a_single_line_append_launches_with_the_rendered_line_byte_identical` — Grok agent, no attachments, `ReplyStyle.Normal` (composes nothing), `SystemPromptAppend = "Reply tersely; sign as {agentName}. " + SENTINEL` | `adapter.Started == true`; `StartedArgs[IndexOf("--rules") + 1]` ordinal-equals `ChannelPreamble.Render(append, agent.Name, [])`.

- V-5: dispatcher settlement after the claim transaction | integration | in `GrokDelegateDispatchTests` (real `TickAsync`, shared DB, real `AgentSessionLaunchQueue`, `RecordingSessionStopper`).
  - V-5a: `dispatching_a_grok_worker_on_windows_is_refused_after_the_claim_and_settles_failed_with_the_rules_code` — `SeedQueuedTaskAsync(workspace, AgentKind.Grok)` (Worker/Docs composes `delegate-basics`) extended with `ReplyTo = Session` and `ParentSessionId` = a seeded parent session; `TickAsync` | task `Status == Failed`; `FailureReason` contains `CODE` and does **not** start with `Dispatch failed before a session existed`; `AgentSessionId != null` and that session row has `Status == Failed`, `FailureReason` containing `CODE`, `TerminationSource == SystemRequest`, `EndedAt != null`; `SessionQueuedMessages.Where(m => m.AgentSessionId == session.Id)` empty (no brief); exactly one `SessionQueuedMessages` row for the parent session with origin `Delegation` whose body contains `CODE`; the ephemeral pool agent row is gone; `stopper` recorded no stop; `TickResult.Dispatched == 0` and `Failures == 1` for this task; a second `TickAsync` leaves the task Failed, creates no new session, and adds no `AgentTaskEvent` naming another kind or backend (no reroute, no watchdog wait).
  - V-5b: `a_refused_cold_grok_dispatch_leaves_a_warm_grok_agent_untouched` — `SeedWarmAgentAsync` in another directory before the tick | after the refused dispatch the warm agent's `Status`, `PersistentSessionId`, and session row are unchanged; `stopper` empty.
  - V-5c: E2E — rewrite of `GrokDelegateEndToEndTests::a_Kind_Grok_worker_runs_from_the_delegate_script_to_a_grok_priced_settlement` as `a_Kind_Grok_worker_dispatched_from_the_delegate_script_is_refused_before_any_process_starts`: keep steps 1–2 (`delegate.ps1` → queued row → tick → `LaunchQueue.WaitForIdleAsync`) | task Failed with `CODE`; `harness.Runner.SpecFor(sessionId) == null`; `harness.Runner.ListAsync` empty; no `summary.json` under `grokHome`; no queued brief; session Failed/SystemRequest. Same class attributes (`[ParallelLimiter<ProcessSpawnLimit>]`, Windows and fakegrok skip gates).

Existing cases Build must adjust — exact list; nothing else in these files changes:

| File / case | Change |
|---|---|
| `DelegateBundleLaunchTests::a_grok_investigate_launch_carries_the_stage_bundle_on_rules` | Becomes a refusal: `Should.Throw<ConflictException>` with `CODE` naming the task. The stage-bundle content assertion moves to pure composition: `InstructionBundleComposer.Compose(InstructionBundles.ForDelegate(Worker, Investigate)).Text` contains `[bundle:stage-investigate v…]`, the bundle text, and `[bundle:delegate-basics v`. The Codex sibling is unchanged. |
| `DelegateLaunchArgvIntegrityTests::Every_dispatched_launch_round_trips_through_both_backends` and `…_with_role_defaults_alone` | For `agentKind == Grok` with a non-specialist role, `ComposeLaunchArgs` now throws: catch `ConflictException`, assert `CODE`, count it in `refusedGrokCases`; specialist roles on Grok (empty composition) still round-trip; final assertion `checkedCases + refusedGrokCases == full matrix`. Catalog and hostile-seed cases (Claude flag) unchanged. |
| `GrokDelegateDispatchTests::a_grok_delegate_launches_the_grok_definition_with_rules_and_a_grok_model` | Rename to `…_with_a_grok_model_when_nothing_composes`; use `TaskFor(Grok, High, Worker, AgentTaskRole.Check)` (specialist role → `ForDelegate` returns `[]`) so the definition, `--always-approve`, no `--name`, `--model grok-4.6`, and effort assertions survive; replace `ShouldContain("--rules")` with `ShouldNotContain`. V-1c owns the refusal. |
| `GrokDelegateDispatchTests::the_spec_a_grok_dispatch_builds_would_spawn_the_real_fakegrok_binary` | Same specialist-role treatment; `ShouldNotContain("--rules")`. |
| `GrokDelegateDispatchTests::the_command_line_budget_guards_a_grok_launch_the_same_way` | Unchanged (D-T1). |
| `GrokDelegateDispatchTests::dispatching_a_grok_task_writes_a_grok_session_a_grok_pool_row_and_a_spilled_brief` | Replaced by V-5a (same seed; Worker/Docs now refuses). The spill behaviour keeps its unit coverage (`FitBriefForTyping` in `DelegationUnitTests`/`GrokDeliveryShapeTests`). |
| `GrokDelegateDispatchTests::a_cold_grok_dispatch_records_a_grok_model_in_its_event` | Stays green (`LatestDispatchDetailAsync` filters on `Dispatched` events written in the claim transaction); add `task.Status == Failed` so the test states the whole truth. |
| `GrokDelegateDispatchTests` warm-pool, escalation, reuse-event cases and `a_kind_this_installation_cannot_launch_…` | Unchanged; must stay green (warm reuse composes nothing). |
| `GrokDelegateEndToEndTests::a_Kind_Grok_worker_runs_from_the_delegate_script_to_a_grok_priced_settlement` | Becomes V-5c. |
| `GrokDelegateEndToEndTests::an_unmarked_grok_reply_inside_one_tailer_poll_of_the_nudge_settles_unmarked_after_nudge`, `…::a_provider_that_never_answers_the_boot_prompt_is_failed_killed_and_retried_once` | Explicit early `SkipTestException("CARD-0382 D-6: Windows Grok delegate launches with composed rules are refused; re-enable when CARD-0395's transport lands")` (D-T5; see Out of scope). |
| `GrokDelegateEndToEndTests::a_ClaudeCode_worker_still_launches_claude_types_its_brief_inline_and_prices_on_the_claude_ladder` | Unchanged; keeps the dispatch→pty→settlement chain runnable. |
| `CodexDelegateDispatchTests::a_grok_delegate_is_untouched_by_the_codex_branch` | Use `AgentTaskRole.Check`; replace `ShouldContain("--rules")` with `ShouldNotContain`. |
| `CardSpawnModelArgumentTests` Grok cases (`Grok_assigned_card_spawn_carries_the_reasoning_effort_flag`, `Grok_4_5_frontier_card_spawn_clamps_xhigh_to_high`, `Spawn_with_remote_control_name_on_a_grok_card_is_refused_and_creates_no_session`) | Unchanged (no composition on those agents); V-1d added beside them. |
| `AgentControlServiceIntegrationTests` Grok cases (`T6_blank_field_grok_profile_starts_without_a_model_argument`, the two remote-control cases) | Unchanged. |
| `AgentSessionLaunchFailureTests` Grok resume cases (3) | Unchanged; V-2a–V-2e added to the file. |
| `HerdrLaunchShapeTests::Script_content_doubles_single_quotes_is_BOM_and_preserves_newline_args` | Rename to `…_for_generic_arguments`; fixture exe `C:\tools\it's\tool.exe`, flag `--note` instead of `--rules`, no Grok wording; escaping and BOM assertions unchanged. `Grok_create_is_never_gated`, the gkp and resume cases are unchanged. |
| `GrokNativeSessionCanaryTests`, `HerdrGrokNativeSessionLiveTests` | Do not use bundles; unaffected. |

### Guards the regression

- R-1: rendered rules appended before or without the payload check (`AgentSessionLaunchComposer.cs` Grok branch near line 114, `AgentTaskDispatcher.ComposeDelegateArgs` near line 3579) | caught by V-1a/V-1c because `adapter.Started` must be false and `ConflictException.Code == CODE`.
- R-2: rules routed through a whole-token `$env:NAME` on a registry definition or profile arg on Herdr | caught by V-2c and V-3d/V-3e because both guards resolve tokens with the `DollarEnvArg`/`TryResolveEnvToken` semantics before scanning.
- R-3: the policy starts normalizing newlines, truncating, or encoding the value | caught by V-4 and the byte-identical safe-lane case because a transformed value is neither refused nor ordinal-equal to the rendered text.
- R-4: runner backstop moved after `new RunnerSession`/`TryAdd`, or dropped on one lane | caught by V-3a and V-3h because `runtime.List()` and `fake.Requests` must be empty and the PtyHost exception type must be the rules refusal.
- R-5: dispatcher refusal handled by the generic outer catch (session left `Starting`, brief queued, reason mislabelled) | caught by V-5a because the claimed session row must be Failed/SystemRequest with the code and no child brief may exist.
- R-6: guard widened to Claude/Codex or to non-Windows | caught by V-3j, V-4 (`isGrok == false`, `isWindows == false`), the Claude/Codex cases in `DelegateBundleLaunchTests`, the non-Grok kinds of the `DelegateLaunchArgvIntegrityTests` matrix (multiline payloads still round-trip), and `CodexDelegateDispatchTests`.
- R-7: a future path-on-argv transport blocked by the guard | caught by V-4 path-shaped cases because a single-line path must pass untouched.
- R-8: Herdr env application/redaction, gkp routing guard, native-resume guard, idle-pane retention, generic script escaping | caught by `HerdrLaunchShapeTests`, `HerdrGkpLaunchGuardTests`, `HerdrGrokResumeGuardTests`, `HerdrAttachTests`, `DollarEnvArgTests`, `AgentTuiLaunchResolverTests` staying green in the forced run list.

### Positive controls

- PC-1: break V-1a/V-1b/V-1d by commenting out the policy call in `AgentSessionLaunchComposer` before the Grok `extraArgs.AddRange`; expect `GrokRulesLaunchRefusalTests` named cases and the `CardSpawnModelArgumentTests` refusal red (`adapter.Started` true, no throw).
- PC-2: break V-1c/V-5a by commenting out the policy call in `AgentTaskDispatcher.ComposeDelegateArgs`; expect `Cold_grok_delegate_with_composed_bundles_is_refused_at_compose_time` and `dispatching_a_grok_worker_on_windows_is_refused_after_the_claim_…` red.
- PC-3: break V-2a/V-2b/V-2e by commenting out the argv guard call in `AgentSessionService.BuildRuntimeLaunchSpecAsync`; expect those `AgentSessionLaunchFailureTests` cases red.
- PC-4: break V-2c by removing the Herdr `DollarEnvArg` expansion inside that guard (scan raw tokens); expect `Grok_herdr_launch_whose_rules_come_from_an_env_token_is_refused_server_side` red while V-2d stays green.
- PC-5: break V-4 by reducing the forbidden character set in `GrokRulesArgvPolicy` to `'\n'` only; expect the CR-only and NUL cases red.
- PC-6: break V-4/V-3f by changing the limit to 4,097 (or `>` to `>=`); expect the 4,096/4,097 boundary pair and `Herdr_grok_oversized_rules_are_refused` red.
- PC-7: break V-3 by commenting out the runner backstop call in `SessionRunnerRuntime.StartAsync`; expect V-3a–V-3h red (`fake.Requests` non-empty on Herdr; a launcher exception instead of the rules refusal on PtyHost).
- PC-8: break V-3d/V-3e by skipping `TryResolveEnvToken` before scanning in the runner backstop; expect those two red.
- PC-9: break V-5a by removing the session terminalization from the dispatcher's narrow refusal branch (let the generic catch run); expect it red (session `Starting`, reason prefixed `Dispatch failed before a session existed`).
- PC-10: break V-3k by changing the mapping method's `type` to `"conflict"`; expect `Grok_rules_refusal_maps_to_409_with_its_code` red.

Build runs each: break, see red, revert, see green, and reports all three; the commit message lists PC-1..PC-10 with the red test names.

### Out of scope

- Full composed-rules delivery to Grok and Herdr detection before the timeout: card acceptance items 1–2 remain **unmet** under D-6 and no test here may be cited for them. Follow-up: CARD-0382 stays open on those items; CARD-0395 owns the file transport and must confirm a native append mechanism with the installed binary and a captured request first.
- Live or headed Grok, a real Herdr instance, real `grok.exe`: not needed to prove refusal. The `RealCliStubEnv.ForGrok` dual-hit canary is reserved for the future transport; a refused or skipped launch must never be read as that canary passing.
- An HTTP-level runner test of the `Program.cs` catch clause: the runner has no `partial class Program` or `WebApplicationFactory` fixture. V-3k covers the mapping function; the catch wiring (`POST /sessions`, near lines 184–192) is a Review-stage check.
- Two Windows Grok delegate E2E behaviours lose runnable coverage until a transport lands: Grok nudge settlement and boot-stall retry via dispatch (`an_unmarked_grok_reply_…`, `a_provider_that_never_answers_…`). Every `delegate.ps1` dispatch composes `delegate-basics` (multiline) and specialist roles cannot be dispatched by the script, so there is no supported dispatched Grok launch on Windows to test them through. The Claude E2E keeps the dispatch→pty→settlement chain covered.
- Non-Windows runtime behaviour: only the pure policy's `isWindows: false` branch is tested; no CI leg runs the composition sites on Linux.
- Multiline values on flags other than the two recognized rules flags, or after a bare `--`: not this guard's remit (D-1); the 30,000-char budget still applies.
- Operator wrapper `gkp.ps1` behaviour, and edits under `docs/cards/`.
- CARD-0395 reservation: no test asserts the existence, path, or content of any rules file, and no refusal message mentions one. The only file assertions are the runner's own `.launch.ps1`, sidecar, and last-pane absences.

### Cost

- suites forced: `Antiphon.Tests` classes `GrokRulesLaunchRefusalTests`, `AgentSessionLaunchFailureTests`, `GrokDelegateDispatchTests`, `DelegateBundleLaunchTests`, `DelegateLaunchArgvIntegrityTests`, `CodexDelegateDispatchTests`, `CardSpawnModelArgumentTests`, `GrokDelegateEndToEndTests`, `AgentControlServiceIntegrationTests`, `DollarEnvArgTests`, `AgentTuiLaunchResolverTests`; `Antiphon.SessionRunner.Tests` classes `GrokRulesArgvPolicyTests`, `GrokRulesRunnerRefusalTests`, `HerdrLaunchShapeTests`, `HerdrGkpLaunchGuardTests`, `HerdrGrokResumeGuardTests`, `HerdrAttachTests`. One run per class: `dotnet run --project tests/<Project> --property:OutputPath=bin-card0382/ -- --treenode-filter "/*/*/<Class>/*"`; server project first, runner second, never overlapping `Antiphon.Agents.Pty.Tests`; delete every `bin-card0382` directory afterwards.
- verification floor ~ 45 min: two alternate-output builds ~8 min; class-filtered server runs ~15 min (`GrokDelegateEndToEndTests` ~4 min on the `ProcessSpawnLimit` lane, `AgentSessionLaunchFailureTests` ~3 min, `GrokDelegateDispatchTests` ~3 min, the rest ~5 min); runner classes ~4 min; PC-1..PC-10 at ~2 min each ~20 min. Authoring time is on top of the floor.

## Plan validation and handoff

Plan-stage checks: full card/related-card reads, source trace, installed Grok version/help, artifact link/diff checks. No tests, builds, model calls, or headed launches were run. Runtime implementation is entirely outstanding.

Land this plan's commit through the normal task landing operation before creating the next stage worktree. TestDesign must preserve D-6's availability/acceptance distinction, enumerate affected existing Grok-positive tests, and make V-1/V-2/V-3 red when their respective guard is removed. Code must implement and verify S1-S4 as one mitigation; do not deploy only the composition half without the runner backstop.
