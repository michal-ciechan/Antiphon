# CARD-0632: close the remote-prep backoff and claim-lock request as implemented

Date: 2026-09-26. Investigation base: `4e873cf20a61b0475f9a013b57168e6ecdd7aea5`, verified equal to both the local `origin/master` and `git ls-remote origin refs/heads/master` before this document was written. CARD-0632's 2026-09-23 symptom describes the pre-CARD-0633 dispatcher. CARD-0633's existing [plan](2026-09-23-card-0633-dispatcher-remote-prep-plan.md) explicitly folded in CARD-0632. This artifact records the current disposition; it changes no production code or tests.

## Verified findings

| Finding | Current source evidence |
|---|---|
| A failed branch push or mirror no longer causes a retry on every tick. | `server/Domain/Entities/AgentTask.cs:233-243` persists `RemotePrepFailures` and `DispatchNotBeforeAt`; `server/Migrations/20260923225802_AddRemotePrepBackoff.cs:12-25` added the columns. `server/Application/Services/RemoteWorkspacePreparer.cs:58-65,169-187` increments failures and sets exponential not-before time after a failed attempt. Defaults are 30 seconds and 900 seconds (`server/Application/Settings/DelegationSettings.cs:616-624`). `server/Application/Services/AgentTaskDispatcher.cs:1033-1047` holds an in-flight or not-yet-due task before a claim. |
| The branch push and WorkspaceMirror no longer run inside the claim or serial tick. | `server/Application/Services/AgentTaskDispatcher.cs:4303-4319` opens and locks the claim row. On an unprepared runner task it saves the worktree, commits at `:4644-4646`, and calls non-awaiting `TryBegin` at `:4647`. `server/Application/Services/RemoteWorkspacePreparer.cs:71-80,112-145` runs the push/mirror on a background task with its own scope, then records the mirror path. `PrepareRemoteWorkspaceAsync` now only gates eligibility and reads a recorded path (`server/Application/Services/AgentTaskDispatcher.cs:5775-5828`); it makes no runner RPC. A prepared task launches on its next crossing. |
| The original cancel and starvation failures have dedicated existing regression tests. | `tests/Antiphon.Tests/Application/RemoteWorkspacePreparerTests.cs:36-58` proves the silent mirror leaves the row Queued and a later tick returns; `:92-149` covers 30/60/120-second backoff and reset; `:241-269` cancels while the mirror is pending. `tests/Antiphon.Tests/Application/DispatcherRemotePrepStarvationTests.cs:30-158` puts three silent remote tasks before a local recipient and verifies its complete delivery plus three later warnings. These tests are source evidence, not a fresh run in this investigation. |
| CARD-0727 R2 changed the drain path after the original fix. | `server/Application/Services/AgentTaskDispatcher.cs:944-991` reloads a queued task, removes a recorded old mirror, and resets failures/not-before on a successful redirect. `:1037-1046` holds an in-flight mirror **before** rebind, then applies the backoff to the selected runner. `:4623-4638` drops the claim lock before writing a late-drain warning. `tests/Antiphon.Tests/Application/PhoneHomeRollingRunnerDrainTests.cs:563-593,649-667` covers retry on the target and late-drain refusal. This does not restore the original in-claim mirror. |
| A narrower claim-lock defect remains on its own card. | `server/Application/Services/AgentTaskDispatcher.cs:5785-5800` calls `RemoteWarnAsync` for a non-drain runner-unavailable refusal while the claim is still open; `:5829-5857` inserts from a separate scope. That insert can wait for the claim row lock. CARD-0750 records this exact defect and its possible serial tick stall. Its fix belongs on CARD-0750; it is a database warning write, not the five-minute WorkspaceMirror RPC named by CARD-0632. |

## Smallest fix and disposition

No CARD-0632 code change remains. Close CARD-0632 as folded into CARD-0633; keep CARD-0750 open for the runner-unavailable warning lock. Do not remove the existing backoff columns or fold CARD-0750 into this closure. The runner drain's old-mirror removal (`AgentTaskDispatcher.cs:962-965`) is also a serial network await before the claim, so a future drain performance investigation may need its own scope; it is not the failed prep retry described by this card.

## Verification design

The following closed list is a **verification-only** gate if the owner wants a fresh run before closure. It requires no implementation slice. Each row has one isolated build and one exact class-level TUnit filter; invoke `dotnet run --project tools/Antiphon.Checkpoints -- run --plan docs/superpowers/plans/2026-09-26-card-0632-plan.md` after commit, then `wait` until the exit is not 75. The test classes require the usual isolated-schema PostgreSQL fixture. `Min` counts TUnit results, including `[Arguments]` expansions, not assertions. The observed results must be reported; this investigation did not execute the rows.

### Checkpoints

| CP | After | Build | Group | Filter | Covers | Expect | Min | EstimatedMinutes |
|---|---|---|---|---|---|---|---:|---:|
| CP-1 | all | `tests/Antiphon.Tests -> bin-c632-prep/` | remote-prep | `/*/*/RemoteWorkspacePreparerTests/*` | G-1 backoff, unlocked cancel, restart, prepared launch | >= 18 executed, 0 failed | 18 | 7 |
| CP-2 | all | `tests/Antiphon.Tests -> bin-c632-starve/` | starvation | `/*/*/DispatcherRemotePrepStarvationTests/*` | G-2 silent mirrors do not block a local recipient | >= 4 executed, 0 failed | 4 | 6 |
| CP-3 | all | `tests/Antiphon.Tests -> bin-c632-drain/` | drain-rebind | `/*/*/PhoneHomeRollingRunnerDrainTests/*` | G-3 CARD-0727 drain rebind and late refusal | >= 19 executed, 0 failed | 19 | 9 |

### Cost

Estimated ordinary verification: **22 minutes** (7 + 6 + 9), three isolated builds and 41 TUnit executions. Positive-control floor: **0 minutes / 0 new red runs**, because this is a closure check of existing behavior with no new test or production line. If CARD-0750 is implemented, its own plan must price and demonstrate a red positive control for the warning-lock path. No build or test was run for this investigation.

## Risks

- Source inspection and existing tests establish the implementation, but this investigation did not re-run them or verify the live server's activated SHA. A closure requiring current execution evidence can use the exact checkpoint list above.
- The runner-unavailable warning can still stall on a row lock (CARD-0750). Closing CARD-0632 must not be read as closing that distinct defect.
- A drain redirect can await removal of an old mirror before claim. The CARD-0727 rebind path is separate from ordinary remote preparation and merits attention if drain ticks stall.
