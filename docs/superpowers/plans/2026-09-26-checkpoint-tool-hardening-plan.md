# Checkpoint tool hardening: CARD-0759, CARD-0760, CARD-0762

## Verified baseline (2026-09-26)

Source baseline is `8805577f19bc4d63fb7139552fa04bc1fe5bc0a7`, the current `master` and this task branch's starting HEAD. All three board cards are open with no revisions. The CARD-0759 and CARD-0762 incident times and exit codes are board evidence; this investigation did not reproduce either live incident.

| Card | Finding on current master and cause |
|---|---|
| CARD-0759 | Still open. `CheckpointApp.Start` detaches `execute` without task ownership arguments (`tools/Antiphon.Checkpoints/CheckpointApp.cs:154-169`); `Program` calls `ExecuteAsync(..., CancellationToken.None)` (`Program.cs:64-65`); the scheduler only cancels on its total timeout (`Execution/RunScheduler.cs:41-80`). `--parent` belongs to the **slot holder**, started with the executor pid (`Slots/LeaseHolder.cs:20-22,35-50`; `Program.cs:72-98`), so it keeps the broker lease tied to the executor, but cannot notice that the owning Antiphon task has settled. `BuildSlotClient` captures `ANTIPHON_TASK_ID` only as lease metadata (`Slots/BuildSlotClient.cs:133-140`), and releases after driver completion (`:170-192`). The tool can therefore start CP-2 after settlement while its executor and holder remain alive. `EstimatedMinutes` is one integer (`Manifest/PlanTableImporter.cs:80-95`), and row timeout is three times it (`Execution/RowTimeout.cs:5-11`); the CARD-0759 Windows 9-minute CP-1 estimate is about 3x low. |
| CARD-0760 | The absolute claim “no way to make a row serial” is stale: YAML has `CheckpointSpec.Serial` (`Manifest/CheckpointManifest.cs:64-79`), and the scheduler honors it (`Execution/RunScheduler.cs:278-305`). CARD-0726's checked-in YAML at commit `89a00a7b` sets CP-12 `serial: true`; that commit is on `feat/card-task-ad64242f`, **not current master**, so the named YAML file is absent here. The live gap is the documented `run --plan` path: the importer has no serial column and assigns only the first nine fields (`Manifest/PlanTableImporter.cs:16-17,62-89`), while `ExtractPayload` takes only the backticked filter and drops the following `TUNIT_MAX_PARALLEL_TESTS=1` text (`:281-293`). `RowRunner` has no per-row environment field (`Execution/RowRunner.cs:3-30`), so the resolved Markdown manifest stays `serial: false`. |
| CARD-0762 | Still open. Concurrent builds/rows share one `BuildSlotClient` logging callback (`CheckpointApp.cs:19-20,37-40`), and each slot grant/wait/release invokes it (`Slots/BuildSlotClient.cs:149-171,206-211,232-244`). The callback uses independent `File.AppendAllText` opens with no synchronization. On Windows, overlapping exclusive opens can throw `IOException`; that escapes the scheduler to `CheckpointApp.ExecuteAsync`'s catch and becomes exit 6 (`CheckpointApp.cs:69-76`). `--serial` reduces row overlap but the log writer must be safe even when build and row lease events coincide. |

### Importer integrity and authoring friction

- `Filter` text such as `same as CP-1` is **not a filter**. The importer treats any payload not starting `/` as a shell command (`PlanTableImporter.cs:98-109`), silently ignoring a CP reuse build cell for that command (`:126-129`). Refuse this phrase with a diagnostic requiring the exact filter. Auto-expanding it would undermine the closed-list, exact-filter contract.
- `Build` accepts only bare `CP-n` or `<project> -> bin-x/` (`:131-147`). Thus `CP-1 (-NoBuild)` from CARD-0711's plan needs hand editing. Accept this exact annotation as equivalent to `CP-1` and still check prior-row/build identity; reject other prose. This belongs with CARD-0760's importer slice.
- The CARD-0585 rule says reuse only within the same `After` group (`docs/testing-and-build.md:155-162`). The current importer instead warns and sets `RelaxSharedBuildAfter=true` (`PlanTableImporter.cs:133-140,164`), bypassing the YAML validator's proper rejection (`Manifest/ManifestValidator.cs:63-70`). Remove the relaxation and reject cross-slice reuse; require a fresh build row. This integrity repair belongs in the importer slice.
- A bare `|` inside a Markdown filter splits cells; `SplitRow` already unescapes `\|` (`PlanTableImporter.cs:249-278`). Keep Markdown escaping, add a targeted malformed-column diagnostic, and show the escaped combined-class form in the doc. No implicit reconstruction of ambiguous cells.

## Design and slices

### S1 — CARD-0759: stop with the owning task

Add an executor ownership guard enabled when the inherited `ANTIPHON_TASK_ID` and `ANTIPHON_API` are present. It polls `GET /api/agent-tasks/{id}` with the inherited task token, reading `summary.status` and `session.status` (`server/Api/Endpoints/AgentTaskEndpoints.cs:155-164`; `server/Application/Dtos/AgentTaskDtos.cs:248-277,382-385,386-442`). `Succeeded`, `Failed`, or `Canceled` settles ownership; `Stopped`/`Failed`/`Stopping` on the matching task session means the task's process owner has gone. Do not watch the transient `run` CLI pid: it exits normally after detaching. Do not persist the token or echo request headers into `request.json`, executor.log, or reports. Local tool use without task env stays unguarded.

Check ownership before creating a run, at executor entry, and before each build/row launch; poll during a running driver and slot wait (target interval 2-5 seconds, bounded HTTP timeout). On settlement or dead session, cancel all work, kill **each** child process tree, await all row/build tasks and lease disposal, mark pending rows `owner-ended`, publish a terminal report and return a distinct nonzero ownership exit (propose 7; keep exit 6 for crashes). A status read failure is not proof of settlement: retry briefly, then fail closed for a task-bound executor with a recorded `owner-unverified` reason. Fix `RunScheduler`'s external-cancellation path, which currently checks only `totalTimedOut` (`Execution/RunScheduler.cs:66-81`) and can otherwise schedule later rows. `ProcessDriver` must cancel its own local process rather than its shared `_current` field (`Execution/ProcessDriver.cs:19,35-45,57-73`) so two simultaneous drivers are both killed and their leases can dispose. Ensure cancellation during baseline comparison and cleanup cannot start new work.

For estimates, add optional `EstimatedMinutesWindows` after the nine required table columns, selecting the Windows value into the resolved row's `EstimatedMinutes` and derived row/total deadlines. Require positive integers and retain the original `EstimatedMinutes` for Linux. Document separate ordinary cost floors. This avoids a global multiplier hiding short Windows rows; CARD-0759's git-heavy row can state 9 Linux / 27 Windows. Keep the existing nine-column table valid.

Verification design: new `CheckpointTaskOwnershipTests` with four deterministic cases: terminal task before executor admission starts zero drivers; settlement during two parallel drivers cancels both, releases both fake leases and skips later rows; a stopped bound session stops the executor while a normally exiting start CLI does not; transient HTTP error recovers, persistent unknown ends `owner-unverified` with no further launch. Use an in-memory HTTP handler/fake driver; no production task, runner, or build slot. Positive control PC-1 removes the terminal-status cancellation branch; the settled-during-run case must fail at its no-later-launch/lease assertion, then pass restored.

### S2 — CARD-0760: table serial declaration and strict import

Add optional `Serial` after the required table columns (`true`/`false`, blank = false); importer sets `CheckpointSpec.Serial`. `run --plan` and `import --plan` must yield the same value, and the resolved YAML must show it. `Serial=true` means scheduler exclusion only. It does **not** set `TUNIT_MAX_PARALLEL_TESTS=1`, which governs tests within one TUnit host; add a separate explicit per-row `Environment` column with `NAME=value` parsing and child-only propagation, rather than accepting prose in Filter. CARD-0726 CP-12 can then declare `TUNIT_MAX_PARALLEL_TESTS=1` and `Serial=true` independently. Put the parsed map on `CheckpointSpec`/`RowRequest`/`DriverRequest`, and apply it to the child `ProcessStartInfo.Environment` (`Execution/IDriver.cs:15-20`; `Execution/ProcessDriver.cs:75-84`), never to global process environment or other rows. Validate names and duplicate keys. Update `docs/testing-and-build.md` schema/example. Support the narrow annotated Build form and refuse `same as CP-n`, cross-After reuse, and malformed extra cells as above.

Verification design: extend `CheckpointImportTests` with at least four cases: optional Serial/Environment are imported and round-tripped, a fake two-row run observes the environment only on its target row, annotated reuse maps to the prior build, and the three invalid shorthand/reuse/malformed cases refuse before execution. Retain its eight existing tests, so the class floor is 12. Existing `RunSchedulerTests.serial_rows_run_alone` covers scheduler exclusion. Positive control PC-2 removes the importer Serial assignment: the new import case must fail at `Serial=true`; restore it and rerun.

### S3 — CARD-0762: one executor log writer

Replace `Note`'s per-event `File.AppendAllText` with a single executor-owned serialized writer (lock around one append handle or a channel consumed by one writer). Dispose/flush it after all build and row work ends; keep the catch path able to write a crash reason without recursively failing. Do not suppress I/O errors as if evidence were saved. Keep each row's separate build/console log paths. Do not rely on `--serial`.

Verification design: new `CheckpointExecutorLogTests` with two tests: coordinate two parallel row/slot callbacks on a barrier and assert every line appears once, uncorrupted, with exit not 6; inject a write failure and assert a clear terminal error without a recursive logging crash. Positive control PC-3 removes serialization; the concurrent append test must fail under a deterministic shared-open seam on Windows and Linux, then pass restored. A stress loop alone is not a sufficient red control.

## Verification design

The Code stage runs the following **closed list** through `dotnet run --project tools/Antiphon.Checkpoints -- run --plan docs/superpowers/plans/2026-09-26-checkpoint-tool-hardening-plan.md --after S1` (then `S2`, `S3` separately after each slice commit), waiting through exit 75. Each row builds once into its own isolated output and runs one exact class filter. Do not co-schedule `Antiphon.Tests` with `Antiphon.Agents.Pty.Tests`. The three optional columns are implemented by S1/S2 before their rows execute; the table itself needs no edit. The Test design section below specifies bootstrap leasing, exact methods, controls and the final roster.

### Cost

Ordinary checkpoint floor: **20 minutes Linux** (8+6+6) or **28 minutes Windows** (12+8+8), excluding broker wait and authoring. Test design below supersedes the initial three-control/12-minute Mutation estimate and 45/55-minute Code estimate with the full guard inventory and numeric costs. Every new test must have a production-line red control, not merely a compile error, fixture failure, or zero-test result. Code runs ordinary V/R; Mutation executes the controls after land.

### Checkpoints

| CP | After | Build | Group | Filter | Covers | Expect | Min | EstimatedMinutes | EstimatedMinutesWindows | Serial | Environment |
|---|---|---|---|---|---|---|---:|---:|---:|---|---|
| CP-1 | S1 | `tests/Antiphon.Tests -> bin-c759/` | task-ownership | `/*/*/CheckpointTaskOwnershipTests/*` | V-1, V-2, V-3, V-4, R-1 | all 17 methods, 0 failed/skipped | 17 | 8 | 12 | false | n/a |
| CP-2 | S2 | `tests/Antiphon.Tests -> bin-c760/` | importer-serial | `/*/*/CheckpointImportTests/*` | V-5, V-6, V-7, V-8, R-2 | all 19 methods, 0 failed/skipped | 19 | 6 | 8 | true | `TUNIT_MAX_PARALLEL_TESTS=1` |
| CP-3 | S3 | `tests/Antiphon.Tests -> bin-c762/` | executor-log | `/*/*/CheckpointExecutorLogTests/*` | V-9, V-10, R-3 | all 3 methods, 0 failed/skipped | 3 | 6 | 8 | false | n/a |

V-1..V-4 are ownership admission, running cancellation/leases, dead session, and API uncertainty. V-5..V-8 are table fields, environment isolation, annotated reuse, and strict refusals. V-9..V-10 are simultaneous logging and write failure. R-1..R-3 are unguarded local use, existing import fixtures/YAML, and parallel scheduler behavior. TestDesign must preserve these floors and exact class filters if it names individual methods.

## Risks and acceptance

- The task GET is a server read, and the executor can lose connectivity independently of the task. Bound retries and report uncertainty; never convert an HTTP error into “task still live.” A settled task may be observed up to one poll interval after the transition; recheck immediately before every new launch. The holder's `--parent` remains a lease safety net when the executor dies, not an owner signal.
- Cancellation must drain every task and child process before reporting `done`; a successful DELETE alone is insufficient if a child still runs. Test two concurrent drivers and slot leases. Preserve the existing `stop`, timeout, and crash exit semantics for unbound runs.
- Optional table columns must be named and validated; ignored unknown columns are unsafe because a typo can silently remove serialization. The existing nine-column plans continue to import. `run --plan` is the acceptance path, not a hand-edited YAML workaround.
- The CARD-0726 YAML evidence is branch-only as of this baseline. Recheck its landed state before Code uses it as a fixture; the implementation does not depend on that branch.

No production code or tests changed in this Investigate stage. TestDesign should refine method names and red controls while keeping the three slices and checkpoint roster.

## Test design

Designed at `7c7e685a085d20c89cce8fdb872f7fd1723ec80f`. This section refines S1/S2/S3; it does not add a fourth slice. Only this plan changes in TestDesign. The three checkpoint IDs, isolated outputs, exact class filters and estimates above are retained; execution floors increase to the explicit roster below. Cases inside a method are separate arrangements/assertions, **not** extra TUnit executions. All new methods are ordinary `[Test]` methods without platform skips or argument expansion: CP-1 = 17 new, CP-2 = eight existing plus 11 new, CP-3 = three new, **39 executed** in total.

### Inspection and boundaries

Bodies read: `CheckpointImportTests`, its three Markdown fixtures, `CheckpointTestSupport` (`FakeDriver`, `FakePlatform`, HTTP handlers, TRX writer), `RunSchedulerTests`, `TimeoutTests`, `ProcessDriverTests`, `CheckpointAppTests`, `WaitCommandTests`, `ExitCodeTests`, `BaselineComparerTests`, and `DetachedLauncherTests`. Production paths read: `Program` import/run/execute/selection, `CheckpointApp`, `RunScheduler`, `ProcessDriver`, `RowTimeout`, `RowRunner`, manifest importer/validator/loader/models, `BuildSlotClient`/`SlotLease`, `FixedSlotClient`, `BaselineComparer`, and `DetachedLauncher`. The task detail DTO supplies `summary.id`, `summary.agentSessionId`, `session.sessionId`, and string status values; the request header is `X-Antiphon-Task-Token`.

- V-1/V-2: admission, detached entry, each build/row launch, slot waiting, cancellation, driver exit and lease disposal. A driver cancel notification alone is insufficient.
- V-3/V-4/R-1: all three terminal task statuses; matching dead sessions; live/blocked tasks; unbound execution; starter exit; unavailable, malformed and mismatched API responses; credentials; baseline and cleanup; Windows/Linux deadlines.
- V-5/V-6/R-2: named optional headers, serial exclusion versus intra-host test parallelism, YAML parity, environment isolation through the actual child-start path, nine-column compatibility.
- V-7/V-8: annotated prior-row reuse, same-After identity, malformed table cells and forbidden shorthand. Invalid fixtures must otherwise be valid so the intended guard is decisive.
- V-9/V-10/R-3: simultaneous log callbacks, I/O failures, flush/disposal ordering, and retained separate build/row log paths.

### Required deterministic setup

Add the support to `tests/Antiphon.Tests/Checkpoints/CheckpointTestSupport.cs` (or a sibling support file). Existing `FakeDriver.KillCount` and `WaitUntil` are inadequate for the races. Do not use wall-clock sleeps, probabilistic stress, production task IDs, the live API, the production runner, or live build-slot leases **inside these tests**.

1. **Executor composition.** Add injectable external-I/O dependencies used by the real start/execute/CLI paths: owner HTTP handler, environment lookup, clock/delay, detached launch, driver/slots, and log sink. Keep the production scheduler, row runner, request serialization, report/state writer and command selection in the exercised path. Tests use a temporary repository/run directory, synthetic task/session GUIDs, a synthetic token, and a handler that refuses every unexpected URI. Never clear or overwrite the test host's inherited `ANTIPHON_*` variables. A helper that computes the expected result instead of calling these production paths is not acceptable.
2. **Owner clock/HTTP.** Default policy for this design is a three-second poll, two-second HTTP deadline, and three consecutive failed reads before `owner-unverified`. A successful read resets the failure count. No new launch is admitted while that launch's owner read is unresolved. Use a controllable delay/clock and queued response-completion gates: advance exact polls/deadlines, acknowledge observation, then inspect results. HTTP 404/401/403/500, malformed JSON, missing/unknown status and wrong identity are unverified, never settlement or permission to proceed. A task ID with incomplete configuration is unverified; only absence of a task ID is local unbound use. These values are implementation defaults, not sleeps in tests.
3. **Driver/lease ordering.** Record monotonic event sequence numbers, not timestamps. Independently gate `Entered`, `CancellationObserved`, `ExitObserved`, and `ReleaseCompleted` for each invocation and lease. Acquire two distinct fake leases. Hold exit and release after cancellation; completion must remain pending. A final `finally` opens every gate and awaits all tasks, even on an assertion failure. A bounded wait may diagnose a broken harness but never establishes a passing assertion.
4. **Real ProcessDriver logic.** Add an external process-I/O factory/handle seam to `ProcessDriver`; its normal adapter wraps `System.Diagnostics.Process`. The test drives two calls on the **same production ProcessDriver**, using independently observable handles A/B, including child-tree kill flags and exit/drain receipts. The adapter must not implement the cancellation policy: `ProcessDriver` must decide which handle to kill and await. This catches restoring `_current` even when all scheduler tests with `FakeDriver` pass. No native process is necessary for this control; it verifies handle ownership and `entireProcessTree=true`, not the operating system's implementation of tree kill.
5. **Log race.** Use a single executor-owned writer with an awaitable serialized append core and injected asynchronous sink (a semaphore around one append handle is an acceptable implementation of S3). The synchronous slot callback can await that core. In the test, call append A directly; it enters the sink and pauses before completing. Invoke append B directly, without `Task.Run`: both calls have now reached their first incomplete await. With serialization, B waits at the writer gate and the sink has one active write. With the production gate bypassed, B synchronously enters the sink while A owns it. The sink records an overlap and returns a faulted task containing `IOException("overlapping append")`. Release A and await both. Thus the mutant fails by an observed outcome, not by waiting to see whether a second thread happens to run. Do not put a two-party barrier inside the protected sink: the correct writer would deadlock. Also exercise the executor's actual callback adapter with two gated slot/row callback producers and the same writer instance; synthetic grant/release messages have unique IDs.

The seams above are missing setup to implement in the relevant slice. Keep them narrow external-I/O seams, with normal production defaults; do not add a parallel test-only scheduler or mutable global test hooks. Tests stay `Category("Unit")`; if implementation substitutes a real process for a handle, that class must take the assembly's `ParallelLimiter<ProcessSpawnLimit>` and its native assertion/cost must be reported.

The owner watcher is the tool's polling protocol, not an admission into the server's CARD-0717 Polly read pipelines (`docs/resilience.md`). Each poll makes one bounded GET; a later poll is a new ownership observation. Do not broaden the server retry registry or apply a global retry policy as part of this work.

### Delivery inventory

There is no messaging/session-input delivery change and no UserPrompt claim. The async transfers here are:

| Producer -> recipient | Identity and persistence | Recovery and decisive receipt |
|---|---|---|
| `run/start` -> detached `execute` | Synthetic task ID, bound session ID, run directory; resolved manifest and request persist nonsecret identity only | Starter exit is harmless; executor rechecks owner before work. Receipt is zero launch on refused ownership, or the actual driver's `Entered` event on acceptance. |
| Owner observation -> scheduler -> drivers/leases | Run ID + row/build ID + each lease/process handle | Busy-slot and already-eligible-row cases below. Receipt is every local handle exited/drained and every lease released, followed by persisted exit 7 and `owner-ended`/`owner-unverified` reasons; never just cancel/DELETE success. |
| Slot/row producers -> executor log -> report/wait | Run directory + unique message ID; append sink, `state.json`, `report.json`/`report.md` | Busy sink, already queued callback, write/flush failure and drain controls below. Receipt is exact persisted lines and terminal state/report read back through production wait. An enqueue acknowledgment alone does not count. |

HTTP/process/slot substitutes prove policy, ownership and ordering. They do not prove deployed authentication, OS process-tree behavior or recovery after an OS forcibly kills the executor before a report can be written. Existing exit-6 handling remains that crash path; adding durable work resumption is outside these three slices.

### Proves it works now: S1

Every method in this table is in **`tests/Antiphon.Tests/Checkpoints/CheckpointTaskOwnershipTests.cs`** and runs in **CP-1**. Each row names its exact assertion and a compiling production defect for its PC; all methods call production components through the setup above. G-n is the guard protected by PC-n, with matching numeric IDs.

| Guard / PC | Exact method | Arrangement and exact assertion | Production defect that must turn the method red |
|---|---|---|---|
| G-1 / PC-1: settlement cancels active work | `settlement_cancels_two_drivers_and_skips_later_rows` | Width two, shared successful build, A/B inside drivers, C queued. Deliver `Succeeded` and await owner observation. Both cancellation receipts must exist; release exit/disposal gates; `LiveHandles == 0`, `LiveLeases == 0`, C driver calls = 0, C state = `owner-ended`, executor/report/wait exit = 7. Repeat terminal result setup for `Failed` and `Canceled`. | Remove the owner's terminal-task branch that signals cancellation. Explicitly complete A/B after the observation gate so the mutant completes with exit other than 7 or launches C; a watchdog expiry is not PC evidence. |
| G-4 / PC-4: refusal before creating a run | `terminal_owner_refuses_start_without_creating_a_run` | For each terminal status, invoke the real start admission path: exit = 7, launch count = 0, slot calls = 0, and results directory/run/request files absent. A live control reaches fake detached launch exactly once. | Remove the pre-`CreateRun` owner admission check. It creates files or launches once, failing the zero-side-effect assertion even if executor-entry protection would later catch it. |
| G-5 / PC-5: detached entry checks again | `executor_rechecks_owner_after_start_admission` | Start admitted while live, settle before the captured executor entry is invoked. Existing request retained; build and row driver calls = 0, all selected rows `owner-ended`, persisted terminal exit = 7. | Remove the owner check at execute entry; suspend periodic polling until this entry boundary has been exercised. A permissive recording slot/driver detects the forbidden call. |
| G-6 / PC-6: build launch admission | `each_build_rechecks_owner_immediately_before_launch` | Admit executor live. Gate the next build at its acquired-lease boundary, change owner to terminal, release boundary; that build's driver calls = 0 and its lease releases once. Earlier completed build evidence remains intact. | Omit the owner recheck at the final build launch boundary. Keep periodic polling paused so only this check can reject the call. |
| G-7 / PC-7: row and retry launch admission | `each_row_and_rerun_rechecks_owner_before_launch` | Independently arrange a ready next row and an eligible known-flaky retry. Owner changes after selection/TRX parse but before launch. Target driver calls = 0, target state `owner-ended`, report exit = 7. A live control executes the exact selected filter. | Bypass the common row-driver admission check, including its retry path. Permissive recording driver must observe the otherwise forbidden call; cancellation at another layer must not mask the assertion. |
| G-8 / PC-8: slot waits remain cancelable | `ownership_loss_cancels_slot_wait_and_rejects_a_late_grant` | Enter a busy fake slot acquisition, signal settlement, observe cancellation of its request. In a second arrangement deliver a grant concurrently with cancellation. Zero driver calls in both; every created holder/late lease disposed exactly once; zero live holders/leases on terminal return. | Pass `CancellationToken.None` into the owner-bound acquisition. The fake explicitly records token cancelability and cancellation delivery, then completes its wait so a failure is an assertion rather than a hang. |
| G-9 / PC-9: each process belongs to its own call | `process_driver_cancellation_kills_each_local_tree` | Start A then B through the same real `ProcessDriver`. Cancel A while B's token is live: kills(A) = 1, kills(B) = 0, A kill has `entireProcessTree=true`. Cancel B: kills(B) = 1; both calls await their own exit and output-drain receipts before completing. | Replace the cancellation catch's local-handle kill with `Kill(...)` targeting shared `_current`. A's expected kill count becomes 0 and B's becomes 1 before B is canceled. |
| G-10 / PC-10: terminal means drained | `terminal_publication_waits_for_driver_exit_and_lease_disposal` | After both cancels, complete A but keep B exit blocked; then complete B but keep one release blocked. At both barriers executor task incomplete and no terminal report/state. Open the last release: terminal readback exit 7, each release once, zero active tasks. | Restore scheduler fire-and-forget continuation for canceled running rows instead of awaiting them; advance the executor to its publish boundary and assert it published with B/release outstanding. |
| G-11 / PC-11: dead bound session | `stopped_bound_session_ends_an_unsettled_task` | Live task summary plus matching session `Stopped`, `Failed`, or `Stopping` ends with exit 7/reason `owner-ended` and no later launch. Controls with a live bound session and nonterminal task (`Dispatched`, `Working`, `Blocked`) continue. | Remove the dead-session branch while leaving terminal-task handling intact; a live task/dead session is wrongly admitted. |
| G-12 / PC-12: owner identity is authoritative | `mismatched_task_or_session_is_unverified` | Wrong `summary.id`, wrong `session.sessionId` versus `summary.agentSessionId`, and a session change after initial binding each yield bounded unverified recovery; zero launch while unresolved, then exit 7/reason `owner-unverified` if persistent. A mismatched dead session is never reported as this owner's death. | Bypass the single response-identity validation step. Supply a wrong-ID but live payload so admission occurs; the zero-driver-call assertion turns red. |
| G-13 / PC-13: local use and detachment survive | `unbound_runs_and_normal_starter_exit_remain_allowed` | With injected environment lacking task ID, no owner HTTP calls and a successful run returns 0. With a live owner and captured starter now exited, rows still finish at 0; no owner liveness lookup ever receives starter PID. Preserve unbound timeout exit 5 and crash exit 6 in subcases. | Use starter-process liveness as an owner-ended condition after detachment. The bound/starter-exited arrangement returns 7 instead of 0. |
| G-14 / PC-14: uncertainty is bounded and closed | `transient_owner_errors_recover_and_persistent_errors_stop` | One failed read then live response allows work, resets failure count; three consecutive failed/expired reads stop active work with exit 7/`owner-unverified`. Drive two-second HTTP deadlines and three-second polls explicitly. Assert exactly three consecutive failures, zero subsequent launch, and no `owner-ended` classification for failure cases. Include incomplete task environment and null/malformed/unknown-status payloads. | Return live/allowed when the uncertainty budget is exhausted instead of canceling. Complete held drivers and assert the missing exit 7/no-later-launch outcome. |
| G-15 / PC-15: token stays in transport only | `owner_token_is_used_only_in_the_http_header` | Fake handler requires the synthetic token in `X-Antiphon-Task-Token`; a valid live read succeeds. Byte-scan request/resolved manifest, log, reports, state and captured launch argv: sentinel absent everywhere. Error arrangements use the same scan. Nonsecret task identity round-trips. | Add a serialized token property to `RunRequest` populated from the inherited token. The persisted sentinel-absence assertion must fail; use only the synthetic fixture token. |
| G-16 / PC-16: baseline respects canceled ownership | `ownership_loss_prevents_new_baseline_processes` | Arrange a failed row with baseline requested. Cancel before baseline entry, and separately at an acquired baseline lease after a synthetic worktree add. No fetch/build/test/remove process may newly start after owner-ended receipt; existing driver drains, leases release, terminal exit stays 7. Existing baseline workspace is retained and named as residue. Live control still removes its fake workspace. | Bypass baseline's common pre-driver ownership fence, including its former `finally` launch using `CancellationToken.None`. The recorder observes a forbidden post-cancel process. |
| G-17 / PC-17: cancellation cannot start output cleanup | `ownership_loss_keeps_outputs_and_stops_new_cleanup` | Gate immediately before executor output cleanup, deliver terminal owner, then resume. Cleanup entry count = 0, owned output marker and evidence remain, terminal exit = 7. If cleanup already entered, finish the current synchronous operation, recheck before the next; assert no next operation starts. | Omit the cancellation/ownership fence before cleanup operations. The owned marker disappears or the forbidden cleanup entry is recorded. |
| G-18 / PC-18: host estimate controls real deadlines | `windows_estimate_selects_row_and_total_deadlines` | Import the same row through injected Windows/Linux platform choices: base 9 and Windows 27 resolve to estimates 9/27, row deadlines 27/81, total deadlines 30/64 minutes. Blank/absent override falls back to 9. Assert actual executor/scheduler timeout inputs and resolved YAML, not only arithmetic helpers. Explicit row/total overrides 7/11 still win. | Ignore `EstimatedMinutesWindows` during resolution. Windows estimate becomes 9 and deadline assertions fail. |
| G-19 / PC-19: invalid estimates are refused | `invalid_estimates_are_refused_before_start` | Base and nonblank Windows values `0`, `-1`, `abc`, overflow and an integer whose derived arithmetic overflows all return exit 2 naming row/column, with zero start/driver calls. Validate Windows values even on Linux. Nine-column positive-integer input remains accepted. | Remove estimate validation and use the old permissive parse/default path; the invalid-base/override case becomes accepted at the import boundary. |

All cancellation tests use a production owner-cancellation reason distinct from ordinary total/row timeout. Exit 7 is the run-level ownership verdict even if a canceled row would otherwise contribute exit 5 or build-failed 2; persist the reason and never aggregate it back to green. The exact report representation may use an added reason field, but state/report/wait must agree. Do not change existing 2/6/4/5/1/3/0 precedence for unbound runs.

### Proves it works now: S2

Every method below is in **`tests/Antiphon.Tests/Checkpoints/CheckpointImportTests.cs`** and runs in **CP-2**. Keep all eight pre-existing methods, including the fixture amendment described below.

Final table grammar: nine required headers in their existing order; zero or more uniquely named optional `EstimatedMinutesWindows`, `Serial`, `Environment` headers afterward, in any order. Reject unknown/duplicate headers and data/header arity mismatch. `Serial` accepts case-insensitive `true`/`false` or blank (false); `n/a` is not a boolean. `Environment` blank/`n/a` is an empty map; otherwise one backtick-wrapped or plain list of `NAME=value` entries separated by semicolons. Split each entry at its **first** `=`, trim entry/name edges, retain value content including further `=`; an empty value is allowed. Names match `[A-Za-z_][A-Za-z0-9_]*`; reject NUL/newline and duplicate names case-insensitively on both hosts. No shell expansion or quoting language is added. A literal semicolon in a value uses YAML instead. Document this grammar and child-only semantics in the owner doc during S2.

| Guard / PC | Exact method | Arrangement and exact assertion | Production defect that must turn the method red |
|---|---|---|---|
| G-2 / PC-2: table serial reaches scheduling | `serial_import_round_trips_and_excludes_other_rows` | Import serial A plus ordinary B at width two; resolved YAML and reload have A.Serial = true/B.Serial = false. Gate A, advance scheduler to its next selection attempt, assert B entered = 0; release A, both finish and maximum active rows = 1. A separate two-ordinary-row control reaches two entries before release. Serial alone supplies no TUnit environment variable. | Remove importer `Serial` assignment. `A.Serial.ShouldBeTrue()` fails before any race assertion. |
| G-20 / PC-20: environment survives import/YAML | `environment_map_round_trips_without_changing_serial` | `TUNIT_MAX_PARALLEL_TESTS=1;C760_VALUE=a=b;C760_EMPTY=` yields exactly three keys/values in both imported and YAML-reloaded maps; blank/`n/a` empty; row remains Serial=false. A nine-column fixture has empty map/Serial=false. | Drop imported environment assignment; exact dictionary equality fails. |
| G-21 / PC-21: environment is child-local end to end | `environment_reaches_only_its_row_and_its_reruns` | Overlap target A and untouched B with a unique `C760_*` key. Exercise scheduler -> row -> real `ProcessDriver` -> captured child start info; A initial/retry have the value, B and build start info do not, parent lookup unchanged. Repeat for a command row; assert no mutation of the input map. Check existing TUnit parent value by snapshot rather than assuming it is absent. | Replace child `ProcessStartInfo.Environment` assignment with process-global `Environment.SetEnvironmentVariable`; parent/sibling isolation fails. A second local diagnostic removal of child propagation would also fail A's value assertion, but does not replace this required PC. |
| G-22 / PC-22: no permissive booleans | `invalid_serial_is_refused_before_execution` | `yes`, `1`, `n/a` each exit 2 naming `CP-1` and `Serial`, with no manifest/start/driver; `TRUE`, `false`, blank accepted with correct booleans. | Default failed boolean parses to false. The `yes` case returns success instead of 2. |
| G-23 / PC-23: unknown headers cannot silently drop a guard | `unknown_duplicate_and_malformed_columns_are_refused` | Unknown `Serail`, duplicate `Serial`, duplicate `Environment`, short row, extra row cell and a bare pipe within an OR filter each exit 2, identify header/row and offending column/count, and never launch. Malformed-filter diagnostic tells author to escape the pipe. Reordered optional headers and correctly escaped OR filters succeed exactly. | Bypass only unknown-header rejection. The misspelled Serial header is silently accepted, failing its exact exit-2 assertion. Duplicate/arity controls are PC-32/33. |
| G-24 / PC-24: environment key grammar | `invalid_environment_names_and_duplicates_are_refused` | Invalid names `1BAD`, `BAD-NAME`, empty name, missing `=`, NUL/newline, repeated key and case-only duplicate each exit 2 naming `Environment`/row, no start/driver. Run the same boundary matrix through YAML validation as well as Markdown import (missing `=` is Markdown-only). | Bypass shared environment entry/name validation while keeping duplicate rejection. The invalid-name case is accepted at import/validation instead of exit 2, not merely rejected later by ProcessStartInfo. Duplicate control is PC-34. |
| G-25 / PC-25: annotated reuse means the same build | `annotated_reuse_resolves_only_an_earlier_filter_build` | CP-2 Build `CP-1 (-NoBuild)` with same After resolves to CP-1's exact build ID/project/output; one scheduled build, two row invocations with `--no-build`. Forward/missing/command-row reference and `CP-1 (anything-else)` refuse at import with exit 2. Bare CP-1 remains equivalent. | Broaden the exact suffix matcher to accept arbitrary parenthesized prose. The `CP-1 (anything-else)` case succeeds instead of exit 2. Prior-reference control is PC-43. |
| G-26 / PC-26: filter suffix cannot be discarded | `filter_shorthand_and_trailing_environment_prose_are_refused` | `same as CP-1` and a backticked filter followed by `TUNIT_MAX_PARALLEL_TESTS=1` both exit 2 naming Filter and requiring an exact filter/Environment column; no command/driver calls. Give shorthand `Min=n/a` so the old command-Min guard cannot make the test pass accidentally. Ordinary exact command rows remain accepted. | Restore `ExtractPayload` dropping text after a backticked filter. The trailing-environment case succeeds instead of exit 2. Shorthand control is PC-44. |
| G-27 / PC-27: imported reuse stays within its committed slice | `cross_after_reuse_is_refused_without_a_relaxation_flag` | Different-After bare and annotated CP reuse, plus two explicit declarations of the same build across After groups, all exit 2 naming the later row and `After`. Same-After reuse succeeds and resolved YAML does not enable `relaxSharedBuildAfter`. Handwritten YAML cannot re-enable cross-After reuse with that flag. Historical CARD-0723 source table is a negative fixture. | Remove the importer's cross-After rejection (including its call to shared validation if that owns the check). The direct `ImportMarkdown` result for the minimal two-row case becomes success instead of 2. Independent YAML bypass is PC-45. |
| G-28 / PC-28: build identity cannot be silently replaced | `conflicting_projects_cannot_share_an_output_identity` | Same output ID/path declared for different projects in the same After group exits 2 naming the conflicting build/project, zero start. Repeating the identical project/output remains one build; a unique output creates a second build. | Keep the first build solely by output ID, ignoring a conflicting later project (the current deduplication behavior). The conflict is accepted instead of refused. |
| G-29 / PC-29: run uses the import contract | `run_plan_and_import_resolve_the_committed_table_identically` | Through the real CLI command dispatcher with fake launch/wait, import this plan and separately `run --plan` it with each `--after S1`, `S2`, `S3`. Read persisted manifests, normalize only provenance/run paths, and assert equal row specs, CP IDs, filters, floors 17/19/3, build outputs, host estimates and CP-2 Serial/environment; selected request contains exactly its one CP. Fake driver executes selected path to exit 0. No recursive real test invocation. | In the run selection path clear optional row settings after import while leaving `import` intact. The CP-2 persisted equality/Serial/environment assertion fails. |

The additional controls below split independently bypassable guards within these matrices. Code should centralize identical Markdown/YAML environment validation so both paths use the same validation logic. Every malformed input must fail at import/validation, before creating a child.

### Proves it works now: S3

Every method below is in **`tests/Antiphon.Tests/Checkpoints/CheckpointExecutorLogTests.cs`** and runs in **CP-3**.

| Guard / PC | Exact method | Arrangement and exact assertion | Production defect that must turn the method red |
|---|---|---|---|
| G-3 / PC-3: one serialized executor log sink | `concurrent_callbacks_append_each_line_once_without_overlap` | Use deterministic append A/B setup above, then actual executor callback adapter with parallel fake row/slot producers, without `--serial`. `OverlapCount == 0`, maximum sink writes = 1, exact multiset of unique payload lines matches input (each count 1, whole newline-delimited records), exit = 0 (specifically not 6). Allow timestamp prefix; assert full payload, not substring counts. Build/console log paths stay distinct per build/row. | Bypass the writer gate around the shared append handle, leaving the test sink unchanged. A/B synchronously overlap; the test sees overlap count 1 and a failed append/result instead of the expected successful outcome. The injected I/O failure itself is not the verdict: the named outcome assertion must fail. |
| G-30 / PC-30: logging failure is a terminal error | `log_io_failure_publishes_a_terminal_error_without_recursive_logging` | Inject open, append and flush failure in separate arrangements using one sentinel IOException. Executor returns 6, terminal state/report name executor.log and the sentinel error, fake children/leases drained; fallback stderr/state/report is independent of broken log. Failed sink is attempted once at its failure boundary and never used to log that same failure. Also inject error while handling a scheduler crash. | Replace log-failure handling with swallow-and-continue/green. Assert return/state/report exit 6 fails. Independently restoring the old catch's call to broken `Note` must be caught by the bounded write-attempt assertion; a recursive stack overflow/harness timeout does not count as red evidence. |
| G-31 / PC-31: all callbacks finish before terminal receipt | `terminal_report_waits_for_all_callbacks_and_log_flush` | Hold the last lease-release callback in append and then block flush; at each acknowledged boundary no terminal publish and execute task incomplete. Release them: exact final release/done lines visible when report/state first become terminal, flush-before-dispose event order, dispose exactly once, and wait returns the same exit. Cover success, ownership exit 7 and crash exit 6. | Publish terminal success without awaiting the pending append callback. Capture that publication synchronously and assert the required last line is missing; then release gates for clean teardown. Independent flush control is PC-47. |

### Independently bypassable guard controls

These are additional PCs against the **same methods and assertions**, not additional TUnit executions. File mapping is literal: class `CheckpointTaskOwnershipTests` -> `tests/Antiphon.Tests/Checkpoints/CheckpointTaskOwnershipTests.cs` (CP-1), `CheckpointImportTests` -> `tests/Antiphon.Tests/Checkpoints/CheckpointImportTests.cs` (CP-2), `CheckpointExecutorLogTests` -> `tests/Antiphon.Tests/Checkpoints/CheckpointExecutorLogTests.cs` (CP-3).

| Guard / PC | Exact class.method | Compiling production defect and decisive red assertion | Checkpoint |
|---|---|---|---|
| G-32 / PC-32: optional headers are unique | `CheckpointImportTests.unknown_duplicate_and_malformed_columns_are_refused` | Remove duplicate-header rejection only; keep first or last value. Duplicate `Serial` input returns 0 instead of 2. | CP-2 |
| G-33 / PC-33: row arity is exact | `CheckpointImportTests.unknown_duplicate_and_malformed_columns_are_refused` | Truncate surplus cells to header length instead of rejecting. Add one harmless final surplus cell to an otherwise valid row; result is 0 instead of 2. The bare-OR-pipe case independently pins the actionable diagnostic. | CP-2 |
| G-34 / PC-34: environment duplicates cannot override silently | `CheckpointImportTests.invalid_environment_names_and_duplicates_are_refused` | Change duplicate insertion to last-value-wins; repeated identical-name input returns 0 instead of 2. Also run the case-only variant with a case-sensitive duplicate comparer; it must independently fail the exit-2 assertion. | CP-2 |
| G-35 / PC-35: child environment actually applied | `CheckpointImportTests.environment_reaches_only_its_row_and_its_reruns` | Remove the environment overlay on child start info. A's `C760_*` value is absent instead of the exact supplied value. Keep PC-21's global-isolation mutation separate. | CP-2 |
| G-36 / PC-36: error reporting cannot recurse into failed log | `CheckpointExecutorLogTests.log_io_failure_publishes_a_terminal_error_without_recursive_logging` | In the log-failure handler call the same broken Note sink again. The sink records all attempts and faults only the first so the mutant terminates; attempt count is 2 instead of 1. No stack overflow or timeout is accepted as evidence. | CP-3 |
| G-37 / PC-37: owner read carries task authentication | `CheckpointTaskOwnershipTests.owner_token_is_used_only_in_the_http_header` | Remove the task-token header assignment. The fake handler returns 403; the expected successful owner read and exact received-header assertion fail, independently of token-persistence PC-15. | CP-1 |
| G-38 / PC-38: successful reads reset uncertainty budget | `CheckpointTaskOwnershipTests.transient_owner_errors_recover_and_persistent_errors_stop` | Remove failure-counter reset on successful read. Feed failure/failure/live/failure/live, advancing each read gate. Fixed code permits the final live launch; mutant prematurely ends at the third cumulative failure with exit 7 instead of 0. | CP-1 |
| G-39 / PC-39: an unresolved owner read admits no new launch | `CheckpointTaskOwnershipTests.transient_owner_errors_recover_and_persistent_errors_stop` | Treat an in-progress/failed admission read as allowed using cached live state. While its recovery gate is held, recorder observes one driver entry instead of zero. | CP-1 |
| G-40 / PC-40: cancellation targets the entire local tree | `CheckpointTaskOwnershipTests.process_driver_cancellation_kills_each_local_tree` | Change local-handle `Kill(entireProcessTree: true)` to false. Recorded tree-kill flag is false instead of true even though root kill counts still pass. | CP-1 |
| G-41 / PC-41: lease disposal completes before terminal | `CheckpointTaskOwnershipTests.terminal_publication_waits_for_driver_exit_and_lease_disposal` | Fire-and-forget `DisposeAsync` while still awaiting the drivers. At the release-blocked boundary terminal state appears with one live lease instead of remaining pending; drain PC-10 alone need not catch this. | CP-1 |
| G-42 / PC-42: explicit deadline overrides remain authoritative | `CheckpointTaskOwnershipTests.windows_estimate_selects_row_and_total_deadlines` | Select derived Windows limits ahead of explicit override values. Actual scheduler inputs become 81/64 instead of the required 7/11. | CP-1 |
| G-43 / PC-43: reuse reference must identify a prior filter build | `CheckpointImportTests.annotated_reuse_resolves_only_an_earlier_filter_build` | Replace unknown/prior-reference refusal with first-build fallback. Use one valid earlier build and a later reference to nonexistent CP-99; import succeeds instead of exit 2. Forward and command-row cases retain the same boundary assertions. | CP-2 |
| G-44 / PC-44: shorthand is never a shell command | `CheckpointImportTests.filter_shorthand_and_trailing_environment_prose_are_refused` | Remove only `same as CP-n` refusal. With Min=n/a the importer returns a command checkpoint instead of exit 2; the test never executes that shell text. | CP-2 |
| G-45 / PC-45: YAML cannot opt out of same-After reuse | `CheckpointImportTests.cross_after_reuse_is_refused_without_a_relaxation_flag` | Restore a writable `RelaxSharedBuildAfter` property and the validator bypass it enables. Load the direct YAML fixture with that property true; validation succeeds instead of exit 2. | CP-2 |
| G-46 / PC-46: each owner request has a bounded deadline | `CheckpointTaskOwnershipTests.transient_owner_errors_recover_and_persistent_errors_stop` | Omit the per-request deadline/linked timeout token. Advance two seconds while handler response remains held: required request-cancellation receipt is absent. The handler completes explicitly after this assertion so the test cannot hang. | CP-1 |
| G-47 / PC-47: persistence waits for the final flush | `CheckpointExecutorLogTests.terminal_report_waits_for_all_callbacks_and_log_flush` | Await append callbacks but omit awaiting flush before terminal publication. A buffering sink has accepted the final line yet its persisted view lacks it at publish; exact persisted-line assertion fails. This isolates flush from callback-drain PC-31. | CP-3 |

### Existing tests affected and assertion-preserving amendments

The guaranteed existing regression is **`CheckpointImportTests.imports_the_card_0723_table`**: the current source table's CP-2 reuses CP-1 across `S1-S2` versus `S3-S4`; later rows also share that build across groups. Its CP-6 filter also contains trailing prose. Strict import makes its `ExitCode.ShouldBe(0)` fail before its nine-row/YAML assertions. In **S2**, keep the test method and every assertion, but read a new test fixture `tests/Antiphon.Tests/Checkpoints/Fixtures/plan-table-0723-same-after.md`: a copy of those nine rows with every After set to the same `S1-S7` group and CP-6's redundant trailing `(-MinExecuted 1000)` removed. Preserve filters, Min values, row IDs, builds, commands and every remaining cell. Do not edit the historical plan. G-27's method separately imports the unchanged historical table and expects the precise cross-After rejection. This preserves success coverage and adds the missing failure coverage rather than changing success to a permissive result.

The other seven existing `CheckpointImportTests` remain unchanged and execute in CP-2:

- `imports_the_card_0688_table`: exact 13 rows, **four** build IDs (`bin-c688a/b/c/e`), command and roster assertions.
- `unescapes_pipes_in_filters`: contains `|`, contains no `\|` after import.
- `maps_cp_reuse_to_the_same_build`: CP-7 -> `bin-c688a`, CP-10 shares CP-9; both reuse groups are already valid.
- `command_rows_become_command_checkpoints`: CP-11/13 command semantics and illustrative client command.
- `derives_roster_tokens_from_the_filter`: exact roster and category-empty assertions.
- `refuses_the_legacy_eight_column_table`: exit 2 and `CARD-0617`/`EstimatedMinutes` diagnostics.
- `warns_when_estimate_exceeds_row_timeout`: estimate 20 still resolves to timeout 60 and retains the existing warning containing 45; this work does not silently clamp it.

No other inspected existing test is expected to turn red if compatibility is kept: retain unbound default overloads for direct `CreateRun`/`Finish` calls and existing `IDriver`/request call sites. `CheckpointAppTests.phase_done_is_published_before_cleanup_and_cleanup_failures_are_kept` remains valid for the **unbound Finish helper**. Owner-bound execute must finish or skip its fenced cleanup, and S3 must drain the log, before publishing terminal state; it cannot publish green first and then discover owner loss during cleanup. It can use a separate completion path or call Finish with cleanup already handled. `WaitCommandTests.executor_finish_leaves_the_image_and_wait_deletes_it_once_the_pid_is_gone` retains all tool-copy custody assertions. `TimeoutTests.row_deadline_kills_the_driver_tree_and_marks_timeout` retains kill count 1 and exit 5; `total_deadline_kills_running_and_skips_queued` retains exactly `skipped`/`timeout`. All seven `ExitCodeTests` argument cases remain unchanged. Baseline normal success/build-failure removal assertions remain unchanged; only owner-canceled cleanup is refused. The native Linux `DetachedLauncherTests` is unchanged: no launcher-native behavior changes are part of this design.

These compatibility bodies were inspected, not executed in TestDesign. They are not silently additional checkpoint runs: the three exact class filters above are the closed list. New ownership tests directly cover the changed cancellation/report/timeout behavior, the import test drives serial/parallel scheduling, and S3 drives log completion. If Code must change an existing test outside these three classes, name the discovered incompatibility and obtain coverage by explicitly amending this plan's closed list before that additional run; never weaken an old assertion or claim a class ran because it was read.

### Guard inventory and positive-control execution

The guard/PC columns above are the inventory: **G-1 through G-47**, each mapped to the same-numbered **PC-1 through PC-47** exactly once. Guard counts: S1 = 24, S2 = 18, S3 = 5; **guards=47, mapped=47, missing=0, duplicate PC mappings=0**. These use 31 new methods; CP-2 also retains eight existing methods. All controls have an exact file, method, assertion and compiling production defect. PC-34 has two independently executed variants (overwrite and case comparison), making **48 red/restore/green cycles**. The earlier PC-1/2/3 sketches are refined by these rows; the remaining controls cover the other safety boundaries. Mutation still performs repository-required discovery for implementation guards beyond this inventory.

Mutation runs only after ordinary Code, separate Review and confirmed land on the commissioned SourceLanding snapshot. For each row, exact method filter is `/*/Antiphon.Tests.Checkpoints/<class>/<method>` using that row's class/method. Green baseline -> compiling defect -> intended assertion-red -> restore -> fresh rebuild -> green, with nonzero TRX counts and assertion recorded for each control. Run shared-file/guard controls separately. Do not use an entire class filter for a PC, treat a compile error as red, mutate the fake's expected answer, or run an unawaited task. The cancellation and log controls use their gates on Windows and Linux alike; no host-specific skip is acceptable. Code's report labels these PCs **designed/pending**, not passed.

### Runnable checkpoint procedure and cost

There is exactly one `### Checkpoints` table in this artifact. Do not append a second executable table: `ExtractSection` consumes the first, which would make a later correction ineffective. There is no hand-edited YAML detour.

**S1 bootstrap:** the baseline importer already accepts extra columns. Implement Windows estimate support in S1; Serial/Environment support and strict optional-header validation arrive together in S2. S1's selected CP-1 uses false/empty settings, so it does not depend on S2 behavior; unselected CP-2 is never executed in S1. After S2, the full table imports strictly, `n/a` environment means empty, and CP-2 proves direct `run --plan` parity. Do not introduce strict rejection of the known S2 columns in S1 and then ask the caller to edit the table.

Bootstrap the tool itself under a build-slot lease, separately from the leases the detached executor acquires for checkpoint drivers. Use the same isolated tool output after each committed slice (the Tests builds already isolate theirs):

```powershell
pwsh -NoProfile -File scripts/build-slot.ps1 -Label checkpoint-tool-bootstrap -- dotnet build tools/Antiphon.Checkpoints --property:OutputPath=bin-c759-tool/ --nologo
dotnet run --project tools/Antiphon.Checkpoints --no-build --property:OutputPath=bin-c759-tool/ -- run --plan docs/superpowers/plans/2026-09-26-checkpoint-tool-hardening-plan.md --after S1 --max-wait 570s
dotnet run --project tools/Antiphon.Checkpoints --no-build --property:OutputPath=bin-c759-tool/ -- wait --run <returned-run-id> --max-wait 570s
```

Repeat bootstrap/run after S2 and S3 commits with the matching `--after`; repeat **wait**, not run, for exit 75. The bootstrap is an explicit supporting build outside the table because the tool being changed must exist before it can execute the table; report its three invocations separately with that reason. It does not build `Antiphon.Tests` or replace any CP. The normal `run` owns its inner leases; do not hold an outer lease across the detached wait and thereby block its drivers. Slot timeout 4 is reported, never bypassed. Use the returned run ID, not a possibly unrelated latest run. Retain task-owned output inventory/evidence. No native/E2E/production-server work is implied by these commands.

Estimated costs, not measurements:

| Work | Linux minutes | Windows minutes |
|---|---:|---:|
| Ordinary CP-1 exact class filter, Min 17, isolated build included | 8 | 12 |
| Ordinary CP-2 exact class filter, Min 19, isolated build included | 6 | 8 |
| Ordinary CP-3 exact class filter, Min 3, isolated build included | 6 | 8 |
| **Ordinary Code/Review checkpoint floor** | **20** | **28** |
| Three leased tool bootstrap builds, total allowance | 2 | 3 |
| Code authoring allowance including seams, fixtures and owner-doc update | 240 | 240 |
| **Code wall budget before slot waits** | **262** | **271** |
| Mutation setup plus fresh isolated baseline build | 5 | 8 |
| 47 method-scoped controls / 48 cycles, each red/rebuild/restore/fresh-build/green allowance | 192 (48 x 4) | 288 (48 x 6) |
| **Post-land Mutation floor** | **197** | **296** |
| **Verification total: bootstrap + ordinary + Mutation** | **219** | **327** |

Each additional independently discovered PC adds an estimated 4 Linux / 6 Windows minutes; count it explicitly. Review separately repeats ordinary V/R as its brief requires and is not hidden inside Code's authoring estimate. Savings booked for this hardening work: **0 minutes**; no incident reproduction or benchmark supports a numeric runtime saving. Determinism and preventing post-settlement launches are acceptance criteria, not timing claims.

### Out of scope and handoff audit

No production/test/fixture code is written at TestDesign; no build, test, PC or live API call is claimed. No separate YAML-only solution, global test environment mutation, production-runner test traffic, silent logging-error suppression, changed native detach flags, durable executor restart protocol, unrelated watchdog work, or rewrite of historical plan tables belongs in these slices. Process/log external-I/O seams above must be implemented in Code so the assertions reach production behavior. Bodies read, exact checkpoint roster accounted for, 47 guard mappings complete, deterministic red controls specified, numeric costs supplied. Next stage: **Code**.
