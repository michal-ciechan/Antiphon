# CARD-0432: distiller availability, ingestion and deadlines

Plan, 2026-09-07. Based on investigation commit `6a3d5865` and the code at that commit. Verification design appended below; current next stage: Code. This deliverable changes documentation only.

Integration note: `cda42403` landed the independent distiller contract-v2 character-budget change while this plan was being finalized. This plan is rebased on it. That change touches the bundle, contract version and provisioner tests, not the timeout/ingestion paths below. Preserve it and partition subsequent measurements by actual `BundleStamp`; the investigation's contract-v1 rejection rate is historical, not a prediction for v2.

## Outcome and scope

Keep `OutputDistillerMode=Shadow` and `OutputDistillerWaitSeconds=45`. Fast-degrade a known model hold, stop slow runtime side effects from blocking transcript ingestion, and carry one absolute deadline from completion-note admission through application. Preserve the raw report and standing-seat ownership on every degradation. Make late task outcome and cost visible without applying a late answer.

The investigation is [2026-09-07-card-0432-distiller-timeouts.md](../../investigations/2026-09-07-card-0432-distiller-timeouts.md). Its 140-row sample has 38 timeouts: 19 never dispatched under a model hold, 17 eventually succeeded, and two failed delivery/correlation. Of the 17, twelve produced their marked report within 45 seconds of task creation; five incurred substantial queue delay before generation. Across 90 eventual successes, prompt-to-report was median 16.752 seconds, maximum 30.634 seconds. Report-to-`CompletedAt` reached 80.084 seconds. These are producer timestamps compared with settlement-entry timestamps, not measurements of database commit latency.

Compression-gate tuning is CARD-0430. Do not edit the distiller bundle, gates, provider choice, quotas, seat count, or enable Apply. Apply behavior must nevertheless be corrected and tested using isolated fixtures. No production restarts, model turns, broker sends, or live configuration changes are part of Code verification.

## Ground truth

| Assumption or desired behavior | Code/evidence at the planning baseline | Consequence |
|---|---|---|
| A timeout means slow Haiku generation. | `SpecialistTaskRunner.WaitForRunAsync` polls durable task state. Availability, dispatch, ingestion and settlement all precede success. | Keep separate reasons and phase clocks; a timeout-only increase is rejected. |
| Unavailable models are detected before specialist work is created. | `AgentTaskDispatcher` checks `ModelAvailability`; `SpecialistTaskRunner` does not. `DiagnoseService` already uses `IModelAvailability` for preflight. | Use the existing hold authority before creating Distill work and recheck while queued. |
| There is a single 45-second clock. | `AgentTaskReplyService.DeliverToParentAsync` sets Apply `HoldUntil` before queue enqueue; the specialist deadline starts after provisioning and task creation. | The request must carry its original absolute deadline; no downstream clock reset. |
| The current backlog setting bounds pending requests. | `OutputDistillationQueue` is unbounded and serial. `OutputDistillerMaxBacklog=3` counts unfinished Distill task rows, not channel items. | Bound request admission separately from unfinished model work. |
| `WaitMs` includes the whole request. | It excludes channel residence and includes specialist provisioning and timeout incident/alert handling. | Preserve the legacy field and add explicit phase timestamps/durations. |
| An expired but undelivered note cannot be replaced. | `TryApplyAsync` checks Pending and zero attempts, but no deadline; its read/save is outside the queue's session lock. | Deadline and delivery-claim checks must occur together under the queue's existing lock. |
| The fleet event pump only ingests events. | `SessionRunnerEventPump` awaits `ObserveTranscriptAsync`, which awaits channel/review/task routing, compaction and queue flushing. Startup/reconnect `SyncTranscriptAsync` also performs side effects serially across sessions. | Slow work must leave both live ingestion and reconnect catch-up paths. |
| Only the final queue flush can block ingestion. | `FlushQueueOnIdleAsync` calls task settlement first; `DeliverToParentAsync` awaits `SessionMessageQueueService.EnqueueAsync`, whose default idle path calls delivery verification inline. `ObserveTranscriptAsync` also awaits SignalR before persistence. | Instrument each awaited child, including nested parent-note delivery and publication; do not fix only the obvious final flush. |
| Persisted transcript order makes arbitrary background execution safe. | Persistence rebases sequences and deduplicates `(uuid, kind)`; current side effects mostly select the latest turn. Catch-up, streamed replay, split TurnEnd/text and answered-Blocked watermarks have specific guards. | Separate ingestion from effects with explicit sequence identity, ownership and replay recovery, not untracked `Task.Run`. |
| Timed-out work costs zero. | `SpecialistTaskRunner.Finish` returns zero/null on timeout, but dispatched tasks can later settle with cost. Ledger stats sum the captured zero. | Preserve the original timeout verdict while resolving eventual status/cost from the linked task. |

Relevant owners: [project-context](../../project-context.md), [orchestration-loop](../../orchestration-loop.md), [session-runtime-invariants](../../session-runtime-invariants.md), and [testing-and-build](../../testing-and-build.md). Code must reread the owners for any additional area it changes.

## Decisions

### D-1. Keep the current operating mode and budget

Shadow and 45 seconds remain the defaults and live settings throughout this work. The sample supports fixing wasted waiting before buying more of it. A larger serial wait can worsen burst latency; all 73 promptly completed attempts were rejected by compression gates anyway. A 75-90 second Shadow trial is a possible later operator decision only after phase measurements, not a change in this card.

### D-2. A model hold is an immediate, distinct fallback

Add a held result to the specialist runner and append `DegradedHeld` to `DistillationOutcome` without renumbering existing values. Record a structured reason, canonical kind/alias and observation time. A known hold creates no Distill task, invokes no model and leaves/releases the raw note immediately; it must not raise a fresh generic unavailable alert for every report. Existing model-hold attention remains authoritative.

Resolve the alias with the same rule as `AgentTaskDispatcher.ResolveDispatchAliasAsync`: normalized pinned `Agent.ModelId`, otherwise the task kind's Low-tier alias. Extract/reuse that rule rather than hard-code `haiku` or match log strings. For a missing seat use the spec's intended kind/tier for a read-only preflight before `EnsureAsync`; check again after Ensure in case the seat identity changed. An unknown/error availability read is `DegradedUnavailable` with an availability-check reason, not proof of a hold or permission to bypass one. Existing length/role/disabled eligibility semantics remain first.

Use an explicit opt-in execution policy for Distill when extending the shared runner. Check and Diagnose retain their existing caller contracts unless a separately justified change is necessary. The dispatcher still checks holds at dispatch. While a Distill task remains Queued, the caller rechecks availability at the existing two-second polling cadence (each read is bounded by remaining time). A newly held queued task is conditionally canceled and returns `DegradedHeld`. A task that won the dispatch race is left standing and awaited only to the original deadline; do not kill/requeue it or relabel an active run as never-dispatched.

### D-3. One deadline, beginning before note enqueue

Capture `RequestedAt` and `DeadlineAt = RequestedAt + max(1, OutputDistillerWaitSeconds)` once immediately before the completion-note enqueue. Extend `DistillRequest` to carry those values and the mode snapshot with source/queued-note IDs. Apply `HoldUntil` is exactly that deadline. Shadow carries the same observation deadline with no hold. A mode refresh must not turn an already admitted Shadow request into Apply.

Carry that deadline through the worker, provisioning, task creation, queued dispatch and application. Persist a nullable deadline on the Distill `AgentTask` before dispatch can see it, so a server restart or caller cleanup failure cannot release expired queued work into the model. Existing non-Distill tasks and historical null-deadline rows retain current semantics. Add a structured expiry phase (`request-queue`, `provision`, `dispatch-queue`, `await-settlement`, `apply`) and append `DegradedExpired` for work rejected before execution; an in-flight run whose wait expires remains `DegradedTimeout`.

Check remaining time before every expensive stage and after awaited results, including a successful final poll: success observed after the deadline does not become an in-budget result. Clamp poll delays and all request-path I/O to the remaining budget. A producer report timestamp inside the window is diagnostic evidence; it does not authorize application after the note deadline. Do not clamp an already-expired request back up to another one-second wait.

The dispatcher must reject expired deadline-bearing Queued tasks during candidate selection and revalidate under its dispatch claim immediately before transition/delivery. The queued brief must also carry expiry into the terminal queue: a Dispatched row can still have an untyped Pending brief. Before its first attempt, cancel an expired, never-typed brief through the established queue lock and reconcile the task without typing. If typing has begun or a prompt is confirmed, retain the running task/standing owner and existing confirmation recovery. This closes the gap between a task's `DispatchedAt` and actual input.

Timeout is a decision at the deadline, not a promise that an unavailable database commits cleanup at that instant. Hold expiry independently makes the raw note eligible. Give best-effort cancellation/hold release/ledger finalization a separate **two-second total cleanup budget**, linked to host shutdown but not to the expired execution token. Log unfinished cleanup for the existing/added recovery sweep. Incident/alert emission must run outside the serial specialist wait and must not extend this cleanup allowance. Never reuse a scoped DbContext concurrently or abandon a still-running I/O operation against a disposed scope.

### D-4. Bounded, nonblocking request admission

Add `OutputDistillerQueueCapacity`, default **3 waiting requests**, distinct from the existing maximum of 3 unfinished Distill task rows. Keep one distiller reader. Use bounded-channel `TryWrite` with rejection when full; do not use a drop mode that reports success while discarding another request. Reserve no unbounded overflow list.

Record `DegradedBusy` with `queue-full` when admission fails, clear a previously established hold, and keep the raw note eligible. Ensure the producer handles a missing/closed worker as well as a full queue. Its fallback bookkeeping may await bounded database work, never model execution or external alert delivery. A request expired on dequeue records `DegradedExpired` without provisioning/creating work. Existing short/long skips should remain identifiable rather than be mislabeled model failures.

For completion notes, call the existing queue enqueue with inline idle delivery disabled, persist the note, and request an owned asynchronous flush. Admit distillation before a slow parent delivery can consume its budget. Both Shadow and ordinary raw completions must still get an idle flush without waiting for a future TurnEnd. Duplicate-note suppression that returns no new note ID must not enqueue duplicate distillation work. A process restart may abandon this optional in-memory optimization; the persisted raw note and its finite hold remain the fallback, and an expired task cannot launch afterward.

### D-5. Persist transcripts independently of ordered side effects

The default design is a durable, per-session sequence of runtime-action intents plus a bounded hosted worker. The live stream awaits transcript persistence and intent recording, then continues. Channel/review/task routing, compaction recovery, queue delivery, git/deliverable work and external publication must not be awaited in that fleet receive loop. Reconnect catches up transcript storage before resuming live ingestion without waiting for those effects. Instrumentation in S0 must confirm the exact blocked child and validate this boundary, not claim the historical 80-second event has been fully reconstructed.

Persist meaningful action identity using session ID, stored sequence, trigger kind and action phase in the same transaction as new transcript rows. A proposed `SessionRuntimeAction` entity has a unique identity on those keys, pending/completed state and retry metadata; names may follow nearby conventions. This is durable intent, not a new store of report bodies. A channel of IDs only wakes the worker; a startup/periodic database scan recovers pending actions after full channels, crashes or missed wakeups. Do not backfill all historical boundaries as new sends: initial migration/recovery targets currently owed task/queue/rules work, using existing durable consume guards.

Serialize transcript persistence per session across stream and pull paths, including the unseen-boundary decision and sequence allocation. That lock covers storage only. It is never held while executing effects, sending input or waiting for confirmation. `CatchUpTranscriptAsync` remains a fetch/persist-only operation safe under the queue lock; it must never await the side-effect worker. Cancellation must reach persistence, which currently has no token argument.

Workers have one owner per session, independent DI scopes and bounded global concurrency (initial default **4**); no fire-and-forget tasks or lock held across sessions. Bound external waits at their I/O seam and record queue age/saturation. A blocked delivery must not consume the transcript-ingestion lane, including that same session's confirming UserPrompt. Do not cancel a partially submitted delivery just to meet a distiller timer; its existing verification/parking/late-confirm protocol owns its outcome. If all workers are occupied, retain durable pending work and expose age, rather than lose triggers or spawn unbounded workers.

Preserve the current logical effect order: channel/review routing, task settlement, then next-message delivery; manual compaction is recovery followed by narrow idle flush, never report settlement. Preserve the second channel dispatch after late-confirm promotion. Carry the triggering stored sequence/turn boundary into extraction so processing an older job cannot select a newer turn merely because ingestion ran ahead. AssistantText after an early TurnEnd must trigger a later routing/settlement pass; `(uuid, kind)`/ApiCallId replay dedup, canceled/error turns, answered-Blocked watermarks and Grok rules barriers remain authoritative.

Before a delivery phase, re-read current idle/blocked/rules state and defer while earlier owed routing phases remain. A backlog of several already-ingested turns must be routed before another prompt is injected; do not use a historical idle snapshot to type now. Reuse existing durable task settlement and channel-send claims to avoid duplicates on retry. Record completion per phase so retrying a failed flush does not repeat successful routing or finished notifications. Explicitly test commit-before-wakeup, send/settle-before-action-ack, and worker restart; an in-memory dedup set is insufficient.

This is the largest-risk slice and requires separate TestDesign and Review. If S0 reveals that a narrower owned delivery scheduler removes all measured inline waits while preserving these contracts, Code may propose that smaller design in the plan with evidence before implementing it. It may not replace D-5 with arbitrary concurrent calls to today's latest-turn methods.

### D-6. Application shares the delivery lock

Move body replacement into a narrow method on `SessionMessageQueueService` using its existing per-session lock. Lock acquisition consumes remaining request time. Within a fresh scoped read, require matching note/source/digest, Pending, zero attempts, request mode Apply, unexpired original deadline and no full-report poll suppression. Re-read suppression under the same protection used by polled-note shrinking. Keep `NoteHeader`, raw `ContentDigest`, source identity, header handoff and full-report pointer unchanged.

Replace body and release the hold in one guarded save. A final time check precedes the update; a database statement deadline must prevent a delayed write from being treated as timely. The concurrency protection must be shared with claim-for-delivery, not a distiller-only semaphore. Already Sent/Canceled/attempted or explicitly SendNow-delivered notes are immutable to the distiller. A valid result that loses this race records `AppliedLate` with a reason and makes no replacement or second note. Expired results likewise never replace a Pending raw note. In Shadow, successful in-budget work can still stamp `DistilledResult` and gate outcomes, but no note is held or replaced.

### D-7. Account for late work without revising the note decision

Keep each ledger decision (`Outcome`, mode, captured wait/cost) as recorded. Extend list/stats projections with eventual linked task status, completion time, eventual cost and a cost-known flag, joined by `DistillTaskId`. This is read-side reconciliation against the authoritative task row: it handles settlement before or after the ledger write and survives process restart without callbacks or a second model request.

Add an effective total that counts a distinct linked run once, using its settled cost when available and the captured cost otherwise; show unresolved runs separately instead of presenting unknown cost as verified zero. Retain the old captured-cost total explicitly for compatibility and comparison. Apply the requested ledger time window before resolving later task outcomes, so a run settling tomorrow still belongs to yesterday's request cohort. Avoid changing Feedback/FullReadAt or original timeout classification. No late result updates the note or schedules another completion. Historical missing tasks remain unknown, not reconstructed from guessed prices. Existing task costs may be estimates; these changes do not improve the pricing model itself.

### D-8. Phase evidence is part of the fix

Use `TimeProvider` for deadlines/testable timestamps and monotonic elapsed measurements for individual operations. Preserve producer timestamps and transcript `CreatedAt`; the latter is assigned before save and must not be called a commit timestamp. Add structured logs/spans for fleet event receipt, persistence start/end, action enqueue/start/end, matched-report observation, settlement entry and successful settlement save. Include session/source/run/note IDs and stored sequence, not prompt text or secrets. Time each dispatcher sweep and candidate wait separately from its nominal five-second poll.

Add nullable phase fields to the distillation ledger/DTO: requested/deadline/dequeued/run-created/decision times, queue wait, specialist wait, cleanup duration, reason/expiry phase and availability alias. Keep legacy `WaitMs` semantics documented; absent historical data stays null. Diagnostics must have no external blocking sink on ingestion and must not change delivery or settlement if logging fails.

## Implementation slices

### S0. Instrument and reproduce the actual blocking call

Files: `server/Infrastructure/Agents/SessionRunner/SessionRunnerEventPump.cs`, `server/Application/Services/AgentSessionRuntime.cs`, `AgentTaskReplyService.cs`, `SessionMessageQueueService.cs`, `AgentTaskDispatcher.cs`, and `SpecialistTaskRunner.cs`.

Add the D-8 phase observations before moving awaits. Use the existing fake runner and test database: hold session A's delivery-confirmation await open, stream B's exact task UserPrompt, marked AssistantText and TurnEnd, and observe receipt/persistence/settlement. Exercise A via both direct turn-end flushing and settlement's parent-note enqueue. Also block SignalR publication and a routing dependency independently to identify their contribution. Repeat with reconnect backfill. No live model or live broker.

Exit evidence: an operation-named trace proving where B stops while A is gated, plus an inventory of every slow awaited child in live/catch-up/exit/status event handling. Write the evidence and chosen boundary into this plan. Trace a representative synthetic outlier from producer through durable settlement; distinguish ingress delay from post-entry settlement work. If the assumed block does not reproduce, complete availability/deadline slices but report ingestion as unfinished and return for investigation; instrumentation alone is not the ingestion fix.

### S1. Contract, deadline and phase schema

Files: `OutputDistillationQueue.cs`, `OutputDistillationService.cs`, `SpecialistTaskRunner.cs`, `server/Application/Settings/DelegationSettings.cs`, `server/Domain/Entities/AgentTask.cs`, `SessionQueuedMessage.cs` if expiry metadata is required, `OutputDistillationRecord.cs`, `server/Domain/Enums/OutputDistillationEnums.cs`, `server/Application/Dtos/OutputDistillationDtos.cs`, EF model configuration and CLI-generated migration/snapshot, `server/Program.cs`.

Add the carried request contract, nullable persisted task deadline, append-only enum values/reasons, opt-in runner policy and nullable timing fields. Do not use the mutable hold as the only source of the original deadline: releasing it must not erase expiry evidence. Preserve old callers through an explicit compatibility path. Test existing null fields/old enums and mode snapshots before activating behavior.

### S2. Held-model and queue/deadline fallback

Files: `OutputDistillationService.cs`, `OutputDistillationQueue.cs`, `OutputDistillationHostedService.cs`, `SpecialistTaskRunner.cs`, `AgentTaskDispatcher.cs`, `AgentTaskReplyService.cs`, shared alias resolver adjacent to `ModelAvailability`, and `SessionMessageQueueService.cs`.

Implement D-2/D-3/D-4, including pre-Ensure checks, queued rechecks, bounded admission, atomic dispatch-expiry guard and final pre-type expiry guard. Give request paths a remaining-budget token and cleanup its separate bound. Route alert work outside the serial wait using owned, bounded processing. Do not hide shutdown cancellation in a generic Timeout result. Make raw fallback eligibility independent of successfully writing the diagnostic ledger.

### S3. Remove slow effects from the fleet receive/catch-up loops

Files: `SessionRunnerEventPump.cs`, `AgentSessionRuntime.cs`, new concrete `SessionRuntimeAction` domain entity and application coordinator, new hosted worker under `server/Infrastructure`, EF configuration/CLI migration, `Program.cs`; sequence-bound extraction changes in `ChannelReplyDispatcher.cs`, `ReviewReplyDispatcher.cs`, `AgentTaskReplyService.cs` as required; queue/compaction/rules call sites identified by S0.

Implement D-5 using durable intent and bounded owned workers. Keep external I/O implementation in Infrastructure and domain records dependency-free. Do not use a shared DbContext across workers. Re-run the S0 blocked cases: B's transcript must persist while A remains blocked, and B's task must settle within its remaining deadline when worker capacity is available. Validate same-session confirmation progress, several buffered turns, replay and reconnect, not just two independent direct method calls. Audit all fleet event branches so exit, status, fault or publication handling cannot retain an unbounded inline wait.

### S4. Atomic application and eventual-cost projections

Files: `OutputDistillationService.cs`, `SessionMessageQueueService.cs`, `OutputDistillationDtos.cs`, `server/Api/Endpoints/DistillationEndpoints.cs` only if projection wiring needs adjustment, and current distillation API client/types/stat display/script consumers discovered by `rg`.

Implement D-6/D-7. Keep current endpoint routes, outcome names and captured values backward compatible; add fields for eventual/effective accounting. No dashboard redesign. Ensure projection queries avoid N+1 reads and count distinct runs. Update `docs/orchestration-loop.md` section 10 with actual deployed semantics and `docs/session-runtime-invariants.md` with the new storage/effect ordering contract after implementation; do not present this plan as already shipped.

### S5. Verify, review and measure in Shadow

TestDesign supplies executable V/R/PC cases before Code. Code runs them with isolated output and reports counts, failures and phase evidence. This cross-session delivery/settlement change goes to Review before landing. Deploy/measurement is a later operator-owned stage following the canonical runbook, not an implicit restart during tests.

## Verification requirements for TestDesign

The separate TestDesign stage must turn these acceptance contracts into fixtures, named tests, regression filters and a small set of meaningful red/revert/green positive controls. The following are requirements, not claims of executed tests.

| Area | Required proof | Existing anchors / proposed new class |
|---|---|---|
| Availability | Held before provisioning creates no Distill row; normalized alias and kind isolation; hold appearing while queued cancels only Queued; dispatch wins safely; hold removal permits a later request; read failure differs from hold. | `ModelAvailabilityDispatcherTests`, `ModelAvailabilityTests`, `OutputDistillationTests`; new `SpecialistTaskRunnerDeadlineTests`. |
| Queue | Capacity+1 rejects the extra request explicitly; producer never waits for a model; raw hold is released; duplicate completion admits no second request; expired requests never Ensure or dispatch. | `OutputDistillationTests`; new `OutputDistillationQueueTests` and `OutputDistillationHostedServiceTests`. |
| Deadline | Provisioning/channel/dispatch/brief-queue time all consume one budget; exact-boundary and just-late reads/writes; restart retains expiry; no minimum-budget reset; typed/confirmed work survives caller expiry. | New `OutputDistillationDeadlineTests`; dispatcher and message-queue fixtures. |
| Cleanup | Blocked alert cannot extend waiting; bounded cleanup with DB failure; shutdown does not fake success/timeout; raw hold expires even if release fails; dispatched standing seat remains owned. | `OutputDistillationTests`, new runner deadline tests, `AgentTaskCheckInterpreterTests`, `SpecialistRoleContractTests`, existing Diagnose behavior tests selected by actual caller changes. |
| Ingestion | S0 reproductions red under original await shape, green after separation while the blocking gate is still held; actual hosted stream/catch-up path; persistence timing distinguished from CompletedAt and save. | New `SessionRunnerEventPumpTests`; `AgentSessionRuntimeTests`, `FakeSessionRunnerClient`. |
| Ordering/recovery | Own UserPrompt ingests during verification; split end/text, multiple buffered turns, duplicate/restarted tailer sequence, interrupted/error/manual/auto-compaction semantics; durable pending work after crash/full wakeup channel; saturated workers remain bounded. | `AgentTaskSettlementRaceTests`, `SessionMessageQueueDeliveryVerificationTests`, `ChannelReplyDurabilityTests`, `CompactionRecoveryTests`, `GrokRulesCompactionRecoveryTests`; new runtime-action tests. |
| Application | Before deadline replaces exactly once; pending after expiry never replaces; concurrent delivery/SendNow/full-report poll wins safely; attempted/canceled note immutable; Shadow never holds/rewrites; raw report, digest, header/handoff and full pointer survive fallback. | `OutputDistillationTests`, `PolledCompletionNoteShrinkTests`, `OutputDistillationGateTests`; new deadline tests. |
| Accounting | Late success/failure/cancellation updates eventual projection without changing original verdict or note; settlement on either side of ledger insert; restart; repeated reads and duplicate links count once; unknown is visible; cohort window stable. | `DistillationEndpointTests`; new `OutputDistillationAccountingTests`. |

Use `dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c432/ -- --treenode-filter "/*/*/<NamedClass>/*"` with each actual class substituted. TestDesign must select methods for large regression classes rather than force the whole Application namespace. Require nonzero executed counts and intended assertion failures for positive controls. Use the established production-runner guard and row-scoped shared-Postgres assertions. Tests driving global sweeps require ungrouped `[NotInParallel]`. A process-spawning fixture needs the assembly's `ParallelLimiter<ProcessSpawnLimit>`; do not run `Antiphon.Tests` concurrently with `Antiphon.Agents.Pty.Tests`.

Drive deadline and polling tests using controllable timers/barriers, not 45-second sleeps or a frozen timestamp with real timers. An offset clock or properly driven FakeTimeProvider must satisfy the queue's clock contract. The stream test must feed a real fake stream through the hosted pump, not call the newly split ingest method and assume the wiring uses it. External sends stay captured by fakes. If API consumer types change, use scoped `scripts/test-client.ps1` tests plus the client type/build check; no browser E2E is required for this backend change.

## Post-deploy measurement and completion bar

After a separately authorized deployment, inspect the running mode and budget directly and collect the next naturally occurring Shadow cohort (target at least 100 eligible attempts across an available period and a hold period; report if a hold never occurs). No synthetic specialist jobs are needed. Preserve request counts including admission/length skips and distinguish all-ledger rate from attempted-run rate.

Report held-degrade latency and created-run count, request-queue age/rejections, available-run dispatch and prompt-to-report time, fleet receipt-to-persist, report-observed-to-settlement-save, cleanup time, phase-specific expiry rates, worker saturation and unresolved/effective cost. Compare available-model requests separately so relabeling holds cannot masquerade as latency improvement, and separate bundle versions so the independent v2 prompt change is not attributed to CARD-0432. Use the investigation's baseline only for comparable intervals; no promise of a particular timeout percentage or gate acceptance follows from this design.

Shipping requires the bounded-ingestion reproductions to pass, no duplicate/raw-report loss, correct expiry before launch/application, and unchanged Shadow mode. Production trend measurement can remain pending until enough natural traffic arrives and must be reported as pending, not inferred from a healthy server. A continued large ingestion tail after the selected waits are removed is new evidence for the named phase, not grounds for an automatic timeout bump.

## Decisions and handoff status

No user decision blocks TestDesign. D-1 through D-8 are implementation defaults. The unresolved historical blocker is explicitly scoped to S0, with named reproduction gates and a required evidence artifact before the concurrency slice. The separate verification design must cover the durable-action complexity rather than fold this into a small timeout-setting test.

## Verification design

TestDesign, 2026-09-07, against landed plan `1e3b30af`. D-1 through D-8 and S0-S5 remain the fix design. This section specifies tests to implement and execute during Code; no tests, builds, runtime probes or model calls were executed by TestDesign. New class/method names below are proposed stable test targets, not claims that they already exist. All new C# test classes go in `tests/Antiphon.Tests/Application/` unless another directory is named.

### Fixtures and execution contract

**F-1: real services and isolated storage.** Extract the reusable seeding/scoped-read portions of `OutputDistillationTests.Harness` into `TestHelpers/OutputDistillationHarness.cs`; compose `BridgeQueueHarness` through its existing `HarnessOptions.ConfigureServices` and `ConfigureDbContext` hooks for queue/runtime cases. Use real `OutputDistillationService`, specialist runner, dispatcher, queue, runtime and reply dispatchers. Replace only external I/O: `ISessionRunnerClient`, protocol adapter, `IAntiphonMessagingProducer`, `IEventBus`, alert router and any git/workspace operation that would leave the temporary test repository. Register the git graph through `DelegationTestServices`. Never mock a service's success/timeout/settlement result in the integration cases that are meant to prove that service.

Use `TestDbFixture` PostgreSQL with unique source/run/note/session/agent IDs, a unique specialist slug and temporary working directory. Route specialist trust/config seeding into a temporary `claudeConfigJsonPath`; no live provider home. Seed an AlwaysOn, non-pool standing specialist and a Running session. Unexpected launches, kills and provider changes are recorded and fail the test; no child provider process is needed. Assert rows by fixture IDs. Classes invoking global dispatcher/recovery sweeps, setting real kind/alias holds, or asserting isolated cohort totals use `[NotInParallel]` without a group. Each fixture owns and disposes its scopes, hosted services, gates and timers. Each class is explicitly Unit or Integration. If the script compatibility fixture starts pwsh, it also uses the assembly-local `ParallelLimiter<ProcessSpawnLimit>`.

**F-2: an actual scripted fleet stream.** Add `TestHelpers/ScriptedSessionRunnerClient.cs`, with a channel-backed `StreamEventsAsync`, a per-session snapshot store, live Running session/buffer/snapshot responses, exact input/kill/start call capture, and stream-generation/MoveNext acknowledgements. The existing `FakeSessionRunnerClient` returns an empty stream, and `BridgeQueueHarness.EmptyRunnerClient.StreamEventsAsync` throws; neither is sufficient unchanged. Replace the runner registration before resolving the runtime. Start the actual `SessionRunnerEventPump` and actual runtime-action worker via `StartAsync` in a small host; do not call `ObserveTranscriptAsync` directly to claim pump wiring coverage. For dispatch-only tests, use `FakeAgentProtocolAdapter` on seeded sessions so no process launches.

The scripted runner's producer log and snapshot store advance when the fixture produces an event, independently of when the pump requests it. Keep three separate observations: Produced, DeliveredToEnumerator, and Persisted/Committed. This allows an old blocked pump to leave B produced but not yet received, instead of inaccurately labeling that interval database latency. Disable incidental transcript reads/sweeps in the baseline blocking test so catch-up does not accidentally heal B; reconnect tests enable them deliberately. Calls to the raw snapshot API are captured.

**F-3: timing and I/O barriers.** Use `FakeTimeProvider` with functioning timers and explicit advancement after an awaited phase-entry barrier for deadline arithmetic; do not reuse the existing unconstrained `PumpClockAsync` loop for exact boundaries. UTC and delay timers share the provider. Runtime/queue tests use an offset-over-real clock, or the same explicitly driven fake with every timer advanced; never a frozen UTC provider with real timers. Give each barrier a short real watchdog (five seconds with diagnostics) for fixture failure, not an acceptance latency budget. Negative assertions occur after a known phase/barrier or completed operation, not after an arbitrary sleep.

Use `TaskCompletionSource` gates with asynchronous continuations on external I/O, and EF `DbCommandInterceptor`/`SaveChangesInterceptor` hooks keyed to fixture IDs for selection/read/save boundaries. Capture the cancellation token, operation start/end and whether the operation is still active when its owning scope is disposed. Never fake an EF SaveChanges success. For a genuine blocked-write proof, use a second Postgres transaction holding a row lock and a short real request deadline, then release it after deadline cancellation; do not assume advancing a fake .NET clock advances the database clock. This is the only deliberately real deadline wait in the new backend tests. Log exact commanded/observed deadlines; allow scheduling time only for observing completion, never for authorizing a late write.

**F-4: report and note identities.** Seed observable transcript history before typing so confirmation cannot take an unobservable screen fallback. A complete B turn consists of its exact `DelegationReportFormatter.TaskMarker` in UserPrompt, marked AssistantText ending with `ReportToken(B, "done")`, and a successful TurnEnd with stable Uuid/ApiCallId. Preserve a raw source report above 1,200 characters containing a card ID, commit, path, URL, failure count and full-report pointer; generate a known gate-passing shorter result. Verify the gate fixture passes the unchanged gate before testing application. Source reports with distinct sentinel text make wrong-turn attribution visible. The fake records normalized body writes, bracketed-paste framing and separate Enter writes; only the matching complete UserPrompt confirms delivery.

**F-5: replay and external effects.** The messaging fake records every send attempt and successful acceptance separately, without adding a new dedup filter that could conceal duplicate calls. Keep its acceptance log across reconstructed application providers, representing the external recipient. Recreate all runtime/coordinator/worker/queue service instances for restart tests while preserving only PostgreSQL, the runner snapshots and that external acceptance log. Fault gates around commit and action acknowledgement are test interceptors, not test-only production decisions. For crash-after-effect tests stop before action acknowledgement with the existing domain send/settlement claim already committed. Also test failure before produce: a thrown fake send must leave/reinstate owed work according to the existing dispatcher contract. These cases do not claim to solve arbitrary broker acknowledgement ambiguity.

Review routing needs an explicit fixture: use `ReviewReplyDispatcherTests.Harness.TrackThread`'s seed shape (`ReviewPromptFormat.EnvelopePrefix`, a real ReviewThread and PendingThreadReply) and register the real dispatcher. Current `ReviewReplyDispatcher.Track` stores correlation only in `_pending`; reconstructing that dispatcher loses it. The durable-action implementation must retain the already-authorized review target and its consumption identity before the original process can disappear. V-16 must not call Track again after restart or infer authorization to an arbitrary thread solely from a transcript tag. Test both commit-before-wakeup and comment-committed/action-unacked: exactly one correct ReviewComment must survive. This is a concrete dependency of D-5's durable phases, not evidence that today's review dispatcher already has channel-style durable claims. A swallowed child failure/empty correlation is not proof that a worker phase completed successfully.

**F-6: observable evidence.** Use a recording nonblocking logger/span sink keyed by fixture IDs plus EF post-save/transaction-commit observation. Write phase traces and TRX under an ignored `tests/Antiphon.Tests/TestOutput/card-0432/<run-id>/`. Include counts of model dispatches, body writes, Enter presses, confirmed prompts, note rows, channel acceptances and phase executions. Do not log report bodies. A diagnostics-failure case uses a throwing diagnostics-only sink; the independently recorded database/wire outcome remains the verdict. For the script test, execute the real `scripts/distiller.ps1` with `ANTIPHON_API` pointing only to a random-loopback stub serving synthetic JSON, and restore the prior environment value afterward.

Boundary interpretation for tests: `now < DeadlineAt` is eligible; equality is expired. A never-executed request uses `DegradedExpired` with its phase, a started run whose wait expires uses `DegradedTimeout`, and a valid in-budget result losing the later application race uses `AppliedLate` with its reason. A hold observed after dispatch does not retrospectively make that run never-dispatched. Do not equate an absent ledger during database failure with absence of the durable raw note.

### Proves it works now

All V cases are Integration except V-1's pure settings/enum assertions and V-4's channel-capacity test. Parameterized rows named below are mandatory, not optional examples.

| ID | Test target and setup | Required observable result |
|---|---|---|
| V-1 | `OutputDistillationAdmissionTests.Defaults_and_legacy_contract_are_preserved`. Read `DelegationSettings` and repository configuration without launching Program; round-trip the twelve existing outcome enum values and old nullable ledger/task data. | Shadow, 45 seconds, queue capacity 3 and unfinished-run cap 3; existing outcome numbers 0-11 unchanged. Check/Diagnose use their existing policy, historical null deadline/phase fields remain null, and v2 bundle/contract tests still pass. No mutation of live settings. |
| V-2 | `OutputDistillationAdmissionTests.Held_preflight_never_creates_work` with missing/existing seat, alias string/exact model ID, manual/timed/kind-wide hold; `Availability_error_is_not_a_hold`; `Hold_checks_follow_the_pinned_alias`. Use real `ModelAvailability` for hold rows and a recording throwing I/O fake for the error arm. | No Distill row, no Ensure/start side effect when already known held, no typed bytes and no per-report generic unavailable alert. Outcome Held with the canonical kind/alias; error outcome Unavailable with check reason. A hold on another alias/kind does not block the request. After hold expiry/removal, a new request can create one run. Length/role/disabled skips still precede availability. |
| V-3 | `SpecialistTaskRunnerDeadlineTests.Hold_appearing_after_ensure_is_rechecked`; `Hold_and_dispatch_race_cancel_only_queued` (cancel-first and dispatch-first). Gate Ensure return and conditional queued cancellation separately; advance to the next two-second poll. | Post-Ensure hold creates no run. Cancel-first leaves one Canceled never-dispatched row and Held ledger. Dispatch-first leaves the standing session owned, no kill/requeue, and continues to success or original deadline. Assert captured cancellation never updates Working/Dispatched and no alternate provider/seat is selected. |
| V-4 | `OutputDistillationQueueTests.Capacity_rejects_without_dropping_an_accepted_request` (Unit): enqueue A/B/C while reader stopped, then D, drain and enqueue E. `OutputDistillationAdmissionTests.Full_missing_and_closed_worker_keep_raw_fallback` (three rows). | A/B/C accepted in order, D explicitly rejected, E accepted after a slot frees; no hidden overflow/drop. Actual producer records queue-full Busy, or worker-unavailable reason, with zero new run for the rejected request and no remaining artificial raw hold. Finish the producer while the model/alert gates are still blocked. |
| V-5 | `OutputDistillationAdmissionTests.Idle_parent_delivery_does_not_delay_admission` (Shadow, Apply, feature disabled); `Duplicate_completion_admits_once`. Settle a real delegate whose parent is idle, hold the parent's delivery-confirmation operation, and run the actual producer/hosted worker. | Note persistence and eligible distillation admission finish before releasing parent verification. Apply hold equals the captured deadline; Shadow/disabled have no hold. An owned flush eventually delivers the idle raw note without another TurnEnd, including a non-AlwaysOn parent. Duplicate source+digest gives one note and one request/run at most. |
| V-6 | `OutputDistillationDeadlineTests.Queue_provision_and_wait_share_one_deadline`. At T0 admit with D=T0+45s; keep another request active until +20s, gate provisioning for 10s, then dispatch-queue for 8s. Release run at +38s and advance polling to D. | Same RequestedAt/DeadlineAt on request, ledger and task; Apply HoldUntil=D. Only seven seconds remain after dispatch; no 45-second restart. QueueWait is 20s; provisioning and dispatch wait are separately observable. In-budget success at D-1ms can be used, success observed at D or D+1ms cannot. Parameterize expired dequeue, expired Ensure return, and final DB poll returning a terminal row after D; earlier producer timestamps do not override expiry. |
| V-7 | `OutputDistillationDeadlineTests.Dispatch_rechecks_expiry_under_claim` (expired at candidate selection; expires after selection while claim blocked); `Restart_does_not_dispatch_expired_rows`. Seed deadline-bearing Queued Distill tasks, plus an ordinary/null-deadline control, and call the real dispatcher. | Expired Distill has no brief/terminal input and is conditionally canceled with the expiry reason; the control follows previous behavior. Dispose/recreate dispatcher before the second sweep and confirm the stored deadline still prevents launch. A cancellation failure followed by recovery must not resurrect expired work. |
| V-8 | `OutputDistillationDeadlineTests.Expired_brief_is_rejected_before_first_input` (Pending zero-attempt, already typing, already confirmed). Seed a Dispatched Distill task whose brief waits behind a busy composer, then advance D and make it idle. | Never-typed brief is canceled with no bytes, task reconciled and no unowned session. Typing/confirmed cases are not canceled by distiller expiry, receive no duplicate body, remain standing-owned, and finish the existing confirmation/recovery protocol. Include a brief blocked on its queue lock across D. |
| V-9 | `SpecialistTaskRunnerDeadlineTests.Cleanup_has_one_separate_budget` (blocked alert, blocked cancellation DB call, blocked hold release, blocked ledger write); `Host_shutdown_is_not_reported_as_timeout`. Record operation tokens and active scopes. | Decision occurs at D; cooperative cleanup is bounded by D+2s total, not two seconds per operation. Alert delivery neither blocks this nor delays the next admitted request. At D the raw hold is already expired even if explicit release fails; recovery later clears/cancels remaining eligible work. Host cancellation propagates as shutdown, no counterfeit successful ledger, no scope disposed with outstanding DB work, no kill of active standing work. |
| V-10 | `SessionRunnerEventPumpTests.Blocked_effect_does_not_block_another_session` with gate rows `queue-confirm`, `parent-note-confirm`, `channel-send`, `signalr-publish`. Use the S0 procedure below. | While A's gate remains held, B's exact producer events are received and persisted, its marked task settles durably before its original deadline with spare worker capacity, and its raw report exists. At least one gate must be the real queue confirmation loop and one must be nested parent-note delivery. No early release, bypassed pump or manufactured Succeeded row can satisfy this test. |
| V-11 | `SessionRunnerEventPumpTests.Reconnect_catches_up_storage_before_effects`; `Nontranscript_effects_do_not_block_transcripts` with Output, Exited, TranscriptFault, TranscriptBound, Adopted and AgentStatus event rows. Gate each branch's slow I/O, not its bookkeeping. | A blocked catch-up effect does not prevent B backfill or later live ingestion. Backfill order and replay identities remain stable when stream resumes. Each other branch permits B persistence with its external effect blocked, or returns within its explicit finite I/O bound. Exit/status bookkeeping is eventually retained; no bare transport OCE permanently stops the pump. |
| V-12 | `SessionRunnerEventPumpTests.Confirming_prompt_is_ingested_during_its_own_delivery`; `Catchup_under_queue_lock_has_no_effect_wait`. Queue a known complete body, observe first Enter, then emit its confirming UserPrompt through the same hosted stream. Separately call fetch/persist catch-up while the queue lock is held. | Confirmation becomes Sent exactly once without waiting for its own runtime-action slot, no second body write/kill; completeness and identity still govern. Catch-up commits and returns with queue/effect gates held, so the storage lock cannot depend on delivery completion. Screen redraw alone remains insufficient. |
| V-13 | `SessionRuntimeActionTests.Transcript_and_intent_commit_together`; `Concurrent_pull_and_stream_preserve_sequence_identity`. Fail before commit, after commit/before wakeup, and race a pull with stream persistence of overlapping UUIDs and new tailer sequences. | Either both transcript+intent commit or neither does. A retry/sweep processes committed pending intent after a lost wakeup. Stored sequences are unique/monotonic, `(uuid, kind)` is unique logically, both distinct new entries survive, and no effect ran on uncommitted storage. A persistence OCE stops promptly without dispatching an unstored report. |
| V-14 | `SessionRuntimeActionTests.Buffered_turns_route_in_sequence_before_new_input`; `Late_text_revisits_the_owning_turn`. Stop the worker, ingest two complete turns for different Channel correlations, then drain. Separately seed one open task whose marked report is followed by a newer unrelated human turn; never seed two simultaneously dispatched tasks on one standing composer. In another row ingest early TurnEnd, drain its no-text attempt, then ingest late AssistantText before a newer prompt. | Each reply uses its own prompt/turn bounds and correct target; effects occur channel, review, task, then eligible queue delivery. Both already-owed routing turns complete before new input. Late text is routed/settled once and the open task receives its own report rather than the newer human turn. Capture actual published bodies against fixture sentinels, not just call order. |
| V-15 | `SessionRuntimeActionTests.Boundary_types_preserve_their_own_actions` (successful end, canceled end, API-error end, interrupt marker, manual compact, auto compact, internal rules turn, answered-Blocked watermark). Reuse measured shapes from the named regressions below, via the worker. | Ordinary marked end can settle; canceled/interrupt/internal/stale-answer shapes cannot falsely Succeed. Manual compaction executes recovery then narrow idle flush, no stale report settlement/finished toast; auto compaction does not make a working session idle. Replaying Uuid/ApiCallId produces no duplicate effect, while distinct later AssistantText still gets a pass. |
| V-16 | `SessionRuntimeActionTests.Restart_recovers_each_durable_phase` with crash at transcript+intent committed/no wakeup, routing complete/flush pending, channel accepted/action unacked, task settled/action unacked, review-target-persisted/no wakeup, review-comment-committed/action unacked, and finished publication complete/next phase fails. `Failed_send_remains_owed`. Recreate providers using F-5. | New worker drains precisely the owed work without another model turn, duplicate parent note, duplicate channel send, duplicate ReviewComment or duplicate finished publication. Successful phases are not repeated; failed pre-produce sends remain owed and retry even if the child dispatcher swallowed the exception. Two subsequent recovery sweeps are idempotent. Old fully-settled historical turns produce no sends/toasts when the new worker first starts. |
| V-17 | `SessionRuntimeActionTests.Worker_ownership_and_capacity_are_bounded`; `Delivery_rechecks_current_eligibility`. Queue work for six sessions; hold four effects, race duplicate wakeups for one, then release gates one by one. Before a pending delivery phase, add a real working prompt, blocked status or unresolved rules barrier (three rows). | Active workers <=4, active owners per session <=1, no shared active DbContext; remaining intent stays durable with nonnegative queue-age/saturation observations, and drains after capacity frees. Ingestion of all six continues. New working/blocked/rules state prevents input despite an older idle boundary; clearing it permits one delivery. Shutdown leaves recoverable pending intent. |
| V-18 | `OutputDistillationApplyRaceTests.Apply_eligibility_matrix`. Fresh future-deadline gate-passing result with rows: correct eligible note, wrong note ID, wrong source, wrong digest, wrong origin, Canceled, Sent, Pending with attempts=1, matching full-report poll, Shadow snapshot with settings changed to Apply, and expired original deadline after hold cleared. | Only the correct eligible Apply row changes. Header, source, raw digest and full-report pointer retained; raw authoritative Result unchanged. Every denied row has an unchanged body/attempt state and appropriate AppliedLate reason (or no Apply in Shadow). Shadow holds nothing and may only stamp the in-budget distilled result. Null/missing note is safe and produces no replacement note. |
| V-19 | `OutputDistillationApplyRaceTests.Apply_and_delivery_share_the_session_lock` (application-first and delivery-first, plus SendNow and full-report-poll competitors). Gate after state read but before save/claim; launch the competitor through the real queue/service API. | Competitor cannot bypass the same session exclusion. Application-first types the fully composed distilled body once; delivery/SendNow-first types raw once and application loses without changing a Sent/attempted row. Full-report suppression wins when committed before application validation; no mixed body, lost note or second completion. Poll-after-application still follows the existing shrink behavior. |
| V-20 | `OutputDistillationApplyRaceTests.Expired_lock_wait_and_database_write_never_apply`. First hold the in-process queue lock across D. Then hold the note row in an independent PostgreSQL transaction, allow the actual apply statement to block, let its short real deadline expire, release the row lock and await disposal. | No replacement after either wait, even though the row remains Pending with attempts=0. Assert the persisted body after releasing every gate, not merely an early timeout return. Capture effective statement cancellation/remaining budget, no late write after caller return and no scope-use-after-dispose. The application decision cannot be made timely by a pre-wait timestamp. |
| V-21 | `OutputDistillationAccountingTests.Late_outcomes_are_projected_without_new_delivery`; `Distinct_runs_and_request_cohorts_define_effective_cost`. Use the exact cost cohort below, read before/after settlement and after provider recreation. | Eventual status/cost follows task settlement, original outcome/cost/Feedback/FullReadAt remain byte-for-byte stable, no source DistilledResult change from a late result, no new queue/action/message/agent task. Duplicate linked runs count once, unresolved/missing history is visibly unknown and the original request window selects the cohort. No price lookup or synthetic completion is used to fill missing history. |
| V-22 | Add `DistillationEndpointTests.Eventual_fields_preserve_legacy_json_and_filters`; add `tests/Antiphon.Tests/Scripts/DistillerScriptTests.cs` method `Stats_and_list_show_effective_cost_and_unknown_runs`. Serve the real endpoint through `AntiphonWebAppFactory`; serve captured synthetic responses to the real script's isolated loopback stub. | Existing routes, enum strings, feedback overwrite and captured `costUsd` survive. New fields expose late/effective values and unknown counts; `since` is ledger-cohort based. Script labels captured versus effective cost and does not display unknown as verified zero; list/flag/since behavior survives legacy JSON without the added fields. No production HTTP requests. |
| V-23 | `OutputDistillationDiagnosticsTests.Phase_trace_separates_producer_receipt_commit_and_settlement`; `Diagnostics_failure_does_not_change_outcome`. Insert known virtual gaps: producer +0, enumerator receipt +3s, pre-save +4s, successful save +6s, action start +8s, settlement entry +9s, settlement save +11s. Also gate a dispatcher sweep/candidate and cleanup. | Structured fields preserve distinct instants/IDs/stored sequence and name the blocked child; elapsed phases are monotonic. Transcript CreatedAt and task CompletedAt are not mislabeled commit times. Legacy WaitMs retains its old scope, queue/cleanup are separate and historical missing phase fields remain null. Throwing/nonblocking-overflow diagnostics do not lose a report or stall ingestion; no raw report text in the new events. |
| V-24 | `OutputDistillationMigrationTests.Upgrade_preserves_legacy_rows_and_does_not_replay_history`. Use an isolated test database/schema on the test Postgres instance, migrate to the parent of the new migration(s), seed old ledger/null-deadline tasks/settled historical turns plus genuinely owed work, then migrate to current and start recovery. | Old enum/cost/feedback/null values survive; no blanket historical sends/toasts or regenerated model work. New nullable fields and unique action identity work, owed work recovers once. Verify new deadline/task/brief metadata survives provider reconstruction. Migration failure must name the database error; do not hand-create a guessed production schema. |

**S0 red-to-green protocol (V-10/V-11).** First implement the fixture and phase instrumentation against the original await shape, before S3. Session A is observable and idle; enqueue a body with a confirming record deliberately withheld, and wait for the real verification-poll/SendAsync gate to be entered. In the parent-note variant, A is a delegate settling to an idle parent P, whose verification is held. Then produce B's complete marked turn, snapshot it in the fake runner, and keep A/P gated. In the original code the assertion that B is persisted while A remains blocked must fail with B's missing Uuid and an operation-named trace. The five-second observation bound is only how the fixture reaches that assertion; a generic test timeout, fixture exception or no tests is not an acceptable red. Release gates in finally and await shutdown so the experiment cannot strand a test host.

After S3, rerun the identical assertion with A/P still blocked: B persists and settles, and the fake confirms it did not provide B through an unplanned snapshot read. Use two available workers for this test so worker saturation cannot explain the result; test saturation separately in V-17. Channel/SignalR gates and reconnect repeat the same construction. For reconnect, preseed snapshots A then B, start the host, and prove B catch-up completes before releasing A's side effect. Record which precise awaited operation reproduced the issue; a synthetic confirmation reproduction demonstrates the mechanism, not the cause of every historical outlier. If no original-path gate reproduces, follow S0's investigation handoff instead of reporting ingestion fixed. Append S0 evidence paths/results to this plan in Code.

**Exact cost cohort (V-21/V-22).** Choose a fixture-specific ledger `since` window. Inside it seed six ledger rows: two timeouts linked to run A (captured 0 each), one successful row linked to B (captured and settled 0.0020), one timeout linked to still-working C (captured 0), one Held row with no run (known no-attempt zero), and one historical timeout with no surviving run link (captured 0.0005, eventual cost unknown). The existing FK uses its configured deletion behavior; model missing history through that supported null-link shape, not by disabling constraints. A initially has no terminal result, then succeeds the following day at 0.0031. C later fails at 0.0012. Add an older out-of-window linked run costing 1.0000 to catch incorrect join/window order.

Captured total is always 0.0025. Before A settles effective total is 0.0025 with three distinct unresolved runs/unknown histories (A, C, legacy); after A, 0.0056 with two; after C, 0.0068 with one. A is counted once despite two ledger links. Held no-attempt zero is known, and legacy captured fallback is included but not relabeled known. A's two rows remain DegradedTimeout, the window does not change tomorrow, and repeated list/stats reads write nothing. Test late C Succeeded/Canceled as additional outcome rows with the same accounting rule. For database query-count assertions, compare one versus many linked rows and require a fixed bounded number of commands rather than a brittle SQL string snapshot. Scope exact totals to an isolated fixture dataset; do not assert exact whole-table totals in shared Postgres.

### Guards the regression

The new V cases cover the behavior changed by this card. These existing regression targets protect adjacent contracts. Run the listed methods, or the whole named small class where marked; do not silently substitute namespace-wide tests. Existing direct-runtime tests may need to drain the new owned worker before asserting effects, but their message/result expectations must remain intact.

| ID | Future regression | Existing target / why it catches the regression |
|---|---|---|
| R-1 | Optional compression breaks raw fallback, gates or prompt versioning. | Whole `OutputDistillationTests`, `OutputDistillationGateTests`, `OutputDistillerProvisionerTests`, `SpecialistRoleContractTests`. Their existing raw/header/digest, role and bundle contracts stay green. The old queued timeout assertion in `timeout_cancels_a_still_queued_distill_task_and_clears_the_hold` must be migrated deliberately to D-3's pre-execution Expired classification for the new policy; keep equivalent legacy caller coverage in R-3 rather than globally changing Timeout expectations. |
| R-2 | A model hold becomes fleet-wide or never clears. | Whole `ModelAvailabilityDispatcherTests`; `ModelAvailabilityTests/IsHeld_is_true_only_for_the_paused_alias`, `Kind_wide_star_holds_every_alias_of_that_kind`, `Lazy_IsHeld_clears_an_expired_timed_hold`. They distinguish kind/alias and actual expiry. |
| R-3 | The shared runner changes Check/Diagnose timeout or fallback semantics. | `AgentTaskCheckInterpreterTests/a_settled_interpretation_replaces_the_digest_in_the_note`, `an_interpretation_that_never_settles_degrades_and_the_queued_task_is_cancelled`, `a_backlog_at_the_cap_degrades_without_creating_anything`; `AgentTaskAutoTitleTests/a_good_answer_replaces_the_title_and_writes_Diagnosed`, `a_held_alias_creates_no_seat_row_and_ledgers_DegradedHeld`, `a_timeout_cancels_a_queued_run_and_ledgers_DegradedTimeout`. These invoke real callers, not just the new opt-in policy. |
| R-4 | Concurrent settlement, stale answered-Blocked turns or rules turns duplicate/overwrite a result. | `AgentTaskSettlementRaceTests/concurrent_on_turn_end_for_the_same_task_enqueues_one_parent_note`, `enqueue_skips_a_second_delegation_note_with_the_same_source_task_and_digest`, `an_answered_blocked_task_is_not_re_blocked_by_the_stale_boundary`, `the_answer_turn_settles_the_task_and_delivers_one_done_note`, `a_stale_done_boundary_after_a_conflict_reply_does_not_re_succeed`, `Deferred_sweep_never_hands_an_internal_rules_turn_to_settlement` (both argument rows). |
| R-5 | Decoupling mistakes redraws for delivery, retypes a late confirmation or kills working sessions. | `SessionMessageQueueDeliveryVerificationTests/Screen_output_advancing_without_a_record_is_no_longer_delivered`, `Late_confirm_marks_the_message_sent_with_zero_writes_to_the_terminal`, `A_working_session_is_not_killed_when_the_record_never_arrives`, `A_record_that_lands_just_after_the_deadline_confirms_instead_of_killing`, `A_clipped_prefix_parks_as_truncated_not_sent`. New V-8/V-12 additionally exercise these contracts through deadline-bearing work and the hosted stream. |
| R-6 | Replay changes transcript order or loses compaction-specific behavior. | `AgentSessionRuntimeTests/Transcript_entries_from_a_new_tailer_generation_survive_a_sequence_restart`, `Error_turn_end_is_acted_on_once_replay_dedups`; `CompactionRecoveryTests/Duplicate_boundary_events_are_deduped_after_simulated_replay`, `Mid_work_compaction_defers_the_note_to_the_next_turn_end`; `SessionMessageQueueDeliveryVerificationTests/Manual_boundary_with_an_empty_queue_publishes_no_finished_event`, `Auto_compaction_boundary_mid_turn_still_reads_working`; `GrokRulesCompactionRecoveryTests/Captured_native_boundary_reaches_one_durable_refresh_without_idle_input` (all declared arguments). |
| R-7 | A durable channel correlation is lost or answered twice, including late-confirm redispatch, or review prompt ownership changes. | `ChannelReplyDurabilityTests/A_runtime_batch_redispatches_a_late_confirmed_channel_reply_once`, `A_failed_late_confirm_recovery_warns_with_the_correlation_and_leaves_it_owed`, `A_correlation_survives_a_process_restart_and_the_reply_is_published`, `A_restarted_process_does_not_answer_an_already_answered_turn_twice`; whole `ReviewReplyDispatcherTests` (seven existing tests of withheld/API-error, queued/owning prompt and genuine answer routing). |
| R-8 | New application/accounting bypasses full-report poll suppression or changes the public ledger API. | Whole `PolledCompletionNoteShrinkTests` and `DistillationEndpointTests`; V-18/V-19 add the concurrent-application cases absent from those existing tests. |

### Positive controls

Run each control on an otherwise-green implementation. Change one production line/guard, run its named test and record the specific assertion red; revert that exact edit, rebuild, and run the same test green. No committed mutant, changed expected value or fake instructed to return a bad application result. A compile/setup failure, watchdog escape, unavailable database or zero-test selection is not a successful control. Where a new guard has no current symbol, the table identifies its semantic location; Code records the actual path/line/diff. The test must make the mutated guard observable rather than rely on a later independent guard to fail too.

| ID | One-line production mutation | Required red target and assertion |
|---|---|---|
| PC-1 | Bypass the pre-Ensure held predicate in the Distill admission path. | V-2 `Held_preflight_never_creates_work`, missing-seat row: Ensure/seat side-effect count becomes nonzero even if the dispatcher later refuses work. |
| PC-2 | Bypass the post-Ensure availability recheck. | V-3 `Hold_appearing_after_ensure_is_rechecked`: a Distill row is created after the gated identity/hold change. |
| PC-3 | Remove `Status == Queued` from conditional held/expired caller cancellation. | V-3 `Hold_and_dispatch_race_cancel_only_queued`, dispatch-first row: a Dispatched/Working task is incorrectly Canceled. |
| PC-4 | Set the runner deadline to `GetUtcNow() + waitBudget` at worker start. | V-6 `Queue_provision_and_wait_share_one_deadline`: task/decision deadline differs from T0+45s and the run waits past D. |
| PC-5 | Let the terminal-row branch return success without the post-read deadline check. | V-6's final-read-after-D row: the held DB read returns a success classified in-budget. Invoke the runner with a controlled read completing across D so a previous caller check cannot mask this. |
| PC-6 | Remove the dispatch-claim expiry revalidation, leaving the earlier candidate check. | V-7 `Dispatch_rechecks_expiry_under_claim`, expiry-between-check-and-claim row: an expired task becomes Dispatched/gets a brief. |
| PC-7 | Bypass the terminal queue's never-typed expired-brief rejection. | V-8 `Expired_brief_is_rejected_before_first_input`, zero-attempt row: captured body/Enter count becomes nonzero. |
| PC-8 | Broaden brief expiry cancellation to include attempts >0/confirmed work. | V-8 already-typing/confirmed rows: protected input/task is canceled or confirmation no longer completes. |
| PC-9 | Change bounded-channel full behavior to DropOldest. | V-4 `Capacity_rejects_without_dropping_an_accepted_request`: D is reported accepted and/or A disappears; compare accepted IDs with drained IDs. |
| PC-10 | Set completion-note enqueue's `deliverIfIdle` back to true. | V-5 `Idle_parent_delivery_does_not_delay_admission`: producer cannot finish/admit while parent confirmation gate is still held. |
| PC-11 | Await the runtime-action completion in the fleet transcript receive branch. | V-10 queue-confirm row: B Uuid is still absent while A is held. This is the post-fix control in addition to S0's original-code red. |
| PC-12 | Commit transcript storage before adding/committing its action intent (move the transaction commit to the earlier production point). | V-13 `Transcript_and_intent_commit_together`, injected failure between writes: one transcript exists with no recoverable intent. Use a syntactically valid single-line commit insertion at that boundary. |
| PC-13 | Bypass the shared per-session storage-lock acquisition. | V-13 concurrent pull/stream case: the second operation enters the storage critical section while the first is gated, violating one storage owner per session. Then release them and assert neither entry is lost. A database uniqueness constraint may still protect rows; it must not hide the exclusion assertion. |
| PC-14 | Ignore the action's stored turn bound and select the latest turn. | V-14 `Buffered_turns_route_in_sequence_before_new_input`: first published sentinel/target belongs to the second turn or first owed turn is missing. |
| PC-15 | Treat a manual compaction action as ordinary turn-end work. | V-15 manual row: stale task settles or an unwanted SessionFinished event is published. Seed a plausible old marked report so the mutation is observable. |
| PC-16 | Skip the current eligibility recheck in the queued delivery phase. | V-17 `Delivery_rechecks_current_eligibility`: after the barrier inserts a working prompt, blocked status or rules hold, input is incorrectly typed. Mutate at the final effective eligibility gate; if downstream existing guards still protect input, record that guard and exercise its removal instead. |
| PC-17 | Acknowledge a runtime-action phase before awaiting its durable side effect. | V-16 `Restart_recovers_each_durable_phase`, routing-pending failure row: after the injected failure/restart the owed reply/note never appears. |
| PC-18 | Bypass the completed-phase replay guard for the finished-publication phase. | V-16 finished-publication/next-phase-fails row: successful SessionFinished is published twice after provider recreation. |
| PC-19 | Bypass the coordinator's per-session ownership claim. | V-17 `Worker_ownership_and_capacity_are_bounded`: two jobs for one session enter the instrumented effect concurrently. This assertion precedes any downstream queue/settlement lock that could hide duplicate ownership. |
| PC-20 | Raise the worker concurrency bound from 4 to 5. | V-17 saturation row: five held effect invocations are active at once. No model/real send exists behind these gates. |
| PC-21 | Replace captured request mode with the current settings mode at application. | V-18 Shadow-to-Apply refresh row: a Shadow-admitted note is held or rewritten. |
| PC-22 | Remove the status/attempt eligibility predicate from the queue-owned replacement operation. | V-18 Pending-attempted row with all identity/time guards satisfied: already-typed body changes. Repeat the one-line edit separately for the status clause and Sent/Canceled rows if those clauses are independent. |
| PC-23 | Remove the note/source/raw-digest identity predicate from replacement. | V-18 wrong-source/digest row: an unrelated note changes. If implemented as separate clauses, mutate each once against its matching data row; origin and note-ID routing also need their corresponding row. |
| PC-24 | Bypass the replacement operation's matching full-report-poll suppression. | V-18 matching-poll row: body becomes distilled despite the durable matching read. |
| PC-25 | Replace the effective application deadline with an unbounded future instant at the application entry (including its linked remaining-time budget). | V-20 expired-lock/blocked-write rows: releasing the lock after original D permits a persisted replacement. Assert after all work finishes; an early caller timeout is insufficient. |
| PC-26 | Remove the shared queue-lock acquisition from application while retaining its read/save checks. | V-19 delivery-first interleave: pause application after its stale read, let delivery claim/type raw, resume application; its changed Sent body or mixed delivery fails. If an atomic database claim independently prevents that result, require the production lock-exclusion assertion to turn red rather than weakening the independent guard. |
| PC-27 | Reuse the expired execution token for timeout cleanup instead of its separate budget. | V-9 ordinary queued timeout with healthy cleanup: hold release/conditional cancellation/ledger finalization are skipped at D. A second edit resetting the two-second allowance per cleanup operation must separately make the multi-gate row exceed D+2s. Report the two edits as PC-27a/b. |
| PC-28 | Add a one-line fixture-ID-scoped update in the eventual projection that overwrites a linked timeout ledger's Outcome with Applied on late success. | V-21 late-outcome test: the original DegradedTimeout decision changes. The independent no-note-write assertions remain; deadline/application controls cover attempted late replacement. This mutation models incorrectly treating eventual success as a revised note decision. |
| PC-29 | Aggregate effective cost per ledger row instead of per distinct linked run. | V-21 exact cohort: effective total after A becomes 0.0087 instead of 0.0056. |
| PC-30 | Filter linked task CompletedAt by `since` instead of selecting the ledger request cohort. | V-21 out-of-window/next-day rows: an old-request/new-completion cost leaks in or an in-window late result disappears. Use both time arrangements so the wrong filter cannot accidentally give the same set. |
| PC-31 | Remove the answered-Blocked prompt-watermark rejection from sequence-bound task extraction. | V-15 stale answered-Blocked row or R-4 `an_answered_blocked_task_is_not_re_blocked_by_the_stale_boundary`: the task is re-blocked by the old report. |
| PC-32 | Remove `m.ChannelReplySettledAt == null` from `ChannelReplyDispatcher.OpenCorrelations`' query predicate. | R-7 `A_restarted_process_does_not_answer_an_already_answered_turn_twice`: fake producer receives a second send for the same turn. This pins the external-send guard on which D-5 phase replay relies. |
| PC-33 | Omit persisting the review-target correlation in its runtime-action intent. | V-16 review-target-persisted/no-wakeup row: after provider recreation the correct thread receives no reply; no test-side Track call may repair it. Separately bypass the durable review-comment consume check for the comment-committed/action-unacked row and require a duplicate comment red (PC-33a/b). |

For PC-5/PC-12/PC-13/PC-26, the barrier must target the named production boundary after the mutation; a barrier that is never reached is a fixture failure, not red evidence. If Code chooses D-5's evidence-backed narrower scheduler, it must update the action-specific fixtures/controls to the actual ownership/durable-recovery mechanism before claiming the corresponding V/R items complete. It cannot mark those requirements out of scope simply because the implementation used different names.

### Commands and required report

Run from the Code checkout, sequentially. The command lists below are the execution manifest after the named new tests are implemented. New class names can change only with matching updates to this section and the V/PC mapping. Each run writes a fresh TRX; record executed/pass/fail/skip counts and check that every intended argument row ran. `--list-tests` is not execution evidence. Use `--no-build` only while compiled inputs remain unchanged; every C# mutant/restoration requires a fresh build.

```powershell
$c432Classes = @(
    'OutputDistillationAdmissionTests', 'OutputDistillationQueueTests',
    'SpecialistTaskRunnerDeadlineTests', 'OutputDistillationDeadlineTests',
    'SessionRunnerEventPumpTests', 'SessionRuntimeActionTests',
    'OutputDistillationApplyRaceTests', 'OutputDistillationAccountingTests',
    'OutputDistillationDiagnosticsTests', 'OutputDistillationMigrationTests',
    'DistillerScriptTests', 'OutputDistillationTests', 'OutputDistillationGateTests',
    'OutputDistillerProvisionerTests', 'SpecialistRoleContractTests',
    'ModelAvailabilityDispatcherTests', 'PolledCompletionNoteShrinkTests',
    'DistillationEndpointTests', 'ReviewReplyDispatcherTests'
)
foreach ($testClass in $c432Classes) {
    dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c432/ -- --treenode-filter "/*/*/$testClass/*" --report-trx --report-trx-filename "c432-$testClass.trx"
    if ($LASTEXITCODE -ne 0) { throw "CARD-0432 failed: $testClass (exit $LASTEXITCODE)" }
}

$c432Methods = @(
    'ModelAvailabilityTests/IsHeld_is_true_only_for_the_paused_alias',
    'ModelAvailabilityTests/Kind_wide_star_holds_every_alias_of_that_kind',
    'ModelAvailabilityTests/Lazy_IsHeld_clears_an_expired_timed_hold',
    'AgentTaskCheckInterpreterTests/a_settled_interpretation_replaces_the_digest_in_the_note',
    'AgentTaskCheckInterpreterTests/an_interpretation_that_never_settles_degrades_and_the_queued_task_is_cancelled',
    'AgentTaskCheckInterpreterTests/a_backlog_at_the_cap_degrades_without_creating_anything',
    'AgentTaskAutoTitleTests/a_good_answer_replaces_the_title_and_writes_Diagnosed',
    'AgentTaskAutoTitleTests/a_held_alias_creates_no_seat_row_and_ledgers_DegradedHeld',
    'AgentTaskAutoTitleTests/a_timeout_cancels_a_queued_run_and_ledgers_DegradedTimeout',
    'AgentTaskSettlementRaceTests/concurrent_on_turn_end_for_the_same_task_enqueues_one_parent_note',
    'AgentTaskSettlementRaceTests/enqueue_skips_a_second_delegation_note_with_the_same_source_task_and_digest',
    'AgentTaskSettlementRaceTests/an_answered_blocked_task_is_not_re_blocked_by_the_stale_boundary',
    'AgentTaskSettlementRaceTests/the_answer_turn_settles_the_task_and_delivers_one_done_note',
    'AgentTaskSettlementRaceTests/a_stale_done_boundary_after_a_conflict_reply_does_not_re_succeed',
    'AgentTaskSettlementRaceTests/Deferred_sweep_never_hands_an_internal_rules_turn_to_settlement',
    'SessionMessageQueueDeliveryVerificationTests/Screen_output_advancing_without_a_record_is_no_longer_delivered',
    'SessionMessageQueueDeliveryVerificationTests/Late_confirm_marks_the_message_sent_with_zero_writes_to_the_terminal',
    'SessionMessageQueueDeliveryVerificationTests/A_working_session_is_not_killed_when_the_record_never_arrives',
    'SessionMessageQueueDeliveryVerificationTests/A_record_that_lands_just_after_the_deadline_confirms_instead_of_killing',
    'SessionMessageQueueDeliveryVerificationTests/A_clipped_prefix_parks_as_truncated_not_sent',
    'AgentSessionRuntimeTests/Transcript_entries_from_a_new_tailer_generation_survive_a_sequence_restart',
    'AgentSessionRuntimeTests/Error_turn_end_is_acted_on_once_replay_dedups',
    'CompactionRecoveryTests/Duplicate_boundary_events_are_deduped_after_simulated_replay',
    'CompactionRecoveryTests/Mid_work_compaction_defers_the_note_to_the_next_turn_end',
    'SessionMessageQueueDeliveryVerificationTests/Manual_boundary_with_an_empty_queue_publishes_no_finished_event',
    'SessionMessageQueueDeliveryVerificationTests/Auto_compaction_boundary_mid_turn_still_reads_working',
    'GrokRulesCompactionRecoveryTests/Captured_native_boundary_reaches_one_durable_refresh_without_idle_input',
    'ChannelReplyDurabilityTests/A_runtime_batch_redispatches_a_late_confirmed_channel_reply_once',
    'ChannelReplyDurabilityTests/A_failed_late_confirm_recovery_warns_with_the_correlation_and_leaves_it_owed',
    'ChannelReplyDurabilityTests/A_correlation_survives_a_process_restart_and_the_reply_is_published',
    'ChannelReplyDurabilityTests/A_restarted_process_does_not_answer_an_already_answered_turn_twice'
)
foreach ($testMethod in $c432Methods) {
    $trxName = 'c432-' + $testMethod.Replace('/', '-') + '.trx'
    dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c432/ -- --treenode-filter "/*/*/$testMethod" --report-trx --report-trx-filename $trxName
    if ($LASTEXITCODE -ne 0) { throw "CARD-0432 regression failed: $testMethod (exit $LASTEXITCODE)" }
}
```

The class/method loops are intentionally explicit. To reduce repeated runner startup with the pinned TUnit runner, they may be grouped using the owner-documented per-operand parentheses/trailing-wildcard syntax, only if the resulting fresh TRX is audited against this manifest. Do not broaden the scope to the whole Application namespace. Rerun all touched/new tests after final changes; do not repeat unchanged passing classes merely for elapsed-time reassurance.

For each PC, substitute its exact `Class/Method` in the same command shape and use distinct `c432-PC-n-red.trx` and `c432-PC-n-green.trx`. Code's report has one row per V-1..V-24, R-1..R-8 and PC-1..PC-33, including subedits, actual filter/argument coverage, counts, observed assertion and artifact path. Show the original S0 red separately from post-fix positive controls. Require Review after Code. Report natural Shadow cohort measurement as pending until the later deployment/measurement stage supplies it; do not manufacture a production timeout-rate improvement from these fixtures.

### Out of scope

- Live Haiku/model calls, runner 17204, real broker/channel sends, provider authentication/configuration, seat restarts and mode/timeout changes. F-1/F-2/F-6 give the needed boundaries without those dependencies.
- Prompt/gate quality changes, model latency benchmarking and timeout-percentage targets. Contract v2 is preserved; its gate-rejection effect is independent of this card. Existing gate/provisioner tests protect compatibility.
- Browser E2E, frontend redesign, full solution/full Application suites and headed/PTY canaries. Current source search finds no distillation-specific client consumer; the existing user-facing consumer is `scripts/distiller.ps1`. If Code adds or modifies a real client consumer, add its concrete `scripts/test-client.ps1 <file>` filter and client type/build check before handoff; do not run client tests based on a hypothetical consumer.
- Physical process death as a prerequisite for every crash window. New-provider reconstruction plus real committed PostgreSQL and an external fake acceptance log removes application memory deterministically. It does not certify power-loss durability beyond PostgreSQL's contract, broker exactly-once delivery across arbitrary unknown acknowledgements, or recovery of a provider process with no owner.
- Exhaustive unrelated channel formatting, all provider error grammars, worktree landing and global fleet capacity policy. R-4..R-7 cover the delivery/ordering contracts this change can affect; expand only for an actual additional changed path or a named failure.

### Cost

Suites forced: **Antiphon.Tests only**, nineteen named classes (eleven new) and thirty-one named existing method filters, plus each listed positive-control red/restored-green run. No live model cost. Dependencies are the pinned .NET SDK, the repository's isolated Postgres test fixture and pwsh for the script fixture. Migration upgrade uses a separate schema/database on that test instance. The class/method counts describe the execution manifest, not a passing test count.

Estimated verification floor after implementation is **about 45 minutes with warm caches**, including original-path S0 evidence, genuine blocked-write cases and all positive-control subedits; allow 60-90 minutes for cold build/container startup and diagnosing failed controls. This is a planning estimate, not a measured runtime. Most 45-second timelines are virtual; repeated C# rebuilds dominate the mutation cost. The plan's broad runtime-ordering change, not model usage, creates this verification cost. Keep the output path fixed and follow `docs/testing-and-build.md` if builds stall. TestDesign itself performed only document/source checks; no runtime acceptance is claimed.

--- next stage ---
next: code
handoff: Implement CARD-0432 S0-S5 with Shadow and 45 seconds unchanged; execute V-1..V-24, R-1..R-8 and PC-1..PC-33 including subedits. First record S0's blocked hosted-pump red, then prove isolation, deadline/application races, durable recovery including review correlations, and late-cost accounting with isolated fakes. Report counts/evidence and request Review.
artifact: docs/superpowers/plans/2026-09-07-card-0432-distiller-timeouts-plan.md

## Code dispatch 5e4516c6: S0-S2 only

The subsequent Code brief limits this dispatch to S0 reproduction, S1 held-model fallback and
S2's original admission deadline, including the application race guard. It explicitly excludes
the durable runtime-action worker/review correlation and eventual-cost accounting. Those
later stages and their V/PC cases are not acceptance claims for this implementation.

S0 confirmed the blocking hypothesis using the actual hosted pump with gated SignalR and real
queue-confirmation callbacks, including serial reconnect catch-up. S1-S2 are implemented with
bounded admission, canonical held checks, durable task/brief deadlines, queue-owned application,
one separate cleanup allowance and owned completion/incident work. Shadow, 45 seconds and the
v2 content contract remain unchanged. Implementation evidence, the awaited-child inventory,
test manifests, deliberate mutation results and remaining stage boundaries are recorded in
[the S0-S2 evidence report](../../investigations/2026-09-07-card-0432-s0-s2-evidence.md).

Review this slice before the separate S3 amendment/Code dispatch. S3 must carry the already
authorized review target thread ID and owning prompt in durable work, and persist a review
consume marker rather than relying on the in-memory `ReviewReplyDispatcher._pending` map.
Natural Shadow cohort measurement is pending deployment/measurement. `DegradedExpired` will
absorb formerly queued requests; a worse-looking headline degrade rate can be the intended
fail-fast result, not a regression.
