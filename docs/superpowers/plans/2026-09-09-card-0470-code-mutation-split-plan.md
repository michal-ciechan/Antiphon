# CARD-0470: Separate Code from mutation verification

Date: 2026-09-09. Stage: Plan. Verification design is a separate TestDesign dispatch.

Code implements the plan, runs every ordinary functional/regression V-n and R-n, commits and pushes, then settles `next: mutation`. A separate writable `Mutation` stage runs the deliberate PC-n battery against that committed implementation. The next card can occupy Code's project/role slot while Mutation runs. Landing follows successful mutation verification and any required Review.

## Ground truth

| Assumption or question | Current implementation | Design consequence |
|---|---|---|
| Code must do the PCs before completing. | `server/Bundles/stage-code.md` explicitly requires every V/R and PC, including break/red/restore/green, and adding a missing PC. `InstructionBundleTests.stage_bundle_invariants_are_pinned_by_substring` pins this text. | Change the standing contract and its tests; there is no executable PC-result gate to move. |
| Verification is folded everywhere consistently. | The delegate skill and `docs/orchestration-loop.md` fold Verify into Code. `stage-test-design.md` says Build runs PCs. `stage-review.md` simultaneously says read-only and run PCs. The testing guide calls this Code-stage PC execution. | Update all these owners, including the contradictory Review instruction. |
| Another Code dispatch would free capacity. | `DelegationOpenGate` selects open rows by `ProjectId`, including a distinct null bucket; `Snapshot.RoleCount` compares the exact role. Queued/Dispatched/Working count. `RolePolicy["Code"].RecommendedInFlight` is 1. Project-wide `MaxOpenTasks` and dispatch-time `MaxConcurrentTasks` are additional limits. | A new exact role gets a separate slot; do not increase or bypass any existing cap. |
| Role settings are at the brief's path. | The actual file is `server/Application/Settings/DelegationSettings.cs`, not `Application/Services/DelegationSettings.cs`. Code currently defaults to Frontier. | Add Mutation to this settings dictionary with its own entry. |
| Test or Review can run the battery unchanged. | Test is a helper that runs and reports without changing production/tests. Review is a read-only stage. | Neither owns deliberate source mutations. |
| A new name in prose is sufficient. | `AgentTaskRole` ends at `TestDesign = 15`; `IsStage` and its EF expression enumerate five roles. `PipelineHandoffKind` ends at `None = 7`; formatter/parser/mapping use a closed vocabulary. CLI ValidateSets, complexity routing and client maps enumerate roles too. | Add the role and destination through all public surfaces; preserve old numeric values. |
| `verify` is available for the new stage. | `PipelineHandoff` explicitly aliases `verify` to Review, and `OrchestrationStage.Verify` already names the landing/test outcome axis. | Use `mutation`; retain both existing meanings of verify. |
| `-OnAgent` reliably preserves a Code worktree session. | `AgentTaskService.CreateAsync` keeps requested Role but forces a live follow-up to Shared and inherits the model/kind. `TryReuseWarmAgentAsync` refuses Shared reuse of a `card-task-<8 hex>` worktree delegate (CARD-0221). A retired-agent fallback inherits report context but does not itself select the old worktree. | Same-agent continuation is not the simplest reliable default on this checkout. |
| A changed Role updates a running session's bundle. | `InstructionBundles.ForDelegate` composes at launch. Warm reuse does not generally deliver the new stage bundle; Grok also checks its rules receipt. `DelegateBundleLaunchTests` pins launch-only bundle delivery. | Choose a fresh launch in the retained Code worktree. Do not add live bundle injection or change the reuse safety guard. |
| A new `-Worktree` started from Code's directory necessarily includes Code's commit. | Worktree creation uses the merge target/master lineage, not an unlanded sibling task branch (CARD-0215). | Do not use `-Worktree` for the Mutation recipe and do not land Code early merely to supply the source. |

## Decisions

### D-1: Mutation is a writable pipeline role

Append `AgentTaskRole.Mutation = 16`. Include it in both `AgentTaskRoles.IsStage` and `AgentTaskRoles.Stage`. It is a Worker stage, not a specialist or a helper. Add `RolePolicy["Mutation"]` with `Level = Frontier` and `RecommendedInFlight = 1`, keeping the existing timeout defaults and normal kind/pin/complexity resolution. Frontier matches the current Code tier: evaluating whether a mutant failed at the intended assertion and adding a missing control require judgement beyond Test's run/report remit.

Mutation participates normally in writer leases, scope handling, checks, task/card transitions and project/global capacity limits. Do not special-case it out of any cap. Do not clone, clear or migrate live Code routing pins into Mutation. Add it to `ComplexityRoutingService.RoutableRoles` and the routing/complexity CLI role selectors so the independent policy is actually configurable.

Writable means the dispatch uses Shared in the explicitly retained, isolated Code worktree. Reject an explicitly ReadOnly Mutation request with the existing 422 validation pattern; do not silently upgrade access. Do not change Review's or Test's access contract.

No schema change is expected: Role and NextStage are existing enum columns without a closed database constraint in the inspected mapping. Keep every previous ordinal stable; confirm schema/converter parity in TestDesign. Do not create a migration just to add enum members.

Rejected: reuse Code (same bottleneck), Test (non-editing helper), Review (read-only), Custom/Debug as the permanent stage (no distinct standing contract or pipeline destination), and Verify (breaks an established alias and adds ambiguity).

### D-2: Add the canonical destination `mutation`

Append `PipelineHandoffKind.Mutation = 8`. Add `mutation` to the parser's case-insensitive vocabulary, `Token`, and `TryToStageRole`. Add it to `DelegationReportFormatter.StageHandoffContract`, while retaining the 400-character handoff and existing contract-size budget. No new aliases are needed. `verify` still parses and normalizes to `review`; `test` remains outside the next-stage vocabulary.

Update client Role and PipelineHandoffKind unions, role picker, stage label, and task-detail next-stage token map. Mutation must appear in the pipeline and routing UI and receive ready rows from a settled Code report. The completion header remains the orchestrator's dispatch signal. A missing block still settles as `next=unmarked`; unknown tokens remain visible diagnostics. Do not add a PC-report parser or an automated dispatch engine.

### D-3: Code owns implementation and ordinary verification

Replace the Code bundle's PC loop and missing-PC requirement with these obligations:

1. Implement the plan, including the tests specified by its verification design.
2. Run every V-n and R-n; report every ID and its actual outcome. A shared command may cover several IDs, with the mapping stated.
3. Commit and push each meaningful slice and the final ordinary-tested state. Report the full commit SHA and worktree/branch. Finish all running commands before settlement.
4. Report all PC IDs as pending for Mutation, carrying the plan artifact and any newly noticed coverage gaps. Code must not execute deliberate mutants as part of its default completion criterion.
5. Set `next: mutation` when implementation and V/R are complete, including when the plan lists zero PCs. Mutation then confirms applicability and checks for missing PCs. `next: code` means implementation or ordinary verification work remains; `next: decide` means a human choice is required. Carry required Review and restart information through Mutation.

Code cannot settle `next: land` under this default, and a hard/safety-critical Review requirement does not bypass Mutation. Substantial plan deviations are reported as `review-required: yes`, then pass through Mutation before Review. Review remains an escalation, not a substitute PC runner.

### D-4: Mutation owns every PC and missing-control discovery

Create `server/Bundles/stage-mutation.md`, mapped by `InstructionBundles.StageMutation` and `StageKeyFor`. Keep the stage bundle ASCII, provider-neutral and within the existing 2,500-character cap. Use `delegate-basics` and the testing owner for detailed execution mechanics rather than duplicating them.

The Mutation contract is:

- Read the plan/verification design and Code's full report. Verify the reported base SHA equals this worktree's HEAD and that the index and tracked source are clean before changing anything. Record the starting state of untracked files and outputs. Missing SHA, dirty source, a moved branch or an absent worktree is an unresolved precondition, not permission to switch to master or overwrite changes.
- Run each PC-n and every separately named variant: establish green as needed, apply exactly the specified deliberate defect, observe the intended assertion-level red, restore the fixed bytes, refresh timestamps/rebuild, and observe restored green. Run each PC's exact methods; zero tests, build errors and fixture errors do not count as red. Report the expected assertion as well as counts and evidence paths for both arms.
- Add and name a missing PC for a touched guard. Keep this obligation in Mutation, including the zero-PC-plan case. A control that can use an existing assertion may be added to the plan and executed here. If adequate detection needs production or test repairs, restore all mutants, record the gap, and return `next: code`; Code implements the repair and reruns affected V/R before another Mutation pass. Do not quietly rewrite assertions to make a PC succeed.
- Mutation may retain a plan/evidence amendment and commit/push it after restoration. It does not retain deliberate defects or take over feature implementation. For an amendment-only commit, report both the tested implementation SHA and the final branch SHA, and establish that production/test files did not change. A production/test change requires Code's ordinary verification and a new Mutation pass against the resulting SHA.
- Before returning: await every owned command, restore every mutation, establish a clean tracked source/index, preserve the per-PC evidence, and report the exact final SHA. Leave the delegated worktree for landing. An interrupted run reports any remaining mutation precisely and cannot claim completion.

Successful Mutation uses `next: review` when the card is hard/safety-critical, Code requested Review, or the pass found a substantive plan deviation that needs judgement; otherwise `next: land`. A surviving mutant/missing detection uses `next: code` with the defect and exact test/guard named. An inadequate verification design uses `next: test-design`; a human choice uses `next: decide`. Operational inability to obtain the required evidence is reported truthfully as failed/blocked, never green. Review judges the implementation and PC evidence read-only; remove its instruction to run deliberate PCs.

Existing optional method-scoped batching/sharding guidance remains an execution capability of the PC runner. This card adds no new sharding infrastructure and does not require sharding: its purpose is concurrency between different cards' stages, not concurrent mutations of one card.

### D-5: Fresh dispatch in Code's retained worktree

Use a new task and fresh delegate process against the committed, still-unlanded Code branch. The default recipe is:

```powershell
$mutationGoal = Get-Content -LiteralPath '<prepared-brief-file>' -Raw
pwsh -NoProfile -File scripts/delegate.ps1 -Role Mutation -Card CARD-nnnn -Title "mutation checks <card>" -Shared -Dir <code-worktree> -ExpectAbout <pc-floor+analysis> -Goal $mutationGoal
```

`-Dir` is the actual Code task WorktreePath, not its parent WorkingDirectory. The prepared brief contains the prior handoff verbatim, artifact path, Code task ID (the landing owner), full Code commit SHA, branch, PC IDs, V/R evidence/report reference, Review requirement, and restart target. The caller uses the same commissioning project scope; a card ID or filesystem directory does not override `ProjectId`.

Do not pass `-OnAgent`, `-Agent`, `-ReadOnly` or `-Worktree` in this recipe. The existing worktree-name reuse guard makes this normal Shared dispatch take a fresh launch, which composes `stage-mutation`. Pin this behavior with a real dispatcher/launch-contract test. Keep the current CARD-0221 guard and launch-only bundle contract. A configured standing-agent pin or a nonstandard/shared Code checkout needs an explicitly isolated source and a confirmed Mutation composition; do not silently reinterpret this recipe or bypass the pin. Such a fallback is outside the default path.

The `Shared` flag describes the new task's relationship to an existing directory; the directory is still Code's isolated Git worktree. The previous Code task must be terminal and have no running commands. Never run another task in that exact worktree while Mutation is active. The next card's Code uses its own `-Worktree`, so ordinary scope overlap warns rather than serializes it. Additional global caps, provider capacity and shared-resource constraints can still hold it; this change only removes the Code-role bottleneck. Same-assembly process limiters are not cross-process locks: unsafe PC cases requiring shared external state remain coordinated/serial.

Do not land or remove Code's branch before Mutation/Review finish. When Mutation reports `next: land`, invoke `delegate.ps1 -Land <code-task-id>`, not `-Land <mutation-task-id>`: the Mutation task is Shared and does not own a new worktree. Carry that original owner ID in every subsequent handoff, including Review. A repair dispatch also uses the retained branch; carry its original Worktree owner forward. Landing's existing rebase/verification policy still applies.

Rejected: land Code first and mutate master; start a fresh sibling Worktree assuming it contains the unlanded Code commit; or redesign same-agent relaunch, retired-agent fallback, live bundle refresh and worktree ownership just to preserve context. A later optimization may make OnAgent safe, but it is not required for the throughput improvement.

### D-6: Keep handoff context compact and explicit

Code's full report carries V/R outcomes, pending PCs, full SHA, branch/worktree, plan path and evidence locations. Its one-line handoff can be:

```text
Mutation: code-task=<id>; sha=<40-hex>; PCs=PC-1..PC-n; review-required=no; restart: server. Use the retained Code worktree and plan; after PC success land code-task=<id>.
```

The `artifact:` field remains the repo-relative plan path. It is not a JSON manifest or a path to raw test logs. The caller reads the full preceding report when necessary and builds the next brief; it does not infer a ready stage from prose. No new persistence fields or public SHA/worktree-base flags are needed for this default.

Do not introduce a new `OrchestrationStage` enum member. Mutation is an AgentTaskRole and a PipelineHandoffKind. This recipe omits `-Stage`; ordinary role/task reporting is sufficient. An operator may explicitly use the existing `-Stage Verify` for outcome accounting, which remains a separate axis. Keep `verify -> review` and the existing `-OnAgent -> FollowUp` telemetry default unchanged.

### D-7: Update every standing explanation of the default

The pipeline becomes Investigate (when needed) -> Plan -> TestDesign (separate for medium/hard) -> Code -> Mutation -> optional Review -> Land. Plan is never skipped. Easy has three standard dispatches; medium has four to five; hard has five to six, depending on Investigate. Mutation has its own WIP recommendation of one. Plan-side and Code backlog rules are otherwise unchanged.

Update the delegate skill's role table, stage recipes, vocabulary, timing guidance and landing-owner explanation; `docs/orchestration-loop.md` cycle, role table, folded-verification discussion, complexity shape and WIP discussion; `stage-test-design.md` PC ownership and cost instructions; `stage-review.md`; the orchestrator bundle's stage vocabulary; bundle README; and the AGENTS.md short stage list. Rename the testing guide's Code-stage PC section to Mutation-stage and change actor-specific references there. In delegate-basics, qualify PC execution as belonging to the Mutation runner so Code does not inherit a competing obligation. Preserve the method scoping, restoration, foreground and commit safety rules.

TestDesign's Cost block separates ordinary V/R floor from PC floor and names the suites/filters on each side. Code's ExpectAbout is authoring plus ordinary V/R; Mutation's is PC execution plus analysis/reporting. Preserve the existing total verification-floor information for older consumers. Do not rewrite historical plan documents wholesale; a new Code brief must explicitly state this ownership split when executing an older plan that says Build runs PCs.

### D-8: Roll out without requiring the new role on the old server

This card is self-hosted: editing the worktree does not make the running server recognize Mutation or `next: mutation`. Land the Plan, then the separate TestDesign artifact, before Code is dispatched, as usual.

For CARD-0470's own implementation only, Code's brief explicitly overrides the old folded-PC bundle: implement, run V/R, commit/push, stop. Until the runtime is upgraded, Code reports the existing `next: decide` token and names the bootstrap action. The caller dispatches a **separate Debug worker** in the retained Code worktree with the complete planned Mutation contract and PC evidence requirements in its file-backed `-Goal`, using the same read-then-pass pattern as D-5. Debug is write-capable and has a distinct project/role bucket; do not substitute read-only Review or Test. This temporary worker reads the new bundle as an artifact because the running server cannot compose it yet. Its helper report can use the existing `next: land` or `next: code` vocabulary.

After this separate PC pass and any required Review, the caller lands the original Code worktree and restarts the server from the canonical checkout using the runbook; rebuild/serve the client containing the new role maps. Check the loaded role/bundle/pipeline surface directly, not just health. Only then emit or dispatch `mutation` as the default. Do not change live pins, provider holds or shared-stack ports to make rollout succeed. No runner restart is intrinsically required by this role/bundle/client change.

Do not reparse or rewrite historical reports/tasks. Old `verify` reports continue to mean Review. In-flight agents keep their launch instructions; if adopting the split on one, give the ownership change explicitly in its authorized task brief. Finish already-running mutation cycles before changing that task's work. Subsequent fresh launches receive the new bundles.

## Implementation slices

| Slice | Files / work | Required test surfaces |
|---|---|---|
| S1: Role and destination | `server/Domain/Enums/AgentTaskEnums.cs`, `PipelineHandoffKind.cs`; `Application/Settings/DelegationSettings.cs`; `Application/Services/{PipelineHandoff,DelegationReportFormatter,ComplexityRoutingService}.cs`. Add Mutation to both stage predicates, its exact policy, token and ready-role mapping. | `PipelineHandoffParseTests`, `DelegationUnitTests`, `AgentTaskConcurrencyLimitTests`, `AgentTaskPipelineStatusTests`, `ComplexityChainRoleTests`/`ComplexityChainServiceTests`. Exercise actual create/DB projection, not just enum membership. |
| S2: Executable stage contract | `server/Bundles/stage-mutation.md`, `stage-code.md`, `stage-test-design.md`, `stage-review.md`, `delegate-basics.md`; `InstructionBundles.cs`; `AgentTaskService.cs` for ReadOnly Mutation validation. Include Mutation in the Shared retained-change commit instruction if appropriate, explicitly after restoration. | `InstructionBundleTests`, `DelegateBundleLaunchTests`, `GrokRulesCompositionTests`, `DelegationUnitTests`, `AgentTaskServiceIntegrationTests`. Verify role-specific instructions, no conflicting Code/Review PC obligation, ASCII/size budget, provider-neutral composition, normal report/pointer and readonly validation. |
| S3: Dispatcher/workspace acceptance | Preserve existing fresh-worktree and writer-lease mechanisms. Add behavior coverage for the chosen Shared-in-Code-worktree recipe. Production dispatcher changes are unnecessary unless this acceptance test disproves the inspected path; return to Plan if a new worktree/session mechanism is required. | `AgentTaskPoolTests`, `DelegationScopeHoldTests`, `AgentTaskPipelineStatusTests`: new Mutation task launches its bundle in the correct directory; settled Code stops occupying Code; a different card's Worktree Code is admitted/dispatched during Mutation; a second Mutation is capped. Verify the source branch/HEAD remains the reported commit. |
| S4: CLI and client parity | `scripts/{delegate,routing-pin,complexity-chain}.ps1`; `client/src/api/agentTasks.ts`; `features/orchestrator/pipelineStageModel.ts`; `features/delegations/TaskDetailBody.tsx`; pipeline fixture and affected role-list fixtures/stories. Inspect home ready labels and routing UI consumers for exhaustive lists. | PowerShell ValidateSet/API-body tests using an intercepted endpoint (no real dispatch), `pipelineStageModel.test.ts`, `PipelineStagesPanel.test.tsx`, `RoutingSettingsTab.test.tsx`, relevant task-detail test, fixture contract checks and client type/build validation. |
| S5: Standing guidance | `.claude/skills/antiphon-delegate/SKILL.md`, `docs/orchestration-loop.md`, `docs/testing-and-build.md`, `server/Bundles/{README,orchestrator}.md`, AGENTS.md. Explain fresh dispatch, exact SHA, separate costs, original landing owner, Review placement and bootstrap. | Focused bundle/role contract checks plus a consistency review for obsolete active claims that Code/Build runs PCs. Historical artifacts are excluded from this sweep. |

Keep `server/Infrastructure/Data/Seeding/DatabaseSeeder.cs`'s BMAD workflow-template steps separate: their `test-design`/`code-review` strings are workflow template IDs, not this AgentTaskRole pipeline. Do not rename them because they match a search. Do not add areas, migrations, a second dispatch engine, new board columns or live routing configuration.

## TestDesign handoff

Produce the executable `## Verification design` in this document in a separate dispatch. Name each V/R case, exact existing or proposed test method, expected assertion, and each localized PC mutation. The following are acceptance obligations, not a substitute for that section:

- End-to-end role admission in one isolated project: settled Code A -> working Mutation A; Code B creation/dispatch succeeds with normal limits; concurrent Mutation B is refused on the Mutation role axis. Cover the reverse occupancy order, distinct projects and null project. The absolute cap must still refuse when exhausted. Use isolated test schemas and no production runner.
- A Code report carrying `next: mutation` persists Mutation, yields `next=mutation`, and creates a Mutation-ready pipeline row. A later Mutation consumes readiness; its `next: review`/`land` behaves normally. Omitting Mutation from the EF stage predicate must be detected. Preserve old verify alias, unmarked/unknown handling and non-stage helpers.
- Mutation resolves its role policy and complexity cell independently of Code, and the scripts and UI can choose/configure it. Preserve old enum serialization/ordinals. A new role must not become a specialist or lose writer-lease participation.
- Fresh launch in the retained `card-task-<id>` worktree carries `stage-mutation` across supported launch composition paths and uses the correct SHA. Pin the existing no-Shared-reuse guard rather than making an assertion against the role map alone. Explicit ReadOnly Mutation is refused; no real provider session or user profile is used.
- A meaningful report/contract check proves Code is allowed to stop after green V/R and commit while PCs are pending, Mutation owns red/restore/green and missing controls, and Review stays read-only. No forced PC execution in Code's instructions should survive through delegate-basics or the TestDesign template.
- Include targeted defect mutations for destination/role-map removal, loss of the stage EF predicate, accidental shared Code/Mutation admission counting, missing Mutation role cap, missing/wrong stage bundle, and readonly validation bypass. Each PC names one exact assertion method; do not obtain red through compilation errors or by merely deleting expected prose.
- Separate the ordinary V/R cost from the PC cost, with exact filters. Follow `docs/testing-and-build.md`: `dotnet run --project tests/Antiphon.Tests`, scoped classes for ordinary regression, exact methods for each PC, fresh nonzero execution evidence, and forward-slash alternate outputs. Use `scripts/test-client.ps1` for scoped client tests. No full assembly, browser E2E or Pty suite is automatically forced by this metadata/dispatch-contract change; justify any added scope. Process-spawning tests retain their assembly-local limiter and never overlap the Pty test assembly.
- Explicitly document this card's first-rollout Debug PC dispatch from D-8 so Code can finish without either consuming its own PC tail or emitting a token the old server cannot parse.

Plan completion is this committed artifact and the decisions above. No product files or live configuration are changed by the Plan dispatch, and no runtime behavior or test outcomes are claimed here.

## Verification design

TestDesign, 2026-09-09, against plan commit `8e387352`. This section adds executable acceptance criteria; D-1 through D-8 remain the fix design. Named `C470_*` methods and the focused `Mutation*Tests` classes below are **proposed tests for Code to implement**, not tests already run. Existing regression methods are identified separately. TestDesign changes only this document.

**Execution ownership:** Code implements the tests, executes all V/R automated checks and pre-handoff audits, commits/pushes, and reports every PC as pending. The separate Mutation worker executes every PC variant and discovers a missing control. For this card's first rollout, that worker is the separate Debug dispatch in D-8, with the same battery and restoration obligations. Code must use `next: decide` until the running server understands the new destination. Passing tests in a worktree does not prove the deployed server has upgraded.

### Proves it works now

**Harness and evidence rules.** Put new focused backend classes in `tests/Antiphon.Tests/Application/`. Use `[Category("Unit")]` for pure contracts and `[Category("Integration")]` for database/dispatcher tests. Base their arrangements on `AgentTaskConcurrencyLimitTests`, `AgentTaskPipelineStatusTests`, `AgentTaskReplyIntegrationTests`, `AgentTaskPoolTests` and `DelegationScopeHoldTests`; use the real services, not a second implementation of admission or readiness. Reuse/extract the smallest test-harness helpers needed, registering the git graph through `DelegationTestServices`. Each new integration method uses `TestDbFixture.CreateIsolatedSchemaAsync`, task/card/project IDs unique to the case, a temporary workspace, and a fresh DbContext to verify persisted state. Do not assert global counts in the shared schema. Use the real advancing clock or the documented offset clock.

`MutationDispatchTests` creates only disposable local git repositories/worktrees and uses the established fake runner/launch capture; it never starts a provider. Give it the assembly-local `[ParallelLimiter<ProcessSpawnLimit>]` because git is a child process. Script tests retain that limiter. Do not co-schedule the Pty assembly. Real `Program` HTTP tests use `AntiphonWebAppFactory` and `ProductionRunnerGuard`; no request may launch against 17204. Record the selected method names, nonzero executed counts, exit status, elapsed time, SHA and fresh result/log paths for every command. Fixture/build errors and zero executed tests are failures of verification, not evidence of behavior.

| ID | Behavior / layer | Exact proposed test or audit | Setup and expected assertion |
|---|---|---|---|
| V-1 | Stable public role and destination / unit + integration | `MutationRoleContractTests.C470_enum_and_stage_contract`; `MutationPipelineTests.C470_storage_and_http_enum_contract` | Pin the literal old Role mapping `Custom=0, Plan=1, Code=2, Review=3, Debug=4, Coverage=5, Docs=6, Commit=7, Test=8, Deploy=9, Merge=10, Check=11, Distill=12, Diagnose=13, Investigate=14, TestDesign=15`, plus `Mutation=16`; pin all eight old handoff ordinals from D-2 plus `Mutation=8`. Mutation is a stage, not a specialist or optional work. Save/reload a task with Role and NextStage Mutation through the current migrated schema; compare representative old Code/Review rows too. Read actual task/pipeline HTTP DTOs from the guarded factory: values are string `Mutation`, never integer 16/8. Use the production JSON options; a test-local converter alone is insufficient. Inspect model/migrations for unchanged integer storage and no new closed constraint/migration. |
| V-2 | Independent policy and routing / unit + integration | `MutationRoleContractTests.C470_mutation_policy_is_independent`; `MutationAdmissionTests.C470_mutation_routing_does_not_inherit_code` | Default Mutation policy exists, Frontier, recommendation 1, normal timeout fallback, Worker kind. Change Code's policy tier/cap and show Mutation unchanged. Through the real create/routing services, install a Code-only pin and distinct Mutation complexity cell, then create Mutation with no explicit kind/tier: it selects the Mutation cell. Updating/clearing that cell leaves Code's row/pin unchanged; no cell uses the normal any-role/config fallback. Assert the role is present in `ComplexityRoutingService.RoutableRoles` and usable through the real complexity/pin API, not just an enum array. All configuration is test-scoped. |
| V-3 | Separate exact-role admission / integration | `MutationAdmissionTests.C470_code_and_mutation_have_separate_slots`; `C470_second_mutation_is_role_limited` | For each open status Queued/Dispatched/Working, seed Code A and create Mutation A in the same project with room under MaxOpenTasks; reverse the order and create Code B while Mutation A occupies its slot. Both succeed without overrides. A second Mutation is refused with `concurrency_limit`, axis `role`, role `Mutation`, count/limit 1, and only the existing Mutation occupant; no new row/token/event is inserted for the rejected goal. Code remains limited to one. Parameterize the second-Mutation case over all three open statuses and verify Succeeded/Failed/Canceled/Blocked occupants do not consume that role slot. |
| V-4 | Scope and hard caps still apply / integration | `MutationAdmissionTests.C470_project_and_null_buckets_stay_separate`; `C470_absolute_cap_includes_mutation`; `MutationDispatchTests.C470_process_cap_includes_mutation` | An occupied Mutation slot in project P refuses another P, admits Q and null; an occupied null slot refuses another null and admits P. Card/Dir choices do not change the caller's ProjectId. At MaxOpenTasks=3 with three non-specialists including Mutation, another Code/Mutation request is refused on `absolute`, with unchanged rows. Two concurrent Mutation creates in the same empty project produce exactly one success and one role refusal using separate contexts and the real transaction gate. Independently set MaxConcurrentTasks=1 with Working Mutation and queued Code in another isolated worktree: a real dispatcher tick leaves Code queued and captures no launch. Settling Mutation permits one Code launch. |
| V-5 | Destination parsing and old alias / unit | `PipelineHandoffParseTests.C470_mutation_token_maps_to_mutation`; `C470_mutation_destination_maps_to_role`; `C470_verify_stays_review` | Parse `mutation`, `MUTATION` and `Mutation` before a valid report token: Kind Mutation, unchanged handoff/artifact, canonical header `next=mutation`. `TryToStageRole(Mutation)` returns true and Role Mutation. `verify`/`VERIFY` still parse, emit and map to Review; `test` stays an unrecognised raw token. Pin literal expected kinds/roles, never derive expected values from the production map under test. |
| V-6 | Settlement feeds readiness / integration | `MutationPipelineTests.C470_code_settlement_persists_mutation_ready`; `C470_mutation_completion_consumes_ready` | Feed a Code report containing a valid closing report token, `next: mutation`, full handoff and this plan artifact through the real reply settlement service. A fresh context sees NextStage Mutation/NextHandoff/artifact; captured caller note has `next=mutation`. Pipeline has one ready row targeting Mutation for that card/source task. Creating a newer Mutation consumes readiness in Queued and Working states. Settle it with `next: review`: ready Review comes from the Mutation task and no stale ready Mutation remains. Variant `next: land` produces neither ready row. The final settled-Mutation assertions MUST exercise `.Where(AgentTaskRoles.Stage)` in SQL; an in-memory predicate check or merely seeing an open Mutation row misses that guard. |
| V-7 | Explicit write-access validation / integration | `MutationAdmissionTests.C470_readonly_mutation_is_refused`; `C470_shared_mutation_is_writable_worker` | POST/create Worker+Mutation+ReadOnly returns the existing 422 validation shape naming workspace/access, before task/token/event insertion or launch. Cover a normal request and a live `FollowUpOnTask` request that would otherwise overwrite Workspace to Shared; validate the explicitly requested access before it is lost. A default-recipe Shared request persists Worker/Mutation/Shared and is included in writer scope/check/task handling. Explicit ReadOnly Review and Test remain accepted with their existing non-writing briefs. Do not make following up on a live agent the successful Mutation recipe. |
| V-8 | Fresh process in Code's exact retained checkout / integration | `MutationDispatchTests.C470_retained_worktree_launch_is_fresh`; `C470_next_card_code_dispatches_during_mutation` | Create a main repo at SHA M and a real `worktrees/card-task-<8hex>` checkout at unlanded SHA C, with a committed sentinel absent at M. Seed terminal Code A owning that checkout and an idle, otherwise reusable ClaudeCode Code agent/session **in exactly that directory**, same project/kind/tier/env, within TTL, with no reservation/pin/rules mismatch that could independently prevent reuse. Create a new Worker Mutation, Shared, WorkingDirectory equal to Code.WorktreePath, no AgentId/FollowUpOnTask and no new Worktree request. Tick real dispatcher: one fresh captured launch, new AgentId and AgentSessionId, exact checkout cwd, Role Mutation, literal stage-mutation bundle marker; old warm session is not reused. HEAD remains C, sentinel exists, tracked/index diff empty, no sibling worktree is created, original Code task retains worktree ownership. In the second method keep Mutation Working; create and dispatch a different card's Worktree Code B with same project/scope and sufficient caps. Assert its new directory differs, Code A is terminal, B launches once, and Mutation A remains Working at C. A second Mutation still refuses via V-3. |
| V-9 | Writer lease preserved / integration | `MutationDispatchTests.C470_mutation_obeys_writer_lease` | With SerialiseSharedWriters=true, two Shared writers in the same checkout and a non-allow scope hold each other in both occupancy orders when one is Mutation (other role may be Debug to avoid the Mutation role cap). Repeated ticks leave the waiting task Queued, no launch, and exactly one Held event naming the holder. Settle the holder and show dispatch resumes. The distinct Worktree Code case in V-8 only warns for scope overlap. Do not set AllowOverlap, change areas or use an all-allow docs scope to obtain either result. |
| V-10 | Actual launch composition / integration | `DelegateBundleLaunchTests.C470_mutation_claude_and_codex_launch_contract`; `GrokRulesCompositionTests.C470_mutation_rules_payload_contains_its_stage` | Drive the real BuildLaunchSpec for ClaudeCode and Codex; decode each provider's existing append/config field and assert exactly one literal `[bundle:stage-mutation v...]` before delegate-basics, no stage-code/review/orchestrator, and retained cwd. Grok's typed rules payload contains that same literal marker and exact composed bytes on its temporary disk path, with no bundle text in argv/env. Extend the existing all-stage Grok and bundle parameter rows too. Keep the <=700-character StageHandoffContract limit. Do not compute expected stage solely via StageKeyFor: a wrong map must fail. Check no provider read-only flag/config is introduced for Shared Mutation; no real profile, session, credential or TUI is used. |
| V-11 | Standing ownership contract / unit + audit | `InstructionBundleTests.C470_composed_roles_separate_vr_from_pc`; `DelegationReportFormatterTests.C470_mutation_brief_and_handoff_contract` (in `DelegationUnitTests.cs`); audit V-11 below | Inspect the **complete ForDelegate composition** for Code/Mutation/Review and the generated full brief, not just each markdown file. Pin Code's V/R+commit+pending-PC+next-mutation obligations (including zero-PC plans), Mutation's exact-SHA/clean-source/method-red-restore-green/missing-PC/final-SHA obligations and Code-return on missing detection, Review's read-only evidence review, and the original landing owner. Generic delegate-basics must qualify PC execution to Mutation. The test-design template separates V/R and PC cost floors. Mutation's brief has the stage reporting contract and no ReadOnly admonition; bundle text remains launch-only and spilled brief pointers keep the existing byte ceiling. Extend ASCII <=2,500, catalog, deduplication, provider-neutral and command-line budget tests for Mutation. |
| V-12 | Script/API parity / integration | `RoutingPinScriptTests.C470_delegate_mutation_posts_shared_retained_directory`; `C470_routing_pin_accepts_mutation`; `ComplexityChainScriptTests.C470_mutation_cell_round_trip` | Run the real pwsh scripts against the existing random-port stub API. Delegate args follow D-5 with a file-read multiline Goal (include SHA, owner ID, plan, PC list, review/restart values); captured POST has role Mutation, workspace Shared, exact directory/card and goal bytes, and absent AgentId/FollowUpOnTask, no Worktree request. Pin script PUT body has role Mutation; complexity set/get/clear target `/api/complexity-chains/Mutation/Hard` and `?role=Mutation`, preserving candidate order and provenance. Assert process exit and requests/body, not just ValidateSet source strings. Use a fake token and prevent fallback to the live server. |
| V-13 | Client chooses and displays Mutation / client unit + build | `pipelineStageModel.test.ts` test `C470 mutation rows retain label counts and target`; `PipelineStagesPanel.test.tsx` test `C470 shows mutation ready running and cap`; `RoutingSettingsTab.test.tsx` test `C470 mutation can be selected and saved independently`; proposed `features/delegations/TaskDetailBody.test.tsx` test `C470 renders canonical mutation handoff` | Fixtures use real Role/NextStage strings Mutation. Ready and running Mutation show the right label/count/recommendation and source-card/drawer destination; an empty Mutation stage follows existing idle-stage behavior. Use actual UI events to choose Mutation in the role picker/routing controls and assert saved payload role Mutation while Code's setting is unchanged. Task detail renders `next: mutation`, handoff and plan link. Add `DelegateModal.test.tsx` test `C470 submits selected mutation role` using its real role radio and Delegate button; assert the create payload carries Mutation/Shared and selected directory. `npm run build` in client passes exhaustive TS maps and creates current dist; fixture/story role lists include Mutation. Never hand-edit a fixture to hide an omitted API role. |
| V-14 | Handoff and restoration preconditions / operator contract audit | Evidence file `C:\Antiphon\verification\card0470\<implementation-sha>\contract-audit.md`, cases A-1 through A-6 below | Audit the composed instructions and prepared dispatch briefs against the scenario table below. This is a recorded human/agent judgement check, not a new PC-results parser or runtime precondition service. Code performs the instruction/recipe audit; the separate PC worker records actual starting/final SHA/status and restoration evidence. |
| V-15 | Old-server bootstrap and installed surface / script + rollout probe | `RoutingPinScriptTests.C470_debug_bootstrap_posts_separate_worker`; post-land probe B-1 below | Against the stub, the D-8 Debug recipe creates Worker/Debug/Shared in C's retained directory, no OnAgent/Agent/Worktree, with the explicit full PC contract and owner/SHA in Goal. Compare task IDs/roles to Code so its bucket is distinct. The old-runtime Code report uses `next: decide`; the helper's final block uses existing land/code tokens. Run B-1 only after the separate battery, required Review, original-owner landing and canonical server restart. B-1 is rollout-owned and reported pending by Code; it cannot be truthfully claimed from the worktree's test host. |

**V-11 / V-14 contract scenarios (write each verdict and supporting file/paragraph in the audit):**

| Case | Given | Required result |
|---|---|---|
| A-1 | Green V/R, committed SHA C, PC-1..PC-n pending; repeat with zero PCs | Code finishes its owned work and hands off Mutation, carrying owner task ID, C, branch, exact worktree, plan, V/R evidence, pending PCs, review requirement and restart target. For this card before upgrade, use D-8's decide/Debug path. No Code/Build PC requirement survives in active stage-code, delegate-basics, stage-test-design, delegate skill, orchestration/testing owners, orchestrator/README or AGENTS stage list. Historical plans are excluded from this consistency sweep. |
| A-2 | Missing SHA, HEAD different from C, missing worktree, staged/unstaged tracked edits, or previous Code command still running | Mutation does not start a defect cycle, checkout master, reset source, or land. Record the exact precondition and stop dependent work. Untracked outputs are inventoried/preserved, not blanket-deleted. All these variants must be explicit in the brief/bundle/owner combination. |
| A-3 | Green baseline then intended assertion red, fixed bytes restored and freshly rebuilt green | PC result records expected assertion, nonzero method counts, both result paths and implementation SHA. A build failure, zero-test run, fixture error, stale restored DLL or surviving mutant cannot be labelled pass. All owned commands finish before handoff. |
| A-4 | No named control for a touched guard, including a zero-PC plan | Mutation adds a named control against an adequate existing assertion and runs it. If detection needs a production/test repair, restore all mutants, report exact missing assertion/guard and hand back to Code; do not weaken assertions or retain an implementation repair under Mutation. Inadequate design goes to TestDesign. |
| A-5 | Complete PCs with no retained source edits; repeat with plan-only amendment, required Review and surviving mutant | Clean success chooses Land, or Review when required; both carry original Code landing owner. Plan-only amendment commits/pushes after restoration and reports tested C plus final SHA F, with a C..F diff proving production/tests unchanged. Surviving mutant means restored state plus next Code, never Land. An interrupted pass identifies any still-mutated paths and cannot claim completion. |
| A-6 | Caller preparing next dispatch / landing | Fresh Mutation uses actual Code.WorktreePath, same commissioning project, no OnAgent/Agent/ReadOnly/Worktree, explicit report+SHA context, and no concurrent writer in that directory. Land targets the original Code task ID through any repair/Review hops; never the Shared Mutation/Debug ID. Nonstandard checkout or standing-agent pin cannot silently fall back to this recipe. No SHA flags, automatic dispatch engine, new OrchestrationStage or runtime PC gate are implied by these instruction tests. |

**B-1, rollout-owned:** after landing, follow `docs/apphost-runbook.md` from the canonical checkout; record its HEAD and the landed Code SHA containment, restart the server, and wait for built-client watcher completion. Through `GET /api/agents/bundles` and `GET /api/agent-tasks/pipeline` on server 17202 (see the ops/API owner) inspect the loaded instruction-bundle catalog/version and pipeline role surface: stage-mutation exists with the expected content version and Mutation is visible with recommendation 1. Inspect the newly served client for the Mutation label/selection and report its current asset/build evidence. Confirm `verify` remains Review in the already-run parser regression. Run `pwsh -File verify-dev-stack.ps1 -SkipBrowser` as health support, not proof of loaded code. Do not create paid probe delegates or mutate live pins for this check. Missing loaded surface is rollout incomplete even if health is green. Code and the PC worker report B-1 as pending with its owner until the caller performs it.

### Guards the regression

These are ordinary checks owned by Code. Run the named existing class when its implementation/tests are changed; the command groups below include the contract classes touched by this plan. For broad unchanged harnesses, the precise listed regression methods are sufficient. Do not expand to the whole Application namespace or full assembly merely to cover a few named methods.

| ID | Regression caught | Test and decisive assertion |
|---|---|---|
| R-1 | Rename `verify`, break closed-vocabulary diagnostics, change handoff/attachment parsing | Entire `PipelineHandoffParseTests`: old aliases still normalize; missing block is unmarked for stages and absent for helpers; unknown retains raw diagnostic; after-token block ignored; handoff clipped at 400; invalid artifact dropped. Extend missing-block coverage to Mutation. |
| R-2 | A new role accidentally becomes furniture or changes the other bundles | Entire `InstructionBundleTests`, `DelegateBundleLaunchTests`, `GrokRulesCompositionTests`: helper/specialist/orchestrator rules, exact bundle order, version/hash/ASCII/size/argv limits, no bundle in brief and launch-only delivery remain true. New Mutation expectations must be literal, not self-derived. |
| R-3 | Admission policy isolation is replaced by global or bypassed counting | `MutationAdmissionTests` V-2..V-4 plus existing `AgentTaskConcurrencyLimitTests` methods `a_role_slot_taken_in_another_project_is_free_in_this_one`, `null_project_tasks_form_their_own_bucket`, `blocked_does_not_count_as_open`, `specialist_at_the_cap_still_creates`. Role/absolute axis and unchanged rejected-goal rows remain asserted. |
| R-4 | Retained-checkout delivery reuses a Code session or starts from master | `MutationDispatchTests.C470_retained_worktree_launch_is_fresh` checks new session/launch and sentinel at C rather than M; existing `AgentTaskPoolTests.a_queued_task_takes_over_a_warm_agent_in_its_directory` still reuses an eligible ordinary Shared pool agent. The new test's fully matching warm seed is essential: the existing recovered-worktree test uses a different queued directory and cannot alone prove this recipe. |
| R-5 | Mutation disappears from SQL readiness, or helper/land creates a spurious ready stage | Entire `MutationPipelineTests`; existing `AgentTaskPipelineStatusTests.a_plan_settled_next_test_design_is_ready_on_test_design_not_code`, `next_land_decide_none_yield_no_ready_even_with_a_plan_doc`, `a_newer_open_task_in_the_target_role_consumes_readiness`; existing `AgentTaskReplyIntegrationTests.a_settled_investigate_report_stamps_next_stage_plan_and_header_next_plan`. Assertions cover persisted values and caller header, not just report prose. |
| R-6 | Writer/read-only distinctions or overlap policies drift | `MutationAdmissionTests.C470_readonly_mutation_is_refused`, `MutationDispatchTests.C470_mutation_obeys_writer_lease` and V-8 concurrency; entire `ScopeOverlapPolicyTests` in `DelegationScopePolicyTests.cs`. Review/Test read-only accepted; Shared/Shared serialises; a Worktree pair warns. |
| R-7 | CLI accepts a role but sends another role/directory, or loses multiline handoff | Entire `RoutingPinScriptTests` and `ComplexityChainScriptTests`; assertions examine real requests and native process exit. Includes D-8 Debug body and D-5 normal Mutation body. No shell parse error counts as intended PC assertion red. |
| R-8 | Client enum changes hide a stage or rewrite its target | The five named V-13 client files, and production client build. Rendered ready/running state, saved role and canonical next token must remain correct with legacy Code/Review fixtures too. |
| R-9 | Old operational instructions put the PC tail back in Code or land the Shared task | V-11 complete-composition tests plus A-1..A-6 and B-1. These are instruction/rollout contracts; they cannot certify that an arbitrary LLM will obey instructions. Retain this limitation in evidence instead of claiming a new executable runtime enforcement gate. |

### Positive controls

**The separate Mutation worker (D-8 Debug for this card) runs these; Code does not.** Every row/letter is a separate named cycle. In the PC table, a method without a class prefix uses the class stated in the same cell. Code implements these assertions in its green test suite before handoff. Mutation edits production code, then executes the unchanged detecting test. All edits below must compile; removing an enum member/resource or deleting expected prose is not an acceptable substitute.

Before the first cycle, verify exact C, clean tracked/index state and no owned commands running; capture untracked/output inventory. Each cycle has a green baseline from Code's exact-C evidence or a fresh exact-method run, a localized deliberate defect, the specified assertion red, restoration to the saved fixed bytes, refreshed source timestamps/rebuild (including embedded bundle resources), and the same exact method green. Log mutation diff and expected-vs-actual failure. Do not commit or push a mutant. After the last cycle, all changed methods have restored-green evidence and `git diff` / `git diff --cached` are empty. Keep evidence in the external `C:\Antiphon\verification\card0470\<implementation-sha>` directory so worktree landing/removal cannot delete it; report its absolute paths.

| ID | Compiling deliberate defect (one site per variant) | Exact detecting method | Required red assertion |
|---|---|---|---|
| PC-1a | In `PipelineHandoff.Aliases`, remove only the new `mutation` dictionary entry. | `PipelineHandoffParseTests.C470_mutation_token_maps_to_mutation` | Parsed Kind must equal Mutation; actual null. |
| PC-1b | In `PipelineHandoff.TryToStageRole`, remove only the Mutation case, leaving the enum/token intact. | `PipelineHandoffParseTests.C470_mutation_destination_maps_to_role` | `TryToStageRole` must be true with Role Mutation; actual false. |
| PC-1c | In `PipelineHandoff.Aliases`, change the existing `verify` entry from Review to Mutation. | `PipelineHandoffParseTests.C470_verify_stays_review` | Expected Review kind/header, actual Mutation. |
| PC-2a | Remove Mutation only from `AgentTaskRoles.IsStage`; retain the EF predicate and bundle map. | `MutationRoleContractTests.C470_enum_and_stage_contract` | IsStage(Mutation) true becomes false. |
| PC-2b | Remove only `\|\| t.Role == AgentTaskRole.Mutation` from `AgentTaskRoles.Stage`. | `MutationPipelineTests.C470_mutation_completion_consumes_ready` | After successful Mutation->Review, Review ready row must name the Mutation task and stale ready Mutation must be absent. Missing SQL stage makes one/both assertions fail. Merely checking open rows is not this control. |
| PC-3 | Replace `Snapshot.RoleCount`'s predicate in `DelegationOpenGate` with `o.Role == Role \|\| (Role == AgentTaskRole.Code && o.Role == AgentTaskRole.Mutation) \|\| (Role == AgentTaskRole.Mutation && o.Role == AgentTaskRole.Code)`. | `MutationAdmissionTests.C470_code_and_mutation_have_separate_slots` | Expected successful create in both occupancy orders; actual `concurrency_limit` refusal. Wrap the create result so refusal is reported by an explicit success assertion, not an unhandled setup exception. |
| PC-4 | Change only shipped `RolePolicy["Mutation"].RecommendedInFlight` to null, leaving its tier/entry intact. | `MutationAdmissionTests.C470_second_mutation_is_role_limited` | Expected role-axis rejection and no row; actual admitted second Mutation. Do not override Mutation's cap in this fixture. |
| PC-5a | In `DelegationOpenGate.LoadSnapshotAsync`, add `.Where(t => t.Role != AgentTaskRole.Mutation)` to the counted rows. | `MutationAdmissionTests.C470_absolute_cap_includes_mutation` | Fourth create must refuse on absolute count 3; mutant admits it. |
| PC-5b | In the dispatcher's initial active-task count only, exclude Mutation from the non-specialist query. | `MutationDispatchTests.C470_process_cap_includes_mutation` | Queued Code must remain queued and launch capture empty while cap=1 is occupied by Mutation; mutant dispatches Code. Do not mutate the cap setting or query in the test. |
| PC-6 | Change Mutation's `InstructionBundles.StageKeyFor` arm to `StageCode` (keep stage-mutation resource present). | `DelegateBundleLaunchTests.C470_mutation_claude_and_codex_launch_contract` | Each decoded fresh launch must contain literal stage-mutation, no stage-code. Both provider cases fail at that assertion, rather than launch throwing for a missing key. |
| PC-7 | Bypass only the new explicit ReadOnly-Mutation rejection in `AgentTaskService.CreateAsync`, retaining all other validation. | `MutationAdmissionTests.C470_readonly_mutation_is_refused` | Expected 422/access validation with zero inserted rows; mutant accepts. Both ordinary and live-follow-up request variants must be covered; a follow-up auto-upgrade to Shared is still a failure. |
| PC-8 | In `TryReuseWarmAgentAsync`, remove only `\|\| !IsWorktreeWorkingDirectory(a.WorkingDirectory)`'s restriction on Shared reuse by replacing that whole Shared/worktree exclusion predicate with true; leave project/kind/tier/env/idle gates intact. | `MutationDispatchTests.C470_retained_worktree_launch_is_fresh` | New AgentId/session and exactly one captured launch are required; matching old Code session is reused and no fresh launch occurs. Verify the mutant actually reaches this candidate; a different-directory or rules-mismatched seed is an invalid control. |
| PC-9 | In `ScopeResolver.ParticipatesInLease` (use its existing workspace predicate), return false for Shared as well as ReadOnly. | `MutationDispatchTests.C470_mutation_obeys_writer_lease` | Waiting Shared writer must stay Queued and have one Held event in both role orders; mutant launches it. Keep declared non-allow scope and sufficient caps so neither is a competing blocker. |
| PC-10 | Remove only Mutation from `ComplexityRoutingService.RoutableRoles`. | `MutationAdmissionTests.C470_mutation_routing_does_not_inherit_code` | Real Mutation cell/pin API operation must succeed and select its cell; mutant rejects/omits it. Assert HTTP/result status explicitly before inspecting the body, so red is the intended routing assertion. |
| PC-11 | In `scripts/delegate.ps1` create-body assignment only, serialize a requested Mutation role as Code; leave ValidateSet accepting Mutation. | `RoutingPinScriptTests.C470_delegate_mutation_posts_shared_retained_directory` | Captured POST role must be Mutation; mutant sends Code. No actual delegate is launched. |
| PC-12 | Set `STAGE_LABEL.Mutation` to `Review` in `pipelineStageModel.ts`; keep exhaustive map type-correct. | `pipelineStageModel.test.ts` / `C470 mutation rows retain label counts and target` | Mutation row/header label must be Mutation; mutant renders Review. |
| PC-13 | In `TaskDetailBody.tsx` next-stage token map, map Mutation to `review` instead of `mutation`. | `TaskDetailBody.test.tsx` / `C470 renders canonical mutation handoff` | Visible canonical `next: mutation` is missing/wrong while fixture still contains NextStage Mutation. |

This is **13 PC groups / 17 specified mutation variants**, before missing-control discovery. Treat PC-1a/b/c, PC-2a/b and PC-5a/b independently; parameter cases within one method must all execute and have the expected outcomes. The worker must add at least one named missing control for a touched guard (for example a distinct request-body/routing or workspace-selection guard with an adequate existing assertion), or report the exact detection gap and return to Code/TestDesign per D-4. This requirement is not satisfied by renaming a listed variant. Do not invent a production guard solely to mutate it.

**Exact execution form:** use the following helper in the worker's foreground PowerShell session, with the class/method from each table row and a unique `arm` such as `PC-2b-red` or `PC-2b-green`. It deliberately records rather than throws on native red; the worker must inspect the fresh TRX for the specified assertion before accepting it. `--results-directory` uses an absolute producer-owned path. The output tree is owned by this worker; retain its inventory and follow the testing owner's cleanup policy.

```powershell
$implementationSha = (git rev-parse HEAD).Trim()
$pcEvidence = Join-Path 'C:\Antiphon\verification\card0470' "$implementationSha/pc"
New-Item -ItemType Directory -Force -Path $pcEvidence | Out-Null
function Invoke-C470Pc([string]$testClass, [string]$testMethod, [string]$arm) {
    $armResults = Join-Path $pcEvidence $arm
    if (Test-Path -LiteralPath $armResults) { throw "Results already exist: $armResults" }
    New-Item -ItemType Directory -Path $armResults | Out-Null
    dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c470-pc/ -- --treenode-filter "/*/*/$testClass/$testMethod" --results-directory $armResults --report-trx --report-trx-filename results.trx *> (Join-Path $armResults 'run.log')
    $runExit = $LASTEXITCODE
    Set-Content -LiteralPath (Join-Path $armResults 'exit.txt') -Value $runExit
    Write-Output "$arm exit=$runExit results=$armResults"
}
Invoke-C470Pc 'MutationPipelineTests' 'C470_mutation_completion_consumes_ready' 'PC-2b-red'
# Restore only the recorded source bytes, refresh LastWriteTime, then:
Invoke-C470Pc 'MutationPipelineTests' 'C470_mutation_completion_consumes_ready' 'PC-2b-green'
```

For PC-12 and PC-13, use `pwsh -File scripts/test-client.ps1 pipelineStageModel.test.ts -t '^C470 mutation rows retain label counts and target$'` and `pwsh -File scripts/test-client.ps1 TaskDetailBody.test.tsx -t '^C470 renders canonical mutation handoff$'`, respectively, once per red/green arm. Capture `$LASTEXITCODE` immediately and copy the wrapper's log to a new per-arm evidence path before the next invocation overwrites it. Keep these titles top-level (or include the exact describe prefix in the anchored filter); require exactly the named executed test with expected assertion red and restored pass. Skipped tests in the file do not count as executed.

Run serially by default. Many controls share PipelineHandoff, enum, service or dispatcher source files and cannot batch. Different files alone do not prove independence: a role-map defect can affect admission/launch tests in another file. Any optional batching must list independent mutations, execute only the exact affected methods and retain each red/green result. No sharding is required for this card and no sub-delegation is authorized. Never edit source while a run in that checkout is active.

### Out of scope

- TestDesign does not implement or execute these future behavior tests, run deliberate defects, modify production files, change live configuration or restart the stack. Its checks are artifact consistency and `git diff --check`.
- No new schema migration, PC-report parser, automatic dispatcher, OrchestrationStage value, worktree manager redesign, live bundle injection or OnAgent continuation optimization. If V-8 disproves the fresh-launch path, return to Plan with the failing behavior; do not expand S3 silently.
- No real paid provider/TUI launch, personal Codex home, headed browser E2E or Pty suite is required. This change is covered by actual dispatch/launch-spec boundaries, real script requests, real database projections and client render/build checks. A later provider-specific implementation change must justify additional coverage under its owner.
- No attempt to prove all parallel mutations or all shared external resources safe, change process limits, benchmark throughput, or assert that the second card must launch despite project/global/provider caps. The acceptance is removal of the exact Code-role bottleneck with all other gates preserved.
- No execution of `-Land`/restart from unit tests and no live bootstrap during Code. Original-owner landing and installed-surface B-1 belong to the caller after separate PC/Review completion. Static instruction tests do not prove future agent compliance with SHA, restoration, review or ownership rules; actual PC records and rollout evidence close those parts of the acceptance.

### Cost

**Suites forced and commands.** Run from the implementation worktree after a checkpoint commit. Backend commands use `dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c470-vr/ -- --treenode-filter <filter> --results-directory <fresh-absolute-result-dir> --report-trx --report-trx-filename results.trx`. Each invocation needs a new result directory and recorded SHA/elapsed/counts. Use these exact class filters for ordinary checks; the parenthesized suffix operands follow the pinned TUnit runner's documented syntax:

1. V-1/2/5/11, R-1/2: `/*/Antiphon.Tests.Application/(MutationRoleContractTests*)|(PipelineHandoffParseTests*)|(InstructionBundleTests*)|(DelegationReportFormatterTests*)|(DelegateBundleLaunchTests*)|(GrokRulesCompositionTests*)/*`.
2. V-1..9, R-3..6: `/*/Antiphon.Tests.Application/(MutationAdmissionTests*)|(MutationPipelineTests*)|(MutationDispatchTests*)|(ScopeOverlapPolicyTests*)/*`.
3. V-12/15, R-7: `/*/Antiphon.Tests.Scripts/(RoutingPinScriptTests*)|(ComplexityChainScriptTests*)/*`.
4. R-3..5: individual exact-method filters from the R table against the unchanged existing concurrency, pool, pipeline and reply classes; enumerate each method in the Code report. If editing one of those classes or their production behavior beyond this plan, run that touched class once as ordinary regression as well.
5. V-13/R-8: `pwsh -File scripts/test-client.ps1 pipelineStageModel.test.ts PipelineStagesPanel.test.tsx RoutingSettingsTab.test.tsx TaskDetailBody.test.tsx DelegateModal.test.tsx`; then `npm run build` from `client/`. V-11/14 and pre-restart V-15 additionally produce the named contract audit; B-1 remains an explicit rollout handoff item.

Verify each selected class/method appears in the fresh execution results. If the combined filter discovers zero tests, correct it using separate class invocations; do not widen to a namespace. Existing test class names can differ from filenames (notably `ScopeOverlapPolicyTests`), so copy the table's class names. The entire Antiphon.Tests assembly, Antiphon.Agents.Pty.Tests and browser E2E are not forced suites.

- **Ordinary V/R verification floor: approximately 20 minutes**, planning estimate, not measured in TestDesign: isolated-output build and focused backend/script groups ~12 min, client tests/build ~4 min, instruction/recipe audit and evidence ~4 min. Code owns it. Test authoring, implementation, diagnosis and commit/push time are additional; use authoring estimate + 20 min for Code ExpectAbout. B-1's restart/client-loaded observation is separately rollout-owned, reserve ~5 min after landing.
- **PC execution floor: approximately 60 minutes**, planning estimate, not measured: 15 backend variants x red/green with rebuild, schema/script/launch fixture startup and assertion inspection (~3 min each), two client variants (~2 min each), plus final restoration/missing-control baseline overhead (~11 min). Separate Mutation/Debug owns it. Discovery of a missing control, new green baselines or a slow build adds time. Use at least PC floor + ~15 min analysis/reporting for Mutation/Debug ExpectAbout; it must never be charged to Code's completion tail.
- **Total verification floor: approximately 80 minutes** (= ordinary 20 + PC 60), retained for older consumers; rollout probe adds ~5 min. Floors are scheduling allowances, not timeouts or permission to skip rows. Record observed times in the Code/PC reports so subsequent dispatches can replace estimates. Batching savings require actual evidence and cannot remove per-variant red/green results.

Code's report maps every V/R ID to actual outcomes and commands, with V-15/B-1 explicitly `pending: caller after bootstrap`, plus C, branch/worktree, original landing owner, review/restart requirements and all PC IDs pending. Its next step on this old-runtime card is D-8's separate Debug worker. That worker reports all 17 variants and its added missing-control result, restoration/cleanliness, tested C/final F, and next land/review/code using the existing vocabulary. After the runtime upgrade, future Code reports use `next: mutation` and the same evidence split without the temporary Debug role.
