# CARD-0395: durable Grok rules files and reread delivery

Status: Plan complete; TestDesign next. This is an availability repair plus a session lifecycle change, so use the hard path with Review. Runtime implementation and live acceptance are outstanding.

Evidence baseline: repository `3d0cf6e3cbe42c75a1522f7f7bce181605cddb1a`; installed `grok 1.0.13 (5e9a58528b76) [stable]`, Windows, 2026-09-06. Read CARD-0395, closed CARD-0411, and CARD-0382 through `scripts/card.ps1 get`. CARD-0411's structural multiline diagnosis is accepted, not reinvestigated.

## Decisions

### D-1. Use a file plus a short, literal bootstrap; do not claim native file expansion

There is **no verified session-specific, append-equivalent file/env transport** in the measured binary. `--rules @path` and `--rules path` succeed, but their literal strings appear inside `<human_rules>`; the file contents do not. Guessed `GROK_RULES` and `GROK_RULES_FILE` do not inject their contents. These are negative results for the tested mechanisms, not a claim that every possible Grok extension has been exhausted.

There ARE native agent-file transports: `--agent <valid-markdown-definition>`, `GROK_AGENT=<definition-path>`, and `[agent] definition = '<path>'` in an isolated `GROK_HOME/config.toml`. However, the explicit agent definition changed the builtin system prompt and tool set even with `promptMode: extend`: 24 tools became 25, `wait_commands_or_subagents` appeared, and the baseline browser-verification instructions disappeared. Do not silently replace the operator's native agent, emulate Grok's builtin definition, or couple Antiphon to an upstream prompt snapshot.

Select the next-best transport permitted by the brief: place the exact composed rules on disk; keep only a short instruction to read that file in `--rules`. Example value (one physical line):

```text
Antiphon standing rules are in "C:\Antiphon\logs\sessions\instructions\grok\<session-id-N>\rules.md". Before doing any work, read the entire file and follow it as this session's standing instructions. Re-read it when Antiphon requests a refresh and after resume or compaction. If any part is unreadable, stop and report the failure.
```

This is **model-mediated file retrieval**, not native expansion and not the full text in the system channel. The bootstrap delegates authority to a known file; its content becomes tool context when read. A real Grok file-tool round trip against the local stub read a 14,262-byte file in a path containing spaces, returning both sentinels and all 180 content lines, including non-ASCII and quotes. The stub selected the tool call: this proves the CLI/file transport, not that a real model will voluntarily obey the bootstrap. V-8/V-9 must establish that separately before anyone declares the card fixed.

Use this same transport for Grok on both supported launch backends and operating systems. Do not preserve a separate inline-rules implementation on non-Windows. Claude/Codex composition remains unchanged.

### D-2. Preserve the argv guard and separate content from argv

Keep `GrokRulesArgvPolicy`'s CR/LF/NUL, 4,096 UTF-16-unit, occurrence, alias, equals-form, and Herdr effective-env semantics. Validate user/profile/registry argv before any file materialization. The runner validates the generated bootstrap at its final effective-argv boundary before process creation; the server validates the returned path/bootstrap receipt before releasing work. Both use the shared policy. The server cannot precompute a remote runner's absolute path. No fallback to raw multiline text if a file write fails.

Replace the **composition-time raw-payload** check with typed file-content validation, because composed text no longer becomes argv. This intentionally changes CARD-0382's normal bundled-launch refusal tests to successful file transport tests; it does not weaken the existing unsafe-argv backstops. Reject NUL and unresolved `{{key:` placeholders in file content with bounded, content-free diagnostics. CR/LF are valid file contents. Preserve `InstructionBundle.Render`, `BlockSeparator`, composition order, and stamps byte for byte.

Give file content its own explicit limit: default 262,144 UTF-8 bytes, counted without a BOM. Reject, never truncate. Retain the command-line budget for the actual executable/args, including the generated reference. Do not charge the off-argv file body against `CommandLineBudgetChars`; this is an intentional successor to CARD-0382 D-T1, and its old Grok over-budget expectations must change. Claude/Codex limits stay unchanged.

When composed instructions are nonempty and a profile/definition already supplies a safe explicit `--rules`/alias, refuse a duplicate-source conflict with guidance to move the append to `SystemPromptAppend`. An unsafe explicit value still gets `grok_rules_argv_unsafe` first. Do not silently consume, normalize, or reinterpret an operator's literal path-shaped argument. Empty-composition specialists and bare agents keep their original args and create no rules file or refresh turn.

### D-3. The runner owns a stable file; the server owns the rules revision

Use CARD-0382 D-5's reserved location on the **runner host**:

```text
<SessionLogPath>\instructions\grok\<Antiphon-session-id:N>\rules.md
```

The file is UTF-8 without BOM and contains exactly the final rendered composition, with no metadata/front matter. The filename is stable across genuine native resumes of that Antiphon session. Metadata lives separately: content SHA-256, byte count, transport version, and launch generation. Never put a revision/hash into the system bootstrap: native resume retains that bootstrap, so it must remain valid when file contents change.

Carry the body as a typed optional Grok payload through `AgentLaunchComposition`, `AgentLaunchOptions`, `AgentLaunchSpec`, `RunnerGrokAdapter`, and `RunnerLaunchRequest`; it must never be hidden in an argument/env value. Other kinds reject an accidental Grok payload. The runner validates it, derives a canonical absolute path using only its configured root and the session GUID, atomically writes a sibling temp file then replaces `rules.md`, and adds the short bootstrap to effective argv before process creation. Failures occur before session registration/Herdr allocation wherever no child has yet started. Apply the existing owned-process cleanup/terminalization contract for failures after spawn.

Return a typed rules receipt (absolute runner-visible path, hash, version, generation) in the launch/session DTO. Preserve it in the runner's existing adoption/manifests metadata and persist it on the server session before allowing task/channel work. The server must not guess a remote runner path. A restart between runner start and receipt persistence recovers the same receipt from the runner; it does not assume a successful launch means rules were installed.

Add a versioned runner capability (`grokRulesFileV1`) and check it before sending this payload. Use a small explicit `/capabilities` runner endpoint/contract if the existing health DTO cannot carry capabilities cleanly. Old runners must refuse this launch server-side as `grok_rules_transport_unsupported`; silently ignoring an unknown JSON field would start an uninstructed agent. A missing/mismatched receipt is also a launch failure, never permission to send the brief.

Use the session-log directory's access controls; do not publish rules contents through logs, failure messages, a download endpoint, or launch manifests. Store only path/hash/count in receipts and diagnostics. Keep the file through stopped/resumable and pooled states, runner/server restarts, and worktree cleanup. Delete it only with deliberate expiry/deletion of that session's resume artifacts. Remove abandoned temp files within this exact directory; never globally sweep files belonging to another session.

### D-4. Fresh start and resume require an ordered reread turn

Measurement: `grok --session-id <uuid> --rules CARD0395-OLD-RULE -p ...`, then `grok --resume <same-uuid> --rules CARD0395-NEW-RULE -p ...`, both exit 0. Both resumed user-turn requests still contained OLD and not NEW in the system prompt. Rebuilding argv/stamps alone does **not** refresh Grok's native instructions.

On a fresh file-transport launch and every actual native resume:

1. Recompose current instructions, install the file, and persist its receipt and generation.
2. Create a short System-origin queue item with a unique first-line identifier, the absolute path, and expected content revision. It instructs a full read (including continuation reads for files over the tool's 1,000-line default), no task work yet, and a fixed acknowledgement identifying that revision. An unreadable/incomplete file must produce a failure acknowledgement.
3. Deliver through `SessionMessageQueueService` after the sign-in/trust/ready checks. Reuse LF normalization, bracketed paste, separate Enter, and transcript-confirmed delivery. Keep task/card boot prompts, resumed auto-continue, and channel work behind this initialization barrier.
4. Release that barrier only on the matching acknowledgement from the refresh turn, after its `UserPrompt` is confirmed and the turn completes without a provider error. Store this as **provider acknowledgement**, not independently proven byte consumption or obedience. An old-hash or unrelated acknowledgement does not satisfy it.

The refresh turn carries no task-report token. Recognize its queued origin/key before task settlement/nudges and channel reply extraction; it must neither settle an unmarked delegate nor publish acknowledgements to a bound channel. A genuine task report still settles normally after initialization. Failure/timeout surfaces a bounded rules-initialization failure and leaves work queued; follow existing launch-failure cleanup for an owned startup process. Never create an unlimited reread/nudge loop.

Keep the queue's retry and late-confirmation contract. `Sent` with null verdict is not proof; after restart, reconcile the exact first-line identifier against transcript evidence before retrying. Persist the initialization barrier so an interrupted server launch cannot release pending work prematurely.

### D-5. Compaction refresh is durable, idle-gated, and replay-safe

The card attributes `compacted` plus `event_msg/context_compacted` to Grok. Those are the **Codex** shapes in this checkout. Grok's ACP normalizer maps `sessionUpdate: auto_compact_completed` to `CompactBoundary`; `compaction_checkpoint` is bookkeeping. Use the normalized event, with a new fixture taken from a real Grok auto-compaction. Do not add a parser that waits for a Codex-shaped row or types an assumed `/compact` command.

Extend `CompactionRecoveryService` with a Grok file-rules branch, backed by a dedicated `GrokRulesRefreshService`. Every file-backed Grok session qualifies, including pool delegates; do not gate on `Agent.SystemPromptAppend` or the continued existence of an ephemeral agent row. Preserve the existing Claude incident/channel recovery path.

Create the refresh queue row and its durable trigger record in one transaction. A nullable `RulesRefreshKey` on the queue row with a unique `(AgentSessionId, RulesRefreshKey)` index is sufficient: launch keys use generation; compact keys use the persisted transcript-entry identity. Store the expected rules revision and satisfy/cancel state with that item. Do not reuse the current watermark-then-enqueue pattern: a crash between those steps permanently loses a reminder. An in-memory latch can optimize only after the durable transaction commits.

Trigger from BOTH live ingestion and catch-up/replay. `AgentSessionRuntime.SyncTranscriptAsync` currently recovers turn ends/manual-compaction flushing but does not dispatch Grok auto-compaction recovery. A persisted pending refresh must be retried even when every incoming transcript row deduplicates. At server startup, reconcile active file-backed sessions for unhandled compact boundaries and unfinished launch barriers. This covers a crash after transcript persistence but before queue creation.

Queue compact refresh `WhenIdle`, never `Now`: Grok auto-compaction happens mid-turn. It is not a turn end, must not settle a task, and must not interrupt a tool call or synthesize an Enter. At the next genuine idle window, give the refresh precedence over pending ordinary work without interrupting a delivery already in progress. If several boundaries accumulated in one busy turn, one reread of the latest file may satisfy the pending group; persist the covered boundary identities so replay cannot create duplicates. A new boundary after an acknowledged refresh needs another refresh.

Use the same acknowledgement and transcript-delivery machinery as D-4. Do not autoretry a successfully acknowledged reread merely because its own turn compacted: coalesce that boundary into the read that occurred after it, or schedule at most one necessary later reread, with explicit loop detection. A refresh failure on an already working session is an incident/blocked delivery decision, never an automatic session kill.

This is recovery **at the next idle window**, not reinjection before Grok's first post-compact API call inside the current turn. Retention measurements in V-9 determine whether that interval is acceptable. If the bootstrap vanishes or the model acts contrary to rules before idle, stop acceptance and return to Plan; do not claim equivalence to a native SessionStart hook.

### D-6. Pool, legacy sessions, and drift

For an already file-backed warm pool session, reuse requires the desired bundle/content revision to match its installed receipt and initialization to be acknowledged. A mismatch takes the normal cold-dispatch/idle-retirement path; never overwrite rules underneath another task. An empty-composition specialist remains eligible for its existing empty lane and does not acquire Worker bundles to make a test pass.

Legacy native sessions may retain baked-in inline rules across resume; those can contradict a newly read file. Do not claim to replace their system prompt with a typed message. New cold delegates use a fresh native ID. Keep running legacy sessions owned and untouched. Do not reuse legacy bundled pool sessions for new work; retire them at their normal idle opportunity. A standing legacy native resume with nonempty desired rules and no v1 receipt must refuse with a specific migration explanation and require an explicit fresh start through the existing start API: equivalence to its baked-in rules is not established. It must not silently discard history or silently change `--resume` to a fresh identity. The operator may arrange a checkpoint/handoff before that fresh start. This is a one-time compatibility constraint, separate from routine resumes of sessions initialized with the stable-file bootstrap.

For transport-v1 sessions, resume re-renders the file and rereads it. For a live file change without a process launch, retain existing policy-refresh ownership: use its idle-gated actual resume, not an additional watcher writing the active file. Attachment removal to an empty composition must also clear/retire the prior rules contract deliberately; do not leave a resumed bootstrap pointing at stale instructions. TestDesign must specify the empty-transition path (fresh start for removing the system bootstrap is the default).

### D-7. Grok-only behavior; correct the Codex rationale

Leave Claude/Codex transports and compaction working-state behavior unchanged. This is based on code and provider evidence, not the mistaken cross-provider marker in the card. Antiphon's Codex normalizer already documents a real mid-turn compact with a later `task_complete`. Upstream Codex explicitly rebuilds initial context around mid-turn compaction, or clears the reference so the next turn reinjects it; local compaction also obtains base instructions separately from history. See [Codex compact.rs](https://github.com/openai/codex/blob/main/codex-rs/core/src/compact.rs) and [remote compaction](https://github.com/openai/codex/blob/main/codex-rs/core/src/compact_remote.rs).

Thus the sound rationale is **provider-owned instruction/context reconstruction**, not that every developer instruction is simply resent unchanged on every network packet. This is source confirmation and existing Antiphon measurement, not a fresh installed-Codex multi-compaction canary. Pin the no-Grok-refresh behavior for Codex in V-6; do not add another paid provider integration to this urgent repair.

## Ground truth and rejected assumptions

| Question | Evidence / consequence |
|---|---|
| Can prose flattening restore dispatch? | No; CARD-0411 confirms mandatory bundle header LF and shared block separators. Do not modify them. |
| Does successful `--rules @path` prove reading? | No; captured literal `<human_rules>` contains `@C:\...\rules with spaces.md`, no file sentinels. |
| Does a native definition file work? | Yes, by CLI, env, and config with valid front matter; rejected as an append replacement because it changes the builtin agent. |
| Do home rules offer session isolation? | No. Tested isolated home `rules/antiphon.md` and `AGENTS.md` were not discovered in this setup. Global/cwd injection is unacceptable even if enabled: agents sharing a home/cwd would load each other's instructions. |
| Does `--resume` apply new `--rules`? | No in the measured binary: OLD remains; NEW absent. Stable path plus explicit reread is necessary. |
| Where are composed rules built? | `AgentSessionLaunchComposer.ComposeForAgentAsync` (including `ChannelPreamble.Render`) and `AgentTaskDispatcher.ComposeDelegateArgs`. Both currently put composed text on argv. |
| Where can raw overrides bypass composition? | `AgentSessionService.BuildRuntimeLaunchSpecAsync`, then `SessionRunnerRuntime.StartAsync`; retain both final guards. |
| Does the existing recovery cover this? | It is named-agent/SystemPromptAppend-gated; watermark saves before enqueue, and auto-boundary catch-up is missing. It cannot be reused unchanged for durable pool refresh. |
| What did local wire capture actually see? | Grok 1.0.13 / `grok-4.6` used `/responses` for the real tool-bearing user turn as well as helper calls. Distinguish by body/nonce/tools, not just URL. Existing tests' older `/chat/completions` assertion needs a measured compatibility update. |

The detailed measurement record is [CARD-0395 transport measurements](../../investigations/2026-09-06-card-0395-rules-transport-measurements.md). No live production agents, wrappers, auth stores, board routing, or shared stack configuration were changed by Plan.

## Code slices

| Slice | Files / work | Required verification |
|---|---|---|
| S1: payload and store | `server/Application/Dtos/AgentLaunchSpec.cs`, `AgentSessionLaunchComposer.cs`, `AgentTaskDispatcher.cs`, `AgentRegistry.cs`, `AgentTuiLaunchResolver.cs`; new Grok payload/policy helpers; `src/Antiphon.SessionRunner.Contracts/SessionRunnerContracts.cs`; `RunnerGrokAdapter.cs` and client plumbing; new runner `GrokRulesFileStore.cs`, `SessionRunnerRuntime.cs`, DTO/adoption metadata and capability route. Keep shared composition and guard algorithms. | V-1, V-2, V-3. No old runner may silently ignore the payload. |
| S2: initialization and revisions | Session receipt/generation fields; new `GrokRulesRefreshService.cs`; `AgentSessionService.cs` fresh/card/resume/interrupted launch paths; `SessionMessageQueueService.cs` initialization barrier and refresh ordering; `AgentTaskReplyService.cs`/channel dispatch exclusion for refresh turns; DI registration and CLI-generated EF migration. | V-4 plus real CLI initial/read/resume wire canary. Do not queue ordinary work before receipt/ack. |
| S3: compaction and reuse | `CompactionRecoveryService.cs`, `AgentSessionRuntime.cs` live and sync paths, queued refresh idempotency/transaction/index, startup reconciliation, `AgentTaskDispatcher.cs` warm eligibility; narrowly extend `GrokTranscriptNormalizer.cs` only if actual measured ACP shape demands it. | V-5, V-6; crash/replay and settlement controls are mandatory. |
| S4: restore coverage and prove behavior | `GrokDelegateEndToEndTests.cs`, `GrokDelegateDispatchTests.cs`, `GrokRulesLaunchRefusalTests.cs`, `DelegateBundleLaunchTests.cs`, `DelegateLaunchArgvIntegrityTests.cs`, `CardSpawnModelArgumentTests.cs`, `CodexDelegateDispatchTests.cs`; runner guard/store tests and real CLI canaries. Bring stub Responses SSE schema up to the measured version when needed. Update `docs/agent-kinds.md`, `docs/session-runtime-invariants.md`, `docs/ai-agent-tui-configuration.md`, `docs/herdr-sessions.md`, HTTP owner if adding capability route. | V-7, V-8, V-9 and named existing regressions; Code reports every item, skip, failure, and positive control. |

S1 and S2 are the minimum deployable availability slice; a file-pointer launch without initialization ordering is not shippable. S3/S4 remain part of this same card and must complete before closure. Land the Plan before dispatching TestDesign, and land TestDesign before Code so sibling worktrees contain the artifacts.

## Verification design brief for TestDesign

TestDesign must turn these into named runnable tests, fixture shapes, exact assertions and red-then-green positive controls. It must retain the distinction between process launch, confirmed typed delivery, provider acknowledgement, complete file read, and actual model behavior.

| ID | Required proof |
|---|---|
| V-1 | Real production composition for Worker plus a pipeline stage, standing attachment + non-Normal style + append + bound-channel rendering, and card-spawn. Exact final rendered UTF-8 file content, unchanged bundle stamps/order, body absent from every argv element, short bootstrap passes policy. Include CR, LF, CRLF, quotes, `$`, backticks, non-ASCII, marker-like prose, >4,096 units, >old argv budget, byte-limit boundary, >1,000 lines, NUL and secret-marker rejection. No handcrafted single-line substitute for delegate-basics. |
| V-2 | Runner-host materialization on PtyHost and fake-Herdr: spaces/Unicode path, hash receipt, atomic replacement, no stale/partial file on injected write failure, resume same path/new hash, two sessions same cwd stay isolated, adoption/restart retains receipt, worktree deletion does not remove file. Missing capability/receipt fails before work; an old runner cannot produce an uninstructed successful launch. |
| V-3 | Preserve all CARD-0382 unsafe **raw argv** cases server and runner, including aliases/equality/duplicates/Herdr env expansion versus PtyHost literal env tokens, NUL, 4,096/4,097 boundary, kind/OS branches and no runner/pane effects. Path-shaped safe arguments still pass policy unchanged. Validate source-conflict precedence and safe bare/specialist passthrough. File write failure never falls back to inline. |
| V-4 | Initial and resume barrier: ready/trust handled before typing, exact revision ack only after confirmed UserPrompt, work/auto-continue/channel messages held then released, unknown or stale ack ignored, provider error/read failure/timeout cannot release work, restart before/after receipt and before/after queue Sent recovered once. Refresh turn cannot settle/nudge an unmarked task or send a channel acknowledgement. Include legacy-native migration refusal without losing history, and transport-v1 resume updating the file. |
| V-5 | Actual ACP auto_compact_completed fixture -> normalized boundary -> durable System refresh for standing AND delegate. Mid-turn stays working and no input is injected until real idle; live/sync-only/replayed/rebased sequences all yield one logical refresh. Inject crashes at transcript save, trigger/queue transaction, send, and ack boundaries; replay must neither lose nor duplicate it. Multiple boundaries coalesce correctly; another real compact after ack causes another read; refresh-induced compaction cannot loop. |
| V-6 | Claude manual/auto recovery, Codex compact/task_complete timing and developer-instruction argv unchanged. Empty specialists create no file/refresh; warm reuse with matching hash succeeds, mismatched/legacy bundled warm sessions cannot accept new work. Task settlement, channel follow-up exclusion and process ownership stay correct. |
| V-7 | Restore all three Windows Grok delegate E2E behaviors lost under CARD-0382: normal priced settlement, unmarked/nudge settlement, and boot-stall retry. Remove the two CARD-0382 skips. fakegrok models the new bootstrap/ack/receipt contract rather than bypassing it. Keep unsafe-profile refusal E2E as a separate negative control. |
| V-8 | REAL grok.exe dispatched through the REAL delegate.ps1 -> server -> runner -> transcript -> settlement path, with normal multiline goal and actual stage + delegate-basics rules. Run PtyHost plus an isolated Herdr standing-agent attachment case. Confirm file read and final task report, no clap error/refusal, Grok detection before Herdr timeout, pane retained. Run a zero-spend wire arm first, then an authenticated real-model compliance arm. A stub's scripted read/ack is not the live behavioral verdict. |
| V-9 | In a real headed Grok session, observe at least TWO genuine auto-compactions and capture their native ACP identities. Measure baseline single-line `--rules` canaries and the new file bootstrap separately: before compact, first post-compact continuation (before idle refresh), after acknowledged refresh, after second compact. Ask neutral tasks whose required output depends on early/middle/tail rules sentinels, not on repeating their answers in the prompt. Change a rules-file sentinel, genuinely resume, then verify the new answer. Record survival/loss/mixed results and whether refresh repaired them. No conclusion from just seeing a boundary or a reread prompt. |

Positive controls must include: replace file text with a truncated body; put raw multiline content back on argv; remove each raw-argv backstop separately; let an old runner ignore payload; release work before the ack; save trigger before enqueue and crash; omit sync handling; classify auto compact as TurnEnd; remove refresh-turn settlement exclusion; and make the real CLI pointer refer to a missing file. Each must fail a meaningful named assertion, then pass when restored.

### The real dispatch acceptance run

Build an isolated test server/runner graph using `RealCliStubBServerHarness`, `DirectSessionRunnerClient` or the existing isolated runner fixture; do not boot real `Program` against production runner 17204. Give the process lane its assembly-local `ParallelLimiter<ProcessSpawnLimit>` and headed tests their existing opt-ins and `NotInParallel("Headed")` group. `RealCliStubEnv.ForGrok` remains the only committed test env builder: both redirect variables must be present, and a synthetic `/api-key` hit plus a nonce in the **actual tool-bearing user-turn** request must reach the stub. Extend its SSE fixture if required, including usage details, sequence numbers and text annotations; do not make a permissive endpoint-only assertion.

Create a normal multiline goal file in the disposable repo (several paragraphs, quotes, at least 5 KB, independent start/middle/end task markers). Invoke `delegate.ps1` as a PowerShell script **in process** so the long Goal does not itself cross a Windows native argv boundary:

```powershell
$goalText = Get-Content -LiteralPath $goalFile -Raw
& .\scripts\delegate.ps1 Code -Kind Grok -Level High -Dir $disposableRepo -Worktree -ReadOnly -Goal $goalText -Title 'CARD-0395 transport acceptance' -NoInheritEnv
```

Supply the isolated test API/task identity through the harness environment; for the later real-service acceptance use the authorized task/capability identity without changing routing pins or bypassing quota/auth refusals. A transport probe task itself must not spawn further delegates.

Record task/session/native-session IDs, CLI version, backend, rules/brief file hashes, argv metadata without content, confirmed bootstrap and brief UserPrompt identities, matching refresh acknowledgement, native read-file tool output containing all file markers, and the actual marked final report/settlement. Preserve both the full multiline brief file and evidence that the agent consumed its end marker; Grok's normalized transcript can flatten typed newlines, so compare using the existing provider matcher and the on-disk brief, not a screen redraw. A successful `delegate.ps1` create alone is not acceptance.

For real-model compliance, put a harmless response requirement only in the attached rules (distinct from the task markers); the final report must obey it. For compaction, first use the real CLI/local stub to measure how to trigger its own automatic compaction without assuming a manual command, then execute the real-model headed sequence. If auth/quota, inability to induce genuine compaction, or model noncompliance blocks V-8/V-9, report the exact remaining gate and retain the card open; do not replace it with a fake boundary, a unit test, or a silent skip.

### Commands, rollout and stopping rules

Use class-scoped TUnit commands such as `dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokDelegateEndToEndTests/*"`; TestDesign supplies the final class list for server, runner, and Pty suites. Run `Antiphon.Tests` and `Antiphon.Agents.Pty.Tests` sequentially. No frontend tests/build are needed unless Code actually changes the client. Clean only verified worktree-local `bin-card0395` directories using native PowerShell after runs.

Expect several hours of Code/integration work plus real-session acceptance; repeated auto-compaction is an explicit additional runtime gate, not a five-minute unit check. Report test counts/failures and every positive control, not just an aggregate green build.

Deployment changes server, shared contracts and runner: restart both from the main checkout using the documented bootstrap scripts, never this linked worktree. Verify checkout HEAD and that both rebuilt binaries contain the new transport/refresh symbols and capability before the real dispatch. A health check alone is insufficient. Existing running legacy agents are not mass-restarted. If rollback is needed, stop creating file-transport launches and retain rules files/metadata for resumable sessions; the old guarded build will again refuse normal multiline compositions. Do not remove the guard as a rollback mechanism.

## Plan-stage validation

Completed: full card reads; owner/source inspection; installed CLI help/version; 11 final transport-matrix executions and 6 follow-up executions (one is inspect), all final exits 0; captured native file read and old-rules-on-resume behavior. Earlier probe fixture attempts timed out or exited on incomplete Responses SSE; they were corrected, not counted as passing transport cases. No repository tests/builds, live-model completions, production delegate dispatches, compaction endurance run, deployment, or production configuration changes were performed.

No decision is needed to author TestDesign under D-1 through D-7. The selected fallback's model-mediated semantics and legacy fresh-start constraint must remain visible in Code/Review; neither may be described as native full-system-prompt file loading.

## Verification design

TestDesign complete, 2026-09-06, task `c2ab9503`, against fetched Plan commit `34bd0078`. The worktree was clean at that commit; the previous attempt left no changes. This section is a specification, not a test result. D-1 through D-7 and the complete transport measurement record above are the baseline. Code implements and runs the cases below, then hands off to Review. Do not repeat the settled `--rules @path`, native-agent-definition or replacement-on-resume experiments.

### Proves it works now

#### Verification contract and ambiguity register

The following pins resolve details that the fix design left open. They are proposed Antiphon contracts for Code, not claims about installed Grok behavior. Keep the original design above intact. If a pin cannot be implemented under D-1 through D-7, name the conflict and return to Plan instead of substituting an unreported behavior.

| Pin | Ambiguity in Plan | Executable verification default |
|---|---|---|
| T-1 | D-3 leaves capability endpoint selection open. | Extend the **existing** runner `GET /capabilities`, `RunnerCapabilitiesDto.Features`, with exact token `grokRulesFileV1`; do not add a second endpoint or put it under `/api`. Advertise independently of Herdr being enabled. Null capabilities, a 404, null/empty features, or missing token means unsupported. Check the selected runner, including in-process test clients. |
| T-2 | D-3 gives a path template but not receipt serialization or temp naming. | Canonical runner-owned path is exactly `Path.GetFullPath(SessionLogPath)/instructions/grok/{AntiphonSessionId:N}/rules.md`, using native separators (backslashes on Windows). Temp files are siblings `rules.{generation:N}.{nonce:N}.tmp`. Use additive optional `grokRulesPayload` and `grokRulesReceipt` JSON members through the named contracts. Payload fields: `content`, `transportVersion` (=1), `generation` (GUID). Receipt fields: `path`, `sha256` (lowercase 64 hex), `byteCount`, `transportVersion` (=1), `generation` (GUID). Use a server-created, persisted generation per actual launch/resume; retry/adoption of that same launch keeps it. Metadata belongs in the existing PtyHost manifest/Herdr sidecar and server session, under the receipt member; no separate rules metadata file or rules body in either manifest. |
| T-3 | D-2 does not name body-limit configuration or error codes other than the argv guard. | Add typed `GrokRules` settings: `MaxFileBytes=262144`, `InitializationTimeoutSeconds=480`, `RefreshTimeoutSeconds=480`; validate positive values. Body is strict UTF-8 without BOM, no Unicode/line-ending normalization. Reject NUL, unresolved `{{key:` and invalid Unicode, then an oversized byte count; never truncate. Windows argv policy retains its existing precedence and limit. The runner independently validates incoming payload and final argv. |
| T-4 | D-4 omits exact acknowledgement grammar/correlation. | First line: `[antiphon-grok-rules:{queueMessageId:N}]`. Body identifies path, generation, transport version, SHA-256 and byte count, instructs full continuation reads and no work yet. Success is a complete standalone assistant line `ANTIPHON_RULES_ACK id={queueMessageId:N} generation={generation:N} sha256={sha256}`. Failure is `ANTIPHON_RULES_FAILED id={queueMessageId:N} generation={generation:N} reason={unreadable\|incomplete\|revision_mismatch}`. Parse ordinally from assistant response text of the owning refresh turn, after chunk assembly; ignore quoted/tool/user/other-turn copies. No task-report token. A success line is **provider acknowledgement**, not a cryptographic proof that the model read or obeyed every byte. |
| T-5 | D-4 says both “keep work behind barrier” and S2 “do not queue ordinary work before receipt/ack.” | Durable task goals/inbound messages may already exist, and must survive. Do not construct/enqueue a new launch brief or resume auto-continue until initialization succeeds. Pre-existing ordinary queue rows stay `Pending` and ineligible. Under the per-session delivery lock, refuse `Now` and explicit send-now bypasses while the barrier is closed; preserve existing queued work. Receipt and refresh row must commit before any refresh send. Matching ack, confirmed refresh UserPrompt and successful turn completion must all be persisted before releasing ordinary work. |
| T-6 | D-4/D-5 leave timeout start and failure state open. | Startup deadline starts at the first successful ready eligibility; compact-refresh deadline starts at its first eligible idle delivery window, not at a mid-turn boundary. Persist the start/deadline; restarts cannot reset it. Existing queue verification/attempt ceilings govern unconfirmed delivery. A transcript-confirmed refresh is never retyped because its answer is absent. Negative ack/provider error fails immediately; an ended turn without valid ack fails after the existing bounded late-final-text reconciliation grace, within the persisted deadline. A hanging turn expires at the deadline. Persist a closed, failed rules barrier plus bounded reason. Undelivered ordinary rows remain `Pending`, held by that barrier, exempt from automatic orphan-message discard until an explicit retry/cancel decision. Owned startup failure uses existing failed-launch terminalization (`Failed`, `SystemRequest`, owned child reaped; task/run attempt fails and claim releases, card column unchanged). On an established working session, compact-refresh failure blocks further queued delivery and raises the existing incident/attention decision path; it never kills the session. No new card column, autonomous provider reroute or automatic reread loop. |
| T-7 | D-5 mentions a unique queue key but leaves group coverage and identity open. | `RulesRefreshKey` is `launch:{generation:N}` for initialization and `compact:{persistedTranscriptEntryId:N}` for a compact boundary. Enforce unique `(AgentSessionId, RulesRefreshKey)` in Postgres. One durable trigger/queue row per boundary is sufficient: designate one pending row as delivery leader and link covered sibling rows to it; covered siblings can become `Canceled` with explicit satisfied-by/covered identity metadata, never disappear. Trigger insertion, pending queue insertion and coverage changes share one transaction. Sequence is ordering, not the durable identity across replay/rebasing. Never mark a boundary covered until an acknowledged read occurred after it. |
| T-8 | D-5's refresh-causes-compaction loop policy is not bounded numerically. | A compact during a refresh can be covered by that refresh only with evidence of a complete subsequent read and its ack. Otherwise permit at most **one** follow-on refresh for that chain, persisted across restart. Another unresolved boundary caused by the follow-on fails with `grok_rules_refresh_failed`, reason `refresh_loop`, holds work and requests a decision. Independent later work-turn compactions start new chains. |
| T-9 | D-6 does not name migration refusal or empty transition. | Refuse a standing legacy native resume with nonempty desired rules and no v1 receipt **before** stop/kill, identity replacement, file materialization or queue mutation. Explain that Grok keeps old inline rules on resume, history is retained, and the operator must checkpoint/handoff as desired then use existing `POST /api/agents/{id}/start` with `{"fresh":true}`. The same explicit fresh-start requirement applies when removing all rules from a v1 session, because its native system bootstrap survives resume. Empty legacy-to-empty is unchanged. Policy-refresh sweep and manual refresh must preflight this before killing the old process. |
| T-10 | D-3/D-4 do not fully specify preflight order, remote-path validation or torn metadata. | Before runner/pane effects: existing auth/model gates; raw source argv validation; typed body validation and duplicate-source check; migration/reuse eligibility; capability check; persisted launch generation. Runner derives the path, validates bootstrap and actual command-line budget, writes/replaces the file, records receipt metadata, then spawns. Server validates receipt version/generation/hash/count and canonical absolute path grammar **for the runner OS**, plus the expected session-specific suffix and a bootstrap regenerated from that path through the shared policy. Do not stat a remote path or infer its drive/root from the server. A receipt missing after a runner restart or disagreeing with disk hash must hold/fail initialization; successful process adoption alone is insufficient. |

Pin the following bounded, content-free error surface. A synchronous server/runner refusal uses Problem Details with the stated code; a failure after an accepted asynchronous launch stores the same code in session/task failure metadata and attention, **not** a retrospectively invented HTTP response. Session state and queue state remain separate.

| Code | Synchronous status | Exact trigger / useful bounded fields |
|---|---:|---|
| `grok_rules_argv_unsafe` (existing) | 409 | Preserve existing reason/flag/occurrence/UTF-16 count/limit/env-token-name fields and precedence; never include values. |
| `grok_rules_content_invalid` | 422 | `nul`, `unresolved_key`, `invalid_unicode`, `too_large`, or `wrong_kind`; for size include byte count/limit only. |
| `grok_rules_source_conflict` | 409 | Nonempty composed rules plus safe explicit rules/alias occurrence; tell operator to move the append to `SystemPromptAppend`. Unsafe argv wins over this error. |
| `grok_rules_transport_unsupported` | 409 | No positive `grokRulesFileV1` capability evidence. |
| `grok_rules_file_write_failed` | 500 | Canonical-path, temp-write, replace or metadata-persistence failure; bounded operation label, no raw exception containing content. No inline fallback. |
| `grok_rules_receipt_invalid` | 409 | Missing/mismatched receipt, invalid path, generation, version, hash or count. |
| `grok_rules_initialization_pending` | 409 | Explicit `Now`/send-now attempted while rules initialization or its pending reread barrier is closed; no bytes sent. |
| `grok_rules_initialization_failed` | 409 | A request to deliver against a failed startup barrier; async initialization records the code with `unreadable`, `incomplete`, `revision_mismatch`, `missing_ack`, `provider_error`, `delivery_failed` or `timeout`. |
| `grok_rules_refresh_failed` | 409 | Established-session delivery blocked by a failed compact refresh, using the same bounded reasons plus `refresh_loop`. |
| `grok_rules_legacy_resume_requires_fresh_start` | 409 | T-9 legacy migration, with old session identity and explicit fresh-start guidance; no history loss. |
| `grok_rules_removal_requires_fresh_start` | 409 | T-9 v1-to-empty transition; retain old file/session until explicit fresh start and eventual artifact expiry. |

These are implementation pins rather than permission requests. Code must document the new configuration and codes in their living owners. The plan deliberately makes no promise that the model cannot falsely acknowledge; V-8/V-9 carry the separate behavioral acceptance.

#### Fixtures and harness ownership

- Use the real embedded Worker/`delegate-basics` and `stage-code` or `stage-test-design` bundles, real `InstructionBundle.Render`/`BlockSeparator`, `ChannelPreamble.Render`, and production composers. Expected disk bytes come from independently assembling the known rendered blocks in the documented order, not from the new payload/store helper being tested. Keep rules sentinels separate from brief sentinels.
- Use disposable runner roots containing spaces and Unicode, e.g. `<test-temp>\runner rules café\sessions`, separate from the disposable repo and `GROK_HOME`. Do not write rules to the checkout, native agent definitions, a shared home, or `AGENTS.md`. `SessionLogPath` differs between server and runner in remote-path tests.
- Extend `Antiphon.FakeGrok` explicitly: recognize the short bootstrap, actually read that file when the refresh prompt arrives, emit native ACP read/ack/turn-complete records, and only then process a task. Failure modes include no ack, wrong ack, failed read, partial read, compact during read, and “acknowledge rules but never answer task.” Do not bake the expected rules answer into fakegrok or have the harness seed a successful receipt/ack behind the production path.
- Add `tests/Antiphon.SessionRunner.Tests/Fixtures/grok-auto-compact-1.0.13.jsonl` and adjacent `.provenance.md`, captured by Code from a **genuine** installed-CLI automatic compaction. Retain surrounding prompt/tool/continuation/turn-end rows, native `params._meta.eventId`, `promptId` where emitted, timestamps and `tokens_before`/`tokens_after` where emitted. Sanitize content consistently without altering event shapes or relationships. Provenance records CLI hash/version, capture run, raw-artifact hash and redactions. The existing 1.0.5 real `AutoCompactCompletedRow` in `GrokTranscriptTailerTests` is useful regression coverage; fakegrok's typed `/compact` fixture is not the new capture or a live acceptance result.
- Use `RealCliStubBServerHarness`, the real `DirectSessionRunnerClient` and isolated runner fixtures; extend their capability/receipt plumbing instead of faking the feature gate. Reuse `DelegationTestServices` for service collections. Real-Program test hosts require `ProductionRunnerGuard` or their own isolated random-port runner; never production 17204. Scope Postgres assertions to the fixture's session/task/card IDs; global sweeps use ungrouped `NotInParallel`.
- Process tests have assembly-local `ParallelLimiter<ProcessSpawnLimit>`. Queue integration uses the documented moving/auto-advancing test clock; never a frozen whole-graph clock. Only isolated deadline policy tests may use purely virtual time. Live acceptance uses actual UTC timestamps and monotonic elapsed time, with no backdating.

#### V-1: production composition and the argv/file seam

Layer: unit plus server integration. Extend `GrokRulesLaunchRefusalTests` (rename obsolete success cases truthfully), `GrokDelegateDispatchTests`, `DelegateBundleLaunchTests`, `DelegateLaunchArgvIntegrityTests`, `CardSpawnModelArgumentTests`; add `GrokRulesCompositionTests` for the common matrix.

| Case / concrete test name | Arrange and assert |
|---|---|
| V-1a `Worker_and_stage_bundles_reach_typed_rules_payload_without_argv_text` | Worker and each selected pipeline stage through real cold dispatch; actual multiline mandatory bundles and unchanged stamps/order in typed payload; no full body or unique body-only sentinels in any argv/env element. Carry payload through registry and profile resolution, adapter and runner request without loss. |
| V-1b `Standing_channel_composition_preserves_attachment_style_append_and_preamble_bytes` | Bound standing Grok on both backends: `orchestrator` attachment, non-Normal reply style, append, real channel preamble; compare exact UTF-8 bytes and composition stamps. No flattening, separator changes or hand-authored single-line substitute. |
| V-1c `Card_spawn_installs_the_same_composition_before_its_boot_prompt` | Card-spawn path uses typed content and startup barrier; original model/profile precedence stays unchanged; no card work typed before ack. |
| V-1d `Rules_content_preserves_control_newlines_unicode_and_tail_beyond_1000_lines` | Data rows: CR, LF, CRLF, quotes, `$`, backticks, non-ASCII/supplementary characters, marker-like prose, >4,096 UTF-16 units, >old command-line budget, and a 1,105-line body with independent beginning/middle/tail markers. Exact bytes on disk, not normalized read-tool display, are the losslessness oracle. |
| V-1e `File_byte_limit_accepts_exactly_262144_and_rejects_262145_without_truncation` | Construct exact-byte fixtures including multibyte text; compare byte count and SHA. Over-limit, NUL, unresolved key and invalid-Unicode data rows refuse before a store/launch call and expose none of a body sentinel. Test server and direct runner validation independently. |
| V-1f `Off_argv_rules_do_not_consume_command_line_budget_but_bootstrap_does` | Body larger than previous argv budget but below file limit succeeds; actual exe/args including bootstrap over `CommandLineBudgetChars` still refuse. Claude/Codex existing budget behavior unchanged. |
| V-1g `Generated_bootstrap_is_one_unchanged_safe_rules_argument` | Generate from the actual receipt path. Assert one rules occurrence, no CR/LF/NUL, <=4,096 UTF-16 units, shared policy returns no violation, and ordinal equality of bootstrap before/after validation through final effective argv. No body, hash, generation or native `--agent` flag in bootstrap. Windows and non-Windows choose the same typed transport. |
| V-1h `Explicit_rules_conflict_is_refused_without_reinterpreting_literal_paths` | Safe explicit rules/alias, separate and equals forms + composed content => source conflict; unsafe explicit value + composed content => existing unsafe-argv refusal first. Empty bare/specialist composition preserves original literal args and creates no payload/file/refresh. |

Retain the existing exact seam test `GrokRulesArgvPolicyTests.Path_shaped_single_line_values_pass_untouched_for_card_0395`. It proves policy acceptance only; V-1g and V-8 additionally prove the generated prose pointer survives the real pipeline and can cause a read.

#### V-2: runner-owned storage, receipts and compatibility

Layer: runner integration, direct-store unit tests and server integration. New `GrokRulesFileStoreTests`, `GrokRulesFileLaunchTests` in `Antiphon.SessionRunner.Tests`; new `GrokRulesTransportCompatibilityTests` in `Antiphon.Tests`.

| Case / concrete test name | Arrange and assert |
|---|---|
| V-2a `PtyHost_and_herdr_observe_complete_rules_before_spawn` | Store under T-2 path with spaces/Unicode; instrument the actual launch boundary/fake-Herdr allocation. At first possible process creation, final file already has all expected bytes and metadata, and bootstrap is safe. No path is derived from cwd. Herdr test runs with no reachable real Herdr. |
| V-2b `Resume_atomically_replaces_rules_at_the_same_path_and_changes_receipt` | R1 -> R2 same Antiphon/native session and path, different hash/generation; complete old or new file is visible, never partial. Gate write, flush, replace and metadata steps. Fresh write failure leaves no advertised rules file; replacement failure leaves old complete bytes but never claims the new receipt. Crash after replace/before metadata yields mismatch/failure on adoption, not stale ack acceptance. |
| V-2c `Rules_write_failure_has_no_launch_or_inline_fallback` | Inject path, temp-write, replace and metadata errors separately. Assert named failure, zero new registered session/host/pane/detect/send effects before spawn, no alternate argv fallback; clean only own abandoned temp files. |
| V-2d `Shared_cwd_sessions_have_isolated_files_and_generations` | Two session IDs, same cwd/home, concurrent preparation; distinct T-2 paths, each receives only its own content. Cross-session receipt cannot satisfy initialization. |
| V-2e `Adoption_preserves_receipt_across_runner_restart_and_worktree_removal` | Parameterize PtyHost manifest and Herdr sidecar. Restart runner with the owned child still live, using established adoption evidence; receipt survives and content is verified. Remove only the disposable worktree after its test child releases it: rules root remains. Stopped/resumable and pooled states retain it; explicit session-artifact expiry removes only that session's rules directory. |
| V-2f `Missing_capability_refuses_before_sending_a_payload_to_an_old_runner` | Null/404/missing feature => 409 unsupported, zero `StartAsync` and ordinary input calls. Old-server-shaped launch without payload still exercises safe bare behavior. Include fake old runner that would ignore unknown JSON and report Running: it must never receive the launch. |
| V-2g `Invalid_receipt_never_releases_work_and_remote_path_is_not_guessed` | Missing receipt, relative path, traversal, CR/LF/NUL, wrong session suffix, wrong hash/count/version/generation; each fails. Valid runner path on a different root/host succeeds without server filesystem access. Oversized/unsafe regenerated bootstrap remains rejected. |
| V-2h `Restart_between_runner_start_and_server_receipt_commit_recovers_once` | Drop Start response after runner commit; restart server, recover via existing runner session DTO. Same generation/file/refresh logical row, no second child. Runner claiming Running without verifiable receipt must remain blocked/fail owned initialization. |
| V-2i `Capabilities_and_receipts_round_trip_without_rules_body_in_diagnostics` | Contract serialization test plus isolated HTTP route test for `/capabilities` and launch/session responses. Assert optional backward-compatible fields, feature present with Herdr disabled, metadata-only manifests/logs/errors; test sentinels never appear in diagnostic output. The typed private launch payload is the intentional body carrier, not an environment workaround. |

#### V-3: CARD-0382 remains an independent fail-closed guard

Layer: existing `GrokRulesArgvPolicyTests`, `GrokRulesRunnerRefusalTests`, server `AgentSessionLaunchFailureTests`, `DollarEnvArgTests`, `AgentTuiLaunchResolverTests`; new matrices may be data rows in those classes.

- V-3a `Unsafe_raw_rules_are_refused_server_side_before_runner_and_store_calls`: raw profile, registry and extra argv through fresh and native-resume paths, bypassing composition. CR-only/LF/CRLF/NUL, aliases, equals forms, missing value, duplicates with unsafe first/second. Assert 409 existing code with correct occurrence and no adapter start or materialization. Keep genuinely unsafe overrides in E2E separately from bundled-success cases.
- V-3b `Unsafe_raw_rules_are_refused_at_each_final_runner_boundary`: direct request through PtyHost and fake-Herdr, wrapper executable and `TranscriptFormat=grok`. Assert existing typed refusal (not “exe missing”), zero registration/host/script/sidecar/last-pane/detect/Herdr requests before spawn. Removing the server guard cannot make this test irrelevant; it bypasses the server deliberately.
- V-3c `Herdr_effective_env_rules_are_checked_but_pty_literals_are_unchanged`: `$env:RULES` / `${env:RULES}`, and env-supplied flag, with bad resolved values on Herdr. Preserve profile expansion semantics and runner effective-env backstop. The corresponding literal token on the PtyHost final runner boundary is allowed unchanged; do not inadvertently expand ExtraArgs globally.
- V-3d `Argv_boundaries_aliases_terminator_and_precedence_remain_unchanged`: retain every existing policy case, especially 4,096/4,097 BMP and 2,048 supplementary characters, NUL before line-break before length, bad occurrence not hidden by a later good value, both flags/forms, rules after `--` unscanned, whitespace/empty values allowed, next flag-shaped element treated as the value, non-Grok/non-Windows branches inert. Policy validates only; it does not encode/trim/flatten text.
- V-3e `Safe_literal_rules_and_generated_pointer_pass_ordinally_unchanged`: preserve all safe pass-through cases including `@C:\path with spaces\rules.md` and ordinary literal path; no test claims these expand natively. Retain diagnostics no-payload/no-env-value and existing 409 mapping test.

The composition-time raw-body refusal is intentionally replaced by file-content validation, not preserved as a false availability test. Update the old Grok over-budget-body expectations accordingly. The two **raw argv** backstops and the shared policy survive independently.

#### V-4: ordered initialization, resume and migration

Layer: server integration. New `GrokRulesInitializationTests`, `GrokRulesResumeMigrationTests`; extend `AgentSessionInterruptedLaunchResumeTests`, `SessionMessageQueueInterruptedAttemptTests`, `AgentTaskDeliveryWatchdogTests` and `PolicyRefreshServiceTests`.

| Case / concrete test name | Arrange and assert |
|---|---|
| V-4a `Startup_holds_all_work_until_confirmed_refresh_ack_turn_completes` | Record order: receipt commit -> durable refresh row -> sign-in/trust/ready success -> refresh input -> matching UserPrompt -> assistant ack -> successful TurnEnd -> ack commit -> brief/ordinary delivery. Exercise delegate, standing prompt, card boot, resume auto-continue, prequeued Ui/Channel/Delegation/Check/Scheduled rows and concurrent flush/Now requests. Stop at each gate and assert zero task input. Barrier cannot be bypassed by `HoldUntil` expiry or explicit send-now. |
| V-4b `Sign_in_and_trust_gates_precede_rules_refresh_input` | Existing sign-in/trust detector fixtures; sign-in types nothing, trust uses its existing `y` contract; neither a quiet screen nor runner Running releases rules/work. No change to auth/quota refusal semantics. |
| V-4c `Only_current_refresh_assistant_ack_with_confirmed_prompt_releases_barrier` | Data rows: wrong session/queue id/hash/generation, old resumed ack, ack in UserPrompt/tool output/quoted block, unrelated task turn, ack-looking fragment, success before prompt confirmation, error after ack, and no TurnEnd. All stay closed; a split assistant line assembled by the real normalizer plus late-confirmed owning prompt and successful turn releases exactly once. |
| V-4d `Missing_failed_or_timed_out_ack_preserves_work_and_terminalizes_owned_startup` | Native failed read, partial read, mismatched revision, provider error, ended turn without ack, silent provider and exhausted unconfirmed delivery. Assert T-6 codes/states, deadline boundaries, pending-work retention, original task goal unchanged, no report/nudge/channel message, no automatic launch/reread loop. Retype count is zero after transcript confirmation. Check first-writer termination source and owned-process cleanup. |
| V-4e `Interrupted_initialization_reconciles_prompt_and_ack_without_double_send` | Crash before/after receipt commit, refresh row commit, Sent/null-verdict, matching UserPrompt persistence, ack persistence and barrier commit. On restart: exact first-line/provider matcher reconciliation, then existing Enter-only/retry path only if unconfirmed. No ack can be accepted from before this generation. Restart also checks persisted deadlines and recovers when a transcript fetch contains only duplicates. |
| V-4f `Rules_refresh_turn_neither_settles_nudges_nor_publishes_channel_output` | Invoke actual settlement, deferred settlement and channel extraction against normal/late-text refresh turns, including marker-like rules prose/tool output and a channel configured to include System-origin text. Zero task result/report nudge, report-token interpretation, attachment follow-up, channel reply or unmatched-injection incident for that internal turn. A later genuine marked task still settles/publishes normally. |
| V-4g `V1_native_resume_keeps_path_replaces_revision_and_rereads_before_continue` | Seed real v1 receipt and native session existence. R1 -> R2 via actual resume/policy refresh: `--resume` same native identity, stable bootstrap path, new file/hash/generation, old ack rejected, new read/ack required before continue. A healthy launch or updated stamps alone cannot pass. Missing native-session directory remains subject to CARD-0383, without presenting a created conversation as retained history. |
| V-4h `Legacy_native_resume_refuses_before_history_or_process_mutation` | Standing legacy with inline rules and native history, desired bundle nonempty, no receipt. Both explicit resume and policy-refresh sweep/manual endpoint refuse T-9 code **before kill**. Compare native transcript bytes/hash, session identity, live ownership, stamps and queued messages before/after. Failure detail says old inline rules are retained, supplies fresh-start action and checkpoint/handoff choice. No automatic `--session-id` substitution or mass restart. |
| V-4i `Explicit_fresh_start_migrates_legacy_without_deleting_old_history` | After the explicit existing `fresh:true` action, new native conversation uses file bootstrap and barrier; previous native conversation and history remain on disk/readable and identifiable, no claim that the new conversation inherited them. Only operator-supplied handoff becomes new task context. |
| V-4j `Removing_all_rules_requires_explicit_fresh_start_and_retires_old_contract` | V1 nonempty -> desired empty refuses on ordinary resume; file unchanged. Explicit fresh starts bare, with no file/bootstrap/refresh on the new session, while prior resume artifacts remain retained. Empty legacy -> empty follows its existing lane. |
| V-4k `Rules_ack_is_not_task_boot_delivery_or_first_model_reply` | Ack completes, then task brief is confirmed but receives no assistant output. Boot watchdog starts from **task** delivery; refresh assistant rows do not satisfy task-first-reply evidence or suppress the one boot-stall retry. Conversely no task watchdog retries a still-initializing session. |

#### V-5: real ACP compaction through live ingestion and catch-up

Layer: runner/parser and server integration. Extend `GrokTranscriptTailerTests`; new `GrokRulesCompactionRecoveryTests` in `Antiphon.Tests`. Feed the captured **native ACP fixture into the real tailer/normalizer** and route its results through runtime persistence. Calling only `OnCompactBoundaryAsync` with a fabricated normalized entry is insufficient for V-5a/b/c.

| Case / concrete test name | Arrange and assert |
|---|---|
| V-5a `Captured_grok_auto_compact_completed_reaches_durable_rules_refresh` | Native `sessionUpdate:auto_compact_completed` -> usage-bearing `CompactBoundary`, original event identity retained -> server persisted entry -> System/WhenIdle refresh + durable key in one transaction. Parameterize standing attachment-only (`SystemPromptAppend` empty), named channel session and pool delegate whose ephemeral agent row has gone. Receipt qualifies all three. |
| V-5b `Auto_compact_mid_tool_call_stays_working_and_refresh_precedes_next_work` | Captured prompt/tool -> auto compact -> post-compact continuation/tool -> genuine `turn_completed`. Assert working stays true at boundary, zero input/Enter/cancel/settlement there, no interrupted tool. At genuine idle pending refresh precedes older ordinary work, including a concurrent Now/send-now attempt; an already-started delivery completes without interleaving. Repeat with blocked Herdr UI and unblock; no charged attempt while blocked. |
| V-5c `Sync_only_replay_and_rebased_sequences_create_one_logical_refresh` | Suppress live ingestion; run `SyncTranscriptAsync` only. Reconnect, restart runtime, replay from offset zero and rebase sequence while retaining native/persisted identity. Exactly one trigger per persisted boundary and one logical delivery; deduplication of all incoming rows still retries existing pending refreshes. Non-new-boundary rows do not create new work. |
| V-5d `Startup_scan_repairs_crash_after_transcript_save_before_trigger_creation` | Persist ACP-derived boundary, crash before recovery dispatch. Recreate server services and run startup reconciliation; pending refresh appears without requiring another incoming event or a live named Agent row. |
| V-5e `Trigger_and_queue_commit_atomically_under_crash_and_concurrent_ingestion` | Real Postgres transaction: inject failure before commit and between trigger/queue writes; neither survives. After commit/before in-memory latch update both survive. Race live/sync/startup writers; unique key plus transaction leaves one row, no lost reminder or duplicate send. Assert scoped DB rows from a new context. |
| V-5f `Send_and_ack_crashes_reconcile_the_same_compaction_refresh` | Crash after queued, Sent/null, UserPrompt, assistant ack, and satisfied-coverage commit. Reconciliation accepts exact transcript evidence or retries only the unconfirmed attempt. No successful ack is sent twice; a crash before recording satisfaction replays the ack, not the work. |
| V-5g `Multiple_busy_boundaries_coalesce_but_new_boundary_after_ack_rereads` | Two distinct compact identities before idle -> one reread and durable coverage for both; replay never loses coverage. Third genuine boundary after acknowledged reread -> another distinct refresh. Do not replace coverage with “largest seq seen.” |
| V-5h `Refresh_induced_compaction_is_covered_only_by_a_later_read_and_cannot_loop` | Compact before a full read within refresh can be covered; compact after last read requires the one follow-on. Follow-on also compacts after read -> persisted loop failure and held work. Restart between each stage cannot reset the one-follow-on budget. |
| V-5i `Failed_compact_refresh_blocks_delivery_without_killing_owned_work` | Working v1 session reaches idle refresh, then read/ack/provider failure or deadline. Failure/attention visible, ordinary pending work retained; zero kill, ownership release, provider reroute or repeated reread. No deadline expires merely because the original work turn remains busy. |
| V-5j `Checkpoint_and_codex_context_compacted_do_not_trigger_grok_refresh` | Native `compaction_checkpoint` emits no boundary; Codex `compacted`/`event_msg:context_compacted` fixtures stay on Codex path and create no Grok queue row. Actual Grok tokenless auto-boundary remains supported. |

#### V-6: provider, reuse and lifecycle regression boundaries

Layer: existing integration/unit suites plus `GrokRulesPoolReuseTests` (new, `Antiphon.Tests`).

- V-6a `Warm_grok_reuse_requires_matching_revision_and_acknowledged_initialization`: match permits reuse without another bootstrap turn; differing requested bundle, absent/malformed receipt, failed/pending barrier and legacy bundled pool cannot accept work. They take cold dispatch/normal idle retirement, with no active-file overwrite or mid-task kill. Preserve kind/cwd/model pool eligibility and empty-specialist lane. Include same content with changed generation so stale ack cannot qualify.
- V-6b `Policy_drift_uses_idle_resume_and_never_a_live_file_watcher`: changed attachment/append is re-rendered only through the existing idle actual-resume owner, with its receipt/ack; active file stays unchanged while working. Empty transition and legacy preflight remain V-4h/j.
- V-6c `Claude_recovery_and_codex_compaction_timing_remain_provider_owned`: retain `CompactionRecoveryTests` Claude incident/manual/auto/next-idle tests, `TranscriptWorkingStateTests`, `TranscriptNormalizerTests`, `CodexTranscriptTailerTests`, `CodexDelegateDispatchTests`, `ClaudeLaunchArgsTests`, `CodexLaunchArgsTests` and `DelegateLaunchArgvIntegrityTests`. Exact Claude/Codex argv/developer instructions unchanged; Codex auto compact stays mid-turn until `task_complete`, never a Grok rules read. No paid Codex canary added.
- V-6d `Rules_failure_and_success_release_only_to_existing_owned_states`: interrupted launch, failed startup, successful delegate settlement, warm retirement and established-session refresh failure leave processes killed, warm-pooled, or standing-owned as applicable. Retain `SessionTerminationSourcePersistenceTests`, `AgentSessionLaunchQueueOwnershipTests`, `AgentSessionLaunchFailureTests` and `PtyKillProcessTreeTests` cases touched by these paths. A rules ack never moves a card to Review.
- V-6e `Other_kinds_reject_accidental_grok_payload_and_empty_agents_do_nothing`: typed payload on Claude/Codex/Raw/OpenCode refuses content `wrong_kind`; ordinary launches unchanged. Empty-composition specialist/bare Grok creates neither file nor refresh, including after compaction.

#### V-7: restore the three Windows Grok delegate E2E behaviors

Layer: `tests/Antiphon.Tests/Application/GrokDelegateEndToEndTests.cs`, real script/HTTP relay/production service graph/real ConPTY fakegrok/real runner tailer and sync settlement. Remove both unconditional CARD-0382 skips; on a properly staged Windows acceptance run **all three cases must execute**, not skip. Missing fakegrok/build dependencies is an incomplete verification run, not passing restoration.

| Concrete case | Required restoration |
|---|---|
| V-7a Rename `a_Kind_Grok_worker_dispatched_from_the_delegate_script_is_refused_before_any_process_starts` to `a_Kind_Grok_worker_dispatched_from_the_delegate_script_reads_rules_settles_and_prices_on_the_grok_ladder` | Restore the positive capstone: real Worker/stage rules composed to disk, ack before normal multiline brief/spill delivery, actual Grok task/session/model identity, full brief head/middle/tail consumed, marked report settles Succeeded, Grok usage/cost ladder rather than Claude. Prove native file read separately from merely seeing the pointer. Keep an unsafe-profile no-process refusal as new `a_Kind_Grok_worker_with_unsafe_profile_rules_is_refused_before_any_process_starts`. |
| V-7b Keep exact name `an_unmarked_grok_reply_inside_one_tailer_poll_of_the_nudge_settles_unmarked_after_nudge` | Rules init first, unmarked **task** reply then one report nudge and second reply inside the existing tailer/SentAt race. Assert UnmarkedAfterNudge outcome as before; rules ack created zero nudges and never settled the task. |
| V-7c Keep exact name `a_provider_that_never_answers_the_boot_prompt_is_failed_killed_and_retried_once` | Configure fakegrok to acknowledge the rules but give no answer **only to the task brief** (scope existing no-reply behavior or add `ANTIPHON_FAKE_NO_TASK_REPLY=1`). Assert brief-specific transcript confirmation, deadline failure/ProviderUnresponsive, owned process killed, one fresh retry with a new initialization, and original one-retry terminal behavior. Existing `ANTIPHON_FAKE_NO_REPLY` must not turn this into a rules-ack timeout test. Update broad “any UserPrompt/AssistantText” assertions to the owning brief turn. |

The unchanged Claude positive capstone in this class remains a control. Fake startup/read/ack fixture changes must also pass `Antiphon.Agents.Pty.Tests/FakeGrokContractTests` and `SessionMessageQueueGrokPtyIntegrationTests`, including `Swallowed_enter_redraw_is_NoSubmitOutput_then_Enter_only_recovery`.

#### V-8: mandatory real executable dispatch and real model compliance

Layer: opt-in headed integration. New `GrokRulesDispatchAcceptanceTests` in `Antiphon.Tests`, `[Explicit]`, `Category("Integration")`, `NotInParallel("Headed")`, assembly-local process limiter. Cases are explicitly selected and isolated; they do not delegate recursively. Two **distinct arms** must be reported.

| Case | Protocol and passing oracle |
|---|---|
| V-8a `Real_cli_delegate_wire_reads_full_rules_before_brief_and_settles` | Zero-spend arm through real `delegate.ps1` -> isolated server -> real runner/PtyHost -> installed `grok.exe` -> transcript -> real settlement. Use `RealCliStubEnv.ForGrok`, isolated home, synthetic credential, both required redirects, no inherited production provider overlays. Match `/api-key` synthetic credential **and** the unique nonce/tools/full user-turn body on the measured Responses route. Script real `read_file` with continuation reads, full SSE usage details/sequence numbers/annotations, then ack; no script-side fabricated tool result. Require actual subsequent `function_call_output` covering every rules marker/line range. No native agent-definition replacement. |
| V-8b `Real_cli_herdr_standing_attachment_reads_rules_before_work` | Same wire oracle through isolated actual Herdr standing-agent attachment + style + append + channel composition (bound to a test sink). Native Grok detected before configured `Herdr` detection deadline, process identity and transcript bind correct, pane present through read/work/report. Retain pane under existing ownership/normal lifecycle; no transport-driven close or timeout. No existing live pane is borrowed. |
| V-8c `Live_model_delegate_obeys_file_only_rules_and_consumes_full_brief` | Authenticated real model, same real script/server/runner/PtyHost production path with actual Worker and stage rules. No stub choosing the read/ack. File-only harmless output requirement plus early/middle/tail rule lookups must be obeyed in the final marked report. Native tool transcript must show full read, then matching ack, then brief and task work. Work before ack or task output without the rules-only requirement fails. |
| V-8d `Live_model_herdr_standing_attachment_obeys_rules_after_startup_ack` | Authenticated real model repeats the isolated Herdr standing-attachment case, including real ready/trust gating and acknowledgement exclusion from the test channel. Complete rules read and rule-dependent work output required; retain evidence of detection elapsed and pane identity. |

For all four, goal is a disposable **>=5 KB** UTF-8 multiline file with paragraphs, quotes and independent start/middle/end task markers; send the goal with the plan's **in-process PowerShell** `& .\scripts\delegate.ps1 ... -Goal $goalText` invocation, never a long `pwsh.exe -Goal <body>` native argv. The harness supplies the isolated API and authorized task/capability identity, installs a normal Grok definition/profile, and checks the script's resolved kind. A direct adapter-only canary supplements but does not replace this chain. Record and disclose the HTTP relay seam if used; at least the live dispatch arm must exercise the actual mapped create route/caller resolution on the isolated real-Program host.

Use existing opt-ins `ANTIPHON_REAL_CLI_STUB_TESTS=1` for wire cases and `ANTIPHON_HEADED_TESTS=1` for headed eligibility. Add explicit **test-only** `ANTIPHON_GROK_RULES_LIVE_TESTS=1` for authenticated cases; live cases never run because the stub flag alone is set. Wire cases that cannot establish safe redirects do not launch; live auth/quota/model refusal is recorded as an unmet gate, never bypassed or silently routed to another provider. Use approved credential resolution; never copy/print a home/auth store or commit request headers.

Required evidence per case: UTC start/end and monotonic elapsed; code/server/runner commit and capability; CLI version/hash/model; backend; task/session/native-session/queue/prompt/turn/tool-call IDs; runner-visible rules path, byte count and SHA; original brief file/hash and its consumed tail marker; argv element counts/lengths plus sanitized bootstrap only; ordered native/normalized transcript excerpt for full read, ack and final report; real settlement/status and usage/cost; Herdr pane/process identity and detection elapsed where applicable. Store safe fixture artifacts beneath `tests/Antiphon.Tests/TestOutput/Logs/<test>/<run-id>/` with `manifest.json`/`timeline.json`/`transcript.jsonl`/`rules.md`/`brief.md`. These files contain embedded repository bundles and synthetic canary content, never real user-specific attachments, channel content or secrets. Commit a sanitized durable summary to `docs/investigations/2026-09-06-card-0395-rules-transport-acceptance.md`; ignored local paths alone are insufficient Review evidence.

`Sent` without a confirmed UserPrompt, a screen redraw, a healthy runner, delegate creation, an ack copied from the test script, or the scripted wire read are each insufficient for live-model acceptance. On opted-in runs a missing prerequisite must be prominent in the gate ledger; “skipped” is never “passed.”

#### V-9: mandatory genuine two-compaction retention and repair experiment

Layer: real headed model acceptance, new `GrokRulesCompactionAcceptanceTests` in `Antiphon.Tests` using the same isolated server/runner, `[Explicit]`, headed/process-limiter attributes and live opt-ins as V-8. **Two genuine auto-compactions in the file-transport session are mandatory.** Run a separate baseline session with safe single-line inline canaries through the supported empty-composition/bare lane (no synthetic Worker bundles); baseline also observes two compactions so the comparison covers the same checkpoints. Do not dispatch the baseline through a Worker path that necessarily composes rules.

Concrete tests: V-9a `Live_inline_rules_baseline_records_two_genuine_auto_compactions`; V-9b `Live_file_rules_remain_effective_across_two_auto_compactions_and_resume`. A baseline loss is a recorded baseline result, not permission for the file arm to fail; V-9b must obey all canaries, including the pre-idle interval, and complete refreshed-resume verification.

1. First calibrate a **new** real-CLI/local-stub automatic-compaction workload (not a remeasurement of the settled transport matrix). Let the installed CLI accumulate context through real tool exchanges; capture its own automatic compaction initiation/completion. Use a configurable threshold only if an actual installed supported setting is established and recorded; never guess one, write ACP rows, invent token counts, invoke fakegrok `/compact`, or inject `context_compacted`. Record request count, tool-output volume, native token observations and elapsed. This calibration only supplies a reproducible trigger recipe and fixture; it cannot satisfy V-9a/b.
2. Run baseline and file arms in distinct disposable sessions/homes with the same installed version/model and comparable task workload/compaction configuration. Early/middle/tail rule sections define fresh per-run response values for disjoint challenge keys. Put **only challenge keys**, never answers or reread instructions, into neutral tasks. Use different keys at every checkpoint so an earlier answer in history cannot supply the later oracle. File arm has >1,000 lines with the tail rule beyond the default read limit; baseline contains equivalent concise canary rules within the one-line 4,096-unit policy limit. Store expected answers separately in the test harness.
3. Verify initial native read/ack before file-arm work, full file coverage and correct early/middle/tail answers. Record baseline literal `--rules` value metadata and its initial behavior separately. Then use the calibrated actual workload, allowing the **real model** to choose its tool calls, until the CLI's first genuine `auto_compact_completed` is observed.
4. Before starting that long work turn, give it a staged neutral challenge schedule that requires the next unused key after the context-filling tool phase. Capture the first post-compact continuation/request and the resulting rule-dependent answer/tool behavior **inside that same turn, before any idle refresh is sent**. Do not type a probe into the working composer or pause production compaction recovery to create this checkpoint. Correlate native boundary, prompt/tool identities and sequence, not wall-clock sorting alone. If the workload ends at the boundary without an observable pre-idle rule-dependent checkpoint, that run is inconclusive for this requirement.
5. At genuine idle, observe real runtime auto-boundary handling -> durable queued reread -> native full read -> matching ack -> next work. Ask unused neutral keys; all early/middle/tail answers must match. Repeat the workload through **a second distinct native automatic-compaction identity**, with the same pre-idle and after-refresh observations. Seeing two copies of one event after replay counts as one.
6. Change one file-only sentinel through its owned attachment/source, request the existing idle **actual native resume**, and verify same path/native conversation, new hash/generation, old ack rejected, current full reread acknowledged and new-value output. Do not edit the active rules file behind the store or repeat the answer in the resume prompt. Also verify a task-history fact survives that genuine v1 resume.
7. Produce a row for baseline and file arms at: initial, compact 1 first continuation before idle, compact 1 after refresh (baseline: equivalent next-idle challenge, explicitly no file refresh), compact 2 first continuation before idle, compact 2 after refresh, and file-arm resumed revision. Each row has expected/actual early-middle-tail answers, survival/loss/mixed verdict, native boundary ID, transcript/tool/ack IDs, hash/generation and **measured** elapsed. Capture system-bootstrap presence/loss from permitted wire/native evidence where available; if the live interface does not expose it, say unobservable and judge behavior from transcript, never infer system retention from a correct ack.

Use a predeclared 120-minute ceiling per baseline/file endurance arm; record real start/end, time to each compact, boundary-to-idle and idle-to-ack. Ceiling expiry, missing auth/quota, inability to induce two real compactions, incomplete read, missing pre-idle evidence or live noncompliance leaves V-9 unmet and CARD-0395 open. Do not enlarge a ceiling after seeing failure merely to claim green; a revised experiment is separately named and reported. Code may need a later authorized run to complete this gate. No elapsed measurements or success claims exist yet in this TestDesign artifact.

Acceptance stops and returns to Plan if the file bootstrap is lost in observable context or rule-dependent behavior fails in the first post-compact continuation, even when idle reread later repairs it. Report that repair as a measurement, not as equivalence to a native before-work hook. Do not replace V-9 with a seeded `CompactBoundary`, a reread prompt, a stub-selected tool call or two manual compactions.

### Guards the regression

| ID | Change that would reintroduce the defect | Caught by |
|---|---|---|
| R-1 | Flatten bundles or move composed text back onto argv. | V-1a/d/g exact bytes, sentinels, policy and final argv; V-7 normal dispatch. |
| R-2 | Weaken/remove either raw-argv backstop, or validate Herdr tokens before expansion only. | V-3 server and direct-runner independent no-effect assertions and policy boundaries. |
| R-3 | Treat a literal path, `--agent`, or new resume argv as native append transport. | V-8 native tool read/default-agent argv; V-4g/h and V-9 changed-revision behavior. Settled measurements remain authoritative. |
| R-4 | Start before atomic write or trust a new server talking to an old runner. | V-2a/c/f/h observes file and capability at actual launch boundaries. |
| R-5 | Reconstruct remote paths, lose receipts on adoption, delete rules with worktree cleanup. | V-2d/e/g/h distinct roots, identity and retained disk bytes. |
| R-6 | Release on screen/`Sent`/any ack without confirmed owning prompt and turn. | V-4a/c/e negative interleavings; V-8 ordered transcript evidence. |
| R-7 | Lose reminders in save-then-enqueue gap or only implement the live hook. | V-5c/d/e real ACP sync-only/startup recovery and transaction crash faults. |
| R-8 | Classify auto compact as turn end or prioritize ordinary work at idle. | V-5b working/tool/zero-input assertions and subsequent queue order. |
| R-9 | Gate refresh on named Agent/SystemPromptAppend or reuse a legacy/mismatched pool. | V-5a delegate/attachment-only cases; V-6a cold eligibility. |
| R-10 | Treat ack as task reply/report/nudge/channel follow-up. | V-4f/k, V-7b/c brief-specific settlement/watchdog oracles. |
| R-11 | Retry ack indefinitely, retype a confirmed prompt, or kill a working session on failed reread. | V-4d/e and V-5f/h/i attempt counts, persisted chain cap and ownership. |
| R-12 | Silently fresh-start legacy resumes or leave stale bootstrap on removal. | V-4h/i/j native history bytes, pre-kill refusal and explicit `fresh:true`. |
| R-13 | Declare success from fake read/ack or boundaries without behavioral evidence. | V-8c/d live rule-only outputs and V-9 all checkpoint rows/two distinct native identities. |
| R-14 | Leak body in errors/manifests, truncate Unicode or substitute an env transport. | V-1d/e/h and V-2i byte and content-free diagnostic assertions. |

### Positive controls

Code runs each mutation independently in the isolated test checkout: break the named seam, observe the specified **assertion** fail, revert that mutation, rerun the same filter green. A build error, unavailable CLI, incidental auth failure or skipped test is not a positive-control result. Do not commit broken mutations or direct them at production. Report mutation, red test/assertion, revert and green counts for every PC. For new seams, mutate the corresponding policy call/conditional once it exists; the names below specify the behavior to remove rather than prescribing implementation code.

| ID | Deliberate break | Required red oracle |
|---|---|---|
| PC-1 | Slice the rules body before its tail / normalize CRLF to LF in the store. | V-1d exact byte equality and tail coverage fail (exercise truncation and normalization separately). |
| PC-2 | Append raw composed body as `--rules` instead of typed payload. | V-1a/g and V-7a cannot launch/observe clean argv; existing raw guard must still refuse. |
| PC-3 | Remove only server raw-argv guard. | V-3a zero runner/store-call assertion fails with downstream rejecting spy; runner backstop still present. |
| PC-4 | Remove only runner final raw-argv guard. | V-3b pre-effect assertion fails using fake-Herdr/nonexistent Pty executable, independently of server. |
| PC-5 | Skip effective-env resolution for validation at server, then separately runner. | V-3c bad resolved token reaches launch boundary; both independent mutations must go red. |
| PC-6 | Allow CR/NUL; change 4,096 comparison; ignore alias/equals/later occurrences. | Existing V-3d named cases go red; run each change separately, with accepted-boundary control still distinguishing >= from >. |
| PC-7 | Remove body NUL/key/byte-limit/wrong-kind checks one at a time, server then runner. | V-1e / V-6e direct caller negative cases fail at the affected layer, before downstream checks can mask the missing guard. |
| PC-8 | Exclude generated bootstrap from total argv budget. | V-1f actual-argv-over-budget control unexpectedly starts. |
| PC-9 | Ignore safe explicit rules duplicate-source conflict. | V-1h observes two sources/effects instead of the specific refusal; unsafe-source precedence remains separately green. |
| PC-10 | Call launcher before the file replace, or swallow the replace failure. | V-2a launch-boundary disk assertion / V-2c no-launch assertion fail separately. |
| PC-11 | Skip capability requirement so old runner silently ignores payload. | V-2f `StartAsync` count must become nonzero, even if later receipt validation refuses. |
| PC-12 | Accept missing/mismatched receipt (path/hash/generation/version). | V-2g held-work/no-input assertions fail for each data row; V-2h prevents adoption-as-success. |
| PC-13 | Omit receipt from manifest/sidecar serialization. | V-2e real restart cannot recover exact receipt; exercise both backends. |
| PC-14 | Derive file path from cwd or delete file during worktree cleanup. | V-2d isolation / V-2e retained-rules assertions fail separately. |
| PC-15 | Remove barrier check for ordinary flush, then for Now/send-now. | V-4a records task bytes before ack on each independent bypass. |
| PC-16 | Accept any ack hash/id, or accept ack before UserPrompt/TurnEnd/provider-success. | Each V-4c negative data row releases unexpectedly; test each condition separately. |
| PC-17 | Remove refresh-origin/key exclusions from settlement and channel extraction separately. | V-4f/V-7b produce a task result/nudge or channel output from an internal ack. |
| PC-18 | Count rules ack as first assistant response to the task. | V-4k/V-7c fail to produce the expected task boot-stall failure/retry. |
| PC-19 | Persist handled trigger/watermark before enqueue, then fault enqueue. | V-5e loses pending reminder after restart; zero input alone is not a passing oracle. |
| PC-20 | Omit sync-only recovery; then omit startup scan separately. | V-5c and V-5d respectively fail to create/recover a pending refresh from persisted native evidence. |
| PC-21 | Classify Grok auto compact as TurnEnd / flush as Now. | V-5b injects mid-tool or settles before real turn end. |
| PC-22 | Remove persisted uniqueness/coverage dedupe. | V-5c/e/g concurrent/replayed rows produce duplicate logical work; test DB uniqueness as well as request counts. |
| PC-23 | Gate Grok recovery on named Agent or nonempty SystemPromptAppend. | V-5a loses pool/attachment-only refresh. |
| PC-24 | Retry after a confirmed refresh prompt or reset deadline/follow-on budget on restart. | V-4d/e or V-5f/h/i records duplicate input, unbounded chain, or missing failure at persisted deadline. |
| PC-25 | Kill established session on refresh failure. | V-5i zero-kill and standing/pool ownership assertions fail. |
| PC-26 | Remove legacy/empty-removal preflight; or silently switch resume to fresh. | V-4h/j mutate process/history despite refusal expectation; V-4i cannot be used to excuse implicit loss. |
| PC-27 | Allow warm reuse on hash match without ack, or on hash mismatch/legacy receipt. | V-6a warm acquisition/no-overwrite assertions fail independently. |
| PC-28 | Point real-CLI bootstrap at a missing disposable file. | V-8a native read fails, no valid ack/brief/settlement; mark the intentionally negative run separately. This is a zero-spend positive control of CLI/file and barrier wiring. |
| PC-29 | Limit stub-requested native reads to the first 1,000 lines but script a success ack anyway. | V-8a full native tool-output tail/coverage assertion fails despite the ack. Production is not required to detect the lie; the acceptance oracle is. |
| PC-30 | Match stub user turns by endpoint alone; feed only helper/title traffic. | V-8a dual-hit/tool-bearing nonce oracle fails. No real provider fallback is permitted in this mutation. |
| PC-31 | Mutate one captured live expected-answer row or reuse the same native compact identity twice in a copy of the evidence fixture. | V-9 evidence-check assertions fail for canary mismatch/insufficient distinct boundaries. This tests the acceptance checker only, never replaces the unmutated real-model runs. |

### Out of scope

- Runtime implementation, executable test code, tests/builds and live model calls in **this TestDesign stage**. All V-n/PC-n are outstanding until Code reports execution; this document adds no passing-runtime claim.
- Reinvestigating the settled Grok 1.0.13 transport/resume findings, adding a native agent definition, weakening CARD-0382, flattening shared bundles or replacing Grok with another provider.
- Any guarantee of byte consumption/obedience from provider acknowledgement alone. Runtime ordering is mechanically tested; live tool evidence and behavioral gates test the model-mediated assumption. No claim of pre-next-API-call reinjection after compaction.
- Broad Claude/Codex behavior changes or another paid provider endurance run; existing affected regressions are included. No frontend change means no client build/Vitest/browser E2E. If Code changes the client, follow the client owner and rebuild dist before its browser tests.
- Production stack restarts, routing/configuration changes, real channel sends, migration of running legacy agents or deletion of native history. Live acceptance uses isolated owned infrastructure. Any later deployment follows the plan's main-checkout restart and loaded-binary verification requirements.
- Cross-platform **live** acceptance beyond the available Windows installation. Portable composition/store/contract cases run without a Windows-only branch and must cover non-Windows path grammar/transport; a missing non-Windows runtime run is disclosed, never called measured platform parity.

### Cost

Code verification floor estimate: **about 180 minutes** for affected builds, class-scoped deterministic suites, fault/positive-control reruns and wire acceptance, excluding implementation and authenticated endurance. Reserve a further 60 minutes for V-8 live compliance plus up to 120 minutes for **each** V-9 baseline/file arm; these are planning budgets, not elapsed measurements or promises that two compactions will occur. All elapsed acceptance figures must come from actual runs. A missed live gate remains open regardless of deterministic green counts.

Run affected classes one at a time, server assembly then runner assembly then Pty assembly; never overlap process-spawn assemblies. Canonical command template (replace the class/project with an entry below, never a namespace filter):

```powershell
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesInitializationTests/*"
dotnet run --project tests/Antiphon.SessionRunner.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesFileStoreTests/*"
dotnet run --project tests/Antiphon.Agents.Pty.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/FakeGrokContractTests/*"
```

| Assembly | Required class filters (new classes explicitly named above) |
|---|---|
| `Antiphon.Tests` composition/storage | `GrokRulesCompositionTests`, `GrokRulesLaunchRefusalTests`, `GrokRulesTransportCompatibilityTests`, `GrokDelegateDispatchTests`, `DelegateBundleLaunchTests`, `DelegateLaunchArgvIntegrityTests`, `CardSpawnModelArgumentTests`, `AgentTuiLaunchResolverTests`, `DollarEnvArgTests` |
| `Antiphon.Tests` barrier/recovery/lifecycle | `GrokRulesInitializationTests`, `GrokRulesResumeMigrationTests`, `GrokRulesCompactionRecoveryTests`, `GrokRulesPoolReuseTests`, `AgentSessionInterruptedLaunchResumeTests`, `AgentSessionLaunchQueueOwnershipTests`, `AgentSessionLaunchFailureTests`, `SessionMessageQueueInterruptedAttemptTests`, `SessionMessageQueueDeliveryVerificationTests`, `SessionMessageQueueGrokPtyIntegrationTests`, `AgentTaskDeliveryWatchdogTests`, `SessionTerminationSourcePersistenceTests`, `PolicyRefreshServiceTests`, `GrokNativeSessionResumeTests` |
| `Antiphon.Tests` E2E/provider controls | `GrokDelegateEndToEndTests`, `CompactionRecoveryTests`, `TranscriptNormalizerTests`, `CodexDelegateDispatchTests`, `ClaudeLaunchArgsTests`, `CodexLaunchArgsTests`, `RunnerGrokAdapterTrustPromptTests`, `RunnerGrokAdapterSignInPromptTests`, `ChannelMachineTurnTextTests`, `ChannelFollowUpAttachmentTests` |
| `Antiphon.SessionRunner.Tests` | `GrokRulesFileStoreTests`, `GrokRulesFileLaunchTests`, `GrokRulesArgvPolicyTests`, `GrokRulesRunnerRefusalTests`, `GrokTranscriptTailerTests`, `TranscriptWorkingStateTests`, `CodexTranscriptNormalizerTests`, `CodexTranscriptTailerTests`, `PtyHostAdoptionTests`, `HerdrAdoptionSweepTests`, `HerdrLaunchShapeTests`, `HerdrGkpLaunchGuardTests`, `HerdrGrokResumeGuardTests` |
| `Antiphon.Agents.Pty.Tests` | `FakeGrokContractTests`, `PtyKillProcessTreeTests`; retain applicable existing launch-argv/delivery tests if those files change |
| Explicit acceptance, `Antiphon.Tests` | `GrokRulesDispatchAcceptanceTests` (V-8 named cases), `GrokRulesCompactionAcceptanceTests` (V-9 named cases); retain `GrokRealCliStubProxyCanaryTests` and `GrokHerdrRealCliStubProxyCanaryTests` wire regressions if their shared SSE fixture changes |

Select wire and live **methods** separately so eligibility flags cannot blur the two arms:

```powershell
$env:ANTIPHON_HEADED_TESTS = '1'
$env:ANTIPHON_REAL_CLI_STUB_TESTS = '1'
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesDispatchAcceptanceTests/Real_cli_delegate_wire_reads_full_rules_before_brief_and_settles"
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesDispatchAcceptanceTests/Real_cli_herdr_standing_attachment_reads_rules_before_work"
Remove-Item Env:ANTIPHON_REAL_CLI_STUB_TESTS
$env:ANTIPHON_GROK_RULES_LIVE_TESTS = '1'
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesDispatchAcceptanceTests/Live_model_delegate_obeys_file_only_rules_and_consumes_full_brief"
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesDispatchAcceptanceTests/Live_model_herdr_standing_attachment_obeys_rules_after_startup_ack"
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-card0395/ -- --treenode-filter "/*/*/GrokRulesCompactionAcceptanceTests/*"
```

The future acceptance classes must honor these explicit filters/opt-ins using the repository's TUnit eligibility pattern. Restore previous environment values after a run; the snippet assumes an initially unset test-only environment. Before testing, build the referenced fake executables in this isolated graph; missing staged binaries cannot turn V-7 green. Clean only resolved `bin-card0395` directories verified to be inside this worktree, with native PowerShell, after their owned children exit.

Code report ledger: each V-1a through V-9b and R-1 through R-14 maps to implemented method/filter and pass/fail/unrun; each PC lists its individual mutations and red/revert/green results. Include test counts, failures, skips, baseline-only failures, actual elapsed, evidence paths/hashes, native compact identities and any remaining gate. Report documentation-only checks separately. Do not mark CARD-0395 fixed from S1/S2 alone: V-7 restoration, V-8 live compliance and V-9 genuine compactions remain mandatory before closure.

TestDesign artifact validation: original Plan text before this appendix matches `34bd0078`; required headings and 14 regression / 31 positive-control groups are present; existing test-class/assembly references checked against the checkout; `git diff --check` passes. Runtime tests/builds/live calls/positive-control executions: **0**. All runtime and real-model acceptance work remains for Code/Review.
