# CARD-0448: verified source identity and publication before cleanup

Plan stage, 2026-09-08. Ready for a separate TestDesign stage, then Code and a safety-critical Review. No implementation or runtime verification is claimed here.

## Outcome and scope

Replace the branch-existence/ancestry shortcut with a landing protocol that binds one task's actual source to a verified commit, independently confirms that commit on the intended remote destination, and only then authorizes guarded cleanup. A failure to establish any fact preserves the work and reports the uncertainty. Existing task success remains separate from landing success.

The implementation must cover explicit landing, cleanup retries, and the shared removal paths that could later erase a refused worktree. Preserve ordinary child-to-parent local merge semantics; those operations must not acquire a false remote-publication meaning. This is independent of CARD-0442's dispatch/worktree continuity and must not wait for it. Coordinate edits to `WorktreeManager` with CARD-0443's lock-diagnostics work: diagnostics/retries must never restore an unsafe forced-deletion fallback.

Baseline inspected: `f202fcc135bb6701e6c9618dbf0494458fab3fca`. The full investigation was recovered from `GET /api/agent-tasks/f86bd33b` (`result`), because its advertised investigation file and a task commit were absent. Its preserved report is [the investigation artifact](../../investigations/2026-09-08-card-0448-land-false-success.md). Source owners: [orchestration](../../orchestration-loop.md), [project conventions](../../project-context.md), [card lifecycle](../../agent-card-lifecycle.md), [HTTP operations](../../ops-http.md), and [testing/build](../../testing-and-build.md).

## Ground truth

| Premise | Observed implementation/evidence | Design consequence |
|---|---|---|
| The review task's recorded branch identifies the work reviewed. | ac24e1af retained its ancestor branch while its actual HEAD was detached at `a18f250d`. Four commits were absent from the reported master SHA. | Require registration + attached symbolic HEAD + actual SHA + recorded-ref SHA to agree before classification. Do not infer intent from report prose or sibling branches. |
| A missing source branch means landing completed. | `DelegationWorktreeService.IsAlreadyLandedAsync` treats any failed `show-ref` as true; it also ignores fetch failure and falls back to local ancestry. | Delete this boolean contract. Missing, malformed, inaccessible and command-error states are unknown/refused unless a matching durable cleanup receipt explains a completed deletion. |
| Already-present cleanup is subject to normal holds and accounting. | `AgentTaskLandService.RunAsync` runs its shortcut before the Shared-writer hold and `LandAttempt` increment. | One ordered gate for every mode, with a repository lease, task refresh and source inspection. Count a started cleanup or containment attempt too. |
| A resolved target SHA proves a push happened. | `CleanupAlreadyLandedAsync` can do no push and still formats `landed ... pushed`. `FinalizeLandAsync` reads the local target after pushing. | Persist the intended result and independently observed remote ref; never substitute local `rev-parse` for publication proof. |
| Directory removal is ancestry-guarded. | `WorktreeManager.TryRemoveAsync` chooses current branch or metadata fallback, force-removes/deletes the directory, then checks branch ancestry. | Authorize and revalidate before directory mutation; deletion must name the recorded expected branch and SHA. |
| Refusal cannot be undone by later housekeeping. | Residue inspection degrades missing state to ancestor/clean; the residue sweeper can ignore untracked files after `Landed`/`Merged`; TTL prune calls the same remover without a target. | Shared removal must fail closed. Task landing work cannot be deleted on age, event text or a missing branch alone. |
| The CARD-0444/4e84689f refusal deleted an origin branch. | Its refusal precedes cleanup. Land contains no remote-delete command; settlement commits locally. The investigation found no evidence that the source branch had been published. The brief also refers to a CARD-0437 refusal; use the recorded task identity when discussing this evidence. | Do not implement an imagined remote-delete fix. Add an explicit source-publication regression fixture and assert the remote source survives refusal. |
| Local fast-forward followed by crash may be treated as already-pushed. | The existing retry helper pushes an ahead target using current branch/target state; it has no persisted identity tying that state to this attempt. | Resume from a recorded verified commit and destination. A local advance is not publication. |
| The queue is a repository lock. | `AgentTaskLandQueue` has one in-process reader and task-ID deduplication. It does not coordinate another process, janitor, or all repository writers. | Add an I/O lease keyed by canonical Git common-directory identity and revalidate mutable facts at boundaries. |

## Decisions

### D-1. Source identity is mandatory, including for a no-op

Introduce an immutable `LandSourceSnapshot` with task/worktree identity, canonical common Git directory, registered path and per-worktree Git directory, exact `refs/heads/...` recorded branch, symbolic HEAD, actual HEAD commit, recorded-branch commit, and status facts. Resolve the managed path and Git registration independently. Compare path identities using OS-appropriate canonical identity, including junction/alias handling; use Git's ref spelling, not case-insensitive string guesses.

Read registration with porcelain/NUL parsing, resolve commits with `rev-parse --verify ...^{commit}`, use `symbolic-ref -q HEAD`, and inspect index, tracked, untracked, ignored files and nested/submodule state. Take identity readings before and after the status inspection; a changed pair is `source_changed`, not a usable snapshot. Capture bytes/status identities needed for later comparisons; diagnostics list paths and SHAs, never file contents or credentials. Detect active rebase/merge/cherry-pick/sequencer state.

Fresh landing requires the path to be registered in the expected repository, attached to exactly the recorded branch, with actual HEAD equal to the recorded branch SHA, and no uncommitted/untracked work. Refuse detached HEAD even when its commit equals the recorded branch. Refuse branch mismatch even when both SHAs coincide. Missing ref, inaccessible path, ambiguous registration, failed status and Git errors are distinct reason codes. Preserve worktree, refs, registration and contents on these refusals. Never auto-commit, stash, reset, adopt another branch or alter task coordinates to make a check pass.

Ignored files are not automatically disposable. Before deletion, preserve opaque ignored files too. Only known generated outputs with established ownership may be treated as disposable; do not allowlist `.antiphon/**`, `.claude/**` or arbitrary `bin-*` by pattern. A report must already have a verified durable copy before deleting its local copy. It is acceptable to report publication with residue when build outputs or unknown ignored files cannot safely be removed.

Rejected: checking only branch ancestry, matching SHAs without symbolic identity, or trusting a successful review report. None observes the directory about to be deleted.

### D-2. Record a landing operation, not just retry counters

Add `AgentTaskLanding` as a durable entity and `AgentTask.ActiveLandingId` as a nullable association. Use the existing EF context and CLI-generated migration. One active operation per task, enforced in the database; concurrency-token/CAS writes fence transitions. An explicit re-POST resumes an unfinished operation; it may reset the existing automatic-attempt budget but must not erase evidence. A new source/destination after a terminal refusal creates a new operation retaining the old one. It cannot replace an unresolved publication/cleanup operation silently.

**F1 design-review amendment:** after terminal `interrupted_rebase_requires_inspection`, an explicit re-POST may create a NEW operation with unchanged coordinates once fresh D-1 inspection under the repository lease proves attachment to the recorded branch, clean status and no active sequencer. Keep the refused operation and every old pin for audit. Automatic sweep cannot reopen this refusal; elapsed time and changed coordinates are not substitutes for the fresh identity gate.

| Durable fields | Purpose |
|---|---|
| Id, TaskId, schema version, phase, concurrency token, timestamps, last reason | Stable operation identity and guarded progression across server restart. |
| Canonical repository identity, worktree registration identity/path, recorded source full ref | Bind evidence to the directory and branch that were actually inspected. |
| OriginalSourceSha (`S`), RebasedSourceSha (`P`), VerifiedSourceSha, verification command/filter/outcome/time, skip reason | Distinguish original work, rewritten result and exactly what was verified. |
| TargetFullRef, TargetBeforeSha (`T0`), LocalTargetAfterSha, RemoteName, destination full ref, remote configuration fingerprint | Freeze the destination and local mutation boundary. No token-bearing URL is returned or logged. |
| RemoteBeforeSha, ObservedRemoteTargetSha (`R`), RemoteConfirmedAt, confirmation method | Independent containment proof; target lookup failures leave these unset. |
| PushStartedAt, push exit/acknowledgement state, optional actual-update observation | Separate attempted/acknowledged push from confirmed publication. |
| Recovery ref names, cleanup phase/intent, expected deletion SHA, directory/branch/registration results | Recover safely when Git or filesystem mutation succeeds before the next DB save. |

Recommended phases: `Inspected`, `RecoveryPinned`, `RebaseStarted`, `Prepared`, `Verified`, `TargetAdvanceStarted`, `LocalTargetAdvanced`, `PushStarted`, `PublicationConfirmed`, `CleanupStarted`, `Complete`, `Refused`. Publication evidence is monotonic: an error after `PublicationConfirmed` cannot turn the operation into "not published". Store cleanup failure separately. No long database transaction spans Git/build execution; persist and confirm each checkpoint before its dependent mutation.

Historical event prose, `LandAttempt`, branch disappearance, and a legacy `Landed` event do not backfill a receipt. Existing tasks with intact sources can take the new full inspection/containment path. Legacy missing-source tasks without evidence are explicitly unknown and require recovery review. This intentional compatibility tightening prevents the same inference from surviving migration.

### D-3. Use typed Git results and pinned revisions

Move new process I/O behind `ILandingGit` (Application I/O seam) implemented in `Infrastructure/Git/LandingGit`. Return exit code, bounded/redacted stderr and parsed result; distinguish ancestry exit 0 (yes), 1 (no), and other exits (error). Timeouts and cancellation are not false/absent. A failed `show-ref`, failed fetch, malformed OID, empty remote response or unresolved object must never become `AlreadyPresent`.

Pass argument lists without a shell. Validate source/destination refs, reject source=target, revision expressions, option-like names, ambiguous short names and unsupported symbolic destinations. Resolve `MergeTargetRef ?? "master"` once to a full local branch and explicit remote destination. Keep the current `origin` default; refuse ambiguous/multiple push destinations or a remote identity change while an operation is pending. Publication confirmation must contact the actual push endpoint, including a configured push URL, rather than accidentally checking an unrelated fetch URL. No remote source deletion, force push or mirror push is introduced.

Use immutable SHAs in ancestry, rebase target, target advance and push. Recovery refs use a task/operation namespace such as `refs/antiphon/land/<task-guid>/<operation-guid>/{source,prepared,target-before,remote-observed}`. Create refs with expected nonexistence and update/delete with an expected old SHA; never overwrite a previous operation's refs. Git supports expected-old-value updates/deletes and ref transactions. [Git update-ref](https://git-scm.com/docs/git-update-ref)

### D-4. Keep in-place rebase, but retain the original and refuse ambiguous recovery

Persist `S`/`T0` and establish verified recovery refs before rebase. If pinning or saving fails, do not start rebase. Save `RebaseStarted` before running `git rebase <T0>`; do not silently enable autostash or update unrelated refs. Explicitly disable autostash and `rebase.updateRefs` for this operation. A clean completion must still be on the recorded branch with a clean worktree. Pin the actual post-rebase SHA `P`, then persist `Prepared`.

Retain existing conflict handling: collect conflict paths, abort only the rebase started and still owned by this live operation, confirm the restored source identity, and leave the source/ref pins for the existing Merge helper. If abort fails, preserve the conflict worktree and report the failed abort. Do not start a second helper against unresolved state. A successful Merge helper returns through source inspection and a new preparation/verification; it cannot bless old evidence for its changed SHA.

Replace blanket `AbortInterruptedRebaseAsync` with evidence-aware recovery. A crash during rebase or after rebase but before a durable `Prepared` checkpoint leaves ambiguous ownership of intermediate edits: retain all work and refs, report `interrupted_rebase_requires_inspection`, and require the existing recovery/follow-up workflow to resolve it. Do not erase a human's conflict resolutions merely because `rebase-merge` exists. This deliberately tightens CARD-0331's unconditional auto-abort behavior. A prepared/verified checkpoint with matching `P` can resume the next phase. If verification was interrupted, rerun it; a `Prepared` checkpoint is not a verification pass.

After a human resolves or aborts that interrupted rebase in place, D-2's explicit re-POST exit applies even when source coordinates did not change. The new operation repeats inspection, preparation and verification; it inherits no verification authority from the refused operation.

Rejected: a new temporary landing worktree/branch subsystem for this fix. It could reduce in-place mutation, but would also change conflict-helper workspace ownership and cleanup architecture. Durable pins plus conservative ambiguity handling address the required loss/retry windows without that expansion. Recovery refs are retained after success too; no TTL pruning is part of this card. A future explicit retention policy may remove them after independent review.

### D-5. Classify publication independently from cleanup

Use typed `LandPublicationOutcome` (`Landed`, `AlreadyPresent`, `Unconfirmed`, `Refused`) and cleanup status (`NotStarted`, `Pending`, `Complete`, `Refused`); include operation mode `Fresh`, `ResumePublication`, or `CleanupRetry` in the DTO/event details. `CleanupRetry` requires an existing matching `PublicationConfirmed` receipt, not inferred ancestry or missing branches.

`AlreadyPresent` is allowed only after fresh valid source identity and a successful independent remote observation positively contains that same source SHA, or after consulting a valid recorded `P` on a resume. No rebase/build is needed for a fresh `S` already published, but record that verification was skipped because exact commit containment was established. Pin source/target/remote recovery refs and persist the receipt before cleanup on this path too. A local-only ancestor continues preparation/publication; it is not already-present on the remote. A valid AlreadyPresent observation requires no local target advance, so a stale local target alone does not invalidate that remote proof; the remote-ahead refusal in D-7 applies when preparing a publication.

Keep `Landed` and `LandedWithResidue` event values for confirmed publication paths; append an `AlreadyPresent` event value without renumbering existing enum values. Its cleanup state is explicit even when residue remains. A cleanup retry repeats the original publication classification with `mode=CleanupRetry`; it does not count a second publication. Update every success consumer to handle the added outcome through structured evidence, including pipeline/readiness and residue classification. No new card column or session state is required.

Example text:

```text
landed source=S verified=P -> origin:refs/heads/master; remote=R confirmed; cleanup complete
already present source=S in origin:refs/heads/master at R; no push attempted; cleanup pending: <reason>
cleanup retry for operation=<id>, published=P confirmed at <time>; worktree kept: source_changed
land unconfirmed at push confirmation: verified=P, local target=P; remote check failed; worktree and recovery refs kept
```

`LandRefused` may represent a pre-publication refusal or unconfirmed publication, with a machine-readable reason and actual local mutation state. Documentation must stop claiming "target did not advance": a rejected push can follow a local fast-forward, and a lost response can follow a remote update. Never say "pushed" merely because the target resolves, or "branch kept" without observing whether it still exists. A post-confirmation cleanup failure reports known publication with residue, not `LandRefused`. Events, DTOs and delivered notes derive from the same typed result, never reparsed prose.

### D-6. Serialize by repository and fence changes during verification

Provide `IRepositoryMutationLease` as an I/O seam, implemented using an exclusive OS/file lease under the canonical Git common directory (e.g. `antiphon/landing.lock`). Hold it across inspection, fetch, rebase, verification, publication and cleanup. A linked worktree and main checkout must share one key; two unrelated repos must not block one another. An occupied lease yields a visible hold and leaves the request pending. File existence alone is not ownership and does not authorize deleting a lock file.

All participating repository mutations must use the lease: explicit lands, child merge-back/settlement commits that overlap their source/target, worktree cleanup, residue sweep and TTL prune. Thread the acquired lease through nested calls to avoid reacquisition deadlocks. Coordinate Shared writer admission with the same repository exclusion: admission takes the lease while publishing its durable running claim; land takes it before reading those claims and holds it until completion. Check existing Shared writers before any landing mode and prevent admission of a new Shared writer while land owns the lease. Also hold behind any active follow-up/helper claim on the exact source worktree; a Succeeded task row does not mean a later task is not using its directory. Worktree agent edits and external manual Git commands cannot be forced to obey it; boundary revalidation is therefore still mandatory. Preserve current pending/hold behavior without holding a DB transaction for a build.

Every mutating child process is owned, cancellable and awaited. Terminate only that operation's children on cancellation before releasing the lease. Journal process identity (PID plus start identity) for restart checks: do not overlap a surviving Git child after a server crash or equate a reused PID with ownership. If an interrupted mutation's process ownership cannot be established, keep the operation refused/held and preserve work. This is not permission to kill unrelated Git processes or remove Git's own lock files.

At minimum, repeat source registration/branch/HEAD/status and target identity checks after fetch, before rebase, after rebase, before and after verification, before local advance, before push and before each deletion. After verification, HEAD must equal the pinned verified SHA and target must still equal `T0`. Any source movement or source dirtiness invalidates verification and refuses publication. A target change before local advance requires fresh preparation and verification on a subsequent attempt; do not silently merge or reuse old results. Metadata changes also invalidate the operation.

Advance an un-checked-out target with an expected-old-SHA update after proving `T0` is an ancestor of `P`. For a checked-out target, require matching symbolic branch/HEAD and clean index/worktree, then `merge --ff-only <P>` and verify HEAD equals `P`; never use `update-ref` alone beneath a checkout. If the checkout moves during the command, stop with explicit state and retained refs; never reset it backward. Detect target movement before push and refuse until revalidated. Local locks coordinate participants, while Git ref atomicity and non-forcing operations preserve concurrent work from other writers; they do not promise a filesystem transaction against arbitrary external editors.

### D-7. Prove the intended result is remotely published

Preparation must complete a fresh remote read/fetch before deciding remote ancestry. Keep the current local-target policy: a remote target ahead of or divergent from local `T0` refuses with the existing refresh guidance, rather than pulling/restarting the shared checkout. If local `T0` is ahead of the remote, publication may include that captured canonical target history plus this source, as today; record both SHAs. Do not publish later unrelated local changes.

After verification, persist the intended `P` and `TargetAdvanceStarted`, fast-forward the local target, observe/save `LocalTargetAdvanced`, then persist `PushStarted`. Push the explicit refspec `<P>:refs/heads/<destination>` to the frozen push endpoint without force. An explicit refspec fixes both source and destination rather than relying on upstream/push defaults. [Git push](https://git-scm.com/docs/git-push)

After the push attempt, independently read that endpoint's exact destination ref, fetch the observed commit into the operation's dedicated observation ref/object store, and verify `P` is an ancestor of the observed remote SHA `R`. Correlate the fetched ref with the independent ref read; if the remote moves between them, repeat the read/fetch/compare a bounded number of times (three complete observations), otherwise return `remote_changed_during_confirmation`. Do not use a shared tracking ref or `FETCH_HEAD` as the durable proof. Reject deletion, missing target, failed object retrieval or a noncontaining rewrite. A descendant `R != P` is valid: somebody else may have fast-forwarded after publication.

Push exit zero without successful remote proof is `Unconfirmed` and preserves work. A rejected push is normally refused; if an independent observation nevertheless proves the exact intended `P` present (e.g. another publisher won), record `AlreadyPresent` and the rejected push separately. A network-loss/timeout after remote acceptance may likewise be reconciled by positive proof. The success claim is containment of `P`, never that this client necessarily performed the remote update.

Persist `PublicationConfirmed` with `P`, `R`, destination and timestamp, and verify the save, before calling any cleanup method. If the save fails, retain everything; retry may re-observe the remote and commit the receipt. On cleanup retry, independently recheck containment again. A now-unreachable or rewritten remote prevents further deletion; retain the historical receipt and report cleanup residue rather than erase evidence of the earlier publication.

### D-8. Make worktree deletion require explicit, current evidence

Replace the landing call to `TryRemoveAsync(repo,path,mergedInto)` with a typed removal request carrying expected repository/worktree identity, exact branch and expected SHA (`P` after rebase, `S` for a fresh already-present result), durable publication operation identity, and the acquired repository lease. The low-level implementation must re-read these facts; a passed DTO alone is not proof. Do not provide a permissive default implementation that delegates to the old `RemoveAsync` and returns clean.

The sequence is: confirm receipt and current remote containment; inspect actual registered source and all protected contents; persist `CleanupStarted`/expected removal identity; repeat the guard immediately before removal; perform ordinary `git worktree remove` without force; inspect registration/directory result; then delete only the exact expected local branch with an expected-old-SHA operation. Before branch deletion check all worktree registrations for a new checkout of that branch. A moved branch or failed inspection is residue, never permission to delete another branch. Retain all recovery refs.

Git documents clean-worktree requirements and that forcing can remove unclean worktrees. Normal landing will not use `--force` or `--force --force`; a future explicit force action would still need a current identity/content guard and separate scope. [Git worktree](https://git-scm.com/docs/git-worktree)

**F4 correction:** ordinary non-forcing `git worktree remove` still deletes ignored files. Explicit ignored-content inspection and ownership/archive checks before removal are load-bearing. PC-7 must fail its preservation assertion against real Git when each ignored-content guard is bypassed; a mock or the absence of `--force` cannot supply that evidence.

Remove recursive directory deletion as an automatic fallback for landing/task residue. A locked, timed-out, partially removed or already-unregistered nonempty directory is preserved as residue if its content identity cannot be proven. Never infer branch identity from its directory name or detached-HEAD fallback. Do not delete the branch while directory removal is incomplete; leaving both makes recovery simpler. A recreated directory or branch on cleanup retry must match the expected identity or be preserved.

Missing components are idempotent only with matching durable prior publication AND cleanup intent: missing directory/registration plus a surviving expected branch permits guarded branch cleanup; both already absent permits completion. An existing directory without a valid Git registration is unknown residue and is not recursively erased. Failure of a Git query is never treated as an absent component.

General `WorktreeManager` paths must also guard contents and identity before any deletion. Local child merge cleanup uses a separately typed `LocalMerge` proof (source and actual parent-target SHA containment), never a `PublicationConfirmed` receipt or a `Landed` report. Unknown-purpose removals and TTL-only task cleanup preserve task work. Worktree-creation rollback remains a distinct operation allowed to undo only its own incompletely created checkout/registration after proving it never became a working task. Do not silently broaden this exception to mature worktrees.

### D-9. Housekeeping cannot bypass a landing refusal

Update `WorktreeResidueSweepService`, `InspectResidueAsync`, and `PruneStaleAsync` to propagate unknown/error states and request the same guarded removal. Never ignore untracked/opaque ignored work because an event said landed. Any active/unconfirmed landing protects its path and branch. For legacy task residue without receipts, default to kept/unknown; do not migrate old potentially false events into deletion authority.

The low-level task-removal policy must work even when the janitor has no DB task lookup: identify task-managed entries from registration/metadata and require the typed publication/local-merge authorization, otherwise keep them. Recovery refs and operation metadata are not janitor candidates. If classification occurred before a source change, execution must independently detect it under the lease. Existing residue counts and diagnostics must show the refusal without counting it as removal.

### D-10. Keep recovery, attempts and notification semantics honest

Preserve durable request sweep, bounded automatic attempts, explicit retry, conflict helpers and the normal async 202 request response. Increment attempts after acquiring permission to run and before every attempted mode, including AlreadyPresent and CleanupRetry; a lease/Shared-writer hold does not spend an attempt. DB CAS/task refresh prevents two service instances from advancing the same operation. `ClearPending` clears scheduling fields only, not the landing evidence.

Persist terminal evidence and its event in one transaction. Deliver/publish notifications after that transaction; notification failure must not rewrite confirmed publication as `LandRefused`. `FailAsync` inspects phase and emits post-publication residue where appropriate. Sweep follows receipts, not event text, when recovering an interrupted run. No automatic card Done transition is introduced.

## Ordered operation

1. Reload pending task; acquire repository exclusion; recheck task status, Shared writers and operation identity. Choose Fresh/ResumePublication/CleanupRetry from durable evidence only.
2. Inspect all present source components. A cleanup receipt can explain an absent component, but never an identity mismatch in an existing component. Refuse unknowns before shortcuts or mutation.
3. Independently inspect remote target. A fresh intact source already contained remotely gets an AlreadyPresent receipt and may enter guarded cleanup. Local-only containment proceeds to publication.
4. Pin recovery refs and persist original source/target. Rebase to pinned target; preserve/conflict-refuse as D-4 requires. Capture/pin `P` and verification results. A verification skip records the exact reason and SHA.
5. Revalidate source and target; persist intent, advance local target to pinned `P`, observe and save. Persist push intent, push explicit destination and independently confirm remote containment.
6. Persist publication proof. Revalidate and remove only authorized components; record residue or completion. Keep receipt/ref pins permanently for this release.
7. Commit the outcome event with pending-field changes. Send the typed outcome through existing delivery and SignalR paths; release only owned processes/lease in finally.

## Crash and retry contract

| Durable boundary / observed state after restart | Required behavior |
|---|---|
| Request queued or held, no source inspection | Reacquire/reinspect; no inferred success. |
| Recovery intent saved, only some pins created | Check expected ref values; finish pinning only if unchanged source/target. Otherwise refuse and retain pins. |
| Rebase started, no durable Prepared identity | Keep original ref, intermediate work and directory; explicit interrupted-rebase diagnosis. No blind abort, no publication/cleanup. |
| Prepared at P, verification not recorded | Require unchanged source P; rerun required verification. |
| Verified at P, before target mutation | Revalidate exact source and T0; advance only from the expected target. |
| TargetAdvanceStarted; target now P; before LocalTargetAdvanced save | Verified receipt plus current matching source and exact target P allows recording the completed local advance. A different target is a changed-state refusal, not presumed completion. |
| Local target advanced, before push | Resume publication of recorded P to the recorded destination. Never enter cleanup based only on local ancestry. |
| Push started; acknowledgement lost / before receipt save | Independently re-observe remote. Containing P allows receipt persistence; unchanged remote allows safe non-forcing retry; error/noncontaining movement preserves work. |
| PublicationConfirmed, before cleanup or outcome delivery | Refresh remote proof; inspect source; retry cleanup only. Do not rebase or repeat publication. |
| CleanupStarted; directory removed, branch still present | Receipt + expected deletion identity allow guarded exact-branch cleanup. A changed/rechecked-out branch stays. |
| CleanupStarted; directory/branch absent, completion save lost | Successful absence checks plus matching receipt/intent allow Complete. No branch-disappearance-only shortcut. |
| Published receipt exists; source changed, directory recreated, dirty residue or remote rewritten | Publication remains historical fact; cleanup refused/pending, all new work retained. |
| Complete; duplicate request | Return the receipt's classification and verified current cleanup result; no fresh publication claim. |
| Evidence save or notification fails | Before receipt save: no cleanup. After receipt save: resume from receipt; notification errors do not negate publication. |

## Implementation slices

Land this plan before TestDesign dispatch. Code implements the following in order; intermediate commits are reviewable, but do not deploy a partially guarded cleanup path.

| Slice | Files / changes | Required tests |
|---|---|---|
| S1: typed identity and Git evidence | New `server/Application/Dtos/LandingDtos.cs`, `server/Application/Interfaces/ILandingGit.cs`, `server/Infrastructure/Git/LandingGit.cs`; replace land boolean helpers in `DelegationWorktreeService.cs`; parse/ref validation and unknown/error handling. | New `LandingGitTests` (Infrastructure), `LandSourceIdentityTests` (Application); update `DelegationWorktreeTests`. |
| S2: durable protocol and recovery refs | New `Domain/Entities/AgentTaskLanding.cs`, enum definitions; `AgentTask.cs`, `Infrastructure/Data/AppDbContext.cs`, CLI migration and snapshot; new concrete `AgentTaskLandingState` transition policy; checkpoint/ref ordering in `AgentTaskLandService`. | New `AgentTaskLandingStateTests`, `AgentTaskLandingPersistenceTests`; migration upgrade with legacy rows, no evidence backfill; update request/sweep tests. |
| S3: serialization and verified publication | New `IRepositoryMutationLease`/`RepositoryMutationLease`; DI in `server/Program.cs`; update `AgentTaskLandService`, `DelegationWorktreeService`, lease/admission integration in `AgentTaskDispatcher` and settlement/merge-back callers; pinned target advance, verification rechecks, explicit push and remote confirmation. | New `RepositoryMutationLeaseTests`, `AgentTaskLandPublicationTests`, `AgentTaskLandConcurrencyTests`; update `DelegationWorktreeTests`, `AgentTaskLandStageOutcomeTests`. |
| S4: guarded deletion and housekeeping | `IWorktreeManager.cs`, `WorktreeManager.cs`, `WorktreeRemoval.cs`, residue DTOs, `WorktreeResidueSweepService.cs`, `WorktreeJanitorHostedService.cs`/`WorktreeResidueJob.cs` where routing changes; remove permissive default and task deletion fallbacks; preserve creation rollback separately. | `WorktreeManagerTests`, `WorktreeResidueSweepTests`, new `AgentTaskLandCleanupSafetyTests`; update all IWorktreeManager fakes and `TestHelpers/DelegationTestServices.cs`, run `DelegationTestServicesTests` and harness census. |
| S5: resume/DTO/outcome integration | `AgentTaskLandService` Request/Run/Sweep/Fail; `AgentTaskService` DTO mapping, `AgentTaskDtos.cs`, `AgentTaskEnums.cs`, client `api/agentTasks.ts` and actual event-rendering consumers; structured event/result formatter; preserve Rebase/Verify/Cleanup stage accounting. | `AgentTaskLandRequestTests`, `AgentTaskLandSweepTests`, `AgentTaskLandStageOutcomeTests`; new `AgentTaskLandRecoveryTests`; targeted client event rendering/serialization tests if changed. |
| S6: documentation and acceptance | `docs/orchestration-loop.md` sections 0/5/8, `docs/ops-http.md`, `docs/antiphon-api.md`, relevant land help/bundle text and `scripts/delegate.ps1` only if it consumes the new structured outcome; remove branch-gone/pushed/target-did-not-advance promises. | All TestDesign V/R/PC items, named regression classes and isolated real-Git recovery tests. Safety-critical Review must inspect mutation ordering and every cleanup entry point. |

The existing verifier invokes `dotnet test`, contrary to the testing owner. If touched as part of introducing a verification I/O seam, use the documented `dotnet run --project tests/<Project>` contract and validate the exact class filter. Do not add an unrelated build-tool redesign. Tests must inject deterministic verification results/barriers for Git safety cases rather than building the whole repository per scenario; keep one scoped real verification compatibility check.

**F2 S5 scope amendment:** include `server/Application/Services/StageOutcomeBackfillService.cs`. Its live background pass must derive new landing rows from structured evidence or be explicitly retired; historical Landed/LandedWithResidue prose cannot create clean stage rows. Handle the appended AlreadyPresent value deliberately. Include this service in the S4/S5 caller/consumer census and V-33.

## Required acceptance cases for TestDesign

TestDesign must append an executable `## Verification design` with V-n/R-n/PC-n, exact class filters, injected fault boundaries and expected assertions. This section is its required coverage input, not a claim that tests have run. Every destructive guard needs a positive control that demonstrably fails at its intended assertion when the guard is broken; fixture/build/zero-test failures do not count.

Use disposable repositories with a bare local remote, source and target checkouts, deterministic barrier-controlled Git/verification seams, and persisted operation rows. Test remote state from the bare repository or a fresh independent clone, never solely `origin/master`. For every refused destructive step assert exact source refs/HEAD/registration, sentinel file bytes (tracked, untracked and relevant ignored), recovery pins and remote source/target values. If rebase or local FF legitimately preceded refusal, assert the documented S/P/T0 preservation and local mutation instead of pretending nothing changed.

| Case | Required observation |
|---|---|
| A1: detached unique HEAD; recorded branch is ancestor of both local and remote target | Refused before rebase/push/removal; unique HEAD and all contents remain. This is the incident reproduction and must fail on baseline. |
| A2: switched branch, including same SHA on two different branches | Mismatch refusal; neither actual nor recorded branch deleted; never select branch from prose. |
| A3: recorded ref missing; separate unreadable/broken-repo/show-ref error cases | Unknown/refused, not AlreadyPresent; directory retained. |
| A4: dirty ancestor source, tracked staged/unstaged, untracked, opaque ignored, submodule/nested repo | No cleanup or hidden auto-commit/stash. Test dirtiness introduced immediately before deletion too. |
| A5: failed fetch; stale cached tracking ref contains source | No shortcut or success; source retained. Include ancestry exit 128 and malformed remote response. |
| A6: remote ahead/diverged from local target | Existing refresh refusal; no cleanup and no implicit pull. |
| A7: local-only containment and local target ahead | No AlreadyPresent; publish pinned P and independently confirm it before cleanup. |
| A8: push rejected by remote hook/non-fast-forward | Worktree/ref pins remain; recorded local advance is truthful; no confirmed publication unless independent containment actually proves P. |
| A9: push reports success but remote confirmation fails or contains another commit | Unconfirmed; no cleanup. Include different push/fetch URLs and changed remote configuration. |
| A10: source HEAD/ref/registration or dirty state moves during verification | Old verification cannot authorize advance/push/deletion. Inject each mutation at a named barrier. |
| A11: local target advances during verification or checked-out target becomes dirty/switches | Refuse/reprepare on retry; never reset another writer or publish its later changes implicitly. |
| A12: remote target advances before push and after push | Noncontaining movement refuses; a confirmed descendant containing P succeeds and records R != P. No force push. |
| A13: two landing services/processes, same repo via different worktree/alias paths | Mutual exclusion and task CAS; second operation reloads state. Different repo can proceed. |
| A14: Shared writer present; then attempted Shared admission during land; cleanup-only shortcut | All modes respect holds; no admission race allows shared target mutation. |
| A15: crashes at every row in the retry table | Instantiate a fresh service/DbContext/queue; assert exact next commands and absence of forbidden cleanup. Required automatic successes: local-FF-before-push resume and confirmed-push-before-cleanup retry. |
| A16: crash after target mutation / remote push / directory removal but before each next DB save | Durable intent + independent observation recover only the intended operation; save failure never grants deletion authority. |
| A17: rebase interrupted with manual conflict resolution or unknown ownership | Preserve edits and original pin; no blanket abort. Successful current-attempt conflict abort preserves existing helper flow. |
| A18: cleanup retry after source branch advances, detaches, switches, becomes dirty, is checked out elsewhere or path is recreated | Known publication with residue; preserve new work and exact changed branch. |
| A19: missing directory/ref with valid receipt and cleanup intent versus no receipt | First is idempotent cleanup; second unknown/refused. Query failure is a separate non-idempotent case. |
| A20: locked/partially removed/unregistered nonempty directory | No recursive deletion fallback; recoverable residue and branch preserved. |
| A21: explicitly push a source branch before each refusal family | Bare remote still has exactly that branch SHA afterward. Never infer publication from branch absence. |
| A22: positive fresh publication; valid AlreadyPresent; valid CleanupRetry | Different structured outcomes/text; no "pushed" on no-push path; correct attempt counters and stage outcomes. |
| A23: receipt save fails; event/delivery fails after receipt save | No cleanup in first; publication evidence survives and is not rewritten as refusal in second. |
| A24: residue/TTL sweep after refusal or legacy false Landed event | Same contents/ref guards apply; no deletion based on age/event or missing ref; untracked ignored-work distinction stays safe. |
| A25: child local merge, no-change child, create rollback, task worktree reuse | Existing supported local behavior stays distinct from remote landing; each deletion has correct identity/content authority. |
| A26: rebase changes SHA, then source branch deletion | Both S and P remain reachable by recovery refs; receipt points at verified P, remote contains P; original-ref retention does not rely on reflog expiry. |
| A27: remote rewrite/unavailability after publication and before cleanup retry | Historical confirmation remains; fresh cleanup is refused; no deletion. |
| A28: process cancellation/restart with live owned Git child and lease contention | No overlapping mutation and no unrelated process kill; uncertainty preserves work. |
| A29: succeeded task's source reused by an active follow-up/helper | All landing modes wait for its durable worktree claim; no removal of a directory in active use. |

Use meaningful red controls for identity, dirtiness, error-to-unknown mapping, durable receipt prerequisite, remote containment, source/target verification rechecks, deletion ordering/exact-ref CAS, shared exclusion and legacy housekeeping bypass. A1 alone cannot prove the publication/recovery guards.

Process-spawning fixtures carry the assembly's `ParallelLimiter<ProcessSpawnLimit>` and unique temporary roots. Use the shared TestDbFixture with task/operation-scoped assertions and `DelegationTestServices` registration. A global sweep test uses ungrouped `[NotInParallel]`. No test uses the production runner, live origin, live board state, real agent launches or the shared development database.

Expected commands to specialize in TestDesign (one class per invocation is valid):

```powershell
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c448/ -- --treenode-filter "/*/*/AgentTaskLandRecoveryTests/*" --report-trx --report-trx-filename c448-recovery.trx
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c448/ -- --treenode-filter "/*/*/WorktreeManagerTests/*" --report-trx --report-trx-filename c448-worktrees.trx
pwsh -File scripts/test-client.ps1 <changed-event-test-file>
```

Require nonzero executed test counts and intended method names in fresh TRX, plus actual assertion failures for red controls. Run Antiphon.Tests and any later Pty checks sequentially. Clean only verified task-owned `bin-c448` outputs. No runtime tests/builds are needed for this Plan-only artifact; Code records each V/R/PC result and the real failures. TestDesign estimates its scoped execution floor from the selected fixtures rather than mandating an unrelated full suite.

## Rollout and completion

Ship the migration and all safety gates together. There is no automatic historical receipt backfill, remote branch cleanup, live stack restart or deletion authorized by this Plan task. During deployment use the canonical main-checkout restart runbook; verify checkout/build identity directly before testing a real landing. A safe isolated real-Git acceptance run must pass before exercising live task cleanup. The first supervised production use should have an intact attached source and compare returned S/P/R against independently read remote history; operator intervention in this bug's damaged worktrees remains a separate recovery action.

Rollback must stop automatic landing/cleanup execution before restoring code that trusts missing refs or ignores receipts. Retain the additive schema, operation history and recovery refs; do not downgrade by deleting evidence. Increased residue for legacy/dirty/unregistered worktrees is an intentional safety result, not permission to bypass the guard. No user decision blocks the plan under these defaults.

Completion requires TestDesign's regression/positive-control evidence and Review approval of source identity, remote publication proof, crash windows, all shared removal callers, and truthful outcome mapping. A green health check or unchanged master SHA cannot substitute for those proofs.

## Verification design

TestDesign, 2026-09-08, against plan commit `6049d225`. This section specifies executable tests for Code; it does not claim that the new tests exist or have run. D-1 through D-10 and S1 through S6 remain unchanged. Every A1-A29 has a V item below. The additional V items cover protocol/schema/ref validation and the other destructive callers implied by those decisions. Code must return `next: review` after completing the evidence: this is safety-critical.

### Harness and evidence contract

Implement `LandingSafetyHarness` in `tests/Antiphon.Tests/TestHelpers/` using real `LandingGit`/`WorktreeManager`, the real landing service and state policy, `DelegationTestServices`, and an isolated database from `TestDbFixture.CreateIsolatedSchemaAsync()`. That helper now clones a database, despite its historical name. Reuse one clone per compatible fixture, scope every query by task/operation, and never migrate down the shared template. Use a separate empty, uniquely named database on the same testcontainer for the upgrade test. All tests that start Git, .NET, a lease worker or another child carry this assembly's `[ParallelLimiter<ProcessSpawnLimit>]`. Global sweep tests additionally use ungrouped `[NotInParallel]`. Retain the production-runner guard and a refusing runner; no fixture launches an agent. Register a progressing clock for any polling service and use barriers, not timed sleeps, to arrange races.

Each fixture owns a fresh temporary root containing `canonical/`, `trees/source/`, `remote.git` (bare), and `observer/` (independent clone). Additional worktrees/remotes/writer processes are created below that root. Resolve every operation path and remote destination before the test runs; refuse a fixture configuration outside the owned root. Use a private Git config/home, disable inherited signing, credential helpers and hooks, and set an explicit fixture identity. Configure a hook only for the rejection test. Do not read or change the developer's Git credentials, checkout, remote, `.git` locks, or process state. Junction targets also stay within the fixture root; dispose junctions without traversing them. Assert preservation before fixture disposal. A fixture's own disposal is never counted as product cleanup.

Seed a tracked `keep.txt` and `.gitignore`; make file contents unique per test. Let `B` be the shared initial commit, `S` the source tip, `T0` the captured local target, `P` the prepared/verified source tip, and `R` the independently observed remote destination. The ordinary rebase fixture has `B -> S` and `B -> T0` with nonconflicting changes, making `P != S`. Assert these prerequisites before invoking the service. Set the task's exact branch, worktree and repository coordinates and its Succeeded state durably. For **every refusal/hold/residue variant**, first explicitly push the source to `refs/heads/feat/card-task-<id>` in the bare remote and assert its SHA. Re-read that remote source after the action and restart/sweep: it must still equal the seeded SHA, including when local source becomes P. This is A21's cross-cutting assertion, not a separate substitute for the other cases.

Capture the following independent evidence before and after each action:

- `SourceImage`: actual symbolic HEAD (including detached), HEAD SHA, recorded and actual branch refs, NUL-parsed worktree registration, canonical/per-worktree Git identities, task coordinates, index/status, active Git operation state, and bytes of every sentinel. Include staged/unstaged tracked, untracked, `.antiphon/report.txt`, `.claude/notes.txt`, arbitrary ignored `bin-private/keep.txt` and pre-existing `bin-land/keep.txt` where that variant requires them. An ignored report's archive is checked by reading the durable copy and comparing bytes/hash; an event saying it was copied is insufficient.
- `RemoteImage`: direct `git --git-dir <bare> rev-parse --verify <full-ref>^{commit}` results and independent observer ancestry. For missing/error cases retain raw exit classification rather than asserting a fabricated SHA. The oracle must not call the production confirmation helper, read only `origin/master`, or accept shared `FETCH_HEAD` as proof.
- `OperationImage`: a fresh `AsNoTracking` read in a **new DbContext** of the task, active-operation association, phases, S/P/T0/R, destination fingerprint, verification SHA/result, cleanup intent/results, process identity, attempts and task-scoped events/stage outcomes. Snapshot all operation recovery refs; compare expected values and `cat-file -e <sha>^{commit}` reachability after cleanup. Never rely on reflogs.
- A sequence-numbered I/O trace, attached to task/operation: checkpoint commit acknowledged, lease acquire/release, identity reads, verification start/end, Git argument vectors/exits, remote observations, removal/branch deletion, and notification calls. Read the committed row through another connection at the destructive-call boundary; an in-memory phase assignment or `SaveChanges` invocation does not establish a checkpoint. Capture sanitized diagnostics without sentinel contents or credentials.

`Preserved` below means all originally present protected components have the expected identity and exact bytes, no unexpected registration/ref changes, and the remote source is unchanged. A pre-mutation refusal also leaves local target and remote target unchanged. If an owned rebase or local FF already happened, assert the specific allowed S-to-P/T0-to-P change, original/prepared recovery pins, and remaining bytes instead; do not assert a fictitious unchanged HEAD. `NoDestruction` means no worktree remove, recursive directory delete, prune affecting the subject, local/remote source delete, reset/stash/auto-commit or unowned abort was attempted. A result enum or mock call count alone is insufficient: assert both the decision and independent state.

Decorate the real I/O seams to pause/fail a **named operation and occurrence**; all unmodified calls continue to real Git. DB failure injection uses transaction/save interceptors. Verification has an I/O seam returning deterministic pass/fail/cancel results and barriers so Git scenarios do not build this repository hundreds of times. Do not add test-only authorization flags or fake landing-state transitions in production. Pure predicate tests may use a mutation-capable downstream fake to prove the production guard's refusal when another guard/Git would otherwise mask it; pair each such test with the real-Git V case. The fake must record whether the forbidden operation was requested and mutate its modeled bytes/refs if called, not return a canned refusal.

### Fault and race barriers

Expose these points through test decorators/interceptors; their spelling here is the test contract, not a requirement for public production APIs. At each point assert the harness reached the intended phase before applying a fault.

| Barrier | Position |
|---|---|
| B01 | Lease acquired, before task/Shared/helper claim reload. |
| B02 | Between the identity readings surrounding source status inspection. |
| B03 | Fetch/initial remote observation completed, before revalidation/classification. |
| B04 | Recovery intent committed, before each recovery-ref creation and its acknowledgement/save. |
| B05 | `RebaseStarted` committed, before child start; also inside an owned running rebase. |
| B06 | Rebase process exited, before prepared-ref creation and durable `Prepared`. |
| B07 | Before verification; verification started; verification returns, before post-verification checks and `Verified` save (separate occurrences). |
| B08 | Immediately before target-advance intent and immediately before the Git target mutation (separate occurrences). |
| B09 | Target mutation completed, before `LocalTargetAdvanced` save. |
| B10 | `PushStarted` committed, before final source/target/destination recheck and push child start. |
| B11 | Remote accepted push, before client acknowledgement; then after push exit, before remote confirmation. |
| B12 | Between independent remote-ref read, observation-ref fetch and comparison; repeat for observations 1, 2, 3. |
| B13 | Containment proved, before `PublicationConfirmed` commit and immediately after its commit acknowledgement. |
| B14 | Cleanup proof/content inspection complete, before `CleanupStarted` commit and final pre-remove inspection. |
| B15 | Immediately before ordinary worktree removal; then after it exits but before directory/registration result save. |
| B16 | Directory absence established, before all-registration/branch inspection; then after that inspection, before expected-old-SHA branch delete. |
| B17 | Branch delete succeeded, before cleanup/`Complete` result commit. |
| B18 | Terminal evidence/event transaction before commit; then after commit, before delivery and SignalR (independent faults). |
| B19 | Residue classification/dry run finished, before sweep/TTL guarded execution. |
| B20 | Shared/helper/settlement/merge-back writer acquired lease, before durable claim/mutation; inverse race while land owns lease. |
| B21 | Cancellation/crash with a live mutating child, before child-exit confirmation and lease release. |

Run source-movement variants after fetch, before rebase, after rebase, before/after verification, before local advance, before push and before each deletion. For B16, directory removal may already have succeeded: preserve the new/surviving branch and any new checkout, rather than demanding resurrection of the deleted directory. Metadata changes include task source ref/path, merge target, repository identity and remote fingerprint. Deterministic injected query/permission failures are mandatory on every OS; add actual Windows junction, locked-file and separate-process lease runs on Windows. Lack of that required platform evidence is reported as pending, not a pass or silent skip.

### Proves it works now

Class keys below expand to exact TUnit class names in `tests/Antiphon.Tests`. Use the exact method names listed in the V table, with data rows named after every listed variant/barrier. These are **new test requirements**, except the named existing regression classes later in this section.

| Key | Class (namespace) | Layer |
|---|---|---|
| IG | `LandingGitTests` (`Antiphon.Tests.Infrastructure`) | integration, including its parser/ref-validation rows |
| ID | `LandSourceIdentityTests` (`Antiphon.Tests.Application`) | integration |
| ST | `AgentTaskLandingStateTests` (`Antiphon.Tests.Application`) | unit |
| DB | `AgentTaskLandingPersistenceTests` (`Antiphon.Tests.Application`) | integration |
| PB | `AgentTaskLandPublicationTests` (`Antiphon.Tests.Application`) | integration |
| LS | `RepositoryMutationLeaseTests` (`Antiphon.Tests.Infrastructure`) | integration |
| CO | `AgentTaskLandConcurrencyTests` (`Antiphon.Tests.Application`) | integration |
| RC | `AgentTaskLandRecoveryTests` (`Antiphon.Tests.Application`) | integration |
| CL | `AgentTaskLandCleanupSafetyTests` (`Antiphon.Tests.Application`) | integration |
| SW | `WorktreeResidueSweepTests` (`Antiphon.Tests.Application`) | integration |
| DW | `DelegationWorktreeTests` (`Antiphon.Tests.Application`) | integration |
| SO | `AgentTaskLandStageOutcomeTests` (`Antiphon.Tests.Application`) | integration |

| ID / acceptance | Exact test / setup and action | Expected assertions |
|---|---|---|
| V-1 / A1 | ID.`C448_V01_DetachedHeadIsPreserved`: recorded branch at B, B ancestor of local/remote target, detached unique committed U in source. Add same-SHA detached variant. Invoke land. | Typed identity refusal; `NoDestruction`; directory/registration/U and all sentinels preserved; no rebase/push. Neither unique U nor recorded B is misreported as published. The unique-U variant must also fail at its preservation assertion on baseline `f202fcc1` (PC-1). |
| V-2 / A2 | ID.`C448_V02_SwitchedBranchIsPreserved`: switch source to another branch with unequal SHA, then separately with equal SHA. Include case-distinct full-ref spellings and misleading task report prose. | Exact symbolic branch mismatch refusal, both local refs unchanged and actual checkout preserved. Equal SHA/prose is not authority to adopt a branch or rewrite task coordinates. |
| V-3 / A3 | ID.`C448_V03_UnknownIdentityIsPreserved`: remove recorded ref through fixture plumbing; independently inject unreadable path, wrong common dir, malformed `.git`, ambiguous/stale registration, show-ref error, status error and unresolved HEAD object. | Distinct missing/error reason categories; no AlreadyPresent, no mutation; each still-present component preserved. Bad registration cannot redirect an operation to another fixture checkout. |
| V-4 / A4 | ID.`C448_V04_ProtectedContentsArePreserved`: source commit already remotely contained; rows for staged, unstaged, untracked, opaque ignored, dirty submodule, untracked nested repo, active merge/cherry-pick/rebase/sequencer. Repeat content insertion at B14/B15 and use CL low-level request as well. | Tracked/untracked dirt or active Git state refuses before publication mutation; opaque ignored content may produce AlreadyPresent with residue but never deletion. All protected bytes/index entries remain; no stash/auto-commit/clean/reset. Test unknown `.antiphon`, `.claude`, `bin-*`, and unarchived/incorrectly archived report separately. |
| V-5 / A5 | PB.`C448_V05_RemoteErrorsAreUnknown`: cached tracking ref contains S; fail actual fetch; ancestry exits 1 and 128 separately; remote response empty/malformed/missing/timeout/canceled; object fetch fails. | Exit 1 is noncontainment, other failures unknown; no fallback to cache/local ancestry; no success/receipt/removal. Preserve source and capture bounded, redacted error detail. |
| V-6 / A6 | PB.`C448_V06_RemoteAheadRequiresRefresh`: S is not remotely contained; remote is ahead of T0, then divergent. | Refresh refusal before rebase/target advance; no implicit pull/reset/push or cleanup. Companion row: remote contains intact S with stale local T0 is valid AlreadyPresent, proving the preparation rule does not incorrectly block that path. |
| V-7 / A7 | PB.`C448_V07_LocalContainmentStillPublishes`: S contained only in local target; local T0 ahead of remote. | Fresh/ResumePublication, never shortcut AlreadyPresent. Explicit pinned P refspec publishes captured history; commit receipt with independently read R before cleanup. A later local target change is not included in push. |
| V-8 / A8 | PB.`C448_V08_RejectedPushKeepsWork`: reject real push via local pre-receive hook; separate non-FF writer race. | Source at expected P and S/P pins retained, local target may equal P and is reported truthfully, remote lacks P, no receipt or cleanup. Companion race: another publisher places P despite rejection; independent proof permits AlreadyPresent with rejected-ack state retained. |
| V-9 / A9 | PB.`C448_V09_AcknowledgementIsNotProof`: push exits zero but confirmation fails/noncontains; configure two bare fetch/push URLs with fetch containing P and push endpoint not containing P; change fingerprint at B10/B12. | Unconfirmed/preserved for missing proof; no cleanup. A separate successful push-URL case reads/fetches the actual push endpoint and records it. Ambiguous multiple push destinations and pending endpoint change refuse without silently redirecting. |
| V-10 / A10 | PB.`C448_V10_SourceMutationInvalidatesVerification`: each source identity/status/metadata change at B02, B03, B05, B06, B07, B08, B10, B14, B15 and B16 as applicable. Have verifier return pass for old P. | Old result never advances/pushes/deletes the changed source; `source_changed` or specific identity error; new work preserved. If mutation follows FF, local P is recorded without claiming publication. If publication was confirmed, retain it with cleanup residue. Include same SHA/different symbolic branch and unchanged HEAD/new index bytes. |
| V-11 / A11 | PB.`C448_V11_TargetMutationInvalidatesVerification`: advance target at B07/B08/B10, change checked-out target branch, stage/dirty target files, or race after its pre-FF inspection. Also run target not checked out. | Reprepare/refuse; preserve writer's new tip/bytes; no reset or push of later commits. Bare-ref update uses expected T0 and fails CAS if changed. Checked-out target uses clean matching checkout and FF, leaves index/worktree matching P on success, and detects unexpected post-command HEAD. Retry verifies against the new target rather than reusing old verification. |
| V-12 / A12 | PB.`C448_V12_RemoteMovementNeedsCorrelatedContainment`: remote moves noncontaining before push, after push, or between B12 read/fetch; companion containing descendant R != P. | Non-forcing push/confirmation refuses noncontainment; descendant succeeds and receipt records actual R. Changed read/fetch pair retries exactly up to three complete observations; third mismatch gives `remote_changed_during_confirmation`, no cleanup. A successful correlated observation at 2 or 3 may succeed. |
| V-13 / A13 | LS.`C448_V13_LeaseUsesCommonRepositoryIdentity` plus CO.`C448_V13_TwoServicesFenceOneOperation`: main, linked path, path casing and Windows junction alias contend; distinct repository proceeds; two instances post/run same task. | Only one critical section/mutating child per repo; losing instance pending/held with unchanged attempt and reload after acquisition. DB CAS allows one active operation; stale writer cannot commit progress. Empty stale lock file is acquirable; genuinely held file is not unlinked. Held DB writes do not keep a transaction open across verification. |
| V-14 / A14 | CO.`C448_V14_SharedAdmissionAndLandExcludeEachOther`: existing Shared writer and racing admission, each for Fresh, intact AlreadyPresent, ResumePublication and CleanupRetry. Use B01/B20 to arrange both race orders. | Writer-first: land holds before inspection/mutation and does not spend attempt. Land-first: admission cannot publish running claim/launch until lease release. After release claim/state is reloaded. Checkout alias does not create a different repository. |
| V-15 / A15 | RC.`C448_V15_RestartBoundary`: every C row below, including unchanged/changed variants. Restart using fresh service provider, DbContext, queue and process registry against the same persisted Git/DB state. | Exact allowed command suffix and durable state in crash table; preservation and remote-source assertions before and after restart. Required positive automatic completions are C09-C11 publication resume and C14-C18 cleanup resume. |
| V-16 / A16 | RC.`C448_V16_SaveFailureBoundary`: each F row below, with save-before-write, rollback and commit-acknowledgement-loss variants. | A save exception cannot fabricate durable evidence; independent reads distinguish not-committed from committed. No dependent destructive mutation before required checkpoint acknowledgement. Recovery recognizes only exact operation/identity and never branch disappearance alone. |
| V-17 / A17 | RC.`C448_V17_InterruptedRebasePreservesResolution`: real conflict at B05, stop first worker, manually edit/stage a unique resolution before restart; clean rebase exit but no Prepared is a second row. Live same-attempt conflict abort success/failure and helper completion are companion rows. | Restart returns `interrupted_rebase_requires_inspection`; resolution/index and S pin survive; no abort/rebase/push/delete. Live owned abort restores exact S and creates at most one Merge helper; abort failure retains conflict and prevents another helper. Helper-changed SHA requires new inspection/preparation/verification. |
| V-18 / A18 | CL.`C448_V18_CleanupRetryPreservesNewIdentity`: confirmed receipt; advance/detach/switch/dirty source, check branch out elsewhere, or replace path with a new directory/repository. Inject mutations both before retry and at B14-B16. | Original publication persists, mode CleanupRetry, cleanup refused/pending; new branch/content/registration remain. Existing mismatched components cannot be explained away by an old receipt. |
| V-19 / A19 | CL.`C448_V19_AbsentComponentsNeedMatchingIntent`: directory/registration absent and expected branch survives; all absent; missing branch with directory still present; receipt absent; cleanup intent absent; query errors; mismatched task/repo/ref/P/destination/schema/operation. | Only matching durable publication plus cleanup intent and fresh remote proof authorize idempotent absent-component handling. Surviving branch still requires expected SHA and no checkout. Existing unregistered directory is residue. Failed queries never count as absence. |
| V-20 / A20 | CL.`C448_V20_RemovalFailureKeepsResidue`: Git lock, file handle without delete sharing on Windows, remove timeout/error, partially removed tree, unregistered nonempty directory. Repeat direct low-level removal and service retry. | Ordinary remove, no force escalation/recursive fallback/prune workaround. Keep every remaining sentinel and exact branch until directory/registration removal is confirmed. Counts report kept/refused, not removed; release only the fixture's own file handle. |
| V-21 / A21 | PB.`C448_V21_RefusalNeverDeletesRemoteSource` plus mandatory `RemoteImage` checks in every negative V/data row. Register one named row per refusal family from V1-V20, V23-V25, V27-V29 and V30-V35. | Bare remote source remains at the explicitly published seed SHA after refusal, restart and applicable sweep; trace contains no remote-source delete, force or mirror push. Failing fixture source push fails setup and never counts as a safety pass. |
| V-22 / A22 | SO.`C448_V22_OutcomesReflectEvidence`: successful Fresh with P != S; AlreadyPresent including stale local target; published-with-residue; valid CleanupRetry; rejected-but-contained push. | DTO/event/note agree on S/P/R, mode, publication and cleanup. No `pushed` for no-push path; actual rejected ack retained. Attempts increment once per admitted mode, holds zero; cleanup retry does not add another publication. Stage rows describe executed/skipped/failed work truthfully; no second Rebase/Verify on cleanup-only path and no card Done transition. |
| V-23 / A23 | DB.`C448_V23_EvidenceAndNotificationFailures`: B13/B18 failures before/after commit, delivery failure and event-bus failure independently; call `FailAsync`, then sweep/retry. | No cleanup without acknowledged receipt; committed evidence survives new context/restart and notification failure. Terminal evidence plus event are atomic. No LandRefused replacing confirmed publication. Scheduling clear leaves all operation evidence intact. |
| V-24 / A24 | SW.`C448_V24_HousekeepingUsesSameAuthority`: legacy false Landed/Merged text, missing ref, refused/unconfirmed operation, expired TTL, active landing, untracked/ignored work; preview then mutate source at B19 and execute. Also call real `PruneStaleAsync` with task metadata but no task DB lookup, janitor and residue-job entry points. | Same low-level guard; every negative row preserved with kept/unknown diagnostics; no count as removal. Preview has zero mutation. Typed valid publication/local-merge proof and clean current identity is the positive removal row. Recovery refs/operation metadata survive all sweeps. |
| V-25 / A25 | DW.`C448_V25_LocalCleanupHasDistinctAuthority`: changed child merges to actual parent target; no-change child; dirty/moved parent or child; task worktree reuse; creation rollback before/after working-task ownership. | Supported local merge/no-change cleanup uses typed LocalMerge containment at exact parent SHA, no false remote receipt/Landed claim. Parent/child change refuses unsafe deletion. Rollback undoes only its own unfinished creation; reused/mature worktree is retained and cannot receive rollback authority. Missing/unregistered nonempty paths are not automatically cleaned. |
| V-26 / A26 | PB.`C448_V26_OriginalAndPreparedCommitsRemainPinned`: real rebase P != S, verify P, publish and remove exact source branch; remove fixture reflogs and run isolated Git GC if needed to prove independence. | Durable S/P receipt and separate operation refs resolve both commits after branch deletion and sweeps. Observation pin resolves recorded R. No cross-operation pin overwrite; success does not expire recovery refs. |
| V-27 / A27 | CL.`C448_V27_CleanupRefreshesRemoteProof`: stop after PublicationConfirmed; rewrite/delete remote target or make endpoint unavailable before retry and before each deletion at B14-B16; companion remote fast-forward containing P. | Rewrite/error keeps historical P/R/time but refuses further deletion and reports residue; containing descendant refresh permits guarded cleanup. If directory removal already succeeded, preserve the surviving branch/new checkout. Current tracking ref/local P is never substituted for fresh proof. |
| V-28 / A28 | CO.`C448_V28_ChildOwnershipSurvivesCancellation`: owned Git child waits at a fixture hook/barrier; cancel; separately terminate fixture service worker while child survives; add unrelated fixture child and simulated reused PID/start-identity mismatch. | Lease remains held/unavailable until owned child is exited/awaited or recovery is visibly held for uncertainty; no overlapping mutation. Only matching PID+start identity is an owned child; unrelated/reused PID is never killed. No Git lock deletion. All test-owned children accounted for before teardown. |
| V-29 / A29 | CO.`C448_V29_ActiveSourceClaimHoldsEveryMode`: Succeeded original task and active follow-up/Merge helper with durable claim on exact source, then attempted new claim at B20. Test all four V14 modes and path aliases. | Hold/pending, zero attempt/cleanup while in use; admission cannot race lease. Unrelated worktree in another repo proceeds. Release claim and retry succeeds with fresh identity/verification as required. |
| V-30 / D-1,D-3 | IG.`C448_V30_RefsParsingAndDestinationsFailClosed`: NUL/quoted path cases, spaces/non-ASCII path, OS-appropriate aliases; source=target, `--all`, `HEAD~1`, tag/ambiguous short ref, invalid/symbolic destination, multiple push endpoints, missing objects, ref-collision/CAS failure and oversized stderr with a synthetic secret marker. | Exact ref/commit validation before mutation; full refs and immutable SHAs in argument vectors; no shell parsing/force/default push inference. Error exits stay typed, diagnostics bounded and marker absent. Expected-nonexistence pins cannot overwrite earlier operation refs. |
| V-31 / D-2,D-10 | ST.`C448_V31_TransitionsRequireEvidence` and DB.`C448_V31_MigrationAndConcurrentOperations`: enumerate valid/invalid transitions; upgrade predecessor schema with legacy Landed/missing-source/intact-source tasks; roundtrip complete receipt; stale DbContexts race active insert/transition; explicit retry. | DB enforces one active operation and CAS; no receipt backfill or enum renumbering. Illegal/unverified/out-of-order transitions rejected; remote fact monotonic. Unfinished evidence survives retry/ClearPending; changed destination cannot replace unresolved publication/cleanup. New operation after terminal refusal retains old history/pins. Unknown schema or corrupt/mismatched receipt grants no authority. |
| V-32 / D-2,D-4,D-7,D-8 | RC.`C448_V32_CheckpointsPrecedeMutation`: trace every normal/AlreadyPresent/resume path; fail or collide each pin; enable fixture `rebase.autoStash` and `rebase.updateRefs` config; make verification fail/cancel/throw. | Committed S/T0 and correct pins precede owned rebase; Prepared P precedes required verification; recorded pass or documented exact-SHA skip precedes target/push; acknowledged publication and cleanup intent precede removal. Rebase explicitly disables both config options, unrelated refs/stash unchanged. Verify failure preserves P and never advances/pushes/removes. No recovery pin is overwritten/deleted. |
| V-33 / S5,S6 | SO.`C448_V33_StructuredOutcomeConsumers`: serialize all publication/cleanup/mode combinations and exercise server readiness/residue success consumers with receipts and contradictory event prose. Add `AgentTaskPipelineStatusTests.C448_V33_PipelineUsesLandingEvidence` for the server projection. Client `TaskDrawer.test.tsx` renders AlreadyPresent/unconfirmed/residue event payloads; `pipelineStageModel.test.ts` consumes the resulting server-projected pipeline rows. | Added enum is appended, all earlier numeric values pinned; structured facts control publication success. Unconfirmed must not be presented as successfully landed, though it may remain ready for a subsequent stage/retry under existing pipeline rules. Drawer text and pipeline rows do not invent a push or independently reclassify raw landing events. No new card/session state. Documentation/manual checks below match DTO truth. |
| V-34 / S3,S6 | PB.`C448_V34_RealVerifierRunsExactFilter`: one tiny fixture `tests/Antiphon.Tests` executable TUnit project using the repository's pinned package configuration, with one passing selected test and one deliberately failing excluded test. Invoke production verifier with the passing class filter; companion selected failure/zero-tests rows. | Actual fresh TRX contains selected method, nonzero count, excludes failing class; uses `dotnet run --project`, argument-list filter forwarding and isolated output. Failure or zero executed tests cannot authorize publication. Other safety cases use deterministic verifier; no recursive full-repository build per row. |
| V-35 / D-1,D-8 | CL.`C448_V35_VerificationOutputAndReportDeletionNeedOwnership`: pre-existing ignored `bin-land/keep.txt`, arbitrary nested `bin-*`, opaque report versus matching durable archive; verifier succeeds/fails/cancels. | Verifier's current recursive `finally` cleanup cannot erase pre-existing/unknown files. Generated output may be removed only with established ownership and unchanged identity; matching archived report permits only its verified copy's disposal. Changed/missing archive retains report. Guard output cleanup even when verification itself fails; broad ignore patterns never grant authority. |
| V-36 / D-6,D-8,D-9 | CO.`C448_V36_AllMutationCallersUseLease`: barrier-held land versus settlement commit, child merge-back, low-level remove, residue execution and TTL prune, both ordering directions; nested calls reuse the acquired lease. CL.`C448_V36_RemovalRejectsForgedOrMissingAuthority` invokes the public removal surface directly with missing/forged/stale/wrong-repo proof and no valid lease. | Every caller waits/reloads or refuses; no nested reacquisition deadlock; revoked/wrong-repo lease or DTO alone cannot authorize mutation. Old permissive interface default/unknown-purpose deletion cannot bypass policy. Direct calls preserve bytes and branch just like the service. |

#### Crash/restart matrix (V-15)

**V-17/F1 subrow:** `C448_V17_ResolvedInterruptedRebaseCanOpenNewOperation` resolves/aborts a real interrupted rebase in place, then explicitly re-POSTs with the SAME task coordinates. Fresh leased inspection admits a new operation and fresh verification, while the old refused row and pins survive. Dirty/still-active/mismatched source and automatic sweep companions remain refused. Link this row from C23.

**V-33/F2 subrow:** `C448_V33_BackfillCannotInventLandingEvidence` executes the backfill entry point with contradictory Landed/LandedWithResidue prose and appended AlreadyPresent events. Require structured-evidence-derived rows or explicit retirement with zero invented rows; no green rebase/verify/cleanup outcome may arise from prose.

For each C row, persist only the state that existed at the cut. Test both abrupt worker termination (required at C03, C05, C09, C12, C14, C16, C17 and C24) and deterministic in-process checkpoint faults for the rest. Killing is restricted to the fixture worker PID/start identity; never kill the shared test runner/server. A caught exception that lets normal finally/abort/cleanup run is **not** an abrupt-crash simulation. Start a new worker for the real-process rows and a new provider/queue/context for every row. Check allowed command suffixes by semantic command/operation identity, ignoring harmless repeated read-only probes.

| Cut | Last durable evidence / disk fact | Required restart behavior and forbidden suffix |
|---|---|---|
| C01 | Request queued, or held on lease/Shared/helper claim; no snapshot. | Reacquire and inspect. Still-held claim leaves pending/attempt unchanged; no success or deletion. |
| C02 | Inspected or recovery intent committed, no pins. | Revalidate S/T0/metadata, complete pins with expected nonexistence, then prepare. Changed state refuses without rebase. |
| C03 | Some pins created, before ref-result save. | Independently compare each existing pin; complete only missing correct pins. Collision or changed S/T0 retains pins and refuses. No overwrite/cleanup. |
| C04 | All pins verified, before `RebaseStarted` commit. | Save start intent before invoking rebase. Failed save forbids rebase. |
| C05 | `RebaseStarted`, child active or conflict state, no Prepared. | Process-ownership check, then held/`interrupted_rebase_requires_inspection`; original pin and manual edits preserved. No blanket abort or new rebase. |
| C06 | Rebase exited clean at P, Prepared absent (also prepared pin created but save absent). | Same ambiguity refusal even with clean status and P ancestry; pins are recovery assets, not a durable Prepared checkpoint. |
| C07 | Prepared P saved; verification not started, interrupted, or completed in memory only. | Matching source P permits rerun of required verification exactly once on resumed attempt; changed source refuses. No pre-verification advance/cleanup. |
| C08 | Verified P saved, before target intent. | Revalidate P/T0/config and advance only exact expected T0. Changed source/target refuses; next attempt prepares/verifies anew where needed. |
| C09 | TargetAdvanceStarted saved; target still T0 or now P, LocalTargetAdvanced save absent. | At T0 perform guarded FF; at exact P independently record completed advance, without second FF/rebase/verification. Different target refuses. Both clean matching variants then publish recorded P. |
| C10 | LocalTargetAdvanced P saved, before PushStarted. | Save push intent and publish exact P to recorded destination, independently confirm and clean. No ancestry-based AlreadyPresent cleanup shortcut. |
| C11 | PushStarted saved, no push child started. | Independently inspect remote. Unchanged destination safely accepts non-forcing P retry; already containing P can be confirmed without push. Changed noncontaining/error preserves work. |
| C12 | Push child accepted update, ack lost; or push exited, remote receipt absent. | Read/fetch/correlate remote; containing P commits receipt without repeating push. Unchanged remote allows bounded non-force retry. Noncontaining movement/error refuses/unconfirmed; no deletion. |
| C13 | Correlated R contains P in memory, publication save not committed. | No deletion before restart. Re-observe remote and commit receipt before cleanup; never promote the old in-memory observation. |
| C14 | PublicationConfirmed committed; cleanup not begun, including lost commit acknowledgement. | Refresh remote, source and proof identity; CleanupRetry only, no rebase/verify/target advance/push. Noncontaining/unavailable remote leaves historical publication with residue. |
| C15 | CleanupStarted/expected removal identity committed; directory/branch still present. | Fresh guarded cleanup. Changed/dirty/recreated source stays; no publication repeated. |
| C16 | Worktree remove succeeded; directory/registration absent; branch still expected P, result save absent. | Independently prove absence, refresh remote, inspect all registrations and delete exact branch by old-SHA CAS. Branch moved/checked out/query error preserves it. |
| C17 | Branch deletion succeeded; Complete save absent. | Receipt plus cleanup intent and successful current absence checks permits Complete. Missing intent/mismatched receipt/error refuses. No new publication or branch guessing. |
| C18 | One removal component remains, or unexpected nonempty/unregistered directory. | Report component-specific residue, retain surviving branch/bytes, never recursive fallback or success-by-event. |
| C19 | Published receipt, then directory/ref recreated or source modified. | Preserve new work, original receipt unchanged, cleanup refused/pending. Absent-component intent cannot excuse existing identity mismatch. |
| C20 | Complete committed, duplicate explicit request. | Same operation/publication classification, observed current cleanup result; no second publication, no regeneration/deletion of unrelated new work. |
| C21 | Receipt exists, outcome/event transaction rolled back. | Historical publication retained; resume outcome/guarded cleanup from receipt. No success event from rolled-back transaction. |
| C22 | Outcome/event committed, delivery/SignalR not sent or failed. | Stable evidence and same classification; notification retry if existing mechanism supports it. Do not require exactly-once transport delivery or recast as LandRefused. |
| C23 | Explicit re-POST or automatic sweep after an interrupted attempt. | Same active operation and preserved pins/evidence; bounded automatic-attempt budget and per-mode increment. Terminal refusal with newly specified identity makes a new retained-history operation; unresolved published/cleanup identity cannot be silently replaced. |
| C24 | Live mutating child survived server worker; PID matching, PID reused, or ownership unprovable. | No overlapping writer or premature lease release/admission. Only proven owned child is awaited/terminated; uncertainty visibly holds/refuses and preserves work. Separate unrelated fixture child stays alive. |

For C09-C17 add a crossed-receipt negative row: use another task/operation's otherwise valid receipt, or change expected source/destination while keeping matching SHAs. It must not recover or delete the subject. Include source dirtiness/metadata changes at each resumed mutating phase; shared/helper holds apply on every restart, including C14-C20.

#### Persistence-failure matrix (V-16)

| Fault | Commit to fail or acknowledgement to lose | Assertions before retry and after a fresh-context retry |
|---|---|---|
| F01 | Inspection/recovery intent; individual pin result; RecoveryPinned; RebaseStarted. | No rebase without acknowledged complete prerequisite; existing pins retained and compared, never overwritten. |
| F02 | Prepared/prepared pin result; Verified or verification skip evidence. | Preserve S/P; no target advance/publication from in-memory success. Missing Prepared after started rebase is ambiguous; Prepared without Verified reruns verification. |
| F03 | TargetAdvanceStarted; LocalTargetAdvanced after Git mutation. | Failed intent forbids target mutation; failed post-mutation save truthfully leaves target P and resumes only via exact saved intent/verified P. Changed T0/P cannot be adopted. |
| F04 | PushStarted; push acknowledgement/status save after remote acceptance. | No push without acknowledged intent; remote acceptance without ack requires independent containment, never cleanup by exit status. |
| F05 | PublicationConfirmed, including a committed transaction whose acknowledgement throws. | Current invocation performs no dependent cleanup on ambiguous save. New context distinguishes commit/no commit; retry refreshes remote and commits/uses matching receipt before deletion. |
| F06 | CleanupStarted; directory/registration result; branch result/Complete. | Failed cleanup intent forbids remove; successful mutation with failed result save recovers only via matching durable intent and fresh component checks. No fallback deletion. |
| F07 | Atomic terminal evidence/event/pending changes; separately delivery and SignalR. | Transaction rollback changes neither of its coupled rows; previously committed receipt remains. Post-commit failures cannot erase publication or change it to refusal; ClearPending preserves evidence. |

### Guards the regression

Each R item is an ongoing regression requirement, caught by the named V test's actual assertion. Preserve these even if helpers/services are refactored; a search for a guard's spelling is not regression coverage.

| ID | Future defect | Caught by / failing assertion |
|---|---|---|
| R-1 | Reintroduce branch-ancestor/missing-branch shortcut ahead of identity checks. | V1-V3: exact unique HEAD/directory/ref preserved and no AlreadyPresent. |
| R-2 | Treat matching SHAs or report prose as source identity; ignore registration/metadata changes. | V2,V3,V10,V18: exact symbolic/full-ref and canonical registration mismatch refuses. |
| R-3 | Ignore dirty/untracked/opaque ignored content or assume generated output/report is disposable. | V4,V18,V24,V35: bytes/index survive, no forbidden deletion request; matching ownership/archive is required. |
| R-4 | Collapse errors/timeouts into absent/ancestor/clean, or read stale tracking refs. | V3,V5,V9,V12,V19: unknown is preserved and no receipt/cleanup is granted. |
| R-5 | Claim push from local target or successful process exit; observe wrong endpoint. | V7-V9,V12,V27: receipt P/R matches independent push-endpoint containment before deletion. |
| R-6 | Use moving branches/default refspec/force push; skip verification source/target fences. | V10-V12,V30,V32: exact pinned arguments, unchanged new writer's work, zero unsafe advance/push. |
| R-7 | Begin mutation before committing intent/pins/proof; trust an in-memory phase after save failure. | V15,V16,V23,V31,V32: cross-connection boundary read establishes exact prerequisite; forbidden command suffix absent. |
| R-8 | Blindly abort/re-run an interrupted rebase or reuse verification for a helper's new SHA. | V17,C05-C08: staged manual resolution and S pin retained; no unowned abort, fresh verification required. |
| R-9 | Turn local FF/crash into already-pushed, or refuse all resumable states indefinitely. | C09-C14: recorded P is automatically published/confirmed or cleanup resumed with exact command counts. |
| R-10 | Delete first, check later; select branch from current HEAD/name; skip branch CAS/checkouts. | V18-V20,V36,C16-C19: last-moment mutation preserves remaining exact ref/checkout/bytes; no premature branch delete. |
| R-11 | Restore `--force`, recursive fallback, unknown-purpose/interface default or TTL escape hatch. | V20,V24,V25,V35,V36: low-level request refuses/preserves and kept counts stay honest. |
| R-12 | Use process-local/per-worktree locking or admit Shared/helper writer in the gap. | V13,V14,V29,V36: single critical section and no durable admission during land, all modes covered. |
| R-13 | Release lease with live child, kill by PID alone, or delete Git/lease locks to retry. | V28,C24: child ownership/start identity, lease contention and unrelated-child liveness assertions. |
| R-14 | Forget legacy tightening, active-operation CAS, monotonic evidence or operation identity. | V19,V23,V31 and crossed-receipt C09-C17: no backfill/forged authority/stale transition/evidence loss. |
| R-15 | Reuse old remote proof after rewrite or prune original/prepared recovery refs. | V26,V27: fresh containment gates cleanup; S/P/R pins survive branch deletion, GC and janitors. |
| R-16 | Flatten outcomes into `landed ... pushed`, duplicate publication on retry or consume prose as success. | V22,V23,V33: typed DTO/event/note agree; no-push path says no push; no second publication/false ready state. |
| R-17 | Break normal child merge/no-change/rollback/reuse while tightening land. | V25,V36 and existing DW regressions: supported typed local cleanup succeeds; mature/changed work is retained. |
| R-18 | Treat build/zero-test/fixture failure as verification or mutation-test evidence. | V34 and execution gate below: selected method and nonzero fresh TRX counts, assertion-specific RED, rebuilt GREEN required. |
| R-19 / F1 | Permanently strand an in-place resolved interrupted rebase because its coordinates did not change. | V17/F1: explicit re-POST after fresh leased clean attached inspection creates a new operation; old history/pins retained, no automatic reopen. |

Existing tests to revise deliberately: DW.`prepare_land_aborts_an_interrupted_rebase_first`, DW.`already_landed_arm_pushes_a_target_that_is_ahead_of_origin`, SW.`classify_landed_succeeded_ignores_untracked_only`, and DW's self-removed/unregistered-leftover cleanup tests encode the former inference. Replace their expectations with D-4/D-8/D-9 while retaining separately authorized child-merge behavior. Keep existing conflict-helper, request, automatic-attempt cap and stage-outcome coverage. A legacy assertion changed merely to make green needs its replacement V/R ID in the Code report.

### Positive controls

Run **every numbered PC and every independently named variant** GREEN -> one localized production mutation -> assertion-level RED -> restore -> rebuilt GREEN. PC-1 additionally demonstrates the real baseline incident. These are required experiments for Code, not mutations to commit. The one-line edits below name the production decision to change; Code records the actual file/line/diff once S1-S5 introduce those decisions. Do not invent public helper names solely to match this table. A multi-clause gate requires one mutant per listed clause. If the implementation splits an authorization gate, exercise its low-level decision directly with the production policy and mutation-capable downstream fake, plus the real-Git V test; do not disable several unrelated guards to obtain a red.

For a mutated refusal guard, RED must fail `ShouldBe(Refused/Unconfirmed/Held)`, `NoDestruction`, an expected-old-SHA argument assertion at the deletion boundary, or a specific bytes/ref/registration-preservation assertion. An exception thrown by the **assertion** at that boundary counts; a generic Git error, setup failure, timeout, build failure, no tests, or a later incidental failure does not. Natural Git protection can preserve files despite a bypass: in that case require the unauthorized request/decision assertion to fail at the guard being tested and retain the independent state assertions in the green arm. A surviving mutant is a coverage failure; refine the fixture, do not call it an expected pass. Every positive refusal row must first show the legitimate command path is reachable in an unchanged safe companion fixture.

| ID | One localized production edit (independent variants separated by semicolons) | Test / required RED assertion |
|---|---|---|
| PC-1 | Bypass detached-HEAD refusal. Also run V1's unique-U case against baseline `f202fcc1` through a baseline-compatible test adapter, changing test plumbing only. | V1: detached decision/`NoDestruction`; baseline must actually fail unique-U directory/HEAD preservation, with setup completed. Keep its committed sentinel clean so Git's dirty guard cannot hide the incident. |
| PC-2 | Replace exact symbolic-ref comparison with SHA-only equality; separately accept case-insensitive ref equality. | V2 equal-SHA/case-ref rows: mismatch refused and both refs/checkout preserved. |
| PC-3 | Skip common-dir match; skip registration/path identity; skip actual-HEAD/recorded-ref SHA match; accept changed task coordinates. | V3/V10: refusal and zero removal/advance for the mismatched component. |
| PC-4 | Skip second identity reading around status. | V10 at B02: source_changed and zero unsafe publication; the first snapshot must be valid. |
| PC-5 | Treat failed ref/status/registration query as absent/clean; map ancestry exit 128 to contained; treat timeout/cancel as negative/absent success. | V3/V5/V19 data rows: exact unknown classification/no success or deletion, not an unrelated parser exception. |
| PC-6 | Exclude staged; unstaged; untracked; dirty nested/submodule state from content refusal (separate edits). | V4 and B15 low-level rows: expected content refusal/`NoDestruction` plus original bytes/index. |
| PC-7 | Treat opaque ignored `.antiphon`; `.claude`; `bin-*` files as generated/disposable. | V4/V35 separate rows: protected sentinel retained and no unauthorized cleanup request. |
| PC-8 | Accept report archive existence without byte/hash match; accept unowned or changed verifier output as disposable. | V35: changed/missing archive or pre-existing `bin-land/keep.txt` remains, including verifier failure/cancel. |
| PC-9 | Ignore active merge/cherry-pick/rebase/sequencer state on fresh inspection (each state). | V4: refusal and no reset/abort/rebase/cleanup of the existing operation. |
| PC-10 | Use cached remote ref after failed fetch/observation. | V5: no AlreadyPresent/receipt/cleanup even though cache contains S. |
| PC-11 | Classify local-only containment as AlreadyPresent. | V7: actual P push/independent receipt occurs before cleanup; no-push shortcut fails assertion. |
| PC-12 | Replace post-push independent proof with `pushExit == 0`. | V9: Unconfirmed and zero remove on confirmation failure/noncontainment. |
| PC-13 | Confirm at fetch URL instead of frozen push endpoint; accept changed or multiple destinations. | V9/V30: wrong-endpoint no-proof/refusal and unchanged target/source at unintended endpoint. |
| PC-14 | Accept a remote SHA without P ancestry; accept read/fetched-ref mismatch; bypass three-observation exhaustion. | V12: noncontainment/uncorrelated proof refuses with no receipt/removal; bounded trace/reason after observation 3. |
| PC-15 | Drop fresh remote confirmation from cleanup retry. | V27: rewritten/deleted/unavailable remote produces residue and zero deletion despite historical receipt. |
| PC-16 | Ignore remote ahead/divergent preparation check. | V6 noncontaining-S rows: refresh refusal and no implicit publication/target mutation. |
| PC-17 | Push a moving branch/default refspec instead of P; add force/mirror option. | V11/V12/V30: pinned argument assertion and competing remote/local tip preserved; force row uses a real non-FF writer fixture. |
| PC-18 | Skip post-verification source ref/HEAD; status; registration; metadata recheck (each). | V10 B07 rows: source_changed/zero target advance or push from old verifier pass. |
| PC-19 | Skip source recheck after fetch, before/after rebase, before verification, advance, push or deletion (each distinct boundary enforcement). | V10 named barrier row reaches that boundary with a valid earlier snapshot, then fails its immediate refusal/forbidden-next-command assertion. Later guards must not be the sole oracle. |
| PC-20 | Skip target expected-SHA recheck; dirty checkout check; symbolic branch check; post-FF HEAD check (each). | V11 respective rows: refuse/retain writer state and no later push; target index/worktree remains correct in safe companion. |
| PC-21 | Remove expected-old T0 from un-checked-out target update; replace checked-out target FF with ref-only update. | V11 at B08: CAS retains raced target; checked-out success row fails index/worktree-equals-P assertion under ref-only mutation. |
| PC-22 | Continue after required recovery intent/pin save failure; omit source pin; omit target-before pin; overwrite existing operation pin. | V16 F01/V30/V32: no rebase before complete acknowledged pins; original/colliding ref survives. |
| PC-23 | Omit autostash-disable; omit updateRefs-disable. | V32 with config enabled: argument-level disable assertion and unrelated refs/stash preservation; dirty-source guard cannot be the only check. |
| PC-24 | Run blanket abort on restart lacking Prepared. | V17/C05/C06: manual staged resolution/S pin preserved and no abort requested. |
| PC-25 | Treat Prepared/in-memory verification as Verified; accept failed/zero-tests verification; reuse old verifier pass after helper changes SHA. | V15 C07/V32/V34/V17: no target/push until recorded pass or exact-SHA permitted skip, verification rerun observes new P. |
| PC-26 | Skip TargetAdvanceStarted save prerequisite; skip PushStarted save prerequisite. | V16 F03/F04: cross-connection prerequisite and zero respective mutation on save failure. |
| PC-27 | Resume local advance from current target instead of exact stored P/T0/operation identity. | V15 C09 changed/crossed-receipt rows: refusal with rival tip intact and no push. |
| PC-28 | Route LocalTargetAdvanced/PushStarted directly to cleanup on local ancestry. | C10-C13: remote still lacks P at attempted cleanup boundary; no removal until independent committed proof. |
| PC-29 | Permit removal using in-memory/uncommitted PublicationConfirmed; treat save exception as acknowledged. | V16 F05/V23: no remove without independently committed, acknowledged receipt; assertion fires at removal boundary. |
| PC-30 | Ignore receipt task; operation; repository; source ref/P; destination; schema match (each). | V19 crossed-proof rows/V31: proof rejected/`NoDestruction`, even with otherwise containing remote. |
| PC-31 | Skip cleanup-intent save prerequisite; treat query error as successful component absence; accept absent components without matching receipt/intent. | V16 F06/V19: zero remove before intent and no Complete/branch delete on unknown or unproven absence. |
| PC-32 | Move content/identity validation after directory deletion; trust removal DTO without low-level re-read. | V18 at B14/B15/V36 direct call: zero premature removal, changed/new bytes preserved. |
| PC-33 | Restore force worktree remove (including second-force escalation). | V20 locked/dirty boundary rows: force argument is forbidden and lock/bytes/registration preserved; assert command vector before executing fixture mutation. |
| PC-34 | Restore recursive-delete fallback after failed/timed-out/unregistered removal; restore prune workaround. | V20: remaining sentinel and branch preserved; recursive/prune call forbidden even if filesystem would itself reject it. |
| PC-35 | Allow branch delete before directory/registration removal is confirmed. | V20 partial-removal row: branch deletion never attempted; surviving ref retained. |
| PC-36 | Select deletion branch from current HEAD/directory-name fallback instead of expected full ref. | V18/V19: exact authorized ref assertion fails; switched/recreated branch remains in green. |
| PC-37 | Remove expected-old-SHA from branch deletion. | V18/V19 at B16 after final read: fixture moves branch just before update-ref; expected-old argument/CAS preserves new tip in green, mutant red. |
| PC-38 | Omit all-worktree checkout inspection; accept a failed registration query as no checkouts. | V18/V19 B16: branch checked out elsewhere/error refuses and no deletion requested, matching P alone is insufficient. |
| PC-39 | Restore unknown-purpose/default-interface pass-through; admit a forged/stale/wrong-repo lease as removal authority. | V36 direct low-level calls: explicit refusal and zero mutation on missing typed authority or valid lease. |
| PC-40 | Classify legacy Landed/Merged or TTL age as authority; ignore untracked/ignored residue after success; omit execution revalidation at B19. | V24 each entry point/data row: exact bytes/ref kept, no removed count, stale preview cannot authorize execution. |
| PC-41 | Treat mature/reused task cleanup as creation rollback; accept LocalMerge without exact current parent containment/identity. | V25: wrong-proof refusal and preserved child/parent/new task content. Safe unfinished rollback and local merge companions still succeed. |
| PC-42 | Key lease by worktree path or in-process semaphore; remove lease from one participating caller (each V36 caller); reacquire instead of threading nested lease. | V13/V36: max critical-section count 1, no mutation while contender blocked; nested-call barrier completes without timeout. A timeout alone does not satisfy RED: assert duplicate acquisition request before a potential wait. |
| PC-43 | Move Shared check after shortcut; omit claim reload under lease; admit Shared claim without lease. | V14 each mode/race direction: held/pending and zero mutation or prohibited durable admission. |
| PC-44 | Ignore active follow-up/helper exact-source claim; allow its admission outside exclusion. | V29 each mode/race direction: held and intact directory; no claim may appear during land's critical section. |
| PC-45 | Release lease before owned child exits; kill by PID without start identity; delete a held lease/Git lock to retry. | V28/C24: child still live implies no new mutation, unrelated child alive, owned lock remains; assert ordering/ownership at kill/release boundary. |
| PC-46 | Remove active-operation unique DB constraint/CAS check; allow retry/ClearPending to discard unresolved evidence; accept unknown schema. | V31/V23: concurrent insert/stale transition rejected and operation/pins survive. Constraint mutant is a localized schema-test alteration in its disposable DB, followed by schema recreation/restored green, never a production DB edit. |
| PC-47 | Prune S/P recovery refs after success/TTL; overwrite another operation's namespace. | V26/V24/V30: original/prepared/observation pins and competing operation refs resolve expected SHAs after cleanup/sweep. |
| PC-48 | Rewrite confirmed publication to Refused in failure handler; save terminal event outside atomic transaction. | V23/V16 F07: historical proof retained; transaction fault yields neither partial terminal event nor partial scheduling/evidence transition. |
| PC-49 | Map AlreadyPresent or cleanup retry to a new pushed publication; use event prose instead of structured publication state. | V22/V33: no-push text, publication count and readiness/DTO/event consistency assertions fail. |
| PC-50 | Skip attempt increment on AlreadyPresent/CleanupRetry; increment on lease/claim hold. | V22/V14/V29/V15 C23: exact per-task attempt delta and bounded sweep budget fail. |

PC-1 baseline execution must not replace the real plan/code worktree with old files or contact live origin. Create a separate disposable checkout at `f202fcc1`, transplant only the baseline-compatible incident test/harness, prove it compiles and reaches the destructive path, record assertion RED, and dispose only that checked fixture root. If a baseline unrelated build failure occurs, isolate/fix test plumbing and rerun; it is not the required incident evidence. All other PCs mutate the implemented code in an isolated task-owned verification checkout or with byte-exact backups of only the changed files. Save each patch and test result; restore in `finally`, update restored source last-write time, rebuild, and verify the executed DLL is fresh. Never commit a mutant or mistake a stale mutated DLL for restored-green evidence (testing owner CARD-0412).

### Execution, filters and acceptance record

After Code creates the methods above, use one exact class per invocation. Run from the Code task checkout. This PowerShell function uses the documented runner, unique TRX names and no shell-composed test arguments:

```powershell
function Invoke-C448Test {
    param(
        [Parameter(Mandatory)][string] $TestClass,
        [string] $TestMethod = '*',
        [Parameter(Mandatory)][string] $EvidenceTag
    )
    $c448Trx = "$EvidenceTag-$TestClass-$([guid]::NewGuid().ToString('N')).trx"
    $c448Filter = "/*/*/$TestClass/$TestMethod"
    & dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c448/ -- `
        --treenode-filter $c448Filter --report-trx --report-trx-filename $c448Trx
    $c448Exit = $LASTEXITCODE
    Write-Output "C448 exit=$c448Exit filter=$c448Filter trx=$c448Trx"
    # Preserve this exit and the fresh TRX, including for expected assertion RED.
    # The caller evaluates the evidence gate below; exit zero alone is insufficient.
}

Invoke-C448Test -TestClass LandSourceIdentityTests -TestMethod C448_V01_DetachedHeadIsPreserved -EvidenceTag pc01-green
Invoke-C448Test -TestClass AgentTaskLandRecoveryTests -EvidenceTag recovery-green
```

Required backend classes (exact `/*/*/<class>/*` filters), sequentially: `LandingGitTests`, `LandSourceIdentityTests`, `AgentTaskLandingStateTests`, `AgentTaskLandingPersistenceTests`, `RepositoryMutationLeaseTests`, `AgentTaskLandPublicationTests`, `AgentTaskLandConcurrencyTests`, `AgentTaskLandRecoveryTests`, `AgentTaskLandCleanupSafetyTests`, `DelegationWorktreeTests`, `WorktreeResidueSweepTests`, `AgentTaskLandRequestTests`, `AgentTaskLandSweepTests`, `AgentTaskLandStageOutcomeTests`, `AgentTaskPipelineStatusTests`, `WorktreeManagerTests`, `WorktreeManagerSafetyTests`, **`WorktreeManagerGitIntegrationTests`**, `DelegationTestServicesTests`, `DelegationHarnessCensusTests`, `TestLaneCategoryGuardTests` and `ProcessSpawnLimitTests`. `WorktreeManagerTests.cs` contains three classes: selecting only `WorktreeManagerTests` does not run its real-Git removal tests. Add limiter attributes to touched process-spawning legacy fixtures and include new spawning fixtures in the limiter guard's census as required by the owner; do not silently omit their real-Git cases. Preserve request endpoint 202/active-request 409 validation in `AgentTaskLandRequestTests`: a queued response precedes Git work, malformed/non-Succeeded requests do not mutate or grant evidence, and a re-POST preserves the active operation. Use the guarded test host for any HTTP assertion.

Use a method filter from the V table for each PC's RED/restored-GREEN iteration, including all named variants relevant to that mutant. Run the complete named affected class after restoring that class's last mutant, then the complete new safety class set against the final unmutated build. Combined class filters are optional only using the testing owner's documented parenthesized-per-operand syntax and checking actual execution names. Do not run a namespace-wide/full-assembly suite merely to avoid enumerating this list. No Pty assembly is forced; if a later change requires it, run it after Antiphon.Tests, never concurrently.

Required client commands for V33 (extend these existing files; invoke individually so scope is visible):

```powershell
pwsh -File scripts/test-client.ps1 features/delegations/TaskDrawer.test.tsx
pwsh -File scripts/test-client.ps1 features/orchestrator/pipelineStageModel.test.ts
```

The wrapper executes with client working directory; these suffix filters must each select the intended file and nonzero tests. Additional rendering consumers actually edited in S5 get their exact file filter in the evidence table; inspect the typed event consumer rather than adding snapshots of unused mock text. Run client type-check/build through its existing build script if the API union/DTO changes; this is not browser acceptance and does not require shared-stack startup.

For each run locate its unique file with `rg --files --hidden --no-ignore tests/Antiphon.Tests -g '<printed-unique-trx-name>'`. Parse the fresh TRX `ResultSummary/Counters`, `UnitTestResult` outcomes and test definitions using the TRX namespace. Require creation after the invocation, nonzero **executed** count, expected class/method and each required parameter case, no unintended broad selection, and no fixture/initialization error. For GREEN require exit 0, failed 0 and all selected cases Passed; a skipped required Windows/DB/process case is pending. For RED require nonzero exit with the intended named test's assertion failure and expected message/stack/actual value. A fresh zero-test TRX, compilation error, fixture crash or discovery listing earns no V/PC credit. Archive sanitized command trace, crash cut/worker identities, before/after state hashes, unique TRX path/counts, and each temporary mutation patch with the Code report.

For V31 the upgrade fixture applies the **predecessor migration** to its empty private database, seeds legacy rows with SQL/schema-compatible models, then applies the CLI-generated CARD-0448 migration through `IMigrator`; independently query rows/constraints afterwards. A clone already at latest schema only proves fresh-model roundtrip, not upgrade. Test duplicate active operations at the database boundary as well as through the service, and force competing DbContexts to use the same original concurrency token. Do not hand-author the migration or use the dev database.

Perform a reviewable caller census after S4/S5: `rg -n 'TryRemoveAsync|RemoveAsync|PruneStaleAsync|TryDeleteDirectory|Directory.Delete|worktree.*remove|branch.*-D|update-ref|AlreadyPresent|IsAlreadyLandedAsync|AbortInterruptedRebaseAsync' server/Application server/Infrastructure/Git server/Domain client/src/api/agentTasks.ts`. Classify **every matching destructive caller**, including verifier output cleanup, creation rollback, residue job, janitor, settlement/merge-back, hook-triggered removal and any new helper, by typed authority + lease + final identity/content guard + V/PC evidence. This census locates review obligations; tests must execute the paths. Broaden the search to a new implementation location if a caller moves. A caller without an evidence row blocks Review approval.

**F2/F3 required expanded census:** use `rg -n 'TryRemoveAsync|RemoveAsync|PruneStaleAsync|TryDeleteDirectory|Directory.Delete|worktree.*remove|branch.*-D|update-ref|AlreadyPresent|IsAlreadyLandedAsync|AbortInterruptedRebaseAsync|push .*--delete|DeleteBranchAsync|--mirror|push .*--force|StageOutcomeBackfillService' server/Application server/Infrastructure/Git server/Domain client/src`. Explicitly classify `GitService.DeleteBranchAsync` and its `WorkflowEngine.DeleteWorkflowAsync` caller, and positively establish that no land-adjacent path reaches remote source deletion. Include the live stage backfill consumer even if retired. The acceptance table also includes R-19 and the F1/F2 subrows above.

S6 manual verification: inspect changed `docs/orchestration-loop.md` sections 0/5/8, `docs/ops-http.md`, `docs/antiphon-api.md`, landing help/bundles and any changed `delegate.ps1` formatting against the V22/V33 fixtures. Remove operational promises that missing source means success, that local target means pushed, or that LandRefused guarantees no local advance. Historical investigation quotes may retain the old text when explicitly described as the bug. Confirm DTO fields/reason codes and AlreadyPresent/cleanup-retry semantics match implementation. Read-only searches plus document review suffice here; no live land/restart/probe is authorized by this verification run.

Code's acceptance table must contain one row per V-1..V-36, R-1..R-18, C01..C24, F01..F07 and PC-1..PC-50 **with variant subrows where required**, giving exact filter, executed/passed/failed/skipped counts, intended RED assertion, restored-GREEN result, trace/TRX artifact and remaining limitation. R rows may link the same V evidence; C/F rows may link named data rows rather than duplicating runs. Do not collapse all PCs into a single checkmark. Include both mandatory automatic recovery successes, independent S/P/R evidence, migration upgrade result, Windows real-process/alias/locked-file results, caller census, and no-mutant final diff/build identity. Any required missing/green mutant is unfinished; return next: code/review with the specific gap, never land on aggregate green alone.

### Out of scope

- Production task recovery, live origin/board inspection, remote source deletion, stack restart/deploy and live worktree cleanup. All destructive tests operate only on disposable fixture repositories. First production use remains the separately supervised rollout described above.
- Exhaustive failures of arbitrary external filesystem writers between two individual OS/Git operations. The tests cover every explicit fence, exact Git CAS/non-forcing behavior and participating writer exclusion; they do not claim a filesystem transaction against editors that ignore the lease.
- Disk-controller/power-loss durability beyond acknowledged database commits and Git ref updates; the crash model is server/process failure plus I/O/acknowledgement faults. No guarantee of remote history remaining immutable is made: current cleanup must refuse after a rewrite.
- Exactly-once delivery of an external notification, a new remote retention policy, performance benchmarking, browser E2E and unrelated Pty/full assembly suites. Evidence/event atomicity and truthful retries are covered; retained recovery refs have no TTL in this card.

### Cost

Suites forced: the 22 named backend classes in `Antiphon.Tests`, two scoped client files, the conditional client build, the predecessor-to-current migration test, and the isolated baseline incident run. Estimate approximately 25-40 minutes for initial build, container/migration setup and the full unmutated scoped matrix, assuming roughly 200-350 small Git/DB/barrier data rows rather than a repository build per case. Budget a further 90-150 minutes for the 50 PC families and their independent variants, localized rebuilds, required fresh-process crash runs and restored-green passes: **verification floor approximately 115 minutes; practical budget 2-3+ hours**, excluding implementation and any defect diagnosis. These are planning estimates, not measured results. One real verifier fixture and per-class filtered runs avoid multiplying full builds by the crash/content matrix. Do not shorten the safety matrix to meet the estimate; report measured counts/times and isolate fixture cost if the floor is exceeded.
