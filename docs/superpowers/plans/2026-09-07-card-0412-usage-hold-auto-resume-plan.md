# CARD-0412: repair usage holds and resume blocked work

Plan complete under the defaults below. Fix hour-only reset parsing and stale hold repair, then add durable capacity-wait bookkeeping to the existing recovery, queue, supervisor and dispatcher. Both expiry and deliberate clear make still-authorized blocked work eligible for paced recovery. Delivery and useful progress remain separate evidence. Implementation and verification have not run; TestDesign is the next stage.

Basis: the full CARD-0412 read on 2026-09-07, [landed investigation](../../investigations/2026-09-07-card-0412-usage-hold-auto-resume.md) at `2917944d`, and source inspected at that same checkout. The investigation's production observations are historical evidence, not a fresh claim about deployment or current provider capacity. This Plan performed no recovery or live configuration changes.

## Ground truth

| Card premise / required behavior | What the inspected code does | Design consequence |
|---|---|---|
| CARD-0401 should parse the provider reset | `ApiErrorStubText` correctly prefers the sibling AssistantText. `UsageLimitWallParser.ResetRegex` requires `:mm`; `9am` and `2pm` fail. The investigation found four post-commit AutoDetected six-hour fallbacks and 18 hour-only Wall recoveries, none scheduled. | Preserve sibling lookup; add exact production strings and a stable source-time anchor. |
| Empty-text repair enriches one old detection | `TryRepairEmptyWallAsync` reapplies even if its SourceSessionId lookup finds no active hold. `ApplyWallAsync` uses repair time and rewrites ResolvedAt. The investigation correlates a renewed Opus hold with an older recovery after a clear and later UserPrompts. | Repair must require the exact active hold/evidence revision. Absence is a no-op, never permission to upsert. |
| A hold-clear event can wake consumers | Sweep, `FindActiveAsync`, snapshot/list and explicit `ClearAsync` can all stamp ClearedAt. No durable clear cause or consumed transition exists. Manual PUT edits an active row in place. | Store the transition with the clear, including lazy expiry. A sweep count or transient event is insufficient. |
| Nobody resumes today | Queued held tasks are polled. `ResumeRoutingBlockedAsync` re-walks declared candidates. Parsed SessionLimit schedules reset + 2 minutes. AlwaysOn supervision retries dead agents. | Adapt these paths; do not add a second dispatcher, timer job or process launcher. |
| Every wall-parked task is retryable | ModelCap and WallParked are terminal recovery reasons. `HandleApiErrorTurnAsync` can fail and release an ordinary delegate. Routing blocks include intentional cascade exhaustion. | Future recoverable capacity waits need typed metadata before settlement. Never revive arbitrary Failed tasks or reset cascade guards. |
| Fleet availability clearing unblocks channel input | `HasTerminalCapacityHoldAsync` blocks Channel/Scheduled behind WallModelPaused, WallParked, NeedsHuman, UnknownExhausted or WallUnparsed newer than the last UserPrompt, without checking current model availability. | Release only a capacity barrier for an admitted action; retain authentication, unknown-error and unparsed barriers. |
| AlwaysOn recovery preserves the conversation | Running sessions return without a continuation. A `ModelDisabledException` increments generic failure/backoff; enough failures choose Fresh. `StartAsync` also clears user/liveness latches for all callers. | Capacity refusal is a wait, not a crash. Add a guarded internal automatic start path that cannot acknowledge a human barrier. |
| Restart needs a new capacity prompt | `LaunchInteractiveProcessAsync` already sends launch notes, an interrupted-turn continue on real resume, and sometimes a boot probe. | One recovery action must own/coalesce the first useful launch input; no second capacity prompt beside an existing continuation. |
| One active hold identifies all blocked sessions | `SourceSessionId`/`SourceTaskId` are overwritten on a shared alias row; a standing caller can get a create-time 409 without an API-error stub or child task. | Persist actual consumers and refusal evidence independently of the latest hold source. |
| Current wall count bounds consecutive failures | `ApplyWallAsync` counts nearly all historical Wall rows in a session. Even successful intervening work need not remove them from the count. | Count attempts in a capacity episode, retaining the episode through expiry/re-hit until useful progress. |
| Global dispatch concurrency prevents a release stampede | MaxConcurrentTasks defaults to six; no per-kind pacing covers dispatch, live prompts and supervised starts together. | Share a durable admission clock across those recovery paths. Unaffected normal work keeps its existing policy. |
| A queued prompt means recovery happened | Queue insertion can precede recovery save and can immediately flush. Some delivery verdicts have degraded screen fallbacks. | Persist an action identity before delivery; report queued, transcript-confirmed and successful-turn outcomes distinctly. |

## Decisions

All D-n choices are settled implementation defaults. No operator answer blocks TestDesign.

| Decision | Choice and reason | Rejected alternative |
|---|---|---|
| D-1: delivery order | Land parser/time anchoring and exact repair lineage before activating new wake behavior. | Regex-only fix: leaves the confirmed resurrection bug and parked consumers. |
| D-2: reset grammar/time | Accept hour-only forms with AM/PM and existing minute-bearing 12/24-hour forms. Anchor to the persisted stub timestamp, with deterministic stored fallbacks. Keep reset + 2 minutes and the configurable six-hour no-reset fallback. | Reparse against current sweep time, or infer a provider billing period from free text. |
| D-3: clear semantics | Expired and OperatorCleared both release previously authorized work with identical pacing. Source=Manual still preserves its deadline/open-ended hold. Store the clear cause separately from Source. | Treat every Manual source as an explicit-clear event; require a new approval knob for ordinary recovery. |
| D-4: durable transition | Use the immutable hold Id as its lifecycle generation: a cleared row is never reopened. Add revision, clear cause and pending-consumption fields on that row. | A new general event bus/outbox table or wake-on-GET callback; existing hold history can carry this transition. |
| D-5: blocked consumers | Add a durable capacity-wait episode per actual consumer, links to observed hold generations, and a small per-kind admission state. These are coordination records consumed by existing services. | Enumerate every agent sharing an alias, or use mutable SourceSessionId as the subscription list. |
| D-6: pacing | One recovery admission per AgentKind per 60 seconds; stable 0-30-second consumer jitter; oldest ready wait first. Apply at the action's execution boundary, not merely enqueue time. | Six-at-once global concurrency, per-alias buckets for a provider-wide session limit, or speculative credential/profile grouping. |
| D-7: episode budget | At most three admitted recovery attempts per consumer episode after its initial capacity block. A re-hit or restart preserves the budget; a completed successful work turn ends the episode. | Lifetime wall count, a reset on every clear/UserPrompt, or perpetual fallback-expiry/re-hit loops. |
| D-8: task ownership | Retain a future interrupted live task as Working with typed CapacityWait metadata, on the same attempt/session. Existing queued/routing-blocked tasks remain owned by the dispatcher. | Replaying an interrupted brief cold or moving all capacity cases to generic human-question Blocked. |
| D-9: dead sessions | Only an authorized standing AlwaysOn owner is automatically started here. Resume when supported and still valid; never choose Fresh because of capacity-only refusals. | Restart dead unowned/ephemeral sessions or override a provider's unknown resume contract. |
| D-10: legacy compatibility | Reconcile only current live-owned/open-work candidates with fresh transcript checks; repair old nonempty hour-only evidence only with unique lineage. Historical task outcomes remain immutable. | A SQL migration that reopens all WallParked/Failed history or silently changes every old hold. |
| D-11: visibility | Structured lifecycle logs plus durable timeline/task events and durable Attention for exhausted waits. Confirmation cites the matching prompt sequence. | Silent recovery, one alert per polling tick, or calling enqueue/Running proof of resumed work. |
| D-12: scope | Backend coordination and narrow status projection; use current runner transport and existing clocks. Code validation uses database fixtures and fake runners, no paid-provider canary required for this change. | New broker/jobs, provider account discovery, transport redesign, or a production hold-clear experiment as a prerequisite to implementation. |

## Reset parsing and repair contract

### Source-time parsing

`UsageLimitWallParser` remains pure. Rename/document its time argument as evidence time rather than wall-clock now. Parse `You've hit your session limit · resets 9am (Europe/London)` and the `2pm` variant, `reset at 9 AM`, and existing `6:10pm`, `09:00` formats. Hour-only without AM/PM is deliberately outside the new grammar; preserve the documented minute-bearing 24-hour format. Omitted minutes mean zero only in the valid hour-only branch.

Use named regex groups and token boundaries so `9:7am`, `9:000am`, `9amjunk`, `25:00`, `0am`, `13pm` and a malformed supplied zone cannot partially match a shorter valid prefix. Unknown zones and invalid times degrade to ModelCap with an explicit parse-failure diagnostic; distinguish supplied-but-invalid reset from no reset stated in the reason. Preserve alias normalization, named-model extraction, structural Wall classification and Grok credit vocabulary.

Persist `EvidenceAt` and its source on the recovery at first adoption: TurnEnd.Timestamp, else its matched AssistantText.Timestamp, else TurnEnd.CreatedAt, else initial DetectedAt. Null/unusable timestamps fall through this order; enrichment can improve a fallback to a real provider timestamp once, recording the change, but later sweeps never substitute their current time. Pass these facts through `ApiErrorStubText`, adoption and the task reply arm; there must not be two different reset calculations for one stub.

Compute the next occurrence at-or-after that evidence instant in the named zone, preserving current calendar semantics. Thus a source timestamp already past the stated local clock can legitimately mean the next day; this change does not invent a missing provider date. London DST conversion is retained; reject nonexistent local times, choose the later UTC occurrence for an ambiguous local time and pin that conservative policy in tests. No zone means UTC. A delayed repair whose computed deadline is already past clears through the normal expiry transition immediately; it does not roll forward from repair time. The no-reset fallback is `EvidenceAt + EffectiveModelCapFallbackHoldHours`, not each re-adoption's now. HitAt remains write/observation history; it is not the parser clock.

### Exact, idempotent enrichment

Persist on `ApiErrorRecovery` the hold Id/revision it applied, evidence timestamp/source, parse version and the digest/status of the evidence applied. Persist on the active hold its latest evidence recovery Id. Evidence enrichment is an update of that relationship, not a call to the general hold upsert.

An enrichment transaction must prove all of the following:

1. The exact `(sessionId, stubSequence)` recovery exists and is structurally Wall. New text improves empty evidence or is a one-time parser-version correction of the known hour-only failure.
2. Its linked hold is still uncleared and matches the expected revision and latest evidence recovery. Both alias and kind match. A different session overwriting the hold evidence invalidates the old writer.
3. After transcript catch-up, this remains the relevant latest wall and no later real/queued accepted prompt or completion has taken over. Suppression must use the established transcript-kind helpers, excluding local commands, synthetic compaction continuation and housekeeping.
4. No terminal task/cancellation/reroute, manual takeover or completed recovery has superseded it. Legacy WallModelPaused/WallParked may permit evidence-only correction under the compatibility rules; they do not gain a task retry merely by changing text.

Missing hold, cleared hold, revision mismatch, changed source, no improvement or ambiguous legacy identity means no mutation. A cleared row's ClearedAt, clear cause and original resolution history never change. Do not replace `ResolvedAt` on every sweep. A legitimate new structural wall after progress may create a new hold Id; the old stub cannot.

Refactor new adoption so the recovery, hold association and capacity-wait registration commit atomically. `BuildNewRowAsync` currently causes hold saves before the recovery insert; that ordering cannot be the new lineage contract. Allocate the recovery Id first, insert/link in one transaction, and distinguish unique-key adoption races from unrelated DbUpdateExceptions. The auto writer must enlist in the caller's transaction. Explicit model APIs still own their transaction. Manual precedence applies to enrichment as well: no deadline shortening or demotion; a later Manual PUT invalidates earlier evidence revisions.

The relevance check also applies to a previously **unadopted** historical stub: if catch-up already proves a later accepted prompt, successful completion or newer wall, persist only its Superseded/Replaced audit recovery and do not write a hold. Otherwise fixing the existing-row repair branch would leave an equivalent resurrection through first adoption after delayed ingestion.

## Durable state and transaction boundaries

Names below are proposed new code names, not existing APIs. Use concrete application services, existing AppDbContext patterns, TimeProvider and typed IOptions settings. Domain additions remain plain C#; generate migrations with the EF CLI.

| Durable owner | Required additions / keys | Invariant |
|---|---|---|
| `ModelAvailabilityHold` | `Revision`, `EvidenceRecoveryId`, `ClearCause` (Expired/OperatorCleared), `ReleasePendingAt`, `ReleaseConsumedAt`; retain Id/ClearedAt/history | One clear per Id. Clear + cause + pending marker are one transaction. Active updates increment Revision; no mutation reopens a cleared Id. |
| `ApiErrorRecovery` | `EvidenceAt`, timestamp source, parse version/evidence status, `AppliedHoldId`/revision, `CapacityWaitId` | `(AgentSessionId, StubSequence)` stays unique. Non-wall retry ladders retain their behavior. |
| New `CapacityRecoveryWait` | Id is episode Id; consumer kind/key, blocked time, agent/session incarnation, task/attempt/card and authorization snapshot where applicable; requested kind/alias; current action ordinal/key, due time/state, admission count, message/launch/dispatch receipt, confirmed prompt sequence, outcome/reason/version | One active wait per logical consumer. A new wall in an unfinished episode updates the blocker/action, not the budget identity. Coalesce a session wall, its retained task and its pending queue rows into one consumer. |
| New `CapacityRecoveryWaitHold` | `(WaitId, HoldId)` unique; observed revision and release-consumption acknowledgement | Several consumers can depend on one generation; one consumer can observe alias plus `*`, successive holds, or several declared routing candidates. These links are evidence, not a second availability policy. |
| New `CapacityRecoveryProviderState` | AgentKind primary key; NextAdmissionAt, last action/key/time, wave revision, admission claim | One durable clock shared by the three execution paths. Restart cannot reset the pacing clock. No credential material or guessed account keys. |
| `SessionQueuedMessage` | nullable `CapacityRecoveryActionKey`, unique when non-null; wait/expected-generation reference | One selected body per action. Repeating an enqueue returns the existing row, including Sent/Canceled/parked rows. Existing SourceTaskId/ContentDigest completion-note dedup is unchanged. |
| Existing task/supervision/session records | Nullable capacity-wait/action references and typed wait reason; durable launch receipt on the actual session incarnation | Capacity scheduling is distinguishable from crash scheduling and human-question blocking; automatic start recovery can find its prior accepted launch after a crash. |

Use indexes for active consumer uniqueness, pending release rows, ready waits by due/blocked time and kind, and the hold-link reverse lookup. Do not store raw credentials, complete refused briefs or channel bodies in coordination rows. Keep target references nullable where deletion requires retained audit; a missing owner cancels a wait rather than causing a repeated sweep exception.

### Clear and consume

All clear paths call one transactional primitive: explicit DELETE, timed sweep, lazy lookup/list/snapshot, legacy AutoDetected-null normalization and a correction whose deadline has passed. It stamps ClearedAt, ClearCause and ReleasePendingAt exactly once. Repeated clears are no-ops. Concurrent clear/upsert/repair uses the same kind/hold serialization and revision checks; a committed clear cannot be lost by a stale tracked entity.

The existing supervisor hosted loop consumes pending transitions at startup/first tick and periodically. It durably marks linked waits for reevaluation and acknowledges the release in the same database transaction; it performs no input or launch in that transaction. Mark ReleaseConsumedAt only after all relevant links have been processed, in bounded batches with a durable cursor if needed. Registering a wait after its hold cleared must immediately observe the clear; it cannot depend on an event that has already been consumed. Pending waits also reevaluate effective availability periodically, so no transient publish failure can strand them.

Eligibility always calls the current alias + kind-wide availability policy. A cleared specific hold with an active `*`, or a replacement hold after a clear, does not authorize a launch. Register the newly observed blocking generation and defer. Routing waits rerun the existing candidate walk; a stale chosen alias is not authority. Distinct stored clear causes describe the holds that released this work, including both causes when overlapping holds clear differently.

No IEventBus publish, queue flush, transcript RPC or Start runs under these database transactions. Publish only after commit; failure leaves durable state for the next tick. Use a consistent database lock order across the new paths: provider state, affected consumer owner (agent/task/session), wait, then hold rows in stable key order. Refactor existing supervisor/adoption transactions where necessary rather than acquiring the provider lock inside an already inverted order. Existing per-session queue locks continue to serialize composer access; no code acquires that semaphore while holding the database locks.

### Episode/action state

`WaitingForHold -> Ready -> ActionPending -> Admitted -> PromptConfirmed -> Progressed` is the normal live-session path. A launch can record `StartAccepted` and then `Running` before its useful prompt is confirmed. A task dispatch records its accepted attempt and then boot-prompt confirmation. These intermediate outcomes do not claim useful work completed.

Side outcomes are `Deferred` (busy, active hold, unavailable backend or temporary prerequisite), `Superseded`/`Canceled`, `Reheld`, and `Exhausted`. Admission and action transitions compare the wait version; a late callback for an earlier ordinal can enrich that action's evidence but cannot release the current barrier. No unresolved recovery callback can overwrite a newer task attempt.

Every action key derives from `(waitId, actionOrdinal)`. A new ordinal is issued only after a recorded outcome requires another capacity attempt, never on a polling tick or merely because a lease expired. Once side effects may have occurred, recovery inspects the existing queue/launch/dispatch receipt first. An unknown result does not mean create another action. It becomes a visible unresolved outcome handled by existing delivery/launch reconciliation.

## Selecting and executing consumers

| Consumer | Registration and relevance | Existing action path |
|---|---|---|
| Live wall-interrupted session, including standing agents | Adopt a relevant Wall with a canonical alias. Capture session Id **and StartedAt**, latest stub sequence, owner and open task/attempt if present. Later user progress, owner/session replacement or finished work suppresses continuation. | `ApiErrorRecoveryService` prepares a keyed action; `SessionMessageQueueService` selects/delivers WhenIdle. A Running session is never restarted merely to resume. |
| Pending Channel/Scheduled work behind terminal WallModelPaused/WallParked | At the capacity gate register/link the actual pending rows to the session wait. Recheck status, schedule supersession and current channel binding before selection. | Admit one existing eligible queue delivery as the recovery turn, preserving body/origin/correlation. Do not enqueue an extra WallPrompt beside it. |
| Queued task skipped for a hold / queued with IgnoreModelDisabled | Persist the observed blocker and task attempt when the dispatcher skips, including the create-time ignored hold so a clear before the next tick is not missed. | Existing dispatcher checks routing, budgets, scope and concurrency, then asks for capacity admission immediately before its dispatch claim. |
| Routing-exhausted task | Persist typed capacity eligibility only when exhaustion is due to availability and the existing cascade/pin tests allow eventual return. Human-policy, forbid, quota-only or cascade-exhausted blocks do not gain eligibility. | Existing `ResumeRoutingBlockedAsync` can requeue once; actual launch remains dispatcher-owned and paced. A queue transition itself spends no recovery admission. |
| Dead standing AlwaysOn owner | Supervisor records a model refusal as a typed wait with current owner/session generation and prior non-capacity backoff. It also detects an existing eligible wall wait when the session dies. | Existing supervisor -> guarded `AgentControlService` -> launch queue. No independent restart service. |
| Standing orchestration caller refused before a child exists | At the authenticated CreateAsync model-disabled catches, store caller session/owner, transcript boundary, requested kind/alias, card/role/routing identity and canonical request digest. Coalesce repeated equivalent refusals. If an ordinary child row is created instead, track that row only. | One WhenIdle notice asks the caller to re-evaluate the previously blocked dispatch through the normal API. Never persist/replay the refused request as a child launch and never choose a new provider. |

For a create refusal, persist the wait before rethrowing the existing 409 and preserve its code/coda. Capture only a standing caller with a positively resolved session and authority; external capability callers with no session receive the normal refusal, not an invented recipient. Record all effective capacity blockers of an exhausted declared list when applicable. A successful equivalent create, changed card/pin, later user instruction, caller termination or task cancellation obsoletes the recorded intent. The capacity notice contains identity/context and says capacity may now be available; it does not treat clearance as authorization to bypass current guards. A normal assistant turn explaining the refusal is not itself proof that the intended dispatch succeeded.

Keep **blocking capacity** and **execution capacity** distinct on the wait/action. For example, a Codex caller refused a Claude delegate waits for the declared Claude candidate to become eligible, but its wake prompt consumes the Codex admission bucket and must pass the caller's own current model hold. A later child launch consumes Claude admission through the normal dispatcher if it is the resumed blocked intent. Carry the refusal digest into successful equivalent create/wait transfer so the same intent cannot escape pacing by changing from a caller notice into a new queued task. Do not send input to the caller using the blocked child's alias as its effective model.

### Queue and terminal barrier

Keep NeedsHuman, AuthenticationRequired/provider sign-in, UnknownExhausted and WallUnparsed outside capacity auto-resume. Herdr blocked/unreachable, rules initialization/refresh failures, delivery-attempt caps and forbidden/truncated bodies remain authoritative queue barriers.

Do not change `HasTerminalCapacityHoldAsync` to simply return false whenever the model is available. Select exactly one recovery action under the queue's per-session lock after catch-up and stale-message checks. Prefer the oldest still-eligible Channel/Scheduled row; preserve existing contiguous same-conversation batching as one submission. A newer schedule copy cancels the old selection and causes reselection before input. If no queued work exists and the interrupted turn/refused dispatch is still relevant, enqueue the appropriate short Supervision continuation with the action key and `deliverIfIdle: false`.

The ordinary flush is the final authority: verify owner/version and current availability, then acquire admission immediately before durable delivery-start intent. Only the selected message/batch may pass its linked capacity barrier. Other capacity-blocked messages remain pending until that action is confirmed and a normal turn boundary allows the next delivery. Fresh holds and new terminal walls close it again; non-capacity barriers are never cleared by this acknowledgement. UI/manual input retains its existing semantics and can supersede an untyped automatic action.

Persist queue row creation/selection with the wait state, using an enlistable queue operation or keyed enqueue that can safely be rediscovered after save failure. Keep the queue's baseline, complete prompt matcher, late confirmation and LF/bracketed-paste/separate-Enter transport unchanged. A new action cannot reset DeliveryAttempts or retype a parked/truncated message. Crash after input but before acknowledgement reconciles the same message against the transcript. Enqueued or screen-only Delivered is logged as unconfirmed, never `Auto-resumed`.

For later UserPrompt/QueuedUserPrompt evidence, first determine whether it is the chosen action's accepted body. That body confirms the action; unrelated genuine input suppresses the stale continuation. Ignore local commands and compaction housekeeping for useful-progress decisions. A failed catch-up is Deferred, never evidence of absence. Recheck at final flush, because a human can continue after scheduling and before delivery.

### Supervision and launch

Handle ModelDisabledException before the generic failure arm. Neither first refusal nor repeated hold observations increase ConsecutiveFailures, escalation tiers or capacity attempt count. Store capacity timing separately from pre-existing crash NextRestartAt; on release pull forward only delay attributable to capacity. Preserve real crash backoff and Herdr streaks, including typed asynchronous launch failures.

Add an internal automatic-start context carrying the wait/action and expected owner/session generation. In `AgentControlService`, recheck Suspended, LivenessLatchedAt, HerdrFailureHeldAt, AlwaysOn, current ownership and terminal/canceled card intent under the agent transaction before committing a start. This path must not call the human latch-clear behavior. Keep public manual Start/explicit Herdr retry behavior intact. A Stop racing admission wins if committed before start intent; a Stop after an accepted start follows the existing stop/launch-cancellation path. Capacity actions never set ResetHerdrFailureHold or AllowUnauthenticatedProvider.

The standard AlwaysOn `IgnoreSubscriptionQuota: true` precedent is retained for this same caller only; create/dispatch quota policy and explicit routing overrides are not widened. Authentication, rules, placement and model Require guards still run. A 409 for another barrier records/delegates to that barrier's existing handling; it is not automatically retried by spending the capacity budget every minute.

Use normal native resume when ProviderContractCatalog and the existing session identity allow it. Capacity-only failures cannot cross FreshAfterResumeFailures. Existing rules migration/resume-not-found refusals and unsupported/unknown native resume contracts remain visible decisions; never silently replay an unfinished task into a fresh context. An AlwaysOn owner with no interrupted conversation can still perform its ordinary authorized initial start. An actual independent crash recovery may keep its existing Fresh policy, with its cause recorded separately.

Persist the action reference on the accepted launch/session incarnation before async launch, so a restart finds the prior request and never launches twice because NextRestartAt survived. Tie the launch's first useful input to this action. Coordinate `DeliverLaunchNoteAsync`, `EnqueueResumeContinueAsync`, pending channel work and the boot-probe branch: mandatory rules initialization retains priority, but only one selected work continuation is emitted for the capacity action. A queued real message or existing interrupted-turn continue covers it. Do not add a WallPrompt after the launch path already resumed the work. Running/ready alone can complete a pure process-availability wait, but must not be described as a confirmed resumed work turn.

### Tasks and existing dispatch protections

Run current declared routing first. An allowed alternate remains allowed; Required pins, forbidden aliases, cost ceilings, workspace scope, specialist limits, manual card moves and cascade exhaustion retain their precedence. A task already requeued by routing has one dispatcher wait, not a second same-session recovery. Preserve the existing Rerouted event/cascade accounting; do not zero its count to obtain a capacity retry.

For future canonical Wall cases whose current live task remains resumable on its existing session, leave Status=Working with `CapacityWaitId` and a typed wait reason; do not emit Failed or release its delegate at ModelCap merely because the old recovery reason was terminal. Recovery resolution and capacity waiting are separate concepts. Until the episode is exhausted, show that it is waiting for capacity and the next due time. Non-capacity failures keep their current settlement behavior.

Retained tasks keep their workspace lease and count toward existing occupied-task limits; resuming the same task does not consume a second slot or conflict with its own lease. Pausing six dirty Shared tasks may constrain throughput: releasing ownership to improve that is intentionally not part of this change. Update overdue/check/settlement paths to defer only the typed eligible wait; existing specialist execution deadlines and cancellation still win. On exhaustion, use existing visible fail/release behavior and dirty-work diagnostics; leave the process killed, pooled warm or standing-owned. If an ephemeral session dies, do not create a fresh retry from its old work merely because a hold cleared.

## Pacing and retry policy

Add `Supervision:CapacityRecovery` typed settings with enabled default true after all slices are integrated, `AdmissionIntervalSeconds=60`, `JitterSeconds=30`, `MaxEpisodeAttempts=3`, and a bounded reconciliation batch size of 100. Validate ranges (interval 1-3600, jitter 0-300, attempts 1-10, batch 1-1000) and never permit a zero/unbounded loop. Keep existing Supervision/ApiErrorRecovery disable semantics; capacity actions honor the relevant parent enable flags. Parser correctness and durable clears do not depend on the new enable flag.

Stop using `ApiErrorRecovery:WallDeathCap` as a lifetime row-count gate. When MaxEpisodeAttempts is unspecified, use the existing configured WallDeathCap as its validated compatibility default (normally three); an explicit new value wins. Document the old key as deprecated rather than silently discarding a deployment's chosen bound. Existing transient/UnknownAttemptCap schedules are unchanged.

The existing hosted loop drives reevaluation every supervisor tick (10 seconds by default); expiry/adoption can retain their current minute cadence. Inject TimeProvider into the touched clock decisions/timer so tests do not depend on DateTime.UtcNow. Run expiry/transition reconciliation before capacity candidates and supervisor starts on a tick. Dispatcher and queue checks remain independently correct if they run first or cause lazy expiry.

`EligibleAt = latest required clear/availability-observation time + stableHash(waitId, actionOrdinal) modulo [0, jitterSeconds]`. Apply any later existing crash deadline or execution prerequisite. Among ready consumers choose oldest BlockedAt, then Id. Busy/guarded/ineligible consumers do not reserve future slots or block unrelated ready work. The provider clock is a minimum spacing, not a promise to execute exactly at a scheduled time.

Admission is the durable handoff immediately before the first execution step: queue delivery-start intent, guarded supervised start acceptance, or transactional task dispatch claim. Persist the action/key and `NextAdmissionAt = actualAdmissionTime + interval` together. An enqueue, requeue, skipped gate or prospective due reservation does not spend an attempt. A granted action that never executes may conservatively consume a slot/attempt until its existing receipt is reconciled; it never refunds the clock and releases a burst. Do not pregrant many future slots and then execute all overdue slots after restart.

The launch and its first selected work input share one admission; they do not pay the bucket twice. Long-delayed starts must revalidate an unexecuted admission before beginning, using the same action identity and current provider clock, so boot backlog cannot become a late burst. Transport confirmation/re-Enter for the same queued attempt is not a new capacity retry; any later new-body attempt goes through admission again. The policy bounds Antiphon recovery admissions, not the number of model API calls inside one admitted turn or ordinary new work.

Revalidation of the same accepted action may move its provider clock forward but does not increment its episode attempt count again. A provider-wave revision or newly committed hold invalidates a grant before the first process/input side effect; already-started work is not killed. Recheck and commit intent immediately before the external operation. A hold committed after that linearization point can stop the next action, but cannot retract bytes/RPCs already sent; record that race and let the existing outcome path reconcile it.

On a **new structural wall**, persist the hold before admitting further affected work, increment the provider wave revision and invalidate not-yet-executed reservations. Set the recovery bucket's next possible admission no earlier than wall observation + interval. Reevaluate all pending recovery actions of that kind. Only the affected alias/`*` holds defer until clear; an alias-specific wall must not become an indefinite kind-wide availability hold for other aliases. Unaffected kinds retain independent clocks. Expiry is permission to probe capacity, not proof of capacity.

An episode begins at the first actual capacity block of a logical consumer. It spans successive wall stubs, fallback deadlines, operator clears and supervised incarnations for that same unfinished work. Initial block is attempt zero. Admit up to three attempts; a fourth is Exhausted. A queue acceptance or another wall cannot reset it. A transcript-confirmed successful non-API-error work TurnEnd after the selected prompt closes the session/task episode; mere UserPrompt, local command, unrelated housekeeping, restart uptime or the caller explaining a refused dispatch does not. For a refused-dispatch wait, equivalent successful task creation resolves the intent; for a pure standing process wait, confirmed Running with no outstanding interrupted work resolves it without claiming model success.

After exhaustion no timer or further clear resets that consumer's budget. Existing explicit human continuation/start/task follow-up can recover it; subsequent confirmed useful progress begins a new episode for a later independent wall. No time-of-day or period guessing is needed. A new unrelated task has its own authorization and episode; cascade counters continue to apply separately.

## Compatibility, activation and visibility

Migrations add nullable links/state and constrained defaults only. Do not fabricate historical clear causes or episode successes; already-cleared rows have unknown legacy cause and no pending replay. Do not automatically modify old Failed/Succeeded/Canceled task outcomes. Preserve current active holds, including open-ended Manual ones.

At first activation, and via resumable bounded batches, reconcile only current live owned sessions with a latest eligible Wall or pending capacity-blocked queue, open queued/routing-capacity tasks, and current unsuspended AlwaysOn owners with recorded capacity evidence. This is a current-work scan, not the 180-minute transcript adoption window: an older but still genuinely stranded session must be considered. Persist a compatibility version/cursor and per-candidate result so restart does not repeat successful backfill.

Catch up each relevant transcript before deciding it lacks progress. For active legacy empty/nonempty hour-only holds, link only when the latest relevant recovery uniquely matches kind/alias, source session, evidence text and persisted timing; timestamps alone or a shared SourceSessionId are insufficient. Ambiguity leaves the hold untouched and logs a bounded compatibility skip. Unique evidence can enrich a Manual row's diagnostics but never move its deadline. A corrected AutoDetected deadline in the past clears through the normal primitive.

A currently eligible parked consumer may have no trustworthy historical hold link because its hold already cleared or changed source. Do not create another hold. Record a `LegacyAvailable` compatibility observation on its wait, distinct from Expired/OperatorCleared, after proving effective availability and unfinished authorized work. It then uses the same pacing and guards. Latest WallModelPaused/WallParked is usable evidence for current live work; historical lifetime wall counts are not copied into the new episode. Later prompts, terminal task history, stale session incarnations and human/routing barriers remain exclusions. The investigation's specific sessions are test-fixture evidence, never hard-coded recovery targets.

Emit structured logs for registered/scheduled, admitted, queued/start-accepted, prompt-confirmed, progressed, skipped/deferred, re-held and exhausted. Include clear cause(s), hold Id/revision, wait/action Id, consumer/attempt, actual kind/alias, next due and evidence sequence; omit secrets and message bodies. Coalesce repetitive deferred logs by reason/version and aggregate each release-wave count.

Add a timeline-only `CapacityRecovery` incident kind (allocate the next free enum value at implementation time) with one durable scheduled and one confirmed/final outcome per consumer episode, keyed rather than deduped by session lifetime. Use `raiseAlert: false`. Preserve existing task events, adding a typed capacity event if required instead of overloading Failed or successful Rerouted for a no-op. Persist lifecycle audit with the state change, publish AgentChanged/AgentTaskChanged after commit, and reconcile missing notifications from durable state. Operator text must distinguish, for example:

- `Auto-resume scheduled after hold expired; ClaudeCode/opus; next eligible 08:03Z.`
- `Auto-resume queued; awaiting transcript confirmation.`
- `Auto-resumed after operator cleared hold; session ...; prompt sequence 123 confirmed.`
- `Capacity recovery paused after 3 attempts; latest wall ...; manual continuation required.`

Project pending/exhausted wait state and due time through existing task/agent detail and Attention surfaces. Exhausted Attention is derived from the durable wait, so incident retention cannot hide a still-blocked episode. It is a decision signal, not a new card column or chat alert. Ordinary successful recovery is visible in the existing timeline/log; no new alert sink or channel message is needed. Generic incident rendering should suffice; adjust client labels/types only where the new typed reason requires them.

The new enabled setting is a reversible rollback control: turning it off stops new capacity admissions and leaves waits/history intact; it does not clear holds, erase barriers or cancel an already delivered turn. Completed verification precedes enabling this behavior in a deployed server. Deployment itself is a later authorized operational action under the canonical restart runbook, from the main checkout; confirm loaded code/schema, not just health. Never clear production holds just to test the feature. Observe the next natural authorized recovery and record its actual confirmation when available; lack of such a provider event is not a reason to invent one or claim live proof.

## Implementation slices and test owners

Implement in this order; keep activation last. Each Code slice reports the evidence required by the forthcoming Verification design rather than treating a build as acceptance.

| Slice | Named files / responsibility | Tests to extend or add |
|---|---|---|
| S1: exact reset parsing | `server/Application/Services/UsageLimitWallParser.cs`, `ApiErrorStubText.cs`, `ApiErrorRecoveryService.cs`, `AgentTaskReplyService.cs`; evidence-time facts and unambiguous grammar/reasons | `tests/Antiphon.Tests/Application/UsageLimitWallParserTests.cs`, `ApiErrorRecoveryServiceTests.cs`; production hour-only pair, minute regressions, bad prefixes, source timestamp/late repair, zone/DST/noon/midnight. |
| S2: repair lineage and clear transitions | `server/Domain/Entities/ModelAvailabilityHold.cs`, `ApiErrorRecovery.cs`; `server/Application/Services/ModelAvailability.cs`, `ApiErrorRecoveryService.cs`; `server/Infrastructure/Data/AppDbContext.cs`; CLI-generated `server/Migrations/*` | `ModelAvailabilityTests.cs`, `ModelAvailabilityManualTests.cs`, `ModelAvailabilityHttpTests.cs`, `ApiErrorRecoveryServiceTests.cs`; active exact repair positive, clear/re-adopt negative, source overwrite, concurrent new adoption, repeat/lazy clear and transactional rollback. |
| S3: durable wait/admission core | New `server/Domain/Entities/CapacityRecoveryWait.cs`, `CapacityRecoveryWaitHold.cs`, `CapacityRecoveryProviderState.cs`; typed states in `server/Domain/Enums/CapacityRecoveryEnums.cs`; new concrete `server/Application/Services/CapacityRecoveryService.cs` and pure `CapacityRecoveryPolicy.cs`; settings, DbContext, migrations, `server/Program.cs` registrations | New `CapacityRecoveryPolicyTests.cs`, `CapacityRecoveryPersistenceTests.cs`; simultaneous callers using separate DbContexts, wildcard overlap, clear-before-register, restart/cursor, budget and minimum actual admission spacing. |
| S4: live queue recovery | `ApiErrorRecoveryService.cs`, `SessionMessageQueueService.cs`, `AgentSessionRuntime.cs` at confirmation/turn evidence hook; `SessionQueuedMessage.cs`; message DTO additions only if needed | New `CapacityRecoveryQueueTests.cs`; existing `SessionMessageQueueServiceTests.cs`, `SessionMessageQueueSupervisionTests.cs`, `SessionMessageQueueDeliveryVerificationTests.cs`, `GrokRulesQueueBarrierTests.cs`; selected Channel/Scheduled body, no extra prompt, unconfirmed/late/truncated delivery, manual takeover and latest-stub races. |
| S5: task/refusal integration | `AgentTaskService.cs`, `AgentTaskReplyService.cs`, `AgentTaskDispatcher.cs`, `server/Domain/Entities/AgentTask.cs`, `server/Domain/Enums/AgentTaskEnums.cs`; deadline/check projections that currently use generic Working/Blocked | `ModelAvailabilityCreateTests.cs`, `ModelAvailabilityDispatcherTests.cs`, `ComplexityWallRerouteTests.cs`, `RoutingPinDispatcherTests.cs`, `RoutingPinCandidateDispatchTests.cs`, `AgentTaskReplyIntegrationTests.cs`, `AgentTaskOverdueDeadlineTests.cs`; new `CapacityRecoveryTaskTests.cs` and `CapacityRecoveryRefusalTests.cs`. |
| S6: guarded standing recovery | `AgentSupervisorService.cs`, `AgentControlService.cs`, `AgentSessionService.cs`, `AgentSupervisionState.cs`, `AgentSession.cs`; `server/Infrastructure/Supervision/AgentSupervisorHostedService.cs` integrates durable reconciliation into existing tick | `AgentSupervisionTests.cs`, `HerdrSupervisionBackoffTests.cs`, `HerdrAlwaysOnChannelParityTests` hold cases, `AgentSystemPromptLaunchTests.cs`, `GrokRulesResumeMigrationTests.cs`; new `CapacityRecoverySupervisionTests.cs` covers capacity/crash counter separation, Stop race, unsupported resume, launch-note/probe coalescing and restart after launch acceptance. |
| S7: compatibility, visible outcomes, activation | `CapacityRecoveryService.cs`, `AttentionService.cs`, relevant agent/task DTO mappers, incident enum; existing client types/attention labels only as needed; `server/appsettings.json` typed defaults; update `docs/session-runtime-invariants.md`, `docs/agent-kinds.md`, `docs/ops-http.md`, `docs/agent-card-lifecycle.md` where this changes the documented wait contract | New `CapacityRecoveryCompatibilityTests.cs`, `CapacityRecoveryAttentionTests.cs`, `CapacityRecoveryAcceptanceTests.cs`; existing `AttentionServiceTests.cs`, client attention tests if touched. Cross-path expiry and explicit-clear acceptance with visible confirmation, mixed kinds, many aliases/consumers, service restart and re-hit. |

Audit other ordinary machine producers/automatic starts encountered in S4-S6: a capacity-blocked consumer cannot bypass its keyed action merely because its queue origin is Supervision/System or it is started by a channel/provisioner. Route that **recorded consumer's** automatic wake through the same gate; retain existing special rules-initialization and human input permissions. This is a bounded call-site audit, not a request to redesign all startup paths.

## Required TestDesign handoff

Append `## Verification design` to this artifact with V-n/R-n/PC-n, costs and scoped runnable commands. Verification is a separate stage; this section is acceptance input, not a claim that tests have been designed or executed.

1. Prove both production regressions with fixtures: exact null TurnEnd/sibling hour-only text; clear -> later prompt -> old adoption without hold recreation or rewritten resolution. Include the corresponding valid active repair and minute-format positive controls.
2. Prove transaction/restart boundaries with separate service instances/DbContexts: clear-save, link consumption, admission, queue creation, input-before-save, launch acceptance, dispatcher claim and late callbacks. Assert durable identities and side-effect counts scoped to fixture consumers.
3. Prove simultaneous mixed queue/supervisor/dispatcher release obeys a shared per-kind clock at actual admission, including overdue work after restart, slow launch, re-hold between grant and execution, wildcard overlap, cross-kind caller/child refusal transfer and unrelated-kind progress. A scheduled timestamp alone is not this assertion.
4. Prove one selected work turn, including pending channel/schedule correlation, launch notes, interrupted-turn continuation, synthetic boot probes, later real input, local commands/compaction and truncated/late-confirmed delivery. Never accept a new queue row as transcript evidence.
5. Pair every exclusion with an enabled positive control: suspension and Stop race, liveness latch, Herdr acknowledgement, authentication and rules barriers, delivery cap, terminal/card cancellation, changed owner/incarnation/attempt, Required pin/forbid/cascade, budget/scope/global cap, unknown alias and unsupported native resume.
6. Prove three-attempt exhaustion survives clear/restart/re-hit; successful work starts a later episode, but a prompt followed by another wall does not. Prove future live tasks remain owned while waiting, deadline safeguards are specific, and old failed tasks remain unchanged.
7. Prove bounded compatibility includes a genuinely stranded current session older than 180 minutes while excluding historical completed work and ambiguous holds. Verify LegacyAvailable is visibly different from a known expiry/operator clear.
8. End-to-end application acceptance: hold a fake-backed standing session and queued work, expire or deliberately clear it through the real service/API, drive existing loops, observe the actual resumed prompt/dispatch/start and its operator-visible event. Exhausted Attention must survive incident pruning. Repeat for both causes without real provider traffic or production runner access.

Use the [testing/build owner](../../testing-and-build.md): TUnit via `dotnet run`, alternate OutputPath ending in a forward slash, named-class filters and nonzero fresh TRX counts; TestDbFixture row-scoped assertions and global `[NotInParallel]` for global sweeps. Queue clocks must advance timers (offset clock or auto-advancing FakeTimeProvider), never a frozen clock with real delays. Reuse `BridgeQueueHarness`, `DelegationTestServices` and existing launch fakes; a real Program host must retain ProductionRunnerGuard/refusing client or an isolated random runner. No process tests are necessary for unchanged PTY bytes; if one is justified, use the assembly-local ProcessSpawnLimit and run test assemblies sequentially.

Code verification should start with the named parser/recovery/availability classes, then each new integration class and the impacted regressions. An example single-class command is:

```powershell
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c412/ -- --treenode-filter "/*/*/UsageLimitWallParserTests/*" --report-trx --report-trx-filename card0412-parser.trx
```

TestDesign must give exact filters for its final cases and destructive guard-removal positive controls; do not substitute a namespace-wide run, a build failure or zero-test discovery for red/green evidence. If client code changes, use `pwsh -File scripts/test-client.ps1 <named-test-file>`; otherwise no client/E2E rebuild is needed for this backend plan.

## Completion boundary

This artifact settles the design and defaults. Required implementation remains S1-S7 plus the separate verification design and its execution. Plan validation is document/source consistency and `git diff --check` only; no builds/tests, provider calls, message sends, hold changes or stack operations were performed. No operator decision is pending. Next stage: TestDesign.

## Verification design

TestDesign complete, 2026-09-07. This appended section is the executable acceptance contract for S1-S7; the preceding design and defaults are unchanged. The plan at `4fd0d80d` and the existing parser, recovery, availability, queue, supervision and database harnesses were checked. The named `Card0412_*` tests below are **to be implemented by Code**, not existing tests or claimed passes. No runtime tests or mutations were executed during TestDesign. Next stage: Code.

### Proves it works now

**Test layout and harness.** All new cases live under `tests/Antiphon.Tests/Application/`, namespace `Antiphon.Tests.Application`, in the classes named below. Method names begin with the specified `Card0412_Vnn_` prefix; use descriptive suffixes and explicit TUnit data rows for the listed matrices. Every V-n needs executed evidence for every listed arm, not just one passing method with that prefix. Parser and pure policy classes are `[Category("Unit")]`; other new classes are Integration. Use the existing `BridgeQueueHarness`/`FakeAgentProtocolAdapter`, its `ConfigureDbContext` interceptor hook, existing fake launch clients, and `DelegationTestServices` for dispatch/reply graphs. Extend these test helpers narrowly instead of copying their service registrations.

Use a per-test cloned database from `TestDbFixture.CreateIsolatedSchemaAsync()` for cross-path/provider-clock and migration cases: kind-wide rows cannot be isolated by inventing a model alias. Wire **every** DbContext/service instance in that test to the clone's connection string. Cases using shared `TestDbFixture` must scope queries to seeded IDs and restore only their own rows. Every class driving a global sweep takes ungrouped `[NotInParallel]`, even with scoped assertions. Concurrency *within* a test uses independent connections/service scopes, not two tasks sharing one DbContext or an ambient test rollback transaction.

For pure policy tests an explicitly advanced clock is enough. A clock passed to the real queue must be an offset over the real clock or a timer-capable `FakeTimeProvider` with `AutoAdvanceAmount`; never combine frozen `GetUtcNow` with real timers. Do not wait real minutes to test the 60-second interval or six-hour fallback. Use `TaskCompletionSource` barriers and EF transaction/save interceptors for ordered races; a bounded cancellation timeout detects a wedge but sleeping or probabilistic stress is not the race oracle. Each fake external operation captures action key, consumer, kind, alias, invocation time and accepted receipt; record body submissions separately from Enter retries, process starts and dispatch claims. Assert using these receipts plus reloaded durable rows, not the scheduler's returned count.

**Fixed parsing vectors (V-1/V-2).** Seed literal text independently of parser constants. The production shape is one AssistantText with the sentence, followed by an `IsApiError=true`, `rate_limit`/429 TurnEnd with **null Text**, sharing its UUID. Include an unrelated sibling with a different UUID so adjacency alone cannot pass. Use synthetic session IDs, never production session IDs.

| Evidence instant (UTC) | Reset text suffix | Expected ResetAt (UTC) | Expected hold/resume deadline |
|---|---|---|---|
| `2026-09-06T07:35:00Z` | `You've hit your session limit · resets 9am (Europe/London)` | `2026-09-06T08:00:00Z` | `08:02:00Z` that day |
| `2026-09-06T11:53:00Z` | `You've hit your session limit · resets 2pm (Europe/London)` | `2026-09-06T13:00:00Z` | `13:02:00Z` that day |
| `2026-09-06T08:00:00Z` | `resets 9am (Europe/London)` | Same instant, at-or-after boundary | `08:02:00Z` |
| `2026-09-06T08:07:48Z` | `resets 9am (Europe/London)` | `2026-09-07T08:00:00Z` | Next day's `08:02:00Z`; do not invent a provider date |
| `2026-01-15T08:00:00Z` | `reset at 9 AM` / `resets 09:00` | `2026-01-15T09:00:00Z` | `09:02:00Z`, absent zone means UTC |
| `2026-07-15T16:00:00Z` / `2026-01-15T16:00:00Z` | `resets 6:10pm (Europe/London)` | July `17:10:00Z` / January `18:10:00Z` | Each reset + 2 minutes |
| `2026-09-06T10:00:00Z` / `2026-09-06T22:00:00Z` | `12pm (Europe/London)` / `12am (Europe/London)`, each preceded by `resets ` | `2026-09-06T11:00:00Z` / `2026-09-06T23:00:00Z` | Each reset + 2 minutes |
| `2026-03-29T00:00:00Z` | `resets 1:30am (Europe/London)` | Invalid nonexistent local time; ModelCap with parse-failure diagnostic | Evidence + configured fallback |
| `2026-10-25T00:00:00Z` and `2026-10-25T00:45:00Z` | `resets 1:30am (Europe/London)` | `2026-10-25T01:30:00Z`, later ambiguous occurrence in both cases | `01:32:00Z` |

Test malformed `9:7am`, `9:000am`, `9amjunk`, `25:00`, `0am`, `13pm`, bare `9`, empty/unclosed zone parentheses and `Mars/Olympus`: none may match a shorter valid prefix or silently use UTC. Distinguish invalid supplied reset from genuinely absent reset in the reason. Existing named-model, alias normalization, Grok 402/credit and non-capacity fixtures stay in the suite.

| ID / slices | Layer; class and exact method prefix | Arrange, act and mandatory assertions |
|---|---|---|
| V-1 / S1 | Unit; `UsageLimitWallParserTests.Card0412_V01_*` | Run every fixed vector and malformed token above. Assert classification, canonical alias, exact UTC reset and diagnostic, not just non-null parsing. Keep minute-bearing controls green when hour-only parsing is deliberately broken. |
| V-2 / S1 | Integration; `ApiErrorRecoveryServiceTests.Card0412_V02_*` | Adopt the null-TurnEnd production pair through both sweep and task-reply adoption. Both resolve the matching sibling and produce the same hold/deadline/one recovery. Vary evidence precedence: usable TurnEnd.Timestamp, otherwise matching AssistantText.Timestamp, otherwise TurnEnd.CreatedAt, otherwise initial DetectedAt; default/unusable timestamps fall through. Repair at `2026-09-06T14:50:00Z` the `11:53Z`/2pm case: it clears with Expired at observation, retaining a `13:02Z` deadline, never tomorrow or repair+6h. A no-reset wall anchors fallback at EvidenceAt, including non-default fallback hours. A once-improved provider timestamp persists its source/version; repeated sweeps cannot move it again. |
| V-3 / S2 | Integration; `ApiErrorRecoveryServiceTests.Card0412_V03_*` | Reproduce clear -> later accepted UserPrompt -> readopt the old empty-text recovery with real 2pm text. Test explicit clear and expiry, with and without later input, so the absent/cleared-hold guard is independently exercised. Reload: no new hold, no reopened Id, identical ClearedAt/cause, original ResolvedAt/history, no admission or queue write. Separate rows reject wrong hold Id, revision, latest EvidenceRecoveryId/source writer, kind, alias, no text improvement and repeated parse-version correction. Positive: exact active empty evidence enriches once on the same Id; a new wall after confirmed useful progress creates a different Id. Manual timed/open-ended holds retain source/deadline; a later Manual PUT invalidates the previous revision. |
| V-4 / S1-S2 | Integration; `ApiErrorRecoveryServiceTests.Card0412_V04_*` | Run relevance checks for both already-adopted repair and first adoption of an old stub after delayed ingestion. Later unrelated accepted UserPrompt/QueuedUserPrompt, successful work TurnEnd, newer wall, canceled/terminal/rerouted task or manual takeover produces only Superseded/Replaced audit, never a hold/retry. Catch-up failure defers without mutation. A local command, synthetic compaction continuation or housekeeping row alone does not suppress a still-relevant wall. Also test the selected action's own matching prompt: it confirms its action rather than being mistaken for unrelated takeover. |
| V-5 / S2-S3 | Integration; `CapacityRecoveryPersistenceTests.Card0412_V05_*` | Two instances adopt the same `(session, stubSequence)` concurrently; one recovery/hold association/active logical wait and one hold link survive. Inject rollback between hold/recovery/wait saves: fresh context sees all or none, no orphan hold. A unique-key loser reloads the winner; an unrelated constraint/DbUpdateException remains a real reported failure with its constraint. Concurrent clear/manual PUT/source overwrite versus stale repair cannot undo the committed winner. Use F-1/F-2 below. |
| V-6 / S2 | Integration; `ModelAvailabilityTests.Card0412_V06_*` and `ModelAvailabilityHttpTests.Card0412_V06_*` | Exercise explicit ClearAsync/HTTP DELETE, timed sweep, lazy lookup, list/snapshot, legacy AutoDetected-null normalization and correction-to-past. Each stamps ClearedAt/cause/pending marker atomically once; DELETE repeated returns existing 204 behavior. Restart before notification still exposes the pending row. Concurrent expiry/DELETE has one winning cause/history and one release; do not insist on which wins without a barrier. Manual source + timed deadline expires with Expired, not OperatorCleared; open-ended Manual never expires. |
| V-7 / S3 | Integration; `CapacityRecoveryPersistenceTests.Card0412_V07_*` | Clear one generation linked to 205 waits with batch 100: bounded 100/100/5 link processing, final acknowledgement only after all relevant links. Crash/rollback a middle batch, rebuild services, finish without lost/duplicated links or actions. Register a wait before clear, between clear and consumption, and after consumption: each observes release. Concurrent last-batch registration cannot be lost. Alias clear under active `*`, replacement hold, and mixed Expired/OperatorCleared links defer until effective availability, attach the new blocker, and retain both known causes. |
| V-8 / S3 | Unit; `CapacityRecoveryPolicyTests.Card0412_V08_*` | Pin default 60-second spacing, stable inclusive 0-30-second jitter across reconstructed policy instances, next action ordinal recomputation, latest required clear base, oldest BlockedAt then Id ordering, and later real-crash/prerequisite deadlines. Use fixed wait IDs and checked-in expected jitter vectors for the chosen stable hash, not the production helper as expected-value oracle. Ready-but-busy/guarded candidates reserve no future slots and cannot head-of-line block another ready consumer. Enqueue/requeue/hold refusal consumes zero attempts. |
| V-9 / S3-S6 | Integration; `CapacityRecoveryAcceptanceTests.Card0412_V09_*` | In one isolated database release six Claude consumers: two live queues, two standing starts, two dispatcher tasks, across opus/sonnet/fable. Start calls concurrently from independent scopes and repeat with each path running first. With jitter disabled only for this timing test, first admission at T, none at T+59.999s, next at/after T+60s; every adjacent committed admission for Claude is >=60s. Assert actual fake input/start/dispatch receipts agree with action identities and execution-boundary checks. Repeat default jitter, restart with all waits overdue, and two same-kind service instances racing for the first provider row. Another kind advances independently; unrecorded ordinary new work retains its policy. No burst of pregranted overdue actions. |
| V-10 / S3-S6 | Integration; `CapacityRecoveryAcceptanceTests.Card0412_V10_*` | Suspend a grant before first external effect, admit other same-kind work later, restart/release delayed launch: same action is revalidated against current clock, no late burst or second episode increment. Commit a new structural wall before the intent boundary: wave revision invalidates the stale grant, no effect, bucket >=wall observation+60s. Repeat alias-only re-hit: other aliases can recover after spacing, another kind is unaffected. Wall committed after accepted intent does not kill/retract accepted work; its race is recorded and the next action is blocked. Cover queue, supervisor and dispatcher boundaries, not just a policy call. |
| V-11 / S3-S6 | Integration; `CapacityRecoveryPersistenceTests.Card0412_V11_*` | Initial block is zero; admit three attempts separated by clears/re-hits/restart and different stubs, then reject fourth as Exhausted. Same accepted action revalidation or re-Enter never increments again or refunds the clock. Own prompt followed by another API-error wall, queue acceptance, uptime or housekeeping preserves episode/budget. Only matched successful non-API-error work TurnEnd closes the work episode; later independent wall starts a new Id at zero. Exhaustion survives another clear/restart. Explicit human continuation can recover, but no automatic reset occurs before useful progress. Seed many old historical walls to prove lifetime counts do not exhaust a new episode. |
| V-12 / S4 | Integration; `CapacityRecoveryQueueTests.Card0412_V12_*` | Prefer oldest eligible Channel/Scheduled body, preserving origin, channel/conversation/schedule correlation and existing contiguous same-conversation batching as one submission. A newer schedule copy or changed binding after selection cancels/reselects before input. No extra Supervision WallPrompt when real queued work covers recovery; without real work exactly one keyed continuation uses `deliverIfIdle:false`. Competing enqueues rediscover the same row in Pending/Sent/Canceled/parked states; same session wall+retained task+queue is one consumer. Only selected row/batch passes the capacity gate until confirmation and ordinary turn boundary. Existing completion-note digest dedup remains unchanged. |
| V-13 / S4 | Integration; `CapacityRecoveryQueueTests.Card0412_V13_*` | Exercise F-5/F-6/F-9. Persist keyed selection then crash; type accepted body then fail acknowledgement save; restart with complete matching fresh transcript record: reconcile original row and action with zero retype/extra Enter. Enqueue, Running and screen-only Delivered leave capacity result unconfirmed. Wrong/stale prompt, matching prefix with missing tail, and delayed full prompt use existing completeness/baseline matcher; truncated/parked bodies are not reset/retyped. A later complete record confirms once and carries exact sequence; no automatic retry merely because the provider has not replied. LF body + bracketed paste + separate Enter and transport re-Enter for the same attempt retain existing semantics. |
| V-14 / S4 | Integration; `CapacityRecoveryQueueTests.Card0412_V14_*` | Between scheduling and final flush inject real unrelated input, new relevant wall, owner/session StartedAt replacement, changed task attempt, schedule cancellation or failed transcript catch-up. Each suppresses/defers the old selection, with zero input or attempt spend before admission. Local slash commands and compaction housekeeping do not falsely complete the episode; genuine busy work defers without stealing a slot. Late callbacks for a canceled/old action cannot confirm or close the new action. |
| V-15 / S4 | Integration; `CapacityRecoveryQueueTests.Card0412_V15_*` | Paired barrier matrix Q below: keep capacity available and every other prerequisite enabled. Blocked arm emits zero body/Enter/start and no capacity admission; authorized control emits exactly the selected action. Verify NeedsHuman, AuthenticationRequired/provider sign-in, UnknownExhausted, WallUnparsed, Herdr blocked/unreachable, rules initialization/refresh failure, delivery-attempt cap and forbidden/truncated body remain authoritative. A cleared capacity barrier alone permits only its keyed selected action. |
| V-16 / S6 | Integration; `CapacityRecoverySupervisionTests.Card0412_V16_*` | Paired standing barrier matrix S below. Recheck Suspended, LivenessLatchedAt, HerdrFailureHeldAt, AlwaysOn, owner/incarnation and card intent at automatic start acceptance. Stop committed before intent wins; Stop after accepted start invokes existing stop/cancellation exactly once, without another start. Automatic code never acknowledges a latch, sets ResetHerdrFailureHold, or widens AllowUnauthenticatedProvider/IgnoreSubscriptionQuota. Explicit supported manual Start/Herdr retry still works. |
| V-17 / S6 | Integration; `CapacityRecoverySupervisionTests.Card0412_V17_*` | Repeated synchronous ModelDisabledException and typed asynchronous capacity launch failures record wait without increasing generic crash count/tier or Herdr streak. Clear pulls forward only capacity delay; pre-existing independent crash backoff is retained. Capacity-only failures never cross FreshAfterResumeFailures; a supported valid native conversation resumes with same identity, missing/unsupported/unknown resume or rules migration refusal remains visible. Ordinary authorized initial standing start with no interrupted conversation and independent crash recovery retain their own policy. Live Running session receives queue recovery, never a start; dead unowned/ephemeral consumer never gets a new cold task. |
| V-18 / S6 | Integration; `CapacityRecoverySupervisionTests.Card0412_V18_*` | F-7 launch acceptance persisted before async start; rebuild supervisor with surviving NextRestartAt, discover accepted session/action, assert exactly one launch. Matrix: pending channel work, interrupted-turn continue, launch note, boot probe, mandatory rules initialization. Each emits one selected useful work input for the action, not continue+WallPrompt or probe+WallPrompt. Mandatory initialization is ordered before useful work, retains its own non-work meaning, and does not double-charge admission. Pure process wait may finish at confirmed Running; interrupted work still requires prompt/turn evidence. |
| V-19 / S5 | Integration; `CapacityRecoveryTaskTests.Card0412_V19_*` | Future resumable canonical Wall keeps Working, same task/attempt/session, typed CapacityWait, workspace lease and occupied slot; no Failed event or delegate release before exhaustion. Same-task resume needs no second slot or conflicting self-lease. Overdue/check/settlement defers only eligible typed waits; ordinary overdue work, specialist execution deadline and cancellation still settle as before. Exhaustion uses visible fail/release and leaves process killed, pooled warm or standing-owned; dirty-work diagnostics survive. A dead ephemeral session or historical Failed/Succeeded/Canceled task is never relaunched. |
| V-20 / S5 | Integration; `CapacityRecoveryTaskTests.Card0412_V20_*` | Paired routing/dispatch matrix T below. Existing declared candidate walk, pins, forbids, cascade, quota/cost/specialist/scope/global cap, placement and alias/model Require guards run before transactional dispatch claim. Availability-only routing block requeues once, claims/launches once, preserves Rerouted/cascade history; already requeued task has no second same-session wake. IgnoreModelDisabled creation records the blocker even if clear precedes next dispatch tick. Competing claims and restart after accepted claim use F-8. |
| V-21 / S5 | Integration; `CapacityRecoveryRefusalTests.Card0412_V21_*` | Authenticated standing caller refused before child creation gets one durable wait before unchanged 409 code/coda. Equivalent request digests coalesce without storing full briefs/credentials. External capability caller without a resolved session gets no invented recipient. A Codex standing caller with MayDelegate refused a Claude child waits on Claude availability but its one notice uses Codex model/clock and honors Codex's own hold. Subsequent equivalent successful create transfers digest/wait identity into normal Claude dispatch admission; it cannot evade pacing as ordinary new work. No cached request replay or automatic provider substitution. Later user instruction, card/pin change, caller termination or cancellation obsoletes the notice; merely explaining the refusal does not resolve intended dispatch. |
| V-22 / S2-S3/S7 | Integration; `CapacityRecoveryCompatibilityTests.Card0412_V22_*` | In a disposable cloned database only, migrate to predecessor of the first CARD-0412 migration, seed old active timed/open-ended Manual and already-cleared rows plus terminal tasks using the old columns, then migrate forward using EF's real migration chain. Existing IDs/deadlines/source/task outcomes survive; old cleared rows have unknown legacy cause and no pending replay. Real indexes reject duplicate active consumer, hold link, non-null queue action key and provider-kind state; multiple null queue keys remain legal. Missing/deleted nullable owner cancels a wait once with retained audit, not repeated sweep failure. Never downgrade shared/dev databases. |
| V-23 / S7 | Integration; `CapacityRecoveryCompatibilityTests.Card0412_V23_*` | Reconcile 205 current candidates at batch 100, including a live owned wall older than 180 minutes and pending capacity queue without trustworthy old hold link. Assert bounded candidate and transcript-RPC counts per pass, durable cursor/version and per-candidate result across restart, failed catch-up retried without starving later candidates. Unique latest empty/nonempty hour-only legacy evidence can correct once; ambiguous shared-source/timestamp-only lineage and newer evidence do not alter hold. Manual diagnostics may improve without deadline change. Terminal task history, later genuine input, stale incarnation and all Q/S/T barriers are excluded. Available authorized unfinished work receives LegacyAvailable without fabricating a hold/cause. Repeated passes create no duplicate wait/action. |
| V-24 / S7 | Integration; `CapacityRecoveryAttentionTests.Card0412_V24_*` | Read real incident/timeline/task/agent/Attention projections. Assert keyed scheduled plus confirmed/final outcome per episode, exact matching prompt sequence, kind/alias/hold revisions/clear cause(s)/action/attempt/next due; queued and screen-only states never say Auto-resumed. Repeated polls and retry after post-commit publish failure do not duplicate timeline facts or flood deferred logs. Exhausted Attention is still present after pruning incident history. New episode gets its own event; LegacyAvailable has distinct text, unknown cause stays unknown. `raiseAlert:false`, no chat send/new column. Captured logs are structured and omit synthetic secret/full-body sentinels. |
| V-25 / S1-S7 | Integration application acceptance; `CapacityRecoveryAcceptanceTests.Card0412_V25_*` | Four required runs: Expired and OperatorCleared, each uninterrupted and with service reconstruction after clear commit. Fake-backed running standing session + real queued Channel body, dead authorized standing owner with interrupted work, and capacity-blocked queued task start under overlapping alias/kind-wide holds. Clear through actual ModelAvailability service/API, drive real supervisor/dispatcher/ordinary queue loops, observe selected prompt/start/dispatch receipts, feed matching transcript via runtime evidence path, then successful work TurnEnd. Assert paced recovery and visible durable timeline/task confirmation carrying its prompt sequence. Control with no transcript yields queued/unconfirmed text; follow re-hits through three attempts to durable exhausted Attention. No hand-written wait state transitions masquerading as acceptance. |
| V-26 / S3-S7 | Unit `CapacityRecoveryPolicyTests.Card0412_V26_*`; Integration `CapacityRecoveryAcceptanceTests.Card0412_V26_*` | Bind/validate settings: interval 1/3600 valid and 0/3601 invalid; jitter 0/300 valid and -1/301 invalid; attempts 1/10 valid and 0/11 invalid; batch 1/1000 valid and 0/1001 invalid. Unspecified MaxEpisodeAttempts honors configured WallDeathCap; explicit new value wins. Disable new feature or each relevant Supervision/ApiErrorRecovery parent flag after scheduling: no new capacity admission from any affected path, waits/history retained, parser and clear tracking still work; already delivered turn remains intact. Audit recorded-consumer wake from Supervision/System queue origins and channel/provisioner automatic starts through same admission/guards. Human input and mandatory rules initialization keep their existing authority. |

**Paired barrier matrices.** Implement one identifiable blocked/control pair per item below in the specified V prefix. The only difference is the named prerequisite, changed through its established acknowledgement/API where one exists. Before checking zero effects, prove the capacity hold cleared, time is due, fake runner is ready and a wait/selection was actually reached. After removing only that barrier the same authorized consumer must produce its allowed effect. For immutable terminal/obsolete owners, use a separate otherwise identical *current authorized* consumer as the control; do not reopen old history to manufacture a pass.

| Matrix | Independent blocked rows / enabled controls |
|---|---|
| Q / V-15 | NeedsHuman; AuthenticationRequired/provider-sign-in; UnknownExhausted; WallUnparsed; Herdr blocked; Herdr unreachable; rules init pending/failed; rules refresh failed; DeliveryAttempts at cap; forbidden body; truncated body. Controls use resolved prerequisite, trusted complete body below cap, or acknowledged replacement row as existing policy permits. Do not obtain a control by resetting the parked row's attempts. |
| S / V-16-V-17 | Suspended; liveness latch; Herdr failure hold; no standing/AlwaysOn owner; replaced owner/StartedAt; canceled/terminal card; sign-in required; unsupported or missing native resume; rules migration refusal; placement refusal. Controls explicitly acknowledge human barriers where supported; only supported valid resume may continue interrupted work. Assert standard AlwaysOn quota-ignore precedent separately from unchanged create/dispatch quota refusal. |
| T / V-20 | Required pin with only disallowed alternate available; forbidden alias; cascade exhausted; subscription quota; cost ceiling; scope lease conflict; specialist limit; global occupied-task limit; placement mismatch; unknown alias/model Require; manual card move/cancellation; changed owner/attempt. Controls restore the original allowed route/resource or use new authorized intent. Availability-only exhausted candidates re-walk; counters/pin identity stay intact. |

**Fault and race schedule.** Each F-n is a data row under the V-n above. Interceptors pause at the specified production boundary; another independent service acts; then release/throw and reconstruct services on the same database. Fault switches live in test doubles/interceptors, never a production recovery bypass. A rollback must be observed from a fresh context. Event bus, transcript RPC, queue flush and start fakes assert no active service database transaction at entry; a blocked external fake must not block another independent database transition. Check consistent provider -> owner -> wait -> sorted holds lock order by bounded competing calls; no database transaction may wait on the queue semaphore.

| Fault | Pause/fail boundary | Required post-restart or competing-operation oracle |
|---|---|---|
| F-1 / V-5 | Hold change staged, recovery/wait save not committed | Rollback leaves none; retry leaves one fully linked set. Unique conflict is recoverable, unrelated constraint failure is not swallowed. |
| F-2 / V-3/V-5/V-6 | Repair tracked old hold; second context clears, changes Manual revision, or assigns newer evidence | Committed new state wins. Old repair does not change history/deadline or create another generation. Also force reverse order to check serialization rather than a fixed winner. |
| F-3 / V-6/V-7 | Clear before commit, after commit before publish, and release links marked before batch acknowledgement commit | Precommit rollback has no partial clear; postcommit publisher failure leaves pending release; partial batch rolls back together and resumes. |
| F-4 / V-9/V-11 | Admission/wait/clock staged before commit, then committed before any external side effect | Rollback spends nothing; committed claim can conservatively consume its one slot/attempt. Restart never refunds or increments same action twice. |
| F-5 / V-12/V-13 | Queue selection/row write before wait save or immediately after transaction commit | Zero partial visibility or rediscovery by existing key; one selected body and action after restart. No eager enqueue flush. |
| F-6 / V-13 | Fake accepts complete prompt, acknowledgement save throws | Transcript catch-up reconciles same message/action with no retype/extra Enter. If transcript is unavailable, remain unconfirmed/deferred under existing delivery policy; do not invent exactly-once external transport. |
| F-7 / V-18 | Launch intent/session receipt committed, asynchronous launch pending or callback lost | One accepted launch, same incarnation/action rediscovered, no second start despite stale NextRestartAt. |
| F-8 / V-20 | Two dispatchers select same task; claim commits before launch result/save | One accepted task attempt/launch receipt; restart reconciles original claim without replaying brief. |
| F-9 / V-13/V-14 | Confirmation callback arrives after cancellation, new action or session/attempt replacement | Old receipt cannot update replacement action or label it confirmed/progressed; retained old audit is unambiguous. |
| F-10 / V-10/V-16 | Stop/re-hold between eligibility read and start/input/dispatch intent, then after committed intent | Before-intent barrier wins; after-intent action is reconciled/canceled through existing path without duplicate or automatic kill on wall. |

### Guards the regression

| ID | Future regression | Caught by |
|---|---|---|
| R-1 | Requiring `:mm`, partial token match, treating invalid zone as absent, or parsing against sweep time | V-1/V-2 exact UTC/diagnostic vectors, delayed repair and evidence-source persistence. |
| R-2 | Empty repair or first adoption becomes a general upsert, or SourceSessionId substitutes for exact lineage | V-3/V-4/V-5 immutable cleared history, no newly created hold and independent lineage mismatches. |
| R-3 | Clear publishes only an ephemeral event, lazy clear omits pending state, or release acknowledgement precedes all links | V-6/V-7 and F-3 clear rollback, publisher failure, late registration and 205-consumer restart cases. |
| R-4 | Per-alias/in-memory/enqueue-time pacing replaces shared execution-boundary admission | V-9/V-10/F-4 mixed real paths, actual receipts, overdue restart and slow-launch cases. |
| R-5 | Availability of one alias bypasses `*`/replacement hold or grants survive a fresh wall | V-7/V-10 exact blocker links, no pre-intent effect and wave invalidation; unrelated aliases/kinds still progress. |
| R-6 | Every clear/stub/prompt/restart resets attempts, or lifetime Wall count returns | V-11 three admitted actions then exhaustion, successful-turn reset only and independent historical-wall control. |
| R-7 | Ordinary enqueue, launch note/probe or recovery callback emits a duplicate work turn | V-12/V-13/V-18 and F-5-F-9 one selected body, keyed receipt and no post-confirmation retype. |
| R-8 | Broad terminal-capacity bypass or automatic Start calls human latch-clear behavior | V-14-V-17 paired Q/S barriers and ordered Stop race with enabled controls. |
| R-9 | Capacity waiting fails/releases a live delegate, waives real deadlines, or bypasses routing/limits | V-19/V-20 retained attempt/lease, narrow overdue deferral, paired T barriers and release ownership. |
| R-10 | Refused-request replay, inferred recipient or child-kind bucket for caller input | V-21 unchanged 409, no-session negative, cross-kind receipts and digest-preserving transfer. |
| R-11 | Compatibility scans all history, applies the 180-minute cutoff, guesses lineage or repeats on restart | V-22/V-23 bounded current-work/cursor tests, immutable old history, unique correction and LegacyAvailable. |
| R-12 | Recovery becomes silent, screen-only means resumed, incident pruning hides exhaustion, or disabling still admits | V-24-V-26 actual projection/log assertions, non-transcript controls, retained Attention and disabled execution boundaries. |

Also run the existing named-class regressions in the command matrix below. Preserve their non-capacity contracts. Replace `ApiErrorRecoveryServiceTests.Wall_parks_after_three_deaths` with V-11's episode-budget assertion when S3 changes its contract; do not keep its historical-row expectation by weakening the new behavior. Update scheduled-reset assertions only to reflect the keyed admission design while retaining exact reset+2-minute eligibility. Existing screen-only delivery tests may stay green; V-13/V-24 must prove the **capacity outcome** still requires transcript confirmation.

### Positive controls

Code runs **every** PC below: production guard edit -> scoped assertion failure -> revert just that edit -> same cases green. Keep the tests unchanged. These are deliberate local guard removals in the isolated test lane, never a deployed configuration or live recovery experiment. Each row specifies a one-line production expression/assignment mutation at its owning boundary, not a new test-only production flag. Where the implementation introduces the guard, record the final concrete file:line and one-line diff in Code's evidence; use `if (false && <rejection predicate>)` to disable exactly that rejection, not an outer unrelated guard. If defense in depth still prevents effects, assert the violated admission/selection/receipt invariant at that boundary; a compile error or unaffected green run does not count as detecting the mutation.

The PC command is `Invoke-C412Verification -V <number> -Tag 'pcNN-red' -ExpectRed`, then, after reverting the edit, the same command with `-Tag 'pcNN-green'`. V rows that name two classes run both. The V prefix is an exact method-family filter; identify which data row/assertion killed the mutation in the report. Each paired barrier listed inside a PC row is mutated separately and receives its own `pcNN-<barrier>-red/green` evidence; a single failure must not stand in for several guards.

| ID | One-line edit in production, then revert | Must turn red / violated assertion |
|---|---|---|
| PC-1 | Parser: make omitted minutes illegal again (require the colon/minutes branch). | V-1; literal 9am/2pm UTC cases fail, minute controls still pass. |
| PC-2 | Evidence-time call site: pass `UtcNow()` in place of persisted EvidenceAt. | V-2; delayed repair deadline and fallback anchor move incorrectly. |
| PC-3 | Repair: bypass the missing/cleared linked-hold rejection, separately for each arm. | V-3; clear without later prompt still cannot recreate/change hold. |
| PC-4 | Repair: bypass expected hold Id, revision, latest EvidenceRecoveryId, kind and alias equality checks, one at a time. | V-3; each independent mismatch must keep existing history/deadline unchanged. |
| PC-5 | Repair: bypass evidence-improvement/parse-version-once check. | V-3; repeat enrichment cannot advance revision/resolution history. |
| PC-6 | Availability/enrichment: bypass Manual precedence or later Manual revision rejection, separately. | V-3; Manual timed/open-ended deadline and source remain unchanged. |
| PC-7 | Adoption: bypass later accepted prompt/completion/newer-wall relevance rejection, separately on initial-adoption and repair arms. | V-4; stale audit only, zero hold mutation. |
| PC-8 | New adoption: remove the auto-writer's enlistment in the caller transaction. | V-5; injected rollback must not leave an orphan hold. |
| PC-9 | Adoption catch: treat every DbUpdateException as the unique adoption conflict. | V-5; unrelated constraint failure must surface, not return a successful adoption. |
| PC-10 | Clear primitive: omit the ReleasePendingAt assignment on clear. | V-6; each real clear path must retain durable release after publish failure/restart. |
| PC-11 | Release consumption: set ReleaseConsumedAt after the first batch unconditionally. | V-7; all 205 links must be acknowledged before final consume. |
| PC-12 | Wait registration: omit reevaluation when linked hold is already cleared/consumed. | V-7; late-registration immediate ready/due observation fails (do not wait for periodic fallback to hide it). |
| PC-13 | Execution eligibility: bypass current availability rejection at queue/start/dispatch boundary, one path at a time. | V-10 (plus V-7 in green baseline); replacement or wildcard hold must forbid a pre-intent effect. |
| PC-14 | Admission: remove the conditional database claim that checks NextAdmissionAt while holding provider serialization. | V-9; competing instances must not grant two same-kind actions inside 60s. |
| PC-15 | Delayed action: bypass revalidation of an unexecuted old admission. | V-10; overdue slow launch must not create a late burst. |
| PC-16 | Fresh wall: omit wave revision increment/invalidation or wall-observation interval floor, separately. | V-10; old grant cannot execute and new admission cannot precede wall+60s. |
| PC-17 | Episode: bypass admission-count exhaustion check, or reset count on clear/re-hit, separately. | V-11; fourth recovery never admitted and same episode keeps three attempts across restart. |
| PC-18 | Progress hook: close work episode on UserPrompt instead of successful work TurnEnd. | V-11; own prompt then wall cannot reset budget. |
| PC-19 | Queue: generate a fresh CapacityRecoveryActionKey when repeating the same logical enqueue. | V-12; durable key/row/body uniqueness fails even for Sent/Canceled/parked rows. |
| PC-20 | Queue selection: enqueue WallPrompt even when selected Channel/Scheduled work already covers recovery. | V-12; exactly one selected body/batch and preserved correlation. |
| PC-21 | Final flush: bypass selected-action-only capacity acknowledgement. | V-12; unselected blocked queue row cannot pass before confirmation/turn boundary. |
| PC-22 | Restart reconciliation: ignore existing transcript-confirmed receipt and run delivery again. | V-13; accepted-input-before-save case permits zero retype/extra Enter. |
| PC-23 | Capacity confirmation: accept screen-only Delivered, queue insertion or incomplete matching prefix as prompt proof, separately. | V-13; no confirmed state/sequence until fresh full transcript evidence. |
| PC-24 | Final flush: bypass unrelated later-input suppression, then failed-catch-up deferral, separately. | V-14; no stale selected input after human progress or unknown transcript state. |
| PC-25 | Terminal gate: allow capacity acknowledgement to bypass NeedsHuman/auth/UnknownExhausted/WallUnparsed predicate. | V-15; each terminal classification blocked arm remains zero effect. |
| PC-26 | Queue prerequisite: bypass Herdr blocked/unreachable and rules-init/refresh predicates, separately. | V-15; each Q barrier remains effective while capacity is available. |
| PC-27 | Queue prerequisite: bypass delivery-attempt cap, forbidden-body and truncated-body predicates, separately. | V-15; no parked-body reset/retype or admission. |
| PC-28 | Guarded automatic start: bypass Suspended, LivenessLatchedAt and HerdrFailureHeldAt checks, separately. | V-16; each latch survives with no launch and explicit authorized control still works. |
| PC-29 | Guarded start: bypass current owner/StartedAt and terminal/canceled-card checks, separately. | V-16; committed Stop or changed ownership cannot admit stale start. |
| PC-30 | Automatic request: set ResetHerdrFailureHold or AllowUnauthenticatedProvider true; separately set IgnoreSubscriptionQuota true on create/dispatch. | V-16; captured requests and refusal controls show overrides were not widened. |
| PC-31 | Automatic start: bypass standing/AlwaysOn requirement. | V-17; dead ephemeral/unowned session must never be cold-restarted. |
| PC-32 | Supervisor: let ModelDisabledException enter generic crash failure accounting. | V-17; capacity-only failures cannot grow crash ladder/streak or choose Fresh. |
| PC-33 | Start: bypass native-resume support/identity guard, separately from rules-migration refusal. | V-17; missing/unsupported/unknown resume or migration refusal remains a decision, never silent Fresh. |
| PC-34 | Launch: omit existing accepted action/session receipt lookup on restart. | V-18; one accepted launch despite surviving restart schedule. |
| PC-35 | Launch: emit capacity continuation in addition to the selected existing continue/channel/probe input. | V-18; one useful work input, one admission. |
| PC-36 | Reply: let terminal WallModelPaused/WallParked fail/release an eligible live typed capacity wait. | V-19; Working task/attempt/session/lease and occupied slot survive before exhaustion. |
| PC-37 | Deadline gate: defer every Working task, or bypass specialist execution deadline/cancellation, separately. | V-19; unrelated overdue work and hard deadlines still settle. |
| PC-38 | Dispatcher: bypass Required-pin, forbidden-alias and cascade-exhaustion rejection, separately. | V-20; each T route barrier holds with its permitted counterpart enabled. |
| PC-39 | Dispatcher: bypass cost, scope lease, specialist and global concurrency guards, separately. | V-20; each constrained resource rejects admission/claim without blocking its restored control. |
| PC-40 | Dispatcher/start: bypass placement and unknown-alias/model Require guard, separately at each touched capacity execution path. | V-20/V-16; explicit refusal and actual effective kind/alias remain intact. Run both V numbers. |
| PC-41 | Dispatcher: omit existing accepted-attempt claim/receipt check after restart. | V-20; F-8 produces only one attempt/launch. |
| PC-42 | Refusal: use requested child kind instead of actual caller kind as notice's execution bucket. | V-21; Codex notice must obey Codex clock/hold, then child launch Claude clock. |
| PC-43 | Equivalent create: omit refusal digest/capacity-wait transfer to the new task. | V-21; resumed blocked intent cannot enter dispatcher as unpaced normal work. |
| PC-44 | Refusal registration: bypass resolved-standing-session requirement, then current-intent/cancellation check, separately. | V-21; no synthetic recipient and no notice for obsolete request. |
| PC-45 | Compatibility: reapply the 180-minute adoption cutoff to current-work candidates. | V-23; the older genuinely stranded live consumer must recover. |
| PC-46 | Compatibility: bypass terminal-history exclusion, unique-lineage requirement or batch limit, separately. | V-23; old task outcomes/ambiguous hold unchanged, <=100 candidates/RPCs per pass. |
| PC-47 | Late callback: bypass expected action/session incarnation/task-attempt check, separately. | V-14; stale receipt cannot confirm replacement action. |
| PC-48 | Exhausted Attention: require retained CapacityRecovery incident instead of durable exhausted wait. | V-24; Attention survives incident pruning. |
| PC-49 | Visibility: omit lifecycle incident/task-event persistence, then structured lifecycle log call, separately. | V-24; real projections/log sink must contain correlated visible recovery, not just changed wait state. |
| PC-50 | Admission: bypass CapacityRecovery enabled or relevant parent-enable check, separately for queue/start/dispatch. | V-26; no new admission after disabling, even when already ready. |
| PC-51 | Parser: remove reset-token boundary validation, accept a supplied invalid zone as UTC, or omit nonexistent/ambiguous local-time handling, separately. | V-1; malformed suffixes never partially parse, supplied invalid zones fail explicitly, DST gap is rejected and fold chooses later UTC. |
| PC-52 | Queue final selection: bypass expected owner/StartedAt/task-attempt, current schedule or channel-binding check, separately. | V-14 for owner/attempt; V-12 for schedule/binding. No stale body or admission after replacement. |
| PC-53 | Dispatch claim: bypass current card intent/manual-move or expected owner/task-attempt check, separately. | V-20; stale task cannot dispatch after authorization changes even with available capacity and an old ready wait. |
| PC-54 | Clear primitive: omit the already-cleared conditional update guard and restamp the row on repeated clear. | V-6; original ClearedAt/cause/pending-consumption history is immutable and no second release is created. |

PC-1 and PC-3 must additionally produce a **baseline red** against the pre-fix behavior in S1/S2 using tests limited to the existing APIs/fields (so missing new properties cannot cause the red). Use exact production text and clear/later-prompt/readopt fixtures. Retain those black-box assertions as implementation grows. Do not remove a test assertion or change the fake to obtain red evidence. When an independent barrier masks a mutation, arrange the fixture so all other barriers permit the action and assert the specific crossed boundary; do not weaken production barriers together.

### Runnable commands and evidence

Run from the implementation checkout. This PowerShell helper expands each V-n to exact class/method-prefix filters, gives each execution a unique TRX filename, and rejects a surprising exit code. Paste it into the Code verification shell (it is a documentation recipe, not an installed script):

```powershell
$c412Classes = @{
  1 = @('UsageLimitWallParserTests')
  2 = @('ApiErrorRecoveryServiceTests')
  3 = @('ApiErrorRecoveryServiceTests')
  4 = @('ApiErrorRecoveryServiceTests')
  5 = @('CapacityRecoveryPersistenceTests')
  6 = @('ModelAvailabilityTests', 'ModelAvailabilityHttpTests')
  7 = @('CapacityRecoveryPersistenceTests')
  8 = @('CapacityRecoveryPolicyTests')
  9 = @('CapacityRecoveryAcceptanceTests')
  10 = @('CapacityRecoveryAcceptanceTests')
  11 = @('CapacityRecoveryPersistenceTests')
  12 = @('CapacityRecoveryQueueTests')
  13 = @('CapacityRecoveryQueueTests')
  14 = @('CapacityRecoveryQueueTests')
  15 = @('CapacityRecoveryQueueTests')
  16 = @('CapacityRecoverySupervisionTests')
  17 = @('CapacityRecoverySupervisionTests')
  18 = @('CapacityRecoverySupervisionTests')
  19 = @('CapacityRecoveryTaskTests')
  20 = @('CapacityRecoveryTaskTests')
  21 = @('CapacityRecoveryRefusalTests')
  22 = @('CapacityRecoveryCompatibilityTests')
  23 = @('CapacityRecoveryCompatibilityTests')
  24 = @('CapacityRecoveryAttentionTests')
  25 = @('CapacityRecoveryAcceptanceTests')
  26 = @('CapacityRecoveryPolicyTests', 'CapacityRecoveryAcceptanceTests')
}
function Invoke-C412Verification {
  param([ValidateRange(1,26)][int]$V, [string]$Tag = 'green', [switch]$ExpectRed)
  $c412SawNonZero = $false
  foreach ($c412Class in $c412Classes[$V]) {
    $c412Prefix = 'Card0412_V{0:D2}_*' -f $V
    $c412Filter = "/*/Antiphon.Tests.Application/$c412Class/$c412Prefix"
    $c412Stamp = [guid]::NewGuid().ToString('N')
    $c412Trx = "c412-v$V-$Tag-$c412Class-$c412Stamp.trx"
    dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c412/ -- --treenode-filter $c412Filter --report-trx --report-trx-filename $c412Trx
    $c412Exit = $LASTEXITCODE
    Write-Host "CARD-0412 V-$V $Tag $c412Class exit=$c412Exit trx=$c412Trx"
    if (!$ExpectRed -and $c412Exit -ne 0) { throw "Expected green: $c412Trx" }
    if ($c412Exit -ne 0) { $c412SawNonZero = $true }
  }
  if ($ExpectRed -and !$c412SawNonZero) { throw "Mutation was not detected in V-$V" }
}
# Each slice runs its listed V numbers as it lands; final unmutated acceptance:
1..26 | ForEach-Object { Invoke-C412Verification -V $_ }
# Example only, with PC-1 edit present, then after restoring that one edit:
# Invoke-C412Verification -V 1 -Tag 'pc01-red' -ExpectRed
# Invoke-C412Verification -V 1 -Tag 'pc01-green'
```

The helper's exit-code check is only a first check. For **every** invocation locate its exact fresh TRX filename under `tests/Antiphon.Tests` (`rg --files --hidden --no-ignore tests/Antiphon.Tests -g '<printed-filename>'`), read executed `UnitTestResult` names/outcomes and `ResultSummary/Counters`, and retain its absolute path. Green requires nonzero executed cases, zero failed/error/aborted, no required skipped cases and every intended data row. Red requires the **named violated assertion** on an executed intended case; build failure, fixture failure, timeout, exit 8/zero tests or unrelated test failure is invalid. V-6/V-26 PC mutations may affect only one of their two classes: the helper permits the other class to stay green, but requires at least one nonzero exit and the TRX review must prove the expected assertion in the affected class. Rebuild after every mutation/restoration; never use an old `--no-build` binary to report a control. Record original/mutated/restored SHA or diff hash and actual counts, not predicted case totals.

Run these **existing regression classes once unmutated after their slice**, and again only if subsequent changes affect them. All are `Antiphon.Tests.Application` except the test helpers. The single-class filter avoids runner OR discovery ambiguity:

```powershell
$c412Regressions = @(
  'UsageLimitWallParserTests', 'ApiErrorRecoveryServiceTests',
  'ModelAvailabilityTests', 'ModelAvailabilityManualTests', 'ModelAvailabilityHttpTests',
  'SessionMessageQueueServiceTests', 'SessionMessageQueueSupervisionTests',
  'SessionMessageQueueDeliveryVerificationTests', 'SessionMessageQueueInterruptedAttemptTests',
  'GrokRulesQueueBarrierTests',
  'ModelAvailabilityCreateTests', 'ModelAvailabilityDispatcherTests',
  'ComplexityWallRerouteTests', 'RoutingPinDispatcherTests', 'RoutingPinCandidateDispatchTests',
  'AgentTaskReplyIntegrationTests', 'AgentTaskOverdueDeadlineTests',
  'AgentSupervisionTests', 'HerdrSupervisionBackoffTests',
  'AgentSystemPromptLaunchTests', 'GrokRulesResumeMigrationTests', 'AttentionServiceTests'
)
foreach ($c412Class in $c412Regressions) {
  $c412Trx = 'c412-regression-{0}-{1}.trx' -f $c412Class, [guid]::NewGuid().ToString('N')
  dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c412/ -- --treenode-filter "/*/Antiphon.Tests.Application/$c412Class/*" --report-trx --report-trx-filename $c412Trx
  if ($LASTEXITCODE -ne 0) { throw "Regression failed: $c412Trx" }
}
dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c412/ -- --treenode-filter "/*/Antiphon.Tests.Application/HerdrAlwaysOnChannelParityTests/Named_AlwaysOn_herdr_agent_holds_after_three_failures_keeps_its_timeout_shell_and_resumes_only_on_explicit_retry" --report-trx --report-trx-filename c412-herdr-hold.trx
git diff --check
```

Inspect that final Herdr command's exit and fresh nonzero TRX independently; a following successful `git diff` is not its verdict. If client projections/types/labels are changed, run `pwsh -File scripts/test-client.ps1 attentionVisuals.test` plus the exact touched component test file (record its name in Code's report); use the wrapper's `CLIENT TESTS EXIT CODE`. If client code is untouched, this backend application acceptance does not force client rebuild or browser E2E. Any newly justified process-spawning test takes the assembly-local `ParallelLimiter<ProcessSpawnLimit>`; `Antiphon.Tests` and `Antiphon.Agents.Pty.Tests` must run sequentially.

Code's evidence table has one row per V-n, R-n and PC-n (separate subrows for each guard-removal/data matrix arm): method/data-case names, executed/passed/failed counts, expected assertion for red, TRX/log absolute paths, restored-green counts, and unresolved gap. Include a small receipt timeline for V-9/V-10/V-25 with actual UTC admission/effect times, kind, action, prompt sequence and visible event Id; it must substantiate both >=60-second spacing and one selected useful input. A checkmarked design or build alone is not verification.

### Out of scope

- Paid-provider canaries, clearing production holds, production runner port 17204, real broker/chat sends, deployment and restart scripts. Fake-backed application acceptance proves orchestration behavior; it does not claim provider capacity returned or a production conversation recovered. Real Program hosts retain `ProductionRunnerGuard` and disabled check/diagnose/distiller provisioning; override the refusing runner only with a recorded in-process fake for this test, or use an owned isolated random-port runner if a process test becomes necessary.
- Reproving unchanged PTY bytes with headed vendor CLIs, whole-assembly/nightly suites, and broad browser screenshots. Existing queue byte/confirmation regressions plus the selected-input acceptance cover the changed boundary. Transport changes discovered during Code require their owner and an explicit additional test case.
- Historical task resurrection, accounting for every API call inside a model turn, provider credential grouping, auto-rerouting to undeclared providers, load benchmarks and a new timer/dispatcher. The plan authorizes none of these; assertions instead prove current-work bounds and existing routes.
- Claiming exactly-once input across unavailable external transcript evidence. The achievable contract is durable action identity, no duplicate after matching receipt, bounded existing delivery recovery and an honest unconfirmed/deferred outcome when evidence is absent.

### Cost

- Forced suite: scoped `tests/Antiphon.Tests` parser/policy and Integration classes above, plus the one Herdr hold method. Default verification requires no `Antiphon.Agents.Pty.Tests`, real providers or browser E2E. Optional touched client tests use the wrapper.
- Estimated **unmutated** floor: 15-30 minutes including one graph build/Postgres startup and the scoped queue/launch regressions. Pure vectors/policy should take seconds; aim for <5 seconds per new deterministic non-polling case. Compress test delivery windows using established settings, not production intervals or sleeps. These are planning estimates, not measured timings.
- Required PC-1-PC-54 include independent guard subcases, so budget roughly **2-4 hours total verification** with repeated incremental builds/red/restored-green runs; test implementation time is additional. Do not disguise this as the cost of a parser-only change. Run controls beside their slice and reuse a single valid output directory; record actual runtime and any >=5-second new tests for the duration tripwire/allowlist review.
- A failed fixture or mutation that stays green needs diagnosis before declaring its guard verified. Report a remaining V/R/PC item as incomplete; do not activate or report S1-S7 complete on a partial evidence matrix.
