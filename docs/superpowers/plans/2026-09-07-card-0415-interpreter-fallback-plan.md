# CARD-0415: qualified interpreter fallback and durable Attention

Plan complete. The failed Codex mitigation was rejected before delivery because the Check task still requested ClaudeCode. Add an explicitly configured specialist candidate chain, qualify each candidate with actual interpretations, and expose loss of interpretation through durable Attention. Implementation and real-model acceptance remain ahead; TestDesign is next.

Basis: full CARD-0415 and CARD-0412 read on 2026-09-07; source at `1ba060b669c60e1e6a55318e51476b0c5dcc5980`; read-only server task, agent and transcript evidence below. This plan makes no production configuration, provider, session or hold changes.

## Ground truth

| Card assumption / question | Observed behavior | Consequence |
|---|---|---|
| Codex answered ready, then failed to interpret | Task `7fa88286-d832-4f48-80c8-4a4ab636277d`, created 2026-09-06 13:19:33.949947Z, failed at 13:19:38.651805Z: `Task 7fa88286 runs on ClaudeCode, but it is pinned to agent 'antiphon-check-interpreter' whose live session ef59a3a3-539f-4b9c-bb57-00b0c6ae6833 is Codex.` The stored reason begins `Dispatch failed before a session existed`. | This real failure establishes a dispatch identity defect, not model noncompliance. Preserve the dispatcher mismatch guard; fix the producer. |
| The real check reached Codex | The historical session transcript has exactly three entries: liveness UserPrompt (11:47:39.885Z), AssistantText `ready` (11:47:42.989Z), and TurnEnd. No Check prompt is present. | A ready reply cannot qualify interpretation. The original incident's generic reason hid the actionable failure. |
| Guidance might exist only in Details / missing attachments | `StandingSpecialistProvisioner` writes and reconciles `spec.Contract` into `SystemPromptAppend`. `AgentSessionLaunchComposer.ComposeForAgentAsync` composes attachments, reply style and that append; Codex receives `CodexLaunchArgs.DeveloperInstructions(rendered)`. The current seat has empty Details and a complete v5 append. | No evidence of dropped instructions in this incident. Details is metadata, not a replacement instruction channel. Test the effective launch payload on both kinds, without demanding attachments. Historical launch payload was not independently captured here. |
| Changing the agent kind changes all specialist tasks | Legacy `SpecialistTaskRunner.CreateRunTaskAsync` leaves `AgentTask.AgentKind` at its ClaudeCode default and hardcodes Low. Only the newer deadline-policy path snapshots kind. `PlaceOnStandingAgentAsync` compares requested kind to the actual live session and rejects disagreement. | Snapshot the selected execution identity for every specialist task, regardless of deadline policy. Fix exact-model availability checks and generation races too. |
| Fixing kind alone enables a no-tool fallback | `FitBriefForTyping` applies `PtyDeliveryCeilings.ForAgentKind`; Codex's brief ceiling remains zero and produces a file/API pointer. The interpreter must not read either. The deny-all hook is a Claude `.claude/settings.json` mechanism, not Codex enforcement. | Two further capability gates are mandatory: full fact delivery without retrieval and provider-specific hard tool denial. Neither is proved by a successful launch. |
| Codex large input has never been measured | `CodexComposerCanaryTests` records LF preservation and whole 3,466/5,166-character bodies, but also settle/Enter problems. The production default remains conservative. | Reuse those fixtures; certify the current queue, CLI and backend combination. Historical body-length measurements alone do not justify raising all Codex ceilings. |
| Successful output is 3-5 lines | CARD-0339 is already reflected in contract v5: one physical reading line, at most 240 characters, followed by this Check task's done token. Task `9c2f102c` after the rollback contains a valid one-line reading. | Preserve v5. Do not restore the older format from the card/brief wording. |
| Existing warnings tell the operator | `RaiseUnavailableAsync` writes Warning incidents with a one-minute dedupe and optionally raises an alert. `AttentionService.BuildRecentIncidentItemsAsync` only selects Error or greater. No persistent interpreter outage state backs Attention. | Forty-two Warning incidents do not imply visible Attention. Add state-based projection, independent of incident retention and alert configuration. |
| A standing chain can use task routing pins unchanged | `RoutingPin.Role` excludes specialists. `RoutingCandidate`, resolved `RoutingCandidates.Candidate`, and `ComplexityRoutingService.WalkCandidatesAsync` already provide pair lists, ordered outcomes, availability and quota checks. | Reuse candidate/eligibility mechanics, but keep specialist configuration separate from card/stage pins. |
| CARD-0412 already supplies wake-up implementation | Its landed plan describes durable capacity waits, guarded automatic starts and shared admission pacing. No `CapacityRecovery*` implementation exists at this inspected HEAD; the card is in Review. | Integrate with the actual landed CARD-0412 implementation before recovery activation; do not copy its proposed subsystem into this card. |

Read-only evidence can be re-fetched using the environment API/task-token handling in [ops-http.md](../../ops-http.md): `GET /api/agent-tasks/7fa88286` (detail-level `failureReason` and events), `GET /api/sessions/ef59a3a3-539f-4b9c-bb57-00b0c6ae6833/transcript?since=0`, and `GET /api/agent-tasks/9c2f102c`. These are historical identifiers, never production targets in tests. The live interpreter was ClaudeCode/Low with exact `haiku` and session `48a88516-34c8-4782-89a0-94bfaf09c251` when read; that observation does not certify future running state.

## Decisions

These are settled design defaults; no operator answer blocks TestDesign.

| Decision | Choice and reason | Rejected alternative |
|---|---|---|
| D-1: scope | Reusable standing-specialist routing records, enabled for Check first. Diagnose/Distill retain their execution, budgets and output contracts, apart from the shared identity bug fix. | General fallback for every standing orchestrator/channel agent, or turning specialist roles into routing-pin stages. |
| D-2: declared chain | Persist an ordered list of complete `RoutingCandidate` kind/level pairs on a 1:1 `StandingSpecialistRouting` record owned by the interpreter agent. Null configuration preserves the existing exact primary. Maximum three unique pairs. Deployment target: `[ClaudeCode/Low, Codex/Low]`, with the alternate initially unqualified. | Silently append a guessed or more expensive model; equate an installed model with approval to route to it. |
| D-3: physical seats | Keep the current agent/session as the primary physical seat and create a managed standing seat for each alternate, with its own scratch directory and stable relation to the logical owner. Qualified alternates stay warm. | Mutate the primary's kind/model while a live task still owns its session, or kill a stalled session merely to free a slot. |
| D-4: reusable policy | Share candidate serialization, resolved outcomes and the existing ordered eligibility walk/hold reader. Extract a small common eligibility helper if necessary; do not apply role-policy defaults or card pin composition to specialists. | A second availability table, clearing holds to make failover work, or weakening explicit user/task pins. |
| D-5: qualification | Candidate selection requires supported transport and enforced no-tools capability, followed by a successful real interpretation qualification for the exact launch fingerprint. Validate every production reading too. | A liveness probe, stub response, nonempty Result, or `Succeeded` status as model qualification. |
| D-6: transport | Add a narrowly typed no-tool specialist input path that sends the full bounded fact bundle inline through the existing queue. Permit it only for a certified provider/backend/CLI envelope. Reject unsupported/oversized input, never substitute a pointer. | Globally increase Codex limits, allow the interpreter a file-read tool, strip facts silently, or introduce a second direct model API path. |
| D-7: deadlines | One 60-second end-to-end budget for a check's interpretation, including eligibility/queue time. At most two distinct candidate attempts; first gets at most 30 seconds when a second qualified seat is ready. Held candidates consume no model attempt. | Sixty seconds for each candidate, a cold launch in every check, or endless retries that block later checks. |
| D-8: failover/recovery | Use an already-qualified alternate immediately; retain the failed session's ownership. Recover held seats through CARD-0412 admission/guards and requalify before reuse. Prefer the primary again only after validated recovery, with a five-minute minimum active-candidate residence. | Resume an old failed Check, reset global capacity barriers locally, or oscillate on every check. |
| D-9: somebody is told | Durable Warning Attention while running on fallback; Error immediately when the chain is definitively unavailable, otherwise after three consecutive failed requests or five minutes after the first unresolved failure. | More per-minute Warnings, relying on a human recognizing digest prefixes, or a new chat/alert sink. |
| D-10: closure evidence | Only a valid interpretation for the current generation clears its failure streak. A real successful Check clears an outage; qualification alone marks `ready, awaiting successful check`. | Clear on start, ready, hold expiry, Attention acknowledgement, or stale late output. |

## Routing data and execution ownership

Use three bounded records rather than grow `AgentSupervisionState` with provider policy:

- `StandingSpecialistRouting`: unique `AgentId` of the logical owner, `CandidatesJson`, configuration revision/concurrency token and enabled state. Reuse enum-name JSON and `RoutingCandidate` validation; require both fields here even though task pins permit partial pairs. Reject duplicate/unsupported pairs and lists over three. A configuration update changes future admission, never an in-flight task's identity.
- `StandingSpecialistCandidateState`: unique logical owner + pair, physical agent id, resolved kind/level/alias, effective profile revision, session id + StartedAt generation, contract/launch/tool-policy/transport fingerprints, qualification evidence task ids, state/reason, next probe time and failure episode. Store references/stamps, never launch secrets. A change to alias resolution, profile, executable version, backend, composed instructions, tool policy or session generation invalidates qualification. Reordering an unchanged pair does not duplicate its physical seat.
- `StandingSpecialistHealth`: unique logical owner, active candidate, first unresolved failure/last valid real Check times, consecutive failed request count, last evidence/run ids, outage start/resolution and durable status. Add a request/attempt relation (separate table if needed) keyed by checked task + check number to claim attempts and a single winning result atomically. Health counts logical requests, not polling passes or failed candidates followed by success.

Keep logical ownership typed, e.g. a managed-candidate relation, rather than detecting slug prefixes. Provisioner, supervisor, startup notes, policy refresh, liveness and deletion must recognize managed specialist seats. No workspace-reading bootstrap or refocus compact may be sent to them. All remain non-pool agents; alternate tasks stay Role=Check, ReplyTo=None, no CardId, and never enter the worker cascade/check-check loop. Every process remains owned until the ordinary authorized stop/cleanup path ends it.

The primary preserves its current profile, exact model and working directory during compatibility rollout. Its configured head pair must match its kind/tier; an exact ModelId remains authoritative through `DispatchModelAlias.Resolve` and the normal launch resolver. Reject a contradictory head configuration. Alternate pairs resolve from the existing definition/model ladder; do not inherit a Claude profile, environment overrides or Herdr tab label into Codex. V1 supports registry-defined alternate launches in isolated PtyHost scratch seats. Custom alternate profiles or Herdr seats without explicit placement are rejected with an actionable validation reason, not silently relocated. This keeps the initial configuration API pair-compatible with CARD-0322.

Provide `GET/PUT /api/agents/{id}/specialist-routing` with fresh concurrency-token writes, configured candidates, effective identities, readiness and reasons. Restrict it to supported specialist owners. Add a small Agents settings section and a PowerShell helper (`scripts/specialist-routing.ps1`) accepting `Kind/Level,...`, with inspect/set/revalidate operations. Revalidate is an explicit bounded request, respects holds/human barriers, and cannot mark a candidate qualified directly. Saving a declared chain authorizes its bounded qualification work and listed models; it does not activate an unqualified route. Ordinary agent PATCH of a managed execution identity must invalidate qualification and be rejected while that seat has active work.

Alternates are warm standing seats only while their owner/feature/configuration authorizes them. Reconciliation must honor suspension, liveness latch, Herdr hold, authentication, global feature switches and model holds before starting anything. Removing/disabling a candidate stops admitting work, cancels only unclaimed queued probes/check attempts, and drains owned active work. Do not turn suspension into a hidden cleanup mechanism or let an AlwaysOn supervisor immediately resurrect a disabled alternate. Retire an idle removed seat through normal stop ownership rules; retain history. CheckInterpreterEnabled=false suppresses new probes, starts and outage alerts while preserving audit state.

## Capability and interpretation gates

### G-1: provider execution contract

Before a kind/backend combination is admitted, prove all of these through its actual launch composer and queue:

1. The effective instruction text contains the current interpreter bundle and per-task format reminder. Codex uses `developer_instructions`, not the inert `instructions` key. Details and attachment presence are irrelevant to this assertion. The launch has the requested tier/exact model, no competing model flag, and no workspace ritual.
2. The complete deterministic facts reach a transcript-confirmed UserPrompt with the current Check marker. Use LF, bracketed paste and a separate Enter through `SessionMessageQueueService`, with its established confirmation/retry rules. Test non-ASCII text, embedded task/report markers, head/middle/tail facts, near-limit input, CRLF normalization and warm reuse. A matching screen, Sent flag or TurnEnd without prompt evidence does not pass.
3. Tool execution is denied before it runs, including file reads, shell/edit tools, MCP and network/search. Claude's owned deny hook must fail qualification if it cannot be armed; the current log-and-continue workspace preparation is insufficient. Codex needs a verified provider-specific restrictive launch configuration and isolated specialist home/settings; never inherit a general coding profile with permission bypass and call it no-tools. Do not guess a Codex flag or claim read-only sandboxing disables reads. If the installed CLI cannot enforce the required tool policy, mark that candidate Unsupported and return the capability finding for implementation/design follow-up. It cannot become a live fallback.

Add an explicit specialist capability entry/transport policy beside the provider contract catalog, not a general `ForAgentKind` exception. Establish its maximum UTF-8 input size from current production-path canaries; no number is declared safe by this plan. The queue must carry this typed policy through enqueue and retries so both `FitBriefForTyping` and queue spill cannot replace the facts. Oversize is a typed `InputUnsupported` outcome before any submit. Keep the raw digest complete for the existing caller fallback. Unknown/inbox/Herdr combinations remain ineligible unless independently certified. Server/runner feature compatibility must fail closed where the implementation adds protocol fields.

The ordinary Codex delegate pointer policy stays as documented. A one-line encoded full-fact payload is acceptable only if the model/transport fixtures prove semantic preservation; it is not permission to discard evidence to fit a ceiling. No broad transport redesign is implied.

### G-2: model qualification before production selection

Use representative synthetic `DelegateCheckProbe.CheckFacts` through `CheckInterpretation.BuildGoal`, real Check task creation, standing dispatch, queue, transcript ingestion, report settlement and result validation. They must not publish caller notifications, create cards, or use a live broker. The suite covers doing/produced, stuck boot turn, ambiguous evidence, shared-checkout authorship, and settled-at-capture. Include a fixture where the checked delegate's brief is a pointer: the interpreter receives the facts inline and must correctly describe that pointer as delivery to the other delegate. Include instruction-looking text in the facts; it must remain data.

Release acceptance requires all five semantic fixtures on the real primary and real alternate, plus repeated checks in the same warm session and a fresh launch. Assert fixture-specific evidence and classification, current Check done token, one line <=240 characters after token extraction, no fabricated completion/authorship, and zero tool attempts. `ready`, empty output, copied fact dump, wrong token, blocked token or a valid-looking answer unrelated to the fixture fails. A fake provider proves plumbing only and cannot satisfy this gate.

Runtime qualification uses two small deterministic fact fixtures with fresh evidence nonces per new launch fingerprint. It records input/answer hashes, transcript sequence boundaries, settlement task ids, actual model/CLI/profile/backend, validation version and time. Qualifying requests are allowed onto the candidate's session, but production requests cannot select it until qualification passes; keep the two task purposes typed to avoid a circular gate. A runtime probe never recursively invokes fallback to qualify itself.

At most one qualification batch per candidate generation. A failed batch quarantines it; a hold-clear recovery, changed configuration/fingerprint or explicit revalidate can schedule one later batch. No periodic paid liveness loop. On a warm seat, a meaningful compaction/instruction-refresh boundary invalidates the behavioral qualification and schedules requalification at idle; do not force compaction to test production. Real CLI transport canaries use `RealCliStubEnv` and its dual-hit oracle where applicable; authenticated model tests use the dedicated test home and explicit headed opt-in under the testing owner. Never copy the user's credentials/home or use production runner 17204.

### G-3: each returned reading

Introduce a pure `CheckInterpretationValidator` after normal report-token extraction. Require the matching report/turn correlation, terminal success, one nonempty physical line <=240 characters, one of the four current prefixes, and no leftover report/task markers or generic ready response. Validate text before formatting or truncation; presentation truncation must not manufacture a pass. Preserve `Settled at capture` and legitimate evidence language; do not ban the word complete indiscriminately. Semantic correctness is proven by the fixture suite, not falsely guaranteed by the prefix parser.

Record typed outcomes: held/quota/auth unavailable, identity mismatch, input unsupported, delivery unconfirmed, expired before dispatch, timed out after dispatch, task failed (with original bounded FailureReason), empty, invalid reading and valid reading. Keep transport evidence distinct from model/output failures. This removes the current information loss behind `the interpretation failed` without logging credentials or full sensitive bundles.

## Selection, failure and recovery

Claim a logical interpretation request before candidate admission. Snapshot selected kind, tier, effective alias/exact model, physical seat, session generation and configuration revision onto its attempt/task. Apply this identity fix to both `RunAsync` and `RunWithPolicyAsync`; do not regress Distill's deadline behavior. Dispatcher admission rechecks actual session identity and generation. A conflict returns a typed refusal to the bounded chain; it must not silently retag a task already dispatched or remove `PlaceOnStandingAgentAsync`'s guard.

Walk declared candidates in order, filtering support, current qualification, holds (including kind wildcard and Manual), subscription gate, authentication, supervision barriers and session occupancy. Preserve the existing explicit internal primary quota-override policy only where already authorized; alternate selection defaults to the quota gate, with any override explicitly configured/audited. Failures of the availability reader are not evidence of availability. Model holds remain owned by `ModelAvailability`/API-error recovery; invalid output only quarantines the specialist candidate and never disables that model for the whole fleet.

Use one absolute request deadline (`CheckInterpreterWaitSeconds`, default 60) from before eligibility/provision work. Prepare/qualify alternates outside this request in a bounded supervisor reconciliation hook; the check worker remains free to drain. A known held primary is skipped before enqueue, allowing the same check to complete on the warm alternate. Unknown failure can consume the first 30-second attempt allocation if a second ready candidate exists; then attempt that second candidate with the remaining time. A single candidate can use the remaining full budget. At most two submitted attempts per logical request; a third configured candidate is future coverage, not an unbounded latency multiplier. Honor global concurrency/cost limits without inventing implicit expensive-tier escalation.

On expiry, atomically cancel only still-queued task/message work; queued rows need an execution deadline so they cannot start after the caller left. If dispatch won the race, leave the active session owned and mark that attempt expired for this request. Its eventual report settles its own task but cannot win the request, update the current candidate's health or emit a second caller note. At most one attempt result wins by compare-and-swap; preserve all attempt ids/costs for audit. A failed first candidate followed by a valid second result counts as a successful request with failover, not a consecutive outage failure.

Retrying the immutable, scrubbed fact snapshot on another no-tool seat is safe, bounded optional work. It is not another prompt to the timed-out session. Busy alone does not kill, fail or quarantine a model. Attempt a ready alternate when the preferred seat is occupied; if all are transiently busy, preserve the existing busy digest. Persist the oldest blocked/expired outstanding attempt so five minutes of starvation is visible even when new requests keep reporting Busy. No rewrite of the checked delegate's lifecycle, check backoff schedule or successful/deterministic digest wording is needed.

CARD-0412 owns hold expiry/clear, capacity-wait episodes, paced admission and guarded automatic starts/continuations. Register the affected specialist physical seat/current authorized pending qualification as its consumer. An available alternate is ordinary authorized Check execution; the held seat's later wake must use the shared admission gate. Coalesce startup liveness with the qualification task where possible; do not add a second continuation beside a capacity recovery prompt. An old expired Check is never the work to resume. If it remains an owned active task, let existing recovery settle it and discard the late result for caller/health purposes before qualification.

Before implementing that integration, re-read CARD-0412's actual landed symbols and record the base SHA. If only the plan exists, implement independent identity/gate/Attention slices first and keep automatic recovery activation pending. Do not add a private hold-expiry timer, fresh-start override or second resume budget. A specialist output/delivery failure not caused by capacity is quarantined once and requires explicit revalidation; retain Attention until real service recovery. This prevents replacing a sixteen-hour silent outage with a restart/spend loop.

## Durable Attention and operator behavior

The health reconciliation hook runs at least every 30 seconds in the existing supervisor loop and does no provider work inline. It projects one row per logical interpreter, with stable identity and these transitions:

| State | Trigger / surface | Exit |
|---|---|---|
| Healthy | Valid real Check on preferred candidate; no unresolved service failure | Failed request, failover or loss of qualifying candidates |
| UsingFallback | Primary held/quarantined, qualified alternate selected; durable Warning Attention names primary reason, active alternate and last valid interpretation | Preferred candidate requalified and a real Check succeeds there, or outage begins |
| Suspect | First unexplained failed request; persist first failure and counter immediately | Valid real Check, third failure, five-minute timer, or definitive unavailable chain |
| Unavailable | No eligible/qualifiable route because all candidates held/unsupported/quarantined, or three consecutive failed requests, or first unresolved failure/starvation age >=5 minutes; durable Error Attention | A real Check succeeds; qualified-only recovery remains visible as awaiting real Check |
| Disabled / intentionally suspended | Feature/operator intent prevents service; status/configuration visible, no new outage paging/counts | Re-enabled service requalifies; keep prior outage history |

Definitive exhaustion raises Attention on its first observation, even if only one check ran. A known primary hold with no qualified alternate must not wait for N more checks. Timer escalation after an unexplained failed request does not require another request. Short, healthy backlog does not start a failure episode; stuck backlog has its own evidence. Qualified standby status alone is never described as successful interpretation.

Persist state and its bounded incident/task audit in the same database transaction, then publish existing AgentChanged/AgentTaskChanged events for query invalidation. Attention is derived from the persistent state even with alerts disabled, server restart, a 24-hour lookback or deleted incidents. Exclude the transition incident from the generic recent-incident projection to prevent duplicate rows. Existing Attention badge/count and broken/suspect groups must include the new kind; provide OpenAgent and inspect-last-attempt navigation. No card move, alert sink or chat send is part of the design.

The row states: first failure/time unavailable, current candidate, original reason (e.g. kind mismatch), last successful real interpretation, consecutive failures, per-candidate skip/qualification reasons and next recovery eligibility. During fallback, example: `Check interpreter using Codex/Low; ClaudeCode/haiku held until ...; last valid check ...`. During outage: `Check interpretation unavailable since ...; 3 failed requests; no qualified alternate; inspect candidate qualification`. Acknowledge, if offered, marks seen only; it does not clear the outage, qualify a candidate, clear a provider hold or reset a human start barrier. Recovery writes one resolved audit event, rather than a new row on every tick.

## Implementation slices and test owners

| Slice | Files / responsibility | Tests |
|---|---|---|
| S1: identity and actionable failure | `SpecialistTaskRunner.cs`, `AgentTaskCheckService.cs`, `AgentTaskDispatcher.cs` (typed refusal only), attempt/DTO metadata; snapshot kind/tier/effective model and preserve underlying reason | `AgentTaskCheckInterpreterTests`, `SpecialistTaskRunnerDeadlineTests`, `AgentTaskStandingAgentDispatchTests`, `PinnedCodexProfileDispatchLaunchTests`; reproduce the stored ClaudeCode-task/Codex-session failure red then green; verify wrong-kind public pins still refuse |
| S2: input and tool capability | New specialist execution/input policy beside `ProviderContractCatalog.cs`; `AgentSessionLaunchComposer.cs`, `CodexLaunchArgs.cs` only for measured flags, `StandingSpecialistProvisioner.cs`, `SpecialistSpec.cs`, `AgentControlService.cs`, `SessionMessageQueueService.cs`, `AgentTaskDispatcher.cs`, queue metadata/protocol only if required | `NamedCodexAgentLaunchTests`, `AgentSystemPromptLaunchTests`, `CodexLaunchArgsTests`, new `SpecialistInputTransportTests` / `SpecialistToolPolicyTests`; existing `CodexComposerCanaryTests` and focused production-path canary; unsupported launch denies routing |
| S3: candidate persistence/control | New Domain routing/candidate/health/request records and typed enums; `AppDbContext.cs`, CLI-generated migrations, new concrete `StandingSpecialistRoutingService` and pure policy; shared eligibility extraction from `ComplexityRoutingService.cs` if needed; DTO/endpoints/helper | New `StandingSpecialistRoutingTests`, `StandingSpecialistRoutingHttpTests`, `SpecialistRoutingScriptTests`; regress `RoutingPinCandidateTests` / `RoutingPinCandidateDispatchTests`, `ModelAvailabilityManualTests`; validate concurrency, exact model and unchanged pin semantics |
| S4: qualify and execute chain | New concrete `SpecialistQualificationService`, `CheckInterpretationValidator`, `StandingSpecialistHealthService`; provisioner and runner/check-service integration; supervisor maintenance hook and DI | New `SpecialistQualificationTests`, `CheckInterpretationValidatorTests`, `CheckInterpreterFallbackTests`, `SpecialistAttemptConcurrencyTests`; regress check sweep/backlog, deadline, cancellation and format tests; full fake-backed check path |
| S5: visibility and recovery | `AttentionService.cs`, `AttentionDtos.cs`, incident enum; agent/attention API types, Agents settings, attention visuals/invalidation; actual CARD-0412 services at landed base, `AgentSupervisorService.cs` / `AgentControlService.cs` guards | New `SpecialistHealthAttentionTests`, `SpecialistCapacityRecoveryTests`; existing `AttentionServiceTests`, `HerdrSupervisionBackoffTests`, CARD-0412 acceptance classes; client `attentionVisuals.test.ts`, `AgentsPage.test.tsx`, relevant invalidation test |
| S6: qualification evidence and activation | Real-provider specialist contract canary using isolated runner/homes; configured chain rollout; update `docs/agent-kinds.md`, `docs/ops-http.md`, `docs/session-runtime-invariants.md`, `docs/antiphon-api.md`, configuration docs with measured values and recovery ownership | New explicit `CheckInterpreterContractCanaryTests` for primary and alternate; acceptance trace for primary cap -> valid alternate reading, and exhausted chain -> Attention; regression subset from S1-S5 |

S1 can land independently. S2 establishes whether Codex can meet the required no-tool and input contracts; unsupported capability is an explicit finding, never a reason to bypass a gate. S3-S5 can be tested with fake seats while capability evidence is gathered. S6 must not activate Codex or mark CARD-0415 implemented merely because its configuration/chain tests pass. If Codex cannot meet G-1/G-2, retain it as unsupported, return the concrete capability finding, and qualify a separately declared alternate before claiming the availability acceptance.

## Required TestDesign handoff

Append a separate `## Verification design` with V-n/R-n/PC-n, exact scoped commands, positive-control mutations, costs and observable oracles. This plan does not fold TestDesign or claim any tests have run.

1. Pin historical identity failure and current v5 format. Remove the producer identity fix as a positive control; require the real standing dispatcher guard to fail before any Check UserPrompt. Test both runner APIs and changed seat/session generation. A mocked successful Result is not this reproduction.
2. Prove instruction append reaches the model request independently from Details/attachments. Separately defeat the tool-denial configuration and demonstrate the negative-control attempt would execute in an isolated harness; the protected path must refuse it. If no Codex enforcement mechanism is available, record unsupported instead of weakening the oracle.
3. Prove input equivalence through the real queue and native transcript, including second-layer spill, oversize, stale/mismatched token, non-ASCII bytes and inherited startup notes. Removing the specialist input policy must reproduce the file-pointer incompatibility. Run the actual installed CLI/backend before certifying any envelope.
4. Prove a ready-only candidate, unrelated grammatical reading, wrong report marker, multiline/oversized text and tool-using answer cannot be selected. Real-model semantic fixtures must fail their expected-output assertions under a deliberately wrong response; stub plumbing results must never populate real-model qualification records.
5. With a qualified warm alternate and an injected primary provider-cap observation through existing recovery/availability services, a real Check returns the alternate's valid reading within the common budget and exactly one caller notification. Test hold present before request, hold arriving between selection/dispatch, new failure mid-run, both candidates held, wildcard/Manual holds, quota/auth refusal and availability-reader failure. No held provider call, silent extra candidate or hold mutation.
6. Use separate DbContexts/service instances to race request claim, candidate change, dispatch, expiry, late settlement, winner publication and restart. Observe exactly one winning result, bounded attempts/cost, correct health, cancellation only while queued, and continued ownership of active sessions. Test max backlog and global dispatch limits, including qualification starvation.
7. Drive three failures and the five-minute single-failure/starvation timer independently; definitive exhaustion must surface immediately. Inspect actual Attention API and badge invalidation. Prune incidents, restart the service and advance past 24 hours; the same unresolved row remains. A late old success, ready reply, revalidate and acknowledgement cannot clear it. Genuine current real Check recovery does.
8. At CARD-0412's landed base, exercise expiry and explicit clear with shared admission spacing, Stop/liveness/Herdr/auth barriers, queued old expired Check, same-session recovery and alternate drain. No extra resume, restart or budget beside its existing action. Verify feature off suspends automatic work, reenabling requalifies, and ordinary Diagnose/Distill behavior remains intact.
9. Run G-2 with real primary and alternate through production launch/delivery/settlement in an isolated environment, then inject the primary hold locally and obtain another valid alternate interpretation. Report fixture counts, per-model actual identities, failure reasons, generation/fingerprint evidence, zero tool attempts and latency. A cap need not be deliberately incurred at the provider. Record this acceptance as pending until it actually passes.

Use [testing-and-build.md](../../testing-and-build.md): TUnit via `dotnet run`, named-class filters and fresh nonzero TRX execution counts, isolated output ending `/`, no full-namespace substitute. Example after tests exist: `dotnet run --project tests/Antiphon.Tests --property:OutputPath=bin-c415/ -- --treenode-filter "/*/*/CheckInterpreterFallbackTests/*" --report-trx --report-trx-filename card0415-fallback.trx`. Use `pwsh -File scripts/test-client.ps1 attentionVisuals.test` for the client. Queue clocks must advance timers; scope DB assertions; global sweep fixtures require unkeyed NotInParallel. Process tests take the assembly-local ProcessSpawnLimit; run Antiphon.Tests and Antiphon.Agents.Pty.Tests sequentially. Program hosts use ProductionRunnerGuard/refusing clients or a random isolated runner. No test uses live messaging, clears production holds, starts another AppHost or touches a user's Codex home.

## Rollout and completion boundary

Land this plan before dispatching a sibling implementation worktree so it is present on the dispatch base. Implement/verify S1-S5, resolve CARD-0412 integration, then run G-1/G-2 and activate only a declared, qualified chain. Inspect actual loaded code/schema, current effective models, qualification evidence and the resulting reading/Attention after deployment through the canonical main-checkout runbook. A healthy HTTP endpoint or Running process is not acceptance.

Null routing configuration preserves the current primary, while the independently useful identity/failure-detail/Attention fixes remain. Rollback disables alternate admission and drains owned active work without deleting hold/health history. Keep the existing deterministic digest on all unsuccessful interpretation paths. Do not close CARD-0415 until both a real valid fallback interpretation and durable timely Attention on exhaustion have been demonstrated.

Plan validation: source/API consistency review and `git diff --check`; no builds, tests, paid probes, message sends, session control or deployment performed. No decision is pending. Next stage: TestDesign, with Codex tool-denial/transport feasibility as its first explicit gate.
