# CARD-0593: preserve channel inbound across a failed agent wake

Base inspected: `origin/master` and this worktree at `4e873cf20a61b0475f9a013b57168e6ecdd7aea5` (2026-09-26). This is an investigation and implementation plan; no production or test code changed here.

## Verified findings

1. `ChannelBridgeService` records the inbound catalog entry before routing (`server/Application/Services/ChannelBridgeService.cs:101-119`), attempts to start a bound agent once, and polls until the configured deadline (`:425-476`). A timeout returns without entering the debouncer or durable session queue (`:170-186`). The Warning alert at `:399-418` is the only timeout signal; Herdr and model holds already create Critical `ChannelReplyLost` incidents (`:136-168`, `:329-397`). `AgentStartTimeoutSeconds=90` and `AgentReadyDelaySeconds=12` remain current (`server/Application/Settings/ChannelBridgeSettings.cs:18-22`). A queued launch can return from `StartAsync` before Running (`server/Application/Services/AgentControlService.cs:180-181`), so the poll cannot simply be removed.
2. The consumer auto-commits offsets (`src/Antiphon.Messaging.Client/KafkaAntiphonMessagingConsumer.cs:22-55`), including while the handler is waiting for a wake; a crash before durable acceptance can therefore lose the message even if a journal is added. Even a forced Kafka replay is insufficient today: catalog upsert stores only the **last** native message ID and a 200-character preview (`server/Application/Services/ChatChannelService.cs:20,148-195`); the bridge skips that last ID at `ChannelBridgeService.cs:111-116`. Its catalog write is not an acceptance receipt for the full envelope. Older redeliveries may also miss this one-slot dedupe.
3. `SessionMessageQueueService.EnqueueCoreAsync` requires an existing `AgentSession` (`server/Application/Services/SessionMessageQueueService.cs:405-410,4847-4853`), but persists a `WhenIdle` Channel row before typing (`:641-675`) and leaves a non-Running row Pending (`:716-726`). Launch flushes pending rows after boot (`server/Application/Services/AgentSessionService.cs:628-634,866-879`); a new session carries never-attempted pending rows from the old session (`server/Application/Services/AgentControlService.cs:672-676,730-761`). These mechanisms help **after** a queue row exists, not when no session was reserved. The stranded sweep only selects AlwaysOn sessions or machine origins; Channel is excluded (`server/Application/Services/SessionMessageQueueService.cs:1397-1420`, `server/Application/Services/QueueAttention.cs:80-90`). A non-AlwaysOn idle Channel row needs a direct boot or turn-end flush, not this sweep.
4. The bridge catches only Herdr hold and `ModelDisabledException` (`ChannelBridgeService.cs:131-169`). Other start conflicts reach the outer consume-loop catch, delay 15 seconds and resubscribe (`:61-83`). The current start path can refuse specialist starts, continuity holds, launch ownership races, and runner drain (`server/Application/Services/AgentControlService.cs:164-179,515-537,624-635`). The card's exact `standing_resume_current_active` citation is stale: its current early branch (`:175-177`) requires explicit Fresh/resume, which the bridge does not request; the later reservation branch (`:534-537`) returns the same source for ordinary resume. Do not write a test that assumes this code is reachable from an ordinary channel wake; cover an actually reachable conflict and keep a generic conflict fallback.
5. The bridge calls `StartAsync` with its default `automatic=false` (`ChannelBridgeService.cs:465-469`; `AgentControlService.cs:148-149`). Manual starts clear suspension/liveness via `ClearSupervisionLatchAsync` (`AgentControlService.cs:206-211,730-746,1214-1239`). This is a real separate hazard. Preserve `IgnoreSubscriptionQuota:true` (`ChannelBridgeService.cs:466-468`; `AgentControlService.cs:214-224`), while channel wake uses automatic intent and honors `Suspended`, `LivenessLatchedAt`, `HerdrFailureHeldAt`, and `ContinuityHeldAt` (`AgentControlService.cs:170-179,541-548`). Automatic must still be allowed for ordinary non-AlwaysOn agents; only capacity recovery has the AlwaysOn prerequisite (`:183-190`).
6. The card's `ChannelBridgeService` and queue line numbers, and the specific early conflict example, predate current master; the behavior in findings 1-3 remains. Current `ChannelBridgeTests` still asserts Herdr-held inbound is **dropped** (`tests/Antiphon.Tests/Application/ChannelBridgeTests.cs:35-71`); that expectation must change with parking. Its provider-capacity notice says the message is kept (`:825-890`), currently contrary to the bridge's drop path. `HerdrAlwaysOnChannelParityTests` is a process-spawning Windows lane, so it is not a portable checkpoint for this Linux worktree. CARD-0484's timeout-cost note remains in `tests/Antiphon.Tests/slow-tests-allowlist.txt:80`.

## Smallest complete fix

### S1. Accept the full inbound envelope before wake

Add an agent-bound `ChannelInbound` durable row (entity, EF mapping, CLI-generated migration) keyed uniquely by `(provider, conversationId, nativeMessageId)`. Persist the complete normalized `ChannelMessage`, including attachment bytes and reply handle, and the bound `AgentId`/`ChatChannelId` snapshot. Commit it with the catalog upsert for every enabled, bound, deliverable inbound message **before** calling `EnsureAgentSessionAsync` or an in-memory debouncer. A duplicate finds the existing inbound row; never equate `LastChannelMessageId` with delivery. Keep disabled/unbound and empty-message routing policy as today. Preserve accepted rows across server restart. The catalog must not consume the only dedupe identity ahead of acceptance. Existing catalog metrics remain one count per native message.

Replace `EnableAutoCommit=true` with explicit per-record commit after this database transaction succeeds (or after a deliberate self/unbound/disabled/empty disposition). Extend the consumer's delivery API and fake with an acknowledgement handle carrying the consumed topic/partition/offset; commit through the Kafka consumer only after the bridge reports the durable disposition. On a database failure or cancellation before acceptance, do **not** commit; resubscribe/reconsume and let the unique inbound key make that replay harmless. A commit failure after acceptance may replay, also harmless. Define an explicit malformed-payload disposition so it cannot silently advance past an unrecorded valid envelope. The bridge should signal the durable wake worker after acceptance, then acknowledge Kafka promptly; the worker, not the consumer iterator, can spend the 90-second start window.

### S2. Wake and materialize exactly once

Keep one start attempt and the bounded Running poll, including the 12-second settle on a fresh start. Return early only when the launch has a concrete terminal failure; retain the poll while the launch queue owns a pending session. Use `automatic:true` with the existing quota override. Catch all `ConflictException` start refusals explicitly and classify known hold/race codes; park without trying another start while a deliberate hold or launch reservation exists. Unknown conflicts and unexpected failures retain the durable row and surface diagnostics, not an assumed drop.

Add a bounded, restart-safe pending-inbound drain: run after acceptance, on startup, and when a bound agent reaches Running (with a periodic backstop for missed wakeups). Select pending rows FIFO by agent; under a per-agent/row claim, materialize each into **one** Channel `WhenIdle` queue row on the current persistent session. Give the queue row a unique `SourceChannelInboundId`, so a crash between queue insert and inbound-state update reuses the same row. Store the formatted envelope and saved attachment paths in that row before any terminal write; attachment filenames derive from the inbound ID so replay does not create ambiguous files. Leave the inbound payload durable until the queue row owns the complete message; retain a tombstone identity after transfer for dedupe. The existing queue marker, conversation key, delivery attempts, restart transfer, and matching complete `UserPrompt` receipt remain authoritative. A queue insert or flush result is only acceptance/attempt, never proof of delivery. Do not automatically retype an attempted row on a different session without the queue's receipt/attempt checks; park it for a decision at the attempts cap. Avoid routing through `FlushStrandedQueuesAsync` for Channel rows on non-AlwaysOn agents.

The current debouncer buffers only in memory (`server/Application/Services/ChannelInboundDebouncer.cs:58-92,116-162`). Keep its coalescing behavior only as an optimization over accepted row IDs; a restart or callback failure must leave every member recoverable and preserve sender/conversation ordering. Ensure the old `FlushLaneAsync` catch at `ChannelBridgeService.cs:195-236` cannot turn a failed queue insert into a terminal drop.

### S3. Report failed wakes without claiming delivery

On a timeout, add a Critical `ChannelReplyLost` incident with a stable inbound-row/episode key and reason `ChannelWakeTimeout`, plus the existing Warning alert if useful; its text should say the message is **pending** and has not reached the agent. Apply the same durable park to Herdr, capacity, continuity, specialist, suspension/liveness, and other start refusals; preserve existing once-per-hold Critical behavior and provider notice, with truthful wording. Persist incident creation and its dedupe marker together so a restarted drain does not page repeatedly. A later complete matching `UserPrompt` is the only delivery verdict; incident history may remain as a record of delay. Update `docs/telegram.md` and `docs/session-runtime-invariants.md` when implementing.

## Verification design

New `ChannelInboundRecoveryTests` should provide at least eight independent `[Test]` methods:

| ID | Required outcome |
|---|---|
| V-1 | A stopped agent whose wake never reaches Running retains the complete inbound payload, no typed input, and exactly one Critical timeout incident. |
| V-2 | A new server/bridge instance drains that payload on a later Running session, including a non-AlwaysOn agent; verify the marked queue row and a complete matching `UserPrompt` before calling it delivered. |
| V-3 | Kafka acknowledgement is withheld before acceptance, committed after acceptance, and a replay after the queue insert yields one catalog count and one queue row per native message, including redelivery after a newer message. |
| V-4 | Two same-sender messages accepted inside the debounce window survive a restart/flush failure in order; their distinct IDs and full bodies remain recoverable. |
| V-5 | Herdr, capacity, continuity, specialist, suspension/liveness, and an unexpected conflict preserve the envelope; deliberate holds do not auto-start, and incident/notice dedupe holds. |
| V-6 | Channel wake retains `IgnoreSubscriptionQuota:true`, uses automatic intent, and never clears a supervision latch; an ordinary non-AlwaysOn agent still wakes. |
| V-7 | A queued launch that returns before Running is waited for, then delivers exactly once after boot; a failed launch does not spend the full poll window if an explicit terminal failure is already known. |
| V-8 | Transfer to a new persistent session and a failed/uncertain terminal write retain the original queue attempt and full marked body; a screen redraw or enqueue alone cannot settle delivery. |

R-1: the full existing `ChannelBridgeTests` class keeps ordinary inbound, attachments, batching, reply correlation, and the adapted held-agent behavior green. R-2: a new `KafkaInboundCommitTests` class pins manual Kafka commit and fake-consumer acknowledge behavior, including failed handling and replay; use an isolated broker fixture only if that is needed to observe an actual committed offset. Use a fake clock/runner for wake deadlines; do not wait on production 90+12 seconds. A new test must fail against the pre-fix path for the production condition it guards. No production broker or runner is required.

### Cost

The expanded Test design cost below supersedes the preliminary 48-minute ordinary / 60-minute positive-control estimate. Ordinary checkpoints total **42 minutes** (18 + 14 + 10); CP-2 reuses CP-1's build. Authoring, Mutation, and Review are separate costs.

### Checkpoints

Run this closed list once after S1-S3 are committed, with one isolated build per project for that slice group and one exact class filter per row. CP-2 uses the CARD-0585 `CP-1` build reference with the identical `After`; it does not rebuild. Counts are TUnit execution floors, not assertions, internal scenarios, or minutes. All expected executions must pass with zero skips. The importer derives class roster tokens from Filter; Expect remains descriptive.

| CP | After | Build | Group | Filter | Covers | Expect | Min | EstimatedMinutes |
|---|---|---|---|---|---|---|---:|---:|
| CP-1 | S1-S3 | `tests/Antiphon.Tests -> bin-c593-recovery/` | inbound-recovery | `/*/*/ChannelInboundRecoveryTests/*` | V-1–V-8 | all 8 new methods, 0 failed/skipped | 8 | 18 |
| CP-2 | S1-S3 | `CP-1` | bridge-regression | `/*/*/ChannelBridgeTests/*` | R-1 | all 34 methods / 40 argument-expanded results, 0 failed/skipped | 40 | 14 |
| CP-3 | S1-S3 | `tests/Antiphon.Messaging.Tests -> bin-c593-kafka/` | kafka-commit | `/*/*/KafkaInboundCommitTests/*` | R-2, V-3 | both named K-1/K-2 methods, 0 failed/skipped | 2 | 10 |

## Risks and decisions for TestDesign

- The journal adds storage for attachment bytes (bus maximum is much larger than catalog preview). Bound payload size, retention after confirmed transfer, and cleanup must be explicit; never delete the only complete copy before queue ownership commits.
- Queue transfer and journal state must be idempotent across commit ambiguity. A unique inbound-to-queue key is required even if a process-local lock exists.
- Manual Kafka commit changes the `IAntiphonMessagingConsumer` public API and fake. Update the public API inventory and cover rebalances/commit exceptions; never acknowledge a valid record before its full envelope is durable.
- A fresh session can differ from the originally attempted session. Preserve queue attempt floors and refuse duplicate terminal writes until transcript evidence resolves uncertainty; do not claim a mere Running status, buffer redraw, or `SentAt` is receipt.
- Keep the one-start/90-second poll for launch-queue ownership; only shorten it when a concrete terminal launch failure is observed. A blanket retry can defeat Herdr/continuity/suspension holds or start concurrent sessions.
- CARD-0519 covers the outbound mirror. Coordinate any touched `ChannelBridgeTests` timeout setup with CARD-0484; avoid using Windows-only Herdr process tests as this plan's Linux checkpoint.
## Test design

Design base: `d68203aafcef1554605a995e5cb3509eb711e433`; owned branch `feat/card-task-3e89af3c`. This section specifies tests to implement in Code. No implementation, ordinary test result, or executed positive control is claimed here. The three checkpoint IDs, slice group, projects and exact filters above remain; build reuse, the source-derived CP-2 count and its saved build cost are corrected.

### Inspection and required setup

| Bodies/fixtures inspected | Boundary and use |
|---|---|
| `tests/Antiphon.Tests/Application/ChannelBridgeTests.cs`: held cases, inbound/catalog, dedupe, batching, attachments and private harness | R-1: 34 test declarations, two four-argument methods = 40 results. Local harness has no automatic transcript receipt and expects synchronous routing. |
| `tests/Antiphon.Tests/TestHelpers/BridgeQueueHarness.cs`, `TestDbFixture.cs`, `tests/Antiphon.Tests/Agents/FakeAgentProtocolAdapter.cs` | V-1..V-8: real PostgreSQL/queue, fake runner/adapter; attach-existing-session, preserve-database, clock and EF-interceptor hooks. Default OnSubmitted inserts a receipt: disable it for negative cases. Default factory throws on launch: replace it with a controlled offline factory. |
| `tests/Antiphon.Tests/Application/ChannelPromptCorrelationTests.cs`: runtime replay, common-head/different-tail and attempt-floor setup; `ChannelBatchingTests.cs` | V-2/V-4/V-8: receipt ingestion and real queue batching. References, not additional checkpoint filters. |
| `server/Application/Services/ChannelBridgeService.cs`, `AgentControlService.cs` start/transfer paths, `StandingQueueSwitchPolicy.cs`, `SessionMessageQueueService.cs` enqueue/batch/attempt paths, `server/Domain/Entities/SessionQueuedMessage.cs` | Acceptance, holds and attempt preservation. The transfer query reads Pending rows, then NeverAttempted refuses a switch with any attempted input (`standing_resume_delivery_pending`). V-8 protects this existing behavior; V-3 ensures replay cannot bypass it with a new row. |
| `src/Antiphon.Messaging.Client/KafkaAntiphonMessagingConsumer.cs`, `src/Antiphon.Messaging.Client.Testing/FakeAntiphonMessagingClient.cs`, `src/Antiphon.Messaging/ChannelMessage.cs` | V-3/K-1/K-2: current consumer has no acknowledgement or injectable construction; fake dequeues without an acceptance ledger. |
| `tests/Antiphon.Messaging.Tests/Gateway/KafkaConsumerGroupObservationTests.cs`, messaging test project | K-2: Redpanda dependency and admin offset readback. New fixture owns its broker and does not copy the existing opt-in skip. |
| `tools/Antiphon.Checkpoints/Manifest/PlanTableImporter.cs`, `ManifestValidator.cs`, `tests/Antiphon.Tests/Checkpoints/CheckpointImportTests.cs` | Literal CP-1 is supported reuse; identical After obeys CARD-0585. |

New `ChannelInboundRecoveryTests` is Integration, serialized with the MessageQueue group, with eight independent methods. Use a cloned migrated PostgreSQL database from `CreateIsolatedSchemaAsync`, route every DbContext to that connection, and scope counts by agent/channel/inbound IDs. A test-owned inbox outlives both provider instances. Preserve database/files across a simulated crash and abandon buffers without calling the bridge's graceful StopAsync flush. Recreate the entire DI graph, runtime, debouncer and worker; carry no cached service, queued task or copied in-memory envelope into recovery.

Inject FakeTimeProvider, controlled offline runner/adapter factory, and launch/worker barriers. Advance 2-second polls, the configured 90-second deadline, 12-second settle, debounce and backstop only after the relevant barrier. Negative assertions wait for a completed drain iteration, not a short sleep. Known holds use real AgentControlService and launch ownership; an external-I/O fault seam may throw an unknown ConflictException/unexpected exception. Do not fake the bridge's decision. EF interceptors target the acceptance/queue/incident transaction boundary, not setup queries. Code must wire the new acceptance service, worker triggers, source-key queue insertion and injectable Kafka IConsumer construction; a test-only recovery algorithm is insufficient.

Keep CARD-0484's compressed delivery settings or use injected time; preserve attempt counts and receipt predicates. Positive OnSubmitted takes the actual captured bytes through EmptyRunnerClient.SetTranscript and AgentSessionRuntime.SyncTranscriptAsync, following ChannelPromptCorrelationTests, with native timestamp and increasing sequence. Never preinsert expected text. With session-state projection enabled, ingest through runtime instead of SQL behind a warmed projection. This fake proves server processing of a recipient transcript, not a native provider's terminal/normalizer.

S2 batching clarification: every native ID has exactly one durable queue owner. Same-sender coalesced members may share that owner only if **all member mappings commit atomically**; replay of a non-head member must find it. SourceChannelInboundId on only the head is insufficient. Preserve R-1's one-envelope/one-prompt assertion. V-3 disables debounce and asserts one physical row per native message; V-4 asserts one physical batch owner plus every member's durable mapping. This clarifies the ownership invariant without permitting a batching regression.

### Delivery inventory and crash cuts

| Durable join | Producer -> destination | Persistence/recovery | Observable receipt and tests |
|---|---|---|---|
| Kafka topic/partition/offset/assignment -> provider/conversation/native ID | Consumer -> bridge acceptance | Full envelope and catalog commit with captured AgentId/ChatChannelId; DB fault/cancel replays. Ack promptly before slow wake. Lost commit response replays same identity. | Independent DbContext sees full row at ack (V-3); broker committed-next-offset readback (K-2). Neither proves session delivery. |
| Inbound ID -> queue owner/member IDs | Worker/debounce hint -> real WhenIdle queue on persistent session | Cuts after acceptance before signal, after signal before drain, before queue commit, after queue commit before journal update, after update before flush. Independently test startup, Running event and periodic recovery; concurrent drains reuse owner. | V-2 eligible and busy recipients, V-3/V-4 recovered inputs: complete matching UserPrompt on the intended session beyond original attempt floor. Busy queue has zero writes until runtime TurnEnd. |
| Queue ID + original session/attempt/generation | Queue flush/boot/turn-end -> adapter -> transcript | Untouched transfer preserves owner/body. Attempted/uncertain rows cannot reset evidence or authorize cross-session retyping. Cuts before body, after body before Enter, after Enter before transcript, after transcript before final save. | V-8: late complete UserPrompt confirms without another body write. Queue status, SentAt, screen redraw, submit ack and Herdr events never discharge delivery. |
| Inbound/hold episode -> incident; notice episode -> captured chat | Failed wake -> incident store/capacity notice producer | Incident and dedupe stamp commit together; restart does not repage. Notice failure leaves inbound recoverable. | V-1/V-5: exact Critical ChannelReplyLost/reason/pending text; notice producer observes destination/handle/count. Producer acceptance does not prove Telegram display or agent input. |

Keep cleanup conservative: no age-based removal of pending envelopes or tombstones in this slice. Release bytes only after queue ownership includes full formatted body and successfully saved stable attachment paths. Preserve broker-admitted payloads up to the existing bus cap; over-limit/malformed input needs an explicit non-delivery disposition, never truncation into an accepted valid message. No retention scheduler is introduced.

### Proves it works now

Every V method below lives in **`tests/Antiphon.Tests/Application/ChannelInboundRecoveryTests.cs`** and runs in **CP-1**. Each is one TUnit execution; named scenario loops do not increase the count. Every row identifies a behavioral red and whether it is guard-only. For pre-fix comparisons keep new API/schema compiling and restore the old production behavior; missing types/tables or broken fixtures are not red evidence.

| ID / exact method / checkpoint | Arrangement and exact assertions | Guard-only and production defect that turns it red |
|---|---|---|
| V-1 `C593_WakeTimeout_PreservesEnvelopeAndCriticalIncident` / CP-1 | Bound stopped agent with no persistent session; launch never reaches Running. Payload has >200-character multiline body/distinct tail, attachment bytes/metadata, Raw JSON, mentions, reply-to and reply handle. Fresh DbContext sees one pending row with field-for-field envelope and captured bindings; zero queue rows, body writes and receipt. Exactly one agent/episode incident is ChannelReplyLost/Critical/ChannelWakeTimeout with pending/not-reached text. Repeated drain/restart keeps one. Fail incident commit: no lone dedupe stamp; retry creates exactly one. | **No.** Return on failed wake before acceptance gives zero retained envelopes; Warning-only timeout gives zero Critical incidents. PCs 1/2/42/43. |
| V-2 `C593_Restart_DrainsToEligibleAndBusyRecipients` / CP-1 | Accept during failed wake; crash before signal; new graph, later Running non-AlwaysOn recipient. Startup-only, Running-event-only, periodic-only scenarios hold other triggers. Eligible recipient gets one marked owner and one complete receipt. Busy recipient has one Pending owner and zero writes until TurnEnd, then one receipt. Rebind catalog after acceptance: original captured agent receives input; replacement gets zero. Attachment bytes equal source, paths remain under inbox and identical across retry, no extra files. | **No.** Remove one recovery trigger, restrict drain to AlwaysOn, or route using current catalog binding: receipt missing/wrong recipient. PCs 17..19/27/37/46. |
| V-3 `C593_AcceptanceAckAndQueueCommitCuts_AreIdempotent` / CP-1 | Drive hosted consumer. Block acceptance commit: independent read sees no row/count increment and ack=0. DB failure/cancel leaves both unchanged and replayable. Release commit with wake held: ack=1 and full envelope visible while wake is incomplete. Lose commit response; replay M1, accept M2, replay M1 with new normalized Id: catalog delta=2, two inbound identities and one queue owner each. Same native ID in different provider/conversation remains distinct. Competing drains have max active materialization owners=1. Crash after queue commit before journal update: same queue ID/body/marker, one write/receipt per message. Adapter callback independently reads committed full owner before typing. Direct duplicate native/queue-source inserts fail database constraints; distinct keys succeed. Replay after payload release preserves count/tombstone. | **No.** Ack before commit/on failure, await wake before ack, use last-ID-only dedupe, remove durable claim/unique source key, release sole payload early, or create a new queue row on replay. PCs 3..8/23..26. |
| V-4 `C593_DebounceCrash_RetainsEveryMemberInOrder` / CP-1 | Same-sender M1/M2 inside debounce window have distinct native IDs, full tails/attachments. Crash without StopAsync; separately fail queue commit in flush. Both complete envelopes survive. New drain creates one owner plus two durable member mappings and one complete prompt: one header, M1 before M2, both tails. Replay non-head M2 after newer input creates no owner/write. Different sender/same conversation and same sender/different conversation stay outside the lane. Equal timestamps still preserve acceptance order. More than one worker page: one tick claims at most configured bound, later ticks drain remainder. | **No.** Memory-only buffering, swallowed flush loss, head-only mapping, wrong sort, unbounded scan or lane merge fails retained-member/order/count assertions. PCs 20..22/56/57. |
| V-5 `C593_HoldsAndConflicts_ParkWithoutRerouting` / CP-1 | Independent cases: Herdr, provider capacity, continuity, specialist, Suspended, LivenessLatchedAt, reachable ownership race/draining runner refusal, unknown ConflictException, unexpected runner exception. Every case keeps full envelope; zero input/reroute and unchanged hold. Deliberate holds create zero new sessions/adapter starts. Two messages plus restarted drain retain one Critical incident per Herdr/capacity episode; capacity notice goes once to captured chat/reply handle and says kept. Explicitly clear/recreate hold: one additional episode incident/notice. Notice failure preserves inbound. Generic failures retain diagnostic reason. | **No.** Current guards already refuse some starts but discard input. Restore drop/catch bypass or remove a hold/dedupe predicate. Do not pretend the early standing_resume_current_active branch is reachable from ordinary channel wake. PCs 28..34/44/45/58. |
| V-6 `C593_ChannelWake_IsAutomaticWithQuotaOverride` / CP-1 | Real start policy, exhausted quota, eligible AlwaysOn=false agent: capture automatic=true, IgnoreSubscriptionQuota=true, CapacityRecovery=false; one launch and complete receipt. Held variants preserve Suspended and exact liveness/Herdr/continuity timestamps, zero launch. Eligible agent with prior supervision failure counters retains them after automatic acceptance. | **No.** automatic=false clears supervision state; missing quota override refuses; AlwaysOn prerequisite strands ordinary agent. PCs 35..37. |
| V-7 `C593_QueuedLaunch_WaitsOnceAndRecognizesTerminalFailure` / CP-1 | Queue owns Created/Starting session; StartAsync returns early. Multiple signals still yield one start admission/adapter launch and zero input while not Running. At Running, fresh input waits fake 12-second settle, then one complete receipt; already Running control has no fresh settle. Explicit terminal failure at first poll parks before full 90-second deadline (within one 2-second poll). Owned pending control keeps waiting. | **No.** Existing poll/settle subcases are guards; terminal-failure short-circuit is new. Repeated start, Created-as-Running, skipped settle or full wait after terminal failure breaks respective assertion. PCs 38..41. |
| V-8 `C593_TransferAndUncertainWrite_RequireCompleteRecipientReceipt` / CP-1 | Create Channel rows through real queue EnqueueAsync (this guard fixture does not require new inbound schema). In separate real control-start cases, untouched row preserves queue ID/full marked body/files and transfers/delivers once; attempted row refuses switch with `standing_resume_delivery_pending`, leaving persistent-session pointer and original session/attempt identity, DeliveryAttempts, baseline/time/generation unchanged, with zero new-session writes. Inject each terminal/save cut in inventory then recreate graph. At cap, pending for decision and zero extra writes. Delayed actual receipt confirms without retyping. Independent false receipts: enqueue/SentAt alone, redraw, Herdr event, wrong kind/session/marker, same 200-character head/different tail, pre-baseline sequence, stale native Timestamp/fresh CreatedAt, wrong generation. All leave receipt unconfirmed/reply unsettled and full body retained. Only complete matching UserPrompt on intended recipient past attempt floor confirms. | **Yes: guard-only**, expected to pass before and after this fix through existing queue/control behavior. It must still go red under a compiling production mutation: bypass NeverAttempted refusal, cap or one receipt predicate. V-2/V-3 supply new-ingress and source-owner coverage. PCs 47..55. |

### Kafka controls and regression amendments

V-3 also runs four deliberate-disposition cases through the hosted consumer: self, unbound, bound-but-disabled, and bound text-and-attachments-empty. Each acknowledges exactly once, creates zero deliverable inbound/queue rows and zero writes. Self creates no catalog row; the other cases preserve current discovery/count behavior. K-1 observes their explicit acknowledgement contract, while V-3 proves actual routing-policy outcomes. V-8's wrong-kind case supplies an otherwise identical, eligible `AssistantText` row and requires it to remain unconfirmed.

**R-2** is **`tests/Antiphon.Messaging.Tests/KafkaInboundCommitTests.cs`**, exactly two independent methods in **CP-3**:

| ID / exact method / checkpoint | Exact assertions | Guard-only / production defect |
|---|---|---|
| K-1 `C593_ManualCommit_UsesAcceptedRecordAndAssignment` / CP-3 | Inject recording IConsumer at production construction and capture actual config: EnableAutoCommit=false; stored offsets cannot authorize progress (disable auto storage if using that API). A=(topicA,p0,41), B=(topicA,p1,7): yield/exception/cancel/Close commit nothing. A ack commits only (topicA,p0,next=42), never B/current position. Duplicate ack is idempotent. Revoked assignment handle cannot commit. Commit exception propagates/leaves replayable record, never acknowledged success. Same no-ack/replay/ack/failure contract runs against FakeAntiphonMessagingClient. Malformed JSON/null/missing identity has an explicit diagnostic disposition: no valid envelope/commit until deliberately disposed. | **No.** Auto-commit, wrong/current offset, stale assignment acceptance, swallowed commit failure, silent poison skip, or fake destructive dequeue fails the corresponding observation. PCs 9..16. |
| K-2 `C593_BrokerRestart_ReplaysOnlyUnacknowledgedOffsets` / CP-3 | Mandatory owned Redpanda, unique topic/group, two partitions, actual KafkaAntiphonMessagingConsumer. Withhold p0 n ack then close/restart: broker committed-next-offset stays n (or unset for first record); restart yields n. Accepting p0 n+1 cannot pass unresolved n while p1 may progress. After explicit acceptance admin reads n+1; restart resumes next record. Lost commit response may replay same identity. Poison at n cannot silently advance past n to valid n+1; deliberate disposition permits progress. | **No.** Auto-commit, committing beyond unresolved predecessor or wrong next-offset breaks actual broker readback/replay. PCs 9/10/11/14. No environment opt-in or skip; unavailable Docker fails CP-3. |

K-1 proves production configuration/commit calls and assignment fencing; K-2 proves broker behavior; V-3 proves acknowledgement follows a full PostgreSQL commit. A callback fired by the broker test does not establish database durability. Clients use only their owned broker address, never live configuration. No agent/provider child or production runner is launched.

**R-1 / CP-2** retains all ChannelBridgeTests methods. Required amendments (never relax equalities to >= or delete a behavior assertion):

| Existing test/setup | Amendment and assertions preserved |
|---|---|
| `Herdr_held_agent_inbound_is_dropped_with_one_incident_and_no_start_notice_or_reroute` | Rename dropped -> parked. Replace exact obsolete dropped alert text with exact pending/not-delivered text and add two complete inbound rows. Keep zero new sessions/input/notice/reroute, zero reply correlations while held, one Critical HerdrSupervisionHeld incident and unchanged hold timestamp. |
| `Held_agent_without_fallback_sends_notice_and_does_not_throw` | Await completed worker hold iteration; keep single notice, both text assertions, no input and zero correlations. Add full durable envelope and Critical severity: “Your message is kept” now has a storage witness. |
| `Rapid_fire_same_sender_inbound_merges_within_window` | Await all three acceptances then advance/await debounce drain. Keep exactly one SubmittedBodies item, one envelope header, ordered `line one\nline two\nline three` and one reply correlation. Add all source-member mappings. Three prompts is not an acceptable amendment. |
| `Redelivered_message_is_not_routed_twice`, `Bridge_enqueues_with_channel_origin_and_conversation_key`, `Bound_channel_routes_the_message_into_the_agent_session`, attachments and turn/reply cases | Register new acceptance/worker dependencies and await this input's handoff barrier instead of relying on synchronous HandleInboundAsync wake. Keep exact counts, content, origin/key, bytes/paths and replies. For delivery claims runtime-ingest captured submitted bytes as UserPrompt; no assert-only-on-enqueue substitute. |
| Attachment-only/captioned/metadata-only/sanitized-name cases | Inbound-ID prefix may change filename, but preserve safe basename, inbox containment, bytes and visible could-not-import note; no loosening to any temp file. |
| First/unbound discovery, self echo, empty input, disabled behavior | Keep catalog/policy assertions and zero routing. Disposition remains observable when acceptance returns. V-3/K-1 cover deliberate acknowledgement. |

Constructor/interface changes must compile all bridge call sites and consumer implementations (including fake/samples); update public API inventory. Preserve a compatible entry point where feasible. These are build requirements, not permission to widen checkpoint filters. No other behavioral assertions are intentionally invalidated. Newly discovered incompatibilities require a named amendment and a reason before additional test runs. Keep CARD-0484 timeout fixtures; no timeout increase or Windows-only replacement.

### Guard inventory and positive controls

Each G-n maps to exactly one PC-n and one compiling production mutation. V-n/K-n below selects the **exact file and method above**, never its whole class; e.g. PC-1 is `/*/*/ChannelInboundRecoveryTests/C593_WakeTimeout_PreservesEnvelopeAndCriticalIncident`. R-1 selectors name the exact method. New-worker symbols are implementation-owned, so sites name the S1/S2/S3 operation instead of fictitious line numbers.

Code runs ordinary V/R; Review judges the pending PC design; SourceLanding Mutation executes fixed-green -> compiling-defect/intended-red -> restore/fresh-build/green after publication. Retain nonzero executed-method counts and exact assertion failures for every PC. Do not mutate tests, fake outcomes or assertions. Schema controls recreate the isolated migrated database after mutation/restoration. Shared production methods require separate cycles. Compile/fixture errors or zero-test runs do not kill a control.

| Guard -> control | Compiling production defect | Exact method selector / intended assertion failure |
|---|---|---|
| G-1 -> PC-1: S1 accept before wake | Move acceptance after successful EnsureAgentSession. | V-1: retained inbound count 0 instead of 1. |
| G-2 -> PC-2: S1 full envelope | Persist catalog preview as Text instead of full body. | V-1: full text/tail equality fails; ordinary case also checks every normalized field and attachment. |
| G-3 -> PC-3: S1 native-key uniqueness | Remove unique provider/conversation/native-ID constraint from model/generated migration. | V-3: direct duplicate insert succeeds instead of unique violation. |
| G-4 -> PC-4: S1 historical replay/count | Use LastChannelMessageId alone before incrementing catalog metric. | V-3: M1,M2,M1 count delta 3 instead of 2. |
| G-5 -> PC-5: S2 tombstone lifetime | Delete native identity after payload transfer. | V-3: released M1 replay creates another owner/count. |
| G-6 -> PC-6: S1 committed acceptance before ack | Ack before transaction CommitAsync. | V-3: ack=1 while independent reader sees no full envelope. |
| G-7 -> PC-7: S1 prompt acknowledgement | Await slow wake before ack. | V-3: ack=0 after commit while wake barrier is held. |
| G-8 -> PC-8: S1 failed acceptance cannot ack | Ack in finally after DB exception/cancel. | V-3: failed/canceled acceptance advances ack without row. |
| G-9 -> PC-9: S1 manual Kafka progress | Set EnableAutoCommit=true. | K-1: captured production config is true instead of false. |
| G-10 -> PC-10: S1 exact next offset | Commit consumed offset instead of offset+1. | K-1: A commits 41 instead of 42. |
| G-11 -> PC-11: S1 unresolved prefix | Permit next-record acknowledgement to commit past unaccepted predecessor in same partition. | K-2: broker committed-next-offset exceeds unresolved n. |
| G-12 -> PC-12: S1 assignment ownership | Remove assignment-generation ack fence. | K-1: revoked handle issues Commit instead of refusal. |
| G-13 -> PC-13: S1 commit-failure replay | Swallow commit exception and mark acknowledged. | K-1: failed commit is successful/non-replayable. |
| G-14 -> PC-14: S1 explicit poison disposition | Auto-ack malformed/null/invalid-identity without diagnostic disposition. | K-1: commit occurs with disposition count 0. |
| G-15 -> PC-15: S1 ignored-message routing policy | Treat self-authored envelope as ordinary inbound. | R-1 `Own_bot_messages_are_ignored`: channel created for self chat. V-3/K-1 additionally cover unbound/disabled/empty dispositions. |
| G-16 -> PC-16: S1 fake parity | Permanently dequeue unacknowledged fake record. | K-1: recreated consumer cannot replay its native identity. |
| G-17 -> PC-17: S2 startup recovery | Skip startup drain. | V-2 startup-only: receipt count 0. |
| G-18 -> PC-18: S2 Running recovery | Drop bound-agent Running trigger. | V-2 event-only: receipt count 0 while backstop held. |
| G-19 -> PC-19: S2 periodic recovery | Omit periodic pending scan. | V-2 periodic-only: receipt count 0 after scheduled tick. |
| G-20 -> PC-20: S2 bounded drain | Remove claim query page limit. | V-4: iteration claims more than configured limit. |
| G-21 -> PC-21: S2 FIFO | Sort by native ID descending instead of acceptance order. | V-4: M2 occurs before M1 in complete captured prompt. |
| G-22 -> PC-22: S2 batch membership | Persist mapping only for batch head. | V-4: M2 has no durable owner; replay adds prompt. |
| G-23 -> PC-23: S2 exclusive materialization | Bypass durable claim, keeping unique queue constraint. | V-3: concurrent protected materializations max >1; uniqueness cannot mask duplicate file/launch side effects. |
| G-24 -> PC-24: S2 unique queue owner | Remove queue-source/membership unique constraint from model/generated migration. | V-3: direct second owner for same inbound succeeds. |
| G-25 -> PC-25: S2 committed queue before typing | Flush/type before queue ownership commit. | V-3: adapter's independent DB read sees no committed complete marked owner. |
| G-26 -> PC-26: S2 sole payload retention | Clear inbound bytes before queue/files own full envelope. | V-3: queue-commit fault leaves neither source nor queue with full bytes. |
| G-27 -> PC-27: S2 stable attachment paths | Use random filename on every recovery. | V-2: retry path/file inventory differs. |
| G-28 -> PC-28: S2 Herdr hold | Ignore HerdrFailureHeldAt at channel/start admission. | V-5 Herdr: new session/start instead of zero. |
| G-29 -> PC-29: S2 capacity hold | Ignore model-availability refusal for wake. | V-5 capacity: launch instead of held envelope. |
| G-30 -> PC-30: S2 continuity hold | Ignore ContinuityHeldAt at channel/start admission. | V-5 continuity: session/start count increases. |
| G-31 -> PC-31: S2 specialist hold | Bypass reachable specialist start refusal. | V-5 specialist: forbidden seat starts. |
| G-32 -> PC-32: S2 suspension hold | Omit Suspended predicate for automatic wake. | V-5 suspension: start count nonzero. |
| G-33 -> PC-33: S2 liveness hold | Omit LivenessLatchedAt predicate for automatic wake. | V-5 liveness: start count nonzero. |
| G-34 -> PC-34: S2 unknown failure retains input | Delete/terminally dispose inbound in generic error/conflict catch. | V-5 unknown conflict/unexpected exception: full retained envelope missing. |
| G-35 -> PC-35: S2 automatic intent | Start with automatic=false. | V-6: intent/state-preservation assertion fails. |
| G-36 -> PC-36: S2 quota override | Set IgnoreSubscriptionQuota=false. | V-6: quota-exhausted ordinary agent gets no launch/receipt. |
| G-37 -> PC-37: S2 non-AlwaysOn eligibility | Require AlwaysOn for all channel wake/drain. | V-6: ordinary eligible agent receives zero input. |
| G-38 -> PC-38: S2 one wake owner | Reset startAttempted each poll and bypass in-flight wake owner. | V-7: start admissions >1 while launch pending. |
| G-39 -> PC-39: S2 readiness | Treat Created/Starting as Running. | V-7: materialization/flush occurs before Running. |
| G-40 -> PC-40: S2 fresh settle | Remove AgentReadyDelaySeconds delay. | V-7: body arrives before fake settle boundary. |
| G-41 -> PC-41: S2 known terminal failure | Keep polling known failed launch until deadline. | V-7: still waiting more than one poll after explicit failure. |
| G-42 -> PC-42: S3 Critical timeout parity | Record timeout at Warning only. | V-1: ChannelReplyLost/Critical/ChannelWakeTimeout assertion fails. |
| G-43 -> PC-43: S3 atomic incident dedupe | Commit dedupe stamp before incident transaction. | V-1: incident fault/restart leaves 0 incidents instead of 1. |
| G-44 -> PC-44: S3 once-per-hold incident | Omit episode dedupe before incident insertion. | V-5: same episode produces >1 Critical incident. |
| G-45 -> PC-45: S3 capacity notice dedupe | Omit notice episode guard. | V-5: same-episode notice count >1. |
| G-46 -> PC-46: S1 captured recipient | Resolve agent from current catalog during drain. | V-2: original receives zero; rebound replacement receives input. |
| G-47 -> PC-47: S2 attempted ownership preservation | Bypass the pending.Any(!StandingQueueSwitchPolicy.NeverAttempted) switch refusal in AgentControlService. | V-8: expected standing_resume_delivery_pending is absent; pointer/attempt session changes or new session gets duplicate body. |
| G-48 -> PC-48: S2 attempts cap | Admit capped attempts as fresh retries. | V-8: attempt/body count increases instead of parking. |
| G-49 -> PC-49: S2 transcript authority | Treat screen/submit/Herdr evidence alone as delivered inbound. | V-8: obligation closes with complete UserPrompt count 0. |
| G-50 -> PC-50: S2 recipient identity | Remove receipt session predicate. | V-8: wrong-session receipt settles intended recipient. |
| G-51 -> PC-51: S2 queue marker identity | Ignore outer marker in receipt comparison. | V-8: wrong-marker identical body settles. |
| G-52 -> PC-52: S2 complete body | Match first 200 body characters only. | V-8: same-head/different-tail or truncated receipt settles. |
| G-53 -> PC-53: S2 sequence floor | Admit receipt sequence at/below baseline. | V-8: old matching prompt settles later attempt. |
| G-54 -> PC-54: S2 native time floor | Use fresh CreatedAt instead of native Timestamp when baseline null. | V-8: old native prompt imported now settles. |
| G-55 -> PC-55: S2 generation floor | Remove original attempt-generation time check. | V-8: prior-generation receipt settles despite otherwise eligible sequence/time. |
| G-56 -> PC-56: S2 failed flush recovery | Mark members completed when flush callback throws. | V-4: failed-commit members never recover to receipt. |
| G-57 -> PC-57: S2 debounce lane isolation | Key debounce by agent alone. | V-4: foreign sender/conversation joins prompt. |
| G-58 -> PC-58: S3 truthful incident wording | Render timeout input as delivered/dropped instead of pending. | V-1: pending/not-reached text assertion fails. |
| G-59 -> PC-59: S1 unbound disposition | Return from unbound policy without acknowledging its deliberate disposition. | V-3: unbound case ack count 0 instead of 1 despite completed catalog write. |
| G-60 -> PC-60: S1 disabled channel guard | Treat a disabled bound channel as enabled during acceptance. | V-3: disabled case creates deliverable inbound/queue row or writes instead of zero. |
| G-61 -> PC-61: S1 empty-message guard | Admit text-and-attachments-empty input as deliverable. | V-3: empty case creates a deliverable row/prompt instead of zero. |
| G-62 -> PC-62: S2 UserPrompt kind | Admit AssistantText as a submitted prompt in receipt lookup. | V-8: otherwise identical eligible AssistantText settles without any complete UserPrompt. |

Inventory: **62 guards, 62 distinct PCs, missing=0, duplicate PC maps=0**. Each PC is one planned mutation cycle, even when the exact method runs several internal scenarios. If worker/start policy duplicate the same hold predicate, change all its implementations for that predicate's PC while retaining unrelated guards; otherwise redundancy masks detection. Record touched locations. Additional independently bypassable guards found during Code require a new PC and updated cost before Review, not silent credit under an existing control.

### Out of scope and limits

No real Telegram/Slack account, production broker, live runner, paid provider launch, desktop restart, native Windows Herdr qualification or outbound-reply redesign. Existing LF/bracketed-paste/separate-Enter shape and reply formatting remain owned by their suites; this change reuses the queue. Synthetic recipient transcripts prove portable server behavior, not native provider qualification. Kafka is at least once: accepted replay is expected and deduped. Notice producer acceptance cannot prove external chat display; externally ambiguous notice sends do not justify deleting inbound.

### Cost and Code handoff

Estimates, not measured results:

- Ordinary V/R floor **42 minutes**: CP-1 **18**, CP-2 **14** using CP-1's output, CP-3 **10** including owned broker. Execution floors **8 + 40 + 2 = 50**. All named internal scenarios are mandatory; they do not increase execution counts.
- Code authoring/setup/migration allowance **300 minutes** for missing seams, eight recovery methods, two Kafka methods and fixture amendments. Ordinary Code total **342 minutes** before Review. Generate migration through the CLI under the build-slot gate.
- PC floor **374 minutes**: **61 method-scoped cycles x 6 minutes + PC-11 broker cycle x 8 minutes**. Each includes fixed green, compiling mutation, intended red, restore, fresh isolated build and green. Mutation discovery/evidence allowance **30 minutes**: commission at least **404 minutes**. PCs remain pending after ordinary Code/Review.
- Code plus Mutation **746 minutes** (342 + 404); Review/repair/cold pulls additional. Expanded inventory replaces the preliminary ten-control estimate, not a claim 62 controls fit in 60 minutes. CP-2 reuse saves one estimated **6-minute** build: ordinary 48 -> 42. No broader/native suite savings claimed.

After committing S1-S3, run this unchanged table once for the slice group. The bootstrap invocation uses the slot wrapper; checkpoint drivers take their own slots:

```powershell
pwsh -NoProfile -File scripts/build-slot.ps1 -Label card-0593-checkpoints -- dotnet run --project tools/Antiphon.Checkpoints -- run --plan docs/superpowers/plans/2026-09-26-card-0593-plan.md --after S1-S3
```

If exit 75, use `dotnet run --no-build --project tools/Antiphon.Checkpoints -- wait --run <returned-run-directory> --max-wait 570s` until terminal for that same run. Exit 4 is a slot timeout to report, not permission to bypass the gate. Report a CHECKPOINT line per CP with actual expanded counts, evidence paths and reruns/reasons. The sole authoritative `### Checkpoints` table remains above in Verification design; do not add a second table or substitute filters. No ordinary production/test execution is requested in this documentation stage.

TestDesign validation: one isolated, leased **tool-only** build (`tools/Antiphon.Checkpoints`, `bin-c593-design/`) succeeded with 0 warnings/0 errors, solely to validate the authored manifest. Import and ManifestLoader validation accepted 3 rows, 2 builds, CP-2 reuse, 50 minimum executions and 42 estimated ordinary minutes; the guard audit found 62 one-to-one mappings. Import emits an advisory that CP-1's derived 54-minute timeout exceeds its 45-minute warning threshold; it accepts and retains 54 in the manifest. This is not a skipped row or a test result. No production/test build, V/R or PC was executed.

--- next stage ---
next: code
handoff: Implement S1-S3 with V-1..V-8, K-1/K-2 and R-1 amendments; preserve durable batch membership and attempted-session ownership. Run the three exact checkpoint rows once after the committed slice group, report counts, and leave 62 method-scoped PCs pending for post-land Mutation.
artifact: docs/superpowers/plans/2026-09-26-card-0593-plan.md
