# CARD-0514: prevent remote-control management-menu wedges

Date: 2026-09-13. Stage: Plan, task `af7dc5ee`. Source inspected: `a57a8fc2` on `feat/card-task-af7dc5ee`. Implementation and executable verification remain outstanding. Next stage: **TestDesign**, separately commissioned; this brief did not fold TestDesign into Plan.

## Outcome and evidence boundary

Make automatic `/remote-control` arming a generation-bound maintenance operation whose preconditions are checked immediately before typing. Never send it to a bridge already marked armed. Independently observe the exact management menu, persist its episode and recovery receipt, and surface it even when the transcript still says working. Automatically dismiss only after fresh idle evidence; a working-session modal requires operator intervention and remains visible in Attention.

The confirmed occurrence is session `0d64706d-4d60-4009-92f7-40557d7cb491`, 2026-09-13, no later than 16:47:44Z until the manual `KilledByRequest` stop at 16:55:25Z. Transcript #121 started the internal fork; #126 was its queued completion notification; #127-129 were the three queued operator inputs. None became a normal prompt before the stop. The retained PTY contains the Remote Control / Disconnect / Show QR / Continue menu. The paint has **no wall-clock timestamp**. `RcReArmed` at 16:37:02Z and the 16:43:23Z policy-refresh relaunch are context, not proof that either specific operation opened the retained menu.

Evidence read in full: `C:\src\Antiphon\.git\antiphon\investigations\2026-09-13-card-0514-orchestrator-stuck.md`; CARD-0514 description and revisions 1-4; CARD-0292 description and revisions 1-7, via `scripts/card.ps1 get/history`. That external investigation must remain available to TestDesign. Do not copy the production transcript or remote conversation URL into source fixtures.

[CARD-0292's plan](2026-09-01-card-0292-remote-control-wedge.md) is the predecessor, not an unimplemented proposal. Its card records shipment as `c7f7816c`. Its resume probe, matcher, launch dismissal, queue housekeeping, generic watchdog and health-menu check are present in this source. This plan changes the gaps below; it does not repeat its transcript-normalization work. CARD-0354 additionally introduced local-command delivery and cancellation of leftover RC commands. CARD-0509's restart loop and CARD-0512's missing Bash result remain separate.

## Ground truth

Line references identify this inspected revision; method names are the durable navigation anchors.

| Card assumption or tempting inference | What this code/evidence actually does | Consequence for this plan |
|---|---|---|
| The earlier wedge fix was absent. | CARD-0292 is Done and the implementation is present. `RemoteControlMenuScreen.IsPresent` matches Disconnect + Esc literals. | Extend the shipped protection; preserve its housekeeping and delivery rules. |
| A bridge flag proves a currently healthy connection. | `IRcBridgeProbe.cs` explicitly defines `Armed` as **ever armed** in the child process's state file; `BridgeConnections` is a separate inference. `StateFileFound=false` includes unreadable/absent files. | Armed forbids another automatic arm command even at zero connections. Unknown never authorizes typing. |
| Every launch probes before arming. | `AgentSessionService.SendRemoteControlCommandsAsync` (1993+) probes only Resume/Continue; fresh launches skip the probe. `WaitForResumeBridgeArmedAsync` (2263+) maps missing PID/probe/error to false, then sends. | Share an Armed/Unarmed/Unknown preflight across fresh, resumed and health-request sends. Preserve best-effort launch. |
| A health rearm is delivered and verified when `RcReArmed` is recorded. | `WatchRcAsync` (180+) records this on **enqueue**. `SessionHealthActions.EnqueueWhenIdleAsync` uses default Ui origin and returns no operation identity. | Give automatic requests explicit origin, generation and result; enqueue is only Requested. |
| Checking once after scheduling checks the menu produced by the command. | `ReArmSettleUntilUtc` starts at enqueue. Only the dead-bridge arm sets `PendingMenuCheck`; never-armed requests do not. The latch clears **before** the check, is process-memory only, and can run before WhenIdle delivery. State is keyed by session ID, which survives relaunch. | Remove the one-shot latch as recovery authority. Scan independently of request timing and reset health streaks on accepted-generation change. |
| A successful menu action proves the menu disappeared. | `SessionHealthActions.TryDismissRemoteControlMenuAsync` catches up the transcript, refuses while working, sends Esc, then returns true without a post-read. Launch's separate helper does post-read, but allows two Escs. | Replace bool with typed observation/action results; persist intent before one Esc and require post-read proof. |
| The generic watchdog catches this mid-fork wedge. | `QueuedInputWatchdogService.EvaluateSessionAsync` returns while `IsWorkingAsync` is true. It uses the **latest** enqueue and a 3-minute age gate. Sequence movement/spinner output does not fix that. | Exact-modal detection runs regardless of working state, output movement, or queue age. Preserve the generic working gate for ordinary long turns. |
| Nothing noticed any queue stall in this session. | The investigation records an existing `QueuedInputNeverConverted` at 16:43:13Z for enqueue #95. This predates the later three inputs and retained modal paint. | Do not erase or reinterpret that receipt as proof of the later injection. New receipts must identify their own observations. |
| All local-command sends use prompt confirmation and overlay recovery. | `SessionMessageQueueService.DeliverAsync` (2480+ local-command branch) sends declared no-UserPrompt commands via `TypeLocalCommandAsync`, then returns before generic overlay recovery. `/remote-control` is such a command (`ProviderContractCatalog`, 87+). | Automatic RC needs its own arm/menu outcome. Never invent a UserPrompt receipt for a local command or send extra Enter to seek one. |
| Successful boot always removes old requests. | `CancelPendingRemoteControlAsync` (756+) is called only after successful rename/setup; it cancels all Pending rows by first command token, regardless of provenance or previous attempts. Degraded launch can leave them behind. | Resolve automatic requests when bridge evidence is known, independent of rename. Preserve attempted-input evidence and explicit operator intent. |
| A quiet RC failure is detection-only. | `WatchRcAsync` still queues into armed sessions and ultimately calls `KillSessionAsync` after rearm attempts. Its idle inference is output silence, not fresh transcript idle. | Replace this RC-only arm/restart ladder with degradation and Attention. Do not change other supervisor recovery causes. |
| A server-side generation check makes a later raw write safe. | List DTOs carry `AcceptedStartedAt`; snapshots do not. `/sessions/{id}/input` takes only `Input` and looks up the current session at execution. `KillGenerationAsync` already demonstrates the runner's launch-lock/equality pattern. | Add guarded maintenance-input support and generation-bearing snapshots. No fall back to unguarded input on an older runner. |
| The headed canary proves swallowed input recovers. | `ClaudeRemoteControlMenuCanaryTests.One_Esc_dismisses_the_remote_control_management_menu` may skip if no bridge/menu appears and only types a token after Esc; it never submits that token. | A skip or composer paint is not acceptance. TestDesign must require actual UserPrompt conversion in an isolated canary. |

## Decisions

These are the design choices authorized by this brief's instruction to decide safe suppression/dismissal. No caller answer is needed to proceed to TestDesign.

**D-1 — Suppress an automatic arm whenever the current child is already armed.** Apply this both at scheduling and immediately before the write, on every launch mode. Positive current menu evidence also suppresses arming, but does not certify connection health. Explicit Unarmed means a readable current-child state file with no bridge ID; missing PID/file, probe exception, unknown generation or unavailable guarded transport means Unknown. Unknown withholds automatic RC input, records degradation and leaves the session available for its work. Keep the existing bounded startup probe window/setup budget; a fresh launch may use the same short window rather than paying the entire setup timeout.

Rejected: treating zero TCP connections or a read failure as permission to rearm; a resume-only check; lengthening sleeps to mask the race. The CLI can still arm between probe and submit: detection/dismissal remains an independent backstop, not a claim of atomicity with Claude's own bridge.

**D-2 — Automatic arming is identified maintenance, delivered at most once after an ambiguous write.** Keep WhenIdle scheduling for a positively unarmed established session, including requests scheduled while it is busy. Stamp a dedicated maintenance discriminator and the accepted generation on the queue row, with Supervision origin for health requests. Recheck generation, current bridge state, pending modal and fresh transcript idle under the existing per-session queue lock immediately before bytes. Startup uses the same RC policy/executor under launch ownership and the same delivery exclusion; it must not recursively enqueue or acquire a lock it already holds.

An automatic RC attempt uses the existing composer-evidence contract and **one submitting Enter**. It does not use the generic three-whole-submit boot retry or generic interrupted-message retyping. Persist `SubmissionStarted` before the first possible write. After a timeout/crash with possible bytes, reconcile bridge/menu evidence and retain `ArmUnconfirmed`; never infer that retry is safe from missing evidence. One unresolved/possibly written request blocks another automatic arm in that generation. A positively refused, zero-byte conditional write may be reconsidered at the next ordinary watch pass. `/rename` retains its own existing behavior and only runs after armed evidence plus a clear menu.

Use the same durable maintenance-row reservation for boot (System origin) and health (Supervision origin). Reserve/coalesce under the queue lock with a database uniqueness constraint for the active automatic arm of a generation, then execute through a helper whose caller already owns that lock. Health cannot execute while launch ownership is active or the session is Stopping; a Running status alone is insufficient because current launch code stamps Running before RC setup finishes. Preserve a terminal possibly-written result as a generation-level rearm veto even after the active-row slot closes.

Rejected: bare Ui-origin command rows, timing recovery from enqueue, classifying a local command as a delivered work prompt, or using a new general-purpose job subsystem. Request rows already provide ordering, persistence and queue ownership; RC-specific outcomes must stay distinct from `DeliveryVerdict.Delivered`.

**D-3 — A stale armed connection produces degradation, not an RC-only process restart.** Delete the armed/zero-connection rearm and `RcRestart` kill branches from health repair. Preserve the observation thresholds for connection warnings and healthy-reset behavior. Never-armed scheduling remains, subject to D-2. Keep old enum values and historical incidents readable; deprecate the RC restart-attempt setting rather than repurposing it. No change to operator Stop, genuine process-exit recovery, policy refresh, or other independently justified supervisor actions.

Reason: opening a known management dialog cannot repair a bridge merely because TCP inference reports zero, and monitoring is not the session's purpose. Rejected: automated Disconnect/reconnect, restarting to clear a modal, or routing the modal through a generic delivery-kill ladder.

**D-4 — Detect the exact modal regardless of working/idle and regardless of who opened it.** Run a passive rendered-screen sweep for live RC-capable sessions, including card sessions and non-AlwaysOn sessions. It must not depend on `PendingMenuCheck`, a prior health request, `RemoteControlEnabled`, queued-message origin, an idle output sequence, or the generic watchdog's age gate. Use the existing watch cadence (60 seconds by default) with bounded runner reads; observe at startup and before/after an automatic arm as well. RC connection-watch disablement must not silently disable modal observation; give the modal watch a separate enabled setting, default true. Global supervision shutdown still stops background work.

Strengthen the shared matcher using a coherent ordered block: Remote Control heading, Disconnect row, Show QR row, Continue choice and documented Esc footer. Whitespace/border/selected-row normalization is permitted; arbitrary prose containing the words, old scrollback, help text and partial paints are not active-menu proof. Keep a weaker `HasRemnant` result separate from actionable `IsPresent`: a partial remnant cannot authorize Esc or certify a clean composer. Use rendered grids, not substring matches against append-only ANSI history. Full-screen passive observations record first-observed time, never invented paint time.

Rejected: globally removing the generic watchdog's working gate, interpreting a spinner as progress, or matching any dialog heading. A legitimate long turn with queued input and no modal remains healthy under the existing generic detector.

**D-5 — Automatic Esc is narrowly idle-gated and has a receipt.** A live modal produces an immediate Warning episode (Critical when channel-bound), even with an in-flight fork or no queued input. Detection never waits three minutes. Attempt automatic dismissal only after acquiring the queue delivery lock, pulling the transcript with `CatchUpTranscriptAsync`, proving it bound/current and idle, and obtaining a fresh actionable modal snapshot from the same accepted generation. A matching generation/sequence conditional write sends exactly one Esc with `trackManualTurn: false`. Never Enter, navigation or a work-body replay. The runner rejects stale generation/sequence; output changes cause re-observation, not blind retries.

For working, unavailable transcript, stale/partial screen or unsupported conditional transport, record the reason and keep the episode open for the next passive observation. In particular, the recorded mid-fork CARD-0514 shape is **detected and surfaced, not automatically interrupted**. A fresh observation after a later TurnEnd may become eligible. If one Esc might have been sent, no second automatic Esc in that episode, including after server restart. Re-read after the existing 500ms settle, bounded by the existing 3-second dismissal budget; require two clear observations separated by a settle interval, the same generation, and no menu/remnant before recording verified dismissal. A read failure or deadline leaves `DismissUnverified`, not success. No disappearance claim from write acknowledgment alone.

Rejected: removing the working guard because the footer says Esc, assuming Esc is harmless during an active model turn, or clearing the episode's latch before checking. The existing idle-composer canary does not establish busy-turn Esc safety. Future busy dismissal would require a separately approved design and provider measurement.

**D-6 — Persist modal episodes separately from generic queue symptoms.** Add a small `RemoteControlModalEpisode` entity, with one open episode per `(SessionId, AcceptedStartedAt)` enforced by a unique filtered index. It contains an immutable episode ID, first/last observation UTC, child PID, before/after output sequence, transcript catch-up watermark/working observation, optional related maintenance queue ID, dismissal intent/sent/verified times and typed result, and resolution/time. Associate a maintenance request only when actually known; otherwise source is Unknown. No inference tying the September 13 paint to a specific request.

Commit detection and Esc intent before side effects. Persist/update the episode and emit linked `AgentIncident` receipts only on meaningful transitions, using `FailureReason` as a versioned episode reference, not free-form state parsing. Expose typed evidence in the existing incident/session detail path. Keep prompt bodies, full screens, bridge URLs, state-file contents and credentials out of these receipts. Unknown/failed post-observation is explicit. Open episode state is not lost when ordinary incident retention prunes history; Attention reads the durable episode without the recent-incidents `since` cutoff. Resolved episodes follow normal configured retention.

Modal clear and input delivery are **different outcomes**. Persist `DismissedVerified`, `ObservedClear` (no attributable automatic Esc) or `GenerationEnded` as appropriate. None means queued input was delivered. Record observed enqueue/drain/prompt sequence watermarks for diagnosis. A queue dequeue/remove or unrelated prompt may show movement, but cannot certify each of the three inputs. Existing per-message identity/completeness confirmation remains authoritative; no new synthetic `UserPrompt`, TurnEnd or successful task receipt.

**D-7 — Preserve queued work while a modal is known open.** Integrate an open-modal barrier under the queue lock before ordinary local commands, Now/WhenIdle delivery, boot work prompts, Enter retries, and failure cleanup can touch this generation. Catch up transcript and permit existing late confirmation first. Known open or dismissal-unverified episodes withhold additional automated bytes, publish a blocked reason, and do not consume ordinary delivery attempts, park as a delivery failure, or kill. The passive screen watch and operator raw terminal access remain available to clear it. Once a fresh observation proves clear, release the modal barrier and use existing late-confirm-before-redelivery rules. Do not automatically resend inputs held inside Claude's own queue; they have no new Antiphon queue identity to replay.

If RC setup merely fails without positive modal evidence, retain CARD-0056's best-effort launch/work behavior. If a modal is positively known and remains open, keep the process alive but explicitly defer the work prompt and skip rename, rather than reporting the TUI usable. An unrelated task's normal deadline may still report incomplete work; modal detection itself is not kill authority.

For the direct card-run boot path, persist the deferred original prompt once with its RunAttempt and accepted-generation identity before yielding launch ownership. Resume its delivery/first-output observation through that durable continuation after modal clearance; do not run the existing first-output timeout before any work was submitted, return a successful run, or lose the prompt in a local variable. Reuse the existing queue/launch continuation machinery and add the narrow missing linkage if necessary. A server restart while deferred must recover that same work identity exactly once. Interactive launch notes already routed through the queue keep their identities and simply wait at the barrier.

**D-8 — Respect generation identity at the runner input boundary.** Add generation-bearing snapshot metadata and a capability-advertised conditional maintenance-input operation carrying accepted generation and expected screen sequence. Resolve and write the same session object under the runner's existing launch gate, analogous to `KillGenerationAsync`; return Written, StaleObservation, GenerationMismatch, Missing/Exited, or Unsupported distinctly. A lost HTTP response is Unknown. All automatic RC command/body/Enter and Esc writes use the guarded path; each phase stops on mismatch. Do not extend the scope into rewriting ordinary human raw input.

Older runners may ignore additive input fields, so a field on the old request alone is insufficient. Advertise a distinct capability and route/contract; no fallback to `/input`. A compatible server with an older runner continues passive detection and records monitoring degradation while withholding automated RC. Preserve UTC microsecond-normalized `AcceptedStartedAt` equality, not runner `StartedAt`, wall-clock tolerances or PID identity alone.

An old-runner screen without generation metadata is explicitly `ObservationGenerationUnproven`: retain a diagnostic against the expected DB generation with the observed generation null, without claiming a current-generation match or authorizing Esc. A later compatible observation must establish identity afresh. On proven generation replacement, close the old episode as GenerationEnded; no old intent, barrier or successful receipt transfers to the replacement.

## Implementation slices

Commit each meaningful slice with its actual validation outcome. S1 precedes side-effect adoption in S2/S3; S4 completes user-visible behavior; S5 supplies native acceptance and documentation. Each slice names required coverage for TestDesign to turn into exact methods, assertions and later positive controls. This is not a claim that these tests already exist or passed.

### S1 — Shared observation, durable identity and guarded runner I/O

Files:

- `server/Application/Services/RemoteControlMenuScreen.cs`: coherent modal and remnant classification, shared by launch/watch/recovery.
- New `server/Domain/Entities/RemoteControlModalEpisode.cs`, RC-specific enum file(s) under `server/Domain/Enums/`, and `server/Infrastructure/Data/AppDbContext.cs`: D-6 entity, constraints and indexes.
- `server/Domain/Entities/SessionQueuedMessage.cs`: explicit maintenance discriminator, accepted-generation stamp, RC result/evidence fields. Use the row ID as request ID. Keep ordinary origins/delivery verdicts intact.
- CLI-generated migration under `server/Migrations/` plus generated model snapshot. Do not hand-write a migration or modify generated `docs/cards/`.
- `src/Antiphon.SessionRunner.Contracts/SessionRunnerContracts.cs` and a focused new conditional-input contract file; `src/Antiphon.SessionRunner/SessionRunnerRuntime.cs`, `Program.cs` and capability advertisement; mirrored `server/Application/Dtos/SessionRunnerDtos.cs`, `ISessionRunnerClient.cs`, `IAgentProtocolAdapter.cs`, `server/Infrastructure/Agents/SessionRunner/SessionRunnerHttpClient.cs`, `RunnerTerminalSession.cs`, `RunnerClaudeAdapter.cs`, and runtime dispatch in `AgentSessionRuntime.cs`.

Tests: extend `RemoteControlMenuScreenTests`, `RunnerSessionGenerationTests`, `SessionRunnerGenerationWireTests`, `RunnerTerminalSessionGenerationTests`; new `RemoteControlModalPersistenceTests`. Include the sanitized 0292 and 0514 menu shapes, partial/ghost/prose negatives, matching-generation input, replacement between observation and send, unknown generation, old-runner capability refusal, lost response, one-open-episode uniqueness and restart reload. All mismatch cases assert **zero input bytes**, not only a returned refusal.

### S2 — Guard both automatic arm senders at execution

Files: `AgentSessionService.cs`, `SessionHealthService.cs`, `SessionMessageQueueService.cs` (prefer an RC-focused partial), `server/Infrastructure/Supervision/SessionHealthActions.cs`, `server/Application/Interfaces/ISessionHealthActions.cs`, `server/Application/Settings/SupervisionSettings.cs`; new concrete `server/Application/Services/RemoteControlRecoveryService.cs` for shared RC policy/coordination. Composition in `server/Program.cs`; scoped database work follows the existing service patterns, without a service-interface/repository layer.

- Replace health's generic `EnqueueWhenIdleAsync("/remote-control")` with a typed, deduplicated maintenance request returning its queue ID. Start the settle clock from actual execution, not queue insertion. Remove armed-session rearm/restart and `PendingMenuCheck`.
- Preflight immediately before startup and queued execution. Re-read kind/capability, accepted generation, PID/bridge and open-modal state. Fresh startup gets the probe too. Unknown cannot fall through to a send. Reset in-memory health streaks on generation change.
- Separate Requested, SuppressedAlreadyArmed, SupersededGeneration, WithheldUnknown, SubmissionStarted, ArmedObserved and ArmUnconfirmed. Record `RcReArmed` only for an observed unarmed-to-armed result associated with this attempt; use an honest suppressed/already-armed outcome for a pre-existing bridge. Monitoring health remains a separate fact.
- Route maintenance rows outside ordinary boot retry, batch coalescing, generic late-delivery success, interrupted-attempt retyping and delivery-failure kill. Guard SendNow/amend paths too: they must not turn a stamped maintenance request into an unguarded body. Reuse the queue's lock and composer encoding; do not introduce a second independent input lock or recursively acquire `GetLock`.
- Resolve known automatic pending requests as soon as bridge evidence is obtained, even if rename later fails. Do not use `CancelPendingRemoteControlAsync`'s broad first-token cancellation against attempted or operator-authored rows.

Rollout of old rows: pre-existing health requests are indistinguishable from explicit Ui commands. Do not fabricate origin/generation from body text. At migration/first reconciliation, mark existing unresolved rows whose first command token is exactly `/remote-control` as **legacy-unclassified**, preserve body, attempts and baselines, and withhold their automatic delivery with an actionable queue reason. An already attempted row is reconciled, never automatically retyped. A user may explicitly cancel or reissue it; new automatic requests use the typed path. Other queued bodies remain unchanged. TestDesign must pin this migration and every bypass, including an old row surviving a failed rename/relaunch.

Tests: update `SessionHealthTests` (old restart/menu-latch expectations intentionally change), `AgentSessionLaunchFailureTests` (fresh/resume Armed/Unarmed/Unknown; RC-only retries change, work-prompt retry stays), `AgentRemoteControlGateTests`, `ProviderContractCatalogTests`; add `RemoteControlMaintenanceQueueTests`. Cover unarmed-at-enqueue/armed-at-delivery, request held through a long turn, duplicated health ticks, rename failure, policy refresh with same SessionId/new generation, server crash after intent/write, missing state file with zero connections on an idle session, and no RC-driven kills. Inspect actual production `SessionHealthActions`, not only its current bool-returning fake.

### S3 — Continuous observation and verified idle dismissal

Files: new `server/Application/Services/RemoteControlModalWatchService.cs`; `RemoteControlRecoveryService.cs`, `SessionHealthActions.cs`, `ISessionHealthActions.cs`, `AgentSessionService.cs`; `server/Infrastructure/Supervision/SessionHealthHostedService.cs`, settings and DI registration. Keep modal observation independent of RC connection repair enablement and schedule. Bound each runner read and continue the sweep after a non-shutdown timeout; do not interpret missing snapshots as empty screens.

The sweep reads the runner inventory once and observes all live capable candidates, irrespective of output movement or the generic watchdog. It updates durable episodes and invokes the idle-gated dismissal coordinator. Startup preflight/postflight and the scheduled request's postflight reuse this observation path. State survives server restart and sees a menu that paints after the former settle check would have completed. Pull transcript without queue callbacks while holding the delivery lock. Do not hold a database transaction across runner I/O: durable intent commits first, then guarded I/O, then outcome commit.

Tests: new `RemoteControlModalWatchTests` and `RemoteControlRecoveryTests` with the **production action implementation** and scripted runner; extend `SessionGenerationDeliveryOverlapTests`. Required cases: never-armed-path late menu, working fork plus continuing spinner plus several enqueues, non-AlwaysOn/card session, RC disabled but menu present, unchanged output sequence, missing/partial snapshot, stale generation, fresh idle/no-modal no-op, working and unbound transcript zero Esc, fresh idle menu exactly one Esc, swallowed Esc, post-read failure, delayed disappearance, crash after persisted Esc intent, repeated service recreation, and no second Esc in the same episode. Verify command exclusion while an ordinary delivery holds the lock and stop/resume replacement while a request is pending.

### S4 — Durable Attention and modal barrier without false delivery receipts

Files: `SessionMessageQueueService.cs`, `AgentSessionService.cs`, `AgentSessionLaunchQueue.cs`, `AttentionService.cs`, `DataRetentionService.cs`, `server/Application/Dtos/AttentionDtos.cs`, relevant existing queue/incident DTO mapping, and the narrow queue-to-RunAttempt linkage for deferred direct boot work; append new incident/attention kinds without renumbering old values. `QueuedInputWatchdogService.cs` keeps its generic age/working rules and queue kinds; share only read-side evidence helpers if useful. Protect unresolved maintenance, deferred work and modal evidence from queue/session retention. Existing `AgentSupervisorService.PruneIncidentsAsync` may prune transition copies because the open episode remains authoritative.

Attention is an existing Process-group item with OpenAgent/OpenDrawer actions, not a new card state, external alert sink or automatic message to a channel. Show "Remote Control menu blocks input" plus observation time, working/idle state, whether Esc was withheld/attempted/verified and whether input conversion remains unproven. Open modal episodes remain visible independently of incident pruning or the recent-history window. Resolved menu history remains inspectable and must not say inputs were delivered. Avoid duplicate Attention cards for the same session symptom: prefer the specific open modal item while retaining generic `QueuedInputStuck` incident history. Once the menu closes, any still-applicable generic symptom can appear again.

Add an explicit modal-blocked field/reason to the queue DTO if required by the existing UI. The queue barrier has no Pending-to-Sent success side effect and must precede the local-command fast path and generic failure handling. On an externally cleared menu, catch up and late-confirm before resuming ordinary queue work. Drain/removal is not prompt delivery. Do not release a barrier from an unrelated `UserPrompt` while the menu is still positively observed.

Client files only where exhaustiveness/rendering requires them: `client/src/features/attention/AttentionPanel.tsx`, its test, `client/src/features/agents/SessionMessageQueue.tsx`, its test, and the actual generated/shared API types located by Code. There is no global working/idle semantics change and no new kill/Esc button in this card. Reuse current operator session controls.

Tests: new focused `RemoteControlModalAttentionTests`; extend `AttentionServiceTests`, `SessionMessageQueueDeliveryVerificationTests`, `SessionMessageQueueInterruptedAttemptTests` and `AgentSessionLaunchFailureTests`. Pin live working-modal visibility, bounded incident emission, incident-pruning survival, per-generation closure, modal-specific versus generic dedupe, barrier on Now/WhenIdle/local-command/boot/retry paths, unchanged attempt counts, preserved queue rows, deferred direct-card boot surviving service recreation and clearing exactly once, no premature first-output timeout, no kill or retype, recovery by matching complete UserPrompt, and nonconfirmation from QueueEnqueue/QueueRemove/unrelated prompt. Client tests only for changed mappings/rendered reasons.

### S5 — Native regression evidence, canary and owner documentation

Files: `src/Antiphon.FakeClaude/Program.cs`, `tests/Antiphon.Agents.Pty.Tests/FakeClaudeContractTests.cs`; new focused `tests/Antiphon.Tests/Application/RemoteControlRecoveryPtyIntegrationTests.cs`; `tests/Antiphon.Agents.Pty.Tests/ClaudeRemoteControlMenuCanaryTests.cs`. Add sanitized fixtures under the appropriate existing test-fixture/golden directories; replay through `TerminalScreen`, including fragmented paints and clear frames. The full production ANSI log is not a fixture.

FakeClaude must model arm-first/menu-on-repeat, delayed menu appearance, a working turn that remains working while a modal blocks queued input, and an Esc that may be swallowed/delayed. Its current immediate bool-menu helper is insufficient evidence for the new coordinator. Native coverage runs the queue/coordinator/runner path and checks the persisted receipt as well as PTY input. In the idle recovery case, submit a unique bounded harmless input once and prove it becomes a matching UserPrompt once after clearance. In the working case, prove automatic detection/Attention and **no automatic Esc, extra Enter, restart or work replay**; then use an explicit test-owned dismissal to prove conversion can resume.

The real canary must deliberately establish a bridge on its own isolated session before testing the management menu; it cannot count "no menu, skipped" as passed. Measure actual menu shape, single idle Esc, two clear observations, retained bridge and actual unique UserPrompt conversion. It must never attach to `0d64706d...`, a production standing agent or the production runner. A provider/session prerequisite failure is reported as pending acceptance with the reason; do not weaken the assertions or claim native acceptance from fake-only results. Busy automatic Esc is outside this design and this canary's acceptance claim.

Update `docs/session-runtime-invariants.md`, `docs/agent-kinds.md`, and the applicable inspection guidance in `docs/ops-http.md` for typed receipts, conditional-input capability and operator recovery. The [ConPTY owner](../../adr/0002-modern-conpty-backend.md), especially full-grid/paint evidence, and [testing/build owner](../../testing-and-build.md) govern execution. Preserve the RC-at-startup settings overlay; do not modify provider home/settings or credentials.

Tests named above plus focused `TerminalScreen` replay coverage if a new replay class is needed. TestDesign must supply exact method names, coverage-to-class mapping, meaningful failure oracles, fresh TRX checks and separate source-restored positive-control specifications. It must distinguish deterministic fake/runner integration coverage from real-provider acceptance.

## Delivery and next-stage contract

Plan-only validation: source, owners, investigation and live card history were read; no implementation, build, unit/integration run or live reproduction is claimed. Validate this Markdown with `git diff --check`, commit and push the task branch. The caller must land this plan before commissioning a sibling Worktree TestDesign task, per the documented dispatch-base rule.

TestDesign is the next stage. It must stress the enqueue/delivery/generation gap, the working-modal receipt, and the persistence/conditional-input boundaries above. It may refine test seams and fixture mechanics, but must not silently loosen D-1 through D-8 or substitute passing mock booleans for observable I/O and durable state. Code follows only after the executable verification section has been added to the plan and reviewed as the TestDesign deliverable.

Run later verification in the foreground with producer-owned alternate output paths ending in `/`; keep `Antiphon.Tests` and `Antiphon.Agents.Pty.Tests` sequential and use each assembly's process-spawn limiter. No tests or restart commands target 17204. Clean only inventoried task-produced alternate outputs. Report actual executed counts and failures; verify any inherited failure at the base revision before blaming or changing this work. This plan does not authorize a production restart or manipulation of the live orchestrator during testing.

After separate Code/Review and confirmed landing, deploy compatible runner/server through the canonical checkout/runbooks and record both build identities and the new conditional-input capability. Exercise a disposable acceptance session and inspect the **persisted** request/modal/input receipts. A healthy HTTP response or pushed branch is not activation or delivery evidence. Rollback/old-runner compatibility withholds automatic RC and preserves observations; it must not silently restore unsafe raw writes.

Remaining limits are explicit: exact historical injection attribution is unavailable; busy-menu clearance stays an operator action; the existing probe is local-child/local-profile evidence and Unknown on an inaccessible deployment; the live provider can change its paint/bridge behavior. The future observation timestamps, request IDs and generation-scoped receipts make a recurrence attributable without claiming to reconstruct September 13's missing timestamp.

## Verification design

TestDesign `d2e50bff`, 2026-09-13. Inspected plan landing `f26aa721e81905f046b47247054e6696262e12ef`, from shared checkout `39c45a67`. This appendix supplies the executable specification for D-1 through D-8; it does not change those decisions. New methods below are requirements for Code, not claims of existing or passing tests. Code implements the fixtures/tests and runs ordinary V/R; ordinary Review evaluates both the tests and this PC design before land. SourceLanding Mutation subsequently implements each compiling defect and records break, assertion-red, restore, rebuilt-green against the landed source.

### Inspection

Paths below are relative to the repository. Method/body ranges identify what was actually read, rather than claiming an entire large test file was inspected. `Application/` and `TestHelpers/` in this table mean `tests/Antiphon.Tests/`; `Runner/` means `tests/Antiphon.SessionRunner.Tests/`; `Pty/` means `tests/Antiphon.Agents.Pty.Tests/`.

| Test/fixture bodies read | Boundaries -> verification |
|---|---|
| `Application/RemoteControlMenuScreenTests.cs`, all nine bodies and `IncidentScreen`; `tests/Antiphon.Tests/Agents/FakeAgentProtocolAdapter.cs` menu literal/OnSubmitted contract; `Pty/TerminalScreenTests.cs` helpers and plain-text/cursor bodies; `Pty/ClaudeGoldenReplayTests.cs` loader/first three tests and the nearest `golden/claude-turn.golden.txt` paint excerpt | Complete 0292/0514 blocks, fragmented VT paints, erased/partial/prose negatives -> V-1, R-1. Replace both existing real conversation URLs with synthetic literals when touching the fixtures. Do not copy production ANSI or transcripts. |
| `Application/SessionHealthTests.cs`: RC rearm/restart, busy, never-armed, two settle-menu tests, unreadable-file test, `BuildHarness`, clock/probe/actions fakes; production `SessionHealthActions.cs` in full; `SessionHealthService.WatchRcAsync` and candidate query; `IRcBridgeProbe.cs` | Current fake dismisses synchronously without post-read; current Unknown+idle+zero-connections can reach repair. Scheduling versus delivery and connection-warning thresholds -> V-2, R-2/R-3/R-5. |
| `Application/AgentSessionLaunchFailureTests.cs`: RC cases from `Remote_control_delivery_failure_leaves_the_session_running_and_records_an_incident` through both menu branches, probes, `A_swallowed_boot_prompt_is_retyped_and_the_second_attempt_lands`, `LaunchFixture` construction, launch/card helpers and `WireDelivery`; `Application/AgentRemoteControlGateTests.cs` create/PATCH bodies; `ProviderContractCatalogTests.cs` kind/axis and local-command/overlay bodies | Fresh/resume/continue, RC-only retry removal, rename, best-effort work, capability -> V-2/V-7, R-2/R-4/R-11. The swallowed-boot test currently retries RC, despite its broad name; rewrite that case and add an ordinary work retry case. |
| `TestHelpers/BridgeQueueHarness.cs` DI, options, creation, default OnSubmitted and transcript helpers; `Application/SessionMessageQueueInterruptedAttemptTests.cs` first eleven cases; `SessionMessageQueueDeliveryVerificationTests.cs` C475 overlay-disabled/pull-before-working bodies | Harness callback fabricates a successful prompt and end; must be disabled for negative delivery and maintenance cases. Interrupted Sent/Pending recovery, Enter-only, late confirmation, Now -> V-6, R-4/R-9/R-10. |
| `Application/SessionGenerationDeliveryOverlapTests.cs` all three bodies/helpers; `AgentSessionLaunchQueueOwnershipTests.cs` all four bodies and `OwnershipFixture` | Running before launch setup completes, queue lock, old launch tail, replacement A/B, interrupted resume -> V-3/V-7, R-3/R-11/R-12. |
| `Runner/RunnerSessionGenerationTests.cs` and `Runner/LocalHttpRunner.cs`, complete; `tests/Antiphon.Tests/Agents/SessionRunnerGenerationWireTests.cs` and `RunnerTerminalSessionGenerationTests.cs`, complete; production runner `GetSnapshot`, `SendInputAsync`, `KillGenerationAsync`, inner `GetSnapshot` and `WriteAsync` | Current input resolves by ID without launch exclusion; input log is appended before actual backend write. Real route/capability, same-object fencing, receipt ambiguity -> V-3, R-12/R-13. Input-log presence is therefore not receipt. |
| `TestHelpers/TestDbFixture.cs` complete, `TestDbFixtureLifecycle.cs` container/migrate/template/clone operations, `ProductionRunnerGuard.cs` initialization; `Application/DataRetentionServiceTests.cs` running/stale transcript cases and service/session/queue helpers | PostgreSQL constraints, rollback and restart visibility, retention. `CreateIsolatedSchemaAsync` now clones a database, not a schema. All helper contexts must use its connection -> V-5, R-7/R-8/R-11. |
| `Application/AttentionServiceTests.cs` kind-43 open/closed/dead cases, ItemsFor/BuildService, `Scenario` session/agent/channel ownership helpers; `QueuedInputWatchdogTests.cs` first nine tests plus service/scenario construction | Specific modal versus generic queue symptom; working and age gates stay on generic watchdog; severity, historical cutoff -> V-4/V-5, R-5/R-8. |
| `Application/SessionMessageQueuePtyIntegrationTests.cs` first producer-to-prompt test and provider/start/seed/pump/receipt/cleanup helpers; `TestHelpers/DirectSessionRunnerClient.cs` construction/startup/restart contract; `SessionQueueTranscriptPump.cs` complete | Real queue/runtime/runner/PTY with independently read child JSONL; pump is an ingestion substitute, not provider/tailer proof -> V-8, R-14. |
| `Pty/FakeClaudeContractTests.cs` readiness/launch helpers and both RC tests; `src/Antiphon.FakeClaude/Program.cs` Esc, enqueue, RC paint branches; `Pty/EchoGatedSubmit.cs` complete | Existing mode opens on first arm, immediately drains on Esc, and lacks a continuing working turn. Required state-machine additions -> R-14. |
| `Pty/ClaudeRemoteControlMenuCanaryTests.cs` and `Pty/ClSession.cs`, complete; `ClaudeRemoteControlAtStartupCanaryTests.cs` three cases and `CanarySandbox.Create/Trust/Dispose` | Existing menu canary skips and never submits its token; startup sandbox writes user `.claude.json`. Neither behavior is permitted in the new acceptance fixture -> V-9, R-15. |
| `client/src/features/attention/AttentionPanel.test.tsx` item/serve and first four cases; `features/agents/SessionMessageQueue.test.tsx` complete; `client/src/test/utils.ts`, MSW `mocks/server.ts`; `scripts/test-client.ps1` argument forwarding | UI consumes typed receipt, working-modal rendering and unchanged attempt count -> R-16. MSW/SignalR stubs cannot prove server delivery or live invalidation. |

Required setup additions (Code owns these concrete deliverables):

- Add `tests/Antiphon.Tests/TestHelpers/RemoteControlRecoveryHarness.cs` beside `BridgeQueueHarness`: same production queue/runtime/session/launch graph, production `SessionHealthActions`, new watch/recovery, probe, and recording scripted runner. It must expose separate snapshot, transcript-catch-up, conditional-body, conditional-Enter, conditional-Esc and persistence gates with `TaskCompletionSource` using asynchronous continuations. No fake recovery result or auto-created UserPrompt. Supply explicit accepted generation, child PID, transcript binding and watermarks. Use offset-over-real `TimeProvider` or advancing fake time for delivery deadlines; the frozen health clock is unsuitable for queue polling.
- Use a fresh `TestDbFixture.CreateIsolatedSchemaAsync()` database per concurrency/crash/migration case, with every scoped context, transcript callback, verification read and cleanup pointed to it. `BridgeQueueHarness.ConnectionString` alone is insufficient: its static transcript helpers and the existing pump still use the shared connection. Extend/parameterize those helpers or use the new harness's connection-bound equivalents. No EF InMemory/SQLite substitute for filtered uniqueness or migrations.
- Add test-only runner input-boundary rendezvous next to the existing generation fixtures. It pauses after lookup/check and before backend invocation, records actual backend input separately from the runner input log, and permits controlled replacement/output advance. Keep the launch gate exercised; no reflection that swaps the session after bypassing the production gate. For the real HTTP test, use `LocalHttpRunner` with random loopback port and test-owned processes.
- Add SaveChanges/transaction interception at reservation, SubmissionStarted, episode detection, Esc intent, dismissal result, deferred-work linkage, and final input receipt. Each interception has before-commit failure and after-commit process/service-loss modes. Dispose the complete server service provider and construct another against the same test database and same independently owned runner; merely disposing a DbContext does not model restart. One native case runs an owned server worker process and terminates only that worker at the gate, preserving runner/child for recovery. Await every owned worker and pump at teardown.
- Extend FakeClaude under an explicit RC scenario opt-in, leaving default clipping/other modes intact: readable synthetic per-child bridge state, first arm sets bridge, repeated arm paints menu, delayed/fragmented menu, explicit working hold with spinner and no TurnEnd, test-owned release gate, swallowed/delayed Esc, complete clear frames, and JSONL QueueEnqueue/QueueDequeue/UserPrompt records. Readiness must announce each armed scenario. A control pipe/file under the test root changes these states without typing hidden work into the recipient. Add sanitized `Fixtures/RemoteControl` paints beside the Application matcher test and copy them to output; existing Pty `golden` conventions govern VT replay. Do not reuse live IDs, URLs or prompt text.
- Native fixture pins `modern` for acceptance and exercises `inbox` once for the supported short-body fallback. Child/pump/runner all use one test-owned transcript path and accepted generation. Production-tailer acceptance additionally uses an explicit transcript binding to the newly launched native session. Add a HTTP lane using the production `SessionRunnerHttpClient`; the direct client alone does not prove capability/serialization. Link the inspected `LocalHttpRunner.cs` into Antiphon.Tests as a shared test helper, and stage the runner executable plus dependencies in that project's alternate output. Its own namespace does not require a reference to another test assembly. Stage the owned crash worker there too. The Pty canary has no server dependency: it measures real-provider native input directly, while N proves the production queue/HTTP path.
- The inspected direct client currently omits `spec.AcceptedStartedAt` when constructing `RunnerLaunchRequest`, omits the new snapshot metadata, and advertises a handwritten feature list without session-generation support. Extend its request/result/snapshot mappings and conditional operation forwarding to match the real runtime; derive advertised support from implemented runtime capability, not a test-only claim. Keep the HTTP lane so fixing the fake cannot hide a broken real client/route. For ownership PC-8 hold a real launch continuation just before it acquires the RC delivery lock (ownership true, lock free); PC-10 separately holds the delivery lock. That makes each independently guarded boundary reachable.
- Replace the canary's environment/setup with a unique session ID, empty temp cwd, per-launch `remoteControlAtStartup:false` overlay and bounded real readiness/trust handling. Reuse read-only authenticated provider context only as the existing headed lane allows; never call `CanarySandbox.Trust`, write user settings, copy credentials, or attach an existing session. Missing provider authentication/RC entitlement is a named acceptance prerequisite failure. Isolated session means no existing conversation is resumed; a fresh profile is not claimed by `HeadedSafeEnv`. The test must record that distinction.

### Delivery inventory

`G = (SessionId, SessionGeneration.Normalize(AcceptedStartedAt))`; timestamp equality is UTC microseconds. `Q = SessionQueuedMessage.Id` is the maintenance/work request identity, never command text. `E = RemoteControlModalEpisode.Id` is immutable and generation scoped. `W = (RunAttemptId, original work queue Id, G)` connects a direct-card continuation. Ordinary delivery additionally names its attempt baseline and matching transcript UUID/sequence plus complete body; identical-looking text from another generation or before the baseline is insufficient.

| Path: producer -> destination | Persistence boundary and recovery at each handoff | Observable receipt and tests |
|---|---|---|
| Boot RC (`System`) / health tick (`Supervision`) -> shared reservation -> WhenIdle executor -> conditional runner -> child | Before reservation commit failure: zero bytes, no accepted durable request, later normal tick/boot reconciliation may reserve. Commit succeeds but reply/flush is lost: find same Q, coalesce; never reserve a duplicate active G. Before SubmissionStarted commit failure: no write. After intent commit, before/after body or Enter, or lost transport response: retain Q and ArmUnconfirmed, reconcile fresh bridge/menu; no repeat automatic arm. Outcome-commit failure after armed evidence: restart reconciles same Q. R-2/R-3/R-4/R-13. | Requested/queue insertion is not armed. Require current-child Armed observation, or honest suppression/unconfirmed result associated with Q/G; no `DeliveryVerdict.Delivered` or synthetic UserPrompt for RC. V-2 and V-8 run both initially busy and already-idle recipients through the real producer/queue path. Native child arm counter = 1 and persisted ArmedObserved on that Q; unrelated/pre-existing bridge is SuppressedAlreadyArmed. |
| Passive/startup/pre/post-arm observer -> episode store -> Attention/detail recipient | Detection-commit failure: no Esc; next sweep redetects with first durable observation time. Commit succeeds but incident/event publication fails: read E directly through fresh Attention/detail query; next sweep emits/reconciles only a missing meaningful transition. Incident pruning is not episode deletion. R-5/R-7/R-8. | Fresh projection contains E/G, observed timestamp/state/action reason and correct severity; optional Q only with actual linkage. API projection is the server-side recipient, UI DOM in R-16 proves rendering separately. SignalR acknowledgement alone does not satisfy projection receipt. |
| Recovery coordinator -> persisted Esc intent on E -> conditional runner -> child menu -> post-observation receipt | Fail intent commit: zero Esc. Lose process after intent, after physical Esc, or after either clear read/result commit: reload E, at most one possibly sent Esc for the episode; continue observation without replay. Refused zero-byte generation/sequence write records refusal and permits fresh observation. Read failure is Unknown, not empty screen. R-6/R-7/R-12/R-13. | One Esc write is not dismissal. DismissedVerified needs two clear same-G grids separated by at least 500ms, no remnant, within the 3s budget. External clear gives ObservedClear, replacement gives GenerationEnded. None is input delivery. V-4/V-8. |
| UI/channel/delegation enqueue or interactive launch note -> existing queue -> modal barrier -> child -> transcript ingestion -> per-message confirmation | Enqueue DB failure: explicit failure, no bytes/receipt. Commit then lost scheduling wakeup: stranded sweep finds same Q. Hold open episode without attempt changes. Crash after release but before body uses original baseline; after possible body/Enter or child UserPrompt but before receipt commit, catch up and late-confirm before any replay. Crash before/after transcript batch commit: retry ingestion by UUID, no duplicates. R-9/R-10/R-14. | For each original Q, matching complete UserPrompt in the child's JSONL and persisted session transcript after its baseline, exactly once; then receipt names that evidence. Run a busy recipient with three distinct short prompts, including equal prefixes/different suffixes, and an already-idle recipient. QueueEnqueue/QueueRemove/QueueDequeue, Sent, unrelated prompt, truncated prompt, modal-clear event and transport ack cannot pass. |
| Direct card `StartAsync`/RunAttempt -> deferred original work reservation -> recovered launch continuation -> queue -> child -> transcript/first output | Fail deferred-work/link commit: keep launch responsibility or explicit recoverable failure, never yield successfully and lose local prompt. Commit then lose launch owner/wakeup: reconstructed provider finds same W. Concurrent resumes coalesce W. Crash before submission, after child prompt, before receipt and before first-output observation: preserve W/baseline; late-confirm and observe same attempt without duplicate prompt or premature timeout/success. Replacement G ends old authority, does not retarget old continuation. R-11/R-14. | Original body arrives once as complete UserPrompt associated with W/G. First-output clock begins only after submission, and RunAttempt is not Succeeded while deferred. Already-idle recovery and busy-to-idle recovery both traverse production launch continuation and queue. V-7/V-8. |

For every fault point above, assert durable state from a new DbContext, run recovery twice, and inspect child input plus final recipient evidence. A fake input counter proves exclusion/ordering only. A scripted transcript proves matching logic only. `SessionQueueTranscriptPump` proves persistence/normalization of real child JSONL but substitutes for runner discovery/SSE; it cannot prove production binding or event replay. The HTTP native lane covers the route and production binding/ingestion. FakeClaude proves deterministic native transport and modeled modal behavior, not current Claude behavior. V-9 is the separate real-provider acceptance. **Review must reject a delivery claim that stops before the specified recipient evidence**, including an all-green fake run offered in place of V-9.

### Proves it works now

These are post-Code verification commands, not TestDesign execution claims. Class aliases in R/PC tables resolve through this inventory; new Application classes live under `tests/Antiphon.Tests/Application/`, new runner classes under `tests/Antiphon.SessionRunner.Tests/`.

| ID | Behavior / layer | Tests and expected result |
|---|---|---|
| V-1 | Active-grid classification / unit | `RemoteControlMenuScreenTests` (alias M), `TerminalScreenTests`: R-1. Whole menu true, prose/partial/erased false, remnant remains non-clear. |
| V-2 | All RC producers / production Application integration | `RemoteControlMaintenanceQueueTests` (Q), `AgentSessionLaunchFailureTests` (L), `SessionHealthTests` (H), `AgentRemoteControlGateTests`, `ProviderContractCatalogTests`: R-2/R-3/R-4. Busy requests wait; idle Unarmed writes once; Armed/Unknown/stale write zero. |
| V-3 | Conditional boundary / wire + real runner | `RemoteControlConditionalInputTests` (B, new Runner class), `RunnerSessionGenerationTests`, `SessionRunnerGenerationWireTests` (Y), `RunnerTerminalSessionGenerationTests` (T), `SessionGenerationDeliveryOverlapTests` (O): R-12/R-13. Every refusal has zero backend bytes; valid input reaches the owned child. |
| V-4 | Passive watch and idle recovery / production Application integration | `RemoteControlModalWatchTests` (W), `RemoteControlRecoveryTests` (E): R-5/R-6. Working menu immediately visible, no automatic Esc; idle case verified by two observations. |
| V-5 | PostgreSQL and Attention / integration | `RemoteControlModalPersistenceTests` (P), `RemoteControlModalAttentionTests` (A), `DataRetentionServiceTests` (D), `AttentionServiceTests`, `QueuedInputWatchdogTests` (J): R-7/R-8. Reloaded episode survives incident pruning; migration preserves intent. |
| V-6 | All ordinary input barriers / production queue integration | `SessionMessageQueueDeliveryVerificationTests` (V), `SessionMessageQueueInterruptedAttemptTests` (I): R-9/R-10. Zero new bytes/attempt charges while blocked; exact late confirmation precedes redelivery. |
| V-7 | Direct boot continuation / Application integration | L plus `AgentSessionLaunchQueueOwnershipTests` (K): R-11. Same durable W resumes once; no first-output timer or success before submission. |
| V-8 | Producer to recipient / native integration | `RemoteControlRecoveryPtyIntegrationTests` (N) and `FakeClaudeContractTests` (F): R-14. Complete unique UserPrompt in independent child JSONL and DB; working case holds safely; crash recovers original identities. |
| V-9 | Current provider / isolated headed acceptance | `ClaudeRemoteControlMenuCanaryTests.C514_Idle_menu_clearance_preserves_bridge_and_converts_prompt` (C): R-15. Required pre-established bridge, exact menu, one idle Esc, two clear grids, unchanged bridge and actual complete UserPrompt. Zero skips for acceptance. |

Use producer-owned `bin-c514/` outputs, build each of the three test projects once, then execute with `--no-build --property:OutputPath=bin-c514/`. Ordinary command form: `dotnet run --project tests/Antiphon.Tests --no-build --property:OutputPath=bin-c514/ -- --treenode-filter '/*/*/RemoteControlMaintenanceQueueTests/*' --report-trx --report-trx-filename run.trx --results-directory .antiphon/c514-v2-queue`. Substitute each exact class/project above and a fresh, previously nonexistent result directory per invocation. For a batch use the documented OR form, for example `'/*/*/(RemoteControlModalWatchTests*)|(RemoteControlRecoveryTests*)/*'`; verify both classes actually executed. Run Unit once with `'/*/*/*/*[Category=Unit]'`. V-9 command sets `ANTIPHON_HEADED_TESTS=1` for its owned foreground process and uses exact filter `'/*/*/ClaudeRemoteControlMenuCanaryTests/C514_Idle_menu_clearance_preserves_bridge_and_converts_prompt'`; restore the original environment afterward. Explicit test selection must produce a fresh executed result, not discovery output.

Every run records source SHA, tool/runtime version, filter, start/end UTC, pass/fail/skip counts and fresh TRX. Assert all intended methods/argument rows appear and counts are nonzero; for the concrete V-2 invocation run `pwsh -File scripts/test-duration-tripwire.ps1 -Trx .antiphon/c514-v2-queue/run.trx`, and run the same check against each other invocation's recorded result. Tests default to Unit xor Integration in Antiphon.Tests; native classes use its assembly-local process-spawn limiter. Run the test assemblies sequentially, never against 17204. A Code task following the delegate full-run instruction runs the complete suite once in foreground chunks before targeted follow-ups; see Cost for that additional budget and the newer owner's warning that 25.5 minutes is obsolete.

### Guards the regression

All `C514_...` methods in the PC table are distinct required regression methods. They are part of these R groups and the V class filters, not a separate mutation-only suite. Parameter rows must set every other eligibility guard to a passing state, so removing the named guard reaches its decisive assertion. Include the companion allowed case (same G, readable Unarmed, bound idle transcript, clear grid/capability) in each refusal family to detect a blanket no-op implementation. No test substitutes only a returned bool for the bytes/durable-receipt assertions.

| ID | Test family and boundary combinations / decisive assertion |
|---|---|
| R-1 | M methods PC-26..29 plus `C514_Sanitized_menu_shapes_replay_fragmented_and_clear`. Replay 0292 and 0514 at 120x30 and 80x24, selected-row/border/whitespace variants, split every escape sequence and row, then clear/scroll off. Each missing heading/action/footer, reordered rows, quoted prose/help and partial remnant must be non-actionable; a remnant must not be certified clear. A full menu on a current grid is actionable even with unrelated surrounding text. |
| R-2 | Q/L/H methods PC-1..7, 12, 23, 77. Cross fresh/resume/continue/health with Armed (connections 0 and 2), Unarmed readable (0 and 2), Unknown missing PID/file/probe/generation/capability. All positive menus suppress arm. Unarmed at enqueue becomes Armed/Unknown/stale before flush; verify no RC body/Enter. Reverse Armed-to-Unarmed needs a new ordinary eligibility pass. RC failure with a clear grid preserves normal work; rename runs only with positive armed evidence and no menu/remnant. |
| R-3 | Q/O/K methods PC-8..11, 13..14, 25, 76, 80. Race two health ticks and boot/health reservation; hold launch ownership after Running; independently test Stopping. Busy at enqueue and at delivery then TurnEnd. Shared lock pauses ordinary body-to-Enter and forbids RC/Esc interleave. Two independent connections contend on active uniqueness. Fault before/after every reservation/intent commit; reconnect twice, same Q/G. Generation changes with same session ID and same PID; health streaks start afresh. |
| R-4 | Q/L methods PC-15..22 plus `C514_Maintenance_faults_reconcile_without_retyping`. Lose server/HTTP response before body, between body/evidence/Enter, after Enter, before final result save; possible bytes veto retries even if no current menu/bridge evidence and active slot has closed. A positively refused zero-byte operation can be reconsidered on a later tick. SendNow/amend must retain typed RC metadata; legacy rows never acquire invented System/Supervision origin. Migration cases: Pending/Sent-without-verdict, attempts 0/1, exact token with argument/leading whitespace, non-command prose and `/remote-control-other`; preserve explicit new operator commands and attempted baselines after failed rename/relaunch. |
| R-5 | W/H/J methods PC-24, 30..34, 82..83. Sweep working internal-fork + spinner + 0 or 3 enqueues, young/old queue ages, constant/moving output, no health request, delayed menu beyond the old settle window, capable card/non-AlwaysOn/RC-disabled sessions. RC watch off/modal watch on still observes. Modal watch off or global supervision off does not run. One hung runner read times out and next candidate is observed; caller cancellation stops promptly. Plain long working turn remains excluded by generic watchdog. |
| R-6 | E methods PC-35..44, 73, 81. Cached idle then caught-up working; idle but unbound/missing/failed transcript; current/stale/missing/partial snapshot; stale sequence/current sequence; supported/unsupported conditional transport. Exactly one Esc only in fully eligible case, no manual turn. Swallowed Esc, first clear then remnant, second read failure, different generation, clearance just inside/outside 3s. Record intent before possible I/O; crash/recreate after intent/write/each read never sends second Esc. Rejected zero-byte stale write can reobserve. Two clear observations >=500ms apart; external clear is ObservedClear. |
| R-7 | P/D methods PC-45..48, 63 plus `C514_Migration_and_receipts_survive_recreation`. Run generated migration from immediate pre-C514 schema with real legacy rows, and create clean database; assert filtered constraints through actual conflicting inserts, not EF metadata only. Open duplicate same G rejected, resolved history plus one open allowed, different G allowed. Test detection/intent/result SaveChanges failure and transaction rollback. Immutable E/first-observed time; nullable source Q; before/after sequence and catch-up watermark round-trip. Use sentinel full screen/prompt/URL/state-file/secret-like text and assert none reaches episode/incident/detail. Bound transition emission under 100 repeated sweeps. |
| R-8 | A/D methods PC-49, 74..75 plus `C514_Receipt_projection_survives_publication_failure`. Fresh Attention GET/detail contains working modal and unproven input conversion, Warning unbound and Critical channel-bound. Prune linked incidents and advance beyond `since`: E still visible. Specific modal suppresses duplicate generic Attention only while open; historical kind-43 persists and may return after clear. Fail event-bus publication after commit and prove fresh projection still sees E; inspect typed fields without parsing prose. |
| R-9 | V/I/L methods PC-50..56, 58, 84. Individually exercise Enqueue Now, pending SendNow, WhenIdle flush, ordinary local command, interactive note, direct boot, composer Enter retry, interrupted Sent/Pending recovery, generic failure cleanup. Open/DismissUnverified barrier yields zero extra bytes, same body/IDs/baselines/attempt count, no parking, kill, or Delivered. Pair barrier present with no barrier to show allowed delivery. Unrelated prompt and queue removal never release a still-observed menu. Preserve LF, bracketed multiline body and separate submitting Enter. |
| R-10 | I/N methods PC-57, 59, 78 plus `C514_Clearance_reconciles_each_message_identity_before_redelivery`. After external clear, catch up a late matching complete prompt before typing; receipts identify only those Qs. Three bodies have equal prefixes and distinct tails; truncated/other-session/old-generation/old-baseline UserPrompt and enqueue/dequeue/remove-only records confirm none. Native internally queued bodies drain without new Antiphon queue identities or replay. |
| R-11 | L/K methods PC-54, 60..62, 72, 80 plus `C514_Deferred_card_work_recovers_all_handoffs`. Real card/RunAttempt/launch ownership: persist original W before returning deferred; commit failure cannot release the last recoverable owner. Recreate provider before flush, after work submit, after UserPrompt and before first-output receipt. Two concurrent resumes produce one Q and one exact prompt. Assert Phase not Succeeded/TimedOut and timer not started while modal/busy held longer than FirstDeltaTimeout. Same-session replacement closes old G episode and never transfers old continuation or veto. |
| R-12 | B/Y/T/O methods PC-64..69, 71..72. Snapshot and conditional request use AcceptedStartedAt, not StartedAt. Matching microsecond-normalized instant succeeds; one microsecond difference, missing generation, same PID/new generation fail. Capability absent/null/old route 404 never posts raw `/input`. Replace between lookup/check/backend write, advance output at each command/body/Enter/Esc phase, and contend with launch gate. Assert actual old/new child byte logs, not merely status. Missing, Exited and Unsupported remain distinct outcomes. |
| R-13 | Y/Q/E methods PC-13..14, 39..40, 43, 70 plus `C514_Lost_write_reply_is_unknown_until_observed`. A recording HTTP handler forwards the conditional operation then loses its reply. Acknowledgement, empty screen due to read failure, or absence of bridge data never authorizes repeat or verification. Restart after outcome-commit failure recovers Q/E from stored intent and actual observation, with no second arm/Esc. |
| R-14 | F: `C514_First_arm_then_repeat_menu_and_working_hold_are_distinct`, `C514_Esc_swallow_delay_and_queue_conversion_are_independent`. N: `C514_Health_request_reaches_busy_and_idle_recipients`, `C514_Idle_modal_clearance_delivers_three_complete_prompts`, `C514_Working_modal_waits_for_operator_then_converts_without_replay`, `C514_Server_crash_recovers_each_delivery_handoff`, `C514_Deferred_card_boot_reaches_recipient_once`. Each asserts announced scenario and complete child JSONL plus DB receipt. Busy/idle are separate argument rows; fault rows enumerate every delivery inventory boundary. For working scenario, hold the fork and spinner past several sweeps, assert zero auto Esc/Enter/restart/replay, explicitly clear via test-owned operator input, release the turn, then require each prompt once. HTTP lane must use real ingestion; pump lane must identify its substitution. |
| R-15 | C method in V-9: fresh session proves Unarmed under settings overlay. Submit a harmless unique primer and await its real JSONL UserPrompt and TurnEnd to establish binding/idle; do not assume an unflushed empty startup transcript is idle proof. Then submit one deliberate test-owned arm, wait for current-child bridge ID, and deliberately open management menu using explicit operator command. Confirm idle in this bound transcript before one Esc; two clear grids >=500ms apart with same bridge, then submit a second harmless unique bounded prompt (nonce-prefixed request to reply OK) through the echo-gated native input path and require full UserPrompt once after the primer baseline in the bound real JSONL. Capture provider version, owned native session identity, backend, sanitized grid classes and receipt. N separately records runner build/capability/G and proves the queue path. No menu/no bridge/no transcript is failed or pending acceptance with reason, never a pass. No busy automatic-Esc claim. |
| R-16 | Client exact `it` names: AttentionPanel `C514 renders a working remote-control modal with unproven input conversion`; SessionMessageQueue `C514 renders modal blocked without parking or changing attempts`. Serve typed DTOs and assert visible reason, OpenAgent/OpenDrawer actions, no invented Delivered/Recovered text; no new action authority. Run both files through `pwsh -File scripts/test-client.ps1 AttentionPanel.test.tsx SessionMessageQueue.test.tsx -JsonResultPath .antiphon/c514-client.json`. These presentation checks supplement server safety guards PC-49/50/74/75. |

The finite matrices above deliberately do not take the Cartesian product of every clock, launch mode, provider kind and fault point. R-2 fully crosses launch mode with probe state; R-3/R-12 isolate each race with all other preconditions valid; R-6 fully crosses transcript eligibility with current/partial screen and tests each transport refusal separately; R-9 separately reaches every byte-producing path. Duplicate combinations with the same shared executor are excluded after these entry paths reach it. No safety guard is excluded from the inventory below.

Additional independently bypassable assertions PC-85..96 belong to the same ordinary filters: R-2/R-4 owns PC-85/91/93; R-7 owns PC-79/86/87/89/95; R-11 owns PC-88/90/94; R-6/R-9 owns PC-92/96. These split origin, episode identity versus observation time, causal linkage, RunAttempt success versus timer start, and the three retention queries; a passing test for one is not evidence for another. All receipt canaries use distinctive bodies longer than `PromptSubmissionMatch.MinMatchChars` (12); short weak-arm inputs are excluded from the acceptance claim. The existing `IsCompleteIn` implementation and queue's identity/completeness split were inspected; this card does not redefine transcript normalization.

### Guard inventory

Each row is an independently bypassable safety assertion. Source references are D/S anchors plus production owner; PC-n maps only to G-n. Guards whose implementation is new remain inventoried and must land with their named tests. There are no untested exceptions and no shared PC mappings. Test-only isolation/readiness checks remain prerequisite assertions, not product recovery authority; they must fail before a native launch if ownership or staging is missing.

| Guard | Plan reference and invariant | Control |
|---|---|---|
| G-1 | D-1/S2 executor rechecks RC-capable kind before bytes | PC-1 |
| G-2 | D-1 health scheduler never requests arm for Armed, even zero connections | PC-2 |
| G-3 | D-1 boot/queued executor re-probes Armed immediately before write | PC-3 |
| G-4 | D-1 Unknown probe withholds automatic arm | PC-4 |
| G-5 | D-1 positive current menu independently suppresses arm | PC-5 |
| G-6 | D-2 server compares queue accepted generation at execution | PC-6 |
| G-7 | D-2 established maintenance requires fresh transcript idle at execution | PC-7 |
| G-8 | D-2 launch ownership excludes health execution even when Running | PC-8 |
| G-9 | D-2 Stopping independently excludes health execution | PC-9 |
| G-10 | D-2/D-5 same queue delivery lock excludes ordinary input/RC/Esc interleave | PC-10 |
| G-11 | D-2 DB unique active automatic arm per generation, not only in-memory coalescing | PC-11 |
| G-12 | D-2 durable maintenance discriminator and reserved generation survive queue projection/reload | PC-12 |
| G-13 | D-2 SubmissionStarted commits before first possible automatic-arm write | PC-13 |
| G-14 | D-2 terminal possibly-written request vetoes future arms for G after active slot closes | PC-14 |
| G-15 | D-2 boot RC bypasses whole-submit retry | PC-15 |
| G-16 | D-2 maintenance bypasses interrupted-attempt retyping/batch work recovery | PC-16 |
| G-17 | D-2 submitting Enter requires composer evidence for the arm body | PC-17 |
| G-18 | D-2 automatic arm submits at most one Enter after possible write | PC-18 |
| G-19 | D-2 SendNow/amend cannot strip maintenance identity and enter raw delivery | PC-19 |
| G-20 | S2 legacy exact-token unresolved requests are classified without inventing provenance | PC-20 |
| G-21 | S2 legacy attempted/unclassified rows never auto-retype, including SendNow bypass | PC-21 |
| G-22 | S2 housekeeping preserves attempted evidence and explicit operator intent | PC-22 |
| G-23 | D-2/S2 rename requires Armed plus clear menu, independently of rename success housekeeping | PC-23 |
| G-24 | D-3 RC degradation/modal is never process-kill authority | PC-24 |
| G-25 | D-2/S2 health streaks reset at accepted-generation change | PC-25 |
| G-26 | D-4 coherent heading/actions/footer order is required for actionable menu | PC-26 |
| G-27 | D-4 partial remnant is not actionable menu evidence | PC-27 |
| G-28 | D-4/D-5 remnant is not a clear composer | PC-28 |
| G-29 | D-4 rendered current grid, not append-only raw history, is the matcher input | PC-29 |
| G-30 | D-4 working/spinner/output movement cannot suppress exact-modal observation | PC-30 |
| G-31 | D-4 live capable card/non-AlwaysOn/RC-disabled candidates are included | PC-31 |
| G-32 | D-4 modal-watch enablement is independent of RC connection-watch enablement | PC-32 |
| G-33 | D-4/S3 bounded read failure does not stop later sweep candidates | PC-33 |
| G-34 | D-4/S3 late observation does not require a pending request/latch/settle window | PC-34 |
| G-35 | D-5 catch up transcript under exclusion before idle decision | PC-35 |
| G-36 | D-5 missing/unbound/stale transcript is not idle proof | PC-36 |
| G-37 | D-5 current working transcript independently forbids automatic Esc | PC-37 |
| G-38 | D-5 fresh actionable same-generation grid is re-read after acquiring exclusion | PC-38 |
| G-39 | D-5/D-6 Esc intent commits before side effect | PC-39 |
| G-40 | D-5 possible Esc consumes this episode's automatic Esc allowance across restart | PC-40 |
| G-41 | D-5 Esc never tracks a manual turn or creates transcript activity | PC-41 |
| G-42 | D-5 verified dismissal requires two clear reads separated by settle | PC-42 |
| G-43 | D-5 post-read failure/deadline cannot become verified dismissal | PC-43 |
| G-44 | D-5 both post-read observations belong to the same accepted generation | PC-44 |
| G-45 | D-6 DB permits only one open episode per G | PC-45 |
| G-46 | D-6 first observation is immutable and not a fabricated paint timestamp | PC-46 |
| G-47 | D-6 persisted and exposed diagnostics exclude screens/bodies/URLs/state/credentials | PC-47 |
| G-48 | D-6 incident copies emit once per meaningful episode transition | PC-48 |
| G-49 | D-6 Attention reads durable open episode beyond incident prune/history cutoff | PC-49 |
| G-50 | D-6/D-7 menu resolution cannot stamp an input Delivered or synthesize UserPrompt | PC-50 |
| G-51 | D-7 open-modal barrier precedes Now and pending SendNow bytes | PC-51 |
| G-52 | D-7 open-modal barrier precedes WhenIdle flush bytes | PC-52 |
| G-53 | D-7 open-modal barrier precedes ordinary no-UserPrompt local-command fast path | PC-53 |
| G-54 | D-7 open-modal barrier precedes boot work and interactive notes | PC-54 |
| G-55 | D-7 open-modal barrier precedes Enter-only retries/recovery | PC-55 |
| G-56 | D-7 blocked outcome avoids attempt charges, parking and generic failure cleanup/kill | PC-56 |
| G-57 | D-7 catch-up and existing late confirmation precede any redelivery after clear | PC-57 |
| G-58 | D-7 unrelated prompt/queue movement cannot release positive open-modal barrier | PC-58 |
| G-59 | D-7 Claude-internal queued inputs do not acquire replay work identities | PC-59 |
| G-60 | D-7 original direct-card prompt/RunAttempt/G linkage persists before launch yields | PC-60 |
| G-61 | D-7 recovery resumes the same deferred work exactly once across competing workers | PC-61 |
| G-62 | D-7 first-output timer is deferred until work submission | PC-62 |
| G-63 | D-2/D-7 queue retention protects unresolved/possibly-written maintenance veto | PC-63 |
| G-64 | D-8 capability/route refusal never falls back to unguarded `/input` | PC-64 |
| G-65 | D-8 runner compares normalized AcceptedStartedAt, not PID/StartedAt/tolerance | PC-65 |
| G-66 | D-8 runner refuses changed expected output sequence before backend bytes | PC-66 |
| G-67 | D-8 lookup/check/write use same object under launch gate | PC-67 |
| G-68 | D-8 missing/exited/unsupported destinations are refused distinctly before bytes | PC-68 |
| G-69 | D-8 each automatic body/Enter/Esc phase is guarded and stops on mismatch | PC-69 |
| G-70 | D-8 lost HTTP response is Unknown, never positive zero-byte refusal | PC-70 |
| G-71 | D-8 old snapshot missing generation is ObservationGenerationUnproven, not current evidence | PC-71 |
| G-72 | D-8 proven replacement closes old episode and transfers no intent/barrier/receipt | PC-72 |
| G-73 | D-7 only fresh proven clearance releases held ordinary work | PC-73 |
| G-74 | D-5/D-6 immediate modal severity is Critical when channel-bound | PC-74 |
| G-75 | S4 specific open modal dedupes generic Attention without destroying incident history | PC-75 |
| G-76 | S3 intent transaction ends before runner I/O; no DB transaction held through waits | PC-76 |
| G-77 | D-7 RC-only degradation without positive modal preserves best-effort work launch | PC-77 |
| G-78 | D-6 each delivery confirms its own complete prompt identity/baseline | PC-78 |
| G-79 | D-6 failed final receipt commit cannot fabricate completion or erase recoverable intent | PC-79 |
| G-80 | D-2/D-7 enqueue/link failure cannot discard responsibility before durable handoff | PC-80 |
| G-81 | D-6 no attributable Esc means ObservedClear, not DismissedVerified | PC-81 |
| G-82 | D-4 ordinary long working turns retain generic watchdog working exclusion | PC-82 |
| G-83 | D-4 global supervision shutdown/caller cancellation stops background work | PC-83 |
| G-84 | D-7 release preserves LF/bracketed-paste/separate-Enter input framing | PC-84 |
| G-85 | D-2 boot and health requests explicitly retain System/Supervision origin | PC-85 |
| G-86 | D-6 repeated observations/recreation preserve immutable episode ID | PC-86 |
| G-87 | D-6 related Q is recorded only with actual request/observation linkage | PC-87 |
| G-88 | D-7 RunAttempt cannot succeed while original work is deferred | PC-88 |
| G-89 | D-6 open-episode retention is independent of ordinary incident pruning | PC-89 |
| G-90 | D-7 deferred original work and owning RunAttempt/session survive retention | PC-90 |
| G-91 | D-2 Requested/suppressed/pre-existing bridge is never attributed ArmedObserved/RcReArmed | PC-91 |
| G-92 | D-5 recovery payload is exactly Esc, never Enter/navigation/work replay | PC-92 |
| G-93 | D-2 post-arm settle/observation time is based on execution, not enqueue | PC-93 |
| G-94 | D-7/D-8 old deferred work does not retarget a replacement generation | PC-94 |
| G-95 | D-6 detection persists before any automatic dismissal starts | PC-95 |
| G-96 | D-7 operator raw terminal input remains available behind the automated barrier | PC-96 |

### Positive controls

Each control runs its resolved class and exact method from this table; for example PC-3 selects `/*/*/RemoteControlMaintenanceQueueTests/C514_Bridge_arms_between_enqueue_and_delivery`. Class aliases resolve in V-1..V-9. `bytes` means writes that reached the recording backend/child, not requested input or runner input-log entries. Each method establishes its intended guarded state and a reachable allowed case before the decisive assertion. Repeated parameter rows inside one exact method are allowed; no PC selects an entire class. Each compiling defect changes production behavior or the generated migration; never delete/change the decisive assertion or make a fixture fail to obtain red. If Code chooses different internal member names, update only the corresponding mutation location and keep the same guard/test/oracle before Review.

| PC / break the corresponding G | Compiling defect to apply after land | Exact method expected red and decisive assertion |
|---|---|---|
| PC-1 | In RC execution preflight replace current-kind capability predicate with `true` | Q.`C514_Executor_rechecks_kind`: non-capable recipient RC bytes count = 0 |
| PC-2 | In health request decision let Armed+zero-connections enter never-armed scheduling | H.`C514_Armed_zero_connections_never_reserves_arm`: newly reserved automatic Q count = 0 |
| PC-3 | Remove Armed early return from execution preflight, retaining scheduling check | Q.`C514_Bridge_arms_between_enqueue_and_delivery`: RC body count = 0 after delivery-time Armed |
| PC-4 | Map Unknown probe result to Unarmed | Q.`C514_Unknown_never_authorizes_automatic_arm`: body/Enter bytes = 0 for each Unknown reason |
| PC-5 | Remove current-menu suppression from arm policy | Q.`C514_Current_menu_independently_suppresses_arm`: readable Unarmed plus menu emits no RC body |
| PC-6 | Skip queued request G versus current DB G comparison | Q.`C514_Stale_request_is_superseded_before_transport`: conditional operation call count = 0; result SupersededGeneration |
| PC-7 | Treat working transcript as idle in maintenance executor | Q.`C514_Busy_at_delivery_holds_until_fresh_TurnEnd`: bytes before TurnEnd = 0 |
| PC-8 | Remove launch-ownership predicate only | Q.`C514_Running_launch_owner_excludes_health_delivery`: bytes while owner held = 0 |
| PC-9 | Admit Stopping as maintenance-executable status | Q.`C514_Stopping_without_launch_owner_excludes_health_delivery`: bytes = 0 |
| PC-10 | Give RC coordinator a separate semaphore instead of queue GetLock | O.`C514_Maintenance_and_Esc_share_ordinary_delivery_exclusion`: no RC/Esc backend entry while ordinary body-to-Enter gate is held |
| PC-11 | Remove only active-maintenance unique-index creation from C514 migration | P.`C514_Concurrent_active_arm_insert_is_database_constrained`: second independent insert throws PostgreSQL 23505; one active Q |
| PC-12 | Persist maintenance discriminator as ordinary/None while preserving body | Q.`C514_Producer_identity_survives_reload`: reloaded discriminator/G equal expected typed request for System and Supervision rows |
| PC-13 | Move SubmissionStarted save until after conditional body invocation | Q.`C514_Arm_intent_commit_precedes_first_byte`: faulting intent save produces 0 backend calls |
| PC-14 | Restrict rearm veto query to active rows, excluding terminal ArmUnconfirmed | Q.`C514_Terminal_ambiguous_arm_veto_survives_restart`: second-generation-local RC body count remains 1 after provider recreation |
| PC-15 | Route boot RC through existing `SendBootPromptWithRetryAsync` | L.`C514_Boot_ambiguous_arm_is_not_whole_submit_retried`: RC body count = 1 under composer timeout |
| PC-16 | Remove maintenance exclusion in interrupted-attempt recovery | I.`C514_Interrupted_maintenance_never_retypes_as_work`: after recreation/sweep backend input list is empty |
| PC-17 | Bypass composer evidence wait and send arm Enter immediately | Q.`C514_No_composer_evidence_withholds_arm_Enter`: body count 1, Enter count 0 |
| PC-18 | Allow automatic arm into generic transcript/Enter-retry loop after its first Enter | Q.`C514_Arm_without_UserPrompt_submits_one_Enter`: Enter count exactly 1 after full confirmation deadline |
| PC-19 | Clear maintenance fields on amend or SendNow dispatch | Q.`C514_SendNow_and_amend_preserve_maintenance_policy`: reloaded typed identity unchanged and Armed recipient input count 0 |
| PC-20 | Skip legacy-unclassified backfill/reconciliation for exact `/remote-control` rows | P.`C514_Legacy_rows_keep_provenance_and_become_unclassified`: exact-token unresolved row discriminator = LegacyUnclassified, prior origin/body/attempt/baseline unchanged |
| PC-21 | Allow legacy-unclassified into automatic and SendNow delivery branches | Q.`C514_Legacy_attempted_rows_are_never_retyped`: attempted legacy input count = 0 even with empty composer |
| PC-22 | Restore broad first-token cancellation in `CancelPendingRemoteControlAsync` | Q.`C514_Housekeeping_preserves_operator_and_attempted_rows`: explicit operator row remains Pending and attempted evidence unchanged after armed boot/failed rename |
| PC-23 | Replace rename Armed-and-clear predicate with success after setup attempt | L.`C514_Rename_requires_armed_and_clear`: rename count = 0 for unarmed, unknown, menu and remnant rows |
| PC-24 | Insert `await _actions.KillSessionAsync(session.Id, ct)` in terminal RC-degraded branch | H.`C514_Repeated_rc_degradation_never_kills`: kill call count = 0 after thresholds and repeated watch passes |
| PC-25 | Retain previous `SessionHealthStateStore.Entry` streaks on G change | H.`C514_New_generation_resets_health_streaks`: first fresh-G below-threshold tick reserves 0 requests |
| PC-26 | Replace ordered-block matcher with old Disconnect+Esc substring predicate | M.`C514_Reordered_help_and_prose_are_not_actionable`: `IsPresent` false for all reordered/prose cases |
| PC-27 | Make actionable matcher accept the weak remnant classifier | M.`C514_Partial_menu_is_only_a_remnant`: `IsPresent` false and `HasRemnant` true |
| PC-28 | Remove remnant predicate from clear-observation certification | E.`C514_Remnant_after_Esc_keeps_barrier_unverified`: result DismissUnverified; verified timestamp null |
| PC-29 | Pass `snapshot.RawOutput` instead of `RenderedScreen` to observer | W.`C514_Erased_menu_in_raw_history_is_not_current`: 0 open episodes after clear-grid/old-raw-menu snapshot |
| PC-30 | Add `if (isWorking) return` before exact screen observation | W.`C514_Working_fork_with_spinner_is_observed`: one open E despite fresh working transcript and advancing output |
| PC-31 | Restrict modal candidates to current health AlwaysOn/cardless/RC-enabled query | W.`C514_Card_nonAlwaysOn_and_rcDisabled_sessions_are_observed`: each capable live candidate has an E |
| PC-32 | Gate modal watch on RcWatch.Enabled | W.`C514_Modal_watch_is_independent_of_connection_watch`: E exists when RcWatch=false/modalWatch=true |
| PC-33 | Return from whole sweep on a candidate's read timeout | W.`C514_Hung_candidate_does_not_hide_next_menu`: second candidate E exists within bounded test deadline |
| PC-34 | Require pending-menu latch/prior request before scanning | W.`C514_Late_menu_without_request_is_still_observed`: delayed E exists with related Q null |
| PC-35 | Remove fresh `CatchUpTranscriptAsync` call before recovery idle check | E.`C514_Recovery_pulls_before_deciding_idle`: cached idle, runner working -> Esc backend count 0 |
| PC-36 | Treat unavailable/unbound transcript as an empty idle transcript | E.`C514_Unproven_transcript_never_authorizes_Esc`: Esc count 0 and explicit withheld reason |
| PC-37 | Remove recovery working predicate after successful bound catch-up | E.`C514_Fresh_working_menu_never_receives_automatic_Esc`: Esc count 0 despite all other eligibility predicates passing |
| PC-38 | Reuse pre-lock actionable grid instead of fresh in-lock observation | E.`C514_Grid_is_reobserved_after_waiting_for_delivery_lock`: menu-to-partial/clear while waiting -> Esc count 0 |
| PC-39 | Move Esc-intent commit after conditional Esc call | E.`C514_Esc_intent_failure_has_zero_side_effects`: failing save yields 0 Esc backend calls |
| PC-40 | Reset Esc allowance on coordinator construction/reload | E.`C514_Possibly_sent_Esc_is_not_repeated_after_restart`: cumulative Esc count exactly 1 across three service recreations |
| PC-41 | Set `trackManualTurn: true` for maintenance Esc | E.`C514_Automatic_Esc_does_not_create_manual_turn_evidence`: manual-turn counter and new transcript activity count = 0 |
| PC-42 | Return DismissedVerified after first clear read | E.`C514_Dismissal_needs_two_separated_clear_observations`: result remains unverified before second read at >=500ms |
| PC-43 | Convert post-read exception/deadline to verified/empty-screen success | E.`C514_Postread_failure_or_deadline_remains_unverified`: verified timestamp null, result DismissUnverified |
| PC-44 | Remove G equality check between before/after clearance reads | E.`C514_Clear_frames_from_replacement_do_not_verify_old_Esc`: old E not DismissedVerified when second clear names B |
| PC-45 | Remove only open-episode unique-index creation from C514 migration | P.`C514_Concurrent_open_episode_insert_is_database_constrained`: second independent same-G open insert throws 23505 |
| PC-46 | Overwrite FirstObservedAt on every repeated observation | P.`C514_Episode_identity_and_first_observation_are_immutable`: E ID and FirstObservedAt equal original after 100 observations; no inferred Q |
| PC-47 | Assign rendered screen to incident diagnostic/message during episode projection | P.`C514_Receipts_exclude_sensitive_payloads`: serialized DB receipts/detail contain none of the screen/body/URL/state sentinels |
| PC-48 | Emit incident for every unchanged sweep | P.`C514_Only_meaningful_episode_transitions_emit_incidents`: unchanged 100-sweep episode has one detection transition receipt |
| PC-49 | Filter open episode projection by recent-incidents `since` | A.`C514_Open_episode_survives_incident_pruning_and_since`: fresh Attention has exactly one E item after pruning and cutoff advance |
| PC-50 | On verified modal resolution stamp associated pending work Delivered | V.`C514_Menu_resolution_is_not_work_delivery`: no Delivered verdict and no new UserPrompt before matching recipient record |
| PC-51 | Skip modal barrier in Now/SendNow admission | V.`C514_Now_and_SendNow_wait_behind_open_modal`: backend byte count 0 for both API paths |
| PC-52 | Skip modal barrier in pending WhenIdle flush | V.`C514_WhenIdle_flush_waits_behind_open_modal`: pending work input count 0 while bound idle/menu open |
| PC-53 | Move modal barrier below local-command fast return | V.`C514_Local_command_fast_path_obeys_modal_barrier`: no command body/Enter for `/rename` while open |
| PC-54 | Omit modal check in boot work/launch-note dispatch | L.`C514_Boot_work_and_notes_wait_behind_open_modal`: original work/note body count 0 before clear |
| PC-55 | Skip modal barrier in interrupted Enter-only branch | I.`C514_Interrupted_composer_Enter_waits_behind_modal`: primed body plus open E emits no Enter |
| PC-56 | Return ordinary NoSubmitOutput instead of modal-blocked outcome | V.`C514_Blocked_work_spends_no_attempts_and_never_kills`: DeliveryAttempts unchanged, Parked=false, kill count 0 after repeated sweeps |
| PC-57 | Redeliver before running late transcript confirmation after barrier release | I.`C514_Clearance_late_confirmation_precedes_redelivery`: matching persisted UserPrompt yields zero bytes and LateConfirmed on original Q |
| PC-58 | Resolve E on any transcript UserPrompt/queue removal | V.`C514_Unrelated_prompt_or_queue_removal_cannot_release_modal`: E still open and held work emits zero bytes |
| PC-59 | Add replay queue rows from Claude QueueEnqueue records after clear | N.`C514_Internal_queue_drain_creates_no_replay_request`: new Antiphon Q count 0; each child prompt once |
| PC-60 | Skip persisting original work-to-RunAttempt/G linkage when boot defers | L.`C514_Deferred_original_work_is_durable_before_launch_yields`: fresh context finds exactly one original W with complete body |
| PC-61 | Allocate new work Q instead of coalescing W on each recovered continuation | K.`C514_Two_recovery_workers_resume_one_original_work`: one W/Q and one submitted original body |
| PC-62 | Start first-output timer before deferred submission | L.`C514_Deferred_boot_has_no_first_output_timeout_or_success`: after >FirstDeltaTimeout with barrier open, timer start null and phase neither TimedOut nor Succeeded |
| PC-63 | Remove unresolved/possibly-written maintenance exclusion from queue retention | D.`C514_Retention_keeps_ambiguous_arm_veto`: original ambiguous Q remains and still vetoes another automatic arm after configured prune |
| PC-64 | Fall back to SendInputAsync when conditional capability/route is unsupported | Y.`C514_Conditional_transport_never_falls_back_to_raw_input`: recording HTTP handler raw `/input` POST count 0 for absent capability and 404 |
| PC-65 | Compare conditional expected generation with runner StartedAt instead of AcceptedStartedAt | B.`C514_Input_uses_microsecond_accepted_generation`: valid accepted G with different runner StartedAt reaches backend once; mismatch rows reach zero |
| PC-66 | Remove expected output-sequence comparison before backend write | B.`C514_Stale_sequence_refuses_with_zero_backend_bytes`: stale snapshot request writes 0 bytes and returns StaleObservation |
| PC-67 | Release runner launch gate after validation and re-resolve by ID at write | B.`C514_Replacement_cannot_enter_between_validation_and_write`: replacement cannot cross the held write gate; replacement child receives zero old-generation bytes |
| PC-68 | Treat missing/exited/unsupported conditional destination as Written | B.`C514_Nonwritable_destinations_have_distinct_refusals`: expected Missing/Exited/Unsupported result, backend bytes 0 |
| PC-69 | Send submitting Enter through raw input after a successful guarded body | T.`C514_Every_maintenance_phase_is_guarded_and_stops_on_mismatch`: replacement/stale phase gets no raw call or later phase; arm-body, Enter and Esc rows all recorded conditionally |
| PC-70 | Map lost/timeout HTTP write response to a positive zero-byte refusal | Y.`C514_Lost_conditional_reply_is_unknown`: result Unknown and no retry-safe flag despite forwarded backend write |
| PC-71 | Fill missing snapshot AcceptedStartedAt from expected DB generation | W.`C514_Old_runner_snapshot_is_generation_unproven`: observed generation null, reason ObservationGenerationUnproven, Esc count 0 |
| PC-72 | Rebind old episode/intent to new accepted generation on replacement | P.`C514_Generation_replacement_ends_old_episode_without_transfer`: old E resolution GenerationEnded; B has no inherited intent/barrier/success |
| PC-73 | Keep modal barrier active even after fresh proven external clearance | E.`C514_Fresh_external_clear_releases_original_work`: original pending Q reaches complete UserPrompt after ObservedClear |
| PC-74 | Always select Warning for channel-bound modal episode | A.`C514_Channel_bound_modal_is_immediately_critical`: first Attention item severity Critical before any queue age threshold |
| PC-75 | Leave generic QueuedInputStuck in projection while specific E is open | A.`C514_Specific_modal_dedupes_attention_but_keeps_history`: one symptom item while open; generic historical incident still exists and returns when appropriate |
| PC-76 | Keep explicit episode/intent transaction open across conditional runner await | E.`C514_Intent_is_committed_without_holding_transaction_during_IO`: independent context reads intent and can update unrelated episode while runner gate held; intercepted CurrentTransaction is null at I/O |
| PC-77 | Treat any RC setup degradation as modal-blocked work | L.`C514_Unknown_rc_without_menu_preserves_best_effort_work`: complete original work UserPrompt appears with session Running, despite withheld RC |
| PC-78 | Replace complete per-message transcript match with prefix or any-UserPrompt match | I.`C514_Only_matching_complete_prompt_confirms_each_message`: other-tail/truncated/old-G/old-baseline/other-session records leave Q unconfirmed; exact prompt alone confirms it |
| PC-79 | On final receipt-save failure clear intent or publish completion anyway | P.`C514_Final_receipt_commit_failure_remains_recoverable`: new context retains unresolved intent, zero published completion; recovery finalizes once from observations |
| PC-80 | Swallow reservation/link-save failure then report handoff accepted/release launch owner | K.`C514_Failed_enqueue_cannot_drop_launch_responsibility`: no successful deferred handoff without durable W; original work remains owned/recoverable and byte count 0 |
| PC-81 | Label externally observed clear as DismissedVerified even without Esc intent | E.`C514_Clear_without_Esc_is_ObservedClear`: resolution ObservedClear and sent/verified-Esc times null |
| PC-82 | Remove working early return from generic `QueuedInputWatchdogService.EvaluateSessionAsync` | J.`Working_session_is_suppressed`: sweep result 0 and incident list empty for long ordinary working turn |
| PC-83 | Ignore global Enabled/caller cancellation before starting modal sweep | W.`C514_Shutdown_stops_modal_background_work`: runner read count stops at cancellation boundary; disabled global settings produce 0 reads |
| PC-84 | Send released multiline work as body plus CR in one unwrapped write | V.`C514_Multiline_release_keeps_LF_paste_and_separate_Enter`: exact backend writes are one LF-normalized bracketed body and one separate CR; child full-body receipt is additionally checked by R-14 |
| PC-85 | Omit explicit origin from boot/health maintenance construction, allowing default Ui | Q.`C514_Boot_and_health_keep_their_explicit_origins`: fresh DB reload has System for boot and Supervision for health |
| PC-86 | Resolve/create a new episode on every unchanged observation instead of updating existing E | P.`C514_Repeated_observation_keeps_the_same_episode_id`: all receipt references and reloaded open episode retain initial E ID |
| PC-87 | Associate any menu with latest RC-looking queue row despite absent causal linkage | P.`C514_Unattributed_menu_keeps_related_request_null`: related Q null for independent manual/external menu even with nearby RC rows |
| PC-88 | Transition RunAttempt to Succeeded when returning modal-deferred launch | L.`C514_Deferred_run_cannot_report_success`: fresh RunAttempt phase is nonterminal and CompletedAt null before original UserPrompt |
| PC-89 | Apply normal resolved-episode age cutoff to open episodes too | D.`C514_Open_modal_survives_episode_retention`: aged open E still exists and fresh Attention still names E; resolved aged control is pruned |
| PC-90 | Remove deferred-work reference exclusions in work/session retention | D.`C514_Deferred_work_retains_its_attempt_and_session`: W/Q, RunAttempt and session all survive while pending; eligible resolved control is pruned |
| PC-91 | Emit RcReArmed/ArmedObserved as soon as maintenance reservation is accepted | Q.`C514_Arm_receipt_requires_observed_execution`: busy Requested has no ArmedObserved/RcReArmed; pre-existing Armed suppression has no attributed success |
| PC-92 | Replace automatic dismissal payload `"\u001b"` with `"\r"` | E.`C514_Dismissal_writes_only_one_Esc_payload`: recorded maintenance payloads equal exactly one Esc and contain no Enter/navigation/work body |
| PC-93 | Set post-arm observation deadline from Q.CreatedAt instead of SubmissionStarted/execution time | Q.`C514_Postflight_window_begins_at_execution`: request held busy beyond settle duration still gets full postflight window after actual submit; delayed menu E is recorded |
| PC-94 | Replace stored deferred W generation with current session G on resume | K.`C514_Old_deferred_work_never_targets_replacement`: replacement adapter receives no old W body and old W is not reported delivered to B |
| PC-95 | Swallow detection SaveChanges failure and proceed into dismissal | E.`C514_Detection_commit_failure_cannot_start_dismissal`: backend Esc count 0 and no dismissal intent/success published; next normal sweep redetects |
| PC-96 | Apply automated modal barrier to the raw operator runtime input path | E.`C514_Operator_raw_input_remains_available_during_modal`: explicit test-owned raw Esc reaches backend once while automated pending work remains held |

Mutation execution: for each row, save a source hash/copy, apply only the specified defect, build, run its exact method and inspect the named assertion in a fresh red TRX. Restore fixed source, refresh last-write timestamps, rebuild and run the same method into a different fresh green TRX. Zero cases, a compiler/fixture failure, or the wrong assertion is not red. Report every PC separately, including source-restoration evidence. No edits while a run is in flight. Tests for database uniqueness use a newly migrated isolated database on both halves so the removed index is actually absent on red. A production/test repair returns to separately commissioned Code/Review/land, not a mutation-only amendment. SourceLanding records remain in the assigned external evidence root and are not committed from its snapshot.

### Out of scope

- Reconstructing which historical injection opened September 13's untimestamped paint. Synthetic fixtures prove future attribution boundaries, not missing historical causality.
- Automatic Esc during a working turn, Disconnect/reconnect, navigation/Enter recovery, RC-driven kill/restart, or live-orchestrator reproduction. R-14's working scenario uses explicit test-owned operator clearance; it makes no busy automatic-Esc safety claim.
- Changes to CARD-0509 restart recovery, CARD-0512 Bash completion, non-RC supervisor actions, generic watchdog age policy, manual raw-input semantics, or provider home/settings. Regression tests preserve these boundaries; no production activation is authorized by this TestDesign task.
- Atomicity with Claude's own bridge/output after runner admission. B proves generation exclusion through the runner write and expected-sequence validation at its admission boundary. It cannot prevent the independent provider from opening a menu afterward; passive/postflight observation and the durable barrier are required for that race, as D-1 states.
- Arbitrary screen sizes below a usable menu grid and future provider menu formats. 80x24 and 120x30 plus fragmented/partial paints cover supported measured shapes; unrecognized forms remain non-actionable and cannot certify clearance. Unsupported/non-live candidates get zero modal-action calls; global shutdown stops the watcher.
- Claiming a remote/inaccessible state-file probe is Unarmed, or claiming current-generation evidence from an older runner. These remain explicit Unknown/unproven results, covered by R-2/R-12 rather than fabricated native coverage.
- Real-provider entitlement/authentication provisioning. V-9 needs the existing authorized headed account with RC available, and uses two harmless model turns. Report unavailable prerequisite as pending native acceptance. Fake/HTTP green does not close that requirement. A provider display change that prevents the promised conservative observation returns to Plan; it is not permission to loosen the matcher.
- A full browser/E2E stack and real SignalR push acceptance. This card adds no external notification channel or action; durable Attention GET/detail plus changed component rendering are its recipient path. The publication-failure test proves refresh recovery. MSW cannot establish real live invalidation, and no such claim is made.
- Redefining all weak short-prompt confirmation or all existing transport framing. Acceptance bodies exceed the 12-character text-identity minimum and are independently checked in full. PC-78/84 preserve the existing strong completeness/framing contracts where this feature resumes work.

### Cost

All execution costs below are **estimates**, not measurements or timeout increases. TestDesign ran no build, V/R, PC or native canary. Windows/modern ConPTY, staged runner/FakeClaude/worker executables, Docker/PostgreSQL, restored .NET packages, and available headed RC account are prerequisites. Missing authentication is not included as an arbitrary wait. Use bounded gate rendezvous and compressed application settings, retaining the production one-Enter/one-Esc counts and the measured native settle intervals. No long model turn or network sleep belongs in a deterministic test.

| Ordinary Code floor | Suites/filters | Minutes estimated |
|---|---|---:|
| Setup/database readiness/output inventory | Fresh test clone, owned roots, package restore | 4 |
| Build/staging | `dotnet build tests/Antiphon.Tests --property:OutputPath=bin-c514/ --nologo`, then the same command for `tests/Antiphon.SessionRunner.Tests` and `tests/Antiphon.Agents.Pty.Tests` | 8 |
| Unit | Antiphon.Tests `/*/*/*/*[Category=Unit]` once | 2 |
| Application integration | Named V-2/V-4/V-5/V-6/V-7 classes, including their PC-linked regression methods | 45 |
| Native producer-to-recipient | N exact class, R-14 busy/idle and fault argument rows; no simultaneous Pty test assembly | 18 |
| Runner/wire | Named V-3 classes including new B; real HTTP route, generation races | 8 |
| Pty contracts/replay | F, M's VT replay support in TerminalScreenTests, changed golden replay cases | 12 |
| Real-provider acceptance | C exact V-9 method, two harmless model turns, no skip counted green | 4 |
| Client | Two R-16 files through `test-client.ps1` | 2 |
| **Targeted ordinary V/R subtotal** | **2 + 45 + 18 + 8 + 12 + 4 + 2** | **91** |
| **Setup/build + targeted ordinary floor** | **12 + 91** | **103** |

The delegate instruction to run the full suite once is separately budgeted, not hidden in the 103-minute targeted figure. Reserve **278 additional minutes** for a one-time pre-change/default-suite baseline: Antiphon.Tests **180**, Antiphon.SessionRunner.Tests **45**, Antiphon.Agents.Pty.Tests **45**, client **8**, excluding unrelated opt-in live-provider tests. These are conservative planning estimates; the testing owner explicitly says a current full wall time is unmeasured and the old 25.5-minute figure is obsolete. Chunk Antiphon.Tests by discovered namespace using the documented form `/*/Antiphon.Tests.Application/*/*`, then every other discovered namespace, and reconcile the fresh TRX union against the assembly's case inventory. The broad exception is the delegate's explicit once-only requirement and this card's cross-cutting queue/launch/runner/persistence change. Do not repeat the full baseline after a fix; run affected classes. If that once-only baseline was already executed on the exact dispatch base, cite its SHA/fresh TRX and deduct 278 minutes rather than rerunning it. **Ordinary Code floor with that baseline outstanding = 381 minutes.** It is a budget, not an automatic kill deadline or proof those suites are green.

| Separate Mutation floor | Exact-method cycles | Minutes estimated |
|---|---|---:|
| Snapshot/output setup, initial build, ordinary baseline spot-check, final restoration audit | No full-suite repeat; external evidence inventory | 14 |
| Unit/wire PCs | PC-26/27/64/69/70: 5 cycles x (1.0 red rebuild + 0.2 red execution + 1.0 restored rebuild + 0.2 green execution) | 12 |
| Database/application PCs | Remaining 86 application controls excluding PC-59: 86 x (1.0 red rebuild + 1.0 red execution/recovery + 1.0 restored rebuild + 1.0 green execution/recovery) | 344 |
| Native PCs | PC-59/65/66/67/68: 5 x (1.0 red rebuild + 2.0 red execution + 1.0 restored rebuild + 2.0 green execution) | 30 |
| **Every PC break/red/restore/green** | **96 cycles; 192 executed method selections; 12 + 344 + 30** | **386** |
| **Mutation total** | **14 + 386** | **400** |

**Total verification floor = setup/build 26 + ordinary V/R 91 + every PC 386 = 503 minutes** when a valid exact-base full-suite baseline already exists. **With the required one-time broad baseline outstanding: 503 + 278 = 781 minutes (13h 1m).** Ordinary Review's independent evaluation and production activation are separate stages, not counted as test runtime. Provider/setup failures and inherited-red investigation are reported as additional actual cost; they are not a reason to relax assertions. TestDesign's measured execution count is zero tests, zero builds, zero PC cycles; document-only validation is recorded in its commit/report.

Savings are from method-scoped PC execution and one build per ordinary project, not omitted guards. Compared with repeating each PC's whole project twice, the estimated alternative is 92 Application-project controls x 2 x 180 minutes + 4 Runner controls x 2 x 45 minutes = **33,480 minutes**. The 386-minute method cycles save **33,094 minutes (98.85%)** against that deliberately broad alternative; this is estimate arithmetic, not a benchmark. Ordinary targeted follow-ups avoid another 278-minute full baseline per fix. Concurrency savings counted: **0 minutes**, because 96 controls largely share queue/recovery files and sourced snapshots permit only the assigned tree; assemblies run sequentially. Live-provider PC cycles: **0**, justified because all safety guards have deterministic method controls and current-provider behavior is separately required once by V-9, not mutated against an external service.

Before Code handoff: inspected bodies and nearest helpers are listed above; **guards=96, mapped=96, missing=0, duplicate PC mappings=0**. All controls have a compiling production/migration defect, an exact required method, an assertion-red oracle and a restored-green command policy. Setup gaps are assigned concrete helper/fixture deliverables, including real ingestion and the owned crash worker. No unverifiable delivery seam is accepted. Code may begin after this TestDesign document is landed; it must implement and run V/R, retain pending native acceptance if V-9 cannot run, and return to Plan if the guarded transport cannot meet the specified runner boundary. Mutation runs only after ordinary Review and confirmed source landing.
