# CARD-0758: recover a queued Check after a standing session restart

## Verified findings (2026-09-26)

- The failure is present on current `origin/master` `2404f17a2b5a66d000d967237753af7bd77a56f1`. The deployed server reports `4e873cf20a61b0475f9a013b57168e6ecdd7aea5`; the relevant dispatcher, producer, Check selector and identity test files have no diff between that SHA and this checkout. CARD-0758 has no content revisions after creation.
- The production Check is on the **legacy** path: `AgentTaskCheckService.cs:474-487` activates the request graph only for an enabled declared routing with a qualified candidate; otherwise `:493-510` calls `SpecialistTaskRunner.RunAsync`. Live `GET /api/agents/be5d4502-89df-4412-8ebb-26c1e9171021/specialist-routing` has no enabled declaration or qualified candidate. The card's claim that one permanent specialist task remains pinned is stale: `SpecialistTaskRunner.cs:309-350` creates a fresh task per Check and snapshots the agent's current persistent session ID, `StartedAt`, effective model and profile revision.
- Live task `19f001a5` was **created at 14:53:33Z**, but its Created event recorded session `cea73d57` with `StartedAt=11:24:07Z`. It was held behind another Check and then for no live session; at 14:54:30Z it failed when the same session ID had `StartedAt=14:54:22Z`. Thus the runner/server recovery changed the generation while the task was queued. This supports a race, not a permanent pin. `AgentTaskDispatcher.cs:7257-7266` determines live status from the database row, and `:6562-6572` resolves the current standing session at dispatch.
- `AgentTaskDispatcher.cs:6587-6609` first checks kind, tier, exact model, effective model and alias, then rejects any session ID, start time **or** profile revision difference as `SpecialistIdentityMismatchException`. `:904-913` turns it into a terminal failure before session assignment, and `SpecialistTaskRunner.cs:273-281` returns the reason to the degraded Check. This guard was intentional for CARD-0415 (`docs/agent-kinds.md:556-563`) and must remain for routed/typed attempts: `SpecialistRequestService.cs:225-243` and `SpecialistInputPolicy.cs:60` bind those to an authorized generation.
- The exact failure text does **not** distinguish a generation change from a profile change. The observed old and new `StartedAt` values prove a generation change; no evidence here proves profile revision drift. `SpecialistExecutionIdentityTests.cs:23-41,96-145` currently expects **all** generation drift to fail, including legacy tasks. That expectation needs a Check-specific amendment, not a broad removal of identity protection.
- The outage is no longer active at inspection time. Live agent `be5d4502` is Running on `cea73d57`, now started at `15:28:45Z`. The 25 Check tasks created after that start through `16:52Z` all Succeeded; the latest, `dae94df4`, has a nonempty reading. The card's “every check digest” statement is stale. No live restart is warranted now. The server SHA is older than current master, so verify deployment SHA separately when assessing future incidents.

## Operator recovery available now

1. Read `GET /api/agents` for slug `antiphon-check-interpreter` and `GET /api/agent-tasks?includeChecks=true&since=<UTC>` (then filter its `.items` to that agent and Role=Check). A stable current `liveSession.startedAt` plus new Succeeded Check rows means the race has already cleared, as it has today. Do not restart a healthy seat merely to clear an old digest.
2. If new Checks continue to fail or the seat has no usable live session, and it is idle with no owned Working/Dispatched Check, use the named-agent front doors: `POST /api/agents/be5d4502-89df-4412-8ebb-26c1e9171021/stop` with `{}`; then `POST /api/agents/be5d4502-89df-4412-8ebb-26c1e9171021/start` with `{"fresh":true}`. Verify a new Running `liveSession.startedAt`, then a **new** Check result. `docs/ops-http.md:78,160-162,233-236,306-311` and `AgentControlService.cs:175-177,1139-1165` establish the lever: Fresh is refused while live, Stop kills and suspends AlwaysOn supervision until manual Start. An already failed Check is terminal and its old digest is not rewritten.

## Smallest fix

**S1 — dispatch-time re-resolution of a queued legacy Check.** In `PlaceOnStandingAgentAsync` (`AgentTaskDispatcher.cs:6555-6634`), retain the existing kind/model/alias checks and the terminal profile-drift guard. When the sole mismatch is session ID or `StartedAt`, allow a queued **legacy Check** on the configured primary interpreter (`Role=Check`, `SpecialistInputPolicyJson == null`, owned standing seat) to adopt the current Running session only if its profile revision still equals the task's snapshot, its effective model and seat identity still match, and the optional deadline has not expired. Under the existing claimed-task transaction, refresh `SpecialistSessionId` and `SpecialistSessionStartedAt`, record a Warning event with old/new generation, and continue through the existing occupancy and delivery path. A changed profile revision, model, kind, tier or alias still fails loudly. Do not rebind a task with `SpecialistInputPolicyJson`: routed attempts also bind an authorization, fingerprint and complete-input transport policy to the original generation; they need their own request-graph retry design.

**S2 — focused regression.** Extend `SpecialistExecutionIdentityTests` with three argument cases for a legacy Check: same session ID/new `StartedAt`, replacement session ID/same approved profile, and changed profile revision. The first two must dispatch and create exactly one queued message on the current session, with none on the old generation; the third must fail before input. Keep existing Codex, model, tier, live-model and public-kind refusals. Exercise both the no-deadline `RunAsync` and deadline policy as appropriate; a terminal or expired task must never be rebound. A new test is useful only if its first two cases go red on the current guard and green after S1.

## Verification design

V-1: same-ID restart and replacement-ID restart re-resolve only a queued legacy Check and deliver exactly once to the current session. V-2: changed profile or execution identity still refuses before input, including routed typed-policy attempts. V-3: expiry and occupancy retain their existing behavior. R-1: all existing `SpecialistExecutionIdentityTests` cases stay green; the old broad `started` expectation remains for non-Check/Codex cases. TestDesign should settle the exact fixture shape and confirm the class's executed roster before Code. The checkpoint below is the closed ordinary Code list; run it once after S1-S2 are committed with `dotnet run --project tools/Antiphon.Checkpoints -- run --plan docs/superpowers/plans/2026-09-26-card-0758-plan.md`, then `wait` until exit is not 75. The tool owns the build slot and isolated output. No extra builds or runs without a stated reason.

### Checkpoints

| CP | After | Build | Group | Filter | Covers | Expect | Min | EstimatedMinutes |
|---|---|---|---|---|---|---|---:|---:|
| CP-1 | S1-S2 | `tests/Antiphon.Tests -> bin-c758/` | specialist-identity | `/*/*/SpecialistExecutionIdentityTests/*` | V-1, V-2, V-3, R-1 | all 48 results: 15 retained + 33 TD-01 through TD-12 arguments; 0 failed/skipped | 48 | 6 |
| CP-2 | S1-S2 | `tests/Antiphon.Tests -> bin-c758-transport/` | specialist-transport | `/*/*/SpecialistInputTransportTests/*` | V-1, V-2, V-3, R-1 | all 16 results: 12 retained + 4 TD-13 arguments; 0 failed/skipped | 16 | 6 |

## Risks and cost

- A database Running row can lag the runner during restart. Keep the present queue's generation check at delivery; if the session changes again after dispatch, typed delivery must refuse or retry safely, never write into an unverified composer. This plan does not claim a native-process end-to-end proof from the database fixture.
- A profile revision may encode changed instructions, tool access or transport. It is not safe to treat it as a cosmetic restart stamp. The observed incident establishes only generation drift; profile drift retains the typed refusal until equivalence or fresh-work semantics are designed.
- Rebinding must occur once under the existing task lock and before any dispatch/message intent; re-evaluate deadline and seat occupancy. Do not resurrect terminal Checks, create another task, reset a routed candidate's qualification, or alter the old digest.

Ordinary cost: **26 minutes** implementation and test authoring + **12 minutes** CP-1/CP-2 + **6 minutes** Review = **44 minutes**. Positive-control floor: **3 controls**, each one red and one restored-green exact-method execution, **6 method executions / about 9 minutes**, separate from ordinary Code. PC-1 remove the new re-resolution branch: same-ID restart must fail its Dispatched/current-message assertion. PC-2 permit profile drift: profile-refusal case must fail its no-input assertion. PC-3 bypass the existing model guard: the retained model-drift case must fail its identity-refusal assertion. Total planned work floor **53 minutes**; measurements supersede estimates.

## Test design

This section supersedes the preliminary roster, three-control inventory and cost above. S1's recovery boundary is unchanged. Only this document changes in TestDesign; the methods below are Code work. The single earlier `### Checkpoints` table is authoritative: `PlanTableImporter.ExtractSection` selects the first heading with that exact name, so do not append a competing table. Its filters and output paths remain the plan's two class-level checkpoints.

### Inspection and fixture decisions

Bodies inspected at `b96ed0790670d6fe69267f3d908ac253e4ee3178`:

- `tests/Antiphon.Tests/Application/SpecialistExecutionIdentityTests.cs`: producer snapshots, eleven argument cases, two wrappers and the legacy Codex canary; **15 source-expanded results**, not fifteen distinct methods.
- `tests/Antiphon.Tests/Application/SpecialistInputTransportTests.cs`: full inline UTF-8, no spill/batching, complete receipt, warm transcript pull and reload refusal; **12 source-expanded results**.
- `tests/Antiphon.Tests/Application/AgentTaskStandingAgentDispatchTests.cs`: `CreateHarness`, busy/current-session ownership, dead-generation exclusion and standing-row preservation. `AgentTaskReuseEnqueueTests.cs`: the pre-insert fault seam and its durable incident. `SpecialistTaskRunnerDeadlineTests.cs`: polling and a real-clock-offset fixture.
- `tests/Antiphon.Tests/TestHelpers/BridgeQueueHarness.cs`: isolated `ConnectionString`, `AttachAgentId`/`AttachSessionId`, registered fake adapter, real queue, transcript insertion and reload. `DelegationTestServices` registrations remain the graph front door.
- `SpecialistTaskRunner.CreateRunTaskAsync`, `AgentTaskDispatcher.TickAsync`/`DispatchOneAsync`/`PlaceOnStandingAgentAsync`/`DeliverReuseMessagesAsync`, `CheckInterpreterProvisioner.Spec`, `StandingSpecialistSeatPolicy`, `SpecialistInputPolicy.RequireSession`, queue enqueue/reload/receipt checks, and profile/revision EF constraints.
- `tools/Antiphon.Checkpoints/Manifest/PlanTableImporter.cs` and `ManifestValidator.cs`: nine-column schema, isolated output grammar, count and class-roster extraction. No test discovery, build or test was executed in TestDesign; Code must report actual TRX counts.

Use an isolated schema per new identity case. Create a real `AgentTuiProfile` and two `AgentTuiProfileRevision` rows P0/P1 (distinct positive revision numbers, valid JSON, subscription authentication, no secrets), then reference P0 from the live session. An arbitrary revision GUID without its FK row is an invalid fixture. Save/reload timestamps before capturing T0; mutate to a distinct database-round-tripped T1, never compare a pre-save nanosecond timestamp. Seed T0 two minutes in the past and T1 one minute in the past so a real-clock receipt can be after T1 without sleeps. Seed ClaudeCode/High with explicit `sonnet` on both the agent and session. Keep a small multiline fact bundle below ordinary legacy spill limits.

Create the queued task through the real `SpecialistTaskRunner`: TD-01 uses `RunAsync` (assert `ExecutionDeadlineAt == null`), TD-02 uses `RunWithPolicyAsync` (assert the original future deadline survives). Use a unique configured `CheckInterpreterAgentSlug`, the same settings in the spec and dispatcher, `AlwaysOn=true`, `IsPoolDelegate=false`, and the same `AgentId` throughout. TD-01 uses the compatibility primary with both standing-ownership fields null; TD-02 uses `StandingSpecialistRole=Check`, `StandingSpecialistOwnerId=seat.Id`. A foreign owner or an explicit non-Check seat role is never compatibility-primary evidence. Read the producer's snapshot before mutating the database through a separate context. Do not modify existing Codex fixtures to make them eligible.

For new cases use a local fixture/helper in the identity test file, callable by the transport class. Attach `BridgeQueueHarness` to the seeded current session with the isolated connection string; register the dispatcher and its existing standing-harness dependencies through `ConfigureServices` and `AddDelegationWorktreeGraph`. Supply the same `DelegationSettings`, disable unrelated role timeouts/sweeps as the standing harness does, and resolve the dispatcher in a fresh scope. Do not boot `Program`, provision a real interpreter, or contact a production runner. `BridgeQueueHarness.CreateContext()` is the shared database: use `new AppDbContext(TestDbFixture.CreateDbContextOptions(h.ConnectionString))` for every new isolated read/write instead. Dispose providers and cancel/await the producer's pending task in `finally`. Use `TimeProvider.System` or an offset over the real clock, never a frozen queue clock.

"Seat replaced" here means its **session** was replaced: the standing agent still owns the work. It does not authorize moving a queued task to another Agent row, qualifying a fallback, or resetting a routed request. TD-02 stops S0, adds Running S1 with P0 and the same execution identity, then changes only the standing agent's persistent-session pointer. Include an older Dispatched task on S0 to prove it does not occupy S1.

### Exact assertion vocabulary

These are outcome assertions, not comparisons of a helper to itself. All queries are scoped by the fixture task/session IDs; capture the baseline agent/session/task roster after deliberate fixture edits and before dispatch.

- **A (adopt):** the original task is `Dispatched`, `FailureCode == null`, `AgentId == seat.Id`, `AgentSessionId == current.Id`, `SpecialistSessionId == current.Id`, `SpecialistSessionStartedAt == current.StartedAt`, `SpecialistProfileRevisionId == P0`. Kind, tier, model/alias/effective-model snapshots, goal, deadline, root ID and input-policy null remain exactly as captured. No additional Agent, AgentSession or AgentTask is created. The standing Agent row equals its pre-dispatch snapshot. After a second fresh dispatcher tick, exactly one `Dispatched` event and exactly one queue row with `ExecutionTaskId == task.Id` exist; the row targets current.Id, has the complete expected brief and `Origin == Delegation`. TD-02 has zero such messages on S0. For TD-01 S0 and current have the same ID: zero old-generation input is established by zero rows/submits before restart, persisted T1 snapshot, and a receipt after T1, not by the impossible assertion that the shared session ID has zero messages.
- **W (audit):** exactly one task `Warning` describing generation adoption contains both full old/new session GUIDs and round-tripped old/new start timestamps; assert these values, not an entire English sentence. The warning is visible with the adopted snapshot and does not multiply on a second tick. Profile drift/refusal and rolled-back occupancy create no adoption warning.
- **F (refuse):** `Status == Failed`, `FailureCode == SpecialistIdentityMismatch`, `AgentSessionId == null`, original specialist session/start/profile snapshots unchanged, zero task queue rows and zero adapter inputs/submitted bodies; no adoption warning. Await the real producer and assert `Outcome == IdentityMismatch`, nonempty reason containing the seat name. This verifies the caller-facing refusal too.
- **Q (wait):** `Status == Queued`, `AgentSessionId == null`, captured specialist snapshot unchanged, zero task queue rows/submits and no adoption warning. A later eligible tick must satisfy A/W; a hold must not persist a partial rebind.
- **U (receipt):** exactly one complete normalized brief appears as a `TranscriptKinds.UserPrompt` on the current session after the recorded baseline sequence and T1; its marker is this task's marker. Queue `DeliveryVerdict == Delivered`, stored body equals the complete expected brief, `SubmittedBodies == [expected]`, and `Inputs == ["\u001b[200~" + expected + "\u001b[201~", "\r"]]`. Queue insertion, `Dispatched`, Warning, screen redraw and fake-adapter submission alone do not establish U.

### Proves it works now and guards the regression

File abbreviations in **every row**: **I** = `tests/Antiphon.Tests/Application/SpecialistExecutionIdentityTests.cs`; **T** = `tests/Antiphon.Tests/Application/SpecialistInputTransportTests.cs`. Arguments below are the complete executed roster, not loops counted as tests. A row's PC references identify the compiling production change that must make its exact assertion red; mutation details follow.

| Row / coverage | File and exact method (arguments; result count) | Setup and decisive assertions | Red-producing production change / checkpoint | Classification |
|---|---|---|---|---|
| TD-01 / V-1 | I: `Card0758_legacy_Check_restart_profile_matrix("started", false)`; 1 | Queue with S0/T0/P0; same ID now Running at T1/P0. A, W and U; null deadline. | PC-1 removes recovery; PC-22 removes audit; PC-23 duplicates enqueue. CP-1. | New acceptance; red on base at `Status == Dispatched`. |
| TD-02 / V-1,V-3 | I: `Card0758_legacy_Check_restart_profile_matrix("session", true)`; 1 | Replace S0 by S1/T1/P0 on the same typed primary; stale Dispatched owner stays on S0. A, W and U; original future deadline unchanged. | PC-1; PC-21 incorrectly counts S0 occupancy; PC-23. CP-1. | New acceptance; red on base at `Status == Dispatched`. |
| TD-03 / V-2 | I: `Card0758_legacy_Check_restart_profile_matrix("profile", false)`; 1 | Same ID with T1 **and P1**, otherwise identical. F, including original P0 retained. | PC-2 admits profile drift. CP-1. | **Guard-only**; base already refuses. |
| TD-04 / V-2 | I: `Card0758_profile_revision_changes_refuse_before_input(string change)` with `"profile-only"`, `"add"`, `"remove"`; 3 | P0→P1, null→P1, P0→null, with unchanged session ID/start; F using that case's original profile. | PC-2 removes profile comparison, including profile-only drift. CP-1. | **Guard-only**; equality is null-safe, not "both nonnull". |
| TD-05 / V-2 | I: `Card0758_typed_Check_drift_is_never_rebound(string drift)` with `"started"`, `"session"`, `"profile"`; 3 | Otherwise eligible primary; valid serialized full-inline policy bound to S0/T0/task, with nonempty synthetic fingerprint and sufficient byte budget. Change one field only. F plus byte-identical task policy/fingerprint and zero queue rows. | PC-3 allows typed generation rebinding; profile case belongs to PC-2. CP-1. | **Guard-only**; never rewrite the policy to make this pass. |
| TD-06 / V-2,R-1 | I: `Card0758_only_the_owned_primary_legacy_Check_can_rebind(string exclusion)` with `"task-role"`, `"slug"`, `"owner"`, `"seat-role"`, `"always-on"`, `"pool"`; 6 | Start from eligible stale T0→T1 and change only: task Role to Diagnose; slug away from configured primary; owner to another seeded Agent; seat role to Diagnose; AlwaysOn to false; or IsPoolDelegate to true with Status Running/no idle timestamp. First five assert F; pool asserts Q and no session launch. | PC-4 through PC-9, one exclusion each. CP-1. | **Guard-only**; no broad specialist/Codex exception. |
| TD-07 / V-3 | I: `Card0758_restart_waits_for_a_Running_generation(SessionStatus state)` with `Starting`, `Stopped`; 2 | Current generation has same profile but is not Running. Q; change to Running, then A/W/U. | PC-10 treats Starting as eligible. CP-1. | Starting is new acceptance (base fails); Stopped is **guard-only**. |
| TD-08 / V-3 | I: `Card0758_expired_Check_is_never_rebound(string boundary)` with `"before-tick"`, `"after-snapshot"`; 2 | Give a producer-created task a deadline just in the past before Tick, or use `AfterQueuedSnapshotAsync` to persist that expiry after selection, before claim. Assert hook fired where applicable; `Canceled`, nonnull `CompletedAt`, original snapshots, no message/input/adoption warning. | PC-17 bypasses optional expiry at admission, not merely one of its redundant checks. CP-1. | **Guard-only**. Avoid sleeps and the producer's policy timer competing with the test. |
| TD-09 / V-3,R-1 | I: `Card0758_nonqueued_Check_is_not_resurrected(AgentTaskStatus status)` with `Dispatched`, `Working`, `Blocked`, `Succeeded`, `Failed`, `Canceled`; 6 | After the tick's queued snapshot, write exactly the requested status through another context without changing its token. Assert hook fired; status/result/failure/completed fields equal that saved row, snapshots unchanged, zero new messages/adoption warnings/dispatch events. | PC-18 removes the locked claim's Queued predicate. CP-1. | **Guard-only**; exercises a stale selected task, not an already-filtered-out row. |
| TD-10 / V-3 | I: `Card0758_restarted_Check_waits_for_the_current_owner(AgentTaskStatus ownerStatus)` with `Dispatched`, `Working`; 2 | Another task owns the current session. First Tick: Q, including rollback of refreshed snapshot/W. Mark owner Succeeded and use a fresh dispatcher; second Tick: A/W/U. | PC-20 ignores current-session occupancy. CP-1. | New hold/recovery acceptance; base fails at Q due to stale identity. |
| TD-11 / V-2,R-1 | I: `Card0758_restart_keeps_every_execution_identity_guard(string drift)` with `"seat-kind"`, `"tier"`, `"exact-model"`, `"effective-model"`, `"alias"`; 5 | All have stale T0→T1. Change seat.Kind only, seat.ModelLevel only, task.SpecialistModelId only, task.SpecialistEffectiveModelId only, or task.SpecialistModelAlias only, respectively; F. Snapshot-only edits intentionally isolate a single comparison from alias normalization. Assert initial producer snapshots first. | PC-11 through PC-15. CP-1. | **Guard-only**; the new recovery branch must not short-circuit identity checks. |
| TD-12 / V-3 | I: `Card0758_changed_claim_token_leaves_the_Check_queued()`; 1 | `AfterQueuedSnapshotAsync` changes only ConcurrencyToken; assert hook fired, first Tick Q with the new token preserved, second fresh Tick A/W/U. | PC-19 drops the concurrency-token claim predicate. CP-1. | **Guard-only** claim refusal plus recovery. |
| TD-13 / V-1,V-3 | T: `Card0758_rebound_legacy_Check_reaches_the_recipient(string boundary)` with `"idle"`, `"busy"`, `"reload"`, `"enqueue-fault"`; 4 | Use real producer/dispatcher/attached queue, same-ID restart. Idle: A/W/U immediately. Busy: real working transcript, then A/W and exactly one Pending row but zero input; append TurnEnd and flush for U. Reload: stop at Pending as busy, recreate queue with `ActivatorUtilities`, append TurnEnd, flush for U. Fault: `ReuseEnqueueOverride` throws before insertion; A/W except queue-count/U, zero rows/input/receipt and one scoped `DeliveryTransportFailed` incident naming this task and "brief"/"was not queued"; a second Tick adds no duplicate dispatch. | PC-1; PC-32 removes incident persistence; PC-33 ignores persisted pending queue on reload; PC-34 bypasses busy-session deferral. CP-2. | First three are acceptance/recovery; fault is **guard-only** observability after adoption. |

The first fixture's Q/A definitions concern only its new task: TD-02 and TD-10 deliberately also contain an older owner task. Do not assert a database-global queue/task count. TD-06 changes the persisted task Role after capturing the real producer's snapshot; the generic runner still observes its terminal identity refusal by the original task ID.

### Existing tests affected and retained transport assertions

**No existing result is expected to turn red under S1's narrow eligibility.** All existing drift arguments are Codex tasks on `snapshot-seat`, with no primary ownership relation and a slug different from `CheckInterpreterProvisioner.Slug(settings)`. The unchanged-generation Codex canary on `identity-canary` also remains supported. A broad "all Check tasks may rebind" implementation would turn these three existing executions red:

| File / method | Existing arguments and assertion | Required amendment |
|---|---|---|
| I: `Card0415_V01_generation_and_public_pin_guards` | `"started"`: Failed/IdentityMismatch, zero messages, producer IdentityMismatch. | **None.** Preserve the wrapper and assertion; correct the production eligibility if it fails. |
| I: `Card0415_V01_execution_snapshot_and_dispatch_drift` | `(Codex,false,"started")`, `(Codex,true,"session")`: same refusal assertions. | **None.** Add TD-01/02 separately for the eligible primary rather than invert these expectations. |

The wrapper's direct call to another `[Test]` method adds no extra TUnit result. Preserve every Codex/model/tier/live-model/public-kind assertion, the non-low exact-model wrapper, and the successful four `"none"` matrix arguments. No baseline failure is waived. If a safe implementation actually changes an existing fixture's eligibility, document that concrete change and split its success case from the original refusal; do not delete the refusal branch or replace exact assertions with "not failed".

The following retained rows all run in CP-2, are **guard-only for CARD-0758**, and need no assertion amendment. They are also executable PC targets; each name is in **T**:

| Exact method / results | Exact retained assertion | Production defect that makes it red |
|---|---|---|
| `Card0415_V04_complete_utf8_envelope_survives_both_spill_layers` / 2 | `body == expected`, `SubmittedBodies == [expected]`, paste then separate CR, policy retained, `Delivered`, no `.antiphon` spill directory. | PC-26 enables ordinary brief spill for typed input. |
| `Card0415_V04_oversize_is_refused_before_file_or_submit_even_when_characters_fit` / 1 | Fit and enqueue throw `SpecialistInputUnsupportedException`; `Outcome == InputUnsupported`; zero adapter inputs/queue rows/spill files and original Goal. | PC-25 compares character count instead of UTF-8 byte count. |
| `Card0415_V04_reload_retains_policy_and_does_not_batch_Checks` / 1 | First flush submits `[Fit(first)]`; second submits `[Fit(first), Fit(second)]`. | PC-27 permits typed Check rows to batch. |
| `Card0415_V04_only_the_complete_current_UserPrompt_confirms` / 6 | For `middle`, `tail`, `extra`, `queued`, `screen`, `wrong-token`: verdict is neither Delivered nor LateConfirmed; body/policy unchanged; exactly one submitted body. | PC-28 incomplete text; PC-29 wrong transcript kind; PC-30 screen-only success; PC-31 second body on failed receipt. |
| `Card0415_V04_warm_native_pull_confirms_complete_input_when_live_stream_is_missing` / 1 | `Delivered`, nonnull baseline, matching persisted UserPrompt, `[body]` submitted. | PC-35 suppresses warm transcript recovery. |
| `Card0415_V04_generation_change_at_reload_refuses_before_submit` / 1 | Reload throws `SpecialistInputUnsupportedException`; `Inputs` empty. Add `SubmittedBodies` empty and byte-identical queued/task policy assertions; existing assertions stay exact. | PC-24 removes delivery-time start-generation comparison. |

### Delivery inventory and evidence limits

| Handoff and durable identity | Persistence / recovery | Evidence and test |
|---|---|---|
| `SpecialistTaskRunner` → queued Check, `AgentTask.Id` | Original snapshot + Created event saved before dispatch. Runner restart changes session, not task identity. The dispatcher selects/claims the durable Queued row. | TD-01/02 observe the producer-created row before independent restart edits; TD-08/09/12 prove expired/changed claims cannot become new work. |
| Claim → adopted standing session, task ID + session ID/start | Refresh and Warning join the existing claimed-task transaction. Busy admission rolls it back; later fresh-scope Tick resolves again. Commit precedes `DeliverReuseMessagesAsync`. | TD-10 Q then A/W/U is the rollback/recovery cut. TD-01/02 re-read fresh persisted values and repeat Tick to check single dispatch/enqueue. No new transaction/outbox mechanism is commissioned. |
| Committed dispatch → queue insert, `ExecutionTaskId` | `TryEnqueueReuseAsync` catches a pre-insert exception and saves `DeliveryTransportFailed`. Current code has **no automatic replay of a missing standing brief** from this seam. | TD-13 fault proves a visible failure and absence of receipt; it must not claim successful delivery or synthesize a retry. Process death in the commit-to-insert gap is an unchanged, unclosed reliability boundary, not fixed by generation rebinding. A durable outbox/replay redesign is outside S1. |
| Persisted Pending row → eligible/busy/reloaded composer, queue ID + ExecutionTaskId + current generation | Real queue retains Pending while busy and reloads durable input. Delivery-time typed policy still requires its authorized generation. | TD-13 idle/busy/reload → U; retained typed reload refusal → no input. This models loss of queue-service memory after insertion, without killing a process. |
| Composer → confirmation, task marker + whole normalized UserPrompt + session/sequence/time floor | Default attached adapter records a whole UserPrompt then TurnEnd. Damaged transcript and warm pull use the existing transport fixtures. | U and retained six negative receipts/warm-pull result. A fake native adapter proves dispatcher→real queue→transcript verification, **not** a real restarted PtyHost, provider authorization, model quality or live interpretation. |
| Refused dispatch → waiting producer, original task ID | Terminal failure row is polled by the real runner and returned as `SpecialistRunOutcome.IdentityMismatch`. | F in profile, typed and identity cases; no synthetic success/digest rewrite. Successful interpretation/settlement after input is unchanged and outside this dispatch fix. |

### Guard inventory and positive controls

Each G-n maps **1:1** to PC-n. Use the exact method's file from TD/retained rows above, and its class checkpoint as the ordinary owner. Every mutation is a temporary, compiling production defect in the landed snapshot; never weaken a test or mutate fixture data instead. Unless explicitly named, production locations are `server/Application/Services/AgentTaskDispatcher.cs`. Preserve PC numbers from this inventory, which replaces the preliminary three PCs.

| Guard → control | Production defect | Exact red target / decisive assertion |
|---|---|---|
| G-1 generation adoption → PC-1 | Remove the legacy recovery branch, restoring unconditional generation refusal. | I.`Card0758_legacy_Check_restart_profile_matrix`: started/session must fail A at Dispatched (2 intended reds). |
| G-2 profile equality, independent of generation → PC-2 | Omit all profile-revision mismatch rejection/eligibility checks while keeping generation recovery. | I.`Card0758_profile_revision_changes_refuse_before_input`: F status on all 3 profile boundaries. |
| G-3 typed authorization excludes rebind → PC-3 | Remove only the `SpecialistInputPolicyJson == null` recovery requirement. Do not rewrite policy or disable the queue check. | I.`Card0758_typed_Check_drift_is_never_rebound`: started/session wrongly become Dispatched; F fails before queue refusal can mask it. |
| G-4 task Check role → PC-4 | Remove Check-role requirement from recovery. | I.`Card0758_only_the_owned_primary_legacy_Check_can_rebind`: task-role F. |
| G-5 configured primary identity → PC-5 | Ignore the configured primary slug in recovery admission. | Same I method: slug F. |
| G-6 self-owned or unadopted primary, never foreign-owned → PC-6 | Accept a nonnull foreign StandingSpecialistOwnerId as primary. | Same I method: owner F. |
| G-7 explicit seat role remains Check → PC-7 | Accept an explicit non-Check StandingSpecialistRole. | Same I method: seat-role F. |
| G-8 supervised standing seat → PC-8 | Drop AlwaysOn from recovery admission. | Same I method: always-on F. |
| G-9 pool is not standing ownership → PC-9 | In `TryReuseWarmAgentAsync`, treat a pinned pool row as nonpool for this dispatch, including the recovery classification. | Same I method: pool Q/no-launch becomes adopted. |
| G-10 Running generation only → PC-10 | Widen recovery readiness to Starting. | I.`Card0758_restart_waits_for_a_Running_generation`: Starting Q fails at first tick. |
| G-11 seat kind → PC-11 | Delete `standing.Kind != claimed.AgentKind` from specialist identity rejection. | I.`Card0758_restart_keeps_every_execution_identity_guard`: seat-kind F. |
| G-12 tier → PC-12 | Delete standing/task ModelLevel comparison. | Same I method: tier F; explicit model keeps alias constant. |
| G-13 exact model → PC-13 | Delete standing.ModelId/snapshot comparison. | Same I method: exact-model F; only the captured exact-model field differs. |
| G-14 observed effective model → PC-14 | Delete live.EffectiveModelId/snapshot comparison. | Same I method: effective-model F; actual live alias still matches. |
| G-15 availability/model alias → PC-15 | Remove current-alias and live-model-resolved-alias comparisons to expectedAlias. | Same I method: alias F. These two checks enforce the same alias and both must be disabled to make the defect observable. |
| G-16 public kind pin without specialist snapshot → PC-16 | Remove the public live-session kind mismatch throw. | I.`Card0415_V01_generation_and_public_pin_guards`: public-kind loses Failed/IdentityMismatch; started remains refused. |
| G-17 deadline admission → PC-17 | Bypass optional Check expiry in the initial sweep, claimed-expiry helper and new recovery deadline predicate together. Keep all queue deadline checks. | I.`Card0758_expired_Check_is_never_rebound`: Canceled/snapshot assertions, before any queue expiry can hide erroneous dispatch. |
| G-18 still Queued under claim → PC-18 | Remove the Queued predicate from the locked `AgentTasks` claim query. | I.`Card0758_nonqueued_Check_is_not_resurrected`: saved status/snapshot equality. |
| G-19 same claim token → PC-19 | Remove the locked query's ConcurrencyToken equality. | I.`Card0758_changed_claim_token_leaves_the_Check_queued`: first-tick Q. |
| G-20 current composer occupancy → PC-20 | Ignore `busy` in standing admission. | I.`Card0758_restarted_Check_waits_for_the_current_owner`: first-tick Q/zero input for both owner statuses. |
| G-21 old session is not current occupancy → PC-21 | Remove `t.AgentSessionId == session` from that occupancy query. | I.`Card0758_legacy_Check_restart_profile_matrix`: session argument A, because S0's old task wrongly blocks S1. |
| G-22 durable adoption audit → PC-22 | Omit the new Warning event while retaining successful rebind. | Same I method: W's exact single-event assertion. |
| G-23 single brief per dispatch → PC-23 | Call `TryEnqueueReuseAsync` twice for the reused specialist brief. | Same I method: A/U exact row and body-submit counts. |
| G-24 typed policy revalidated on delivery → PC-24 | In `SpecialistInputPolicy.RequireSession`, remove `session.StartedAt != SessionStartedAt`. | T.`Card0415_V04_generation_change_at_reload_refuses_before_submit`: expected exception/no-input assertion. |
| G-25 certified UTF-8 bound → PC-25 | In `SpecialistInputPolicy.Fit`, compare normalized.Length with MaxUtf8Bytes. | T.`Card0415_V04_oversize_is_refused_before_file_or_submit_even_when_characters_fit`: required InputUnsupported exception. |
| G-26 full typed envelope, no file pointer → PC-26 | In `FitBriefForTyping`, send a typed-policy task through ordinary size/spill handling. | T.`Card0415_V04_complete_utf8_envelope_survives_both_spill_layers`: `body == expected`. |
| G-27 typed Checks remain separate → PC-27 | Remove typed-policy exclusions from queue batching (head and subsequent-row barriers). | T.`Card0415_V04_reload_retains_policy_and_does_not_batch_Checks`: first submitted body/list equality. |
| G-28 complete correlated receipt text → PC-28 | In the queue's `TranscriptConfirm.Classify`, accept any matching task marker as a complete typed receipt, ignoring body equality. | T.`Card0415_V04_only_the_complete_current_UserPrompt_confirms`: middle/tail/extra verdicts must fail; wrong-token remains refused. |
| G-29 receipt must be UserPrompt → PC-29 | Permit QueuedUserPrompt as confirming evidence in the full-inline transcript confirmation path. | Same T method: queued verdict must fail. |
| G-30 screen is not receipt → PC-30 | Return Delivered for full-inline input when composer evidence exists but transcript confirmation is absent. | Same T method: screen verdict must fail. |
| G-31 missing receipt never repeats the body → PC-31 | In the full-inline no-confirmation retry branch, resubmit the body once instead of retaining the original typed attempt. | Same T method: `SubmittedBodies.Count == 1` must fail. |
| G-32 failed enqueue is observable → PC-32 | Omit `RecordReuseEnqueueFailedAsync` in the reuse enqueue catch. | T.`Card0758_rebound_legacy_Check_reaches_the_recipient`: enqueue-fault's single durable incident assertion. |
| G-33 durable queue recovery after lost in-memory state → PC-33 | Suppress loading persisted Pending messages in a fresh queue. | T.`Card0758_rebound_legacy_Check_reaches_the_recipient`: reload U. |
| G-34 busy recipient defers typing → PC-34 | Ignore the working-transcript admission check in queue flush for a delegation brief. | T.`Card0758_rebound_legacy_Check_reaches_the_recipient`: busy/reload zero-input assertion before TurnEnd. |
| G-35 warm transcript recovery after live-stream loss → PC-35 | Bypass the warm native transcript pull when the live stream has no confirmation. | T.`Card0415_V04_warm_native_pull_confirms_complete_input_when_live_stream_is_missing`: Delivered plus stored UserPrompt. |

Inventory totals: **35 guards, 35 mapped controls, 0 missing mappings, 0 duplicate mappings**. The redundant checks mentioned within PC-15/17/27 protect one invariant and must be bypassed together to create that invariant's defect; surviving one removed conjunct is not a detection failure.

Run each exact method, e.g. `/*/*/SpecialistExecutionIdentityTests/Card0758_legacy_Check_restart_profile_matrix`, using the real method names above. A parameterized method executes all its arguments; record which arguments fail at the intended assertion, and all expanded counters. PC cycles are baseline-green → compiling-defect/intended-red → restore/fresh-build/green at landed SHA L. Use isolated producer-owned outputs and the required build-slot wrapper for every driver outside the checkpoint tool. Do not batch controls sharing these production methods. Compile failures, fixture exceptions, no tests and timeout-only failures are not positive-control evidence. Code executes CP-1/CP-2; separate Review judges ordinary evidence and this pending inventory; controls run only in the post-land SourceLanding Mutation companion.

### Out of scope

- A real runner restart, native PtyHost or provider/model qualification is not proved by this schema/adapter fixture. No live stack restart is needed to test this dispatcher race.
- Profile equivalence is deliberately not inferred: unchanged ID with changed content is the profile owner's immutable-revision contract; this fix compares revision identity. Profile drift is never automatic reauthorization.
- Routed retries, authorization/fingerprint regeneration, changed logical Agent ownership, successful Check reading/settlement, old digest repair, and a durable outbox for the existing commit-to-enqueue gap require separate designs. The pre-insert fault row records the actual limitation rather than promising recovery that does not exist.
- No broad Unit lane, namespace suite, E2E or extra class is added. The required ordinary scope is completely within the two existing checkpoint classes; fixture helpers are setup, not extra execution counts.

### Cost

All times are **estimates**, superseding the preliminary 53-minute total; no timings were measured here.

- Code authoring/setup: **65 minutes** (S1 plus isolated fixtures, 37 new argument results and additive assertions). Ordinary V/R floor: **6 + 6 = 12 minutes** for exactly CP-1/CP-2, including their two builds. Code dispatch estimate: **77 minutes**, plus observed build-slot wait. Checkpoint floors: **48 + 16 = 64 executed results**, zero failures/skips.
- Separate ordinary Review: **10 minutes**, including guard and receipt-evidence audit.
- Mutation: **35 controls**, one baseline/red/restored-green exact-method invocation per control = **105 invocations**; parameterized results are more numerous and must be reported from TRX. Budget **3 minutes per complete cycle = 105 minutes**, including isolated builds, plus **10 minutes** discovery/evidence/restoration reporting: **115 minutes**. No PC execution is charged to ordinary Code.
- Total implementation-through-Mutation floor: **77 + 10 + 115 = 202 minutes**, excluding this design stage and slot waiting. Savings claimed: **0 minutes** against the preliminary estimate, because that estimate omitted eligibility, transaction-admission and delivery controls. Ordinary execution still uses only two bounded class runs; measurements supersede every estimate.

Code command (after committed S1-S2): `dotnet run --project tools/Antiphon.Checkpoints -- run --plan docs/superpowers/plans/2026-09-26-card-0758-plan.md`. Continue with `dotnet run --project tools/Antiphon.Checkpoints -- wait <run-id>` while the tool returns 75. Report one CHECKPOINT line per row with actual expanded counts, failures/skips, commit and TRX path; no unlisted build/test run without a stated reason.
