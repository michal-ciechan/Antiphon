# CARD-0497: Codex command-line length fix

Plan complete; implementation and real-session qualification remain pending. Resolve the standard Windows npm Codex launcher to `node.exe <installed codex.js>` at the runner boundary, preserve the complete `developer_instructions` argument, and enforce a launcher-aware final command-line budget before creating a child. Do not ship the alternative launcher on `--version` evidence alone.

## Ground truth

| Assumption or question | Evidence and consequence |
|---|---|
| The AppHost restart broke Codex generally. | Investigation task `87782a55` found a successful fresh Frontier launch after the restart. Both incident PTY logs instead contain `The command line is too long.` The children exited code 1 within approximately 0.26 and 0.11 seconds. The three-minute task durations were settlement delays. |
| The task brief was too large. | The brief already travels by file. The failing launch composed `stage-test-design v4fe98b59` and `delegate-basics vb8805261`: 8,366 instruction characters, before other arguments and quoting. Changing brief delivery does not repair this bug. |
| The existing 30,000-character guard protects the child. | `InstructionBundleComposer.EnsureWithinCommandLineBudget` counts composed text and the caller's partial arguments using a Claude flag and three characters per argument. It does not count the fully resolved executable, all profile/model arguments, or actual quote expansion. It assumes the native Windows process limit, while `codex.cmd` crosses the smaller batch-command limit. |
| Only delegates need fixing. | `AgentTaskDispatcher.ComposeDelegateArgs` and `AgentSessionLaunchComposer.ComposeForAgentAsync` both emit `-c developer_instructions=<complete text>`. Both ultimately reach `SessionRunnerRuntime.StartCoreAsync`, including profile-based launches. |
| Direct Node is already proven. | Investigation measured installed CLI 0.153.4: `codex.cmd --version` succeeds at 6,700 synthetic instruction characters and fails at 8,366; `node codex.js --version` succeeds at 8,366. These are process probes, not interactive launch/delivery evidence. |
| The batch wrapper does essential provider setup. | Read-only inspection of this installation's npm shim shows Node selection (sibling `node.exe`, else PATH), npm boilerplate, and invocation of `node_modules\@openai\codex\bin\codex.js`. The JS entrypoint itself selects the platform executable, sets package-management environment, inherits stdio, forwards termination signals and mirrors exit status. Preserve that entrypoint rather than copying its platform-package logic. |
| Codex has a drop-in instruction-file flag. | Official configuration reference distinguishes additional `developer_instructions` from replacement `model_instructions_file`. Installed `--help` offers `-c key=value` and `-p <name>` loading `$CODEX_HOME/<name>.config.toml`; it does not advertise a developer-instruction file/stdin flag. A config-profile file is a possible native file channel, but is not a measured transparent per-launch overlay. |
| Existing Codex stub tests prove the proposed launcher. | `HeadedCodexGate` prefers a vendored native executable, and `CodexRealCliStubProxyCanaryTests.B_runner_launch_hits_stub_and_kills_clean` runs `exec`. Reusing either unchanged can miss both the npm wrapper and interactive TUI. |
| The runner never observed the exit. | `SessionReconciliationService` means the database still had a live row when the runner reported `Exited`. The investigation's host logs include launch, exit and acknowledgement. Why ordinary exit handling had not closed the rows is unproven and separate. |

Evidence: full investigation report via `pwsh -NoProfile -File scripts/delegate.ps1 -Status 87782a55`; raw logs `C:\logs\antiphon\session-runner\6624fee9971c4843a223fd48d63862ab.ansi.log` and `b572dac2d44148618e535f6f38d015c4.ansi.log`, with corresponding files under `pty-hosts\logs`. The investigation report is the evidence for these incidents; this Plan did not repeat its live reproduction.

Documentation checked on 2026-09-12: [OpenAI configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference) and [CLI/developer commands](https://learn.chatgpt.com/docs/developer-commands?surface=cli). The installed CLI help was also read with an isolated scratch `CODEX_HOME`; exit 0, no model turn. Documentation is not installed-version behavioral proof.

## Decisions

### D-1. Normalize the standard npm Codex shim once, inside the runner

Add a small Codex-specific Windows launch policy called in `SessionRunnerRuntime.StartCoreAsync` after ordinary request validation and before session registration, file writes, host creation or Herdr contact. Use the request's existing Codex identity (`TranscriptFormat` or Herdr kind), not an executable name alone. Return an effective request with Node as `Exe` and the absolute installed `codex.js` path prepended to the unchanged argument vector.

Resolve the shim using the effective launch PATH/cwd. Recognize only the expected npm package/entrypoint and forwarding-only wrapper shape; do not execute or bypass a customized wrapper. Resolve Node as the shim does: sibling `node.exe` first, then effective PATH. Missing dependencies fail explicitly, without falling back to the overflowing shim.

Existing `codex.cmd` definitions gain the fix without profile migrations. Already native/explicit direct-Node launches, other providers and non-Windows behavior remain intact. Custom wrappers are not automatically rewritten.

Herdr receives the same normalized request. Add Node to the Codex executable family if needed, retaining legacy cmd recognition and all recorded-PID, pane and foreign-process checks. A Node name alone does not prove ownership. No new `pwsh` intermediary.

### D-2. Keep native developer instructions; do not introduce file retrieval

The chosen fix removes the approximately 8 KiB cmd hop, not all Windows argument limits. It retains bundle ordering/stamps, named-agent preamble/style/append, model, reasoning effort, paste-burst setting, credentials, transcript identity and existing Codex compaction behavior.

Rejected alternatives for this card:

- **Lower only the budget:** rejects an ordinary TestDesign contract instead of repairing launch; 8,191 payload characters also excludes other arguments.
- **`model_instructions_file`:** changes built-in instruction semantics. `instructions` is also unsuitable; this repository measured it as inert.
- **`--profile` plus generated TOML:** a possible native file channel, but needs proof of profile precedence/composition and per-launch file/home lifecycle. Reconsider if Node qualification fails; do not modify shared user config or copy authentication.
- **Environment/`@path`/stdin:** no measured equivalent developer-instruction loader. Environment expansion inside cmd still hits its limit.
- **Grok-style file-read bootstrap or brief injection:** changes instructions into model-mediated retrieval/conversation and adds unnecessary receipt/refresh machinery.
- **Vendored `codex.exe` directly:** bypasses JS package/environment/lifecycle handling.

### D-3. Make the final guard authoritative and launcher-aware

Keep `Delegation:CommandLineBudgetChars` as a configurable ceiling, default 30,000 UTF-16 code units. A larger configured number must never raise a transport ceiling. Validate the fully resolved invocation after argument additions and the Herdr whole-argument environment expansion. Reuse the existing Windows CRT quoting implementation, exposing a small counting/serialization helper if necessary; do not introduce a second escaping algorithm or use `sum(length + 3)` as final proof.

For the normalized Node/native path, cap the serialized invocation at `min(configured budget, 30000)`, including executable, JS argument, spaces and quote/backslash expansion. Reject nonpositive budgets and NUL. Count UTF-16. Also bound the Node-to-native hop using the longest installed native Codex path in the recognized package layout; this only counts overhead, while JS still selects/launches the executable. An unknown layout is an explicit unsupported-launcher result, not permission to assume a short path. Test long installation paths on both hops.

For a remaining explicit Windows batch/cmd Codex launch, retain short custom-launch behavior but apply a conservative ceiling of `min(configured budget, 7000)` to the rendered invocation. The 1,191-character reserve below 8,191 is containment, not proof of arbitrary wrapper internals or environment expansion. Refuse oversized/unmeasurable shell forms with guidance to configure a native/recognized launcher; never treat 7,000 as a universally safe *payload* length. No generic shell parser is part of this fix.

Keep the composer check as an early estimate; correct its comments/diagnostics and make the runner authoritative. Do not lower Claude's budget, alter Grok transport or truncate content. Refusals name session, launcher, measured length and effective budget, without argv/instruction text.

### D-4. Surface deterministic launch refusals and correct the reconciliation wording

Return HTTP 409 Problem Details (`codex_command_line_too_long`, `codex_launcher_unavailable`/`codex_launcher_unsupported`) before process creation. Include the safe cause, transport/limit and remedy. Reuse `SessionRunnerHttpClient.ThrowForRunnerProblemAsync`, which already maps 409 detail/type to `ConflictException`; verify persistence and delegate failure visibility.

Change the reconciler's fallback to a factual message such as `Reconciliation found the runner exited while the database session was still live (ProcessExited, code 1).` Preserve a more specific failure reason already recorded for that launch generation. Keep status, termination-source and cleanup mappings unchanged.

Defer general post-spawn output transport across host manifests/events/reconciliation. This card surfaces its deterministic length/resolution causes; unknown failures retain exit reason/code and existing raw log evidence. Do not forward arbitrary output containing instructions or secrets.

### D-5. Qualify before adoption; retain worker authorization

TestDesign is separate. Node requires an interactive real-session gate; help/version, argv echo, `codex exec`, mocks and warm reuse are insufficient. A failed qualification returns to Plan with evidence, without silently substituting another transport.

The worker's Create-action 403 is not this defect. The caller owns fresh authenticated dispatch acceptance; delegates own isolated harness tests. No worker-permission bypass or production runner in tests.

## Implementation slices and test ownership

| Slice | Files and work | Tests to extend/add |
|---|---|---|
| S1: effective Codex invocation | New `src/Antiphon.SessionRunner/CodexWindowsLaunchPolicy.cs`; call from `SessionRunnerRuntime.cs`; bounded failure mapping in runner `Program.cs`. Normalize recognized npm shim for both pty-host and Herdr. Keep existing request wire shape. | New `tests/Antiphon.SessionRunner.Tests/CodexWindowsLaunchPolicyTests.cs`: standard vs customized shim, missing dependencies, PATH/sibling precedence, spaces/Unicode paths, idempotence, native/non-Codex/non-Windows behavior, and refusal before any child/session/pane side effect. |
| S2: final budget and producer regressions | Reuse quoting from `src/Antiphon.Agents.Pty/ModernConPtyConnection.cs` through a narrow shared helper; use it in S1. Correct early-guard documentation/diagnostics in `server/Application/Services/InstructionBundleComposer.cs`, `CodexLaunchArgs.cs`, callers `AgentTaskDispatcher.cs`/`AgentSessionLaunchComposer.cs`, and `server/Application/Settings/DelegationSettings.cs` only as needed. | New exact-budget tests; existing `LaunchArgvGuardTests`, `ModernConPtyConnectionCommandLineTests`, `InstructionBundleTests`, `CodexLaunchArgsTests`, `CodexDelegateDispatchTests`, `NamedCodexAgentLaunchTests`, `PinnedCodexProfileDispatchLaunchTests`, `DelegateLaunchArgvIntegrityTests`. Cover both server producers with full >8,191-character instructions, all tiers, profile/default/model overrides and final rejection after base args/quoting expand. |
| S3: lane compatibility and real TUI | `src/Antiphon.SessionRunner.Contracts/HerdrAgentKinds.cs` and narrow `HerdrPaneChild.cs` changes only if required for Node process recognition. New interactive canary using existing isolated runner/stub helpers, explicitly starting from the configured npm shim. | Extend `HerdrLaunchShapeTests`, `HerdrPaneChildKillTests`, `HerdrAdoptionSweepTests`; add `CodexCommandLengthSessionTests` alongside `CodexRealCliStubProxyCanaryTests`. Extend `CodexHerdrRealCliStubProxyCanaryTests` for the normalized launcher. Require real modern PTY, inbox fallback and Herdr evidence for the paths changed. |
| S4: error propagation and owner docs | `server/Application/Services/SessionReconciliationService.cs`; `AgentSessionRuntime.cs` only if preserving a specific reason needs the same adjustment. Reuse runner HTTP mapper. Update `docs/agent-kinds.md`, `docs/ai-agent-tui-configuration.md`, `docs/herdr-sessions.md` and relevant guard comments with final behavior and measured qualification. | `AgentSessionLaunchFailureTests`, `CodexDelegateDispatchTests`, `SessionReconciliationServiceTests`, and runner HTTP Problem Details tests. Verify session/task failure contains safe cause/length/limit, no prompt or secret echo, no child created, and reconciler fallback/preservation without changed status semantics. |

Keep implementation to these launch/error boundaries. No database migration, new instruction-file protocol, instruction shortening, model rerouting, client UI work, supervisor retry changes, timeout tuning or provider-compaction redesign.

## Acceptance gates for TestDesign

1. **Incident regression:** fixed 8,366-character multiline fixture plus current TestDesign composition; quotes, backslashes, Unicode, CR/LF and start/middle/end sentinels. Record serialized lengths. Legacy batch fails; normalized launch preserves the entire value. Add smaller control and serialized budget-minus-one/equal/plus-one cases, including quote-heavy expansion.
2. **Real interactive PTY:** installed Codex TUI through `SessionRunnerRuntime` -> detached pty-host -> modern ConPTY, starting from the npm shim. Use `RealCliStubEnv.ForCodex`, `FakeLlmApiServer`, isolated cwd/home/logs and synthetic credential. Normal adapter/queue submission must produce a stub request with the full developer block once, nonce and injected credential, and unchanged base instructions against a short control. Require this session's matching complete `UserPrompt`, scripted assistant reply and completion. Avoid the helper's native-exe preference and `exec` shortcut.
3. **Lifecycle/lane regression:** clean kill/dispose of Node and native Codex; pty-host adoption ownership retained. Real argv preservation and interactive turn on inbox fallback. Herdr real CLI stub launch/reply/cleanup plus foreign-pane protection. Unavailable/skipped gates stay pending.
4. **Failure visibility:** final length/dependency refusal reaches persisted session/task reason without content leakage or child creation; verify cleanup and reconciliation preservation for the same launch generation.
5. **Caller-owned acceptance:** after deployment, fresh Codex TestDesign dispatches at Frontier and Low, and a fresh named agent with >8,191 composed characters. Use dedicated validation cwd/home and authorized credentials. Prove new child launches (exclude pool reuse), matching boot `UserPrompt`, assistant response/report, full instruction availability and ordinary settlement/ownership. Record IDs, tier, bundle stamps, CLI version, launcher, deployed SHA and result; clean up only validation sessions through their owners.

TestDesign supplies V/R/PC rows and exact filters. Follow `docs/testing-and-build.md`: isolated `--property:OutputPath=bin-c0497/`, `dotnet run`, Unit lane plus affected named integrations, fresh TRX/nonzero counts, sequential process-spawning projects with assembly-local `ParallelLimiter<ProcessSpawnLimit>`. Set real CLI/stub/headed opt-ins explicitly; no production runner, copied auth or normal user Codex home.

## Delivery and remaining work

This dispatch changes only this plan. No implementation builds or test suites ran; one isolated CLI help probe exited 0. All real-session qualification above is pending.

Land this plan before dispatching TestDesign; that dispatch still needs a currently launchable provider chosen by the caller. After Code/Mutation, deploy from the canonical checkout using existing runbooks. Restart/update SessionRunner as well as the server, preserving live hosts; AppHost restart alone cannot load this fix. Verify running build identity and a new child's actual invocation, not health alone. This Plan restarts no shared service.

Separate follow-ups, without expanding this card: investigate the missed ordinary exit-to-database path; design safe bounded post-spawn diagnostics if needed. Worker delegation refusal is an existing authorization boundary, not a prerequisite change.

## Verification design

TestDesign for the plan above (task 58e21153, 2026-09-12). The fix design is unchanged; this section adds the verification contract Code implements and Mutation executes. Measured on this machine while designing: codex-cli 0.153.4; npm shim `%APPDATA%\npm\codex.cmd` (44-char path) is the stock npm forwarder (`IF EXIST "%dp0%\node.exe" ... "%_prog%" "%dp0%\node_modules\@openai\codex\bin\codex.js" %*`); `codex.js` path 74 chars; vendored `codex-win32-x64\vendor\x86_64-pc-windows-msvc\bin\codex.exe` path 142 chars; `C:\Program Files\nodejs\node.exe` 32 chars (quoted 34). `codex.js` spawns `codex.exe` with `process.argv.slice(2)` and `stdio: "inherit"`, so the second (Node to native) command line on this install is **33 UTF-16 units longer** than the first: the hop-2 bound in D-3 is load-bearing, not defensive. The Herdr lane renders `& 'exe' @('arg', ...)` into a PowerShell script file (`HerdrLaunchScript.BuildContent`) and PowerShell performs the child quoting there, so the runner's CRT-rule count is an estimate on that lane; the interactive Herdr canary is the evidence for it.

Live data point for whoever implements this: TestDesign task `fd0772bf` on this same card, dispatched to Codex, died with `Runner reported an exit that was never observed (ProcessExited, code 1)` and an empty transcript. Its brief composed `stage-test-design v4fe98b59` plus `delegate-basics vb8805261`, the same 8,366-character composition as the incident. The bug reproduces on demand by dispatching any Codex TestDesign task.

### Inspection

- Read in full: `CodexRealCliStubProxyCanaryTests`, `CodexHerdrRealCliStubProxyCanaryTests`, `CodexBootWedgeProbeTests` (P1 body, ready/submit/UserPrompt helpers), `HeadedCodexGate`, `RealCliStubGate`, `RealCliStubEnv`, `FakeLlmApiServer`, `HerdrRealCliCanarySupport` (trust accept, `SendWrappedBodyAsync`), `DirectSessionRunnerClient` (wraps `SessionRunnerRuntime`), `RunnerCodexAdapter.SendPromptAsync`/`WaitForReadyAsync`, `LaunchArgvGuardTests`, `ModernConPtyCommandLineTests`, `CodexLaunchArgsTests`, `InstructionBundleTests` (budget block 385-445, 577-595), `CodexDelegateDispatchTests` (harness 560-660, budget case 164), `NamedCodexAgentLaunchTests`, `PinnedCodexProfileDispatchLaunchTests.T10`, `DelegateLaunchArgvIntegrityTests` (harness 500-578), `AgentSessionLaunchFailureTests` (effort/409/placeholder/Grok-rules refusals, `LaunchFixture`), `SessionReconciliationServiceTests` (exit mirroring 70-140), `GrokRulesRunnerRefusalTests` (pty-host and Herdr refusal-before-side-effect pattern, 409 mapper), `GrokRulesHttpAcceptanceTests` (real runner exe over HTTP), `GrokRulesArgvPolicyTests`, `GrokRulesLaunchRefusalTests.AssertNamedGrokRefused`, `HerdrLaunchShapeTests` (Codex occupied-pane case 828, `StartAsync` helper 1143), `HerdrPaneChildKillTests` (named launch failure 308), `HerdrAdoptionSweepTests` (R1 Codex format row), `SessionRunnerHttpClientHerdrWireTests` (409 mapping 223-330), `AgentTaskLivenessTests` (existing-reason preservation), `SessionRunnerRuntime.StartCoreAsync` 146-300, `HerdrAgentKinds`, `HerdrPaneChild` occupant/4c/sidecar paths, `HerdrLaunchScript.BuildContent`, `ModernConPtyConnection.Spawn`/`ResolveAppPath`/`BuildCommandLine`, `HostSession` launch-failed path, `AgentTaskDispatcher` 626-700 (Grok-only `ConflictException` catch, generic `FailAndNotifyAsync`) and `FailDeadSessionTasksAsync`, `AgentSessionService` launch catches (280-300, 360-380), `HerdrSupervisionFailureEvidence`, `RestartFailurePolicy`, `ThrowForRunnerProblemAsync`, `HerdrProblemMapper`, runner `Program.cs` catch ladder, the installed `codex.cmd` and `codex.js`.
- Missing setup recorded: there is no pty-host interactive Codex canary with a UserPrompt oracle today (`CodexBootWedgeProbeTests` measures, it does not assert; `CodexRealCliStubProxyCanaryTests.B_runner_launch_hits_stub_and_kills_clean` runs `exec`; `CodexAdapterIntegrationTests` needs a seeded real-service home). The new `CodexCommandLengthSessionTests` supplies it, reusing the P1 ready/submit shape and the Herdr canary's UserPrompt wait. There is no shared 8,366-character fixture; add `CodexInstructionFixtures` to `src/Antiphon.FakeLlmApi` (referenced by both `Antiphon.Tests` and `Antiphon.SessionRunner.Tests`) with a length pin.
- Boundaries: lane {pty-host modern, pty-host inbox, Herdr} x launcher {recognized npm shim, explicit batch/cmd, explicit native exe, explicit `node.exe codex.js`, customized wrapper} x payload {short control, 8,366 incident, serialized budget-1/budget/budget+1 on hop 1, hop-2-only overflow, quote-heavy expansion, configured budget above 30,000, nonpositive budget, NUL, surrogate pairs} x install path {plain, spaces plus Unicode, missing node, missing native package}. Mapped: V-1..V-4 (lanes x recognized shim x incident and near-cap), V-6..V-13 and R-2..R-9 (unit boundaries on every launcher and payload edge), V-14/V-15 (install-path edges), R-10/R-11 (lane-specific side effects). Excluded with reasons in Out of scope: Herdr x budget boundaries interactively, inbox x near-cap, arm64 layout live.

### Delivery inventory

Two outcome-delivery paths change. Durable identity for both: the runner session id, plus the per-run stub nonce and the three fixture sentinels (`C0497-START`, `C0497-MID`, `C0497-END`) inside the developer instructions.

| Path | Producer | Destination | Persistence boundary | Recovery | Observable receipt |
|---|---|---|---|---|---|
| Developer instructions to the Codex child (the fix) | server producers (`AgentTaskDispatcher.ComposeDelegateArgs`, `AgentSessionLaunchComposer.ComposeForAgentAsync`) via `-c developer_instructions=` argv; runner policy rewrites Exe/args | Codex's first developer message, on the wire to `POST /v1/responses` | none; argv is consumed at `CreateProcessW` in the pty-host (or PowerShell in the Herdr pane). A refused launch creates nothing | none needed: the launch either creates the child with the whole vector or refuses before creation | stub request body whose JSON-decoded developer text contains the entire fixture exactly once (V-1); the session's own `UserPrompt` transcript row for the nonce prompt and the scripted assistant reply (V-1) |
| Launch refusal to the caller (D-4) | `SessionRunnerRuntime.StartCoreAsync` throws the typed refusal; `Program.cs` maps to 409 Problem Details | (a) `AgentSessions.FailureReason` via the background launch catch in `AgentSessionService`; (b) `AgentTasks.FailureReason` via `FailDeadSessionTasksAsync`/`AgentTaskLiveness.ClassifyFailure`; (c) the caller session's inbox via the task failure notification | Postgres rows (a) and (b) | the dead-session sweep is idempotent per tick; a server restart re-runs it. A runner restart between 409 and the catch is the existing generic launch-failure path (unchanged) | (a) V-16 asserts the persisted row; (b) V-18 asserts the task reason; (c) the caller's `UserPrompt` transcript row carrying the notification is gate 5 (caller-owned) |

Busy recipient and already-eligible recipient: the refusal notification rides the existing task-failure delivery (WhenIdle queue to the caller); V-18 covers the Working caller (busy) through the queued prompt, and the Idle caller through immediate delivery, both by the fake adapter's captured prompts. Crash/enqueue-failure recovery at the runner-to-server handoff: a 409 lost to a server crash leaves the session `Starting`; `SessionReconciliationService` closes it with the corrected factual wording (V-19) and the sweep fails the task (existing path). Substitutes declared: `FakeAgentProtocolAdapter.Prompts` stands in for the caller's real `UserPrompt` transcript row; it proves the server enqueued and delivered the text to the adapter, not that a real TUI accepted it. `FakeRunnerClient` stands in for the runner's HTTP surface in reconciler tests; V-17 (real runner exe over HTTP) closes that gap for the 409 itself. Neither an accepted request, a queue insert, a `Failed` status alone, nor a 409 body satisfies delivery acceptance; the accepting evidence for the fix is V-1's stub body plus transcript, and for the caller notification it is gate 5's transcript, which no delegate can produce (Worker Create is 403).

### Proves it works now

Real interactive PTY (all opt-in `ANTIPHON_REAL_CLI_STUB_TESTS=1`, `[Explicit]`, `[Category("RealCliStubProxy")]`, `[NotInParallel("RealCliStubProxy")]`, `[ParallelLimiter<ProcessSpawnLimit>]`, new class `tests/Antiphon.Tests/Agents/CodexCommandLengthSessionTests.cs`). Each starts from the **configured npm shim**: `Exe = "codex.cmd"` exactly as `server/appsettings.json` defines it, gated by `File.Exists(%APPDATA%\npm\codex.cmd)` (skip otherwise), never `HeadedCodexGate.ResolveCodex()` (it prefers the vendored exe). Env from `RealCliStubEnv.ForCodex` plus `TERM=xterm-256color`; per-test cwd, `CODEX_HOME` and log root under one temp root; `TranscriptEnabled: true`, `TranscriptFormat: codex`, `Backend: pty-host`. Launch and submit through `RunnerCodexAdapter` over `DirectSessionRunnerClient(codexTranscript: true)` so the ordinary adapter path (`WaitForReadyAsync` trust handling, `SendPromptAsync` with `CodexSubmitConfirmation`) is what is exercised, not a hand-typed `\r`.

- V-1: incident regression, modern pty-host | Antiphon.Tests interactive | `CodexCommandLengthSessionTests.Incident_fixture_launches_through_node_and_delivers_the_whole_developer_block` with `PtyBackend = "modern"`, args `--no-alt-screen --dangerously-bypass-approvals-and-sandbox <ForCodex 10 args> -c disable_paste_burst=true -c developer_instructions=<CodexInstructionFixtures.Incident (8,366 UTF-16 units, pinned)>` | (1) `StartAsync` returns `Running`; (2) `Win32_Process` for the DTO `Pid` has `Name == "node.exe"` and `CommandLine` contains `\bin\codex.js` and not `codex.cmd`; (3) `WaitForReadyAsync` true; (4) `SendPromptAsync(nonce)` then `WaitForTurnCompleteAsync` with `TurnCompleted` and `ResponseText` containing the scripted reply; (5) `GetTranscript` has a `UserPrompt` entry containing the nonce and a later assistant entry containing the reply; (6) `POST /v1/responses` recorded with `Authorization: Bearer <synthetic>` and `GET /v1/models` before it; (7) walking every JSON string value in the body, the full fixture (CR/LF, `"`, `\`, `café`, `😀`, all three sentinels) appears exactly once and the nonce appears; (8) `KillAsync` returns `Exited` and afterwards no process has `127.0.0.1:<stubPort>` on its command line (node and native both gone).
- V-2: short control on the same cwd/home, then V-1 in the same test method | same | `Incident_fixture_...` runs a 200-character control launch first | the control body's base `instructions` string equals the large run's byte for byte, and the control body contains its own short block exactly once; this is the "unchanged base instructions" gate.
- V-3: near-cap control | same class | `Near_cap_fixture_launches_and_arrives_intact`: developer text sized so the **hop-2** serialized line (measured with the shared helper against the installed native path) is 29,990 UTF-16 units | same eight assertions as V-1; proves 30,000 is not above the real ceiling on the longer hop.
- V-4: inbox fallback | same class | `Incident_fixture_launches_on_the_inbox_backend` with `PtyBackend = "inbox"` | assertions (1)-(8) of V-1; the pre-escaped verbatim line preserves the vector.
- V-5: Herdr lane | extend `CodexHerdrRealCliStubProxyCanaryTests` | `B_runner_herdr_launch_from_the_npm_shim_carries_the_incident_fixture` with `Exe = "codex.cmd"`, `HerdrAgentKinds.Codex`, the incident fixture, existing stall-skip rules | sidecar `AgentKind == codex` and `ChildPid` whose `Win32_Process.Name == "node.exe"`; pane agent `codex`; stub body contains the fixture once plus nonce with the Bearer; `UserPrompt` row for the nonce; `KillAsync` `Exited`; `LastLaunchScriptContent` names `node.exe` and `codex.js`, not `codex.cmd`.

Runner policy and budget (new `tests/Antiphon.SessionRunner.Tests/CodexWindowsLaunchPolicyTests.cs`, `[Category("Unit")]`, hermetic: a synthetic npm layout under the test root with the stock shim text, `node_modules\@openai\codex\bin\codex.js`, `...\codex-win32-x64\vendor\x86_64-pc-windows-msvc\bin\codex.exe` and a sibling `node.exe`, all zero-byte files; nothing is spawned).

- V-6: standard shim normalizes | unit | `Standard_npm_shim_becomes_node_exe_plus_codex_js` | effective `Exe` is the sibling `node.exe` full path; `Args[0]` is the absolute `codex.js`; `Args[1..]` equal the input vector by reference-equality of content, including the 8,366 fixture unchanged.
- V-7: sibling precedence | unit | `Sibling_node_wins_over_PATH_node` and `PATH_node_is_used_when_no_sibling` (PATH from `request.Env["PATH"]` when present, else process PATH, mirroring `ResolveAppPath`) | resolved Node path per case.
- V-8: spaces and Unicode install root | unit | `Install_root_with_spaces_and_unicode_round_trips` (root `npm rôot 😀`) | effective Exe/Args round-trip through `LaunchArgvGuard.ParseArgv` of `BuildCommandLine`; counts use UTF-16 units.
- V-9: idempotence | unit | `An_already_normalized_request_is_not_prefixed_twice` | identical request returned; `Args.Count` unchanged.
- V-10: explicit native and explicit direct-Node untouched | unit | `Explicit_codex_exe_is_left_alone`, `Explicit_node_codex_js_is_left_alone_but_still_budgeted` | Exe unchanged; budget still applied (V-11).
- V-11: hop-1 boundary | unit | `Serialized_hop1_at_budget_is_accepted_and_plus_one_is_refused` with `CommandLineBudgetChars = 30000`; payload sized by the shared helper so the serialized line is exactly 29,999 / 30,000 / 30,001 | accept, accept, refuse with `codex_command_line_too_long`; the message names session id, launcher (`node.exe codex.js`), measured 30,001 and effective 30,000, and contains none of the payload.
- V-12: hop-2 boundary | unit | `Native_hop_is_bounded_by_the_longest_installed_native_path`: hop 1 serialized 29,980, hop 2 (native exe path replaces node+js prefix) 30,013 on the synthetic layout | refused; message names the native hop and both measured lengths.
- V-13: quote-heavy expansion | unit | `Quote_expansion_is_counted_not_estimated`: payload of 9,000 `"` and `\` pairs where `sum(len+3)` is 9,050 but the CRT-escaped line is > 18,000 with `CommandLineBudgetChars = 15000` | refused; the old `sum(length + 3)` estimate would accept (assert both numbers in the test to document why).
- V-14: missing dependencies | unit | `Missing_sibling_and_PATH_node_is_codex_launcher_unavailable`, `Shim_without_codex_js_is_codex_launcher_unavailable`, `Recognized_shim_without_a_vendored_native_package_is_codex_launcher_unsupported` | typed refusal with the named code; the original `codex.cmd` request is never returned as the effective request.
- V-15: customized wrapper | unit | `A_customized_wrapper_is_not_rewritten_and_falls_under_the_batch_ceiling` (shim text with one extra line) | not normalized; explicit batch ceiling applies (R-4); nothing executed.
- V-16: persisted reason on the server | Antiphon.Tests Integration | `AgentSessionLaunchFailureTests.Codex_command_line_refusal_409_persists_the_measured_length_and_limit_without_the_instructions` using `FakeAgentProtocolAdapter { ThrowOnStart = new ConflictException(detail, "codex_command_line_too_long") }` where `detail` is the runner's real format with a sentinel-free payload reference | `ConflictException` surfaces; row `Failed`, `FailureReason` contains the code, `30,001` and `30,000`, not the sentinel; `TerminationSource` SystemRequest; `HerdrSupervisionFailureKind` NonQualifying; `adapter.Started` false; `adapter.Prompts` empty.
- V-17: real runner over HTTP | Antiphon.SessionRunner.Tests Integration/Headed group | new `CodexCommandLengthHttpAcceptanceTests.Oversized_codex_launch_is_a_409_problem_with_no_session_and_no_leak`, same shape as `GrokRulesHttpAcceptanceTests` (runner exe, random port, synthetic npm layout under the root with `PATH` prepended in the request Env) | `POST /sessions` returns 409, problem `type == codex_command_line_too_long`, detail carries length and limit and not the sentinel; `GET /sessions` lists nothing; no `pty-hosts` manifest and no `<id>.ansi.log` under the root; runner stdout has no sentinel; a second request at exactly the budget with a `cmd.exe /d /q /k prompt $G` control is not needed here because V-1 covers acceptance.
- V-18: delegate visibility | Antiphon.Tests Integration | `AgentTaskDeadSessionReconciliationTests.A_codex_launch_refusal_reason_reaches_the_task_and_the_caller` (seed a Dispatched task whose session is `Failed` with the V-16 reason; run `FailDeadSessionTasksAsync`) | task `Failed` with `FailureReason` containing the code and both numbers; the caller notification captured by the fake adapter contains the same; existing `AgentTaskLivenessTests.ClassifyFailure_preserves_an_existing_session_failure_reason` stays green.
- V-19: reconciler wording | Antiphon.Tests Integration | `SessionReconciliationServiceTests.Unobserved_failed_exit_gets_the_factual_reconciliation_wording` (Running row, runner `Exited` code 1, `ExitReason ProcessExited`, `FailureReason` null) | `FailureReason == "Reconciliation found the runner exited while the database session was still live (ProcessExited, code 1)."`; status `Failed`, `TerminationSource` ProcessExit, `ExitCode` 1 as before.
- V-20: 409 mapping on both sides | unit | runner: `CodexLaunchRefusalTests.Refusal_maps_to_409_with_its_code` (mapper returns `ProblemHttpResult` 409 with `Type` per code for all three codes); server: `SessionRunnerHttpClientHerdrWireTests.Launch_409_codex_command_line_too_long_maps_to_conflict_with_the_code` | `ex.Code` equals the runner type; `ex.Message` equals the detail.
- V-21: producers at >8,191 | Antiphon.Tests Integration | `CodexDelegateDispatchTests.every_tier_carries_a_whole_developer_block_past_the_batch_ceiling` (`[Arguments]` for Frontier/High/Medium/Low, `AgentTaskRole.TestDesign`, the incident fixture as `SystemPromptAppend` via an attached agent so the composed text exceeds 8,191) and `NamedCodexAgentLaunchTests.A_codex_agents_long_standing_instructions_arrive_whole` and `PinnedCodexProfileDispatchLaunchTests.T11_pinned_codex_profile_keeps_a_long_developer_block_whole` | the `developer_instructions=` argv element equals the composed text exactly (length and content), once; the early estimate passes at 30,000; model and effort overrides still present.
- V-22: the whole Codex matrix fits the runner's real ceiling | Antiphon.Tests Integration | `DelegateLaunchArgvIntegrityTests.Every_codex_launch_fits_both_launcher_hops_at_production_budget` (every kind x role on `AgentKind.Codex` with all attachments; measure hop 1 with `node.exe`+`codex.js` and hop 2 with a 200-character synthetic native path via the shared helper) | both measured lengths ≤ 30,000; the maximum is written to the test output.
- V-23: incident composition still exceeds the batch ceiling | Antiphon.Tests Unit | `InstructionBundleTests.the_test_design_composition_is_past_the_batch_command_ceiling` (`ForDelegate(Worker, TestDesign, [])`) | `composed.Text.Length >= 8192` with the measured value in the message; if a future bundle edit shrinks it the case re-pins deliberately.
- V-24: shared serializer helper | Antiphon.Agents.Pty.Tests | `CommandLineLengthTests.Measure_equals_the_line_the_child_parses` | `Measure(exe, args) == BuildCommandLine(exe, args, false).Length` and the line round-trips through `CommandLineToArgvW` for the incident fixture; `😀` counts 2.
- V-25: composer diagnostics corrected | Antiphon.Tests Unit | `InstructionBundleTests.an_oversized_composition_throws_and_names_what_to_shrink` (extend) | existing assertions plus `ex.Message` contains `estimate` and names the runner as the final check.
- V-26: Herdr family and lifecycle | Antiphon.SessionRunner.Tests | `HerdrLaunchShapeTests.Codex_node_launcher_child_is_recognized_as_own_occupant` (fake `SetPaneProcessInfo` with `(900, "node.exe", ["node.exe", "<codex.js>", "--no-alt-screen"])`, launch script asserted to reference `node.exe`); `HerdrAdoptionSweepTests.R1_readopt_restores_the_sidecars_format` row for Codex extended with a node occupant; `HerdrPaneChildKillTests.Node_child_kill_by_pid_leaves_the_pane_open` using a real `node.exe` dummy | `IsFamilyMember("codex", "node.exe")` true; `cmd.exe` still accepted; adoption restores `codex` format; kill by pid succeeds and the pane stays open.

### Guards the regression

- R-1: the legacy batch launcher still fails at 8,366 (proves the normalization is load-bearing, not a version probe of the fix) | `CodexCommandLengthSessionTests.Legacy_batch_launcher_still_dies_on_the_incident_fixture`: `cmd.exe /d /c codex.cmd --version -c developer_instructions=<fixture>` via `RealCliStubProcess.RunAsync` | exit code 1 and combined output contains `The command line is too long`; a 6,700-character control exits 0.
- R-2: the budget-plus-one line is refused before any process exists | `CodexLaunchRefusalTests.Pty_host_oversized_codex_launch_is_refused_before_a_session_is_registered` (runtime with synthetic layout; `runtime.List().ShouldBeEmpty()`, `Get` throws `KeyNotFoundException`, no `PtyHostManifest`, no `.ansi.log`) and `..._herdr_..._before_herdr_is_contacted` (`fake.Requests.ShouldBeEmpty()`, no launch script, no sidecar, no last-pane file) | decisive: `runtime.List()` empty and the typed exception code.
- R-3: a configured budget above 30,000 cannot raise the ceiling | `CodexWindowsLaunchPolicyTests.A_configured_budget_above_30000_does_not_raise_the_transport_ceiling` with `CommandLineBudgetChars = 40000` and a 30,001 line | refused; message says effective 30,000.
- R-4: explicit batch launcher ceiling | `CodexWindowsLaunchPolicyTests.Explicit_cmd_launcher_is_capped_at_7000` (`Exe = "C:\custom\codex-wrapper.cmd"`, rendered line 7,000 accepted, 7,001 refused; with `CommandLineBudgetChars = 6000` a 6,001 line refused) | refused with guidance naming a recognized launcher.
- R-5: nonpositive budget and NUL | `Nonpositive_budget_is_refused` (`0`, `-1`), `Nul_in_any_argument_is_refused` | typed refusal; no side effects.
- R-6: UTF-16 counting | `Surrogate_pairs_count_two_units` (2,048 `😀` plus filler sized to 30,001 units) | refused at 30,001 even though `Encoding.UTF8.GetByteCount` would say otherwise.
- R-7: Node path accepts what the batch path could not | `CodexWindowsLaunchPolicyTests.The_incident_fixture_is_accepted_on_the_node_path` (8,366 fixture plus the production base args) | accepted with measured hop lengths written to output.
- R-8: no fallback to the overflowing shim on missing Node | `Missing_node_never_returns_the_batch_request` | effective request is never `codex.cmd`.
- R-9: identity by request kind, not exe name | `A_claude_request_whose_exe_ends_in_codex_cmd_is_untouched` and `A_codex_transcript_request_with_a_non_shim_exe_is_untouched` | Exe unchanged.
- R-10: reconciler preserves a specific reason | `SessionReconciliationServiceTests.A_specific_failure_reason_already_on_the_live_row_survives_reconciliation` (Starting row past grace with `FailureReason = "codex_command_line_too_long: ..."`; runner `Exited` code 1) | `FailureReason` unchanged; status/termination as today.
- R-11: existing reconciler mappings unchanged | existing `Runner_reported_exit_is_mirrored_to_the_db_session`, `Runner_reported_CpuSpinKilled_exit_records_SystemRequest`, `Runner_reported_HerdrPaneClosed_exit_fails_the_session_not_a_clean_stop`, `Unobserved_exit_does_not_overwrite_an_OperatorRequest_source` | green after the wording change.
- R-12: foreign-pane protection survives adding Node to the family | `HerdrLaunchShapeTests.Codex_occupied_pane_is_refused_even_with_the_right_kind` extended with a `node.exe` occupant whose argv runs some other script; `HerdrPaneChildKillTests.Foreign_foreground_process_kills_our_child_by_pid_leaves_pane_open_and_returns_true` | `pane_occupied` for the foreign Node; our child killed by recorded pid only.
- R-13: existing argv integrity | `DelegateLaunchArgvIntegrityTests.Every_dispatched_launch_round_trips_through_both_backends`, `LaunchArgvGuardTests`, `ModernConPtyCommandLineTests` | green; the shared helper must not change `BuildCommandLine` output.
- R-14: early estimate still refuses gross overflow | existing `CodexDelegateDispatchTests.the_command_line_budget_guards_a_codex_launch_the_same_way`, `InstructionBundleTests.an_oversized_composition_throws_and_names_what_to_shrink`, `the_other_arguments_count_towards_the_budget_not_just_the_append` | green with the corrected wording (V-25).

### Guard inventory

- G-1: D-3 hop-1 serialized cap `min(configured, 30000)` on the Node path | PC-1
- G-2: D-3 hop-2 (Node to native) bound from the longest installed native path | PC-2
- G-3: D-3 configured budget never raises the transport ceiling | PC-3
- G-4: D-3 NUL refused | PC-4
- G-5: D-3 nonpositive budget refused | PC-5
- G-6: D-3 explicit batch/cmd ceiling `min(configured, 7000)` | PC-6
- G-7: D-1 only the stock forwarding shim shape is normalized; a customized wrapper is neither rewritten nor executed | PC-7
- G-8: D-1 missing Node/`codex.js` is an explicit unavailable refusal, never a fallback to the shim | PC-8
- G-9: D-1 unknown native package layout is `codex_launcher_unsupported`, not an assumed short path | PC-9
- G-10: D-1 refusal precedes every side effect (session registration, manifest, log, sidecar, Herdr contact) | PC-10
- G-11: D-3 refusal detail carries no argv/instruction text | PC-11
- G-12: D-4 runner maps the typed refusal to 409 with its code | PC-12
- G-13: D-4 server persists the runner detail as the session `FailureReason` | PC-13
- G-14: D-4 reconciler preserves a specific reason already on the row | PC-14
- G-15: D-4 reconciler fallback uses the factual wording | PC-15
- G-16: S3 Codex executable family recognizes `node.exe` while keeping `cmd` | PC-16
- G-17: S3 a family name alone never adopts a foreign pane (4c stays occupied) | PC-17
- G-18: S2 producers hand the complete developer block to argv (no truncation) | PC-18
- G-19: D-1/D-5 the normalized launch really delivers an 8,366-character block interactively | PC-19
- G-20: D-1 idempotence (no double prefix) | PC-20
- G-21: D-1 Codex identity comes from request kind; other kinds and platforms untouched | PC-21
- G-22: D-1 sibling `node.exe` outranks PATH | PC-22
- G-23: S2 the count is the real CRT-escaped line, not `sum(length + 3)` | PC-23

guards=23, mapped=23, missing=0, duplicate PC mappings=0.

### Positive controls

Each PC: apply the mutation, run only the named method with `--treenode-filter "/*/*/<Class>/<Method>"`, confirm the named assertion is the red one, restore, run green. Filters use `--property:OutputPath=bin-pc/` (forward slash). PC-19 needs `ANTIPHON_REAL_CLI_STUB_TESTS=1` and the installed npm Codex; it is the only PC that spawns a real CLI.

- PC-1: break G-1 by changing the hop-1 comparison from `> effective` to `> effective + 1`; expect `CodexWindowsLaunchPolicyTests.Serialized_hop1_at_budget_is_accepted_and_plus_one_is_refused` red at the 30,001 `Should.Throw` (accepted instead of refused).
- PC-2: break G-2 by returning hop-1 length for the native hop (skip the native-path substitution); expect `Native_hop_is_bounded_by_the_longest_installed_native_path` red at `Should.Throw` (30,013 accepted).
- PC-3: break G-3 by using `configured` instead of `Math.Min(configured, 30000)`; expect `A_configured_budget_above_30000_does_not_raise_the_transport_ceiling` red at `Should.Throw`.
- PC-4: break G-4 by deleting the `IndexOf('\0')` check; expect `Nul_in_any_argument_is_refused` red at `Should.Throw`.
- PC-5: break G-5 by treating `budget <= 0` as unlimited (`int.MaxValue`); expect `Nonpositive_budget_is_refused` red at `Should.Throw` for `0`.
- PC-6: break G-6 by changing the batch ceiling constant 7000 to 8191; expect `Explicit_cmd_launcher_is_capped_at_7000` red at the 7,001 `Should.Throw`.
- PC-7: break G-7 by returning `true` from the shim-shape recognizer; expect `A_customized_wrapper_is_not_rewritten_and_falls_under_the_batch_ceiling` red at `effective.Exe.ShouldBe(original)`.
- PC-8: break G-8 by returning the original request when Node is missing; expect `Missing_node_never_returns_the_batch_request` red at `Should.Throw` with code `codex_launcher_unavailable`.
- PC-9: break G-9 by defaulting the native path length to 0 when no vendored package is found; expect `Recognized_shim_without_a_vendored_native_package_is_codex_launcher_unsupported` red at `Should.Throw`.
- PC-10: break G-10 by moving the policy call in `StartCoreAsync` to after `_sessions.TryAdd` (inside the try that starts the host); expect `CodexLaunchRefusalTests.Pty_host_oversized_codex_launch_is_refused_before_a_session_is_registered` red at `runtime.StartCoreSessionRegistrations.ShouldBe(0)`. `List()` stays empty because the launch catch at `SessionRunnerRuntime` removes and tears down the session; the registration counter is the ordering evidence.
- PC-11: break G-11 by appending `args[^1]` to the refusal message; expect `Serialized_hop1_at_budget_is_accepted_and_plus_one_is_refused` red at `ex.Message.ShouldNotContain(sentinel)`.
- PC-12: break G-12 by mapping the refusal to `Results.Problem(statusCode: 500, type: null)`; expect `CodexLaunchRefusalTests.Refusal_maps_to_409_with_its_code` red at `problem.StatusCode.ShouldBe(409)`.
- PC-13: break G-13 by setting `session.FailureReason = ex.GetType().Name` in the `AgentSessionService` launch catch; expect `AgentSessionLaunchFailureTests.Codex_command_line_refusal_409_persists_the_measured_length_and_limit_without_the_instructions` red at `FailureReason.ShouldContain("30,001")`.
- PC-14: break G-14 by assigning the fallback wording unconditionally; expect `SessionReconciliationServiceTests.A_specific_failure_reason_already_on_the_live_row_survives_reconciliation` red at `FailureReason.ShouldBe(seeded)`.
- PC-15: break G-15 by restoring the string `Runner reported an exit that was never observed`; expect `Unobserved_failed_exit_gets_the_factual_reconciliation_wording` red at the exact-text `ShouldBe`.
- PC-16: break G-16 by removing `"node.exe"` and `"node"` from `ExecutableFamily(Codex)`; expect `HerdrLaunchShapeTests.Codex_node_launcher_child_is_recognized_as_own_occupant` red at the `PaneForeign`/occupant assertion.
- PC-17: break G-17 by returning `TargetPaneKind.Adopt` in 4c whenever `IsFamilyMember` is true; expect `Codex_occupied_pane_is_refused_even_with_the_right_kind` (node occupant row) red at `ex.Code.ShouldBe(CodePaneOccupied)`.
- PC-18: break G-18 by truncating in `CodexLaunchArgs.DeveloperInstructions` to 8,191 characters; expect `CodexDelegateDispatchTests.every_tier_carries_a_whole_developer_block_past_the_batch_ceiling` red at `value.ShouldBe(composed.Text)`.
- PC-19: break G-19 by making the policy return the request unchanged for the recognized shim (normalization off); expect `CodexCommandLengthSessionTests.Incident_fixture_launches_through_node_and_delivers_the_whole_developer_block` red at the `node.exe` process-name assertion or at `started.Status.ShouldBe("Running")` (the child dies with `The command line is too long`); either is the expected red, record which.
- PC-20: break G-20 by returning `request with { Args = Prepend(originalArgs[0], originalArgs) }` from the direct-node branch (`IsDirectNodeCodexJs`); expect `An_already_normalized_request_is_not_prefixed_twice` red at `effective.ShouldBe(request)` or `Args.Count.ShouldBe(original)`. Removing the already-normalized check does not double-prefix: control falls through and returns the request unchanged.
- PC-21: break G-21 by dropping the transcript-format/Herdr-kind test so the policy applies to any exe named `codex.cmd`; expect `A_claude_request_whose_exe_ends_in_codex_cmd_is_untouched` red at `effective.Exe.ShouldBe(original)`.
- PC-22: break G-22 by consulting PATH before the sibling; expect `Sibling_node_wins_over_PATH_node` red at the resolved-path `ShouldBe`.
- PC-23: break G-23 by measuring with `exe.Length + args.Sum(a => a.Length + 3)`; expect `Quote_expansion_is_counted_not_estimated` red at `Should.Throw`.

### Out of scope

- Herdr lane budget boundaries interactively: PowerShell native-argument passing performs the quoting in that lane, so the runner's CRT count is a containment estimate; D-3's reserve plus V-5 is the evidence. Measuring PowerShell's own quoting is a separate probe.
- Inbox backend at the near-cap size: the inbox lane pre-escapes with the same `EscapeArgument` and passes verbatim, so the serialized line is identical to modern; V-4 at 8,366 proves the lane.
- arm64 package layout live: covered by the glob in the synthetic layout (`codex-win32-*`); no arm64 machine here.
- Fresh authenticated dispatch acceptance (gate 5): caller-owned; see the manual step below.
- Why ordinary exit handling missed the incident rows, post-spawn output forwarding, supervisor retry classification of the new codes (`RestartFailurePolicy` returns `Unknown` for a `ConflictException`; unchanged by plan), `--profile`/TOML alternatives: all deferred by the plan.

### Manual step handed back to the orchestrator

Worker sessions cannot dispatch (Create is 403). After Code/Mutation land and the runner and server are redeployed from the canonical checkout, the orchestrator runs gate 5: fresh Codex TestDesign dispatches at Frontier and Low plus a fresh named Codex agent with > 8,191 composed characters, dedicated validation cwd/home, authorized credentials; prove a new child (exclude pool reuse) whose `Win32_Process` name is `node.exe` with `codex.js` on its command line, the boot `UserPrompt` row, an assistant reply and settled report, and record IDs, tier, bundle stamps, CLI version, launcher, deployed SHA. Review must not mark this card done on delegate evidence alone.

### Cost

Filters (all with `--property:OutputPath=bin-c0497/`; `dotnet run --project tests/<X> -- --treenode-filter ...`). Minutes are estimated on this machine; the two process-spawning assemblies run sequentially.

| Item | Suite and filter | Minutes (estimated) |
|---|---|---|
| Setup/build | three test projects, isolated output | 8 |
| Unit floor | `Antiphon.SessionRunner.Tests` `/*/*/(CodexWindowsLaunchPolicyTests*)\|(CodexLaunchRefusalTests*)/*`; `Antiphon.Agents.Pty.Tests` `/*/*/(CommandLineLengthTests*)\|(LaunchArgvGuardTests*)\|(ModernConPtyCommandLineTests*)/*`; `Antiphon.Tests` `/*/*/(InstructionBundleTests*)\|(CodexLaunchArgsTests*)/*` | 2 |
| Integration floor | `Antiphon.Tests` `/*/*/(CodexDelegateDispatchTests*)\|(NamedCodexAgentLaunchTests*)\|(PinnedCodexProfileDispatchLaunchTests*)\|(DelegateLaunchArgvIntegrityTests*)\|(AgentSessionLaunchFailureTests*)\|(SessionReconciliationServiceTests*)\|(SessionRunnerHttpClientHerdrWireTests*)\|(AgentTaskDeadSessionReconciliationTests*)\|(AgentTaskLivenessTests*)/*`; `Antiphon.SessionRunner.Tests` `/*/*/(HerdrLaunchShapeTests*)\|(HerdrPaneChildKillTests*)\|(HerdrAdoptionSweepTests*)\|(CodexCommandLengthHttpAcceptanceTests*)/*` | 14 |
| Interactive floor (opt-in) | `ANTIPHON_REAL_CLI_STUB_TESTS=1`; `Antiphon.Tests` `/*/*/CodexCommandLengthSessionTests/*` (V-1..V-4, R-1) then `/*/*/CodexHerdrRealCliStubProxyCanaryTests/*` (V-5, may skip on stall) | 8 |
| Ordinary V/R floor (Code) | sum of the three rows above | 24 |
| PC floor (Mutation) | 22 hermetic PCs x (mutate, incremental build, method run, restore, method run) ≈ 1.8 each = 40; PC-19 red/restore/green interactive = 5 | 45 |
| Total verification floor | setup/build + V/R + every PC red/restore/green | 77 |

Savings: method-scoped PC cycles against `AgentSessionLaunchFailureTests` and `SessionReconciliationServiceTests` cost about 20 s each instead of the 3-4 minute class runs, roughly 40 minutes saved over the PC set; the named-class Integration filter replaces the 2,349-case Application namespace run (about 26 minutes) on every V/R pass. Mutation may shard the 23 PCs across two worktrees off the task branch (PC-19 and the Herdr rows stay serial with the process-spawn lane).

Bodies read; boundaries mapped; guards=23, mapped=23, missing=0, duplicate PC mappings=0; all PCs are compiling defects with a named red assertion; cost is numeric and labelled estimated.

Next stage: **code**.
